// aspire.ts - Capability-based Aspire SDK
// This SDK uses the ATS (Aspire Type System) capability API.
// Capabilities are endpoints like 'Aspire.Hosting/createBuilder'.
//
// GENERATED CODE - DO NOT EDIT
import { AspireClient, AppHostUsageError, CancellationToken, CapabilityError, registerCallback, wrapIfHandle, registerHandleWrapper, isPromiseLike } from './transport.js';
import { ResourceBuilderBase, AspireDict, AspireList } from './base.js';
export { InputType, InteractionInputCollection } from './base.js';
// ============================================================================
// Enum Types
// ============================================================================
/** Defines the scope of custom certificate authorities for a resource. The default scope for most resources is `Append`, but some resources may choose to override this default behavior. */
export var CertificateTrustScope;
(function (CertificateTrustScope) {
    /** Disable all custom certificate authority configuration for a resource. This indicates that the resource should use its default certificate authority trust behavior without modification. */
    CertificateTrustScope["None"] = "None";
    /** Append the specified certificate authorities to the default set of trusted CAs for a resource. Not all resources support this mode, in which case custom certificate authorities may not be applied. In that case, consider using `Override` or `System` instead. This is the default mode unless otherwise specified. */
    CertificateTrustScope["Append"] = "Append";
    /** Replace the default set of trusted CAs for a resource with the specified certificate authorities. This mode indicates that only the provided custom certificate authorities should be considered trusted by the resource. */
    CertificateTrustScope["Override"] = "Override";
    /** Attempt to configure the resource to trust the default system certificate authorities in addition to any configured custom certificate trust. This mode is useful for resources that don't otherwise allow appending to their default trusted certificate authorities but do allow overriding the set of trusted certificates (e.g. Python, Rust, etc.). */
    CertificateTrustScope["System"] = "System";
})(CertificateTrustScope || (CertificateTrustScope = {}));
/** Specifies the format of a command result. */
export var CommandResultFormat;
(function (CommandResultFormat) {
    /** Plain text result. */
    CommandResultFormat["Text"] = "Text";
    /** JSON result. */
    CommandResultFormat["Json"] = "Json";
    /** Markdown result. */
    CommandResultFormat["Markdown"] = "Markdown";
})(CommandResultFormat || (CommandResultFormat = {}));
/** Lifetime modes for container resources. */
export var ContainerLifetime;
(function (ContainerLifetime) {
    /** Create the resource when the app host process starts and dispose of it when the app host process shuts down. */
    ContainerLifetime["Session"] = "Session";
    /**
     * Attempt to re-use a previously created resource (based on the container name) if one exists. Do not destroy the container on app host process shutdown.
     *
     * In the event that a container with the given name does not exist, a new container will always be created based on the
     * current `ContainerResource` configuration.
     * When an existing container IS found, Aspire MAY re-use it based on the following criteria:
     * -
     * -
     * -
     */
    ContainerLifetime["Persistent"] = "Persistent";
})(ContainerLifetime || (ContainerLifetime = {}));
/** Represents the type of a container mount. */
export var ContainerMountType;
(function (ContainerMountType) {
    /** A local directory or file that is mounted into the container. */
    ContainerMountType["BindMount"] = "BindMount";
    /** A volume. */
    ContainerMountType["Volume"] = "Volume";
})(ContainerMountType || (ContainerMountType = {}));
/** Describes the context in which the AppHost is being executed. */
export var DistributedApplicationOperation;
(function (DistributedApplicationOperation) {
    /** AppHost is being run for the purpose of debugging locally. */
    DistributedApplicationOperation["Run"] = "Run";
    /** AppHost is being run for the purpose of publishing a manifest for deployment. */
    DistributedApplicationOperation["Publish"] = "Publish";
})(DistributedApplicationOperation || (DistributedApplicationOperation = {}));
/** Represents the properties of an endpoint that can be referenced. */
export var EndpointProperty;
(function (EndpointProperty) {
    /** The entire URL of the endpoint. */
    EndpointProperty["Url"] = "Url";
    /** The host of the endpoint. */
    EndpointProperty["Host"] = "Host";
    /** The IPv4 address of the endpoint. */
    EndpointProperty["IPV4Host"] = "IPV4Host";
    /** The port of the endpoint. */
    EndpointProperty["Port"] = "Port";
    /** The scheme of the endpoint. */
    EndpointProperty["Scheme"] = "Scheme";
    /** The target port of the endpoint. */
    EndpointProperty["TargetPort"] = "TargetPort";
    /** The host and port of the endpoint in the format `{Host}:{Port}`. */
    EndpointProperty["HostAndPort"] = "HostAndPort";
    /** Whether TLS is enabled on the endpoint. Returns `TrueString` or `FalseString`. */
    EndpointProperty["TlsEnabled"] = "TlsEnabled";
})(EndpointProperty || (EndpointProperty = {}));
/** Enum type for HealthStatus */
export var HealthStatus;
(function (HealthStatus) {
    HealthStatus["Unhealthy"] = "Unhealthy";
    HealthStatus["Degraded"] = "Degraded";
    HealthStatus["Healthy"] = "Healthy";
})(HealthStatus || (HealthStatus = {}));
/** Specifies how an HTTP command should surface the HTTP response body as command result data. */
export var HttpCommandResultMode;
(function (HttpCommandResultMode) {
    /** Do not capture the HTTP response body as command result data. */
    HttpCommandResultMode["None"] = "None";
    /** Infer the command result format from the HTTP response content type. */
    HttpCommandResultMode["Auto"] = "Auto";
    /** Return the HTTP response body as JSON command result data. */
    HttpCommandResultMode["Json"] = "Json";
    /** Return the HTTP response body as plain text command result data. */
    HttpCommandResultMode["Text"] = "Text";
})(HttpCommandResultMode || (HttpCommandResultMode = {}));
/** The icon variant. */
export var IconVariant;
(function (IconVariant) {
    /** Regular variant of icons. */
    IconVariant["Regular"] = "Regular";
    /** Filled variant of icons. */
    IconVariant["Filled"] = "Filled";
})(IconVariant || (IconVariant = {}));
/** Image pull policies for container resources. */
export var ImagePullPolicy;
(function (ImagePullPolicy) {
    /** Default image pull policy behavior. Currently this will be the same as the default behavior for your container runtime. */
    ImagePullPolicy["Default"] = "Default";
    /** Always pull the image when creating the container. */
    ImagePullPolicy["Always"] = "Always";
    /** Pull the image only if it does not already exist. */
    ImagePullPolicy["Missing"] = "Missing";
    /** Never pull the image from the registry even if it is missing locally. */
    ImagePullPolicy["Never"] = "Never";
})(ImagePullPolicy || (ImagePullPolicy = {}));
/** Protocols available for OTLP exporters. */
export var OtlpProtocol;
(function (OtlpProtocol) {
    /** A gRPC-based OTLP exporter. */
    OtlpProtocol["Grpc"] = "Grpc";
    /** Http/Protobuf-based OTLP exporter. */
    OtlpProtocol["HttpProtobuf"] = "HttpProtobuf";
    /** Http/JSON-based OTLP exporter. */
    OtlpProtocol["HttpJson"] = "HttpJson";
})(OtlpProtocol || (OtlpProtocol = {}));
/** Enum representing the type of probe. */
export var ProbeType;
(function (ProbeType) {
    /** Startup probe. */
    ProbeType["Startup"] = "Startup";
    /** Readiness probe. */
    ProbeType["Readiness"] = "Readiness";
    /** Liveness probe. */
    ProbeType["Liveness"] = "Liveness";
})(ProbeType || (ProbeType = {}));
/** Enum type for ProtocolType */
export var ProtocolType;
(function (ProtocolType) {
    ProtocolType["IP"] = "IP";
    ProtocolType["IPv6HopByHopOptions"] = "IPv6HopByHopOptions";
    ProtocolType["Unspecified"] = "Unspecified";
    ProtocolType["Icmp"] = "Icmp";
    ProtocolType["Igmp"] = "Igmp";
    ProtocolType["Ggp"] = "Ggp";
    ProtocolType["IPv4"] = "IPv4";
    ProtocolType["Tcp"] = "Tcp";
    ProtocolType["Pup"] = "Pup";
    ProtocolType["Udp"] = "Udp";
    ProtocolType["Idp"] = "Idp";
    ProtocolType["IPv6"] = "IPv6";
    ProtocolType["IPv6RoutingHeader"] = "IPv6RoutingHeader";
    ProtocolType["IPv6FragmentHeader"] = "IPv6FragmentHeader";
    ProtocolType["IPSecEncapsulatingSecurityPayload"] = "IPSecEncapsulatingSecurityPayload";
    ProtocolType["IPSecAuthenticationHeader"] = "IPSecAuthenticationHeader";
    ProtocolType["IcmpV6"] = "IcmpV6";
    ProtocolType["IPv6NoNextHeader"] = "IPv6NoNextHeader";
    ProtocolType["IPv6DestinationOptions"] = "IPv6DestinationOptions";
    ProtocolType["ND"] = "ND";
    ProtocolType["Raw"] = "Raw";
    ProtocolType["Ipx"] = "Ipx";
    ProtocolType["Spx"] = "Spx";
    ProtocolType["SpxII"] = "SpxII";
    ProtocolType["Unknown"] = "Unknown";
})(ProtocolType || (ProtocolType = {}));
/** The state of a resource command. */
export var ResourceCommandState;
(function (ResourceCommandState) {
    /** Command is visible and enabled for use. */
    ResourceCommandState["Enabled"] = "Enabled";
    /** Command is visible and disabled for use. */
    ResourceCommandState["Disabled"] = "Disabled";
    /** Command is hidden. */
    ResourceCommandState["Hidden"] = "Hidden";
})(ResourceCommandState || (ResourceCommandState = {}));
/** Describes where a resource command is visible. */
export var ResourceCommandVisibility;
(function (ResourceCommandVisibility) {
    /** The command is not visible to any clients. */
    ResourceCommandVisibility["None"] = "None";
    /** The command is displayed in UI clients. */
    ResourceCommandVisibility["UI"] = "UI";
    /** The command is exposed through resource command API discovery. */
    ResourceCommandVisibility["Api"] = "Api";
})(ResourceCommandVisibility || (ResourceCommandVisibility = {}));
/** Specifies where the URL should be displayed. */
export var UrlDisplayLocation;
(function (UrlDisplayLocation) {
    /** Show the URL in locations where either the resource summary or resource details are being displayed. */
    UrlDisplayLocation["SummaryAndDetails"] = "SummaryAndDetails";
    /** Show the URL in locations where the full details of the resource are being displayed. */
    UrlDisplayLocation["DetailsOnly"] = "DetailsOnly";
})(UrlDisplayLocation || (UrlDisplayLocation = {}));
/** Specifies the behavior of the wait. */
export var WaitBehavior;
(function (WaitBehavior) {
    /** If the resource is unavailable, continue waiting. */
    WaitBehavior["WaitOnResourceUnavailable"] = "WaitOnResourceUnavailable";
    /** If the resource is unavailable, stop waiting. */
    WaitBehavior["StopOnResourceUnavailable"] = "StopOnResourceUnavailable";
})(WaitBehavior || (WaitBehavior = {}));
// ============================================================================
// Exported Values
// ============================================================================
export var WellKnownPipelineSteps;
(function (WellKnownPipelineSteps) {
    /** The step that runs before the application starts. */
    WellKnownPipelineSteps.BeforeStart = "before-start";
    /** The well-known step for building resources. */
    WellKnownPipelineSteps.Build = "build";
    /** The prerequisite step that runs before any build operations. */
    WellKnownPipelineSteps.BuildPrereq = "build-prereq";
    /** The step that checks whether the container runtime (e.g., Docker or Podman) is running. Build steps that need a container runtime should depend on this step. */
    WellKnownPipelineSteps.CheckContainerRuntime = "check-container-runtime";
    /** Aggregation step for all deploy operations. All deploy steps should be required by this step. */
    WellKnownPipelineSteps.Deploy = "deploy";
    /** The prerequisite step that runs before any deploy operations. */
    WellKnownPipelineSteps.DeployPrereq = "deploy-prereq";
    /** Aggregation step for all destroy operations. All destroy steps should be required by this step. */
    WellKnownPipelineSteps.Destroy = "destroy";
    /** The prerequisite step that runs before any destroy operations. */
    WellKnownPipelineSteps.DestroyPrereq = "destroy-prereq";
    /** The diagnostic step that dumps dependency graph information for troubleshooting. */
    WellKnownPipelineSteps.Diagnostics = "diagnostics";
    /** The step that prompts for parameter values before build, publish, or deployment operations. */
    WellKnownPipelineSteps.ProcessParameters = "process-parameters";
    /** Aggregation step for all publish operations. All publish steps should be required by this step. */
    WellKnownPipelineSteps.Publish = "publish";
    /** The prerequisite step that runs before any publish operations. */
    WellKnownPipelineSteps.PublishPrereq = "publish-prereq";
    /** The meta-step that coordinates all push operations. All push steps should be required by this step. */
    WellKnownPipelineSteps.Push = "push";
    /** The prerequisite step that runs before any push operations. */
    WellKnownPipelineSteps.PushPrereq = "push-prereq";
    /** The step that validates compute resources are assigned to unambiguous compute environments. */
    WellKnownPipelineSteps.ValidateComputeEnvironments = "validate-compute-environments";
})(WellKnownPipelineSteps || (WellKnownPipelineSteps = {}));
export var WellKnownPipelineTags;
(function (WellKnownPipelineTags) {
    /** Tag for steps that build compute resources. */
    WellKnownPipelineTags.BuildCompute = "build-compute";
    /** Tag for steps that deploy to compute infrastructure. */
    WellKnownPipelineTags.DeployCompute = "deploy-compute";
    /** Tag for steps that provision infrastructure resources. */
    WellKnownPipelineTags.ProvisionInfrastructure = "provision-infra";
    /** Tag for steps that push container images to a registry. */
    WellKnownPipelineTags.PushContainerImage = "push-container-image";
})(WellKnownPipelineTags || (WellKnownPipelineTags = {}));
// ============================================================================
// AfterPublishEventImpl
// ============================================================================
/** This event is published after the distributed application is published. */
class AfterPublishEventImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    services() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.Publishing/AfterPublishEvent.services', { context: this._handle });
            return new ServiceProviderImpl(handle, this._client);
        })();
        return new ServiceProviderPromiseImpl(promise, this._client, false);
    }
    model() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.Publishing/AfterPublishEvent.model', { context: this._handle });
            return new DistributedApplicationModelImpl(handle, this._client);
        })();
        return new DistributedApplicationModelPromiseImpl(promise, this._client, false);
    }
}
/**
 * Thenable wrapper for AfterPublishEvent that enables fluent chaining.
 */
class AfterPublishEventPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    services() {
        return new ServiceProviderPromiseImpl(this._promise.then(obj => obj.services()), this._client, false);
    }
    model() {
        return new DistributedApplicationModelPromiseImpl(this._promise.then(obj => obj.model()), this._client, false);
    }
}
// ============================================================================
// AfterResourcesCreatedEventImpl
// ============================================================================
/**
 * This event is published after all resources have been created.
 *
 * Subscribing to this event is analogous to implementing the `AfterResourcesCreatedAsync`
 * method. This event provides access to the `IServiceProvider` interface to resolve dependencies including
 * `DistributedApplicationModel` service which is passed in as an argument
 * in `AfterResourcesCreatedAsync`.
 */
class AfterResourcesCreatedEventImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    services() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/AfterResourcesCreatedEvent.services', { context: this._handle });
            return new ServiceProviderImpl(handle, this._client);
        })();
        return new ServiceProviderPromiseImpl(promise, this._client, false);
    }
    model() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/AfterResourcesCreatedEvent.model', { context: this._handle });
            return new DistributedApplicationModelImpl(handle, this._client);
        })();
        return new DistributedApplicationModelPromiseImpl(promise, this._client, false);
    }
}
/**
 * Thenable wrapper for AfterResourcesCreatedEvent that enables fluent chaining.
 */
class AfterResourcesCreatedEventPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    services() {
        return new ServiceProviderPromiseImpl(this._promise.then(obj => obj.services()), this._client, false);
    }
    model() {
        return new DistributedApplicationModelPromiseImpl(this._promise.then(obj => obj.model()), this._client, false);
    }
}
// ============================================================================
// BeforePublishEventImpl
// ============================================================================
/** This event is published before the distributed application is published. */
class BeforePublishEventImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    services() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.Publishing/BeforePublishEvent.services', { context: this._handle });
            return new ServiceProviderImpl(handle, this._client);
        })();
        return new ServiceProviderPromiseImpl(promise, this._client, false);
    }
    model() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.Publishing/BeforePublishEvent.model', { context: this._handle });
            return new DistributedApplicationModelImpl(handle, this._client);
        })();
        return new DistributedApplicationModelPromiseImpl(promise, this._client, false);
    }
}
/**
 * Thenable wrapper for BeforePublishEvent that enables fluent chaining.
 */
class BeforePublishEventPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    services() {
        return new ServiceProviderPromiseImpl(this._promise.then(obj => obj.services()), this._client, false);
    }
    model() {
        return new DistributedApplicationModelPromiseImpl(this._promise.then(obj => obj.model()), this._client, false);
    }
}
// ============================================================================
// BeforeResourceStartedEventImpl
// ============================================================================
/**
 * This event is raised by orchestrators before they have started a new resource.
 *
 * Resources that are created by orchestrators may not yet be ready to handle requests.
 */
class BeforeResourceStartedEventImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    resource() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/BeforeResourceStartedEvent.resource', { context: this._handle });
            return new ResourceImpl(handle, this._client);
        })();
        return new ResourcePromiseImpl(promise, this._client, false);
    }
    services() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/BeforeResourceStartedEvent.services', { context: this._handle });
            return new ServiceProviderImpl(handle, this._client);
        })();
        return new ServiceProviderPromiseImpl(promise, this._client, false);
    }
}
/**
 * Thenable wrapper for BeforeResourceStartedEvent that enables fluent chaining.
 */
class BeforeResourceStartedEventPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    resource() {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.resource()), this._client, false);
    }
    services() {
        return new ServiceProviderPromiseImpl(this._promise.then(obj => obj.services()), this._client, false);
    }
}
// ============================================================================
// BeforeStartEventImpl
// ============================================================================
/**
 * This event is published before the application starts.
 *
 * Subscribing to this event is analogous to implementing the `BeforeStartAsync`
 * method. This event provides access to the `IServiceProvider` interface to resolve dependencies including
 * `DistributedApplicationModel` service which is passed in as an argument
 * in `BeforeStartAsync`.
 */
class BeforeStartEventImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    services() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/BeforeStartEvent.services', { context: this._handle });
            return new ServiceProviderImpl(handle, this._client);
        })();
        return new ServiceProviderPromiseImpl(promise, this._client, false);
    }
    model() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/BeforeStartEvent.model', { context: this._handle });
            return new DistributedApplicationModelImpl(handle, this._client);
        })();
        return new DistributedApplicationModelPromiseImpl(promise, this._client, false);
    }
}
/**
 * Thenable wrapper for BeforeStartEvent that enables fluent chaining.
 */
class BeforeStartEventPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    services() {
        return new ServiceProviderPromiseImpl(this._promise.then(obj => obj.services()), this._client, false);
    }
    model() {
        return new DistributedApplicationModelPromiseImpl(this._promise.then(obj => obj.model()), this._client, false);
    }
}
// ============================================================================
// CommandLineArgsCallbackContextImpl
// ============================================================================
/** Represents a callback context for the list of command-line arguments associated with an executable resource. */
class CommandLineArgsCallbackContextImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    args() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/CommandLineArgsCallbackContext.args', { context: this._handle });
            return new CommandLineArgsEditorImpl(handle, this._client);
        })();
        return new CommandLineArgsEditorPromiseImpl(promise, this._client, false);
    }
    log() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/CommandLineArgsCallbackContext.log', { context: this._handle });
            return new LogFacadeImpl(handle, this._client);
        })();
        return new LogFacadePromiseImpl(promise, this._client, false);
    }
    resource() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/CommandLineArgsCallbackContext.resource', { context: this._handle });
            return new ResourceImpl(handle, this._client);
        })();
        return new ResourcePromiseImpl(promise, this._client, false);
    }
    executionContext() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/CommandLineArgsCallbackContext.executionContext', { context: this._handle });
            return new DistributedApplicationExecutionContextImpl(handle, this._client);
        })();
        return new DistributedApplicationExecutionContextPromiseImpl(promise, this._client, false);
    }
}
/**
 * Thenable wrapper for CommandLineArgsCallbackContext that enables fluent chaining.
 */
class CommandLineArgsCallbackContextPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    args() {
        return new CommandLineArgsEditorPromiseImpl(this._promise.then(obj => obj.args()), this._client, false);
    }
    log() {
        return new LogFacadePromiseImpl(this._promise.then(obj => obj.log()), this._client, false);
    }
    resource() {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.resource()), this._client, false);
    }
    executionContext() {
        return new DistributedApplicationExecutionContextPromiseImpl(this._promise.then(obj => obj.executionContext()), this._client, false);
    }
}
// ============================================================================
// CommandLineArgsEditorImpl
// ============================================================================
/** Provides an ATS-first editor for command-line arguments within polyglot callbacks. */
class CommandLineArgsEditorImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    /** @internal */
    async _addInternal(value) {
        value = isPromiseLike(value) ? await value : value;
        const rpcArgs = { context: this._handle, value };
        await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/add', rpcArgs);
        return this;
    }
    /**
     * Adds a command-line argument.
     * @param value The argument to add.
     */
    add(value) {
        return new CommandLineArgsEditorPromiseImpl(this._addInternal(value), this._client);
    }
}
/**
 * Thenable wrapper for CommandLineArgsEditor that enables fluent chaining.
 */
class CommandLineArgsEditorPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    add(value) {
        return new CommandLineArgsEditorPromiseImpl(this._promise.then(obj => obj.add(value)), this._client);
    }
}
// ============================================================================
// ConnectionStringAvailableEventImpl
// ============================================================================
/** The {@link ConnectionStringAvailableEvent} is raised when a connection string becomes available for a resource. */
class ConnectionStringAvailableEventImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    resource() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/ConnectionStringAvailableEvent.resource', { context: this._handle });
            return new ResourceImpl(handle, this._client);
        })();
        return new ResourcePromiseImpl(promise, this._client, false);
    }
    services() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/ConnectionStringAvailableEvent.services', { context: this._handle });
            return new ServiceProviderImpl(handle, this._client);
        })();
        return new ServiceProviderPromiseImpl(promise, this._client, false);
    }
}
/**
 * Thenable wrapper for ConnectionStringAvailableEvent that enables fluent chaining.
 */
class ConnectionStringAvailableEventPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    resource() {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.resource()), this._client, false);
    }
    services() {
        return new ServiceProviderPromiseImpl(this._promise.then(obj => obj.services()), this._client, false);
    }
}
// ============================================================================
// ContainerImagePushOptionsImpl
// ============================================================================
/**
 * Represents options for pushing container images to a registry.
 *
 * This class allows customization of how container images are named and tagged when pushed to a container registry.
 * The `RemoteImageName` specifies the repository path (without registry endpoint or tag),
 * and `RemoteImageTag` specifies the tag to apply. Use `GetFullRemoteImageNameAsync`
 * to construct the complete image reference including registry endpoint and tag.
 */
class ContainerImagePushOptionsImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    remoteImageName = {
        get: async () => {
            return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/ContainerImagePushOptions.remoteImageName', { context: this._handle });
        },
        set: async (value) => {
            await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/ContainerImagePushOptions.setRemoteImageName', { context: this._handle, value });
        }
    };
    remoteImageTag = {
        get: async () => {
            return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/ContainerImagePushOptions.remoteImageTag', { context: this._handle });
        },
        set: async (value) => {
            await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/ContainerImagePushOptions.setRemoteImageTag', { context: this._handle, value });
        }
    };
}
// ============================================================================
// ContainerImagePushOptionsCallbackContextImpl
// ============================================================================
/** Provides context information for container image push options callbacks. */
class ContainerImagePushOptionsCallbackContextImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    resource() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/ContainerImagePushOptionsCallbackContext.resource', { context: this._handle });
            return new ResourceImpl(handle, this._client);
        })();
        return new ResourcePromiseImpl(promise, this._client, false);
    }
    async cancellationToken() {
        const result = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/ContainerImagePushOptionsCallbackContext.cancellationToken', { context: this._handle });
        return CancellationToken.fromValue(result);
    }
    async options() {
        const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/ContainerImagePushOptionsCallbackContext.options', { context: this._handle });
        return new ContainerImagePushOptionsImpl(handle, this._client);
    }
}
/**
 * Thenable wrapper for ContainerImagePushOptionsCallbackContext that enables fluent chaining.
 */
class ContainerImagePushOptionsCallbackContextPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    resource() {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.resource()), this._client, false);
    }
    cancellationToken() {
        return this._promise.then(obj => obj.cancellationToken());
    }
    options() {
        return this._promise.then(obj => obj.options());
    }
}
// ============================================================================
// ContainerImageReferenceImpl
// ============================================================================
/** Represents the fully‑qualified container image reference that should be deployed. */
class ContainerImageReferenceImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    resource() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/ContainerImageReference.resource', { context: this._handle });
            return new ResourceImpl(handle, this._client);
        })();
        return new ResourcePromiseImpl(promise, this._client, false);
    }
    async valueExpression() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/ContainerImageReference.valueExpression', { context: this._handle });
    }
}
/**
 * Thenable wrapper for ContainerImageReference that enables fluent chaining.
 */
class ContainerImageReferencePromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    resource() {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.resource()), this._client, false);
    }
    valueExpression() {
        return this._promise.then(obj => obj.valueExpression());
    }
}
// ============================================================================
// ContainerMountAnnotationImpl
// ============================================================================
/** Represents a mount annotation for a container resource. */
class ContainerMountAnnotationImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    async source() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/ContainerMountAnnotation.source', { context: this._handle });
    }
    async target() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/ContainerMountAnnotation.target', { context: this._handle });
    }
    async type() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/ContainerMountAnnotation.type', { context: this._handle });
    }
    async isReadOnly() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/ContainerMountAnnotation.isReadOnly', { context: this._handle });
    }
}
/**
 * Thenable wrapper for ContainerMountAnnotation that enables fluent chaining.
 */
class ContainerMountAnnotationPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    source() {
        return this._promise.then(obj => obj.source());
    }
    target() {
        return this._promise.then(obj => obj.target());
    }
    type() {
        return this._promise.then(obj => obj.type());
    }
    isReadOnly() {
        return this._promise.then(obj => obj.isReadOnly());
    }
}
// ============================================================================
// ContainerPortReferenceImpl
// ============================================================================
/** Represents a TCP/UDP port that a container can expose. */
class ContainerPortReferenceImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    resource() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/ContainerPortReference.resource', { context: this._handle });
            return new ResourceImpl(handle, this._client);
        })();
        return new ResourcePromiseImpl(promise, this._client, false);
    }
    async valueExpression() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/ContainerPortReference.valueExpression', { context: this._handle });
    }
}
/**
 * Thenable wrapper for ContainerPortReference that enables fluent chaining.
 */
class ContainerPortReferencePromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    resource() {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.resource()), this._client, false);
    }
    valueExpression() {
        return this._promise.then(obj => obj.valueExpression());
    }
}
// ============================================================================
// DistributedApplicationImpl
// ============================================================================
/** Represents a distributed application that implements the {@link IHost} and {@link IAsyncDisposable} interfaces. */
class DistributedApplicationImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    /** @internal */
    async _runInternal(cancellationToken) {
        const rpcArgs = { context: this._handle };
        if (cancellationToken !== undefined)
            rpcArgs.cancellationToken = CancellationToken.fromValue(cancellationToken);
        await this._client.invokeCapability('Aspire.Hosting/run', rpcArgs);
        return this;
    }
    /**
     * Runs the distributed application
     *
     * When the Aspire app host is launched via `RunAsync` there are
     * two possible modes that it is running in:
     * -
     * -
     * Developers extending the Aspire application model should consider the lifetime
     * of `IHostedService` instances which are added to the dependency injection
     * container. For more information on determining the mode that the app host is running
     * in refer to `DistributedApplicationExecutionContext`.
     * @param options Additional options.
     */
    run(options) {
        const cancellationToken = options?.cancellationToken;
        return new DistributedApplicationPromiseImpl(this._runInternal(cancellationToken), this._client);
    }
}
/**
 * Thenable wrapper for DistributedApplication that enables fluent chaining.
 */
class DistributedApplicationPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    run(options) {
        return new DistributedApplicationPromiseImpl(this._promise.then(obj => obj.run(options)), this._client);
    }
}
// ============================================================================
// DistributedApplicationExecutionContextImpl
// ============================================================================
/** Exposes the global contextual information for this invocation of the AppHost. */
class DistributedApplicationExecutionContextImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    publisherName = {
        get: async () => {
            return await this._client.invokeCapability('Aspire.Hosting/DistributedApplicationExecutionContext.publisherName', { context: this._handle });
        },
        set: async (value) => {
            await this._client.invokeCapability('Aspire.Hosting/DistributedApplicationExecutionContext.setPublisherName', { context: this._handle, value });
        }
    };
    async operation() {
        return await this._client.invokeCapability('Aspire.Hosting/DistributedApplicationExecutionContext.operation', { context: this._handle });
    }
    serviceProvider() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting/DistributedApplicationExecutionContext.serviceProvider', { context: this._handle });
            return new ServiceProviderImpl(handle, this._client);
        })();
        return new ServiceProviderPromiseImpl(promise, this._client, false);
    }
    async isPublishMode() {
        return await this._client.invokeCapability('Aspire.Hosting/DistributedApplicationExecutionContext.isPublishMode', { context: this._handle });
    }
    async isRunMode() {
        return await this._client.invokeCapability('Aspire.Hosting/DistributedApplicationExecutionContext.isRunMode', { context: this._handle });
    }
}
/**
 * Thenable wrapper for DistributedApplicationExecutionContext that enables fluent chaining.
 */
class DistributedApplicationExecutionContextPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    operation() {
        return this._promise.then(obj => obj.operation());
    }
    serviceProvider() {
        return new ServiceProviderPromiseImpl(this._promise.then(obj => obj.serviceProvider()), this._client, false);
    }
    isPublishMode() {
        return this._promise.then(obj => obj.isPublishMode());
    }
    isRunMode() {
        return this._promise.then(obj => obj.isRunMode());
    }
}
// ============================================================================
// DistributedApplicationModelImpl
// ============================================================================
/** Represents a distributed application. */
class DistributedApplicationModelImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    /**
     * Gets all resources in the distributed application model.
     * @returns The resources in the model.
     */
    async getResources() {
        const rpcArgs = { model: this._handle };
        return await this._client.invokeCapability('Aspire.Hosting/getResources', rpcArgs);
    }
    /** @internal */
    async _findResourceByNameInternal(name) {
        const rpcArgs = { model: this._handle, name };
        const result = await this._client.invokeCapability('Aspire.Hosting/findResourceByName', rpcArgs);
        return new ResourceImpl(result, this._client);
    }
    /**
     * Finds a resource by name.
     * @param name The resource name.
     * @returns The matching resource, or `null` when not found.
     */
    findResourceByName(name) {
        return new ResourcePromiseImpl(this._findResourceByNameInternal(name), this._client);
    }
}
/**
 * Thenable wrapper for DistributedApplicationModel that enables fluent chaining.
 */
class DistributedApplicationModelPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    getResources() {
        return this._promise.then(obj => obj.getResources());
    }
    findResourceByName(name) {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.findResourceByName(name)), this._client);
    }
}
// ============================================================================
// DockerfileBuilderImpl
// ============================================================================
/** Builder for creating Dockerfiles programmatically. */
class DockerfileBuilderImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    /** @internal */
    async _argInternal(name, defaultValue) {
        const rpcArgs = { builder: this._handle, name };
        if (defaultValue !== undefined)
            rpcArgs.defaultValue = defaultValue;
        const result = await this._client.invokeCapability('Aspire.Hosting/dockerfileBuilderArg', rpcArgs);
        return new DockerfileBuilderImpl(result, this._client);
    }
    /**
     * Adds a global ARG statement to the Dockerfile
     * @param options Additional options.
     */
    arg(name, options) {
        const defaultValue = options?.defaultValue;
        return new DockerfileBuilderPromiseImpl(this._argInternal(name, defaultValue), this._client);
    }
    /** @internal */
    async _fromInternal(image, stageName) {
        const rpcArgs = { builder: this._handle, image };
        if (stageName !== undefined)
            rpcArgs.stageName = stageName;
        const result = await this._client.invokeCapability('Aspire.Hosting/dockerfileBuilderFrom', rpcArgs);
        return new DockerfileStageImpl(result, this._client);
    }
    /**
     * Adds a FROM statement to start a Dockerfile stage
     * @param options Additional options.
     */
    from(image, options) {
        const stageName = options?.stageName;
        return new DockerfileStagePromiseImpl(this._fromInternal(image, stageName), this._client);
    }
    /** @internal */
    async _addContainerFilesStagesInternal(resource, logger) {
        resource = isPromiseLike(resource) ? await resource : resource;
        logger = isPromiseLike(logger) ? await logger : logger;
        const rpcArgs = { builder: this._handle, resource };
        if (logger !== undefined)
            rpcArgs.logger = logger;
        const result = await this._client.invokeCapability('Aspire.Hosting/dockerfileBuilderAddContainerFilesStages', rpcArgs);
        return new DockerfileBuilderImpl(result, this._client);
    }
    /**
     * Adds Dockerfile stages for published container files
     * @param options Additional options.
     */
    addContainerFilesStages(resource, options) {
        let logger = options?.logger;
        return new DockerfileBuilderPromiseImpl(this._addContainerFilesStagesInternal(resource, logger), this._client);
    }
}
/**
 * Thenable wrapper for DockerfileBuilder that enables fluent chaining.
 */
class DockerfileBuilderPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    arg(name, options) {
        return new DockerfileBuilderPromiseImpl(this._promise.then(obj => obj.arg(name, options)), this._client);
    }
    from(image, options) {
        return new DockerfileStagePromiseImpl(this._promise.then(obj => obj.from(image, options)), this._client);
    }
    addContainerFilesStages(resource, options) {
        return new DockerfileBuilderPromiseImpl(this._promise.then(obj => obj.addContainerFilesStages(resource, options)), this._client);
    }
}
// ============================================================================
// DockerfileBuilderCallbackContextImpl
// ============================================================================
/** Provides context information for Dockerfile build callbacks. */
class DockerfileBuilderCallbackContextImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    resource() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/DockerfileBuilderCallbackContext.resource', { context: this._handle });
            return new ResourceImpl(handle, this._client);
        })();
        return new ResourcePromiseImpl(promise, this._client, false);
    }
    builder() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/DockerfileBuilderCallbackContext.builder', { context: this._handle });
            return new DockerfileBuilderImpl(handle, this._client);
        })();
        return new DockerfileBuilderPromiseImpl(promise, this._client, false);
    }
    services() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/DockerfileBuilderCallbackContext.services', { context: this._handle });
            return new ServiceProviderImpl(handle, this._client);
        })();
        return new ServiceProviderPromiseImpl(promise, this._client, false);
    }
    async cancellationToken() {
        const result = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/DockerfileBuilderCallbackContext.cancellationToken', { context: this._handle });
        return CancellationToken.fromValue(result);
    }
}
/**
 * Thenable wrapper for DockerfileBuilderCallbackContext that enables fluent chaining.
 */
class DockerfileBuilderCallbackContextPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    resource() {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.resource()), this._client, false);
    }
    builder() {
        return new DockerfileBuilderPromiseImpl(this._promise.then(obj => obj.builder()), this._client, false);
    }
    services() {
        return new ServiceProviderPromiseImpl(this._promise.then(obj => obj.services()), this._client, false);
    }
    cancellationToken() {
        return this._promise.then(obj => obj.cancellationToken());
    }
}
// ============================================================================
// DockerfileFactoryContextImpl
// ============================================================================
/** Provides context for Dockerfile factory functions. */
class DockerfileFactoryContextImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    resource() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/DockerfileFactoryContext.resource', { context: this._handle });
            return new ResourceImpl(handle, this._client);
        })();
        return new ResourcePromiseImpl(promise, this._client, false);
    }
}
/**
 * Thenable wrapper for DockerfileFactoryContext that enables fluent chaining.
 */
class DockerfileFactoryContextPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    resource() {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.resource()), this._client, false);
    }
}
// ============================================================================
// DockerfileStageImpl
// ============================================================================
/** Represents a stage within a multi-stage Dockerfile. */
class DockerfileStageImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    /** @internal */
    async _argInternal(name, defaultValue) {
        const rpcArgs = { stage: this._handle, name };
        if (defaultValue !== undefined)
            rpcArgs.defaultValue = defaultValue;
        const result = await this._client.invokeCapability('Aspire.Hosting/dockerfileStageArg', rpcArgs);
        return new DockerfileStageImpl(result, this._client);
    }
    /**
     * Adds an ARG statement to a Dockerfile stage
     * @param options Additional options.
     */
    arg(name, options) {
        const defaultValue = options?.defaultValue;
        return new DockerfileStagePromiseImpl(this._argInternal(name, defaultValue), this._client);
    }
    /** @internal */
    async _workDirInternal(path) {
        const rpcArgs = { stage: this._handle, path };
        const result = await this._client.invokeCapability('Aspire.Hosting/workDir', rpcArgs);
        return new DockerfileStageImpl(result, this._client);
    }
    /** Adds a WORKDIR statement to a Dockerfile stage */
    workDir(path) {
        return new DockerfileStagePromiseImpl(this._workDirInternal(path), this._client);
    }
    /** @internal */
    async _runInternal(command) {
        const rpcArgs = { stage: this._handle, command };
        const result = await this._client.invokeCapability('Aspire.Hosting/dockerfileStageRun', rpcArgs);
        return new DockerfileStageImpl(result, this._client);
    }
    /** Adds a RUN statement to a Dockerfile stage */
    run(command) {
        return new DockerfileStagePromiseImpl(this._runInternal(command), this._client);
    }
    /** @internal */
    async _copyInternal(source, destination, chown) {
        const rpcArgs = { stage: this._handle, source, destination };
        if (chown !== undefined)
            rpcArgs.chown = chown;
        const result = await this._client.invokeCapability('Aspire.Hosting/dockerfileStageCopy', rpcArgs);
        return new DockerfileStageImpl(result, this._client);
    }
    /**
     * Adds a COPY statement to a Dockerfile stage
     * @param options Additional options.
     */
    copy(source, destination, options) {
        const chown = options?.chown;
        return new DockerfileStagePromiseImpl(this._copyInternal(source, destination, chown), this._client);
    }
    /** @internal */
    async _copyFromInternal(from, source, destination, chown) {
        const rpcArgs = { stage: this._handle, from, source, destination };
        if (chown !== undefined)
            rpcArgs.chown = chown;
        const result = await this._client.invokeCapability('Aspire.Hosting/dockerfileStageCopyFrom', rpcArgs);
        return new DockerfileStageImpl(result, this._client);
    }
    /**
     * Adds a COPY --from statement to a Dockerfile stage
     * @param options Additional options.
     */
    copyFrom(from, source, destination, options) {
        const chown = options?.chown;
        return new DockerfileStagePromiseImpl(this._copyFromInternal(from, source, destination, chown), this._client);
    }
    /** @internal */
    async _envInternal(name, value) {
        const rpcArgs = { stage: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/env', rpcArgs);
        return new DockerfileStageImpl(result, this._client);
    }
    /** Adds an ENV statement to a Dockerfile stage */
    env(name, value) {
        return new DockerfileStagePromiseImpl(this._envInternal(name, value), this._client);
    }
    /** @internal */
    async _exposeInternal(port) {
        const rpcArgs = { stage: this._handle, port };
        const result = await this._client.invokeCapability('Aspire.Hosting/expose', rpcArgs);
        return new DockerfileStageImpl(result, this._client);
    }
    /** Adds an EXPOSE statement to a Dockerfile stage */
    expose(port) {
        return new DockerfileStagePromiseImpl(this._exposeInternal(port), this._client);
    }
    /** @internal */
    async _cmdInternal(command) {
        const rpcArgs = { stage: this._handle, command };
        const result = await this._client.invokeCapability('Aspire.Hosting/cmd', rpcArgs);
        return new DockerfileStageImpl(result, this._client);
    }
    /** Adds a CMD statement to a Dockerfile stage */
    cmd(command) {
        return new DockerfileStagePromiseImpl(this._cmdInternal(command), this._client);
    }
    /** @internal */
    async _entrypointInternal(command) {
        const rpcArgs = { stage: this._handle, command };
        const result = await this._client.invokeCapability('Aspire.Hosting/entrypoint', rpcArgs);
        return new DockerfileStageImpl(result, this._client);
    }
    /** Adds an ENTRYPOINT statement to a Dockerfile stage */
    entrypoint(command) {
        return new DockerfileStagePromiseImpl(this._entrypointInternal(command), this._client);
    }
    /** @internal */
    async _runWithMountsInternal(command, mounts) {
        const rpcArgs = { stage: this._handle, command, mounts };
        const result = await this._client.invokeCapability('Aspire.Hosting/runWithMounts', rpcArgs);
        return new DockerfileStageImpl(result, this._client);
    }
    /** Adds a RUN statement with mounts to a Dockerfile stage */
    runWithMounts(command, mounts) {
        return new DockerfileStagePromiseImpl(this._runWithMountsInternal(command, mounts), this._client);
    }
    /** @internal */
    async _userInternal(user) {
        const rpcArgs = { stage: this._handle, user };
        const result = await this._client.invokeCapability('Aspire.Hosting/user', rpcArgs);
        return new DockerfileStageImpl(result, this._client);
    }
    /** Adds a USER statement to a Dockerfile stage */
    user(user) {
        return new DockerfileStagePromiseImpl(this._userInternal(user), this._client);
    }
    /** @internal */
    async _emptyLineInternal() {
        const rpcArgs = { stage: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/emptyLine', rpcArgs);
        return new DockerfileStageImpl(result, this._client);
    }
    /** Adds an empty line to a Dockerfile stage */
    emptyLine() {
        return new DockerfileStagePromiseImpl(this._emptyLineInternal(), this._client);
    }
    /** @internal */
    async _commentInternal(comment) {
        const rpcArgs = { stage: this._handle, comment };
        const result = await this._client.invokeCapability('Aspire.Hosting/comment', rpcArgs);
        return new DockerfileStageImpl(result, this._client);
    }
    /** Adds a comment to a Dockerfile stage */
    comment(comment) {
        return new DockerfileStagePromiseImpl(this._commentInternal(comment), this._client);
    }
    /** @internal */
    async _addContainerFilesInternal(resource, rootDestinationPath, logger) {
        resource = isPromiseLike(resource) ? await resource : resource;
        logger = isPromiseLike(logger) ? await logger : logger;
        const rpcArgs = { stage: this._handle, resource, rootDestinationPath };
        if (logger !== undefined)
            rpcArgs.logger = logger;
        const result = await this._client.invokeCapability('Aspire.Hosting/dockerfileStageAddContainerFiles', rpcArgs);
        return new DockerfileStageImpl(result, this._client);
    }
    /**
     * Adds COPY --from statements for published container files
     * @param options Additional options.
     */
    addContainerFiles(resource, rootDestinationPath, options) {
        let logger = options?.logger;
        return new DockerfileStagePromiseImpl(this._addContainerFilesInternal(resource, rootDestinationPath, logger), this._client);
    }
}
/**
 * Thenable wrapper for DockerfileStage that enables fluent chaining.
 */
class DockerfileStagePromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    arg(name, options) {
        return new DockerfileStagePromiseImpl(this._promise.then(obj => obj.arg(name, options)), this._client);
    }
    workDir(path) {
        return new DockerfileStagePromiseImpl(this._promise.then(obj => obj.workDir(path)), this._client);
    }
    run(command) {
        return new DockerfileStagePromiseImpl(this._promise.then(obj => obj.run(command)), this._client);
    }
    copy(source, destination, options) {
        return new DockerfileStagePromiseImpl(this._promise.then(obj => obj.copy(source, destination, options)), this._client);
    }
    copyFrom(from, source, destination, options) {
        return new DockerfileStagePromiseImpl(this._promise.then(obj => obj.copyFrom(from, source, destination, options)), this._client);
    }
    env(name, value) {
        return new DockerfileStagePromiseImpl(this._promise.then(obj => obj.env(name, value)), this._client);
    }
    expose(port) {
        return new DockerfileStagePromiseImpl(this._promise.then(obj => obj.expose(port)), this._client);
    }
    cmd(command) {
        return new DockerfileStagePromiseImpl(this._promise.then(obj => obj.cmd(command)), this._client);
    }
    entrypoint(command) {
        return new DockerfileStagePromiseImpl(this._promise.then(obj => obj.entrypoint(command)), this._client);
    }
    runWithMounts(command, mounts) {
        return new DockerfileStagePromiseImpl(this._promise.then(obj => obj.runWithMounts(command, mounts)), this._client);
    }
    user(user) {
        return new DockerfileStagePromiseImpl(this._promise.then(obj => obj.user(user)), this._client);
    }
    emptyLine() {
        return new DockerfileStagePromiseImpl(this._promise.then(obj => obj.emptyLine()), this._client);
    }
    comment(comment) {
        return new DockerfileStagePromiseImpl(this._promise.then(obj => obj.comment(comment)), this._client);
    }
    addContainerFiles(resource, rootDestinationPath, options) {
        return new DockerfileStagePromiseImpl(this._promise.then(obj => obj.addContainerFiles(resource, rootDestinationPath, options)), this._client);
    }
}
// ============================================================================
// EndpointReferenceImpl
// ============================================================================
/** Represents an endpoint reference for a resource with endpoints. */
class EndpointReferenceImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    resource() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointReference.resource', { context: this._handle });
            return new ResourceWithEndpointsImpl(handle, this._client);
        })();
        return new ResourceWithEndpointsPromiseImpl(promise, this._client, false);
    }
    async endpointName() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointReference.endpointName', { context: this._handle });
    }
    async errorMessage() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointReference.errorMessage', { context: this._handle });
    }
    async isAllocated() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointReference.isAllocated', { context: this._handle });
    }
    async exists() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointReference.exists', { context: this._handle });
    }
    async isHttp() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointReference.isHttp', { context: this._handle });
    }
    async isHttps() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointReference.isHttps', { context: this._handle });
    }
    async tlsEnabled() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointReference.tlsEnabled', { context: this._handle });
    }
    async isHttpSchemeNamedEndpoint() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointReference.isHttpSchemeNamedEndpoint', { context: this._handle });
    }
    async excludeReferenceEndpoint() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointReference.excludeReferenceEndpoint', { context: this._handle });
    }
    async port() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointReference.port', { context: this._handle });
    }
    async targetPort() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointReference.targetPort', { context: this._handle });
    }
    async host() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointReference.host', { context: this._handle });
    }
    async scheme() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointReference.scheme', { context: this._handle });
    }
    async url() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointReference.url', { context: this._handle });
    }
    /**
     * Gets the URL of the endpoint asynchronously. Waits for the endpoint to be allocated if necessary.
     * @param options Additional options.
     * @returns The URL of the endpoint.
     */
    async getValueAsync(options) {
        const cancellationToken = options?.cancellationToken;
        const rpcArgs = { context: this._handle };
        if (cancellationToken !== undefined)
            rpcArgs.cancellationToken = CancellationToken.fromValue(cancellationToken);
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointReference.getValueAsync', rpcArgs);
    }
    /** @internal */
    async _propertyInternal(property) {
        const rpcArgs = { context: this._handle, property };
        const result = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointReference.property', rpcArgs);
        return new EndpointReferenceExpressionImpl(result, this._client);
    }
    /**
     * Gets the specified property expression of the endpoint.
     * @param property The `EndpointProperty` enum value to use in the reference.
     * @returns An `EndpointReferenceExpression` representing the specified `EndpointProperty`.
     */
    property(property) {
        return new EndpointReferenceExpressionPromiseImpl(this._propertyInternal(property), this._client);
    }
    /**
     * Gets a conditional expression that resolves to the enabledValue when TLS is enabled on the endpoint, or to the disabledValue otherwise.
     *
     * The returned expression evaluates the TLS state lazily each time its value is resolved, making it
     * safe to embed in a `ReferenceExpression` that is built before TLS is configured
     * (e.g., before `BeforeStartEvent` fires). Because the condition and branches are declarative,
     * polyglot code generators can translate this into native conditional constructs in any target language.
     * @param enabledValue The expression to evaluate when TLS is enabled (e.g., `",ssl=true"`).
     * @param disabledValue The expression to evaluate when TLS is not enabled.
     * @returns A conditional `ReferenceExpression` whose value tracks the TLS state of this endpoint.
     */
    async getTlsValue(enabledValue, disabledValue) {
        const rpcArgs = { context: this._handle, enabledValue, disabledValue };
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointReference.getTlsValue', rpcArgs);
    }
}
/**
 * Thenable wrapper for EndpointReference that enables fluent chaining.
 */
class EndpointReferencePromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    resource() {
        return new ResourceWithEndpointsPromiseImpl(this._promise.then(obj => obj.resource()), this._client, false);
    }
    endpointName() {
        return this._promise.then(obj => obj.endpointName());
    }
    errorMessage() {
        return this._promise.then(obj => obj.errorMessage());
    }
    isAllocated() {
        return this._promise.then(obj => obj.isAllocated());
    }
    exists() {
        return this._promise.then(obj => obj.exists());
    }
    isHttp() {
        return this._promise.then(obj => obj.isHttp());
    }
    isHttps() {
        return this._promise.then(obj => obj.isHttps());
    }
    tlsEnabled() {
        return this._promise.then(obj => obj.tlsEnabled());
    }
    isHttpSchemeNamedEndpoint() {
        return this._promise.then(obj => obj.isHttpSchemeNamedEndpoint());
    }
    excludeReferenceEndpoint() {
        return this._promise.then(obj => obj.excludeReferenceEndpoint());
    }
    port() {
        return this._promise.then(obj => obj.port());
    }
    targetPort() {
        return this._promise.then(obj => obj.targetPort());
    }
    host() {
        return this._promise.then(obj => obj.host());
    }
    scheme() {
        return this._promise.then(obj => obj.scheme());
    }
    url() {
        return this._promise.then(obj => obj.url());
    }
    getValueAsync(options) {
        return this._promise.then(obj => obj.getValueAsync(options));
    }
    property(property) {
        return new EndpointReferenceExpressionPromiseImpl(this._promise.then(obj => obj.property(property)), this._client);
    }
    getTlsValue(enabledValue, disabledValue) {
        return this._promise.then(obj => obj.getTlsValue(enabledValue, disabledValue));
    }
}
// ============================================================================
// EndpointReferenceExpressionImpl
// ============================================================================
/** Represents a property expression for an endpoint reference. */
class EndpointReferenceExpressionImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    endpoint() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointReferenceExpression.endpoint', { context: this._handle });
            return new EndpointReferenceImpl(handle, this._client);
        })();
        return new EndpointReferencePromiseImpl(promise, this._client, false);
    }
    async property() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointReferenceExpression.property', { context: this._handle });
    }
    async valueExpression() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointReferenceExpression.valueExpression', { context: this._handle });
    }
}
/**
 * Thenable wrapper for EndpointReferenceExpression that enables fluent chaining.
 */
class EndpointReferenceExpressionPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    endpoint() {
        return new EndpointReferencePromiseImpl(this._promise.then(obj => obj.endpoint()), this._client, false);
    }
    property() {
        return this._promise.then(obj => obj.property());
    }
    valueExpression() {
        return this._promise.then(obj => obj.valueExpression());
    }
}
// ============================================================================
// EndpointUpdateContextImpl
// ============================================================================
/** Provides a mutable callback context for updating an endpoint in polyglot app hosts. */
class EndpointUpdateContextImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    async name() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointUpdateContext.name', { context: this._handle });
    }
    protocol = {
        get: async () => {
            return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointUpdateContext.protocol', { context: this._handle });
        },
        set: async (value) => {
            await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointUpdateContext.setProtocol', { context: this._handle, value });
        }
    };
    port = {
        get: async () => {
            return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointUpdateContext.port', { context: this._handle });
        },
        set: async (value) => {
            await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointUpdateContext.setPort', { context: this._handle, value });
        }
    };
    targetPort = {
        get: async () => {
            return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointUpdateContext.targetPort', { context: this._handle });
        },
        set: async (value) => {
            await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointUpdateContext.setTargetPort', { context: this._handle, value });
        }
    };
    uriScheme = {
        get: async () => {
            return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointUpdateContext.uriScheme', { context: this._handle });
        },
        set: async (value) => {
            await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointUpdateContext.setUriScheme', { context: this._handle, value });
        }
    };
    targetHost = {
        get: async () => {
            return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointUpdateContext.targetHost', { context: this._handle });
        },
        set: async (value) => {
            await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointUpdateContext.setTargetHost', { context: this._handle, value });
        }
    };
    transport = {
        get: async () => {
            return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointUpdateContext.transport', { context: this._handle });
        },
        set: async (value) => {
            await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointUpdateContext.setTransport', { context: this._handle, value });
        }
    };
    isExternal = {
        get: async () => {
            return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointUpdateContext.isExternal', { context: this._handle });
        },
        set: async (value) => {
            await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointUpdateContext.setIsExternal', { context: this._handle, value });
        }
    };
    isProxied = {
        get: async () => {
            return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointUpdateContext.isProxied', { context: this._handle });
        },
        set: async (value) => {
            await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointUpdateContext.setIsProxied', { context: this._handle, value });
        }
    };
    excludeReferenceEndpoint = {
        get: async () => {
            return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointUpdateContext.excludeReferenceEndpoint', { context: this._handle });
        },
        set: async (value) => {
            await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointUpdateContext.setExcludeReferenceEndpoint', { context: this._handle, value });
        }
    };
    tlsEnabled = {
        get: async () => {
            return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointUpdateContext.tlsEnabled', { context: this._handle });
        },
        set: async (value) => {
            await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EndpointUpdateContext.setTlsEnabled', { context: this._handle, value });
        }
    };
}
/**
 * Thenable wrapper for EndpointUpdateContext that enables fluent chaining.
 */
class EndpointUpdateContextPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    name() {
        return this._promise.then(obj => obj.name());
    }
}
// ============================================================================
// EnvironmentCallbackContextImpl
// ============================================================================
/** Represents a callback context for environment variables associated with a publisher. */
class EnvironmentCallbackContextImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    environment() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EnvironmentCallbackContext.environment', { context: this._handle });
            return new EnvironmentEditorImpl(handle, this._client);
        })();
        return new EnvironmentEditorPromiseImpl(promise, this._client, false);
    }
    log() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EnvironmentCallbackContext.log', { context: this._handle });
            return new LogFacadeImpl(handle, this._client);
        })();
        return new LogFacadePromiseImpl(promise, this._client, false);
    }
    resource() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EnvironmentCallbackContext.resource', { context: this._handle });
            return new ResourceImpl(handle, this._client);
        })();
        return new ResourcePromiseImpl(promise, this._client, false);
    }
    executionContext() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/EnvironmentCallbackContext.executionContext', { context: this._handle });
            return new DistributedApplicationExecutionContextImpl(handle, this._client);
        })();
        return new DistributedApplicationExecutionContextPromiseImpl(promise, this._client, false);
    }
}
/**
 * Thenable wrapper for EnvironmentCallbackContext that enables fluent chaining.
 */
class EnvironmentCallbackContextPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    environment() {
        return new EnvironmentEditorPromiseImpl(this._promise.then(obj => obj.environment()), this._client, false);
    }
    log() {
        return new LogFacadePromiseImpl(this._promise.then(obj => obj.log()), this._client, false);
    }
    resource() {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.resource()), this._client, false);
    }
    executionContext() {
        return new DistributedApplicationExecutionContextPromiseImpl(this._promise.then(obj => obj.executionContext()), this._client, false);
    }
}
// ============================================================================
// EnvironmentEditorImpl
// ============================================================================
/** Provides an ATS-first editor for environment variables within polyglot callbacks. */
class EnvironmentEditorImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    /** @internal */
    async _setInternal(name, value) {
        value = isPromiseLike(value) ? await value : value;
        const rpcArgs = { context: this._handle, name, value };
        await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/set', rpcArgs);
        return this;
    }
    /**
     * Sets an environment variable.
     * @param name The name of the environment variable.
     * @param value The value to assign to the environment variable.
     */
    set(name, value) {
        return new EnvironmentEditorPromiseImpl(this._setInternal(name, value), this._client);
    }
}
/**
 * Thenable wrapper for EnvironmentEditor that enables fluent chaining.
 */
class EnvironmentEditorPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    set(name, value) {
        return new EnvironmentEditorPromiseImpl(this._promise.then(obj => obj.set(name, value)), this._client);
    }
}
// ============================================================================
// EventingSubscriberRegistrationContextImpl
// ============================================================================
/** Context passed to ATS-friendly eventing subscriber registrations. */
class EventingSubscriberRegistrationContextImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    executionContext() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.Ats/EventingSubscriberRegistrationContext.executionContext', { context: this._handle });
            return new DistributedApplicationExecutionContextImpl(handle, this._client);
        })();
        return new DistributedApplicationExecutionContextPromiseImpl(promise, this._client, false);
    }
    async cancellationToken() {
        const result = await this._client.invokeCapability('Aspire.Hosting.Ats/EventingSubscriberRegistrationContext.cancellationToken', { context: this._handle });
        return CancellationToken.fromValue(result);
    }
    /**
     * Subscribes to the BeforeStart event from an eventing subscriber registration context.
     * @param callback The callback to invoke when the event fires.
     * @returns The event subscription.
     */
    async onBeforeStart(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new BeforeStartEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { context: this._handle, callback: callbackId };
        return await this._client.invokeCapability('Aspire.Hosting/eventingSubscriberOnBeforeStart', rpcArgs);
    }
    /**
     * Subscribes to the BeforePublish event from an eventing subscriber registration context.
     * @param callback The callback to invoke when the event fires.
     * @returns The event subscription.
     */
    async onBeforePublish(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new BeforePublishEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { context: this._handle, callback: callbackId };
        return await this._client.invokeCapability('Aspire.Hosting/eventingSubscriberOnBeforePublish', rpcArgs);
    }
    /**
     * Subscribes to the AfterPublish event from an eventing subscriber registration context.
     * @param callback The callback to invoke when the event fires.
     * @returns The event subscription.
     */
    async onAfterPublish(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new AfterPublishEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { context: this._handle, callback: callbackId };
        return await this._client.invokeCapability('Aspire.Hosting/eventingSubscriberOnAfterPublish', rpcArgs);
    }
    /**
     * Subscribes to the AfterResourcesCreated event from an eventing subscriber registration context.
     * @param callback The callback to invoke when the event fires.
     * @returns The event subscription.
     */
    async onAfterResourcesCreated(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new AfterResourcesCreatedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { context: this._handle, callback: callbackId };
        return await this._client.invokeCapability('Aspire.Hosting/eventingSubscriberOnAfterResourcesCreated', rpcArgs);
    }
}
/**
 * Thenable wrapper for EventingSubscriberRegistrationContext that enables fluent chaining.
 */
class EventingSubscriberRegistrationContextPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    executionContext() {
        return new DistributedApplicationExecutionContextPromiseImpl(this._promise.then(obj => obj.executionContext()), this._client, false);
    }
    cancellationToken() {
        return this._promise.then(obj => obj.cancellationToken());
    }
    onBeforeStart(callback) {
        return this._promise.then(obj => obj.onBeforeStart(callback));
    }
    onBeforePublish(callback) {
        return this._promise.then(obj => obj.onBeforePublish(callback));
    }
    onAfterPublish(callback) {
        return this._promise.then(obj => obj.onAfterPublish(callback));
    }
    onAfterResourcesCreated(callback) {
        return this._promise.then(obj => obj.onAfterResourcesCreated(callback));
    }
}
// ============================================================================
// ExecuteCommandContextImpl
// ============================================================================
/** Context for {@link ResourceCommandAnnotation.ExecuteCommand}. */
class ExecuteCommandContextImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    async resourceName() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/ExecuteCommandContext.resourceName', { context: this._handle });
    }
    async cancellationToken() {
        const result = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/ExecuteCommandContext.cancellationToken', { context: this._handle });
        return CancellationToken.fromValue(result);
    }
    logger() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/ExecuteCommandContext.logger', { context: this._handle });
            return new LoggerImpl(handle, this._client);
        })();
        return new LoggerPromiseImpl(promise, this._client, false);
    }
    async arguments() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/ExecuteCommandContext.arguments', { context: this._handle });
    }
}
/**
 * Thenable wrapper for ExecuteCommandContext that enables fluent chaining.
 */
class ExecuteCommandContextPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    resourceName() {
        return this._promise.then(obj => obj.resourceName());
    }
    cancellationToken() {
        return this._promise.then(obj => obj.cancellationToken());
    }
    logger() {
        return new LoggerPromiseImpl(this._promise.then(obj => obj.logger()), this._client, false);
    }
    arguments() {
        return this._promise.then(obj => obj.arguments());
    }
}
// ============================================================================
// InitializeResourceEventImpl
// ============================================================================
/**
 * This event is raised by orchestrators to signal to resources that they should initialize themselves.
 *
 * Custom resources can subscribe to this event to perform initialization tasks, including starting background tasks
 * that manage the resource's lifecycle.
 */
class InitializeResourceEventImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    resource() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/InitializeResourceEvent.resource', { context: this._handle });
            return new ResourceImpl(handle, this._client);
        })();
        return new ResourcePromiseImpl(promise, this._client, false);
    }
    eventing() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/InitializeResourceEvent.eventing', { context: this._handle });
            return new DistributedApplicationEventingImpl(handle, this._client);
        })();
        return new DistributedApplicationEventingPromiseImpl(promise, this._client, false);
    }
    logger() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/InitializeResourceEvent.logger', { context: this._handle });
            return new LoggerImpl(handle, this._client);
        })();
        return new LoggerPromiseImpl(promise, this._client, false);
    }
    notifications() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/InitializeResourceEvent.notifications', { context: this._handle });
            return new ResourceNotificationServiceImpl(handle, this._client);
        })();
        return new ResourceNotificationServicePromiseImpl(promise, this._client, false);
    }
    services() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/InitializeResourceEvent.services', { context: this._handle });
            return new ServiceProviderImpl(handle, this._client);
        })();
        return new ServiceProviderPromiseImpl(promise, this._client, false);
    }
}
/**
 * Thenable wrapper for InitializeResourceEvent that enables fluent chaining.
 */
class InitializeResourceEventPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    resource() {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.resource()), this._client, false);
    }
    eventing() {
        return new DistributedApplicationEventingPromiseImpl(this._promise.then(obj => obj.eventing()), this._client, false);
    }
    logger() {
        return new LoggerPromiseImpl(this._promise.then(obj => obj.logger()), this._client, false);
    }
    notifications() {
        return new ResourceNotificationServicePromiseImpl(this._promise.then(obj => obj.notifications()), this._client, false);
    }
    services() {
        return new ServiceProviderPromiseImpl(this._promise.then(obj => obj.services()), this._client, false);
    }
}
// ============================================================================
// InputsDialogValidationContextImpl
// ============================================================================
/** Represents the context for validating inputs in an inputs dialog interaction. */
class InputsDialogValidationContextImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    async inputs() {
        return await this._client.invokeCapability('Aspire.Hosting/InputsDialogValidationContext.inputs', { context: this._handle });
    }
    async cancellationToken() {
        const result = await this._client.invokeCapability('Aspire.Hosting/InputsDialogValidationContext.cancellationToken', { context: this._handle });
        return CancellationToken.fromValue(result);
    }
    /** @internal */
    async _addValidationErrorInternal(inputName, errorMessage) {
        const rpcArgs = { context: this._handle, inputName, errorMessage };
        await this._client.invokeCapability('Aspire.Hosting/InputsDialogValidationContext.addValidationError', rpcArgs);
        return this;
    }
    /**
     * Adds a validation error for the input with the specified name.
     * @param inputName The name of the input to add a validation error for.
     * @param errorMessage The error message to add.
     */
    addValidationError(inputName, errorMessage) {
        return new InputsDialogValidationContextPromiseImpl(this._addValidationErrorInternal(inputName, errorMessage), this._client);
    }
}
/**
 * Thenable wrapper for InputsDialogValidationContext that enables fluent chaining.
 */
class InputsDialogValidationContextPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    inputs() {
        return this._promise.then(obj => obj.inputs());
    }
    cancellationToken() {
        return this._promise.then(obj => obj.cancellationToken());
    }
    addValidationError(inputName, errorMessage) {
        return new InputsDialogValidationContextPromiseImpl(this._promise.then(obj => obj.addValidationError(inputName, errorMessage)), this._client);
    }
}
// ============================================================================
// LogFacadeImpl
// ============================================================================
/** Provides a narrow logging surface for polyglot callback contexts. */
class LogFacadeImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    /** @internal */
    async _infoInternal(message) {
        const rpcArgs = { context: this._handle, message };
        await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/info', rpcArgs);
        return this;
    }
    /**
     * Writes an informational log message.
     * @param message The message to write.
     */
    info(message) {
        return new LogFacadePromiseImpl(this._infoInternal(message), this._client);
    }
    /** @internal */
    async _warningInternal(message) {
        const rpcArgs = { context: this._handle, message };
        await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/warning', rpcArgs);
        return this;
    }
    /**
     * Writes a warning log message.
     * @param message The message to write.
     */
    warning(message) {
        return new LogFacadePromiseImpl(this._warningInternal(message), this._client);
    }
    /** @internal */
    async _errorInternal(message) {
        const rpcArgs = { context: this._handle, message };
        await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/error', rpcArgs);
        return this;
    }
    /**
     * Writes an error log message.
     * @param message The message to write.
     */
    error(message) {
        return new LogFacadePromiseImpl(this._errorInternal(message), this._client);
    }
    /** @internal */
    async _debugInternal(message) {
        const rpcArgs = { context: this._handle, message };
        await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/debug', rpcArgs);
        return this;
    }
    /**
     * Writes a debug log message.
     * @param message The message to write.
     */
    debug(message) {
        return new LogFacadePromiseImpl(this._debugInternal(message), this._client);
    }
}
/**
 * Thenable wrapper for LogFacade that enables fluent chaining.
 */
class LogFacadePromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    info(message) {
        return new LogFacadePromiseImpl(this._promise.then(obj => obj.info(message)), this._client);
    }
    warning(message) {
        return new LogFacadePromiseImpl(this._promise.then(obj => obj.warning(message)), this._client);
    }
    error(message) {
        return new LogFacadePromiseImpl(this._promise.then(obj => obj.error(message)), this._client);
    }
    debug(message) {
        return new LogFacadePromiseImpl(this._promise.then(obj => obj.debug(message)), this._client);
    }
}
// ============================================================================
// PipelineConfigurationContextImpl
// ============================================================================
/** Provides contextual information for pipeline configuration callbacks. */
class PipelineConfigurationContextImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    pipeline() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.Pipelines/PipelineConfigurationContext.pipeline', { context: this._handle });
            return new PipelineEditorImpl(handle, this._client);
        })();
        return new PipelineEditorPromiseImpl(promise, this._client, false);
    }
    log() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.Pipelines/PipelineConfigurationContext.log', { context: this._handle });
            return new LogFacadeImpl(handle, this._client);
        })();
        return new LogFacadePromiseImpl(promise, this._client, false);
    }
    /**
     * Gets all pipeline steps with the specified tag.
     * @param tag The tag to search for.
     * @returns A collection of steps that have the specified tag.
     */
    async getSteps(tag) {
        const rpcArgs = { context: this._handle, tag };
        return await this._client.invokeCapability('Aspire.Hosting.Pipelines/getSteps', rpcArgs);
    }
}
/**
 * Thenable wrapper for PipelineConfigurationContext that enables fluent chaining.
 */
class PipelineConfigurationContextPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    pipeline() {
        return new PipelineEditorPromiseImpl(this._promise.then(obj => obj.pipeline()), this._client, false);
    }
    log() {
        return new LogFacadePromiseImpl(this._promise.then(obj => obj.log()), this._client, false);
    }
    getSteps(tag) {
        return this._promise.then(obj => obj.getSteps(tag));
    }
}
// ============================================================================
// PipelineContextImpl
// ============================================================================
/** Provides contextual information and services for the pipeline execution process of a distributed application. */
class PipelineContextImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    model() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.Pipelines/PipelineContext.model', { context: this._handle });
            return new DistributedApplicationModelImpl(handle, this._client);
        })();
        return new DistributedApplicationModelPromiseImpl(promise, this._client, false);
    }
    executionContext() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.Pipelines/PipelineContext.executionContext', { context: this._handle });
            return new DistributedApplicationExecutionContextImpl(handle, this._client);
        })();
        return new DistributedApplicationExecutionContextPromiseImpl(promise, this._client, false);
    }
    services() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.Pipelines/PipelineContext.services', { context: this._handle });
            return new ServiceProviderImpl(handle, this._client);
        })();
        return new ServiceProviderPromiseImpl(promise, this._client, false);
    }
    logger() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.Pipelines/PipelineContext.logger', { context: this._handle });
            return new LoggerImpl(handle, this._client);
        })();
        return new LoggerPromiseImpl(promise, this._client, false);
    }
    cancellationToken = {
        get: async () => {
            const result = await this._client.invokeCapability('Aspire.Hosting.Pipelines/PipelineContext.cancellationToken', { context: this._handle });
            return CancellationToken.fromValue(result);
        },
        set: async (value) => {
            await this._client.invokeCapability('Aspire.Hosting.Pipelines/PipelineContext.setCancellationToken', { context: this._handle, value: CancellationToken.fromValue(value) });
        }
    };
    summary() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.Pipelines/PipelineContext.summary', { context: this._handle });
            return new PipelineSummaryImpl(handle, this._client);
        })();
        return new PipelineSummaryPromiseImpl(promise, this._client, false);
    }
}
/**
 * Thenable wrapper for PipelineContext that enables fluent chaining.
 */
class PipelineContextPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    model() {
        return new DistributedApplicationModelPromiseImpl(this._promise.then(obj => obj.model()), this._client, false);
    }
    executionContext() {
        return new DistributedApplicationExecutionContextPromiseImpl(this._promise.then(obj => obj.executionContext()), this._client, false);
    }
    services() {
        return new ServiceProviderPromiseImpl(this._promise.then(obj => obj.services()), this._client, false);
    }
    logger() {
        return new LoggerPromiseImpl(this._promise.then(obj => obj.logger()), this._client, false);
    }
    summary() {
        return new PipelineSummaryPromiseImpl(this._promise.then(obj => obj.summary()), this._client, false);
    }
}
// ============================================================================
// PipelineEditorImpl
// ============================================================================
/** Provides an ATS-first editor for pipeline configuration callbacks. */
class PipelineEditorImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    /**
     * Gets all configured pipeline steps.
     * @returns The configured pipeline steps.
     */
    async steps() {
        const rpcArgs = { context: this._handle };
        return await this._client.invokeCapability('Aspire.Hosting.Pipelines/steps', rpcArgs);
    }
    /**
     * Gets all pipeline steps that have the specified tag.
     * @param tag The tag to search for.
     * @returns The matching pipeline steps.
     */
    async stepsByTag(tag) {
        const rpcArgs = { context: this._handle, tag };
        return await this._client.invokeCapability('Aspire.Hosting.Pipelines/stepsByTag', rpcArgs);
    }
}
/**
 * Thenable wrapper for PipelineEditor that enables fluent chaining.
 */
class PipelineEditorPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    steps() {
        return this._promise.then(obj => obj.steps());
    }
    stepsByTag(tag) {
        return this._promise.then(obj => obj.stepsByTag(tag));
    }
}
// ============================================================================
// PipelineStepImpl
// ============================================================================
/** Represents a step in the deployment pipeline. */
class PipelineStepImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    async name() {
        return await this._client.invokeCapability('Aspire.Hosting.Pipelines/PipelineStep.name', { context: this._handle });
    }
    async description() {
        return await this._client.invokeCapability('Aspire.Hosting.Pipelines/PipelineStep.description', { context: this._handle });
    }
    _dependsOnSteps;
    async dependsOnSteps() {
        if (!this._dependsOnSteps) {
            this._dependsOnSteps = new AspireList(this._handle, this._client, 'Aspire.Hosting.Pipelines/PipelineStep.dependsOnSteps', 'Aspire.Hosting.Pipelines/PipelineStep.dependsOnSteps');
        }
        return this._dependsOnSteps;
    }
    _requiredBySteps;
    async requiredBySteps() {
        if (!this._requiredBySteps) {
            this._requiredBySteps = new AspireList(this._handle, this._client, 'Aspire.Hosting.Pipelines/PipelineStep.requiredBySteps', 'Aspire.Hosting.Pipelines/PipelineStep.requiredBySteps');
        }
        return this._requiredBySteps;
    }
    _tags;
    async tags() {
        if (!this._tags) {
            this._tags = new AspireList(this._handle, this._client, 'Aspire.Hosting.Pipelines/PipelineStep.tags', 'Aspire.Hosting.Pipelines/PipelineStep.tags');
        }
        return this._tags;
    }
    /** @internal */
    async _dependsOnInternal(stepName) {
        const rpcArgs = { context: this._handle, stepName };
        await this._client.invokeCapability('Aspire.Hosting.Pipelines/dependsOn', rpcArgs);
        return this;
    }
    /**
     * Adds a dependency on another step.
     * @param stepName The name of the step to depend on.
     */
    dependsOn(stepName) {
        return new PipelineStepPromiseImpl(this._dependsOnInternal(stepName), this._client);
    }
    /** @internal */
    async _requiredByInternal(stepName) {
        const rpcArgs = { context: this._handle, stepName };
        await this._client.invokeCapability('Aspire.Hosting.Pipelines/requiredBy', rpcArgs);
        return this;
    }
    /**
     * Specifies that this step is required by another step. This creates the inverse relationship where the other step will depend on this step.
     * @param stepName The name of the step that requires this step.
     */
    requiredBy(stepName) {
        return new PipelineStepPromiseImpl(this._requiredByInternal(stepName), this._client);
    }
    /** @internal */
    async _addTagInternal(tag) {
        const rpcArgs = { context: this._handle, tag };
        await this._client.invokeCapability('Aspire.Hosting.Pipelines/addTag', rpcArgs);
        return this;
    }
    /**
     * Adds a tag to the step.
     * @param tag The tag to add.
     */
    addTag(tag) {
        return new PipelineStepPromiseImpl(this._addTagInternal(tag), this._client);
    }
}
/**
 * Thenable wrapper for PipelineStep that enables fluent chaining.
 */
class PipelineStepPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    name() {
        return this._promise.then(obj => obj.name());
    }
    description() {
        return this._promise.then(obj => obj.description());
    }
    dependsOnSteps() {
        return this._promise.then(obj => obj.dependsOnSteps());
    }
    requiredBySteps() {
        return this._promise.then(obj => obj.requiredBySteps());
    }
    tags() {
        return this._promise.then(obj => obj.tags());
    }
    dependsOn(stepName) {
        return new PipelineStepPromiseImpl(this._promise.then(obj => obj.dependsOn(stepName)), this._client);
    }
    requiredBy(stepName) {
        return new PipelineStepPromiseImpl(this._promise.then(obj => obj.requiredBy(stepName)), this._client);
    }
    addTag(tag) {
        return new PipelineStepPromiseImpl(this._promise.then(obj => obj.addTag(tag)), this._client);
    }
}
// ============================================================================
// PipelineStepContextImpl
// ============================================================================
/**
 * Provides contextual information for a specific pipeline step execution.
 *
 * This context combines the shared pipeline context with a step-specific publishing step,
 * allowing each step to track its own tasks and completion state independently.
 */
class PipelineStepContextImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    pipelineContext() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.Pipelines/PipelineStepContext.pipelineContext', { context: this._handle });
            return new PipelineContextImpl(handle, this._client);
        })();
        return new PipelineContextPromiseImpl(promise, this._client, false);
    }
    reportingStep() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.Pipelines/PipelineStepContext.reportingStep', { context: this._handle });
            return new ReportingStepImpl(handle, this._client);
        })();
        return new ReportingStepPromiseImpl(promise, this._client, false);
    }
    model() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.Pipelines/PipelineStepContext.model', { context: this._handle });
            return new DistributedApplicationModelImpl(handle, this._client);
        })();
        return new DistributedApplicationModelPromiseImpl(promise, this._client, false);
    }
    executionContext() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.Pipelines/PipelineStepContext.executionContext', { context: this._handle });
            return new DistributedApplicationExecutionContextImpl(handle, this._client);
        })();
        return new DistributedApplicationExecutionContextPromiseImpl(promise, this._client, false);
    }
    services() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.Pipelines/PipelineStepContext.services', { context: this._handle });
            return new ServiceProviderImpl(handle, this._client);
        })();
        return new ServiceProviderPromiseImpl(promise, this._client, false);
    }
    logger() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.Pipelines/PipelineStepContext.logger', { context: this._handle });
            return new LoggerImpl(handle, this._client);
        })();
        return new LoggerPromiseImpl(promise, this._client, false);
    }
    async cancellationToken() {
        const result = await this._client.invokeCapability('Aspire.Hosting.Pipelines/PipelineStepContext.cancellationToken', { context: this._handle });
        return CancellationToken.fromValue(result);
    }
    summary() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.Pipelines/PipelineStepContext.summary', { context: this._handle });
            return new PipelineSummaryImpl(handle, this._client);
        })();
        return new PipelineSummaryPromiseImpl(promise, this._client, false);
    }
}
/**
 * Thenable wrapper for PipelineStepContext that enables fluent chaining.
 */
class PipelineStepContextPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    pipelineContext() {
        return new PipelineContextPromiseImpl(this._promise.then(obj => obj.pipelineContext()), this._client, false);
    }
    reportingStep() {
        return new ReportingStepPromiseImpl(this._promise.then(obj => obj.reportingStep()), this._client, false);
    }
    model() {
        return new DistributedApplicationModelPromiseImpl(this._promise.then(obj => obj.model()), this._client, false);
    }
    executionContext() {
        return new DistributedApplicationExecutionContextPromiseImpl(this._promise.then(obj => obj.executionContext()), this._client, false);
    }
    services() {
        return new ServiceProviderPromiseImpl(this._promise.then(obj => obj.services()), this._client, false);
    }
    logger() {
        return new LoggerPromiseImpl(this._promise.then(obj => obj.logger()), this._client, false);
    }
    cancellationToken() {
        return this._promise.then(obj => obj.cancellationToken());
    }
    summary() {
        return new PipelineSummaryPromiseImpl(this._promise.then(obj => obj.summary()), this._client, false);
    }
}
// ============================================================================
// PipelineStepFactoryContextImpl
// ============================================================================
/** Provides contextual information for creating pipeline steps from a {@link PipelineStepAnnotation}. */
class PipelineStepFactoryContextImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    pipelineContext() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.Pipelines/PipelineStepFactoryContext.pipelineContext', { context: this._handle });
            return new PipelineContextImpl(handle, this._client);
        })();
        return new PipelineContextPromiseImpl(promise, this._client, false);
    }
    resource() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.Pipelines/PipelineStepFactoryContext.resource', { context: this._handle });
            return new ResourceImpl(handle, this._client);
        })();
        return new ResourcePromiseImpl(promise, this._client, false);
    }
}
/**
 * Thenable wrapper for PipelineStepFactoryContext that enables fluent chaining.
 */
class PipelineStepFactoryContextPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    pipelineContext() {
        return new PipelineContextPromiseImpl(this._promise.then(obj => obj.pipelineContext()), this._client, false);
    }
    resource() {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.resource()), this._client, false);
    }
}
// ============================================================================
// PipelineSummaryImpl
// ============================================================================
/**
 * Represents pipeline summary information to be displayed after pipeline completion. This is a general-purpose key-value collection that pipeline steps can contribute to.
 *
 * This class provides a flexible way for any pipeline step to contribute
 * information to be displayed after pipeline execution. The data is stored as
 * key-value pairs that will be formatted as a table or list in the CLI output.
 * Pipeline steps can add any relevant information such as resource group names,
 * subscription IDs, URLs, namespaces, cluster names, or any other details.
 * Values can be plain text or Markdown-formatted by using `MarkdownString`.
 * The summary is available via the `Summary`
 * property and can be accessed from any pipeline step.
 */
class PipelineSummaryImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    /** @internal */
    async _addInternal(key, value) {
        const rpcArgs = { context: this._handle, key, value };
        await this._client.invokeCapability('Aspire.Hosting.Pipelines/PipelineSummary.add', rpcArgs);
        return this;
    }
    /**
     * Adds a key-value pair to the pipeline summary with a plain-text value.
     * @param key The key or label for the item (e.g., "Namespace", "URL").
     * @param value The plain-text value for the item.
     */
    add(key, value) {
        return new PipelineSummaryPromiseImpl(this._addInternal(key, value), this._client);
    }
    /** @internal */
    async _addMarkdownInternal(key, markdownString) {
        const rpcArgs = { summary: this._handle, key, markdownString };
        await this._client.invokeCapability('Aspire.Hosting/addMarkdown', rpcArgs);
        return this;
    }
    /**
     * Adds a key-value pair to the pipeline summary with a Markdown-formatted value.
     * @param key The key or label for the item.
     * @param markdownString The Markdown-formatted value for the item.
     */
    addMarkdown(key, markdownString) {
        return new PipelineSummaryPromiseImpl(this._addMarkdownInternal(key, markdownString), this._client);
    }
}
/**
 * Thenable wrapper for PipelineSummary that enables fluent chaining.
 */
class PipelineSummaryPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    add(key, value) {
        return new PipelineSummaryPromiseImpl(this._promise.then(obj => obj.add(key, value)), this._client);
    }
    addMarkdown(key, markdownString) {
        return new PipelineSummaryPromiseImpl(this._promise.then(obj => obj.addMarkdown(key, markdownString)), this._client);
    }
}
// ============================================================================
// ProjectResourceOptionsImpl
// ============================================================================
/** Various properties to modify the behavior of the project resource. */
class ProjectResourceOptionsImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    launchProfileName = {
        get: async () => {
            return await this._client.invokeCapability('Aspire.Hosting/ProjectResourceOptions.launchProfileName', { context: this._handle });
        },
        set: async (value) => {
            await this._client.invokeCapability('Aspire.Hosting/ProjectResourceOptions.setLaunchProfileName', { context: this._handle, value });
        }
    };
    excludeLaunchProfile = {
        get: async () => {
            return await this._client.invokeCapability('Aspire.Hosting/ProjectResourceOptions.excludeLaunchProfile', { context: this._handle });
        },
        set: async (value) => {
            await this._client.invokeCapability('Aspire.Hosting/ProjectResourceOptions.setExcludeLaunchProfile', { context: this._handle, value });
        }
    };
    excludeKestrelEndpoints = {
        get: async () => {
            return await this._client.invokeCapability('Aspire.Hosting/ProjectResourceOptions.excludeKestrelEndpoints', { context: this._handle });
        },
        set: async (value) => {
            await this._client.invokeCapability('Aspire.Hosting/ProjectResourceOptions.setExcludeKestrelEndpoints', { context: this._handle, value });
        }
    };
}
// ============================================================================
// ReferenceExpressionBuilderImpl
// ============================================================================
/** A builder for creating {@link ReferenceExpression} instances. */
class ReferenceExpressionBuilderImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    async isEmpty() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/ReferenceExpressionBuilder.isEmpty', { context: this._handle });
    }
    /** @internal */
    async _appendLiteralInternal(value) {
        const rpcArgs = { context: this._handle, value };
        await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/appendLiteral', rpcArgs);
        return this;
    }
    /**
     * Appends a literal value to the expression.
     * @param value The literal string value to be appended to the interpolated string.
     */
    appendLiteral(value) {
        return new ReferenceExpressionBuilderPromiseImpl(this._appendLiteralInternal(value), this._client);
    }
    /** @internal */
    async _appendFormattedInternal(value, format) {
        const rpcArgs = { context: this._handle, value };
        if (format !== undefined)
            rpcArgs.format = format;
        await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/appendFormatted', rpcArgs);
        return this;
    }
    /**
     * Appends a formatted value to the expression.
     * @param value The formatted string to be appended to the interpolated string.
     * @param options Additional options.
     */
    appendFormatted(value, options) {
        const format = options?.format;
        return new ReferenceExpressionBuilderPromiseImpl(this._appendFormattedInternal(value, format), this._client);
    }
    /** @internal */
    async _appendValueProviderInternal(valueProvider, format) {
        const rpcArgs = { context: this._handle, valueProvider };
        if (format !== undefined)
            rpcArgs.format = format;
        await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/appendValueProvider', rpcArgs);
        return this;
    }
    /**
     * Appends a value provider to the reference expression
     * @param valueProvider The value provider to append.
     * @param options Additional options.
     */
    appendValueProvider(valueProvider, options) {
        const format = options?.format;
        return new ReferenceExpressionBuilderPromiseImpl(this._appendValueProviderInternal(valueProvider, format), this._client);
    }
    /** Builds the reference expression */
    async build() {
        const rpcArgs = { context: this._handle };
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/build', rpcArgs);
    }
}
/**
 * Thenable wrapper for ReferenceExpressionBuilder that enables fluent chaining.
 */
class ReferenceExpressionBuilderPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    isEmpty() {
        return this._promise.then(obj => obj.isEmpty());
    }
    appendLiteral(value) {
        return new ReferenceExpressionBuilderPromiseImpl(this._promise.then(obj => obj.appendLiteral(value)), this._client);
    }
    appendFormatted(value, options) {
        return new ReferenceExpressionBuilderPromiseImpl(this._promise.then(obj => obj.appendFormatted(value, options)), this._client);
    }
    appendValueProvider(valueProvider, options) {
        return new ReferenceExpressionBuilderPromiseImpl(this._promise.then(obj => obj.appendValueProvider(valueProvider, options)), this._client);
    }
    build() {
        return this._promise.then(obj => obj.build());
    }
}
// ============================================================================
// ResourceCommandServiceImpl
// ============================================================================
/** A service to execute resource commands. */
class ResourceCommandServiceImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    /**
     * Executes a command for the specified resource.
     * @param resource The resource id or resource handle. A resource id can either exactly match the unique id of the resource or the displayed resource name if the resource name doesn't have duplicates.
     * @param commandName The command name.
     * @param options Additional options.
     * @returns The command execution result.
     */
    async executeCommandAsync(resource, commandName, options) {
        const argumentsValue = options?.arguments;
        const cancellationToken = options?.cancellationToken;
        resource = isPromiseLike(resource) ? await resource : resource;
        const rpcArgs = { resourceCommandService: this._handle, resource, commandName };
        if (argumentsValue !== undefined)
            rpcArgs.arguments = argumentsValue;
        if (cancellationToken !== undefined)
            rpcArgs.cancellationToken = CancellationToken.fromValue(cancellationToken);
        return await this._client.invokeCapability('Aspire.Hosting/executeResourceCommand', rpcArgs);
    }
}
/**
 * Thenable wrapper for ResourceCommandService that enables fluent chaining.
 */
class ResourceCommandServicePromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    executeCommandAsync(resource, commandName, options) {
        return this._promise.then(obj => obj.executeCommandAsync(resource, commandName, options));
    }
}
// ============================================================================
// ResourceEndpointsAllocatedEventImpl
// ============================================================================
/**
 * This event is raised by orchestrators to signal to resources that their endpoints have been allocated.
 *
 * Any resources that customize their URLs via a `ResourceUrlsCallbackAnnotation` will have their callbacks invoked during this event.
 */
class ResourceEndpointsAllocatedEventImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    resource() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/ResourceEndpointsAllocatedEvent.resource', { context: this._handle });
            return new ResourceImpl(handle, this._client);
        })();
        return new ResourcePromiseImpl(promise, this._client, false);
    }
    services() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/ResourceEndpointsAllocatedEvent.services', { context: this._handle });
            return new ServiceProviderImpl(handle, this._client);
        })();
        return new ServiceProviderPromiseImpl(promise, this._client, false);
    }
}
/**
 * Thenable wrapper for ResourceEndpointsAllocatedEvent that enables fluent chaining.
 */
class ResourceEndpointsAllocatedEventPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    resource() {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.resource()), this._client, false);
    }
    services() {
        return new ServiceProviderPromiseImpl(this._promise.then(obj => obj.services()), this._client, false);
    }
}
// ============================================================================
// ResourceLoggerServiceImpl
// ============================================================================
/** A service that provides loggers for resources to write to. */
class ResourceLoggerServiceImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    /** @internal */
    async _completeLogInternal(resource) {
        resource = isPromiseLike(resource) ? await resource : resource;
        const rpcArgs = { loggerService: this._handle, resource };
        await this._client.invokeCapability('Aspire.Hosting/completeLog', rpcArgs);
        return this;
    }
    /** Completes the log stream for a resource. */
    completeLog(resource) {
        return new ResourceLoggerServicePromiseImpl(this._completeLogInternal(resource), this._client);
    }
    /** @internal */
    async _completeLogByNameInternal(resourceName) {
        const rpcArgs = { loggerService: this._handle, resourceName };
        await this._client.invokeCapability('Aspire.Hosting/completeLogByName', rpcArgs);
        return this;
    }
    /** Completes the log stream by resource name. */
    completeLogByName(resourceName) {
        return new ResourceLoggerServicePromiseImpl(this._completeLogByNameInternal(resourceName), this._client);
    }
}
/**
 * Thenable wrapper for ResourceLoggerService that enables fluent chaining.
 */
class ResourceLoggerServicePromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    completeLog(resource) {
        return new ResourceLoggerServicePromiseImpl(this._promise.then(obj => obj.completeLog(resource)), this._client);
    }
    completeLogByName(resourceName) {
        return new ResourceLoggerServicePromiseImpl(this._promise.then(obj => obj.completeLogByName(resourceName)), this._client);
    }
}
// ============================================================================
// ResourceNotificationServiceImpl
// ============================================================================
/** A service that allows publishing and subscribing to changes in the state of a resource. */
class ResourceNotificationServiceImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    /** @internal */
    async _waitForResourceStateInternal(resourceName, targetState) {
        const rpcArgs = { notificationService: this._handle, resourceName };
        if (targetState !== undefined)
            rpcArgs.targetState = targetState;
        await this._client.invokeCapability('Aspire.Hosting/waitForResourceState', rpcArgs);
        return this;
    }
    /**
     * Waits for a resource to reach a specified state.
     * @param options Additional options.
     */
    waitForResourceState(resourceName, options) {
        const targetState = options?.targetState;
        return new ResourceNotificationServicePromiseImpl(this._waitForResourceStateInternal(resourceName, targetState), this._client);
    }
    /** Waits for a resource to reach one of the specified states. */
    async waitForResourceStates(resourceName, targetStates) {
        const rpcArgs = { notificationService: this._handle, resourceName, targetStates };
        return await this._client.invokeCapability('Aspire.Hosting/waitForResourceStates', rpcArgs);
    }
    /** Waits for a resource to become healthy. */
    async waitForResourceHealthy(resourceName) {
        const rpcArgs = { notificationService: this._handle, resourceName };
        return await this._client.invokeCapability('Aspire.Hosting/waitForResourceHealthy', rpcArgs);
    }
    /** @internal */
    async _waitForDependenciesInternal(resource) {
        resource = isPromiseLike(resource) ? await resource : resource;
        const rpcArgs = { notificationService: this._handle, resource };
        await this._client.invokeCapability('Aspire.Hosting/waitForDependencies', rpcArgs);
        return this;
    }
    /** Waits for all dependencies of a resource to be ready. */
    waitForDependencies(resource) {
        return new ResourceNotificationServicePromiseImpl(this._waitForDependenciesInternal(resource), this._client);
    }
    /** Tries to get the current state of a resource. */
    async tryGetResourceState(resourceName) {
        const rpcArgs = { notificationService: this._handle, resourceName };
        return await this._client.invokeCapability('Aspire.Hosting/tryGetResourceState', rpcArgs);
    }
    /** @internal */
    async _publishResourceUpdateInternal(resource, state, stateStyle) {
        resource = isPromiseLike(resource) ? await resource : resource;
        const rpcArgs = { notificationService: this._handle, resource };
        if (state !== undefined)
            rpcArgs.state = state;
        if (stateStyle !== undefined)
            rpcArgs.stateStyle = stateStyle;
        await this._client.invokeCapability('Aspire.Hosting/publishResourceUpdate', rpcArgs);
        return this;
    }
    /**
     * Publishes an update for a resource's state.
     * @param options Additional options.
     */
    publishResourceUpdate(resource, options) {
        const state = options?.state;
        const stateStyle = options?.stateStyle;
        return new ResourceNotificationServicePromiseImpl(this._publishResourceUpdateInternal(resource, state, stateStyle), this._client);
    }
}
/**
 * Thenable wrapper for ResourceNotificationService that enables fluent chaining.
 */
class ResourceNotificationServicePromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    waitForResourceState(resourceName, options) {
        return new ResourceNotificationServicePromiseImpl(this._promise.then(obj => obj.waitForResourceState(resourceName, options)), this._client);
    }
    waitForResourceStates(resourceName, targetStates) {
        return this._promise.then(obj => obj.waitForResourceStates(resourceName, targetStates));
    }
    waitForResourceHealthy(resourceName) {
        return this._promise.then(obj => obj.waitForResourceHealthy(resourceName));
    }
    waitForDependencies(resource) {
        return new ResourceNotificationServicePromiseImpl(this._promise.then(obj => obj.waitForDependencies(resource)), this._client);
    }
    tryGetResourceState(resourceName) {
        return this._promise.then(obj => obj.tryGetResourceState(resourceName));
    }
    publishResourceUpdate(resource, options) {
        return new ResourceNotificationServicePromiseImpl(this._promise.then(obj => obj.publishResourceUpdate(resource, options)), this._client);
    }
}
// ============================================================================
// ResourceReadyEventImpl
// ============================================================================
/**
 * Event that is raised when a resource initially transitions to a ready state.
 *
 * This event is only fired the first time a resource transitions to a ready state after starting.
 */
class ResourceReadyEventImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    resource() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/ResourceReadyEvent.resource', { context: this._handle });
            return new ResourceImpl(handle, this._client);
        })();
        return new ResourcePromiseImpl(promise, this._client, false);
    }
    services() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/ResourceReadyEvent.services', { context: this._handle });
            return new ServiceProviderImpl(handle, this._client);
        })();
        return new ServiceProviderPromiseImpl(promise, this._client, false);
    }
}
/**
 * Thenable wrapper for ResourceReadyEvent that enables fluent chaining.
 */
class ResourceReadyEventPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    resource() {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.resource()), this._client, false);
    }
    services() {
        return new ServiceProviderPromiseImpl(this._promise.then(obj => obj.services()), this._client, false);
    }
}
// ============================================================================
// ResourceStoppedEventImpl
// ============================================================================
/**
 * This event is raised after a resource has stopped.
 *
 * This event allows for cleanup or unregistration logic when a resource is stopped by an orchestrator.
 */
class ResourceStoppedEventImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    resource() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/ResourceStoppedEvent.resource', { context: this._handle });
            return new ResourceImpl(handle, this._client);
        })();
        return new ResourcePromiseImpl(promise, this._client, false);
    }
    services() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/ResourceStoppedEvent.services', { context: this._handle });
            return new ServiceProviderImpl(handle, this._client);
        })();
        return new ServiceProviderPromiseImpl(promise, this._client, false);
    }
}
/**
 * Thenable wrapper for ResourceStoppedEvent that enables fluent chaining.
 */
class ResourceStoppedEventPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    resource() {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.resource()), this._client, false);
    }
    services() {
        return new ServiceProviderPromiseImpl(this._promise.then(obj => obj.services()), this._client, false);
    }
}
// ============================================================================
// ResourceUrlsCallbackContextImpl
// ============================================================================
/** Represents a callback context for resource URLs. */
class ResourceUrlsCallbackContextImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    resource() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/ResourceUrlsCallbackContext.resource', { context: this._handle });
            return new ResourceImpl(handle, this._client);
        })();
        return new ResourcePromiseImpl(promise, this._client, false);
    }
    urls() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/ResourceUrlsCallbackContext.urls', { context: this._handle });
            return new ResourceUrlsEditorImpl(handle, this._client);
        })();
        return new ResourceUrlsEditorPromiseImpl(promise, this._client, false);
    }
    log() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/ResourceUrlsCallbackContext.log', { context: this._handle });
            return new LogFacadeImpl(handle, this._client);
        })();
        return new LogFacadePromiseImpl(promise, this._client, false);
    }
    executionContext() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/ResourceUrlsCallbackContext.executionContext', { context: this._handle });
            return new DistributedApplicationExecutionContextImpl(handle, this._client);
        })();
        return new DistributedApplicationExecutionContextPromiseImpl(promise, this._client, false);
    }
    /** @internal */
    async _getEndpointInternal(name) {
        const rpcArgs = { context: this._handle, name };
        const result = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/getEndpoint', rpcArgs);
        return new EndpointReferenceImpl(result, this._client);
    }
    /**
     * Gets an endpoint reference from the associated resource
     * @param name The name of the endpoint.
     */
    getEndpoint(name) {
        return new EndpointReferencePromiseImpl(this._getEndpointInternal(name), this._client);
    }
}
/**
 * Thenable wrapper for ResourceUrlsCallbackContext that enables fluent chaining.
 */
class ResourceUrlsCallbackContextPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    resource() {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.resource()), this._client, false);
    }
    urls() {
        return new ResourceUrlsEditorPromiseImpl(this._promise.then(obj => obj.urls()), this._client, false);
    }
    log() {
        return new LogFacadePromiseImpl(this._promise.then(obj => obj.log()), this._client, false);
    }
    executionContext() {
        return new DistributedApplicationExecutionContextPromiseImpl(this._promise.then(obj => obj.executionContext()), this._client, false);
    }
    getEndpoint(name) {
        return new EndpointReferencePromiseImpl(this._promise.then(obj => obj.getEndpoint(name)), this._client);
    }
}
// ============================================================================
// ResourceUrlsEditorImpl
// ============================================================================
/** Provides an ATS-first editor for resource URLs within polyglot callbacks. */
class ResourceUrlsEditorImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    executionContext() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/ResourceUrlsEditor.executionContext', { context: this._handle });
            return new DistributedApplicationExecutionContextImpl(handle, this._client);
        })();
        return new DistributedApplicationExecutionContextPromiseImpl(promise, this._client, false);
    }
    /** @internal */
    async _addInternal(url, displayText) {
        const rpcArgs = { context: this._handle, url };
        if (displayText !== undefined)
            rpcArgs.displayText = displayText;
        await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/ResourceUrlsEditor.add', rpcArgs);
        return this;
    }
    /**
     * Adds a displayed URL.
     * @param url The URL to add, specified as a string or reference expression.
     * @param options Additional options.
     */
    add(url, options) {
        const displayText = options?.displayText;
        return new ResourceUrlsEditorPromiseImpl(this._addInternal(url, displayText), this._client);
    }
    /** @internal */
    async _addForEndpointInternal(endpoint, url, displayText) {
        endpoint = isPromiseLike(endpoint) ? await endpoint : endpoint;
        const rpcArgs = { context: this._handle, endpoint, url };
        if (displayText !== undefined)
            rpcArgs.displayText = displayText;
        await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/ResourceUrlsEditor.addForEndpoint', rpcArgs);
        return this;
    }
    /**
     * Adds a displayed URL for a specific endpoint.
     * @param endpoint The endpoint the URL is associated with.
     * @param url The URL to add, specified as a string or reference expression.
     * @param options Additional options.
     */
    addForEndpoint(endpoint, url, options) {
        const displayText = options?.displayText;
        return new ResourceUrlsEditorPromiseImpl(this._addForEndpointInternal(endpoint, url, displayText), this._client);
    }
}
/**
 * Thenable wrapper for ResourceUrlsEditor that enables fluent chaining.
 */
class ResourceUrlsEditorPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    executionContext() {
        return new DistributedApplicationExecutionContextPromiseImpl(this._promise.then(obj => obj.executionContext()), this._client, false);
    }
    add(url, options) {
        return new ResourceUrlsEditorPromiseImpl(this._promise.then(obj => obj.add(url, options)), this._client);
    }
    addForEndpoint(endpoint, url, options) {
        return new ResourceUrlsEditorPromiseImpl(this._promise.then(obj => obj.addForEndpoint(endpoint, url, options)), this._client);
    }
}
// ============================================================================
// UpdateCommandStateContextImpl
// ============================================================================
/** Context for {@link ResourceCommandAnnotation.UpdateState}. */
class UpdateCommandStateContextImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    async resourceSnapshot() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/UpdateCommandStateContext.resourceSnapshot', { context: this._handle });
    }
}
/**
 * Thenable wrapper for UpdateCommandStateContext that enables fluent chaining.
 */
class UpdateCommandStateContextPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    resourceSnapshot() {
        return this._promise.then(obj => obj.resourceSnapshot());
    }
}
// ============================================================================
// AspireStoreImpl
// ============================================================================
/**
 * Represents a store for managing files in the Aspire hosting environment that can be reused across runs.
 *
 * The store is created under the AppHost obj folder, or under the path specified by the
 * ASPIRE__STORE__PATH environment variable. Each application gets its own store so files
 * do not conflict with unrelated applications.
 */
class AspireStoreImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    async basePath() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/IAspireStore.basePath', { context: this._handle });
    }
    /**
     * Gets a deterministic file path that is a copy of the `sourceFilename`. The resulting file name will depend on the content of the file.
     * @param filenameTemplate A file name to base the result on.
     * @param sourceFilename An existing file.
     * @returns A deterministic file path with the same content as `sourceFilename`.
     */
    async getFileNameWithContent(filenameTemplate, sourceFilename) {
        const rpcArgs = { aspireStore: this._handle, filenameTemplate, sourceFilename };
        return await this._client.invokeCapability('Aspire.Hosting/getFileNameWithContent', rpcArgs);
    }
}
/**
 * Thenable wrapper for AspireStore that enables fluent chaining.
 */
class AspireStorePromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    basePath() {
        return this._promise.then(obj => obj.basePath());
    }
    getFileNameWithContent(filenameTemplate, sourceFilename) {
        return this._promise.then(obj => obj.getFileNameWithContent(filenameTemplate, sourceFilename));
    }
}
// ============================================================================
// ConfigurationImpl
// ============================================================================
/** Type class for Configuration. */
class ConfigurationImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    /**
     * Gets a configuration value by key.
     * @param key The configuration key (e.g., "ConnectionStrings:Default").
     * @returns The configuration value, or null if not found.
     */
    async getConfigValue(key) {
        const rpcArgs = { configuration: this._handle, key };
        return await this._client.invokeCapability('Aspire.Hosting/getConfigValue', rpcArgs);
    }
    /**
     * Gets a connection string by name.
     * @param name The connection string name.
     * @returns The connection string value, or null if not found.
     */
    async getConnectionString(name) {
        const rpcArgs = { configuration: this._handle, name };
        return await this._client.invokeCapability('Aspire.Hosting/getConnectionString', rpcArgs);
    }
    /** @internal */
    async _getSectionInternal(key) {
        const rpcArgs = { configuration: this._handle, key };
        const result = await this._client.invokeCapability('Aspire.Hosting/getSection', rpcArgs);
        return new ConfigurationSectionImpl(result, this._client);
    }
    /**
     * Gets a configuration section by key.
     * @param key The configuration key.
     * @returns The configuration section handle.
     */
    getSection(key) {
        return new ConfigurationSectionPromiseImpl(this._getSectionInternal(key), this._client);
    }
    /**
     * Gets the child sections of a configuration handle.
     * @returns The child sections.
     */
    async getChildren() {
        const rpcArgs = { configuration: this._handle };
        return await this._client.invokeCapability('Aspire.Hosting/getChildren', rpcArgs);
    }
    /**
     * Checks whether a configuration section exists.
     * @param key The configuration key.
     * @returns `true` when the section exists; otherwise, `false`.
     */
    async exists(key) {
        const rpcArgs = { configuration: this._handle, key };
        return await this._client.invokeCapability('Aspire.Hosting/exists', rpcArgs);
    }
}
/**
 * Thenable wrapper for Configuration that enables fluent chaining.
 */
class ConfigurationPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    getConfigValue(key) {
        return this._promise.then(obj => obj.getConfigValue(key));
    }
    getConnectionString(name) {
        return this._promise.then(obj => obj.getConnectionString(name));
    }
    getSection(key) {
        return new ConfigurationSectionPromiseImpl(this._promise.then(obj => obj.getSection(key)), this._client);
    }
    getChildren() {
        return this._promise.then(obj => obj.getChildren());
    }
    exists(key) {
        return this._promise.then(obj => obj.exists(key));
    }
}
// ============================================================================
// ConfigurationSectionImpl
// ============================================================================
/** Type class for ConfigurationSection. */
class ConfigurationSectionImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    async key() {
        return await this._client.invokeCapability('Microsoft.Extensions.Configuration/IConfigurationSection.key', { context: this._handle });
    }
    async path() {
        return await this._client.invokeCapability('Microsoft.Extensions.Configuration/IConfigurationSection.path', { context: this._handle });
    }
    value = {
        get: async () => {
            return await this._client.invokeCapability('Microsoft.Extensions.Configuration/IConfigurationSection.value', { context: this._handle });
        },
        set: async (value) => {
            await this._client.invokeCapability('Microsoft.Extensions.Configuration/IConfigurationSection.setValue', { context: this._handle, value });
        }
    };
}
/**
 * Thenable wrapper for ConfigurationSection that enables fluent chaining.
 */
class ConfigurationSectionPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    key() {
        return this._promise.then(obj => obj.key());
    }
    path() {
        return this._promise.then(obj => obj.path());
    }
}
// ============================================================================
// DistributedApplicationBuilderImpl
// ============================================================================
/** A builder for creating instances of {@link DistributedApplication}. */
class DistributedApplicationBuilderImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    async appHostDirectory() {
        return await this._client.invokeCapability('Aspire.Hosting/IDistributedApplicationBuilder.appHostDirectory', { context: this._handle });
    }
    environment() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting/IDistributedApplicationBuilder.environment', { context: this._handle });
            return new HostEnvironmentImpl(handle, this._client);
        })();
        return new HostEnvironmentPromiseImpl(promise, this._client, false);
    }
    eventing() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting/IDistributedApplicationBuilder.eventing', { context: this._handle });
            return new DistributedApplicationEventingImpl(handle, this._client);
        })();
        return new DistributedApplicationEventingPromiseImpl(promise, this._client, false);
    }
    executionContext() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting/IDistributedApplicationBuilder.executionContext', { context: this._handle });
            return new DistributedApplicationExecutionContextImpl(handle, this._client);
        })();
        return new DistributedApplicationExecutionContextPromiseImpl(promise, this._client, false);
    }
    pipeline() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting/IDistributedApplicationBuilder.pipeline', { context: this._handle });
            return new DistributedApplicationPipelineImpl(handle, this._client);
        })();
        return new DistributedApplicationPipelinePromiseImpl(promise, this._client, false);
    }
    userSecretsManager() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting/IDistributedApplicationBuilder.userSecretsManager', { context: this._handle });
            return new UserSecretsManagerImpl(handle, this._client);
        })();
        return new UserSecretsManagerPromiseImpl(promise, this._client, false);
    }
    /** @internal */
    async _buildInternal() {
        const rpcArgs = { context: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/build', rpcArgs);
        return new DistributedApplicationImpl(result, this._client);
    }
    /**
     * Builds the distributed application
     *
     * Callers of the `Build` method should only call it once. are responsible for the lifecycle of the
     * `DistributedApplication` instance that is returned. Note that the `DistributedApplication`
     * type implements `IDisposable` and should be disposed of when it is no longer needed. Note that in
     * many templates and samples Dispose is omitted for brevity because in those cases the instance is destroyed
     * when the process exists.
     * @returns A new `DistributedApplication` instance.
     */
    build() {
        const flushAndBuild = async () => { await this._client.flushPendingPromises(); return this._buildInternal(); };
        return new DistributedApplicationPromiseImpl(flushAndBuild(), this._client, false);
    }
    /** @internal */
    async _addContainerRegistryInternal(name, endpoint, repository) {
        endpoint = isPromiseLike(endpoint) ? await endpoint : endpoint;
        repository = isPromiseLike(repository) ? await repository : repository;
        const rpcArgs = { builder: this._handle, name, endpoint };
        if (repository !== undefined)
            rpcArgs.repository = repository;
        const result = await this._client.invokeCapability('Aspire.Hosting/addContainerRegistry', rpcArgs);
        return new ContainerRegistryResourceImpl(result, this._client);
    }
    /**
     * Adds a container registry resource
     * @param options Additional options.
     */
    addContainerRegistry(name, endpoint, options) {
        let repository = options?.repository;
        return new ContainerRegistryResourcePromiseImpl(this._addContainerRegistryInternal(name, endpoint, repository), this._client);
    }
    /** @internal */
    async _addContainerInternal(name, image) {
        const rpcArgs = { builder: this._handle, name, image };
        const result = await this._client.invokeCapability('Aspire.Hosting/addContainer', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a container resource to the application.
     * @param name The name of the resource.
     * @param image The image name or image options for the container.
     * @returns The resource builder.
     */
    addContainer(name, image) {
        return new ContainerResourcePromiseImpl(this._addContainerInternal(name, image), this._client);
    }
    /** @internal */
    async _addDockerfileInternal(name, contextPath, dockerfilePath, stage) {
        const rpcArgs = { builder: this._handle, name, contextPath };
        if (dockerfilePath !== undefined)
            rpcArgs.dockerfilePath = dockerfilePath;
        if (stage !== undefined)
            rpcArgs.stage = stage;
        const result = await this._client.invokeCapability('Aspire.Hosting/addDockerfile', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a Dockerfile to the application model that can be treated like a container resource.
     * @param name The name of the resource.
     * @param contextPath Path to be used as the context for the container image build.
     * @param options Additional options.
     * @returns The resource builder.
     */
    addDockerfile(name, contextPath, options) {
        const dockerfilePath = options?.dockerfilePath;
        const stage = options?.stage;
        return new ContainerResourcePromiseImpl(this._addDockerfileInternal(name, contextPath, dockerfilePath, stage), this._client);
    }
    /** @internal */
    async _addDockerfileFactoryInternal(name, contextPath, dockerfileFactory, stage) {
        const dockerfileFactoryId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new DockerfileFactoryContextImpl(argHandle, this._client);
            return await dockerfileFactory(arg);
        });
        const rpcArgs = { builder: this._handle, name, contextPath, dockerfileFactory: dockerfileFactoryId };
        if (stage !== undefined)
            rpcArgs.stage = stage;
        const result = await this._client.invokeCapability('Aspire.Hosting/addDockerfileFactory', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a Dockerfile to the application model that can be treated like a container resource, with the Dockerfile content generated by an asynchronous factory function.
     *
     * The `contextPath` is relative to the AppHost directory unless it is fully qualified.
     * The factory function is invoked once during the build process to generate the Dockerfile content.
     * The output is trusted and not validated.
     * @param name The name of the resource.
     * @param contextPath Path to be used as the context for the container image build.
     * @param dockerfileFactory An asynchronous function that returns the Dockerfile content as a string.
     * @param options Additional options.
     * @returns The resource builder.
     */
    addDockerfileFactory(name, contextPath, dockerfileFactory, options) {
        const stage = options?.stage;
        return new ContainerResourcePromiseImpl(this._addDockerfileFactoryInternal(name, contextPath, dockerfileFactory, stage), this._client);
    }
    /** @internal */
    async _addDockerfileBuilderInternal(name, contextPath, callback, stage) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new DockerfileBuilderCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, name, contextPath, callback: callbackId };
        if (stage !== undefined)
            rpcArgs.stage = stage;
        const result = await this._client.invokeCapability('Aspire.Hosting/addDockerfileBuilder', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a container resource built from a programmatically generated Dockerfile
     *
     * This method provides a programmatic way to build Dockerfiles using the `DockerfileBuilder` API
     * instead of string manipulation.
     * The `contextPath` is relative to the AppHost directory unless it is fully qualified.
     * Creates a container with a programmatically built Dockerfile:
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddDockerfileBuilder("mycontainer", "path/to/context", context =>
     * {
     * context.Builder.From("alpine:latest")
     * .WorkDir("/app")
     * .Copy(".", ".")
     * .Cmd(["./myapp"]);
     * return Task.CompletedTask;
     * });
     * builder.Build().Run();
     * ```
     * @param name The name of the resource.
     * @param contextPath Path to be used as the context for the container image build.
     * @param callback A callback that uses the `DockerfileBuilder` API to construct the Dockerfile.
     * @param options Additional options.
     * @returns The resource builder.
     */
    addDockerfileBuilder(name, contextPath, callback, options) {
        const stage = options?.stage;
        return new ContainerResourcePromiseImpl(this._addDockerfileBuilderInternal(name, contextPath, callback, stage), this._client);
    }
    /** @internal */
    async _addDotnetToolInternal(name, packageId) {
        const rpcArgs = { builder: this._handle, name, packageId };
        const result = await this._client.invokeCapability('Aspire.Hosting/addDotnetTool', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Adds a .NET tool resource to the application model.
     * @param name The name of the resource.
     * @param packageId The package id of the tool.
     * @returns The resource builder.
     */
    addDotnetTool(name, packageId) {
        return new DotnetToolResourcePromiseImpl(this._addDotnetToolInternal(name, packageId), this._client);
    }
    /** @internal */
    async _addExecutableInternal(name, command, workingDirectory, args) {
        const rpcArgs = { builder: this._handle, name, command, workingDirectory, args };
        const result = await this._client.invokeCapability('Aspire.Hosting/addExecutable', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Adds an executable resource to the application model.
     *
     * You can run any executable command using its full path.
     * As a security feature, Aspire doesn't run executable unless the command is located in a path listed in the PATH environment variable.
     * To run an executable file that's in the current directory, specify the full path or use the relative path `./` to represent the current directory.
     * @param name The name of the resource.
     * @param command The executable path. This can be a fully qualified path or a executable to run from the shell/command line.
     * @param workingDirectory The working directory of the executable.
     * @param args The arguments to the executable.
     * @returns The resource builder.
     */
    addExecutable(name, command, workingDirectory, args) {
        return new ExecutableResourcePromiseImpl(this._addExecutableInternal(name, command, workingDirectory, args), this._client);
    }
    /** @internal */
    async _addExternalServiceInternal(name, url) {
        url = isPromiseLike(url) ? await url : url;
        const rpcArgs = { builder: this._handle, name, url };
        const result = await this._client.invokeCapability('Aspire.Hosting/addExternalService', rpcArgs);
        return new ExternalServiceResourceImpl(result, this._client);
    }
    /** Adds an external service resource */
    addExternalService(name, url) {
        return new ExternalServiceResourcePromiseImpl(this._addExternalServiceInternal(name, url), this._client);
    }
    /** @internal */
    async _addParameterInternal(name, value, publishValueAsDefault, secret) {
        const rpcArgs = { builder: this._handle, name };
        if (value !== undefined)
            rpcArgs.value = value;
        if (publishValueAsDefault !== undefined)
            rpcArgs.publishValueAsDefault = publishValueAsDefault;
        if (secret !== undefined)
            rpcArgs.secret = secret;
        const result = await this._client.invokeCapability('Aspire.Hosting/addParameter', rpcArgs);
        return new ParameterResourceImpl(result, this._client);
    }
    /**
     * Adds a parameter resource
     * @param options Additional options.
     */
    addParameter(name, options) {
        const value = options?.value;
        const publishValueAsDefault = options?.publishValueAsDefault;
        const secret = options?.secret;
        return new ParameterResourcePromiseImpl(this._addParameterInternal(name, value, publishValueAsDefault, secret), this._client);
    }
    /** @internal */
    async _addParameterFromConfigurationInternal(name, configurationKey, secret) {
        const rpcArgs = { builder: this._handle, name, configurationKey };
        if (secret !== undefined)
            rpcArgs.secret = secret;
        const result = await this._client.invokeCapability('Aspire.Hosting/addParameterFromConfiguration', rpcArgs);
        return new ParameterResourceImpl(result, this._client);
    }
    /**
     * Adds a parameter resource to the application, with a value coming from configuration.
     * @param name Name of parameter resource
     * @param configurationKey Configuration key used to get the value of the parameter
     * @param options Additional options.
     * @returns Resource builder for the parameter.
     */
    addParameterFromConfiguration(name, configurationKey, options) {
        const secret = options?.secret;
        return new ParameterResourcePromiseImpl(this._addParameterFromConfigurationInternal(name, configurationKey, secret), this._client);
    }
    /** @internal */
    async _addParameterWithGeneratedValueInternal(name, value, secret, persist) {
        const rpcArgs = { builder: this._handle, name, value };
        if (secret !== undefined)
            rpcArgs.secret = secret;
        if (persist !== undefined)
            rpcArgs.persist = persist;
        const result = await this._client.invokeCapability('Aspire.Hosting/addParameterWithGeneratedValue', rpcArgs);
        return new ParameterResourceImpl(result, this._client);
    }
    /**
     * Adds a parameter with a generated default value
     * @param options Additional options.
     */
    addParameterWithGeneratedValue(name, value, options) {
        const secret = options?.secret;
        const persist = options?.persist;
        return new ParameterResourcePromiseImpl(this._addParameterWithGeneratedValueInternal(name, value, secret, persist), this._client);
    }
    /** @internal */
    async _addConnectionStringInternal(name, environmentVariableNameOrExpression) {
        const rpcArgs = { builder: this._handle, name };
        if (environmentVariableNameOrExpression !== undefined)
            rpcArgs.environmentVariableNameOrExpression = environmentVariableNameOrExpression;
        const result = await this._client.invokeCapability('Aspire.Hosting/addConnectionString', rpcArgs);
        return new ResourceWithConnectionStringImpl(result, this._client);
    }
    /**
     * Adds a connection string resource
     * @param options Additional options.
     */
    addConnectionString(name, options) {
        const environmentVariableNameOrExpression = options?.environmentVariableNameOrExpression;
        return new ResourceWithConnectionStringPromiseImpl(this._addConnectionStringInternal(name, environmentVariableNameOrExpression), this._client);
    }
    /** @internal */
    async _addProjectInternal(name, projectPath, launchProfileOrOptions) {
        launchProfileOrOptions = isPromiseLike(launchProfileOrOptions) ? await launchProfileOrOptions : launchProfileOrOptions;
        const rpcArgs = { builder: this._handle, name, projectPath };
        if (launchProfileOrOptions !== undefined)
            rpcArgs.launchProfileOrOptions = launchProfileOrOptions;
        const result = await this._client.invokeCapability('Aspire.Hosting/addProject', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Adds a .NET project resource
     * @param options Additional options.
     */
    addProject(name, projectPath, options) {
        let launchProfileOrOptions = options?.launchProfileOrOptions;
        return new ProjectResourcePromiseImpl(this._addProjectInternal(name, projectPath, launchProfileOrOptions), this._client);
    }
    /** @internal */
    async _addCSharpAppInternal(name, path, options) {
        options = isPromiseLike(options) ? await options : options;
        const rpcArgs = { builder: this._handle, name, path };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/addCSharpApp', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Adds a C# application resource
     * @param optionsBag Additional options.
     */
    addCSharpApp(name, path, optionsBag) {
        let options = optionsBag?.options;
        return new CSharpAppResourcePromiseImpl(this._addCSharpAppInternal(name, path, options), this._client);
    }
    /** @internal */
    async _getConfigurationInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/getConfiguration', rpcArgs);
        return new ConfigurationImpl(result, this._client);
    }
    /**
     * Gets the application configuration.
     * @returns The configuration handle.
     */
    getConfiguration() {
        return new ConfigurationPromiseImpl(this._getConfigurationInternal(), this._client);
    }
    /**
     * Subscribes to the BeforeStart event, which fires before the application starts.
     *
     * This event provides access to the service provider and distributed application model,
     * allowing you to perform final configuration or validation before resources start.
     * @param callback A callback that receives the exported event when the event fires.
     * @returns A subscription handle that can be used to unsubscribe.
     */
    async subscribeBeforeStart(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new BeforeStartEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        return await this._client.invokeCapability('Aspire.Hosting/subscribeBeforeStart', rpcArgs);
    }
    /**
     * Subscribes to the BeforePublish event, which fires before the application is published.
     *
     * This event provides access to the service provider and distributed application model,
     * allowing you to perform final configuration or validation before publish pipeline steps run.
     * @param callback A callback that receives the exported event when the event fires.
     * @returns A subscription handle that can be used to unsubscribe.
     */
    async subscribeBeforePublish(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new BeforePublishEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        return await this._client.invokeCapability('Aspire.Hosting/subscribeBeforePublish', rpcArgs);
    }
    /**
     * Subscribes to the AfterPublish event, which fires after the application is published.
     *
     * This event provides access to the service provider and distributed application model,
     * allowing you to inspect the model after publish pipeline steps complete.
     * @param callback A callback that receives the exported event when the event fires.
     * @returns A subscription handle that can be used to unsubscribe.
     */
    async subscribeAfterPublish(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new AfterPublishEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        return await this._client.invokeCapability('Aspire.Hosting/subscribeAfterPublish', rpcArgs);
    }
    /**
     * Subscribes to the AfterResourcesCreated event, which fires after all resources are created.
     *
     * At this point, all resources have been instantiated but may not yet be running.
     * This is useful for performing cross-resource configuration.
     * @param callback A callback that receives the exported event when the event fires.
     * @returns A subscription handle that can be used to unsubscribe.
     */
    async subscribeAfterResourcesCreated(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new AfterResourcesCreatedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        return await this._client.invokeCapability('Aspire.Hosting/subscribeAfterResourcesCreated', rpcArgs);
    }
    /** @internal */
    async _addEventingSubscriberInternal(subscribe) {
        const subscribeId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new EventingSubscriberRegistrationContextImpl(argHandle, this._client);
            await subscribe(arg);
        });
        const rpcArgs = { builder: this._handle, subscribe: subscribeId };
        await this._client.invokeCapability('Aspire.Hosting/addEventingSubscriber', rpcArgs);
        return this;
    }
    /**
     * Adds an ATS-friendly eventing subscriber callback to the distributed-application builder.
     * @param subscribe The callback that registers the event subscriptions.
     */
    addEventingSubscriber(subscribe) {
        return new DistributedApplicationBuilderPromiseImpl(this._addEventingSubscriberInternal(subscribe), this._client);
    }
    /** @internal */
    async _tryAddEventingSubscriberInternal(subscribe) {
        const subscribeId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new EventingSubscriberRegistrationContextImpl(argHandle, this._client);
            await subscribe(arg);
        });
        const rpcArgs = { builder: this._handle, subscribe: subscribeId };
        await this._client.invokeCapability('Aspire.Hosting/tryAddEventingSubscriber', rpcArgs);
        return this;
    }
    /**
     * Attempts to add an ATS-friendly eventing subscriber callback to the distributed-application builder.
     * @param subscribe The callback that registers the event subscriptions.
     */
    tryAddEventingSubscriber(subscribe) {
        return new DistributedApplicationBuilderPromiseImpl(this._tryAddEventingSubscriberInternal(subscribe), this._client);
    }
    /** @internal */
    async _addPostgresInternal(name, userName, password, port) {
        userName = isPromiseLike(userName) ? await userName : userName;
        password = isPromiseLike(password) ? await password : password;
        const rpcArgs = { builder: this._handle, name };
        if (userName !== undefined)
            rpcArgs.userName = userName;
        if (password !== undefined)
            rpcArgs.password = password;
        if (port !== undefined)
            rpcArgs.port = port;
        const result = await this._client.invokeCapability('Aspire.Hosting.PostgreSQL/addPostgres', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Adds a PostgreSQL resource to the application model. A container is used for local development.
     * @param name The name of the resource. This name will be used as the connection string name when referenced in a dependency.
     * @param options Additional options.
     * @returns The resource builder.
     */
    addPostgres(name, options) {
        let userName = options?.userName;
        let password = options?.password;
        const port = options?.port;
        return new PostgresServerResourcePromiseImpl(this._addPostgresInternal(name, userName, password, port), this._client);
    }
    /** @internal */
    async _addRedisInternal(name, port, password) {
        password = isPromiseLike(password) ? await password : password;
        const rpcArgs = { builder: this._handle, name };
        if (port !== undefined)
            rpcArgs.port = port;
        if (password !== undefined)
            rpcArgs.password = password;
        const result = await this._client.invokeCapability('Aspire.Hosting.Redis/addRedis', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Adds a Redis container to the application model.
     * @param name The name of the resource. This name will be used as the connection string name when referenced in a dependency.
     * @param options Additional options.
     * @returns The resource builder.
     */
    addRedis(name, options) {
        const port = options?.port;
        let password = options?.password;
        return new RedisResourcePromiseImpl(this._addRedisInternal(name, port, password), this._client);
    }
    /** @internal */
    async _addNodeAppInternal(name, appDirectory, scriptPath) {
        const rpcArgs = { builder: this._handle, name, appDirectory, scriptPath };
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/addNodeApp', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Adds a node application to the application model. Node should be available on the PATH.
     *
     * This method executes a Node script directly using `node script.js`. If you want to use a package manager
     * you can add one and configure the install and run scripts using the provided extension methods.
     * If the application directory contains a `package.json` file, npm will be added as the default package manager.
     * @param name The name of the resource.
     * @param appDirectory The path to the directory containing the node application.
     * @param scriptPath The path to the script relative to the app directory to run.
     * @returns The resource builder.
     */
    addNodeApp(name, appDirectory, scriptPath) {
        return new NodeAppResourcePromiseImpl(this._addNodeAppInternal(name, appDirectory, scriptPath), this._client);
    }
    /** @internal */
    async _addBunAppInternal(name, appDirectory, scriptPath) {
        const rpcArgs = { builder: this._handle, name, appDirectory, scriptPath };
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/addBunApp', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Adds a Bun application to the application model. Bun should be available on the PATH.
     *
     * This method executes the script directly using `bun <script>`. Bun natively runs JavaScript and TypeScript
     * files so no transpile step is required.
     * If the application directory contains a `package.json` file, Bun will be added as the default package manager.
     * When publishing to a container, the default base image is `oven/bun:1` for both the build and runtime stages.
     * @param name The name of the resource.
     * @param appDirectory The path to the directory containing the Bun application.
     * @param scriptPath The path to the script (for example, `server.ts`) relative to `appDirectory` to run.
     * @returns The resource builder.
     */
    addBunApp(name, appDirectory, scriptPath) {
        return new BunAppResourcePromiseImpl(this._addBunAppInternal(name, appDirectory, scriptPath), this._client);
    }
    /** @internal */
    async _addJavaScriptAppInternal(name, appDirectory, runScriptName) {
        const rpcArgs = { builder: this._handle, name, appDirectory };
        if (runScriptName !== undefined)
            rpcArgs.runScriptName = runScriptName;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/addJavaScriptApp', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Adds a JavaScript application resource to the distributed application using the specified app directory and run script.
     *
     * If a Dockerfile does not exist in the application's directory, one will be generated
     * automatically when publishing. The method configures the resource with Node.js defaults and sets up npm
     * integration.
     * @param name The unique name of the JavaScript application resource. Cannot be null or empty.
     * @param appDirectory The path to the directory containing the JavaScript application.
     * @param options Additional options.
     * @returns A resource builder for the newly added JavaScript application resource.
     */
    addJavaScriptApp(name, appDirectory, options) {
        const runScriptName = options?.runScriptName;
        return new JavaScriptAppResourcePromiseImpl(this._addJavaScriptAppInternal(name, appDirectory, runScriptName), this._client);
    }
    /** @internal */
    async _addViteAppInternal(name, appDirectory, runScriptName) {
        const rpcArgs = { builder: this._handle, name, appDirectory };
        if (runScriptName !== undefined)
            rpcArgs.runScriptName = runScriptName;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/addViteApp', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Adds a Vite app to the distributed application builder.
     *
     * The following example creates a Vite app using npm as the package manager.
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddViteApp("frontend", "./frontend");
     * builder.Build().Run();
     * ```
     * @param name The name of the Vite app.
     * @param appDirectory The path to the directory containing the Vite app.
     * @param options Additional options.
     * @returns The resource builder.
     */
    addViteApp(name, appDirectory, options) {
        const runScriptName = options?.runScriptName;
        return new ViteAppResourcePromiseImpl(this._addViteAppInternal(name, appDirectory, runScriptName), this._client);
    }
    /** @internal */
    async _addNextJsAppInternal(name, appDirectory, runScriptName) {
        const rpcArgs = { builder: this._handle, name, appDirectory };
        if (runScriptName !== undefined)
            rpcArgs.runScriptName = runScriptName;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/addNextJsApp', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Adds a Next.js app to the distributed application builder.
     *
     * This method configures the Next.js application for both local development and publishing.
     * In run mode, it starts the Next.js dev server with the correct port binding.
     * In publish mode, it generates a multi-stage Dockerfile using Next.js standalone output mode,
     * which copies `public/`, `.next/standalone/`, and `.next/static/` into a
     * Node.js runtime container.
     * The Next.js application must have `output: "standalone"` configured in `next.config.ts`
     * and a `public/` directory (even if empty) for the published container to build correctly.
     * The following example creates a Next.js app.
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddNextJsApp("frontend", "./frontend");
     * builder.Build().Run();
     * ```
     * @param name The name of the Next.js app.
     * @param appDirectory The path to the directory containing the Next.js app.
     * @param options Additional options.
     * @returns The resource builder.
     */
    addNextJsApp(name, appDirectory, options) {
        const runScriptName = options?.runScriptName;
        return new NextJsAppResourcePromiseImpl(this._addNextJsAppInternal(name, appDirectory, runScriptName), this._client);
    }
}
/**
 * Thenable wrapper for DistributedApplicationBuilder that enables fluent chaining.
 */
class DistributedApplicationBuilderPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    appHostDirectory() {
        return this._promise.then(obj => obj.appHostDirectory());
    }
    environment() {
        return new HostEnvironmentPromiseImpl(this._promise.then(obj => obj.environment()), this._client, false);
    }
    eventing() {
        return new DistributedApplicationEventingPromiseImpl(this._promise.then(obj => obj.eventing()), this._client, false);
    }
    executionContext() {
        return new DistributedApplicationExecutionContextPromiseImpl(this._promise.then(obj => obj.executionContext()), this._client, false);
    }
    pipeline() {
        return new DistributedApplicationPipelinePromiseImpl(this._promise.then(obj => obj.pipeline()), this._client, false);
    }
    userSecretsManager() {
        return new UserSecretsManagerPromiseImpl(this._promise.then(obj => obj.userSecretsManager()), this._client, false);
    }
    build() {
        return new DistributedApplicationPromiseImpl(this._promise.then(obj => obj.build()), this._client);
    }
    addContainerRegistry(name, endpoint, options) {
        return new ContainerRegistryResourcePromiseImpl(this._promise.then(obj => obj.addContainerRegistry(name, endpoint, options)), this._client);
    }
    addContainer(name, image) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.addContainer(name, image)), this._client);
    }
    addDockerfile(name, contextPath, options) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.addDockerfile(name, contextPath, options)), this._client);
    }
    addDockerfileFactory(name, contextPath, dockerfileFactory, options) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.addDockerfileFactory(name, contextPath, dockerfileFactory, options)), this._client);
    }
    addDockerfileBuilder(name, contextPath, callback, options) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.addDockerfileBuilder(name, contextPath, callback, options)), this._client);
    }
    addDotnetTool(name, packageId) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.addDotnetTool(name, packageId)), this._client);
    }
    addExecutable(name, command, workingDirectory, args) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.addExecutable(name, command, workingDirectory, args)), this._client);
    }
    addExternalService(name, url) {
        return new ExternalServiceResourcePromiseImpl(this._promise.then(obj => obj.addExternalService(name, url)), this._client);
    }
    addParameter(name, options) {
        return new ParameterResourcePromiseImpl(this._promise.then(obj => obj.addParameter(name, options)), this._client);
    }
    addParameterFromConfiguration(name, configurationKey, options) {
        return new ParameterResourcePromiseImpl(this._promise.then(obj => obj.addParameterFromConfiguration(name, configurationKey, options)), this._client);
    }
    addParameterWithGeneratedValue(name, value, options) {
        return new ParameterResourcePromiseImpl(this._promise.then(obj => obj.addParameterWithGeneratedValue(name, value, options)), this._client);
    }
    addConnectionString(name, options) {
        return new ResourceWithConnectionStringPromiseImpl(this._promise.then(obj => obj.addConnectionString(name, options)), this._client);
    }
    addProject(name, projectPath, options) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.addProject(name, projectPath, options)), this._client);
    }
    addCSharpApp(name, path, options) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.addCSharpApp(name, path, options)), this._client);
    }
    getConfiguration() {
        return new ConfigurationPromiseImpl(this._promise.then(obj => obj.getConfiguration()), this._client);
    }
    subscribeBeforeStart(callback) {
        return this._promise.then(obj => obj.subscribeBeforeStart(callback));
    }
    subscribeBeforePublish(callback) {
        return this._promise.then(obj => obj.subscribeBeforePublish(callback));
    }
    subscribeAfterPublish(callback) {
        return this._promise.then(obj => obj.subscribeAfterPublish(callback));
    }
    subscribeAfterResourcesCreated(callback) {
        return this._promise.then(obj => obj.subscribeAfterResourcesCreated(callback));
    }
    addEventingSubscriber(subscribe) {
        return new DistributedApplicationBuilderPromiseImpl(this._promise.then(obj => obj.addEventingSubscriber(subscribe)), this._client);
    }
    tryAddEventingSubscriber(subscribe) {
        return new DistributedApplicationBuilderPromiseImpl(this._promise.then(obj => obj.tryAddEventingSubscriber(subscribe)), this._client);
    }
    addPostgres(name, options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.addPostgres(name, options)), this._client);
    }
    addRedis(name, options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.addRedis(name, options)), this._client);
    }
    addNodeApp(name, appDirectory, scriptPath) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.addNodeApp(name, appDirectory, scriptPath)), this._client);
    }
    addBunApp(name, appDirectory, scriptPath) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.addBunApp(name, appDirectory, scriptPath)), this._client);
    }
    addJavaScriptApp(name, appDirectory, options) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.addJavaScriptApp(name, appDirectory, options)), this._client);
    }
    addViteApp(name, appDirectory, options) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.addViteApp(name, appDirectory, options)), this._client);
    }
    addNextJsApp(name, appDirectory, options) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.addNextJsApp(name, appDirectory, options)), this._client);
    }
}
// ============================================================================
// DistributedApplicationEventingImpl
// ============================================================================
/** Supports publishing and subscribing to events which are executed during the AppHost lifecycle. */
class DistributedApplicationEventingImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    /** @internal */
    async _unsubscribeInternal(subscription) {
        const rpcArgs = { context: this._handle, subscription };
        await this._client.invokeCapability('Aspire.Hosting.Eventing/IDistributedApplicationEventing.unsubscribe', rpcArgs);
        return this;
    }
    /**
     * Unsubscribe from an event.
     * @param subscription The specific subscription to unsubscribe.
     */
    unsubscribe(subscription) {
        return new DistributedApplicationEventingPromiseImpl(this._unsubscribeInternal(subscription), this._client);
    }
}
/**
 * Thenable wrapper for DistributedApplicationEventing that enables fluent chaining.
 */
class DistributedApplicationEventingPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    unsubscribe(subscription) {
        return new DistributedApplicationEventingPromiseImpl(this._promise.then(obj => obj.unsubscribe(subscription)), this._client);
    }
}
// ============================================================================
// DistributedApplicationPipelineImpl
// ============================================================================
/** Represents a pipeline for executing deployment steps in a distributed application. */
class DistributedApplicationPipelineImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    /** @internal */
    async _disableBuildOnlyContainerValidationInternal() {
        const rpcArgs = { pipeline: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/disableBuildOnlyContainerValidation', rpcArgs);
        return new DistributedApplicationPipelineImpl(result, this._client);
    }
    /**
     * Disables the publish and deploy validation that requires build-only containers to be consumed by another resource.
     *
     * This is an application-wide escape hatch for scenarios where the build-only container validation is too restrictive
     * for a particular app. Prefer wiring build-only containers through `PublishWithContainerFiles` or
     * `PublishWithStaticFiles` when possible.
     * @returns The distributed application pipeline for chaining.
     */
    disableBuildOnlyContainerValidation() {
        return new DistributedApplicationPipelinePromiseImpl(this._disableBuildOnlyContainerValidationInternal(), this._client);
    }
    /** @internal */
    async _addStepInternal(stepName, callback, dependsOn, requiredBy) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new PipelineStepContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { pipeline: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined)
            rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined)
            rpcArgs.requiredBy = requiredBy;
        await this._client.invokeCapability('Aspire.Hosting/addStep', rpcArgs);
        return this;
    }
    /**
     * Adds an application-level pipeline step in a TypeScript-friendly shape.
     * @param stepName The unique name of the pipeline step.
     * @param callback The callback to execute when the step runs.
     * @param options Additional options.
     */
    addStep(stepName, callback, options) {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        return new DistributedApplicationPipelinePromiseImpl(this._addStepInternal(stepName, callback, dependsOn, requiredBy), this._client);
    }
    /** @internal */
    async _configureInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new PipelineConfigurationContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { pipeline: this._handle, callback: callbackId };
        await this._client.invokeCapability('Aspire.Hosting/configure', rpcArgs);
        return this;
    }
    /**
     * Registers a pipeline configuration callback in a TypeScript-friendly shape.
     * @param callback The callback to execute during pipeline configuration.
     */
    configure(callback) {
        return new DistributedApplicationPipelinePromiseImpl(this._configureInternal(callback), this._client);
    }
}
/**
 * Thenable wrapper for DistributedApplicationPipeline that enables fluent chaining.
 */
class DistributedApplicationPipelinePromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    disableBuildOnlyContainerValidation() {
        return new DistributedApplicationPipelinePromiseImpl(this._promise.then(obj => obj.disableBuildOnlyContainerValidation()), this._client);
    }
    addStep(stepName, callback, options) {
        return new DistributedApplicationPipelinePromiseImpl(this._promise.then(obj => obj.addStep(stepName, callback, options)), this._client);
    }
    configure(callback) {
        return new DistributedApplicationPipelinePromiseImpl(this._promise.then(obj => obj.configure(callback)), this._client);
    }
}
// ============================================================================
// ExecutionConfigurationBuilderImpl
// ============================================================================
/** Builder for gathering and resolving the execution configuration (arguments and environment variables) for a specific resource. */
class ExecutionConfigurationBuilderImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    /** @internal */
    async _buildInternal(executionContext, resourceLogger, cancellationToken) {
        executionContext = isPromiseLike(executionContext) ? await executionContext : executionContext;
        resourceLogger = isPromiseLike(resourceLogger) ? await resourceLogger : resourceLogger;
        const rpcArgs = { builder: this._handle, executionContext };
        if (resourceLogger !== undefined)
            rpcArgs.resourceLogger = resourceLogger;
        if (cancellationToken !== undefined)
            rpcArgs.cancellationToken = CancellationToken.fromValue(cancellationToken);
        const result = await this._client.invokeCapability('Aspire.Hosting/buildExecutionConfiguration', rpcArgs);
        return new ExecutionConfigurationResultImpl(result, this._client);
    }
    /**
     * Builds the execution configuration for the specified builder.
     * @param executionContext The execution context used while building the configuration.
     * @param options Additional options.
     * @returns The resolved execution configuration.
     */
    build(executionContext, options) {
        let resourceLogger = options?.resourceLogger;
        const cancellationToken = options?.cancellationToken;
        const flushAndBuild = async () => { await this._client.flushPendingPromises(); return this._buildInternal(executionContext, resourceLogger, cancellationToken); };
        return new ExecutionConfigurationResultPromiseImpl(flushAndBuild(), this._client, false);
    }
    /** @internal */
    async _withHttpsCertificateConfigInternal(configContextFactory) {
        const configContextFactoryId = registerCallback(async (argData) => {
            const arg = wrapIfHandle(argData);
            return await configContextFactory(arg);
        });
        const rpcArgs = { builder: this._handle, configContextFactory: configContextFactoryId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpsCertificateConfigExport', rpcArgs);
        return new ExecutionConfigurationBuilderImpl(result, this._client);
    }
    /**
     * Adds an HTTPS certificate configuration gatherer using certificate metadata instead of a raw X509 certificate.
     * @param configContextFactory The factory that creates the HTTPS certificate configuration context.
     * @returns The execution configuration builder.
     */
    withHttpsCertificateConfig(configContextFactory) {
        return new ExecutionConfigurationBuilderPromiseImpl(this._withHttpsCertificateConfigInternal(configContextFactory), this._client);
    }
    /** @internal */
    async _withArgumentsConfigInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withArgumentsConfig', rpcArgs);
        return new ExecutionConfigurationBuilderImpl(result, this._client);
    }
    /**
     * Adds a command line arguments configuration gatherer to the builder.
     * @returns The builder with the configuration gatherer added.
     */
    withArgumentsConfig() {
        return new ExecutionConfigurationBuilderPromiseImpl(this._withArgumentsConfigInternal(), this._client);
    }
    /** @internal */
    async _withEnvironmentVariablesConfigInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEnvironmentVariablesConfig', rpcArgs);
        return new ExecutionConfigurationBuilderImpl(result, this._client);
    }
    /**
     * Adds an environment variables configuration gatherer to the builder.
     * @returns The builder with the configuration gatherer added.
     */
    withEnvironmentVariablesConfig() {
        return new ExecutionConfigurationBuilderPromiseImpl(this._withEnvironmentVariablesConfigInternal(), this._client);
    }
    /** @internal */
    async _withCertificateTrustConfigInternal(configContextFactory) {
        const configContextFactoryId = registerCallback(async (argData) => {
            const arg = wrapIfHandle(argData);
            return await configContextFactory(arg);
        });
        const rpcArgs = { builder: this._handle, configContextFactory: configContextFactoryId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withCertificateTrustConfig', rpcArgs);
        return new ExecutionConfigurationBuilderImpl(result, this._client);
    }
    /**
     * Adds a certificate trust configuration gatherer to the builder.
     * @param configContextFactory A factory function to create the configuration context.
     * @returns The builder with the configuration gatherer added.
     */
    withCertificateTrustConfig(configContextFactory) {
        return new ExecutionConfigurationBuilderPromiseImpl(this._withCertificateTrustConfigInternal(configContextFactory), this._client);
    }
}
/**
 * Thenable wrapper for ExecutionConfigurationBuilder that enables fluent chaining.
 */
class ExecutionConfigurationBuilderPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    build(executionContext, options) {
        return new ExecutionConfigurationResultPromiseImpl(this._promise.then(obj => obj.build(executionContext, options)), this._client);
    }
    withHttpsCertificateConfig(configContextFactory) {
        return new ExecutionConfigurationBuilderPromiseImpl(this._promise.then(obj => obj.withHttpsCertificateConfig(configContextFactory)), this._client);
    }
    withArgumentsConfig() {
        return new ExecutionConfigurationBuilderPromiseImpl(this._promise.then(obj => obj.withArgumentsConfig()), this._client);
    }
    withEnvironmentVariablesConfig() {
        return new ExecutionConfigurationBuilderPromiseImpl(this._promise.then(obj => obj.withEnvironmentVariablesConfig()), this._client);
    }
    withCertificateTrustConfig(configContextFactory) {
        return new ExecutionConfigurationBuilderPromiseImpl(this._promise.then(obj => obj.withCertificateTrustConfig(configContextFactory)), this._client);
    }
}
// ============================================================================
// ExecutionConfigurationResultImpl
// ============================================================================
/** Configuration (arguments and environment variables) to apply to a specific resource. */
class ExecutionConfigurationResultImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    /**
     * Gets certificate trust execution-configuration data when present.
     * @returns The certificate trust data. When no additional data is present, an empty DTO is returned.
     */
    async getCertificateTrustData() {
        const rpcArgs = { configuration: this._handle };
        return await this._client.invokeCapability('Aspire.Hosting/getCertificateTrustData', rpcArgs);
    }
    /**
     * Gets HTTPS certificate execution-configuration data when present.
     * @returns The HTTPS certificate data. When no additional data is present, an empty DTO is returned.
     */
    async getHttpsCertificateData() {
        const rpcArgs = { configuration: this._handle };
        return await this._client.invokeCapability('Aspire.Hosting/getHttpsCertificateData', rpcArgs);
    }
}
/**
 * Thenable wrapper for ExecutionConfigurationResult that enables fluent chaining.
 */
class ExecutionConfigurationResultPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    getCertificateTrustData() {
        return this._promise.then(obj => obj.getCertificateTrustData());
    }
    getHttpsCertificateData() {
        return this._promise.then(obj => obj.getHttpsCertificateData());
    }
}
// ============================================================================
// HostEnvironmentImpl
// ============================================================================
/** Type class for HostEnvironment. */
class HostEnvironmentImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    environmentName = {
        get: async () => {
            return await this._client.invokeCapability('Microsoft.Extensions.Hosting/IHostEnvironment.environmentName', { context: this._handle });
        },
        set: async (value) => {
            await this._client.invokeCapability('Microsoft.Extensions.Hosting/IHostEnvironment.setEnvironmentName', { context: this._handle, value });
        }
    };
    applicationName = {
        get: async () => {
            return await this._client.invokeCapability('Microsoft.Extensions.Hosting/IHostEnvironment.applicationName', { context: this._handle });
        },
        set: async (value) => {
            await this._client.invokeCapability('Microsoft.Extensions.Hosting/IHostEnvironment.setApplicationName', { context: this._handle, value });
        }
    };
    contentRootPath = {
        get: async () => {
            return await this._client.invokeCapability('Microsoft.Extensions.Hosting/IHostEnvironment.contentRootPath', { context: this._handle });
        },
        set: async (value) => {
            await this._client.invokeCapability('Microsoft.Extensions.Hosting/IHostEnvironment.setContentRootPath', { context: this._handle, value });
        }
    };
    /**
     * Checks if the environment is Development.
     * @returns True if running in Development environment.
     */
    async isDevelopment() {
        const rpcArgs = { environment: this._handle };
        return await this._client.invokeCapability('Aspire.Hosting/isDevelopment', rpcArgs);
    }
    /**
     * Checks if the environment is Production.
     * @returns True if running in Production environment.
     */
    async isProduction() {
        const rpcArgs = { environment: this._handle };
        return await this._client.invokeCapability('Aspire.Hosting/isProduction', rpcArgs);
    }
    /**
     * Checks if the environment is Staging.
     * @returns True if running in Staging environment.
     */
    async isStaging() {
        const rpcArgs = { environment: this._handle };
        return await this._client.invokeCapability('Aspire.Hosting/isStaging', rpcArgs);
    }
    /**
     * Checks if the environment matches the specified name.
     * @param environmentName The environment name to compare against.
     * @returns True if the environment matches the specified name.
     */
    async isEnvironment(environmentName) {
        const rpcArgs = { environment: this._handle, environmentName };
        return await this._client.invokeCapability('Aspire.Hosting/isEnvironment', rpcArgs);
    }
}
/**
 * Thenable wrapper for HostEnvironment that enables fluent chaining.
 */
class HostEnvironmentPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    isDevelopment() {
        return this._promise.then(obj => obj.isDevelopment());
    }
    isProduction() {
        return this._promise.then(obj => obj.isProduction());
    }
    isStaging() {
        return this._promise.then(obj => obj.isStaging());
    }
    isEnvironment(environmentName) {
        return this._promise.then(obj => obj.isEnvironment(environmentName));
    }
}
// ============================================================================
// LoggerImpl
// ============================================================================
/** Type class for Logger. */
class LoggerImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    /** @internal */
    async _logInformationInternal(message) {
        const rpcArgs = { logger: this._handle, message };
        await this._client.invokeCapability('Aspire.Hosting/logInformation', rpcArgs);
        return this;
    }
    /** Logs an information message. */
    logInformation(message) {
        return new LoggerPromiseImpl(this._logInformationInternal(message), this._client);
    }
    /** @internal */
    async _logWarningInternal(message) {
        const rpcArgs = { logger: this._handle, message };
        await this._client.invokeCapability('Aspire.Hosting/logWarning', rpcArgs);
        return this;
    }
    /** Logs a warning message. */
    logWarning(message) {
        return new LoggerPromiseImpl(this._logWarningInternal(message), this._client);
    }
    /** @internal */
    async _logErrorInternal(message) {
        const rpcArgs = { logger: this._handle, message };
        await this._client.invokeCapability('Aspire.Hosting/logError', rpcArgs);
        return this;
    }
    /** Logs an error message. */
    logError(message) {
        return new LoggerPromiseImpl(this._logErrorInternal(message), this._client);
    }
    /** @internal */
    async _logDebugInternal(message) {
        const rpcArgs = { logger: this._handle, message };
        await this._client.invokeCapability('Aspire.Hosting/logDebug', rpcArgs);
        return this;
    }
    /** Logs a debug message. */
    logDebug(message) {
        return new LoggerPromiseImpl(this._logDebugInternal(message), this._client);
    }
    /** @internal */
    async _logInternal(level, message) {
        const rpcArgs = { logger: this._handle, level, message };
        await this._client.invokeCapability('Aspire.Hosting/log', rpcArgs);
        return this;
    }
    /** Logs a message with a specified log level. */
    log(level, message) {
        return new LoggerPromiseImpl(this._logInternal(level, message), this._client);
    }
}
/**
 * Thenable wrapper for Logger that enables fluent chaining.
 */
class LoggerPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    logInformation(message) {
        return new LoggerPromiseImpl(this._promise.then(obj => obj.logInformation(message)), this._client);
    }
    logWarning(message) {
        return new LoggerPromiseImpl(this._promise.then(obj => obj.logWarning(message)), this._client);
    }
    logError(message) {
        return new LoggerPromiseImpl(this._promise.then(obj => obj.logError(message)), this._client);
    }
    logDebug(message) {
        return new LoggerPromiseImpl(this._promise.then(obj => obj.logDebug(message)), this._client);
    }
    log(level, message) {
        return new LoggerPromiseImpl(this._promise.then(obj => obj.log(level, message)), this._client);
    }
}
// ============================================================================
// LoggerFactoryImpl
// ============================================================================
/** Type class for LoggerFactory. */
class LoggerFactoryImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    /** @internal */
    async _createLoggerInternal(categoryName) {
        const rpcArgs = { loggerFactory: this._handle, categoryName };
        const result = await this._client.invokeCapability('Aspire.Hosting/createLogger', rpcArgs);
        return new LoggerImpl(result, this._client);
    }
    /**
     * Creates a logger for the specified category name.
     * @param categoryName The category name.
     * @returns A logger handle.
     */
    createLogger(categoryName) {
        return new LoggerPromiseImpl(this._createLoggerInternal(categoryName), this._client);
    }
}
/**
 * Thenable wrapper for LoggerFactory that enables fluent chaining.
 */
class LoggerFactoryPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    createLogger(categoryName) {
        return new LoggerPromiseImpl(this._promise.then(obj => obj.createLogger(categoryName)), this._client);
    }
}
// ============================================================================
// ReportingStepImpl
// ============================================================================
/** Represents a publishing step, which can contain multiple tasks. */
class ReportingStepImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    /** @internal */
    async _createTaskInternal(statusText, cancellationToken) {
        const rpcArgs = { reportingStep: this._handle, statusText };
        if (cancellationToken !== undefined)
            rpcArgs.cancellationToken = CancellationToken.fromValue(cancellationToken);
        const result = await this._client.invokeCapability('Aspire.Hosting/createTask', rpcArgs);
        return new ReportingTaskImpl(result, this._client);
    }
    /**
     * Creates a reporting task with plain-text status text.
     * @param options Additional options.
     */
    createTask(statusText, options) {
        const cancellationToken = options?.cancellationToken;
        return new ReportingTaskPromiseImpl(this._createTaskInternal(statusText, cancellationToken), this._client);
    }
    /** @internal */
    async _createMarkdownTaskInternal(markdownString, cancellationToken) {
        const rpcArgs = { reportingStep: this._handle, markdownString };
        if (cancellationToken !== undefined)
            rpcArgs.cancellationToken = CancellationToken.fromValue(cancellationToken);
        const result = await this._client.invokeCapability('Aspire.Hosting/createMarkdownTask', rpcArgs);
        return new ReportingTaskImpl(result, this._client);
    }
    /**
     * Creates a reporting task with Markdown-formatted status text.
     * @param options Additional options.
     */
    createMarkdownTask(markdownString, options) {
        const cancellationToken = options?.cancellationToken;
        return new ReportingTaskPromiseImpl(this._createMarkdownTaskInternal(markdownString, cancellationToken), this._client);
    }
    /** @internal */
    async _logStepInternal(level, message) {
        const rpcArgs = { reportingStep: this._handle, level, message };
        await this._client.invokeCapability('Aspire.Hosting/logStep', rpcArgs);
        return this;
    }
    /** Logs a plain-text message for the reporting step. */
    logStep(level, message) {
        return new ReportingStepPromiseImpl(this._logStepInternal(level, message), this._client);
    }
    /** @internal */
    async _logStepMarkdownInternal(level, markdownString) {
        const rpcArgs = { reportingStep: this._handle, level, markdownString };
        await this._client.invokeCapability('Aspire.Hosting/logStepMarkdown', rpcArgs);
        return this;
    }
    /** Logs a Markdown-formatted message for the reporting step. */
    logStepMarkdown(level, markdownString) {
        return new ReportingStepPromiseImpl(this._logStepMarkdownInternal(level, markdownString), this._client);
    }
    /** @internal */
    async _completeStepInternal(completionText, completionState, cancellationToken) {
        const rpcArgs = { reportingStep: this._handle, completionText };
        if (completionState !== undefined)
            rpcArgs.completionState = completionState;
        if (cancellationToken !== undefined)
            rpcArgs.cancellationToken = CancellationToken.fromValue(cancellationToken);
        await this._client.invokeCapability('Aspire.Hosting/completeStep', rpcArgs);
        return this;
    }
    /**
     * Completes the reporting step with plain-text completion text.
     * @param options Additional options.
     */
    completeStep(completionText, options) {
        const completionState = options?.completionState;
        const cancellationToken = options?.cancellationToken;
        return new ReportingStepPromiseImpl(this._completeStepInternal(completionText, completionState, cancellationToken), this._client);
    }
    /** @internal */
    async _completeStepMarkdownInternal(markdownString, completionState, cancellationToken) {
        const rpcArgs = { reportingStep: this._handle, markdownString };
        if (completionState !== undefined)
            rpcArgs.completionState = completionState;
        if (cancellationToken !== undefined)
            rpcArgs.cancellationToken = CancellationToken.fromValue(cancellationToken);
        await this._client.invokeCapability('Aspire.Hosting/completeStepMarkdown', rpcArgs);
        return this;
    }
    /**
     * Completes the reporting step with Markdown-formatted completion text.
     * @param options Additional options.
     */
    completeStepMarkdown(markdownString, options) {
        const completionState = options?.completionState;
        const cancellationToken = options?.cancellationToken;
        return new ReportingStepPromiseImpl(this._completeStepMarkdownInternal(markdownString, completionState, cancellationToken), this._client);
    }
}
/**
 * Thenable wrapper for ReportingStep that enables fluent chaining.
 */
class ReportingStepPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    createTask(statusText, options) {
        return new ReportingTaskPromiseImpl(this._promise.then(obj => obj.createTask(statusText, options)), this._client);
    }
    createMarkdownTask(markdownString, options) {
        return new ReportingTaskPromiseImpl(this._promise.then(obj => obj.createMarkdownTask(markdownString, options)), this._client);
    }
    logStep(level, message) {
        return new ReportingStepPromiseImpl(this._promise.then(obj => obj.logStep(level, message)), this._client);
    }
    logStepMarkdown(level, markdownString) {
        return new ReportingStepPromiseImpl(this._promise.then(obj => obj.logStepMarkdown(level, markdownString)), this._client);
    }
    completeStep(completionText, options) {
        return new ReportingStepPromiseImpl(this._promise.then(obj => obj.completeStep(completionText, options)), this._client);
    }
    completeStepMarkdown(markdownString, options) {
        return new ReportingStepPromiseImpl(this._promise.then(obj => obj.completeStepMarkdown(markdownString, options)), this._client);
    }
}
// ============================================================================
// ReportingTaskImpl
// ============================================================================
/** Represents a publishing task, which belongs to a step. */
class ReportingTaskImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    /** @internal */
    async _updateTaskInternal(statusText, cancellationToken) {
        const rpcArgs = { reportingTask: this._handle, statusText };
        if (cancellationToken !== undefined)
            rpcArgs.cancellationToken = CancellationToken.fromValue(cancellationToken);
        await this._client.invokeCapability('Aspire.Hosting/updateTask', rpcArgs);
        return this;
    }
    /**
     * Updates the reporting task with plain-text status text.
     * @param options Additional options.
     */
    updateTask(statusText, options) {
        const cancellationToken = options?.cancellationToken;
        return new ReportingTaskPromiseImpl(this._updateTaskInternal(statusText, cancellationToken), this._client);
    }
    /** @internal */
    async _updateTaskMarkdownInternal(markdownString, cancellationToken) {
        const rpcArgs = { reportingTask: this._handle, markdownString };
        if (cancellationToken !== undefined)
            rpcArgs.cancellationToken = CancellationToken.fromValue(cancellationToken);
        await this._client.invokeCapability('Aspire.Hosting/updateTaskMarkdown', rpcArgs);
        return this;
    }
    /**
     * Updates the reporting task with Markdown-formatted status text.
     * @param options Additional options.
     */
    updateTaskMarkdown(markdownString, options) {
        const cancellationToken = options?.cancellationToken;
        return new ReportingTaskPromiseImpl(this._updateTaskMarkdownInternal(markdownString, cancellationToken), this._client);
    }
    /** @internal */
    async _completeTaskInternal(completionMessage, completionState, cancellationToken) {
        const rpcArgs = { reportingTask: this._handle };
        if (completionMessage !== undefined)
            rpcArgs.completionMessage = completionMessage;
        if (completionState !== undefined)
            rpcArgs.completionState = completionState;
        if (cancellationToken !== undefined)
            rpcArgs.cancellationToken = CancellationToken.fromValue(cancellationToken);
        await this._client.invokeCapability('Aspire.Hosting/completeTask', rpcArgs);
        return this;
    }
    /**
     * Completes the reporting task with plain-text completion text.
     * @param options Additional options.
     */
    completeTask(options) {
        const completionMessage = options?.completionMessage;
        const completionState = options?.completionState;
        const cancellationToken = options?.cancellationToken;
        return new ReportingTaskPromiseImpl(this._completeTaskInternal(completionMessage, completionState, cancellationToken), this._client);
    }
    /** @internal */
    async _completeTaskMarkdownInternal(markdownString, completionState, cancellationToken) {
        const rpcArgs = { reportingTask: this._handle, markdownString };
        if (completionState !== undefined)
            rpcArgs.completionState = completionState;
        if (cancellationToken !== undefined)
            rpcArgs.cancellationToken = CancellationToken.fromValue(cancellationToken);
        await this._client.invokeCapability('Aspire.Hosting/completeTaskMarkdown', rpcArgs);
        return this;
    }
    /**
     * Completes the reporting task with Markdown-formatted completion text.
     * @param options Additional options.
     */
    completeTaskMarkdown(markdownString, options) {
        const completionState = options?.completionState;
        const cancellationToken = options?.cancellationToken;
        return new ReportingTaskPromiseImpl(this._completeTaskMarkdownInternal(markdownString, completionState, cancellationToken), this._client);
    }
}
/**
 * Thenable wrapper for ReportingTask that enables fluent chaining.
 */
class ReportingTaskPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    updateTask(statusText, options) {
        return new ReportingTaskPromiseImpl(this._promise.then(obj => obj.updateTask(statusText, options)), this._client);
    }
    updateTaskMarkdown(markdownString, options) {
        return new ReportingTaskPromiseImpl(this._promise.then(obj => obj.updateTaskMarkdown(markdownString, options)), this._client);
    }
    completeTask(options) {
        return new ReportingTaskPromiseImpl(this._promise.then(obj => obj.completeTask(options)), this._client);
    }
    completeTaskMarkdown(markdownString, options) {
        return new ReportingTaskPromiseImpl(this._promise.then(obj => obj.completeTaskMarkdown(markdownString, options)), this._client);
    }
}
// ============================================================================
// ServiceProviderImpl
// ============================================================================
/** Type class for ServiceProvider. */
class ServiceProviderImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    /** @internal */
    async _getEventingInternal() {
        const rpcArgs = { serviceProvider: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/getEventing', rpcArgs);
        return new DistributedApplicationEventingImpl(result, this._client);
    }
    /**
     * Gets the distributed application eventing service from the service provider.
     * @returns The distributed application eventing handle.
     */
    getEventing() {
        return new DistributedApplicationEventingPromiseImpl(this._getEventingInternal(), this._client);
    }
    /** @internal */
    async _getLoggerFactoryInternal() {
        const rpcArgs = { serviceProvider: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/getLoggerFactory', rpcArgs);
        return new LoggerFactoryImpl(result, this._client);
    }
    /**
     * Gets the logger factory from the service provider.
     * @returns A logger factory handle.
     */
    getLoggerFactory() {
        return new LoggerFactoryPromiseImpl(this._getLoggerFactoryInternal(), this._client);
    }
    /** @internal */
    async _getResourceLoggerServiceInternal() {
        const rpcArgs = { serviceProvider: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/getResourceLoggerService', rpcArgs);
        return new ResourceLoggerServiceImpl(result, this._client);
    }
    /**
     * Gets the resource logger service from the service provider.
     * @returns A resource logger service handle.
     */
    getResourceLoggerService() {
        return new ResourceLoggerServicePromiseImpl(this._getResourceLoggerServiceInternal(), this._client);
    }
    /** @internal */
    async _getDistributedApplicationModelInternal() {
        const rpcArgs = { serviceProvider: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/getDistributedApplicationModel', rpcArgs);
        return new DistributedApplicationModelImpl(result, this._client);
    }
    /**
     * Gets the distributed application model from the service provider.
     * @returns The distributed application model handle.
     */
    getDistributedApplicationModel() {
        return new DistributedApplicationModelPromiseImpl(this._getDistributedApplicationModelInternal(), this._client);
    }
    /** @internal */
    async _getResourceNotificationServiceInternal() {
        const rpcArgs = { serviceProvider: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/getResourceNotificationService', rpcArgs);
        return new ResourceNotificationServiceImpl(result, this._client);
    }
    /**
     * Gets the resource notification service from the service provider.
     * @returns A resource notification service handle.
     */
    getResourceNotificationService() {
        return new ResourceNotificationServicePromiseImpl(this._getResourceNotificationServiceInternal(), this._client);
    }
    /** @internal */
    async _getResourceCommandServiceInternal() {
        const rpcArgs = { serviceProvider: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/getResourceCommandService', rpcArgs);
        return new ResourceCommandServiceImpl(result, this._client);
    }
    /**
     * Gets the resource command service from the service provider.
     * @returns A resource command service handle.
     */
    getResourceCommandService() {
        return new ResourceCommandServicePromiseImpl(this._getResourceCommandServiceInternal(), this._client);
    }
    /** @internal */
    async _getAspireStoreInternal() {
        const rpcArgs = { serviceProvider: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/getAspireStore', rpcArgs);
        return new AspireStoreImpl(result, this._client);
    }
    /**
     * Gets the Aspire store from the service provider.
     * @returns The Aspire store.
     */
    getAspireStore() {
        return new AspireStorePromiseImpl(this._getAspireStoreInternal(), this._client);
    }
    /** @internal */
    async _getUserSecretsManagerInternal() {
        const rpcArgs = { serviceProvider: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/getUserSecretsManager', rpcArgs);
        return new UserSecretsManagerImpl(result, this._client);
    }
    /**
     * Gets the user secrets manager from the service provider.
     * @returns A user secrets manager handle.
     */
    getUserSecretsManager() {
        return new UserSecretsManagerPromiseImpl(this._getUserSecretsManagerInternal(), this._client);
    }
}
/**
 * Thenable wrapper for ServiceProvider that enables fluent chaining.
 */
class ServiceProviderPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    getEventing() {
        return new DistributedApplicationEventingPromiseImpl(this._promise.then(obj => obj.getEventing()), this._client);
    }
    getLoggerFactory() {
        return new LoggerFactoryPromiseImpl(this._promise.then(obj => obj.getLoggerFactory()), this._client);
    }
    getResourceLoggerService() {
        return new ResourceLoggerServicePromiseImpl(this._promise.then(obj => obj.getResourceLoggerService()), this._client);
    }
    getDistributedApplicationModel() {
        return new DistributedApplicationModelPromiseImpl(this._promise.then(obj => obj.getDistributedApplicationModel()), this._client);
    }
    getResourceNotificationService() {
        return new ResourceNotificationServicePromiseImpl(this._promise.then(obj => obj.getResourceNotificationService()), this._client);
    }
    getResourceCommandService() {
        return new ResourceCommandServicePromiseImpl(this._promise.then(obj => obj.getResourceCommandService()), this._client);
    }
    getAspireStore() {
        return new AspireStorePromiseImpl(this._promise.then(obj => obj.getAspireStore()), this._client);
    }
    getUserSecretsManager() {
        return new UserSecretsManagerPromiseImpl(this._promise.then(obj => obj.getUserSecretsManager()), this._client);
    }
}
// ============================================================================
// UserSecretsManagerImpl
// ============================================================================
/** Defines an interface for managing user secrets with support for read and write operations. */
class UserSecretsManagerImpl {
    _handle;
    _client;
    constructor(_handle, _client) {
        this._handle = _handle;
        this._client = _client;
    }
    /** Serialize for JSON-RPC transport */
    toJSON() { return this._handle.toJSON(); }
    async isAvailable() {
        return await this._client.invokeCapability('Aspire.Hosting/IUserSecretsManager.isAvailable', { context: this._handle });
    }
    async filePath() {
        return await this._client.invokeCapability('Aspire.Hosting/IUserSecretsManager.filePath', { context: this._handle });
    }
    /**
     * Attempts to set a user secret value synchronously.
     * @param name The name of the secret.
     * @param value The value of the secret.
     * @returns True if the secret was set successfully; otherwise, false.
     */
    async trySetSecret(name, value) {
        const rpcArgs = { context: this._handle, name, value };
        return await this._client.invokeCapability('Aspire.Hosting/IUserSecretsManager.trySetSecret', rpcArgs);
    }
    /**
     * Attempts to delete a user secret value synchronously.
     *
     * The default implementation returns `false` so existing implementations remain compatible.
     * @param name The name of the secret.
     * @returns True if the secret was deleted successfully; otherwise, false.
     */
    async tryDeleteSecret(name) {
        const rpcArgs = { context: this._handle, name };
        return await this._client.invokeCapability('Aspire.Hosting/IUserSecretsManager.tryDeleteSecret', rpcArgs);
    }
    /** @internal */
    async _saveStateJsonInternal(json, cancellationToken) {
        const rpcArgs = { userSecretsManager: this._handle, json };
        if (cancellationToken !== undefined)
            rpcArgs.cancellationToken = CancellationToken.fromValue(cancellationToken);
        await this._client.invokeCapability('Aspire.Hosting/saveStateJson', rpcArgs);
        return this;
    }
    /**
     * Saves state to user secrets from a JSON string.
     * @param json The JSON object payload to persist.
     * @param options Additional options.
     */
    saveStateJson(json, options) {
        const cancellationToken = options?.cancellationToken;
        return new UserSecretsManagerPromiseImpl(this._saveStateJsonInternal(json, cancellationToken), this._client);
    }
    /** @internal */
    async _getOrSetSecretInternal(resourceBuilder, name, value) {
        resourceBuilder = isPromiseLike(resourceBuilder) ? await resourceBuilder : resourceBuilder;
        const rpcArgs = { userSecretsManager: this._handle, resourceBuilder, name, value };
        await this._client.invokeCapability('Aspire.Hosting/getOrSetSecret', rpcArgs);
        return this;
    }
    /**
     * Gets a secret value if it exists in configuration, or sets it to the provided value if it does not.
     * @param resourceBuilder A resource builder from the target application.
     * @param name The secret name.
     * @param value The value to persist when the secret is missing.
     */
    getOrSetSecret(resourceBuilder, name, value) {
        return new UserSecretsManagerPromiseImpl(this._getOrSetSecretInternal(resourceBuilder, name, value), this._client);
    }
}
/**
 * Thenable wrapper for UserSecretsManager that enables fluent chaining.
 */
class UserSecretsManagerPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    isAvailable() {
        return this._promise.then(obj => obj.isAvailable());
    }
    filePath() {
        return this._promise.then(obj => obj.filePath());
    }
    trySetSecret(name, value) {
        return this._promise.then(obj => obj.trySetSecret(name, value));
    }
    tryDeleteSecret(name) {
        return this._promise.then(obj => obj.tryDeleteSecret(name));
    }
    saveStateJson(json, options) {
        return new UserSecretsManagerPromiseImpl(this._promise.then(obj => obj.saveStateJson(json, options)), this._client);
    }
    getOrSetSecret(resourceBuilder, name, value) {
        return new UserSecretsManagerPromiseImpl(this._promise.then(obj => obj.getOrSetSecret(resourceBuilder, name, value)), this._client);
    }
}
// ============================================================================
// BunAppResourceImpl
// ============================================================================
/** A resource that represents a Bun application. */
class BunAppResourceImpl extends ResourceBuilderBase {
    constructor(handle, client) {
        super(handle, client);
    }
    async command() {
        return await this._client.invokeCapability('Aspire.Hosting.JavaScript/JavaScriptAppResource.command', { context: this._handle });
    }
    async workingDirectory() {
        return await this._client.invokeCapability('Aspire.Hosting.JavaScript/JavaScriptAppResource.workingDirectory', { context: this._handle });
    }
    async name() {
        return await this._client.invokeCapability('Aspire.Hosting.JavaScript/JavaScriptAppResource.name', { context: this._handle });
    }
    /** @internal */
    async _withContainerRegistryInternal(registry) {
        registry = isPromiseLike(registry) ? await registry : registry;
        const rpcArgs = { builder: this._handle, registry };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerRegistry', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Configures the resource to use the specified container registry for container image operations.
     *
     * This method adds a `ContainerRegistryReferenceAnnotation` to the resource,
     * indicating that the resource should use the specified container registry for container image operations.
     * @param registry The container registry resource builder.
     * @returns The resource builder for chaining.
     */
    withContainerRegistry(registry) {
        return new BunAppResourcePromiseImpl(this._withContainerRegistryInternal(registry), this._client);
    }
    /** @internal */
    async _withDockerfileBaseImageInternal(buildImage, runtimeImage) {
        const rpcArgs = { builder: this._handle };
        if (buildImage !== undefined)
            rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined)
            rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileBaseImage', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Configures custom base images for generated Dockerfiles.
     *
     * This extension method allows customization of the base images used in generated Dockerfiles.
     * For multi-stage Dockerfiles (e.g., Python with UV), you can specify separate build and runtime images.
     * Specify custom base images for a Python application:
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddPythonApp("myapp", "path/to/app", "main.py")
     * .WithDockerfileBaseImage(
     * buildImage: "ghcr.io/astral-sh/uv:python3.12-bookworm-slim",
     * runtimeImage: "python:3.12-slim-bookworm");
     * builder.Build().Run();
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileBaseImage(options) {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new BunAppResourcePromiseImpl(this._withDockerfileBaseImageInternal(buildImage, runtimeImage), this._client);
    }
    /** @internal */
    async _publishAsDockerFileInternal(configure) {
        const configureId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new ContainerResourceImpl(objHandle, this._client);
            await configure(obj);
        });
        const rpcArgs = { builder: this._handle, configure: configureId };
        const result = await this._client.invokeCapability('Aspire.Hosting/publishAsDockerFile', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Publishes an executable as a Docker file
     *
     * When the executable resource is converted to a container resource, the arguments to the executable
     * are not used. This is because arguments to the executable often contain physical paths that are not valid
     * in the container. The container can be set up with the correct arguments using the `configure` action.
     * @param configure Optional action to configure the container resource
     * @returns The resource builder.
     */
    publishAsDockerFile(configure) {
        return new BunAppResourcePromiseImpl(this._publishAsDockerFileInternal(configure), this._client);
    }
    /** @internal */
    async _withExecutableCommandInternal(command) {
        const rpcArgs = { builder: this._handle, command };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExecutableCommand', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Sets the command for the executable resource.
     * @param command Command.
     * @returns The resource builder.
     */
    withExecutableCommand(command) {
        return new BunAppResourcePromiseImpl(this._withExecutableCommandInternal(command), this._client);
    }
    /** @internal */
    async _withWorkingDirectoryInternal(workingDirectory) {
        const rpcArgs = { builder: this._handle, workingDirectory };
        const result = await this._client.invokeCapability('Aspire.Hosting/withWorkingDirectory', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Sets the working directory for the executable resource.
     * @param workingDirectory Working directory.
     * @returns The resource builder.
     */
    withWorkingDirectory(workingDirectory) {
        return new BunAppResourcePromiseImpl(this._withWorkingDirectoryInternal(workingDirectory), this._client);
    }
    /** @internal */
    async _withMcpServerInternal(path, endpointName) {
        const rpcArgs = { builder: this._handle };
        if (path !== undefined)
            rpcArgs.path = path;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withMcpServer', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Marks the resource as hosting a Model Context Protocol (MCP) server on the specified endpoint.
     *
     * This method adds an `McpServerEndpointAnnotation` to the resource, enabling the Aspire tooling
     * to discover and proxy the MCP server exposed by the resource.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withMcpServer(options) {
        const path = options?.path;
        const endpointName = options?.endpointName;
        return new BunAppResourcePromiseImpl(this._withMcpServerInternal(path, endpointName), this._client);
    }
    /** @internal */
    async _withOtlpExporterInternal(protocol) {
        const rpcArgs = { builder: this._handle };
        if (protocol !== undefined)
            rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability('Aspire.Hosting/withOtlpExporter', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Configures OTLP telemetry export
     * @param options Additional options.
     */
    withOtlpExporter(options) {
        const protocol = options?.protocol;
        return new BunAppResourcePromiseImpl(this._withOtlpExporterInternal(protocol), this._client);
    }
    /** @internal */
    async _withRequiredCommandInternal(command, helpLink) {
        const rpcArgs = { builder: this._handle, command };
        if (helpLink !== undefined)
            rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability('Aspire.Hosting/withRequiredCommand', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Declares that a resource requires a specific command/executable to be available on the local machine PATH before it can start.
     *
     * The command is considered valid if either:
     * 1. It is an absolute or relative path (contains a directory separator) that points to an existing file, or
     * 2. It is discoverable on the current process PATH (respecting PATHEXT on Windows).
     * If the command is not found, a warning message will be logged but the resource will be allowed to attempt to start.
     * @param command The command string (file name or path) that should be validated.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withRequiredCommand(command, options) {
        const helpLink = options?.helpLink;
        return new BunAppResourcePromiseImpl(this._withRequiredCommandInternal(command, helpLink), this._client);
    }
    /** @internal */
    async _withSessionLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withSessionLifetime', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a session lifetime.
     * @returns The resource builder.
     */
    withSessionLifetime() {
        return new BunAppResourcePromiseImpl(this._withSessionLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withPersistentLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPersistentLifetime', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime.
     * @returns The resource builder.
     */
    withPersistentLifetime() {
        return new BunAppResourcePromiseImpl(this._withPersistentLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withLifetimeOfInternal(sourceBuilder) {
        sourceBuilder = isPromiseLike(sourceBuilder) ? await sourceBuilder : sourceBuilder;
        const rpcArgs = { builder: this._handle, sourceBuilder };
        const result = await this._client.invokeCapability('Aspire.Hosting/withLifetimeOf', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to match the lifetime of another resource.
     *
     * The resource lifetime is evaluated from `sourceBuilder` when the application model is prepared, so later lifetime
     * changes to the source resource are reflected by this resource.
     * @param sourceBuilder The resource builder whose lifetime should be used.
     * @returns The resource builder.
     */
    withLifetimeOf(sourceBuilder) {
        return new BunAppResourcePromiseImpl(this._withLifetimeOfInternal(sourceBuilder), this._client);
    }
    /** @internal */
    async _withParentProcessLifetimeInternal(parentProcessId) {
        const rpcArgs = { builder: this._handle, parentProcessId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withParentProcessLifetime', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime that ends when a parent process exits.
     * @param parentProcessId The ID of the parent process to monitor.
     * @returns The resource builder.
     */
    withParentProcessLifetime(parentProcessId) {
        return new BunAppResourcePromiseImpl(this._withParentProcessLifetimeInternal(parentProcessId), this._client);
    }
    /** @internal */
    async _withEnvironmentInternal(name, value) {
        value = isPromiseLike(value) ? await value : value;
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEnvironment', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /** Sets an environment variable */
    withEnvironment(name, value) {
        return new BunAppResourcePromiseImpl(this._withEnvironmentInternal(name, value), this._client);
    }
    /** @internal */
    async _withEnvironmentCallbackInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new EnvironmentCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEnvironmentCallback', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Allows for the population of environment variables on a resource.
     * @param callback A callback that allows for deferred execution for computing many environment variables. This runs after resources have been allocated by the orchestrator and allows access to other resources to resolve computed data, e.g. connection strings, ports.
     * @returns The resource builder.
     */
    withEnvironmentCallback(callback) {
        return new BunAppResourcePromiseImpl(this._withEnvironmentCallbackInternal(callback), this._client);
    }
    /** @internal */
    async _withArgsInternal(args) {
        const rpcArgs = { builder: this._handle, args };
        const result = await this._client.invokeCapability('Aspire.Hosting/withArgs', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Adds arguments to be passed to a resource that supports arguments when it is launched.
     * @param args The arguments to be passed to the resource when it is started.
     * @returns The resource builder.
     */
    withArgs(args) {
        return new BunAppResourcePromiseImpl(this._withArgsInternal(args), this._client);
    }
    /** @internal */
    async _withArgsCallbackInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new CommandLineArgsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withArgsCallback', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Adds a callback to be executed with a list of command-line arguments when a resource is started.
     * @param callback A callback that allows for deferred execution for computing arguments. This runs after resources have been allocated by the orchestrator and allows access to other resources to resolve computed data, e.g. connection strings, ports.
     * @returns The resource builder.
     */
    withArgsCallback(callback) {
        return new BunAppResourcePromiseImpl(this._withArgsCallbackInternal(callback), this._client);
    }
    /** @internal */
    async _withReferenceEnvironmentInternal(options) {
        const rpcArgs = { builder: this._handle, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withReferenceEnvironment', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Configures how information is injected into environment variables when the resource references other resources.
     * @param options Options controlling which reference information is emitted.
     * @returns The resource builder.
     */
    withReferenceEnvironment(options) {
        return new BunAppResourcePromiseImpl(this._withReferenceEnvironmentInternal(options), this._client);
    }
    /** @internal */
    async _withReferenceInternal(source, connectionName, optional, name) {
        source = isPromiseLike(source) ? await source : source;
        const rpcArgs = { builder: this._handle, source };
        if (connectionName !== undefined)
            rpcArgs.connectionName = connectionName;
        if (optional !== undefined)
            rpcArgs.optional = optional;
        if (name !== undefined)
            rpcArgs.name = name;
        const result = await this._client.invokeCapability('Aspire.Hosting/withReference', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Adds a reference to another resource
     * @param options Additional options.
     */
    withReference(source, options) {
        const connectionName = options?.connectionName;
        const optional = options?.optional;
        const name = options?.name;
        return new BunAppResourcePromiseImpl(this._withReferenceInternal(source, connectionName, optional, name), this._client);
    }
    /** @internal */
    async _withEndpointCallbackInternal(endpointName, callback, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpointCallback', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Updates a named endpoint via callback
     * @param options Additional options.
     */
    withEndpointCallback(endpointName, callback, options) {
        const createIfNotExists = options?.createIfNotExists;
        return new BunAppResourcePromiseImpl(this._withEndpointCallbackInternal(endpointName, callback, createIfNotExists), this._client);
    }
    /** @internal */
    async _withHttpEndpointCallbackInternal(callback, name, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        if (name !== undefined)
            rpcArgs.name = name;
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpEndpointCallback', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Updates an HTTP endpoint via callback
     * @param options Additional options.
     */
    withHttpEndpointCallback(callback, options) {
        const name = options?.name;
        const createIfNotExists = options?.createIfNotExists;
        return new BunAppResourcePromiseImpl(this._withHttpEndpointCallbackInternal(callback, name, createIfNotExists), this._client);
    }
    /** @internal */
    async _withHttpsEndpointCallbackInternal(callback, name, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        if (name !== undefined)
            rpcArgs.name = name;
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpsEndpointCallback', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Updates an HTTPS endpoint via callback
     * @param options Additional options.
     */
    withHttpsEndpointCallback(callback, options) {
        const name = options?.name;
        const createIfNotExists = options?.createIfNotExists;
        return new BunAppResourcePromiseImpl(this._withHttpsEndpointCallbackInternal(callback, name, createIfNotExists), this._client);
    }
    /** @internal */
    async _withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (scheme !== undefined)
            rpcArgs.scheme = scheme;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        if (isExternal !== undefined)
            rpcArgs.isExternal = isExternal;
        if (protocol !== undefined)
            rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpoint', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Adds a network endpoint
     * @param options Additional options.
     * @returns The resource builder.
     */
    withEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const scheme = options?.scheme;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        const isExternal = options?.isExternal;
        const protocol = options?.protocol;
        return new BunAppResourcePromiseImpl(this._withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol), this._client);
    }
    /** @internal */
    async _withEndpointProxySupportInternal(proxyEnabled) {
        const rpcArgs = { builder: this._handle, proxyEnabled };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpointProxySupport', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Set whether a resource can use proxied endpoints or whether they should be disabled for all endpoints belonging to the resource. If set to `false`, endpoints belonging to the resource will ignore the configured proxy settings and run proxy-less.
     *
     * This method is intended to support scenarios with persistent lifetime resources where it is desirable for the resource to be accessible over the same
     * port whether the Aspire application is running or not. Proxied endpoints bind ports that are only accessible while the Aspire application is running.
     * The user needs to be careful to ensure that endpoints are using unique ports when disabling proxy support as by default for proxy-less
     * endpoints, Aspire will allocate the target port as the host port, which will increase the chance of port conflicts.
     * @param proxyEnabled Should endpoints for the resource support using a proxy?
     * @returns The resource builder.
     */
    withEndpointProxySupport(proxyEnabled) {
        return new BunAppResourcePromiseImpl(this._withEndpointProxySupportInternal(proxyEnabled), this._client);
    }
    /** @internal */
    async _withHttpEndpointInternal(port, targetPort, name, env, isProxied) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpEndpoint', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP endpoint
     *
     * If an endpoint with the same name already exists on the resource, the existing endpoint is updated
     * with any non-null parameter values. Parameters left as `null` will not modify the existing endpoint's values.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new BunAppResourcePromiseImpl(this._withHttpEndpointInternal(port, targetPort, name, env, isProxied), this._client);
    }
    /** @internal */
    async _withHttpsEndpointInternal(port, targetPort, name, env, isProxied) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpsEndpoint', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTPS endpoint
     *
     * If an endpoint with the same name already exists on the resource, the existing endpoint is updated
     * with any non-null parameter values. Parameters left as `null` will not modify the existing endpoint's values.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpsEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new BunAppResourcePromiseImpl(this._withHttpsEndpointInternal(port, targetPort, name, env, isProxied), this._client);
    }
    /** @internal */
    async _withExternalHttpEndpointsInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExternalHttpEndpoints', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Marks existing http or https endpoints on a resource as external.
     * @returns The resource builder.
     */
    withExternalHttpEndpoints() {
        return new BunAppResourcePromiseImpl(this._withExternalHttpEndpointsInternal(), this._client);
    }
    /**
     * Gets an endpoint reference
     * @param name The name of the endpoint.
     * @returns An `EndpointReference` that can be used to resolve the address of the endpoint after resource allocation has occurred.
     */
    getEndpoint(name) {
        const promise = (async () => {
            const rpcArgs = { builder: this._handle, name };
            const handle = await this._client.invokeCapability('Aspire.Hosting/getEndpoint', rpcArgs);
            return new EndpointReferenceImpl(handle, this._client);
        })();
        return new EndpointReferencePromiseImpl(promise, this._client);
    }
    /** @internal */
    async _asHttp2ServiceInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/asHttp2Service', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to mark all endpoints' transport as HTTP/2. This is useful for HTTP/2 services that need prior knowledge.
     * @returns The resource builder.
     */
    asHttp2Service() {
        return new BunAppResourcePromiseImpl(this._asHttp2ServiceInternal(), this._client);
    }
    /** @internal */
    async _withUrlsInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new ResourceUrlsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrls', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to customize the URLs displayed for the resource.
     * @param callback The callback that will customize URLs for the resource.
     * @returns The resource builder.
     */
    withUrls(callback) {
        return new BunAppResourcePromiseImpl(this._withUrlsInternal(callback), this._client);
    }
    /** @internal */
    async _withUrlInternal(url, displayText) {
        const rpcArgs = { builder: this._handle, url };
        if (displayText !== undefined)
            rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrl', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Adds or modifies displayed URLs
     * @param options Additional options.
     */
    withUrl(url, options) {
        const displayText = options?.displayText;
        return new BunAppResourcePromiseImpl(this._withUrlInternal(url, displayText), this._client);
    }
    /** @internal */
    async _withUrlForEndpointInternal(endpointName, callback) {
        const callbackId = registerCallback(async (objData) => {
            const obj = wrapIfHandle(objData);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrlForEndpoint', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to update the URL displayed for the endpoint with the specified name.
     * @param endpointName The name of the endpoint to customize the URL for.
     * @param callback The callback that will customize the URL.
     * @returns The resource builder.
     */
    withUrlForEndpoint(endpointName, callback) {
        return new BunAppResourcePromiseImpl(this._withUrlForEndpointInternal(endpointName, callback), this._client);
    }
    /** @internal */
    async _publishWithContainerFilesInternal(source, destinationPath) {
        source = isPromiseLike(source) ? await source : source;
        const rpcArgs = { builder: this._handle, source, destinationPath };
        const result = await this._client.invokeCapability('Aspire.Hosting/publishWithContainerFilesFromResource', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Configures the resource to copy container files from the specified source resource during publishing.
     * @param source The resource which contains the container files to be copied.
     * @param destinationPath The destination path within the resource's container where the files will be copied.
     */
    publishWithContainerFiles(source, destinationPath) {
        return new BunAppResourcePromiseImpl(this._publishWithContainerFilesInternal(source, destinationPath), this._client);
    }
    /** @internal */
    async _withContainerFilesSourceInternal(sourcePath) {
        const rpcArgs = { builder: this._handle, sourcePath };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerFilesSource', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Adds a container files source annotation to the resource being built, specifying the path to the container files source.
     * @param sourcePath The path to the container files source to associate with the resource. Cannot be null.
     * @returns The resource builder instance with the container files source annotation applied.
     */
    withContainerFilesSource(sourcePath) {
        return new BunAppResourcePromiseImpl(this._withContainerFilesSourceInternal(sourcePath), this._client);
    }
    /** @internal */
    async _clearContainerFilesSourcesInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/clearContainerFilesSources', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Removes any container files source annotation from the resource being built.
     * @returns The resource builder instance with the container files source annotation applied.
     */
    clearContainerFilesSources() {
        return new BunAppResourcePromiseImpl(this._clearContainerFilesSourcesInternal(), this._client);
    }
    /** @internal */
    async _excludeFromManifestInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromManifest', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Excludes a resource from being published to the manifest.
     * @returns The resource builder.
     */
    excludeFromManifest() {
        return new BunAppResourcePromiseImpl(this._excludeFromManifestInternal(), this._client);
    }
    /** @internal */
    async _waitForInternal(dependency, waitBehavior) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (waitBehavior !== undefined)
            rpcArgs.waitBehavior = waitBehavior;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitFor', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Waits for another resource to be ready
     * @param options Additional options.
     */
    waitFor(dependency, options) {
        const waitBehavior = options?.waitBehavior;
        return new BunAppResourcePromiseImpl(this._waitForInternal(dependency, waitBehavior), this._client);
    }
    /** @internal */
    async _waitForStartInternal(dependency, waitBehavior) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (waitBehavior !== undefined)
            rpcArgs.waitBehavior = waitBehavior;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitForStart', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Waits for another resource to start
     * @param options Additional options.
     */
    waitForStart(dependency, options) {
        const waitBehavior = options?.waitBehavior;
        return new BunAppResourcePromiseImpl(this._waitForStartInternal(dependency, waitBehavior), this._client);
    }
    /** @internal */
    async _withExplicitStartInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExplicitStart', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Prevents resource from starting automatically
     * @returns The resource builder.
     */
    withExplicitStart() {
        return new BunAppResourcePromiseImpl(this._withExplicitStartInternal(), this._client);
    }
    /** @internal */
    async _waitForCompletionInternal(dependency, exitCode) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitForResourceCompletion', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Waits for the dependency resource to enter the Exited or Finished state before starting the resource.
     * @param dependency The resource builder for the dependency resource.
     * @param options Additional options.
     * @returns The resource builder.
     */
    waitForCompletion(dependency, options) {
        const exitCode = options?.exitCode;
        return new BunAppResourcePromiseImpl(this._waitForCompletionInternal(dependency, exitCode), this._client);
    }
    /** @internal */
    async _withHealthCheckInternal(key) {
        const rpcArgs = { builder: this._handle, key };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHealthCheck', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Adds a health check by key
     * @param key The key for the health check.
     * @returns The resource builder.
     */
    withHealthCheck(key) {
        return new BunAppResourcePromiseImpl(this._withHealthCheckInternal(key), this._client);
    }
    /** @internal */
    async _withHttpHealthCheckInternal(path, statusCode, endpointName) {
        const rpcArgs = { builder: this._handle };
        if (path !== undefined)
            rpcArgs.path = path;
        if (statusCode !== undefined)
            rpcArgs.statusCode = statusCode;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpHealthCheck', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Adds a health check to the resource which is mapped to a specific endpoint.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpHealthCheck(options) {
        const path = options?.path;
        const statusCode = options?.statusCode;
        const endpointName = options?.endpointName;
        return new BunAppResourcePromiseImpl(this._withHttpHealthCheckInternal(path, statusCode, endpointName), this._client);
    }
    /** @internal */
    async _withCommandInternal(name, displayName, executeCommand, commandOptions) {
        const executeCommandId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined)
            rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability('Aspire.Hosting/withCommand', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Adds a resource command
     *
     * The `WithCommand` method is used to add commands to the resource. Commands are displayed in the dashboard
     * and can be executed by a user using the dashboard UI.
     * When a command is executed, the `executeCommand` callback is called and is run inside the Aspire host.
     * @param name The name of the command. The name uniquely identifies the command.
     * @param displayName The display name visible in UI.
     * @param executeCommand A callback that is executed when the command is executed. The callback is run inside the Aspire host. The callback result is used to indicate success or failure in the UI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withCommand(name, displayName, executeCommand, options) {
        const commandOptions = options?.commandOptions;
        return new BunAppResourcePromiseImpl(this._withCommandInternal(name, displayName, executeCommand, commandOptions), this._client);
    }
    /** @internal */
    async _withProcessCommandInternal(commandName, displayName, options) {
        const rpcArgs = { builder: this._handle, commandName, displayName, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommand', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /** Adds a command to the resource that starts a local process when invoked. */
    withProcessCommand(commandName, displayName, options) {
        return new BunAppResourcePromiseImpl(this._withProcessCommandInternal(commandName, displayName, options), this._client);
    }
    /** @internal */
    async _withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options) {
        const createProcessSpecId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await createProcessSpec(arg);
        });
        const rpcArgs = { builder: this._handle, commandName, displayName, createProcessSpec: createProcessSpecId };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommandFactory', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Adds a command to the resource that starts a local process created by a callback when invoked.
     * @param options Additional options.
     */
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new BunAppResourcePromiseImpl(this._withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options), this._client);
    }
    /** @internal */
    async _withHttpCommandInternal(path, displayName, options) {
        const rpcArgs = { builder: this._handle, path, displayName };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpCommand', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP resource command
     * @param options Additional options.
     */
    withHttpCommand(path, displayName, options) {
        return new BunAppResourcePromiseImpl(this._withHttpCommandInternal(path, displayName, options), this._client);
    }
    /** @internal */
    async _withDeveloperCertificateTrustInternal(trust) {
        const rpcArgs = { builder: this._handle, trust };
        const result = await this._client.invokeCapability('Aspire.Hosting/withDeveloperCertificateTrust', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Indicates whether developer certificates should be treated as trusted certificate authorities for the resource at run time. Currently this indicates trust for the ASP.NET Core developer certificate. The developer certificate will only be trusted when running in local development scenarios; in publish mode resources will use their default certificate trust.
     *
     * Disable trust for app host managed developer certificate(s) for a container resource.
     * ```
     * var container = builder.AddContainer("my-service", "my-service:latest")
     * .WithDeveloperCertificateTrust(false);
     * ```
     * Disable automatic trust for app host managed developer certificate(s), but explicitly enable it for a specific resource.
     * ```
     * var builder = DistributedApplication.CreateBuilder(new DistributedApplicationOptions()
     * {
     * Args = args,
     * TrustDeveloperCertificate = false,
     * });
     * var project = builder.AddProject<MyService>("my-service")
     * .WithDeveloperCertificateTrust(true);
     * ```
     * @param trust Indicates whether the developer certificate should be treated as trusted.
     * @returns The resource builder.
     */
    withDeveloperCertificateTrust(trust) {
        return new BunAppResourcePromiseImpl(this._withDeveloperCertificateTrustInternal(trust), this._client);
    }
    /** @internal */
    async _withCertificateTrustScopeInternal(scope) {
        const rpcArgs = { builder: this._handle, scope };
        const result = await this._client.invokeCapability('Aspire.Hosting/withCertificateTrustScope', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Sets the certificate trust scope
     *
     * The default scope if not overridden is `Append` which means that custom certificate
     * authorities should be appended to the default trusted certificate authorities for the resource. Setting the scope to
     * `Override` indicates the set of certificates in referenced
     * `CertificateAuthorityCollection` (and optionally Aspire developer certificiates) should be used as the
     * exclusive source of trust for a resource.
     * In all cases, this is a best effort implementation as not all resources support full customization of certificate
     * trust.
     * Set the scope for custom certificate authorities to override the default trusted certificate authorities for a container resource.
     * ```
     * var caCollection = builder.AddCertificateAuthorityCollection("my-cas")
     * .WithCertificate(new X509Certificate2("my-ca.pem"));
     * var container = builder.AddContainer("my-service", "my-service:latest")
     * .WithCertificateAuthorityCollection(caCollection)
     * .WithCertificateTrustScope(CertificateTrustScope.Override);
     * ```
     * @param scope The scope to apply to custom certificate authorities associated with the resource.
     * @returns The resource builder.
     */
    withCertificateTrustScope(scope) {
        return new BunAppResourcePromiseImpl(this._withCertificateTrustScopeInternal(scope), this._client);
    }
    /** @internal */
    async _withHttpsDeveloperCertificateInternal(password) {
        password = isPromiseLike(password) ? await password : password;
        const rpcArgs = { builder: this._handle };
        if (password !== undefined)
            rpcArgs.password = password;
        const result = await this._client.invokeCapability('Aspire.Hosting/withParameterHttpsDeveloperCertificate', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Indicates that a resource should use the developer certificate key pair for HTTPS endpoints at run time. Currently this indicates use of the ASP.NET Core developer certificate. The developer certificate will only be used when running in local development scenarios; in publish mode resources will use their default certificate configuration.
     *
     * Use the developer certificate for HTTPS/TLS endpoints on a container resource:
     * ```
     * builder.AddContainer("my-service", "my-image")
     * .WithHttpsDeveloperCertificate()
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpsDeveloperCertificate(options) {
        let password = options?.password;
        return new BunAppResourcePromiseImpl(this._withHttpsDeveloperCertificateInternal(password), this._client);
    }
    /** @internal */
    async _withoutHttpsCertificateInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withoutHttpsCertificate', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Disable HTTPS/TLS server certificate configuration for the resource. No HTTPS/TLS termination configuration will be applied.
     *
     * Disable HTTPS certificate configuration for a Redis resource:
     * ```
     * var redis = builder.AddRedis("cache")
     * .WithoutHttpsCertificate();
     * ```
     * @returns The resource builder.
     */
    withoutHttpsCertificate() {
        return new BunAppResourcePromiseImpl(this._withoutHttpsCertificateInternal(), this._client);
    }
    /** @internal */
    async _withRelationshipInternal(resourceBuilder, type) {
        resourceBuilder = isPromiseLike(resourceBuilder) ? await resourceBuilder : resourceBuilder;
        const rpcArgs = { builder: this._handle, resourceBuilder, type };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderRelationship', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Adds a relationship to another resource using its builder.
     * @param resourceBuilder The resource builder that the relationship is to.
     * @param type The relationship type.
     * @returns A resource builder.
     */
    withRelationship(resourceBuilder, type) {
        return new BunAppResourcePromiseImpl(this._withRelationshipInternal(resourceBuilder, type), this._client);
    }
    /** @internal */
    async _withParentRelationshipInternal(parent) {
        parent = isPromiseLike(parent) ? await parent : parent;
        const rpcArgs = { builder: this._handle, parent };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderParentRelationship', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Sets the parent relationship
     * @param parent The parent of `builder`.
     * @returns A resource builder.
     */
    withParentRelationship(parent) {
        return new BunAppResourcePromiseImpl(this._withParentRelationshipInternal(parent), this._client);
    }
    /** @internal */
    async _withChildRelationshipInternal(child) {
        child = isPromiseLike(child) ? await child : child;
        const rpcArgs = { builder: this._handle, child };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderChildRelationship', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Sets a child relationship
     * @param child The child of `builder`.
     * @returns A resource builder.
     */
    withChildRelationship(child) {
        return new BunAppResourcePromiseImpl(this._withChildRelationshipInternal(child), this._client);
    }
    /** @internal */
    async _withIconNameInternal(iconName, iconVariant) {
        const rpcArgs = { builder: this._handle, iconName };
        if (iconVariant !== undefined)
            rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability('Aspire.Hosting/withIconName', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Specifies the icon to use when displaying the resource in the dashboard.
     * @param iconName The name of the FluentUI icon to use. See https://aka.ms/fluentui-system-icons for available icons.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withIconName(iconName, options) {
        const iconVariant = options?.iconVariant;
        return new BunAppResourcePromiseImpl(this._withIconNameInternal(iconName, iconVariant), this._client);
    }
    /** @internal */
    async _withComputeEnvironmentInternal(computeEnvironmentResource) {
        computeEnvironmentResource = isPromiseLike(computeEnvironmentResource) ? await computeEnvironmentResource : computeEnvironmentResource;
        const rpcArgs = { builder: this._handle, computeEnvironmentResource };
        const result = await this._client.invokeCapability('Aspire.Hosting/withComputeEnvironment', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Configures the compute environment for the compute resource.
     *
     * This method allows associating a specific compute environment with the compute resource.
     * @param computeEnvironmentResource The compute environment resource to associate with the compute resource.
     * @returns The resource builder.
     */
    withComputeEnvironment(computeEnvironmentResource) {
        return new BunAppResourcePromiseImpl(this._withComputeEnvironmentInternal(computeEnvironmentResource), this._client);
    }
    /** @internal */
    async _withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName) {
        const rpcArgs = { builder: this._handle, probeType };
        if (path !== undefined)
            rpcArgs.path = path;
        if (initialDelaySeconds !== undefined)
            rpcArgs.initialDelaySeconds = initialDelaySeconds;
        if (periodSeconds !== undefined)
            rpcArgs.periodSeconds = periodSeconds;
        if (timeoutSeconds !== undefined)
            rpcArgs.timeoutSeconds = timeoutSeconds;
        if (failureThreshold !== undefined)
            rpcArgs.failureThreshold = failureThreshold;
        if (successThreshold !== undefined)
            rpcArgs.successThreshold = successThreshold;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpProbe', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP health probe to the resource
     * @param options Additional options.
     */
    withHttpProbe(probeType, options) {
        const path = options?.path;
        const initialDelaySeconds = options?.initialDelaySeconds;
        const periodSeconds = options?.periodSeconds;
        const timeoutSeconds = options?.timeoutSeconds;
        const failureThreshold = options?.failureThreshold;
        const successThreshold = options?.successThreshold;
        const endpointName = options?.endpointName;
        return new BunAppResourcePromiseImpl(this._withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName), this._client);
    }
    /** @internal */
    async _excludeFromMcpInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromMcp', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Exclude the resource from MCP operations using the Aspire MCP server. The resource is excluded from results that return resources, console logs and telemetry.
     * @returns The resource builder.
     */
    excludeFromMcp() {
        return new BunAppResourcePromiseImpl(this._excludeFromMcpInternal(), this._client);
    }
    /** @internal */
    async _withHiddenInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHidden', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists
     *
     * Use this method to hide resources that are implementation details and should never be displayed by default.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @returns The resource builder.
     */
    withHidden() {
        return new BunAppResourcePromiseImpl(this._withHiddenInternal(), this._client);
    }
    /** @internal */
    async _withHiddenOnCompletionInternal(exitCode, exitCodes) {
        const rpcArgs = { builder: this._handle };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        if (exitCodes !== undefined)
            rpcArgs.exitCodes = exitCodes;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHiddenOnCompletion', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists after successful completion
     *
     * This method is useful for one-off resources such as setup scripts, migrations, or build steps that should remain visible while running
     * and then be hidden after successful completion.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHiddenOnCompletion(options) {
        const exitCode = options?.exitCode;
        const exitCodes = options?.exitCodes;
        return new BunAppResourcePromiseImpl(this._withHiddenOnCompletionInternal(exitCode, exitCodes), this._client);
    }
    /** @internal */
    async _withImagePushOptionsInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ContainerImagePushOptionsCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImagePushOptions', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Adds an asynchronous callback to configure container image push options for the resource.
     *
     * This method allows customization of how container images are named and tagged when pushed to a registry using an asynchronous callback.
     * Use this overload when the callback needs to perform asynchronous operations such as retrieving configuration values from external sources.
     * The callback receives a `ContainerImagePushOptionsCallbackContext` that provides access to the resource
     * and the `ContainerImagePushOptions` that can be modified.
     * Multiple callbacks can be registered on the same resource, and they will be invoked in the order they were added.
     * @param callback The asynchronous callback to configure push options.
     * @returns The resource builder.
     */
    withImagePushOptions(callback) {
        return new BunAppResourcePromiseImpl(this._withImagePushOptionsInternal(callback), this._client);
    }
    /** @internal */
    async _withRemoteImageNameInternal(remoteImageName) {
        const rpcArgs = { builder: this._handle, remoteImageName };
        const result = await this._client.invokeCapability('Aspire.Hosting/withRemoteImageName', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Sets the remote image name (without registry endpoint or tag) for container push operations.
     *
     * Use this with `withRemoteImageTag` to fully customize the image reference used for container push operations.
     * @param remoteImageName The remote image name (e.g., "myapp" or "myorg/myapp").
     * @returns The resource builder.
     */
    withRemoteImageName(remoteImageName) {
        return new BunAppResourcePromiseImpl(this._withRemoteImageNameInternal(remoteImageName), this._client);
    }
    /** @internal */
    async _withRemoteImageTagInternal(remoteImageTag) {
        const rpcArgs = { builder: this._handle, remoteImageTag };
        const result = await this._client.invokeCapability('Aspire.Hosting/withRemoteImageTag', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Sets the remote image tag for container push operations.
     *
     * Use this with `withRemoteImageName` to fully customize the image reference used for container push operations.
     * @param remoteImageTag The remote image tag (e.g., "latest", "v1.0.0").
     * @returns The resource builder.
     */
    withRemoteImageTag(remoteImageTag) {
        return new BunAppResourcePromiseImpl(this._withRemoteImageTagInternal(remoteImageTag), this._client);
    }
    /** @internal */
    async _withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new PipelineStepContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined)
            rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined)
            rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined)
            rpcArgs.tags = tags;
        if (description !== undefined)
            rpcArgs.description = description;
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineStepFactory', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Adds a pipeline step to the resource that will be executed during deployment.
     * @param stepName The unique name of the pipeline step.
     * @param callback The callback to execute when the step runs.
     * @param options Additional options.
     * @returns The resource builder for chaining.
     */
    withPipelineStepFactory(stepName, callback, options) {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new BunAppResourcePromiseImpl(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description), this._client);
    }
    /** @internal */
    async _withPipelineConfigurationInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new PipelineConfigurationContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineConfiguration', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to be executed during the pipeline configuration phase, allowing modification of step dependencies and relationships.
     * @param callback The callback function to execute during the configuration phase.
     * @returns The resource builder for chaining.
     */
    withPipelineConfiguration(callback) {
        return new BunAppResourcePromiseImpl(this._withPipelineConfigurationInternal(callback), this._client);
    }
    /**
     * Gets the name of the resource from a builder.
     *
     * Why this wrapper exists: This capability accesses a nested property
     * (`resource.Resource.Name`) which requires a wrapper method. There is no single
     * .NET method that returns just the resource name that could be annotated directly.
     * @returns The resource name.
     */
    async getResourceName() {
        const rpcArgs = { resource: this._handle };
        return await this._client.invokeCapability('Aspire.Hosting/getResourceName', rpcArgs);
    }
    /** @internal */
    async _onBeforeResourceStartedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new BeforeResourceStartedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onBeforeResourceStarted', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the BeforeResourceStarted event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onBeforeResourceStarted(callback) {
        return new BunAppResourcePromiseImpl(this._onBeforeResourceStartedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceStoppedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceStoppedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceStopped', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceStopped event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceStopped(callback) {
        return new BunAppResourcePromiseImpl(this._onResourceStoppedInternal(callback), this._client);
    }
    /** @internal */
    async _onInitializeResourceInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new InitializeResourceEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onInitializeResource', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the InitializeResource event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onInitializeResource(callback) {
        return new BunAppResourcePromiseImpl(this._onInitializeResourceInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceEndpointsAllocatedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceEndpointsAllocatedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceEndpointsAllocated', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceEndpointsAllocated event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceEndpointsAllocated(callback) {
        return new BunAppResourcePromiseImpl(this._onResourceEndpointsAllocatedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceReadyInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceReadyEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceReady', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceReady event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceReady(callback) {
        return new BunAppResourcePromiseImpl(this._onResourceReadyInternal(callback), this._client);
    }
    /**
     * Creates an execution configuration builder for the specified resource.
     * @returns The execution configuration builder.
     */
    createExecutionConfiguration() {
        const promise = (async () => {
            const rpcArgs = { resource: this._handle };
            const handle = await this._client.invokeCapability('Aspire.Hosting/createExecutionConfiguration', rpcArgs);
            return new ExecutionConfigurationBuilderImpl(handle, this._client);
        })();
        return new ExecutionConfigurationBuilderPromiseImpl(promise, this._client);
    }
    /** @internal */
    async _publishAsStaticWebsiteInternal(apiPath, apiTarget, outputPath, stripPrefix, targetEndpointName) {
        apiTarget = isPromiseLike(apiTarget) ? await apiTarget : apiTarget;
        const rpcArgs = { builder: this._handle };
        if (apiPath !== undefined)
            rpcArgs.apiPath = apiPath;
        if (apiTarget !== undefined)
            rpcArgs.apiTarget = apiTarget;
        if (outputPath !== undefined)
            rpcArgs.outputPath = outputPath;
        if (stripPrefix !== undefined)
            rpcArgs.stripPrefix = stripPrefix;
        if (targetEndpointName !== undefined)
            rpcArgs.targetEndpointName = targetEndpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/publishAsStaticWebsite', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Publishes the JavaScript application as a standalone static website using YARP.
     * @param options Additional options.
     */
    publishAsStaticWebsite(options) {
        const apiPath = options?.apiPath;
        let apiTarget = options?.apiTarget;
        const outputPath = options?.outputPath;
        const stripPrefix = options?.stripPrefix;
        const targetEndpointName = options?.targetEndpointName;
        return new BunAppResourcePromiseImpl(this._publishAsStaticWebsiteInternal(apiPath, apiTarget, outputPath, stripPrefix, targetEndpointName), this._client);
    }
    /** @internal */
    async _publishAsNodeServerInternal(entryPoint, outputPath) {
        const rpcArgs = { builder: this._handle, entryPoint };
        if (outputPath !== undefined)
            rpcArgs.outputPath = outputPath;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/publishAsNodeServer', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Configures the JavaScript application to publish as a standalone Node.js server that runs a built artifact directly.
     *
     * Use this method for frameworks that produce a Node.js server artifact during the build and recommend
     * running that artifact directly in production rather than invoking a package manager script at runtime.
     * The application source is still built using the configured package manager and build script; this method
     * only changes the publish-time runtime container shape.
     * The container files source path is automatically set to `outputPath` so that only
     * the built output directory is copied into the runtime container, not the full application source.
     * @param entryPoint The relative path to the Node.js entry point to execute in the published container after the build completes, such as `.output/server/index.js` or `build/index.js`.
     * @param options Additional options.
     * @returns The updated resource builder.
     */
    publishAsNodeServer(entryPoint, options) {
        const outputPath = options?.outputPath;
        return new BunAppResourcePromiseImpl(this._publishAsNodeServerInternal(entryPoint, outputPath), this._client);
    }
    /** @internal */
    async _publishAsPackageScriptInternal(scriptName, runScriptArguments) {
        const rpcArgs = { builder: this._handle };
        if (scriptName !== undefined)
            rpcArgs.scriptName = scriptName;
        if (runScriptArguments !== undefined)
            rpcArgs.runScriptArguments = runScriptArguments;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/publishAsPackageScript', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Configures the JavaScript application to publish as a Node.js server that uses a `package.json` script at runtime.
     *
     * Use this method for frameworks where the production server depends on packages in `node_modules` at runtime.
     * The resulting container includes the full application with production dependencies installed.
     * This method is appropriate for frameworks like Nuxt (where `useAsyncData`/`useFetch` requires the
     * full Nitro environment), Remix (where `react-router-serve` is an npm dependency), and Astro SSR
     * (where the built entry point imports unbundled `@astrojs/*` packages).
     * For frameworks that produce a self-contained server artifact that does not require `node_modules`,
     * use `PublishAsNodeServer``1` instead for a smaller runtime image.
     * @param options Additional options.
     * @returns The updated resource builder.
     */
    publishAsPackageScript(options) {
        const scriptName = options?.scriptName;
        const runScriptArguments = options?.runScriptArguments;
        return new BunAppResourcePromiseImpl(this._publishAsPackageScriptInternal(scriptName, runScriptArguments), this._client);
    }
    /** @internal */
    async _withNpmInternal(install, installCommand, installArgs) {
        const rpcArgs = { resource: this._handle };
        if (install !== undefined)
            rpcArgs.install = install;
        if (installCommand !== undefined)
            rpcArgs.installCommand = installCommand;
        if (installArgs !== undefined)
            rpcArgs.installArgs = installArgs;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/withNpm', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Configures the Node.js resource to use npm as the package manager and optionally installs packages before the application starts.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withNpm(options) {
        const install = options?.install;
        const installCommand = options?.installCommand;
        const installArgs = options?.installArgs;
        return new BunAppResourcePromiseImpl(this._withNpmInternal(install, installCommand, installArgs), this._client);
    }
    /** @internal */
    async _withBunInternal(install, installArgs) {
        const rpcArgs = { resource: this._handle };
        if (install !== undefined)
            rpcArgs.install = install;
        if (installArgs !== undefined)
            rpcArgs.installArgs = installArgs;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/withBun', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Configures the JavaScript resource to use Bun as the package manager and optionally installs packages before the application starts.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withBun(options) {
        const install = options?.install;
        const installArgs = options?.installArgs;
        return new BunAppResourcePromiseImpl(this._withBunInternal(install, installArgs), this._client);
    }
    /** @internal */
    async _withYarnInternal(install, installArgs) {
        const rpcArgs = { resource: this._handle };
        if (install !== undefined)
            rpcArgs.install = install;
        if (installArgs !== undefined)
            rpcArgs.installArgs = installArgs;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/withYarn', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Configures the Node.js resource to use yarn as the package manager and optionally installs packages before the application starts.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withYarn(options) {
        const install = options?.install;
        const installArgs = options?.installArgs;
        return new BunAppResourcePromiseImpl(this._withYarnInternal(install, installArgs), this._client);
    }
    /** @internal */
    async _withPnpmInternal(install, installArgs) {
        const rpcArgs = { resource: this._handle };
        if (install !== undefined)
            rpcArgs.install = install;
        if (installArgs !== undefined)
            rpcArgs.installArgs = installArgs;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/withPnpm', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Configures the Node.js resource to use pnpm as the package manager and optionally installs packages before the application starts.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withPnpm(options) {
        const install = options?.install;
        const installArgs = options?.installArgs;
        return new BunAppResourcePromiseImpl(this._withPnpmInternal(install, installArgs), this._client);
    }
    /** @internal */
    async _withBuildScriptInternal(scriptName, args) {
        const rpcArgs = { resource: this._handle, scriptName };
        if (args !== undefined)
            rpcArgs.args = args;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/withBuildScript', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Adds a build script annotation to the resource builder using the specified command-line arguments.
     *
     * Use this method to specify custom build scripts for JavaScript application resources during
     * deployment.
     * @param scriptName The name of the script to be executed when the resource is built.
     * @param options Additional options.
     * @returns The same resource builder instance with the build script annotation applied.
     */
    withBuildScript(scriptName, options) {
        const args = options?.args;
        return new BunAppResourcePromiseImpl(this._withBuildScriptInternal(scriptName, args), this._client);
    }
    /** @internal */
    async _withRunScriptInternal(scriptName, args) {
        const rpcArgs = { resource: this._handle, scriptName };
        if (args !== undefined)
            rpcArgs.args = args;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/withRunScript', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Adds a run script annotation to the specified JavaScript application resource builder, specifying the script to execute and its arguments during run mode.
     *
     * Use this method to specify a custom script and its arguments that should be executed when the resource is executed
     * in RunMode.
     * @param scriptName The name of the script to be executed when the resource is run.
     * @param options Additional options.
     * @returns The same resource builder instance with the run script annotation applied, enabling further configuration.
     */
    withRunScript(scriptName, options) {
        const args = options?.args;
        return new BunAppResourcePromiseImpl(this._withRunScriptInternal(scriptName, args), this._client);
    }
    /** @internal */
    async _withBrowserDebuggerInternal(browser) {
        const rpcArgs = { builder: this._handle };
        if (browser !== undefined)
            rpcArgs.browser = browser;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/withBrowserDebugger', rpcArgs);
        return new BunAppResourceImpl(result, this._client);
    }
    /**
     * Configures a browser debugger for the JavaScript application resource, enabling browser-based debugging through a child resource that launches when the parent application is ready.
     *
     * This method creates a child `BrowserDebuggerResource` that waits for the parent JavaScript
     * application to start, then launches a browser debug session targeting the parent's HTTP or HTTPS endpoint.
     * The parent resource must have at least one HTTP or HTTPS endpoint configured.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withBrowserDebugger(options) {
        const browser = options?.browser;
        return new BunAppResourcePromiseImpl(this._withBrowserDebuggerInternal(browser), this._client);
    }
}
/**
 * Thenable wrapper for BunAppResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
class BunAppResourcePromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    command() {
        return this._promise.then(obj => obj.command());
    }
    workingDirectory() {
        return this._promise.then(obj => obj.workingDirectory());
    }
    name() {
        return this._promise.then(obj => obj.name());
    }
    withContainerRegistry(registry) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withContainerRegistry(registry)), this._client);
    }
    withDockerfileBaseImage(options) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileBaseImage(options)), this._client);
    }
    publishAsDockerFile(configure) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.publishAsDockerFile(configure)), this._client);
    }
    withExecutableCommand(command) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withExecutableCommand(command)), this._client);
    }
    withWorkingDirectory(workingDirectory) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withWorkingDirectory(workingDirectory)), this._client);
    }
    withMcpServer(options) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withMcpServer(options)), this._client);
    }
    withOtlpExporter(options) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withOtlpExporter(options)), this._client);
    }
    withRequiredCommand(command, options) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withRequiredCommand(command, options)), this._client);
    }
    withSessionLifetime() {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withSessionLifetime()), this._client);
    }
    withPersistentLifetime() {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withPersistentLifetime()), this._client);
    }
    withLifetimeOf(sourceBuilder) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withLifetimeOf(sourceBuilder)), this._client);
    }
    withParentProcessLifetime(parentProcessId) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withParentProcessLifetime(parentProcessId)), this._client);
    }
    withEnvironment(name, value) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withEnvironment(name, value)), this._client);
    }
    withEnvironmentCallback(callback) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withEnvironmentCallback(callback)), this._client);
    }
    withArgs(args) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withArgs(args)), this._client);
    }
    withArgsCallback(callback) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withArgsCallback(callback)), this._client);
    }
    withReferenceEnvironment(options) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withReferenceEnvironment(options)), this._client);
    }
    withReference(source, options) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withReference(source, options)), this._client);
    }
    withEndpointCallback(endpointName, callback, options) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withEndpointCallback(endpointName, callback, options)), this._client);
    }
    withHttpEndpointCallback(callback, options) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpEndpointCallback(callback, options)), this._client);
    }
    withHttpsEndpointCallback(callback, options) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpsEndpointCallback(callback, options)), this._client);
    }
    withEndpoint(options) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withEndpoint(options)), this._client);
    }
    withEndpointProxySupport(proxyEnabled) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withEndpointProxySupport(proxyEnabled)), this._client);
    }
    withHttpEndpoint(options) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpEndpoint(options)), this._client);
    }
    withHttpsEndpoint(options) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpsEndpoint(options)), this._client);
    }
    withExternalHttpEndpoints() {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withExternalHttpEndpoints()), this._client);
    }
    getEndpoint(name) {
        return new EndpointReferencePromiseImpl(this._promise.then(obj => obj.getEndpoint(name)), this._client);
    }
    asHttp2Service() {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.asHttp2Service()), this._client);
    }
    withUrls(callback) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withUrls(callback)), this._client);
    }
    withUrl(url, options) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withUrl(url, options)), this._client);
    }
    withUrlForEndpoint(endpointName, callback) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)), this._client);
    }
    publishWithContainerFiles(source, destinationPath) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.publishWithContainerFiles(source, destinationPath)), this._client);
    }
    withContainerFilesSource(sourcePath) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withContainerFilesSource(sourcePath)), this._client);
    }
    clearContainerFilesSources() {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.clearContainerFilesSources()), this._client);
    }
    excludeFromManifest() {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.excludeFromManifest()), this._client);
    }
    waitFor(dependency, options) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.waitFor(dependency, options)), this._client);
    }
    waitForStart(dependency, options) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.waitForStart(dependency, options)), this._client);
    }
    withExplicitStart() {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withExplicitStart()), this._client);
    }
    waitForCompletion(dependency, options) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.waitForCompletion(dependency, options)), this._client);
    }
    withHealthCheck(key) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withHealthCheck(key)), this._client);
    }
    withHttpHealthCheck(options) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpHealthCheck(options)), this._client);
    }
    withCommand(name, displayName, executeCommand, options) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)), this._client);
    }
    withProcessCommand(commandName, displayName, options) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommand(commandName, displayName, options)), this._client);
    }
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommandFactory(commandName, displayName, createProcessSpec, options)), this._client);
    }
    withHttpCommand(path, displayName, options) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpCommand(path, displayName, options)), this._client);
    }
    withDeveloperCertificateTrust(trust) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withDeveloperCertificateTrust(trust)), this._client);
    }
    withCertificateTrustScope(scope) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withCertificateTrustScope(scope)), this._client);
    }
    withHttpsDeveloperCertificate(options) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpsDeveloperCertificate(options)), this._client);
    }
    withoutHttpsCertificate() {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withoutHttpsCertificate()), this._client);
    }
    withRelationship(resourceBuilder, type) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withRelationship(resourceBuilder, type)), this._client);
    }
    withParentRelationship(parent) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withParentRelationship(parent)), this._client);
    }
    withChildRelationship(child) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withChildRelationship(child)), this._client);
    }
    withIconName(iconName, options) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withIconName(iconName, options)), this._client);
    }
    withComputeEnvironment(computeEnvironmentResource) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withComputeEnvironment(computeEnvironmentResource)), this._client);
    }
    withHttpProbe(probeType, options) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpProbe(probeType, options)), this._client);
    }
    excludeFromMcp() {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.excludeFromMcp()), this._client);
    }
    withHidden() {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withHidden()), this._client);
    }
    withHiddenOnCompletion(options) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withHiddenOnCompletion(options)), this._client);
    }
    withImagePushOptions(callback) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withImagePushOptions(callback)), this._client);
    }
    withRemoteImageName(remoteImageName) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withRemoteImageName(remoteImageName)), this._client);
    }
    withRemoteImageTag(remoteImageTag) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withRemoteImageTag(remoteImageTag)), this._client);
    }
    withPipelineStepFactory(stepName, callback, options) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)), this._client);
    }
    withPipelineConfiguration(callback) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withPipelineConfiguration(callback)), this._client);
    }
    getResourceName() {
        return this._promise.then(obj => obj.getResourceName());
    }
    onBeforeResourceStarted(callback) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.onBeforeResourceStarted(callback)), this._client);
    }
    onResourceStopped(callback) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.onResourceStopped(callback)), this._client);
    }
    onInitializeResource(callback) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.onInitializeResource(callback)), this._client);
    }
    onResourceEndpointsAllocated(callback) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.onResourceEndpointsAllocated(callback)), this._client);
    }
    onResourceReady(callback) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.onResourceReady(callback)), this._client);
    }
    createExecutionConfiguration() {
        return new ExecutionConfigurationBuilderPromiseImpl(this._promise.then(obj => obj.createExecutionConfiguration()), this._client);
    }
    publishAsStaticWebsite(options) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.publishAsStaticWebsite(options)), this._client);
    }
    publishAsNodeServer(entryPoint, options) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.publishAsNodeServer(entryPoint, options)), this._client);
    }
    publishAsPackageScript(options) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.publishAsPackageScript(options)), this._client);
    }
    withNpm(options) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withNpm(options)), this._client);
    }
    withBun(options) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withBun(options)), this._client);
    }
    withYarn(options) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withYarn(options)), this._client);
    }
    withPnpm(options) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withPnpm(options)), this._client);
    }
    withBuildScript(scriptName, options) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withBuildScript(scriptName, options)), this._client);
    }
    withRunScript(scriptName, options) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withRunScript(scriptName, options)), this._client);
    }
    withBrowserDebugger(options) {
        return new BunAppResourcePromiseImpl(this._promise.then(obj => obj.withBrowserDebugger(options)), this._client);
    }
}
// ============================================================================
// ContainerRegistryResourceImpl
// ============================================================================
class ContainerRegistryResourceImpl extends ResourceBuilderBase {
    constructor(handle, client) {
        super(handle, client);
    }
    /** @internal */
    async _withContainerRegistryInternal(registry) {
        registry = isPromiseLike(registry) ? await registry : registry;
        const rpcArgs = { builder: this._handle, registry };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerRegistry', rpcArgs);
        return new ContainerRegistryResourceImpl(result, this._client);
    }
    /**
     * Configures the resource to use the specified container registry for container image operations.
     *
     * This method adds a `ContainerRegistryReferenceAnnotation` to the resource,
     * indicating that the resource should use the specified container registry for container image operations.
     * @param registry The container registry resource builder.
     * @returns The resource builder for chaining.
     */
    withContainerRegistry(registry) {
        return new ContainerRegistryResourcePromiseImpl(this._withContainerRegistryInternal(registry), this._client);
    }
    /** @internal */
    async _withDockerfileBaseImageInternal(buildImage, runtimeImage) {
        const rpcArgs = { builder: this._handle };
        if (buildImage !== undefined)
            rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined)
            rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileBaseImage', rpcArgs);
        return new ContainerRegistryResourceImpl(result, this._client);
    }
    /**
     * Configures custom base images for generated Dockerfiles.
     *
     * This extension method allows customization of the base images used in generated Dockerfiles.
     * For multi-stage Dockerfiles (e.g., Python with UV), you can specify separate build and runtime images.
     * Specify custom base images for a Python application:
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddPythonApp("myapp", "path/to/app", "main.py")
     * .WithDockerfileBaseImage(
     * buildImage: "ghcr.io/astral-sh/uv:python3.12-bookworm-slim",
     * runtimeImage: "python:3.12-slim-bookworm");
     * builder.Build().Run();
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileBaseImage(options) {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new ContainerRegistryResourcePromiseImpl(this._withDockerfileBaseImageInternal(buildImage, runtimeImage), this._client);
    }
    /** @internal */
    async _withRequiredCommandInternal(command, helpLink) {
        const rpcArgs = { builder: this._handle, command };
        if (helpLink !== undefined)
            rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability('Aspire.Hosting/withRequiredCommand', rpcArgs);
        return new ContainerRegistryResourceImpl(result, this._client);
    }
    /**
     * Declares that a resource requires a specific command/executable to be available on the local machine PATH before it can start.
     *
     * The command is considered valid if either:
     * 1. It is an absolute or relative path (contains a directory separator) that points to an existing file, or
     * 2. It is discoverable on the current process PATH (respecting PATHEXT on Windows).
     * If the command is not found, a warning message will be logged but the resource will be allowed to attempt to start.
     * @param command The command string (file name or path) that should be validated.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withRequiredCommand(command, options) {
        const helpLink = options?.helpLink;
        return new ContainerRegistryResourcePromiseImpl(this._withRequiredCommandInternal(command, helpLink), this._client);
    }
    /** @internal */
    async _withSessionLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withSessionLifetime', rpcArgs);
        return new ContainerRegistryResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a session lifetime.
     * @returns The resource builder.
     */
    withSessionLifetime() {
        return new ContainerRegistryResourcePromiseImpl(this._withSessionLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withPersistentLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPersistentLifetime', rpcArgs);
        return new ContainerRegistryResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime.
     * @returns The resource builder.
     */
    withPersistentLifetime() {
        return new ContainerRegistryResourcePromiseImpl(this._withPersistentLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withLifetimeOfInternal(sourceBuilder) {
        sourceBuilder = isPromiseLike(sourceBuilder) ? await sourceBuilder : sourceBuilder;
        const rpcArgs = { builder: this._handle, sourceBuilder };
        const result = await this._client.invokeCapability('Aspire.Hosting/withLifetimeOf', rpcArgs);
        return new ContainerRegistryResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to match the lifetime of another resource.
     *
     * The resource lifetime is evaluated from `sourceBuilder` when the application model is prepared, so later lifetime
     * changes to the source resource are reflected by this resource.
     * @param sourceBuilder The resource builder whose lifetime should be used.
     * @returns The resource builder.
     */
    withLifetimeOf(sourceBuilder) {
        return new ContainerRegistryResourcePromiseImpl(this._withLifetimeOfInternal(sourceBuilder), this._client);
    }
    /** @internal */
    async _withParentProcessLifetimeInternal(parentProcessId) {
        const rpcArgs = { builder: this._handle, parentProcessId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withParentProcessLifetime', rpcArgs);
        return new ContainerRegistryResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime that ends when a parent process exits.
     * @param parentProcessId The ID of the parent process to monitor.
     * @returns The resource builder.
     */
    withParentProcessLifetime(parentProcessId) {
        return new ContainerRegistryResourcePromiseImpl(this._withParentProcessLifetimeInternal(parentProcessId), this._client);
    }
    /** @internal */
    async _withUrlsInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new ResourceUrlsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrls', rpcArgs);
        return new ContainerRegistryResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to customize the URLs displayed for the resource.
     * @param callback The callback that will customize URLs for the resource.
     * @returns The resource builder.
     */
    withUrls(callback) {
        return new ContainerRegistryResourcePromiseImpl(this._withUrlsInternal(callback), this._client);
    }
    /** @internal */
    async _withUrlInternal(url, displayText) {
        const rpcArgs = { builder: this._handle, url };
        if (displayText !== undefined)
            rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrl', rpcArgs);
        return new ContainerRegistryResourceImpl(result, this._client);
    }
    /**
     * Adds or modifies displayed URLs
     * @param options Additional options.
     */
    withUrl(url, options) {
        const displayText = options?.displayText;
        return new ContainerRegistryResourcePromiseImpl(this._withUrlInternal(url, displayText), this._client);
    }
    /** @internal */
    async _withUrlForEndpointInternal(endpointName, callback) {
        const callbackId = registerCallback(async (objData) => {
            const obj = wrapIfHandle(objData);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrlForEndpoint', rpcArgs);
        return new ContainerRegistryResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to update the URL displayed for the endpoint with the specified name.
     * @param endpointName The name of the endpoint to customize the URL for.
     * @param callback The callback that will customize the URL.
     * @returns The resource builder.
     */
    withUrlForEndpoint(endpointName, callback) {
        return new ContainerRegistryResourcePromiseImpl(this._withUrlForEndpointInternal(endpointName, callback), this._client);
    }
    /** @internal */
    async _excludeFromManifestInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromManifest', rpcArgs);
        return new ContainerRegistryResourceImpl(result, this._client);
    }
    /**
     * Excludes a resource from being published to the manifest.
     * @returns The resource builder.
     */
    excludeFromManifest() {
        return new ContainerRegistryResourcePromiseImpl(this._excludeFromManifestInternal(), this._client);
    }
    /** @internal */
    async _withExplicitStartInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExplicitStart', rpcArgs);
        return new ContainerRegistryResourceImpl(result, this._client);
    }
    /**
     * Prevents resource from starting automatically
     * @returns The resource builder.
     */
    withExplicitStart() {
        return new ContainerRegistryResourcePromiseImpl(this._withExplicitStartInternal(), this._client);
    }
    /** @internal */
    async _withHealthCheckInternal(key) {
        const rpcArgs = { builder: this._handle, key };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHealthCheck', rpcArgs);
        return new ContainerRegistryResourceImpl(result, this._client);
    }
    /**
     * Adds a health check by key
     * @param key The key for the health check.
     * @returns The resource builder.
     */
    withHealthCheck(key) {
        return new ContainerRegistryResourcePromiseImpl(this._withHealthCheckInternal(key), this._client);
    }
    /** @internal */
    async _withCommandInternal(name, displayName, executeCommand, commandOptions) {
        const executeCommandId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined)
            rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability('Aspire.Hosting/withCommand', rpcArgs);
        return new ContainerRegistryResourceImpl(result, this._client);
    }
    /**
     * Adds a resource command
     *
     * The `WithCommand` method is used to add commands to the resource. Commands are displayed in the dashboard
     * and can be executed by a user using the dashboard UI.
     * When a command is executed, the `executeCommand` callback is called and is run inside the Aspire host.
     * @param name The name of the command. The name uniquely identifies the command.
     * @param displayName The display name visible in UI.
     * @param executeCommand A callback that is executed when the command is executed. The callback is run inside the Aspire host. The callback result is used to indicate success or failure in the UI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withCommand(name, displayName, executeCommand, options) {
        const commandOptions = options?.commandOptions;
        return new ContainerRegistryResourcePromiseImpl(this._withCommandInternal(name, displayName, executeCommand, commandOptions), this._client);
    }
    /** @internal */
    async _withProcessCommandInternal(commandName, displayName, options) {
        const rpcArgs = { builder: this._handle, commandName, displayName, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommand', rpcArgs);
        return new ContainerRegistryResourceImpl(result, this._client);
    }
    /** Adds a command to the resource that starts a local process when invoked. */
    withProcessCommand(commandName, displayName, options) {
        return new ContainerRegistryResourcePromiseImpl(this._withProcessCommandInternal(commandName, displayName, options), this._client);
    }
    /** @internal */
    async _withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options) {
        const createProcessSpecId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await createProcessSpec(arg);
        });
        const rpcArgs = { builder: this._handle, commandName, displayName, createProcessSpec: createProcessSpecId };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommandFactory', rpcArgs);
        return new ContainerRegistryResourceImpl(result, this._client);
    }
    /**
     * Adds a command to the resource that starts a local process created by a callback when invoked.
     * @param options Additional options.
     */
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new ContainerRegistryResourcePromiseImpl(this._withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options), this._client);
    }
    /** @internal */
    async _withRelationshipInternal(resourceBuilder, type) {
        resourceBuilder = isPromiseLike(resourceBuilder) ? await resourceBuilder : resourceBuilder;
        const rpcArgs = { builder: this._handle, resourceBuilder, type };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderRelationship', rpcArgs);
        return new ContainerRegistryResourceImpl(result, this._client);
    }
    /**
     * Adds a relationship to another resource using its builder.
     * @param resourceBuilder The resource builder that the relationship is to.
     * @param type The relationship type.
     * @returns A resource builder.
     */
    withRelationship(resourceBuilder, type) {
        return new ContainerRegistryResourcePromiseImpl(this._withRelationshipInternal(resourceBuilder, type), this._client);
    }
    /** @internal */
    async _withParentRelationshipInternal(parent) {
        parent = isPromiseLike(parent) ? await parent : parent;
        const rpcArgs = { builder: this._handle, parent };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderParentRelationship', rpcArgs);
        return new ContainerRegistryResourceImpl(result, this._client);
    }
    /**
     * Sets the parent relationship
     * @param parent The parent of `builder`.
     * @returns A resource builder.
     */
    withParentRelationship(parent) {
        return new ContainerRegistryResourcePromiseImpl(this._withParentRelationshipInternal(parent), this._client);
    }
    /** @internal */
    async _withChildRelationshipInternal(child) {
        child = isPromiseLike(child) ? await child : child;
        const rpcArgs = { builder: this._handle, child };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderChildRelationship', rpcArgs);
        return new ContainerRegistryResourceImpl(result, this._client);
    }
    /**
     * Sets a child relationship
     * @param child The child of `builder`.
     * @returns A resource builder.
     */
    withChildRelationship(child) {
        return new ContainerRegistryResourcePromiseImpl(this._withChildRelationshipInternal(child), this._client);
    }
    /** @internal */
    async _withIconNameInternal(iconName, iconVariant) {
        const rpcArgs = { builder: this._handle, iconName };
        if (iconVariant !== undefined)
            rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability('Aspire.Hosting/withIconName', rpcArgs);
        return new ContainerRegistryResourceImpl(result, this._client);
    }
    /**
     * Specifies the icon to use when displaying the resource in the dashboard.
     * @param iconName The name of the FluentUI icon to use. See https://aka.ms/fluentui-system-icons for available icons.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withIconName(iconName, options) {
        const iconVariant = options?.iconVariant;
        return new ContainerRegistryResourcePromiseImpl(this._withIconNameInternal(iconName, iconVariant), this._client);
    }
    /** @internal */
    async _excludeFromMcpInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromMcp', rpcArgs);
        return new ContainerRegistryResourceImpl(result, this._client);
    }
    /**
     * Exclude the resource from MCP operations using the Aspire MCP server. The resource is excluded from results that return resources, console logs and telemetry.
     * @returns The resource builder.
     */
    excludeFromMcp() {
        return new ContainerRegistryResourcePromiseImpl(this._excludeFromMcpInternal(), this._client);
    }
    /** @internal */
    async _withHiddenInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHidden', rpcArgs);
        return new ContainerRegistryResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists
     *
     * Use this method to hide resources that are implementation details and should never be displayed by default.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @returns The resource builder.
     */
    withHidden() {
        return new ContainerRegistryResourcePromiseImpl(this._withHiddenInternal(), this._client);
    }
    /** @internal */
    async _withHiddenOnCompletionInternal(exitCode, exitCodes) {
        const rpcArgs = { builder: this._handle };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        if (exitCodes !== undefined)
            rpcArgs.exitCodes = exitCodes;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHiddenOnCompletion', rpcArgs);
        return new ContainerRegistryResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists after successful completion
     *
     * This method is useful for one-off resources such as setup scripts, migrations, or build steps that should remain visible while running
     * and then be hidden after successful completion.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHiddenOnCompletion(options) {
        const exitCode = options?.exitCode;
        const exitCodes = options?.exitCodes;
        return new ContainerRegistryResourcePromiseImpl(this._withHiddenOnCompletionInternal(exitCode, exitCodes), this._client);
    }
    /** @internal */
    async _withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new PipelineStepContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined)
            rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined)
            rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined)
            rpcArgs.tags = tags;
        if (description !== undefined)
            rpcArgs.description = description;
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineStepFactory', rpcArgs);
        return new ContainerRegistryResourceImpl(result, this._client);
    }
    /**
     * Adds a pipeline step to the resource that will be executed during deployment.
     * @param stepName The unique name of the pipeline step.
     * @param callback The callback to execute when the step runs.
     * @param options Additional options.
     * @returns The resource builder for chaining.
     */
    withPipelineStepFactory(stepName, callback, options) {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new ContainerRegistryResourcePromiseImpl(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description), this._client);
    }
    /** @internal */
    async _withPipelineConfigurationInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new PipelineConfigurationContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineConfiguration', rpcArgs);
        return new ContainerRegistryResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to be executed during the pipeline configuration phase, allowing modification of step dependencies and relationships.
     * @param callback The callback function to execute during the configuration phase.
     * @returns The resource builder for chaining.
     */
    withPipelineConfiguration(callback) {
        return new ContainerRegistryResourcePromiseImpl(this._withPipelineConfigurationInternal(callback), this._client);
    }
    /**
     * Gets the name of the resource from a builder.
     *
     * Why this wrapper exists: This capability accesses a nested property
     * (`resource.Resource.Name`) which requires a wrapper method. There is no single
     * .NET method that returns just the resource name that could be annotated directly.
     * @returns The resource name.
     */
    async getResourceName() {
        const rpcArgs = { resource: this._handle };
        return await this._client.invokeCapability('Aspire.Hosting/getResourceName', rpcArgs);
    }
    /** @internal */
    async _onBeforeResourceStartedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new BeforeResourceStartedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onBeforeResourceStarted', rpcArgs);
        return new ContainerRegistryResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the BeforeResourceStarted event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onBeforeResourceStarted(callback) {
        return new ContainerRegistryResourcePromiseImpl(this._onBeforeResourceStartedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceStoppedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceStoppedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceStopped', rpcArgs);
        return new ContainerRegistryResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceStopped event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceStopped(callback) {
        return new ContainerRegistryResourcePromiseImpl(this._onResourceStoppedInternal(callback), this._client);
    }
    /** @internal */
    async _onInitializeResourceInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new InitializeResourceEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onInitializeResource', rpcArgs);
        return new ContainerRegistryResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the InitializeResource event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onInitializeResource(callback) {
        return new ContainerRegistryResourcePromiseImpl(this._onInitializeResourceInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceReadyInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceReadyEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceReady', rpcArgs);
        return new ContainerRegistryResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceReady event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceReady(callback) {
        return new ContainerRegistryResourcePromiseImpl(this._onResourceReadyInternal(callback), this._client);
    }
    /**
     * Creates an execution configuration builder for the specified resource.
     * @returns The execution configuration builder.
     */
    createExecutionConfiguration() {
        const promise = (async () => {
            const rpcArgs = { resource: this._handle };
            const handle = await this._client.invokeCapability('Aspire.Hosting/createExecutionConfiguration', rpcArgs);
            return new ExecutionConfigurationBuilderImpl(handle, this._client);
        })();
        return new ExecutionConfigurationBuilderPromiseImpl(promise, this._client);
    }
}
/**
 * Thenable wrapper for ContainerRegistryResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
class ContainerRegistryResourcePromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    withContainerRegistry(registry) {
        return new ContainerRegistryResourcePromiseImpl(this._promise.then(obj => obj.withContainerRegistry(registry)), this._client);
    }
    withDockerfileBaseImage(options) {
        return new ContainerRegistryResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileBaseImage(options)), this._client);
    }
    withRequiredCommand(command, options) {
        return new ContainerRegistryResourcePromiseImpl(this._promise.then(obj => obj.withRequiredCommand(command, options)), this._client);
    }
    withSessionLifetime() {
        return new ContainerRegistryResourcePromiseImpl(this._promise.then(obj => obj.withSessionLifetime()), this._client);
    }
    withPersistentLifetime() {
        return new ContainerRegistryResourcePromiseImpl(this._promise.then(obj => obj.withPersistentLifetime()), this._client);
    }
    withLifetimeOf(sourceBuilder) {
        return new ContainerRegistryResourcePromiseImpl(this._promise.then(obj => obj.withLifetimeOf(sourceBuilder)), this._client);
    }
    withParentProcessLifetime(parentProcessId) {
        return new ContainerRegistryResourcePromiseImpl(this._promise.then(obj => obj.withParentProcessLifetime(parentProcessId)), this._client);
    }
    withUrls(callback) {
        return new ContainerRegistryResourcePromiseImpl(this._promise.then(obj => obj.withUrls(callback)), this._client);
    }
    withUrl(url, options) {
        return new ContainerRegistryResourcePromiseImpl(this._promise.then(obj => obj.withUrl(url, options)), this._client);
    }
    withUrlForEndpoint(endpointName, callback) {
        return new ContainerRegistryResourcePromiseImpl(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)), this._client);
    }
    excludeFromManifest() {
        return new ContainerRegistryResourcePromiseImpl(this._promise.then(obj => obj.excludeFromManifest()), this._client);
    }
    withExplicitStart() {
        return new ContainerRegistryResourcePromiseImpl(this._promise.then(obj => obj.withExplicitStart()), this._client);
    }
    withHealthCheck(key) {
        return new ContainerRegistryResourcePromiseImpl(this._promise.then(obj => obj.withHealthCheck(key)), this._client);
    }
    withCommand(name, displayName, executeCommand, options) {
        return new ContainerRegistryResourcePromiseImpl(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)), this._client);
    }
    withProcessCommand(commandName, displayName, options) {
        return new ContainerRegistryResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommand(commandName, displayName, options)), this._client);
    }
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new ContainerRegistryResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommandFactory(commandName, displayName, createProcessSpec, options)), this._client);
    }
    withRelationship(resourceBuilder, type) {
        return new ContainerRegistryResourcePromiseImpl(this._promise.then(obj => obj.withRelationship(resourceBuilder, type)), this._client);
    }
    withParentRelationship(parent) {
        return new ContainerRegistryResourcePromiseImpl(this._promise.then(obj => obj.withParentRelationship(parent)), this._client);
    }
    withChildRelationship(child) {
        return new ContainerRegistryResourcePromiseImpl(this._promise.then(obj => obj.withChildRelationship(child)), this._client);
    }
    withIconName(iconName, options) {
        return new ContainerRegistryResourcePromiseImpl(this._promise.then(obj => obj.withIconName(iconName, options)), this._client);
    }
    excludeFromMcp() {
        return new ContainerRegistryResourcePromiseImpl(this._promise.then(obj => obj.excludeFromMcp()), this._client);
    }
    withHidden() {
        return new ContainerRegistryResourcePromiseImpl(this._promise.then(obj => obj.withHidden()), this._client);
    }
    withHiddenOnCompletion(options) {
        return new ContainerRegistryResourcePromiseImpl(this._promise.then(obj => obj.withHiddenOnCompletion(options)), this._client);
    }
    withPipelineStepFactory(stepName, callback, options) {
        return new ContainerRegistryResourcePromiseImpl(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)), this._client);
    }
    withPipelineConfiguration(callback) {
        return new ContainerRegistryResourcePromiseImpl(this._promise.then(obj => obj.withPipelineConfiguration(callback)), this._client);
    }
    getResourceName() {
        return this._promise.then(obj => obj.getResourceName());
    }
    onBeforeResourceStarted(callback) {
        return new ContainerRegistryResourcePromiseImpl(this._promise.then(obj => obj.onBeforeResourceStarted(callback)), this._client);
    }
    onResourceStopped(callback) {
        return new ContainerRegistryResourcePromiseImpl(this._promise.then(obj => obj.onResourceStopped(callback)), this._client);
    }
    onInitializeResource(callback) {
        return new ContainerRegistryResourcePromiseImpl(this._promise.then(obj => obj.onInitializeResource(callback)), this._client);
    }
    onResourceReady(callback) {
        return new ContainerRegistryResourcePromiseImpl(this._promise.then(obj => obj.onResourceReady(callback)), this._client);
    }
    createExecutionConfiguration() {
        return new ExecutionConfigurationBuilderPromiseImpl(this._promise.then(obj => obj.createExecutionConfiguration()), this._client);
    }
}
// ============================================================================
// ContainerResourceImpl
// ============================================================================
/** A resource that represents a specified container. */
class ContainerResourceImpl extends ResourceBuilderBase {
    constructor(handle, client) {
        super(handle, client);
    }
    /** @internal */
    async _withContainerRegistryInternal(registry) {
        registry = isPromiseLike(registry) ? await registry : registry;
        const rpcArgs = { builder: this._handle, registry };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerRegistry', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Configures the resource to use the specified container registry for container image operations.
     *
     * This method adds a `ContainerRegistryReferenceAnnotation` to the resource,
     * indicating that the resource should use the specified container registry for container image operations.
     * @param registry The container registry resource builder.
     * @returns The resource builder for chaining.
     */
    withContainerRegistry(registry) {
        return new ContainerResourcePromiseImpl(this._withContainerRegistryInternal(registry), this._client);
    }
    /** @internal */
    async _withBindMountInternal(source, target, isReadOnly) {
        const rpcArgs = { builder: this._handle, source, target };
        if (isReadOnly !== undefined)
            rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability('Aspire.Hosting/withBindMount', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a bind mount to a container resource.
     * @param source The source path of the mount. This is the path to the file or directory on the host, relative to the app host project directory.
     * @param target The target path where the file or directory is mounted in the container.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withBindMount(source, target, options) {
        const isReadOnly = options?.isReadOnly;
        return new ContainerResourcePromiseImpl(this._withBindMountInternal(source, target, isReadOnly), this._client);
    }
    /** @internal */
    async _withEntrypointInternal(entrypoint) {
        const rpcArgs = { builder: this._handle, entrypoint };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEntrypoint', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Sets the Entrypoint for the container.
     * @param entrypoint The new entrypoint for the container.
     * @returns The resource builder.
     */
    withEntrypoint(entrypoint) {
        return new ContainerResourcePromiseImpl(this._withEntrypointInternal(entrypoint), this._client);
    }
    /** @internal */
    async _withImageTagInternal(tag) {
        const rpcArgs = { builder: this._handle, tag };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImageTag', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Allows overriding the image tag on a container.
     * @param tag Tag value.
     * @returns The resource builder.
     */
    withImageTag(tag) {
        return new ContainerResourcePromiseImpl(this._withImageTagInternal(tag), this._client);
    }
    /** @internal */
    async _withImageRegistryInternal(registry) {
        const rpcArgs = { builder: this._handle, registry };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImageRegistry', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Allows overriding the image registry on a container.
     * @param registry Registry value.
     * @returns The resource builder.
     */
    withImageRegistry(registry) {
        return new ContainerResourcePromiseImpl(this._withImageRegistryInternal(registry), this._client);
    }
    /** @internal */
    async _withImageInternal(image, tag) {
        const rpcArgs = { builder: this._handle, image };
        if (tag !== undefined)
            rpcArgs.tag = tag;
        const result = await this._client.invokeCapability('Aspire.Hosting/withImage', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Allows overriding the image on a container.
     * @param image Image value.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withImage(image, options) {
        const tag = options?.tag;
        return new ContainerResourcePromiseImpl(this._withImageInternal(image, tag), this._client);
    }
    /** @internal */
    async _withImageSHA256Internal(sha256) {
        const rpcArgs = { builder: this._handle, sha256 };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImageSHA256', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Allows setting the image to a specific sha256 on a container.
     * @param sha256 Registry value.
     * @returns The resource builder.
     */
    withImageSHA256(sha256) {
        return new ContainerResourcePromiseImpl(this._withImageSHA256Internal(sha256), this._client);
    }
    /** @internal */
    async _withContainerRuntimeArgsInternal(args) {
        const rpcArgs = { builder: this._handle, args };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerRuntimeArgs', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a callback to be executed with a list of arguments to add to the container runtime run command when a container resource is started.
     * @param args The arguments to be passed to the container runtime run command when the container resource is started.
     * @returns The resource builder.
     */
    withContainerRuntimeArgs(args) {
        return new ContainerResourcePromiseImpl(this._withContainerRuntimeArgsInternal(args), this._client);
    }
    /** @internal */
    async _withLifetimeInternal(lifetime) {
        const rpcArgs = { builder: this._handle, lifetime };
        const result = await this._client.invokeCapability('Aspire.Hosting/withLifetime', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Sets the lifetime behavior of the container resource.
     *
     * Prefer `WithPersistentLifetime``1` or
     * `WithSessionLifetime``1` for new code.
     * Marking a container resource to have a `Persistent` lifetime.
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddContainer("mycontainer", "myimage")
     * .WithPersistentLifetime();
     * builder.Build().Run();
     * ```
     * @param lifetime The lifetime behavior of the container resource. The default behavior is `Session`.
     * @returns The resource builder.
     */
    withLifetime(lifetime) {
        return new ContainerResourcePromiseImpl(this._withLifetimeInternal(lifetime), this._client);
    }
    /** @internal */
    async _withImagePullPolicyInternal(pullPolicy) {
        const rpcArgs = { builder: this._handle, pullPolicy };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImagePullPolicy', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Sets the pull policy for the container resource.
     * @param pullPolicy The pull policy behavior for the container resource.
     * @returns The resource builder.
     */
    withImagePullPolicy(pullPolicy) {
        return new ContainerResourcePromiseImpl(this._withImagePullPolicyInternal(pullPolicy), this._client);
    }
    /** @internal */
    async _publishAsContainerInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/publishAsContainer', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Changes the resource to be published as a container in the manifest.
     * @returns The resource builder.
     */
    publishAsContainer() {
        return new ContainerResourcePromiseImpl(this._publishAsContainerInternal(), this._client);
    }
    /** @internal */
    async _withDockerfileInternal(contextPath, dockerfilePath, stage) {
        const rpcArgs = { builder: this._handle, contextPath };
        if (dockerfilePath !== undefined)
            rpcArgs.dockerfilePath = dockerfilePath;
        if (stage !== undefined)
            rpcArgs.stage = stage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfile', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Causes Aspire to build the specified container image from a Dockerfile.
     * @param contextPath Path to be used as the context for the container image build.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfile(contextPath, options) {
        const dockerfilePath = options?.dockerfilePath;
        const stage = options?.stage;
        return new ContainerResourcePromiseImpl(this._withDockerfileInternal(contextPath, dockerfilePath, stage), this._client);
    }
    /** @internal */
    async _withDockerfileFactoryInternal(contextPath, dockerfileFactory, stage) {
        const dockerfileFactoryId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new DockerfileFactoryContextImpl(argHandle, this._client);
            return await dockerfileFactory(arg);
        });
        const rpcArgs = { builder: this._handle, contextPath, dockerfileFactory: dockerfileFactoryId };
        if (stage !== undefined)
            rpcArgs.stage = stage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileFactory', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Builds the specified container image from a Dockerfile generated by an asynchronous factory function.
     *
     * When this method is called, an annotation is added to the `ContainerResource` that specifies the context path
     * and a factory function that generates Dockerfile content. The factory is invoked at build time to produce the Dockerfile,
     * which is then written to a temporary file and used by the orchestrator to build the container image.
     * The `contextPath` is relative to the AppHost directory unless it is fully qualified.
     * The factory function is invoked once during the build process to generate the Dockerfile content.
     * The output is trusted and not validated.
     * Creates a container called `mycontainer` with a dynamically generated Dockerfile.
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddContainer("mycontainer", "myimage")
     * .WithDockerfileFactory("path/to/context", async context =>
     * {
     * var template = await File.ReadAllTextAsync("template.dockerfile", context.CancellationToken);
     * return template.Replace("{{VERSION}}", "1.0");
     * });
     * builder.Build().Run();
     * ```
     * @param contextPath Path to be used as the context for the container image build.
     * @param dockerfileFactory An asynchronous function that returns the Dockerfile content as a string.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileFactory(contextPath, dockerfileFactory, options) {
        const stage = options?.stage;
        return new ContainerResourcePromiseImpl(this._withDockerfileFactoryInternal(contextPath, dockerfileFactory, stage), this._client);
    }
    /** @internal */
    async _withContainerNameInternal(name) {
        const rpcArgs = { builder: this._handle, name };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerName', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Overrides the default container name for this resource. By default Aspire generates a unique container name based on the resource name and a random postfix (or a postfix based on a hash of the AppHost project path for persistent container resources). This method allows you to override that behavior with a custom name, but could lead to naming conflicts if the specified name is not unique.
     *
     * Combining this with `Persistent` will allow Aspire to re-use an existing container that was not
     * created by an Aspire AppHost.
     * @param name The desired container name. Must be a valid container name or your runtime will report an error.
     * @returns The resource builder.
     */
    withContainerName(name) {
        return new ContainerResourcePromiseImpl(this._withContainerNameInternal(name), this._client);
    }
    /** @internal */
    async _withBuildArgInternal(name, value) {
        value = isPromiseLike(value) ? await value : value;
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuildArg', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a build argument when the container is built from a Dockerfile.
     * @param name The name of the build argument.
     * @param value The build argument value, either a string or a parameter resource.
     * @returns The resource builder.
     */
    withBuildArg(name, value) {
        return new ContainerResourcePromiseImpl(this._withBuildArgInternal(name, value), this._client);
    }
    /** @internal */
    async _withBuildSecretInternal(name, value) {
        value = isPromiseLike(value) ? await value : value;
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withParameterBuildSecret', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a secret build argument when the container is built from a Dockerfile.
     * @param name The name of the secret build argument.
     * @param value The resource builder for a parameter resource.
     * @returns The resource builder.
     */
    withBuildSecret(name, value) {
        return new ContainerResourcePromiseImpl(this._withBuildSecretInternal(name, value), this._client);
    }
    /** @internal */
    async _withContainerCertificatePathsInternal(customCertificatesDestination, defaultCertificateBundlePaths, defaultCertificateDirectoryPaths) {
        const rpcArgs = { builder: this._handle };
        if (customCertificatesDestination !== undefined)
            rpcArgs.customCertificatesDestination = customCertificatesDestination;
        if (defaultCertificateBundlePaths !== undefined)
            rpcArgs.defaultCertificateBundlePaths = defaultCertificateBundlePaths;
        if (defaultCertificateDirectoryPaths !== undefined)
            rpcArgs.defaultCertificateDirectoryPaths = defaultCertificateDirectoryPaths;
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerCertificatePaths', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Adds container certificate path overrides used for certificate trust at run time.
     * @param options Additional options.
     * @returns The updated resource builder.
     */
    withContainerCertificatePaths(options) {
        const customCertificatesDestination = options?.customCertificatesDestination;
        const defaultCertificateBundlePaths = options?.defaultCertificateBundlePaths;
        const defaultCertificateDirectoryPaths = options?.defaultCertificateDirectoryPaths;
        return new ContainerResourcePromiseImpl(this._withContainerCertificatePathsInternal(customCertificatesDestination, defaultCertificateBundlePaths, defaultCertificateDirectoryPaths), this._client);
    }
    /** @internal */
    async _withDockerfileBuilderInternal(contextPath, callback, stage) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new DockerfileBuilderCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, contextPath, callback: callbackId };
        if (stage !== undefined)
            rpcArgs.stage = stage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileBuilder', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Configures the resource to use a programmatically generated Dockerfile
     *
     * This method provides a programmatic way to build Dockerfiles using the `DockerfileBuilder` API
     * instead of string manipulation. Callbacks can be composed by calling this method multiple times - each callback will be invoked
     * in order to build up the final Dockerfile.
     * The `contextPath` is relative to the AppHost directory unless it is fully qualified.
     * Creates a container with a programmatically built Dockerfile using fluent API:
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddContainer("mycontainer", "myimage")
     * .WithDockerfileBuilder("path/to/context", context =>
     * {
     * context.Builder.From("alpine:latest")
     * .WorkDir("/app")
     * .Run("apk add curl")
     * .Copy(".", ".")
     * .Cmd(["./myapp"]);
     * return Task.CompletedTask;
     * });
     * builder.Build().Run();
     * ```
     * @param contextPath Path to be used as the context for the container image build.
     * @param callback A callback that uses the `DockerfileBuilder` API to construct the Dockerfile.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileBuilder(contextPath, callback, options) {
        const stage = options?.stage;
        return new ContainerResourcePromiseImpl(this._withDockerfileBuilderInternal(contextPath, callback, stage), this._client);
    }
    /** @internal */
    async _withDockerfileBaseImageInternal(buildImage, runtimeImage) {
        const rpcArgs = { builder: this._handle };
        if (buildImage !== undefined)
            rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined)
            rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileBaseImage', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Configures custom base images for generated Dockerfiles.
     *
     * This extension method allows customization of the base images used in generated Dockerfiles.
     * For multi-stage Dockerfiles (e.g., Python with UV), you can specify separate build and runtime images.
     * Specify custom base images for a Python application:
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddPythonApp("myapp", "path/to/app", "main.py")
     * .WithDockerfileBaseImage(
     * buildImage: "ghcr.io/astral-sh/uv:python3.12-bookworm-slim",
     * runtimeImage: "python:3.12-slim-bookworm");
     * builder.Build().Run();
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileBaseImage(options) {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new ContainerResourcePromiseImpl(this._withDockerfileBaseImageInternal(buildImage, runtimeImage), this._client);
    }
    /** @internal */
    async _withContainerNetworkAliasInternal(alias) {
        const rpcArgs = { builder: this._handle, alias };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerNetworkAlias', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a network alias to container resource.
     *
     * Network aliases enable DNS resolution of the container on the network by custom names.
     * By default, containers are accessible on the network using their resource name as a DNS alias.
     * This method allows adding additional aliases for the same container.
     * Multiple aliases can be added by calling this method multiple times.
     * @param alias The network alias for the container.
     * @returns The resource builder.
     */
    withContainerNetworkAlias(alias) {
        return new ContainerResourcePromiseImpl(this._withContainerNetworkAliasInternal(alias), this._client);
    }
    /** @internal */
    async _withMcpServerInternal(path, endpointName) {
        const rpcArgs = { builder: this._handle };
        if (path !== undefined)
            rpcArgs.path = path;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withMcpServer', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Marks the resource as hosting a Model Context Protocol (MCP) server on the specified endpoint.
     *
     * This method adds an `McpServerEndpointAnnotation` to the resource, enabling the Aspire tooling
     * to discover and proxy the MCP server exposed by the resource.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withMcpServer(options) {
        const path = options?.path;
        const endpointName = options?.endpointName;
        return new ContainerResourcePromiseImpl(this._withMcpServerInternal(path, endpointName), this._client);
    }
    /** @internal */
    async _withOtlpExporterInternal(protocol) {
        const rpcArgs = { builder: this._handle };
        if (protocol !== undefined)
            rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability('Aspire.Hosting/withOtlpExporter', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Configures OTLP telemetry export
     * @param options Additional options.
     */
    withOtlpExporter(options) {
        const protocol = options?.protocol;
        return new ContainerResourcePromiseImpl(this._withOtlpExporterInternal(protocol), this._client);
    }
    /** @internal */
    async _publishAsConnectionStringInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/publishAsConnectionString', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Changes the resource to be published as a connection string reference in the manifest.
     * @returns The resource builder.
     */
    publishAsConnectionString() {
        return new ContainerResourcePromiseImpl(this._publishAsConnectionStringInternal(), this._client);
    }
    /** @internal */
    async _withRequiredCommandInternal(command, helpLink) {
        const rpcArgs = { builder: this._handle, command };
        if (helpLink !== undefined)
            rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability('Aspire.Hosting/withRequiredCommand', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Declares that a resource requires a specific command/executable to be available on the local machine PATH before it can start.
     *
     * The command is considered valid if either:
     * 1. It is an absolute or relative path (contains a directory separator) that points to an existing file, or
     * 2. It is discoverable on the current process PATH (respecting PATHEXT on Windows).
     * If the command is not found, a warning message will be logged but the resource will be allowed to attempt to start.
     * @param command The command string (file name or path) that should be validated.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withRequiredCommand(command, options) {
        const helpLink = options?.helpLink;
        return new ContainerResourcePromiseImpl(this._withRequiredCommandInternal(command, helpLink), this._client);
    }
    /** @internal */
    async _withSessionLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withSessionLifetime', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a session lifetime.
     * @returns The resource builder.
     */
    withSessionLifetime() {
        return new ContainerResourcePromiseImpl(this._withSessionLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withPersistentLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPersistentLifetime', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime.
     * @returns The resource builder.
     */
    withPersistentLifetime() {
        return new ContainerResourcePromiseImpl(this._withPersistentLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withLifetimeOfInternal(sourceBuilder) {
        sourceBuilder = isPromiseLike(sourceBuilder) ? await sourceBuilder : sourceBuilder;
        const rpcArgs = { builder: this._handle, sourceBuilder };
        const result = await this._client.invokeCapability('Aspire.Hosting/withLifetimeOf', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to match the lifetime of another resource.
     *
     * The resource lifetime is evaluated from `sourceBuilder` when the application model is prepared, so later lifetime
     * changes to the source resource are reflected by this resource.
     * @param sourceBuilder The resource builder whose lifetime should be used.
     * @returns The resource builder.
     */
    withLifetimeOf(sourceBuilder) {
        return new ContainerResourcePromiseImpl(this._withLifetimeOfInternal(sourceBuilder), this._client);
    }
    /** @internal */
    async _withParentProcessLifetimeInternal(parentProcessId) {
        const rpcArgs = { builder: this._handle, parentProcessId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withParentProcessLifetime', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime that ends when a parent process exits.
     * @param parentProcessId The ID of the parent process to monitor.
     * @returns The resource builder.
     */
    withParentProcessLifetime(parentProcessId) {
        return new ContainerResourcePromiseImpl(this._withParentProcessLifetimeInternal(parentProcessId), this._client);
    }
    /** @internal */
    async _withEnvironmentInternal(name, value) {
        value = isPromiseLike(value) ? await value : value;
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEnvironment', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /** Sets an environment variable */
    withEnvironment(name, value) {
        return new ContainerResourcePromiseImpl(this._withEnvironmentInternal(name, value), this._client);
    }
    /** @internal */
    async _withEnvironmentCallbackInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new EnvironmentCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEnvironmentCallback', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Allows for the population of environment variables on a resource.
     * @param callback A callback that allows for deferred execution for computing many environment variables. This runs after resources have been allocated by the orchestrator and allows access to other resources to resolve computed data, e.g. connection strings, ports.
     * @returns The resource builder.
     */
    withEnvironmentCallback(callback) {
        return new ContainerResourcePromiseImpl(this._withEnvironmentCallbackInternal(callback), this._client);
    }
    /** @internal */
    async _withArgsInternal(args) {
        const rpcArgs = { builder: this._handle, args };
        const result = await this._client.invokeCapability('Aspire.Hosting/withArgs', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Adds arguments to be passed to a resource that supports arguments when it is launched.
     * @param args The arguments to be passed to the resource when it is started.
     * @returns The resource builder.
     */
    withArgs(args) {
        return new ContainerResourcePromiseImpl(this._withArgsInternal(args), this._client);
    }
    /** @internal */
    async _withArgsCallbackInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new CommandLineArgsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withArgsCallback', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a callback to be executed with a list of command-line arguments when a resource is started.
     * @param callback A callback that allows for deferred execution for computing arguments. This runs after resources have been allocated by the orchestrator and allows access to other resources to resolve computed data, e.g. connection strings, ports.
     * @returns The resource builder.
     */
    withArgsCallback(callback) {
        return new ContainerResourcePromiseImpl(this._withArgsCallbackInternal(callback), this._client);
    }
    /** @internal */
    async _withReferenceEnvironmentInternal(options) {
        const rpcArgs = { builder: this._handle, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withReferenceEnvironment', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Configures how information is injected into environment variables when the resource references other resources.
     * @param options Options controlling which reference information is emitted.
     * @returns The resource builder.
     */
    withReferenceEnvironment(options) {
        return new ContainerResourcePromiseImpl(this._withReferenceEnvironmentInternal(options), this._client);
    }
    /** @internal */
    async _withReferenceInternal(source, connectionName, optional, name) {
        source = isPromiseLike(source) ? await source : source;
        const rpcArgs = { builder: this._handle, source };
        if (connectionName !== undefined)
            rpcArgs.connectionName = connectionName;
        if (optional !== undefined)
            rpcArgs.optional = optional;
        if (name !== undefined)
            rpcArgs.name = name;
        const result = await this._client.invokeCapability('Aspire.Hosting/withReference', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a reference to another resource
     * @param options Additional options.
     */
    withReference(source, options) {
        const connectionName = options?.connectionName;
        const optional = options?.optional;
        const name = options?.name;
        return new ContainerResourcePromiseImpl(this._withReferenceInternal(source, connectionName, optional, name), this._client);
    }
    /** @internal */
    async _withEndpointCallbackInternal(endpointName, callback, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpointCallback', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Updates a named endpoint via callback
     * @param options Additional options.
     */
    withEndpointCallback(endpointName, callback, options) {
        const createIfNotExists = options?.createIfNotExists;
        return new ContainerResourcePromiseImpl(this._withEndpointCallbackInternal(endpointName, callback, createIfNotExists), this._client);
    }
    /** @internal */
    async _withHttpEndpointCallbackInternal(callback, name, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        if (name !== undefined)
            rpcArgs.name = name;
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpEndpointCallback', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Updates an HTTP endpoint via callback
     * @param options Additional options.
     */
    withHttpEndpointCallback(callback, options) {
        const name = options?.name;
        const createIfNotExists = options?.createIfNotExists;
        return new ContainerResourcePromiseImpl(this._withHttpEndpointCallbackInternal(callback, name, createIfNotExists), this._client);
    }
    /** @internal */
    async _withHttpsEndpointCallbackInternal(callback, name, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        if (name !== undefined)
            rpcArgs.name = name;
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpsEndpointCallback', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Updates an HTTPS endpoint via callback
     * @param options Additional options.
     */
    withHttpsEndpointCallback(callback, options) {
        const name = options?.name;
        const createIfNotExists = options?.createIfNotExists;
        return new ContainerResourcePromiseImpl(this._withHttpsEndpointCallbackInternal(callback, name, createIfNotExists), this._client);
    }
    /** @internal */
    async _withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (scheme !== undefined)
            rpcArgs.scheme = scheme;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        if (isExternal !== undefined)
            rpcArgs.isExternal = isExternal;
        if (protocol !== undefined)
            rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpoint', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a network endpoint
     * @param options Additional options.
     * @returns The resource builder.
     */
    withEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const scheme = options?.scheme;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        const isExternal = options?.isExternal;
        const protocol = options?.protocol;
        return new ContainerResourcePromiseImpl(this._withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol), this._client);
    }
    /** @internal */
    async _withEndpointProxySupportInternal(proxyEnabled) {
        const rpcArgs = { builder: this._handle, proxyEnabled };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpointProxySupport', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Set whether a resource can use proxied endpoints or whether they should be disabled for all endpoints belonging to the resource. If set to `false`, endpoints belonging to the resource will ignore the configured proxy settings and run proxy-less.
     *
     * This method is intended to support scenarios with persistent lifetime resources where it is desirable for the resource to be accessible over the same
     * port whether the Aspire application is running or not. Proxied endpoints bind ports that are only accessible while the Aspire application is running.
     * The user needs to be careful to ensure that endpoints are using unique ports when disabling proxy support as by default for proxy-less
     * endpoints, Aspire will allocate the target port as the host port, which will increase the chance of port conflicts.
     * @param proxyEnabled Should endpoints for the resource support using a proxy?
     * @returns The resource builder.
     */
    withEndpointProxySupport(proxyEnabled) {
        return new ContainerResourcePromiseImpl(this._withEndpointProxySupportInternal(proxyEnabled), this._client);
    }
    /** @internal */
    async _withHttpEndpointInternal(port, targetPort, name, env, isProxied) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpEndpoint', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP endpoint
     *
     * If an endpoint with the same name already exists on the resource, the existing endpoint is updated
     * with any non-null parameter values. Parameters left as `null` will not modify the existing endpoint's values.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new ContainerResourcePromiseImpl(this._withHttpEndpointInternal(port, targetPort, name, env, isProxied), this._client);
    }
    /** @internal */
    async _withHttpsEndpointInternal(port, targetPort, name, env, isProxied) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpsEndpoint', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTPS endpoint
     *
     * If an endpoint with the same name already exists on the resource, the existing endpoint is updated
     * with any non-null parameter values. Parameters left as `null` will not modify the existing endpoint's values.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpsEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new ContainerResourcePromiseImpl(this._withHttpsEndpointInternal(port, targetPort, name, env, isProxied), this._client);
    }
    /** @internal */
    async _withExternalHttpEndpointsInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExternalHttpEndpoints', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Marks existing http or https endpoints on a resource as external.
     * @returns The resource builder.
     */
    withExternalHttpEndpoints() {
        return new ContainerResourcePromiseImpl(this._withExternalHttpEndpointsInternal(), this._client);
    }
    /**
     * Gets an endpoint reference
     * @param name The name of the endpoint.
     * @returns An `EndpointReference` that can be used to resolve the address of the endpoint after resource allocation has occurred.
     */
    getEndpoint(name) {
        const promise = (async () => {
            const rpcArgs = { builder: this._handle, name };
            const handle = await this._client.invokeCapability('Aspire.Hosting/getEndpoint', rpcArgs);
            return new EndpointReferenceImpl(handle, this._client);
        })();
        return new EndpointReferencePromiseImpl(promise, this._client);
    }
    /** @internal */
    async _asHttp2ServiceInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/asHttp2Service', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to mark all endpoints' transport as HTTP/2. This is useful for HTTP/2 services that need prior knowledge.
     * @returns The resource builder.
     */
    asHttp2Service() {
        return new ContainerResourcePromiseImpl(this._asHttp2ServiceInternal(), this._client);
    }
    /** @internal */
    async _withUrlsInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new ResourceUrlsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrls', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to customize the URLs displayed for the resource.
     * @param callback The callback that will customize URLs for the resource.
     * @returns The resource builder.
     */
    withUrls(callback) {
        return new ContainerResourcePromiseImpl(this._withUrlsInternal(callback), this._client);
    }
    /** @internal */
    async _withUrlInternal(url, displayText) {
        const rpcArgs = { builder: this._handle, url };
        if (displayText !== undefined)
            rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrl', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Adds or modifies displayed URLs
     * @param options Additional options.
     */
    withUrl(url, options) {
        const displayText = options?.displayText;
        return new ContainerResourcePromiseImpl(this._withUrlInternal(url, displayText), this._client);
    }
    /** @internal */
    async _withUrlForEndpointInternal(endpointName, callback) {
        const callbackId = registerCallback(async (objData) => {
            const obj = wrapIfHandle(objData);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrlForEndpoint', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to update the URL displayed for the endpoint with the specified name.
     * @param endpointName The name of the endpoint to customize the URL for.
     * @param callback The callback that will customize the URL.
     * @returns The resource builder.
     */
    withUrlForEndpoint(endpointName, callback) {
        return new ContainerResourcePromiseImpl(this._withUrlForEndpointInternal(endpointName, callback), this._client);
    }
    /** @internal */
    async _excludeFromManifestInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromManifest', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Excludes a resource from being published to the manifest.
     * @returns The resource builder.
     */
    excludeFromManifest() {
        return new ContainerResourcePromiseImpl(this._excludeFromManifestInternal(), this._client);
    }
    /** @internal */
    async _waitForInternal(dependency, waitBehavior) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (waitBehavior !== undefined)
            rpcArgs.waitBehavior = waitBehavior;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitFor', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Waits for another resource to be ready
     * @param options Additional options.
     */
    waitFor(dependency, options) {
        const waitBehavior = options?.waitBehavior;
        return new ContainerResourcePromiseImpl(this._waitForInternal(dependency, waitBehavior), this._client);
    }
    /** @internal */
    async _waitForStartInternal(dependency, waitBehavior) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (waitBehavior !== undefined)
            rpcArgs.waitBehavior = waitBehavior;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitForStart', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Waits for another resource to start
     * @param options Additional options.
     */
    waitForStart(dependency, options) {
        const waitBehavior = options?.waitBehavior;
        return new ContainerResourcePromiseImpl(this._waitForStartInternal(dependency, waitBehavior), this._client);
    }
    /** @internal */
    async _withExplicitStartInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExplicitStart', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Prevents resource from starting automatically
     * @returns The resource builder.
     */
    withExplicitStart() {
        return new ContainerResourcePromiseImpl(this._withExplicitStartInternal(), this._client);
    }
    /** @internal */
    async _waitForCompletionInternal(dependency, exitCode) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitForResourceCompletion', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Waits for the dependency resource to enter the Exited or Finished state before starting the resource.
     * @param dependency The resource builder for the dependency resource.
     * @param options Additional options.
     * @returns The resource builder.
     */
    waitForCompletion(dependency, options) {
        const exitCode = options?.exitCode;
        return new ContainerResourcePromiseImpl(this._waitForCompletionInternal(dependency, exitCode), this._client);
    }
    /** @internal */
    async _withHealthCheckInternal(key) {
        const rpcArgs = { builder: this._handle, key };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHealthCheck', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a health check by key
     * @param key The key for the health check.
     * @returns The resource builder.
     */
    withHealthCheck(key) {
        return new ContainerResourcePromiseImpl(this._withHealthCheckInternal(key), this._client);
    }
    /** @internal */
    async _withHttpHealthCheckInternal(path, statusCode, endpointName) {
        const rpcArgs = { builder: this._handle };
        if (path !== undefined)
            rpcArgs.path = path;
        if (statusCode !== undefined)
            rpcArgs.statusCode = statusCode;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpHealthCheck', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a health check to the resource which is mapped to a specific endpoint.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpHealthCheck(options) {
        const path = options?.path;
        const statusCode = options?.statusCode;
        const endpointName = options?.endpointName;
        return new ContainerResourcePromiseImpl(this._withHttpHealthCheckInternal(path, statusCode, endpointName), this._client);
    }
    /** @internal */
    async _withCommandInternal(name, displayName, executeCommand, commandOptions) {
        const executeCommandId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined)
            rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability('Aspire.Hosting/withCommand', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a resource command
     *
     * The `WithCommand` method is used to add commands to the resource. Commands are displayed in the dashboard
     * and can be executed by a user using the dashboard UI.
     * When a command is executed, the `executeCommand` callback is called and is run inside the Aspire host.
     * @param name The name of the command. The name uniquely identifies the command.
     * @param displayName The display name visible in UI.
     * @param executeCommand A callback that is executed when the command is executed. The callback is run inside the Aspire host. The callback result is used to indicate success or failure in the UI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withCommand(name, displayName, executeCommand, options) {
        const commandOptions = options?.commandOptions;
        return new ContainerResourcePromiseImpl(this._withCommandInternal(name, displayName, executeCommand, commandOptions), this._client);
    }
    /** @internal */
    async _withProcessCommandInternal(commandName, displayName, options) {
        const rpcArgs = { builder: this._handle, commandName, displayName, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommand', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /** Adds a command to the resource that starts a local process when invoked. */
    withProcessCommand(commandName, displayName, options) {
        return new ContainerResourcePromiseImpl(this._withProcessCommandInternal(commandName, displayName, options), this._client);
    }
    /** @internal */
    async _withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options) {
        const createProcessSpecId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await createProcessSpec(arg);
        });
        const rpcArgs = { builder: this._handle, commandName, displayName, createProcessSpec: createProcessSpecId };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommandFactory', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a command to the resource that starts a local process created by a callback when invoked.
     * @param options Additional options.
     */
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new ContainerResourcePromiseImpl(this._withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options), this._client);
    }
    /** @internal */
    async _withHttpCommandInternal(path, displayName, options) {
        const rpcArgs = { builder: this._handle, path, displayName };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpCommand', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP resource command
     * @param options Additional options.
     */
    withHttpCommand(path, displayName, options) {
        return new ContainerResourcePromiseImpl(this._withHttpCommandInternal(path, displayName, options), this._client);
    }
    /** @internal */
    async _withDeveloperCertificateTrustInternal(trust) {
        const rpcArgs = { builder: this._handle, trust };
        const result = await this._client.invokeCapability('Aspire.Hosting/withDeveloperCertificateTrust', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Indicates whether developer certificates should be treated as trusted certificate authorities for the resource at run time. Currently this indicates trust for the ASP.NET Core developer certificate. The developer certificate will only be trusted when running in local development scenarios; in publish mode resources will use their default certificate trust.
     *
     * Disable trust for app host managed developer certificate(s) for a container resource.
     * ```
     * var container = builder.AddContainer("my-service", "my-service:latest")
     * .WithDeveloperCertificateTrust(false);
     * ```
     * Disable automatic trust for app host managed developer certificate(s), but explicitly enable it for a specific resource.
     * ```
     * var builder = DistributedApplication.CreateBuilder(new DistributedApplicationOptions()
     * {
     * Args = args,
     * TrustDeveloperCertificate = false,
     * });
     * var project = builder.AddProject<MyService>("my-service")
     * .WithDeveloperCertificateTrust(true);
     * ```
     * @param trust Indicates whether the developer certificate should be treated as trusted.
     * @returns The resource builder.
     */
    withDeveloperCertificateTrust(trust) {
        return new ContainerResourcePromiseImpl(this._withDeveloperCertificateTrustInternal(trust), this._client);
    }
    /** @internal */
    async _withCertificateTrustScopeInternal(scope) {
        const rpcArgs = { builder: this._handle, scope };
        const result = await this._client.invokeCapability('Aspire.Hosting/withCertificateTrustScope', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Sets the certificate trust scope
     *
     * The default scope if not overridden is `Append` which means that custom certificate
     * authorities should be appended to the default trusted certificate authorities for the resource. Setting the scope to
     * `Override` indicates the set of certificates in referenced
     * `CertificateAuthorityCollection` (and optionally Aspire developer certificiates) should be used as the
     * exclusive source of trust for a resource.
     * In all cases, this is a best effort implementation as not all resources support full customization of certificate
     * trust.
     * Set the scope for custom certificate authorities to override the default trusted certificate authorities for a container resource.
     * ```
     * var caCollection = builder.AddCertificateAuthorityCollection("my-cas")
     * .WithCertificate(new X509Certificate2("my-ca.pem"));
     * var container = builder.AddContainer("my-service", "my-service:latest")
     * .WithCertificateAuthorityCollection(caCollection)
     * .WithCertificateTrustScope(CertificateTrustScope.Override);
     * ```
     * @param scope The scope to apply to custom certificate authorities associated with the resource.
     * @returns The resource builder.
     */
    withCertificateTrustScope(scope) {
        return new ContainerResourcePromiseImpl(this._withCertificateTrustScopeInternal(scope), this._client);
    }
    /** @internal */
    async _withHttpsDeveloperCertificateInternal(password) {
        password = isPromiseLike(password) ? await password : password;
        const rpcArgs = { builder: this._handle };
        if (password !== undefined)
            rpcArgs.password = password;
        const result = await this._client.invokeCapability('Aspire.Hosting/withParameterHttpsDeveloperCertificate', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Indicates that a resource should use the developer certificate key pair for HTTPS endpoints at run time. Currently this indicates use of the ASP.NET Core developer certificate. The developer certificate will only be used when running in local development scenarios; in publish mode resources will use their default certificate configuration.
     *
     * Use the developer certificate for HTTPS/TLS endpoints on a container resource:
     * ```
     * builder.AddContainer("my-service", "my-image")
     * .WithHttpsDeveloperCertificate()
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpsDeveloperCertificate(options) {
        let password = options?.password;
        return new ContainerResourcePromiseImpl(this._withHttpsDeveloperCertificateInternal(password), this._client);
    }
    /** @internal */
    async _withoutHttpsCertificateInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withoutHttpsCertificate', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Disable HTTPS/TLS server certificate configuration for the resource. No HTTPS/TLS termination configuration will be applied.
     *
     * Disable HTTPS certificate configuration for a Redis resource:
     * ```
     * var redis = builder.AddRedis("cache")
     * .WithoutHttpsCertificate();
     * ```
     * @returns The resource builder.
     */
    withoutHttpsCertificate() {
        return new ContainerResourcePromiseImpl(this._withoutHttpsCertificateInternal(), this._client);
    }
    /** @internal */
    async _withRelationshipInternal(resourceBuilder, type) {
        resourceBuilder = isPromiseLike(resourceBuilder) ? await resourceBuilder : resourceBuilder;
        const rpcArgs = { builder: this._handle, resourceBuilder, type };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderRelationship', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a relationship to another resource using its builder.
     * @param resourceBuilder The resource builder that the relationship is to.
     * @param type The relationship type.
     * @returns A resource builder.
     */
    withRelationship(resourceBuilder, type) {
        return new ContainerResourcePromiseImpl(this._withRelationshipInternal(resourceBuilder, type), this._client);
    }
    /** @internal */
    async _withParentRelationshipInternal(parent) {
        parent = isPromiseLike(parent) ? await parent : parent;
        const rpcArgs = { builder: this._handle, parent };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderParentRelationship', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Sets the parent relationship
     * @param parent The parent of `builder`.
     * @returns A resource builder.
     */
    withParentRelationship(parent) {
        return new ContainerResourcePromiseImpl(this._withParentRelationshipInternal(parent), this._client);
    }
    /** @internal */
    async _withChildRelationshipInternal(child) {
        child = isPromiseLike(child) ? await child : child;
        const rpcArgs = { builder: this._handle, child };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderChildRelationship', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Sets a child relationship
     * @param child The child of `builder`.
     * @returns A resource builder.
     */
    withChildRelationship(child) {
        return new ContainerResourcePromiseImpl(this._withChildRelationshipInternal(child), this._client);
    }
    /** @internal */
    async _withIconNameInternal(iconName, iconVariant) {
        const rpcArgs = { builder: this._handle, iconName };
        if (iconVariant !== undefined)
            rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability('Aspire.Hosting/withIconName', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Specifies the icon to use when displaying the resource in the dashboard.
     * @param iconName The name of the FluentUI icon to use. See https://aka.ms/fluentui-system-icons for available icons.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withIconName(iconName, options) {
        const iconVariant = options?.iconVariant;
        return new ContainerResourcePromiseImpl(this._withIconNameInternal(iconName, iconVariant), this._client);
    }
    /** @internal */
    async _withComputeEnvironmentInternal(computeEnvironmentResource) {
        computeEnvironmentResource = isPromiseLike(computeEnvironmentResource) ? await computeEnvironmentResource : computeEnvironmentResource;
        const rpcArgs = { builder: this._handle, computeEnvironmentResource };
        const result = await this._client.invokeCapability('Aspire.Hosting/withComputeEnvironment', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Configures the compute environment for the compute resource.
     *
     * This method allows associating a specific compute environment with the compute resource.
     * @param computeEnvironmentResource The compute environment resource to associate with the compute resource.
     * @returns The resource builder.
     */
    withComputeEnvironment(computeEnvironmentResource) {
        return new ContainerResourcePromiseImpl(this._withComputeEnvironmentInternal(computeEnvironmentResource), this._client);
    }
    /** @internal */
    async _withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName) {
        const rpcArgs = { builder: this._handle, probeType };
        if (path !== undefined)
            rpcArgs.path = path;
        if (initialDelaySeconds !== undefined)
            rpcArgs.initialDelaySeconds = initialDelaySeconds;
        if (periodSeconds !== undefined)
            rpcArgs.periodSeconds = periodSeconds;
        if (timeoutSeconds !== undefined)
            rpcArgs.timeoutSeconds = timeoutSeconds;
        if (failureThreshold !== undefined)
            rpcArgs.failureThreshold = failureThreshold;
        if (successThreshold !== undefined)
            rpcArgs.successThreshold = successThreshold;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpProbe', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP health probe to the resource
     * @param options Additional options.
     */
    withHttpProbe(probeType, options) {
        const path = options?.path;
        const initialDelaySeconds = options?.initialDelaySeconds;
        const periodSeconds = options?.periodSeconds;
        const timeoutSeconds = options?.timeoutSeconds;
        const failureThreshold = options?.failureThreshold;
        const successThreshold = options?.successThreshold;
        const endpointName = options?.endpointName;
        return new ContainerResourcePromiseImpl(this._withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName), this._client);
    }
    /** @internal */
    async _excludeFromMcpInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromMcp', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Exclude the resource from MCP operations using the Aspire MCP server. The resource is excluded from results that return resources, console logs and telemetry.
     * @returns The resource builder.
     */
    excludeFromMcp() {
        return new ContainerResourcePromiseImpl(this._excludeFromMcpInternal(), this._client);
    }
    /** @internal */
    async _withHiddenInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHidden', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists
     *
     * Use this method to hide resources that are implementation details and should never be displayed by default.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @returns The resource builder.
     */
    withHidden() {
        return new ContainerResourcePromiseImpl(this._withHiddenInternal(), this._client);
    }
    /** @internal */
    async _withHiddenOnCompletionInternal(exitCode, exitCodes) {
        const rpcArgs = { builder: this._handle };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        if (exitCodes !== undefined)
            rpcArgs.exitCodes = exitCodes;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHiddenOnCompletion', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists after successful completion
     *
     * This method is useful for one-off resources such as setup scripts, migrations, or build steps that should remain visible while running
     * and then be hidden after successful completion.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHiddenOnCompletion(options) {
        const exitCode = options?.exitCode;
        const exitCodes = options?.exitCodes;
        return new ContainerResourcePromiseImpl(this._withHiddenOnCompletionInternal(exitCode, exitCodes), this._client);
    }
    /** @internal */
    async _withImagePushOptionsInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ContainerImagePushOptionsCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImagePushOptions', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Adds an asynchronous callback to configure container image push options for the resource.
     *
     * This method allows customization of how container images are named and tagged when pushed to a registry using an asynchronous callback.
     * Use this overload when the callback needs to perform asynchronous operations such as retrieving configuration values from external sources.
     * The callback receives a `ContainerImagePushOptionsCallbackContext` that provides access to the resource
     * and the `ContainerImagePushOptions` that can be modified.
     * Multiple callbacks can be registered on the same resource, and they will be invoked in the order they were added.
     * @param callback The asynchronous callback to configure push options.
     * @returns The resource builder.
     */
    withImagePushOptions(callback) {
        return new ContainerResourcePromiseImpl(this._withImagePushOptionsInternal(callback), this._client);
    }
    /** @internal */
    async _withRemoteImageNameInternal(remoteImageName) {
        const rpcArgs = { builder: this._handle, remoteImageName };
        const result = await this._client.invokeCapability('Aspire.Hosting/withRemoteImageName', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Sets the remote image name (without registry endpoint or tag) for container push operations.
     *
     * Use this with `withRemoteImageTag` to fully customize the image reference used for container push operations.
     * @param remoteImageName The remote image name (e.g., "myapp" or "myorg/myapp").
     * @returns The resource builder.
     */
    withRemoteImageName(remoteImageName) {
        return new ContainerResourcePromiseImpl(this._withRemoteImageNameInternal(remoteImageName), this._client);
    }
    /** @internal */
    async _withRemoteImageTagInternal(remoteImageTag) {
        const rpcArgs = { builder: this._handle, remoteImageTag };
        const result = await this._client.invokeCapability('Aspire.Hosting/withRemoteImageTag', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Sets the remote image tag for container push operations.
     *
     * Use this with `withRemoteImageName` to fully customize the image reference used for container push operations.
     * @param remoteImageTag The remote image tag (e.g., "latest", "v1.0.0").
     * @returns The resource builder.
     */
    withRemoteImageTag(remoteImageTag) {
        return new ContainerResourcePromiseImpl(this._withRemoteImageTagInternal(remoteImageTag), this._client);
    }
    /** @internal */
    async _withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new PipelineStepContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined)
            rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined)
            rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined)
            rpcArgs.tags = tags;
        if (description !== undefined)
            rpcArgs.description = description;
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineStepFactory', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a pipeline step to the resource that will be executed during deployment.
     * @param stepName The unique name of the pipeline step.
     * @param callback The callback to execute when the step runs.
     * @param options Additional options.
     * @returns The resource builder for chaining.
     */
    withPipelineStepFactory(stepName, callback, options) {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new ContainerResourcePromiseImpl(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description), this._client);
    }
    /** @internal */
    async _withPipelineConfigurationInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new PipelineConfigurationContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineConfiguration', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to be executed during the pipeline configuration phase, allowing modification of step dependencies and relationships.
     * @param callback The callback function to execute during the configuration phase.
     * @returns The resource builder for chaining.
     */
    withPipelineConfiguration(callback) {
        return new ContainerResourcePromiseImpl(this._withPipelineConfigurationInternal(callback), this._client);
    }
    /** @internal */
    async _withVolumeInternal(target, name, isReadOnly) {
        const rpcArgs = { resource: this._handle, target };
        if (name !== undefined)
            rpcArgs.name = name;
        if (isReadOnly !== undefined)
            rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability('Aspire.Hosting/withVolume', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a volume to a container resource.
     *
     * Volumes persist data across container restarts. Named volumes are managed
     * by Docker/Podman and stored in a system-managed location.
     * Why this wrapper exists: The original `ContainerResourceBuilderExtensions.WithVolume`
     * has parameter order `(name?, target, isReadOnly)` where the optional `name` comes first.
     * This wrapper reorders parameters to `(target, name?, isReadOnly)` so the required `target`
     * parameter comes first, providing a better API for polyglot consumers.
     * @param target The mount path inside the container.
     * @param options Additional options.
     * @returns The same resource builder handle for chaining.
     */
    withVolume(target, options) {
        const name = options?.name;
        const isReadOnly = options?.isReadOnly;
        return new ContainerResourcePromiseImpl(this._withVolumeInternal(target, name, isReadOnly), this._client);
    }
    /**
     * Gets the name of the resource from a builder.
     *
     * Why this wrapper exists: This capability accesses a nested property
     * (`resource.Resource.Name`) which requires a wrapper method. There is no single
     * .NET method that returns just the resource name that could be annotated directly.
     * @returns The resource name.
     */
    async getResourceName() {
        const rpcArgs = { resource: this._handle };
        return await this._client.invokeCapability('Aspire.Hosting/getResourceName', rpcArgs);
    }
    /** @internal */
    async _onBeforeResourceStartedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new BeforeResourceStartedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onBeforeResourceStarted', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the BeforeResourceStarted event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onBeforeResourceStarted(callback) {
        return new ContainerResourcePromiseImpl(this._onBeforeResourceStartedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceStoppedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceStoppedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceStopped', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceStopped event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceStopped(callback) {
        return new ContainerResourcePromiseImpl(this._onResourceStoppedInternal(callback), this._client);
    }
    /** @internal */
    async _onInitializeResourceInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new InitializeResourceEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onInitializeResource', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the InitializeResource event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onInitializeResource(callback) {
        return new ContainerResourcePromiseImpl(this._onInitializeResourceInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceEndpointsAllocatedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceEndpointsAllocatedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceEndpointsAllocated', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceEndpointsAllocated event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceEndpointsAllocated(callback) {
        return new ContainerResourcePromiseImpl(this._onResourceEndpointsAllocatedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceReadyInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceReadyEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceReady', rpcArgs);
        return new ContainerResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceReady event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceReady(callback) {
        return new ContainerResourcePromiseImpl(this._onResourceReadyInternal(callback), this._client);
    }
    /**
     * Creates an execution configuration builder for the specified resource.
     * @returns The execution configuration builder.
     */
    createExecutionConfiguration() {
        const promise = (async () => {
            const rpcArgs = { resource: this._handle };
            const handle = await this._client.invokeCapability('Aspire.Hosting/createExecutionConfiguration', rpcArgs);
            return new ExecutionConfigurationBuilderImpl(handle, this._client);
        })();
        return new ExecutionConfigurationBuilderPromiseImpl(promise, this._client);
    }
}
/**
 * Thenable wrapper for ContainerResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
class ContainerResourcePromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    withContainerRegistry(registry) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withContainerRegistry(registry)), this._client);
    }
    withBindMount(source, target, options) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withBindMount(source, target, options)), this._client);
    }
    withEntrypoint(entrypoint) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withEntrypoint(entrypoint)), this._client);
    }
    withImageTag(tag) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withImageTag(tag)), this._client);
    }
    withImageRegistry(registry) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withImageRegistry(registry)), this._client);
    }
    withImage(image, options) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withImage(image, options)), this._client);
    }
    withImageSHA256(sha256) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withImageSHA256(sha256)), this._client);
    }
    withContainerRuntimeArgs(args) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withContainerRuntimeArgs(args)), this._client);
    }
    withLifetime(lifetime) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withLifetime(lifetime)), this._client);
    }
    withImagePullPolicy(pullPolicy) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withImagePullPolicy(pullPolicy)), this._client);
    }
    publishAsContainer() {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.publishAsContainer()), this._client);
    }
    withDockerfile(contextPath, options) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withDockerfile(contextPath, options)), this._client);
    }
    withDockerfileFactory(contextPath, dockerfileFactory, options) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileFactory(contextPath, dockerfileFactory, options)), this._client);
    }
    withContainerName(name) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withContainerName(name)), this._client);
    }
    withBuildArg(name, value) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withBuildArg(name, value)), this._client);
    }
    withBuildSecret(name, value) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withBuildSecret(name, value)), this._client);
    }
    withContainerCertificatePaths(options) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withContainerCertificatePaths(options)), this._client);
    }
    withDockerfileBuilder(contextPath, callback, options) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileBuilder(contextPath, callback, options)), this._client);
    }
    withDockerfileBaseImage(options) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileBaseImage(options)), this._client);
    }
    withContainerNetworkAlias(alias) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withContainerNetworkAlias(alias)), this._client);
    }
    withMcpServer(options) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withMcpServer(options)), this._client);
    }
    withOtlpExporter(options) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withOtlpExporter(options)), this._client);
    }
    publishAsConnectionString() {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.publishAsConnectionString()), this._client);
    }
    withRequiredCommand(command, options) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withRequiredCommand(command, options)), this._client);
    }
    withSessionLifetime() {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withSessionLifetime()), this._client);
    }
    withPersistentLifetime() {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withPersistentLifetime()), this._client);
    }
    withLifetimeOf(sourceBuilder) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withLifetimeOf(sourceBuilder)), this._client);
    }
    withParentProcessLifetime(parentProcessId) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withParentProcessLifetime(parentProcessId)), this._client);
    }
    withEnvironment(name, value) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withEnvironment(name, value)), this._client);
    }
    withEnvironmentCallback(callback) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withEnvironmentCallback(callback)), this._client);
    }
    withArgs(args) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withArgs(args)), this._client);
    }
    withArgsCallback(callback) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withArgsCallback(callback)), this._client);
    }
    withReferenceEnvironment(options) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withReferenceEnvironment(options)), this._client);
    }
    withReference(source, options) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withReference(source, options)), this._client);
    }
    withEndpointCallback(endpointName, callback, options) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withEndpointCallback(endpointName, callback, options)), this._client);
    }
    withHttpEndpointCallback(callback, options) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withHttpEndpointCallback(callback, options)), this._client);
    }
    withHttpsEndpointCallback(callback, options) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withHttpsEndpointCallback(callback, options)), this._client);
    }
    withEndpoint(options) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withEndpoint(options)), this._client);
    }
    withEndpointProxySupport(proxyEnabled) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withEndpointProxySupport(proxyEnabled)), this._client);
    }
    withHttpEndpoint(options) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withHttpEndpoint(options)), this._client);
    }
    withHttpsEndpoint(options) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withHttpsEndpoint(options)), this._client);
    }
    withExternalHttpEndpoints() {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withExternalHttpEndpoints()), this._client);
    }
    getEndpoint(name) {
        return new EndpointReferencePromiseImpl(this._promise.then(obj => obj.getEndpoint(name)), this._client);
    }
    asHttp2Service() {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.asHttp2Service()), this._client);
    }
    withUrls(callback) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withUrls(callback)), this._client);
    }
    withUrl(url, options) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withUrl(url, options)), this._client);
    }
    withUrlForEndpoint(endpointName, callback) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)), this._client);
    }
    excludeFromManifest() {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.excludeFromManifest()), this._client);
    }
    waitFor(dependency, options) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.waitFor(dependency, options)), this._client);
    }
    waitForStart(dependency, options) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.waitForStart(dependency, options)), this._client);
    }
    withExplicitStart() {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withExplicitStart()), this._client);
    }
    waitForCompletion(dependency, options) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.waitForCompletion(dependency, options)), this._client);
    }
    withHealthCheck(key) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withHealthCheck(key)), this._client);
    }
    withHttpHealthCheck(options) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withHttpHealthCheck(options)), this._client);
    }
    withCommand(name, displayName, executeCommand, options) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)), this._client);
    }
    withProcessCommand(commandName, displayName, options) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommand(commandName, displayName, options)), this._client);
    }
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommandFactory(commandName, displayName, createProcessSpec, options)), this._client);
    }
    withHttpCommand(path, displayName, options) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withHttpCommand(path, displayName, options)), this._client);
    }
    withDeveloperCertificateTrust(trust) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withDeveloperCertificateTrust(trust)), this._client);
    }
    withCertificateTrustScope(scope) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withCertificateTrustScope(scope)), this._client);
    }
    withHttpsDeveloperCertificate(options) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withHttpsDeveloperCertificate(options)), this._client);
    }
    withoutHttpsCertificate() {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withoutHttpsCertificate()), this._client);
    }
    withRelationship(resourceBuilder, type) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withRelationship(resourceBuilder, type)), this._client);
    }
    withParentRelationship(parent) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withParentRelationship(parent)), this._client);
    }
    withChildRelationship(child) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withChildRelationship(child)), this._client);
    }
    withIconName(iconName, options) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withIconName(iconName, options)), this._client);
    }
    withComputeEnvironment(computeEnvironmentResource) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withComputeEnvironment(computeEnvironmentResource)), this._client);
    }
    withHttpProbe(probeType, options) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withHttpProbe(probeType, options)), this._client);
    }
    excludeFromMcp() {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.excludeFromMcp()), this._client);
    }
    withHidden() {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withHidden()), this._client);
    }
    withHiddenOnCompletion(options) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withHiddenOnCompletion(options)), this._client);
    }
    withImagePushOptions(callback) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withImagePushOptions(callback)), this._client);
    }
    withRemoteImageName(remoteImageName) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withRemoteImageName(remoteImageName)), this._client);
    }
    withRemoteImageTag(remoteImageTag) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withRemoteImageTag(remoteImageTag)), this._client);
    }
    withPipelineStepFactory(stepName, callback, options) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)), this._client);
    }
    withPipelineConfiguration(callback) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withPipelineConfiguration(callback)), this._client);
    }
    withVolume(target, options) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.withVolume(target, options)), this._client);
    }
    getResourceName() {
        return this._promise.then(obj => obj.getResourceName());
    }
    onBeforeResourceStarted(callback) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.onBeforeResourceStarted(callback)), this._client);
    }
    onResourceStopped(callback) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.onResourceStopped(callback)), this._client);
    }
    onInitializeResource(callback) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.onInitializeResource(callback)), this._client);
    }
    onResourceEndpointsAllocated(callback) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.onResourceEndpointsAllocated(callback)), this._client);
    }
    onResourceReady(callback) {
        return new ContainerResourcePromiseImpl(this._promise.then(obj => obj.onResourceReady(callback)), this._client);
    }
    createExecutionConfiguration() {
        return new ExecutionConfigurationBuilderPromiseImpl(this._promise.then(obj => obj.createExecutionConfiguration()), this._client);
    }
}
// ============================================================================
// CSharpAppResourceImpl
// ============================================================================
class CSharpAppResourceImpl extends ResourceBuilderBase {
    constructor(handle, client) {
        super(handle, client);
    }
    /** @internal */
    async _withContainerRegistryInternal(registry) {
        registry = isPromiseLike(registry) ? await registry : registry;
        const rpcArgs = { builder: this._handle, registry };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerRegistry', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Configures the resource to use the specified container registry for container image operations.
     *
     * This method adds a `ContainerRegistryReferenceAnnotation` to the resource,
     * indicating that the resource should use the specified container registry for container image operations.
     * @param registry The container registry resource builder.
     * @returns The resource builder for chaining.
     */
    withContainerRegistry(registry) {
        return new CSharpAppResourcePromiseImpl(this._withContainerRegistryInternal(registry), this._client);
    }
    /** @internal */
    async _withDockerfileBaseImageInternal(buildImage, runtimeImage) {
        const rpcArgs = { builder: this._handle };
        if (buildImage !== undefined)
            rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined)
            rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileBaseImage', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Configures custom base images for generated Dockerfiles.
     *
     * This extension method allows customization of the base images used in generated Dockerfiles.
     * For multi-stage Dockerfiles (e.g., Python with UV), you can specify separate build and runtime images.
     * Specify custom base images for a Python application:
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddPythonApp("myapp", "path/to/app", "main.py")
     * .WithDockerfileBaseImage(
     * buildImage: "ghcr.io/astral-sh/uv:python3.12-bookworm-slim",
     * runtimeImage: "python:3.12-slim-bookworm");
     * builder.Build().Run();
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileBaseImage(options) {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new CSharpAppResourcePromiseImpl(this._withDockerfileBaseImageInternal(buildImage, runtimeImage), this._client);
    }
    /** @internal */
    async _withMcpServerInternal(path, endpointName) {
        const rpcArgs = { builder: this._handle };
        if (path !== undefined)
            rpcArgs.path = path;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withMcpServer', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Marks the resource as hosting a Model Context Protocol (MCP) server on the specified endpoint.
     *
     * This method adds an `McpServerEndpointAnnotation` to the resource, enabling the Aspire tooling
     * to discover and proxy the MCP server exposed by the resource.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withMcpServer(options) {
        const path = options?.path;
        const endpointName = options?.endpointName;
        return new CSharpAppResourcePromiseImpl(this._withMcpServerInternal(path, endpointName), this._client);
    }
    /** @internal */
    async _withOtlpExporterInternal(protocol) {
        const rpcArgs = { builder: this._handle };
        if (protocol !== undefined)
            rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability('Aspire.Hosting/withOtlpExporter', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Configures OTLP telemetry export
     * @param options Additional options.
     */
    withOtlpExporter(options) {
        const protocol = options?.protocol;
        return new CSharpAppResourcePromiseImpl(this._withOtlpExporterInternal(protocol), this._client);
    }
    /** @internal */
    async _withReplicasInternal(replicas) {
        const rpcArgs = { builder: this._handle, replicas };
        const result = await this._client.invokeCapability('Aspire.Hosting/withReplicas', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Configures how many replicas of the project should be created for the project.
     * @param replicas The number of replicas.
     * @returns The resource builder.
     */
    withReplicas(replicas) {
        return new CSharpAppResourcePromiseImpl(this._withReplicasInternal(replicas), this._client);
    }
    /** @internal */
    async _disableForwardedHeadersInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/disableForwardedHeaders', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Configures the project to disable forwarded headers when being published.
     * @returns The resource builder.
     */
    disableForwardedHeaders() {
        return new CSharpAppResourcePromiseImpl(this._disableForwardedHeadersInternal(), this._client);
    }
    /** @internal */
    async _publishAsDockerFileInternal(configure) {
        const configureId = configure ? registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new ContainerResourceImpl(objHandle, this._client);
            await configure(obj);
        }) : undefined;
        const rpcArgs = { builder: this._handle };
        if (configure !== undefined)
            rpcArgs.configure = configureId;
        const result = await this._client.invokeCapability('Aspire.Hosting/publishProjectAsDockerFileWithConfigure', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Publishes a project as a Docker file with optional container configuration
     *
     * When the executable resource is converted to a container resource, the arguments to the executable
     * are not used. This is because arguments to the project often contain physical paths that are not valid
     * in the container. The container can be set up with the correct arguments using the `configure` action.
     * @param options Additional options.
     * @returns The resource builder.
     */
    publishAsDockerFile(options) {
        const configure = options?.configure;
        return new CSharpAppResourcePromiseImpl(this._publishAsDockerFileInternal(configure), this._client);
    }
    /** @internal */
    async _withRequiredCommandInternal(command, helpLink) {
        const rpcArgs = { builder: this._handle, command };
        if (helpLink !== undefined)
            rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability('Aspire.Hosting/withRequiredCommand', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Declares that a resource requires a specific command/executable to be available on the local machine PATH before it can start.
     *
     * The command is considered valid if either:
     * 1. It is an absolute or relative path (contains a directory separator) that points to an existing file, or
     * 2. It is discoverable on the current process PATH (respecting PATHEXT on Windows).
     * If the command is not found, a warning message will be logged but the resource will be allowed to attempt to start.
     * @param command The command string (file name or path) that should be validated.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withRequiredCommand(command, options) {
        const helpLink = options?.helpLink;
        return new CSharpAppResourcePromiseImpl(this._withRequiredCommandInternal(command, helpLink), this._client);
    }
    /** @internal */
    async _withSessionLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withSessionLifetime', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a session lifetime.
     * @returns The resource builder.
     */
    withSessionLifetime() {
        return new CSharpAppResourcePromiseImpl(this._withSessionLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withPersistentLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPersistentLifetime', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime.
     * @returns The resource builder.
     */
    withPersistentLifetime() {
        return new CSharpAppResourcePromiseImpl(this._withPersistentLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withLifetimeOfInternal(sourceBuilder) {
        sourceBuilder = isPromiseLike(sourceBuilder) ? await sourceBuilder : sourceBuilder;
        const rpcArgs = { builder: this._handle, sourceBuilder };
        const result = await this._client.invokeCapability('Aspire.Hosting/withLifetimeOf', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to match the lifetime of another resource.
     *
     * The resource lifetime is evaluated from `sourceBuilder` when the application model is prepared, so later lifetime
     * changes to the source resource are reflected by this resource.
     * @param sourceBuilder The resource builder whose lifetime should be used.
     * @returns The resource builder.
     */
    withLifetimeOf(sourceBuilder) {
        return new CSharpAppResourcePromiseImpl(this._withLifetimeOfInternal(sourceBuilder), this._client);
    }
    /** @internal */
    async _withParentProcessLifetimeInternal(parentProcessId) {
        const rpcArgs = { builder: this._handle, parentProcessId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withParentProcessLifetime', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime that ends when a parent process exits.
     * @param parentProcessId The ID of the parent process to monitor.
     * @returns The resource builder.
     */
    withParentProcessLifetime(parentProcessId) {
        return new CSharpAppResourcePromiseImpl(this._withParentProcessLifetimeInternal(parentProcessId), this._client);
    }
    /** @internal */
    async _withEnvironmentInternal(name, value) {
        value = isPromiseLike(value) ? await value : value;
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEnvironment', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /** Sets an environment variable */
    withEnvironment(name, value) {
        return new CSharpAppResourcePromiseImpl(this._withEnvironmentInternal(name, value), this._client);
    }
    /** @internal */
    async _withEnvironmentCallbackInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new EnvironmentCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEnvironmentCallback', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Allows for the population of environment variables on a resource.
     * @param callback A callback that allows for deferred execution for computing many environment variables. This runs after resources have been allocated by the orchestrator and allows access to other resources to resolve computed data, e.g. connection strings, ports.
     * @returns The resource builder.
     */
    withEnvironmentCallback(callback) {
        return new CSharpAppResourcePromiseImpl(this._withEnvironmentCallbackInternal(callback), this._client);
    }
    /** @internal */
    async _withArgsInternal(args) {
        const rpcArgs = { builder: this._handle, args };
        const result = await this._client.invokeCapability('Aspire.Hosting/withArgs', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Adds arguments to be passed to a resource that supports arguments when it is launched.
     * @param args The arguments to be passed to the resource when it is started.
     * @returns The resource builder.
     */
    withArgs(args) {
        return new CSharpAppResourcePromiseImpl(this._withArgsInternal(args), this._client);
    }
    /** @internal */
    async _withArgsCallbackInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new CommandLineArgsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withArgsCallback', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Adds a callback to be executed with a list of command-line arguments when a resource is started.
     * @param callback A callback that allows for deferred execution for computing arguments. This runs after resources have been allocated by the orchestrator and allows access to other resources to resolve computed data, e.g. connection strings, ports.
     * @returns The resource builder.
     */
    withArgsCallback(callback) {
        return new CSharpAppResourcePromiseImpl(this._withArgsCallbackInternal(callback), this._client);
    }
    /** @internal */
    async _withReferenceEnvironmentInternal(options) {
        const rpcArgs = { builder: this._handle, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withReferenceEnvironment', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Configures how information is injected into environment variables when the resource references other resources.
     * @param options Options controlling which reference information is emitted.
     * @returns The resource builder.
     */
    withReferenceEnvironment(options) {
        return new CSharpAppResourcePromiseImpl(this._withReferenceEnvironmentInternal(options), this._client);
    }
    /** @internal */
    async _withReferenceInternal(source, connectionName, optional, name) {
        source = isPromiseLike(source) ? await source : source;
        const rpcArgs = { builder: this._handle, source };
        if (connectionName !== undefined)
            rpcArgs.connectionName = connectionName;
        if (optional !== undefined)
            rpcArgs.optional = optional;
        if (name !== undefined)
            rpcArgs.name = name;
        const result = await this._client.invokeCapability('Aspire.Hosting/withReference', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Adds a reference to another resource
     * @param options Additional options.
     */
    withReference(source, options) {
        const connectionName = options?.connectionName;
        const optional = options?.optional;
        const name = options?.name;
        return new CSharpAppResourcePromiseImpl(this._withReferenceInternal(source, connectionName, optional, name), this._client);
    }
    /** @internal */
    async _withEndpointCallbackInternal(endpointName, callback, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpointCallback', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Updates a named endpoint via callback
     * @param options Additional options.
     */
    withEndpointCallback(endpointName, callback, options) {
        const createIfNotExists = options?.createIfNotExists;
        return new CSharpAppResourcePromiseImpl(this._withEndpointCallbackInternal(endpointName, callback, createIfNotExists), this._client);
    }
    /** @internal */
    async _withHttpEndpointCallbackInternal(callback, name, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        if (name !== undefined)
            rpcArgs.name = name;
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpEndpointCallback', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Updates an HTTP endpoint via callback
     * @param options Additional options.
     */
    withHttpEndpointCallback(callback, options) {
        const name = options?.name;
        const createIfNotExists = options?.createIfNotExists;
        return new CSharpAppResourcePromiseImpl(this._withHttpEndpointCallbackInternal(callback, name, createIfNotExists), this._client);
    }
    /** @internal */
    async _withHttpsEndpointCallbackInternal(callback, name, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        if (name !== undefined)
            rpcArgs.name = name;
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpsEndpointCallback', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Updates an HTTPS endpoint via callback
     * @param options Additional options.
     */
    withHttpsEndpointCallback(callback, options) {
        const name = options?.name;
        const createIfNotExists = options?.createIfNotExists;
        return new CSharpAppResourcePromiseImpl(this._withHttpsEndpointCallbackInternal(callback, name, createIfNotExists), this._client);
    }
    /** @internal */
    async _withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (scheme !== undefined)
            rpcArgs.scheme = scheme;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        if (isExternal !== undefined)
            rpcArgs.isExternal = isExternal;
        if (protocol !== undefined)
            rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpoint', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Adds a network endpoint
     * @param options Additional options.
     * @returns The resource builder.
     */
    withEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const scheme = options?.scheme;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        const isExternal = options?.isExternal;
        const protocol = options?.protocol;
        return new CSharpAppResourcePromiseImpl(this._withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol), this._client);
    }
    /** @internal */
    async _withEndpointProxySupportInternal(proxyEnabled) {
        const rpcArgs = { builder: this._handle, proxyEnabled };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpointProxySupport', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Set whether a resource can use proxied endpoints or whether they should be disabled for all endpoints belonging to the resource. If set to `false`, endpoints belonging to the resource will ignore the configured proxy settings and run proxy-less.
     *
     * This method is intended to support scenarios with persistent lifetime resources where it is desirable for the resource to be accessible over the same
     * port whether the Aspire application is running or not. Proxied endpoints bind ports that are only accessible while the Aspire application is running.
     * The user needs to be careful to ensure that endpoints are using unique ports when disabling proxy support as by default for proxy-less
     * endpoints, Aspire will allocate the target port as the host port, which will increase the chance of port conflicts.
     * @param proxyEnabled Should endpoints for the resource support using a proxy?
     * @returns The resource builder.
     */
    withEndpointProxySupport(proxyEnabled) {
        return new CSharpAppResourcePromiseImpl(this._withEndpointProxySupportInternal(proxyEnabled), this._client);
    }
    /** @internal */
    async _withHttpEndpointInternal(port, targetPort, name, env, isProxied) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpEndpoint', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP endpoint
     *
     * If an endpoint with the same name already exists on the resource, the existing endpoint is updated
     * with any non-null parameter values. Parameters left as `null` will not modify the existing endpoint's values.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new CSharpAppResourcePromiseImpl(this._withHttpEndpointInternal(port, targetPort, name, env, isProxied), this._client);
    }
    /** @internal */
    async _withHttpsEndpointInternal(port, targetPort, name, env, isProxied) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpsEndpoint', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTPS endpoint
     *
     * If an endpoint with the same name already exists on the resource, the existing endpoint is updated
     * with any non-null parameter values. Parameters left as `null` will not modify the existing endpoint's values.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpsEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new CSharpAppResourcePromiseImpl(this._withHttpsEndpointInternal(port, targetPort, name, env, isProxied), this._client);
    }
    /** @internal */
    async _withExternalHttpEndpointsInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExternalHttpEndpoints', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Marks existing http or https endpoints on a resource as external.
     * @returns The resource builder.
     */
    withExternalHttpEndpoints() {
        return new CSharpAppResourcePromiseImpl(this._withExternalHttpEndpointsInternal(), this._client);
    }
    /**
     * Gets an endpoint reference
     * @param name The name of the endpoint.
     * @returns An `EndpointReference` that can be used to resolve the address of the endpoint after resource allocation has occurred.
     */
    getEndpoint(name) {
        const promise = (async () => {
            const rpcArgs = { builder: this._handle, name };
            const handle = await this._client.invokeCapability('Aspire.Hosting/getEndpoint', rpcArgs);
            return new EndpointReferenceImpl(handle, this._client);
        })();
        return new EndpointReferencePromiseImpl(promise, this._client);
    }
    /** @internal */
    async _asHttp2ServiceInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/asHttp2Service', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to mark all endpoints' transport as HTTP/2. This is useful for HTTP/2 services that need prior knowledge.
     * @returns The resource builder.
     */
    asHttp2Service() {
        return new CSharpAppResourcePromiseImpl(this._asHttp2ServiceInternal(), this._client);
    }
    /** @internal */
    async _withUrlsInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new ResourceUrlsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrls', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to customize the URLs displayed for the resource.
     * @param callback The callback that will customize URLs for the resource.
     * @returns The resource builder.
     */
    withUrls(callback) {
        return new CSharpAppResourcePromiseImpl(this._withUrlsInternal(callback), this._client);
    }
    /** @internal */
    async _withUrlInternal(url, displayText) {
        const rpcArgs = { builder: this._handle, url };
        if (displayText !== undefined)
            rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrl', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Adds or modifies displayed URLs
     * @param options Additional options.
     */
    withUrl(url, options) {
        const displayText = options?.displayText;
        return new CSharpAppResourcePromiseImpl(this._withUrlInternal(url, displayText), this._client);
    }
    /** @internal */
    async _withUrlForEndpointInternal(endpointName, callback) {
        const callbackId = registerCallback(async (objData) => {
            const obj = wrapIfHandle(objData);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrlForEndpoint', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to update the URL displayed for the endpoint with the specified name.
     * @param endpointName The name of the endpoint to customize the URL for.
     * @param callback The callback that will customize the URL.
     * @returns The resource builder.
     */
    withUrlForEndpoint(endpointName, callback) {
        return new CSharpAppResourcePromiseImpl(this._withUrlForEndpointInternal(endpointName, callback), this._client);
    }
    /** @internal */
    async _publishWithContainerFilesInternal(source, destinationPath) {
        source = isPromiseLike(source) ? await source : source;
        const rpcArgs = { builder: this._handle, source, destinationPath };
        const result = await this._client.invokeCapability('Aspire.Hosting/publishWithContainerFilesFromResource', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Configures the resource to copy container files from the specified source resource during publishing.
     * @param source The resource which contains the container files to be copied.
     * @param destinationPath The destination path within the resource's container where the files will be copied.
     */
    publishWithContainerFiles(source, destinationPath) {
        return new CSharpAppResourcePromiseImpl(this._publishWithContainerFilesInternal(source, destinationPath), this._client);
    }
    /** @internal */
    async _excludeFromManifestInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromManifest', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Excludes a resource from being published to the manifest.
     * @returns The resource builder.
     */
    excludeFromManifest() {
        return new CSharpAppResourcePromiseImpl(this._excludeFromManifestInternal(), this._client);
    }
    /** @internal */
    async _waitForInternal(dependency, waitBehavior) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (waitBehavior !== undefined)
            rpcArgs.waitBehavior = waitBehavior;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitFor', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Waits for another resource to be ready
     * @param options Additional options.
     */
    waitFor(dependency, options) {
        const waitBehavior = options?.waitBehavior;
        return new CSharpAppResourcePromiseImpl(this._waitForInternal(dependency, waitBehavior), this._client);
    }
    /** @internal */
    async _waitForStartInternal(dependency, waitBehavior) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (waitBehavior !== undefined)
            rpcArgs.waitBehavior = waitBehavior;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitForStart', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Waits for another resource to start
     * @param options Additional options.
     */
    waitForStart(dependency, options) {
        const waitBehavior = options?.waitBehavior;
        return new CSharpAppResourcePromiseImpl(this._waitForStartInternal(dependency, waitBehavior), this._client);
    }
    /** @internal */
    async _withExplicitStartInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExplicitStart', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Prevents resource from starting automatically
     * @returns The resource builder.
     */
    withExplicitStart() {
        return new CSharpAppResourcePromiseImpl(this._withExplicitStartInternal(), this._client);
    }
    /** @internal */
    async _waitForCompletionInternal(dependency, exitCode) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitForResourceCompletion', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Waits for the dependency resource to enter the Exited or Finished state before starting the resource.
     * @param dependency The resource builder for the dependency resource.
     * @param options Additional options.
     * @returns The resource builder.
     */
    waitForCompletion(dependency, options) {
        const exitCode = options?.exitCode;
        return new CSharpAppResourcePromiseImpl(this._waitForCompletionInternal(dependency, exitCode), this._client);
    }
    /** @internal */
    async _withHealthCheckInternal(key) {
        const rpcArgs = { builder: this._handle, key };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHealthCheck', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Adds a health check by key
     * @param key The key for the health check.
     * @returns The resource builder.
     */
    withHealthCheck(key) {
        return new CSharpAppResourcePromiseImpl(this._withHealthCheckInternal(key), this._client);
    }
    /** @internal */
    async _withHttpHealthCheckInternal(path, statusCode, endpointName) {
        const rpcArgs = { builder: this._handle };
        if (path !== undefined)
            rpcArgs.path = path;
        if (statusCode !== undefined)
            rpcArgs.statusCode = statusCode;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpHealthCheck', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Adds a health check to the resource which is mapped to a specific endpoint.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpHealthCheck(options) {
        const path = options?.path;
        const statusCode = options?.statusCode;
        const endpointName = options?.endpointName;
        return new CSharpAppResourcePromiseImpl(this._withHttpHealthCheckInternal(path, statusCode, endpointName), this._client);
    }
    /** @internal */
    async _withCommandInternal(name, displayName, executeCommand, commandOptions) {
        const executeCommandId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined)
            rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability('Aspire.Hosting/withCommand', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Adds a resource command
     *
     * The `WithCommand` method is used to add commands to the resource. Commands are displayed in the dashboard
     * and can be executed by a user using the dashboard UI.
     * When a command is executed, the `executeCommand` callback is called and is run inside the Aspire host.
     * @param name The name of the command. The name uniquely identifies the command.
     * @param displayName The display name visible in UI.
     * @param executeCommand A callback that is executed when the command is executed. The callback is run inside the Aspire host. The callback result is used to indicate success or failure in the UI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withCommand(name, displayName, executeCommand, options) {
        const commandOptions = options?.commandOptions;
        return new CSharpAppResourcePromiseImpl(this._withCommandInternal(name, displayName, executeCommand, commandOptions), this._client);
    }
    /** @internal */
    async _withProcessCommandInternal(commandName, displayName, options) {
        const rpcArgs = { builder: this._handle, commandName, displayName, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommand', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /** Adds a command to the resource that starts a local process when invoked. */
    withProcessCommand(commandName, displayName, options) {
        return new CSharpAppResourcePromiseImpl(this._withProcessCommandInternal(commandName, displayName, options), this._client);
    }
    /** @internal */
    async _withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options) {
        const createProcessSpecId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await createProcessSpec(arg);
        });
        const rpcArgs = { builder: this._handle, commandName, displayName, createProcessSpec: createProcessSpecId };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommandFactory', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Adds a command to the resource that starts a local process created by a callback when invoked.
     * @param options Additional options.
     */
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new CSharpAppResourcePromiseImpl(this._withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options), this._client);
    }
    /** @internal */
    async _withHttpCommandInternal(path, displayName, options) {
        const rpcArgs = { builder: this._handle, path, displayName };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpCommand', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP resource command
     * @param options Additional options.
     */
    withHttpCommand(path, displayName, options) {
        return new CSharpAppResourcePromiseImpl(this._withHttpCommandInternal(path, displayName, options), this._client);
    }
    /** @internal */
    async _withDeveloperCertificateTrustInternal(trust) {
        const rpcArgs = { builder: this._handle, trust };
        const result = await this._client.invokeCapability('Aspire.Hosting/withDeveloperCertificateTrust', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Indicates whether developer certificates should be treated as trusted certificate authorities for the resource at run time. Currently this indicates trust for the ASP.NET Core developer certificate. The developer certificate will only be trusted when running in local development scenarios; in publish mode resources will use their default certificate trust.
     *
     * Disable trust for app host managed developer certificate(s) for a container resource.
     * ```
     * var container = builder.AddContainer("my-service", "my-service:latest")
     * .WithDeveloperCertificateTrust(false);
     * ```
     * Disable automatic trust for app host managed developer certificate(s), but explicitly enable it for a specific resource.
     * ```
     * var builder = DistributedApplication.CreateBuilder(new DistributedApplicationOptions()
     * {
     * Args = args,
     * TrustDeveloperCertificate = false,
     * });
     * var project = builder.AddProject<MyService>("my-service")
     * .WithDeveloperCertificateTrust(true);
     * ```
     * @param trust Indicates whether the developer certificate should be treated as trusted.
     * @returns The resource builder.
     */
    withDeveloperCertificateTrust(trust) {
        return new CSharpAppResourcePromiseImpl(this._withDeveloperCertificateTrustInternal(trust), this._client);
    }
    /** @internal */
    async _withCertificateTrustScopeInternal(scope) {
        const rpcArgs = { builder: this._handle, scope };
        const result = await this._client.invokeCapability('Aspire.Hosting/withCertificateTrustScope', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Sets the certificate trust scope
     *
     * The default scope if not overridden is `Append` which means that custom certificate
     * authorities should be appended to the default trusted certificate authorities for the resource. Setting the scope to
     * `Override` indicates the set of certificates in referenced
     * `CertificateAuthorityCollection` (and optionally Aspire developer certificiates) should be used as the
     * exclusive source of trust for a resource.
     * In all cases, this is a best effort implementation as not all resources support full customization of certificate
     * trust.
     * Set the scope for custom certificate authorities to override the default trusted certificate authorities for a container resource.
     * ```
     * var caCollection = builder.AddCertificateAuthorityCollection("my-cas")
     * .WithCertificate(new X509Certificate2("my-ca.pem"));
     * var container = builder.AddContainer("my-service", "my-service:latest")
     * .WithCertificateAuthorityCollection(caCollection)
     * .WithCertificateTrustScope(CertificateTrustScope.Override);
     * ```
     * @param scope The scope to apply to custom certificate authorities associated with the resource.
     * @returns The resource builder.
     */
    withCertificateTrustScope(scope) {
        return new CSharpAppResourcePromiseImpl(this._withCertificateTrustScopeInternal(scope), this._client);
    }
    /** @internal */
    async _withHttpsDeveloperCertificateInternal(password) {
        password = isPromiseLike(password) ? await password : password;
        const rpcArgs = { builder: this._handle };
        if (password !== undefined)
            rpcArgs.password = password;
        const result = await this._client.invokeCapability('Aspire.Hosting/withParameterHttpsDeveloperCertificate', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Indicates that a resource should use the developer certificate key pair for HTTPS endpoints at run time. Currently this indicates use of the ASP.NET Core developer certificate. The developer certificate will only be used when running in local development scenarios; in publish mode resources will use their default certificate configuration.
     *
     * Use the developer certificate for HTTPS/TLS endpoints on a container resource:
     * ```
     * builder.AddContainer("my-service", "my-image")
     * .WithHttpsDeveloperCertificate()
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpsDeveloperCertificate(options) {
        let password = options?.password;
        return new CSharpAppResourcePromiseImpl(this._withHttpsDeveloperCertificateInternal(password), this._client);
    }
    /** @internal */
    async _withoutHttpsCertificateInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withoutHttpsCertificate', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Disable HTTPS/TLS server certificate configuration for the resource. No HTTPS/TLS termination configuration will be applied.
     *
     * Disable HTTPS certificate configuration for a Redis resource:
     * ```
     * var redis = builder.AddRedis("cache")
     * .WithoutHttpsCertificate();
     * ```
     * @returns The resource builder.
     */
    withoutHttpsCertificate() {
        return new CSharpAppResourcePromiseImpl(this._withoutHttpsCertificateInternal(), this._client);
    }
    /** @internal */
    async _withRelationshipInternal(resourceBuilder, type) {
        resourceBuilder = isPromiseLike(resourceBuilder) ? await resourceBuilder : resourceBuilder;
        const rpcArgs = { builder: this._handle, resourceBuilder, type };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderRelationship', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Adds a relationship to another resource using its builder.
     * @param resourceBuilder The resource builder that the relationship is to.
     * @param type The relationship type.
     * @returns A resource builder.
     */
    withRelationship(resourceBuilder, type) {
        return new CSharpAppResourcePromiseImpl(this._withRelationshipInternal(resourceBuilder, type), this._client);
    }
    /** @internal */
    async _withParentRelationshipInternal(parent) {
        parent = isPromiseLike(parent) ? await parent : parent;
        const rpcArgs = { builder: this._handle, parent };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderParentRelationship', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Sets the parent relationship
     * @param parent The parent of `builder`.
     * @returns A resource builder.
     */
    withParentRelationship(parent) {
        return new CSharpAppResourcePromiseImpl(this._withParentRelationshipInternal(parent), this._client);
    }
    /** @internal */
    async _withChildRelationshipInternal(child) {
        child = isPromiseLike(child) ? await child : child;
        const rpcArgs = { builder: this._handle, child };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderChildRelationship', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Sets a child relationship
     * @param child The child of `builder`.
     * @returns A resource builder.
     */
    withChildRelationship(child) {
        return new CSharpAppResourcePromiseImpl(this._withChildRelationshipInternal(child), this._client);
    }
    /** @internal */
    async _withIconNameInternal(iconName, iconVariant) {
        const rpcArgs = { builder: this._handle, iconName };
        if (iconVariant !== undefined)
            rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability('Aspire.Hosting/withIconName', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Specifies the icon to use when displaying the resource in the dashboard.
     * @param iconName The name of the FluentUI icon to use. See https://aka.ms/fluentui-system-icons for available icons.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withIconName(iconName, options) {
        const iconVariant = options?.iconVariant;
        return new CSharpAppResourcePromiseImpl(this._withIconNameInternal(iconName, iconVariant), this._client);
    }
    /** @internal */
    async _withComputeEnvironmentInternal(computeEnvironmentResource) {
        computeEnvironmentResource = isPromiseLike(computeEnvironmentResource) ? await computeEnvironmentResource : computeEnvironmentResource;
        const rpcArgs = { builder: this._handle, computeEnvironmentResource };
        const result = await this._client.invokeCapability('Aspire.Hosting/withComputeEnvironment', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Configures the compute environment for the compute resource.
     *
     * This method allows associating a specific compute environment with the compute resource.
     * @param computeEnvironmentResource The compute environment resource to associate with the compute resource.
     * @returns The resource builder.
     */
    withComputeEnvironment(computeEnvironmentResource) {
        return new CSharpAppResourcePromiseImpl(this._withComputeEnvironmentInternal(computeEnvironmentResource), this._client);
    }
    /** @internal */
    async _withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName) {
        const rpcArgs = { builder: this._handle, probeType };
        if (path !== undefined)
            rpcArgs.path = path;
        if (initialDelaySeconds !== undefined)
            rpcArgs.initialDelaySeconds = initialDelaySeconds;
        if (periodSeconds !== undefined)
            rpcArgs.periodSeconds = periodSeconds;
        if (timeoutSeconds !== undefined)
            rpcArgs.timeoutSeconds = timeoutSeconds;
        if (failureThreshold !== undefined)
            rpcArgs.failureThreshold = failureThreshold;
        if (successThreshold !== undefined)
            rpcArgs.successThreshold = successThreshold;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpProbe', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP health probe to the resource
     * @param options Additional options.
     */
    withHttpProbe(probeType, options) {
        const path = options?.path;
        const initialDelaySeconds = options?.initialDelaySeconds;
        const periodSeconds = options?.periodSeconds;
        const timeoutSeconds = options?.timeoutSeconds;
        const failureThreshold = options?.failureThreshold;
        const successThreshold = options?.successThreshold;
        const endpointName = options?.endpointName;
        return new CSharpAppResourcePromiseImpl(this._withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName), this._client);
    }
    /** @internal */
    async _excludeFromMcpInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromMcp', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Exclude the resource from MCP operations using the Aspire MCP server. The resource is excluded from results that return resources, console logs and telemetry.
     * @returns The resource builder.
     */
    excludeFromMcp() {
        return new CSharpAppResourcePromiseImpl(this._excludeFromMcpInternal(), this._client);
    }
    /** @internal */
    async _withHiddenInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHidden', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists
     *
     * Use this method to hide resources that are implementation details and should never be displayed by default.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @returns The resource builder.
     */
    withHidden() {
        return new CSharpAppResourcePromiseImpl(this._withHiddenInternal(), this._client);
    }
    /** @internal */
    async _withHiddenOnCompletionInternal(exitCode, exitCodes) {
        const rpcArgs = { builder: this._handle };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        if (exitCodes !== undefined)
            rpcArgs.exitCodes = exitCodes;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHiddenOnCompletion', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists after successful completion
     *
     * This method is useful for one-off resources such as setup scripts, migrations, or build steps that should remain visible while running
     * and then be hidden after successful completion.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHiddenOnCompletion(options) {
        const exitCode = options?.exitCode;
        const exitCodes = options?.exitCodes;
        return new CSharpAppResourcePromiseImpl(this._withHiddenOnCompletionInternal(exitCode, exitCodes), this._client);
    }
    /** @internal */
    async _withImagePushOptionsInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ContainerImagePushOptionsCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImagePushOptions', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Adds an asynchronous callback to configure container image push options for the resource.
     *
     * This method allows customization of how container images are named and tagged when pushed to a registry using an asynchronous callback.
     * Use this overload when the callback needs to perform asynchronous operations such as retrieving configuration values from external sources.
     * The callback receives a `ContainerImagePushOptionsCallbackContext` that provides access to the resource
     * and the `ContainerImagePushOptions` that can be modified.
     * Multiple callbacks can be registered on the same resource, and they will be invoked in the order they were added.
     * @param callback The asynchronous callback to configure push options.
     * @returns The resource builder.
     */
    withImagePushOptions(callback) {
        return new CSharpAppResourcePromiseImpl(this._withImagePushOptionsInternal(callback), this._client);
    }
    /** @internal */
    async _withRemoteImageNameInternal(remoteImageName) {
        const rpcArgs = { builder: this._handle, remoteImageName };
        const result = await this._client.invokeCapability('Aspire.Hosting/withRemoteImageName', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Sets the remote image name (without registry endpoint or tag) for container push operations.
     *
     * Use this with `withRemoteImageTag` to fully customize the image reference used for container push operations.
     * @param remoteImageName The remote image name (e.g., "myapp" or "myorg/myapp").
     * @returns The resource builder.
     */
    withRemoteImageName(remoteImageName) {
        return new CSharpAppResourcePromiseImpl(this._withRemoteImageNameInternal(remoteImageName), this._client);
    }
    /** @internal */
    async _withRemoteImageTagInternal(remoteImageTag) {
        const rpcArgs = { builder: this._handle, remoteImageTag };
        const result = await this._client.invokeCapability('Aspire.Hosting/withRemoteImageTag', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Sets the remote image tag for container push operations.
     *
     * Use this with `withRemoteImageName` to fully customize the image reference used for container push operations.
     * @param remoteImageTag The remote image tag (e.g., "latest", "v1.0.0").
     * @returns The resource builder.
     */
    withRemoteImageTag(remoteImageTag) {
        return new CSharpAppResourcePromiseImpl(this._withRemoteImageTagInternal(remoteImageTag), this._client);
    }
    /** @internal */
    async _withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new PipelineStepContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined)
            rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined)
            rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined)
            rpcArgs.tags = tags;
        if (description !== undefined)
            rpcArgs.description = description;
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineStepFactory', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Adds a pipeline step to the resource that will be executed during deployment.
     * @param stepName The unique name of the pipeline step.
     * @param callback The callback to execute when the step runs.
     * @param options Additional options.
     * @returns The resource builder for chaining.
     */
    withPipelineStepFactory(stepName, callback, options) {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new CSharpAppResourcePromiseImpl(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description), this._client);
    }
    /** @internal */
    async _withPipelineConfigurationInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new PipelineConfigurationContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineConfiguration', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to be executed during the pipeline configuration phase, allowing modification of step dependencies and relationships.
     * @param callback The callback function to execute during the configuration phase.
     * @returns The resource builder for chaining.
     */
    withPipelineConfiguration(callback) {
        return new CSharpAppResourcePromiseImpl(this._withPipelineConfigurationInternal(callback), this._client);
    }
    /**
     * Gets the name of the resource from a builder.
     *
     * Why this wrapper exists: This capability accesses a nested property
     * (`resource.Resource.Name`) which requires a wrapper method. There is no single
     * .NET method that returns just the resource name that could be annotated directly.
     * @returns The resource name.
     */
    async getResourceName() {
        const rpcArgs = { resource: this._handle };
        return await this._client.invokeCapability('Aspire.Hosting/getResourceName', rpcArgs);
    }
    /** @internal */
    async _onBeforeResourceStartedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new BeforeResourceStartedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onBeforeResourceStarted', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the BeforeResourceStarted event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onBeforeResourceStarted(callback) {
        return new CSharpAppResourcePromiseImpl(this._onBeforeResourceStartedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceStoppedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceStoppedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceStopped', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceStopped event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceStopped(callback) {
        return new CSharpAppResourcePromiseImpl(this._onResourceStoppedInternal(callback), this._client);
    }
    /** @internal */
    async _onInitializeResourceInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new InitializeResourceEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onInitializeResource', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the InitializeResource event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onInitializeResource(callback) {
        return new CSharpAppResourcePromiseImpl(this._onInitializeResourceInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceEndpointsAllocatedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceEndpointsAllocatedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceEndpointsAllocated', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceEndpointsAllocated event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceEndpointsAllocated(callback) {
        return new CSharpAppResourcePromiseImpl(this._onResourceEndpointsAllocatedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceReadyInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceReadyEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceReady', rpcArgs);
        return new CSharpAppResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceReady event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceReady(callback) {
        return new CSharpAppResourcePromiseImpl(this._onResourceReadyInternal(callback), this._client);
    }
    /**
     * Creates an execution configuration builder for the specified resource.
     * @returns The execution configuration builder.
     */
    createExecutionConfiguration() {
        const promise = (async () => {
            const rpcArgs = { resource: this._handle };
            const handle = await this._client.invokeCapability('Aspire.Hosting/createExecutionConfiguration', rpcArgs);
            return new ExecutionConfigurationBuilderImpl(handle, this._client);
        })();
        return new ExecutionConfigurationBuilderPromiseImpl(promise, this._client);
    }
}
/**
 * Thenable wrapper for CSharpAppResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
class CSharpAppResourcePromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    withContainerRegistry(registry) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withContainerRegistry(registry)), this._client);
    }
    withDockerfileBaseImage(options) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileBaseImage(options)), this._client);
    }
    withMcpServer(options) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withMcpServer(options)), this._client);
    }
    withOtlpExporter(options) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withOtlpExporter(options)), this._client);
    }
    withReplicas(replicas) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withReplicas(replicas)), this._client);
    }
    disableForwardedHeaders() {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.disableForwardedHeaders()), this._client);
    }
    publishAsDockerFile(options) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.publishAsDockerFile(options)), this._client);
    }
    withRequiredCommand(command, options) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withRequiredCommand(command, options)), this._client);
    }
    withSessionLifetime() {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withSessionLifetime()), this._client);
    }
    withPersistentLifetime() {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withPersistentLifetime()), this._client);
    }
    withLifetimeOf(sourceBuilder) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withLifetimeOf(sourceBuilder)), this._client);
    }
    withParentProcessLifetime(parentProcessId) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withParentProcessLifetime(parentProcessId)), this._client);
    }
    withEnvironment(name, value) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withEnvironment(name, value)), this._client);
    }
    withEnvironmentCallback(callback) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withEnvironmentCallback(callback)), this._client);
    }
    withArgs(args) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withArgs(args)), this._client);
    }
    withArgsCallback(callback) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withArgsCallback(callback)), this._client);
    }
    withReferenceEnvironment(options) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withReferenceEnvironment(options)), this._client);
    }
    withReference(source, options) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withReference(source, options)), this._client);
    }
    withEndpointCallback(endpointName, callback, options) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withEndpointCallback(endpointName, callback, options)), this._client);
    }
    withHttpEndpointCallback(callback, options) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpEndpointCallback(callback, options)), this._client);
    }
    withHttpsEndpointCallback(callback, options) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpsEndpointCallback(callback, options)), this._client);
    }
    withEndpoint(options) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withEndpoint(options)), this._client);
    }
    withEndpointProxySupport(proxyEnabled) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withEndpointProxySupport(proxyEnabled)), this._client);
    }
    withHttpEndpoint(options) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpEndpoint(options)), this._client);
    }
    withHttpsEndpoint(options) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpsEndpoint(options)), this._client);
    }
    withExternalHttpEndpoints() {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withExternalHttpEndpoints()), this._client);
    }
    getEndpoint(name) {
        return new EndpointReferencePromiseImpl(this._promise.then(obj => obj.getEndpoint(name)), this._client);
    }
    asHttp2Service() {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.asHttp2Service()), this._client);
    }
    withUrls(callback) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withUrls(callback)), this._client);
    }
    withUrl(url, options) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withUrl(url, options)), this._client);
    }
    withUrlForEndpoint(endpointName, callback) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)), this._client);
    }
    publishWithContainerFiles(source, destinationPath) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.publishWithContainerFiles(source, destinationPath)), this._client);
    }
    excludeFromManifest() {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.excludeFromManifest()), this._client);
    }
    waitFor(dependency, options) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.waitFor(dependency, options)), this._client);
    }
    waitForStart(dependency, options) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.waitForStart(dependency, options)), this._client);
    }
    withExplicitStart() {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withExplicitStart()), this._client);
    }
    waitForCompletion(dependency, options) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.waitForCompletion(dependency, options)), this._client);
    }
    withHealthCheck(key) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withHealthCheck(key)), this._client);
    }
    withHttpHealthCheck(options) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpHealthCheck(options)), this._client);
    }
    withCommand(name, displayName, executeCommand, options) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)), this._client);
    }
    withProcessCommand(commandName, displayName, options) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommand(commandName, displayName, options)), this._client);
    }
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommandFactory(commandName, displayName, createProcessSpec, options)), this._client);
    }
    withHttpCommand(path, displayName, options) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpCommand(path, displayName, options)), this._client);
    }
    withDeveloperCertificateTrust(trust) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withDeveloperCertificateTrust(trust)), this._client);
    }
    withCertificateTrustScope(scope) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withCertificateTrustScope(scope)), this._client);
    }
    withHttpsDeveloperCertificate(options) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpsDeveloperCertificate(options)), this._client);
    }
    withoutHttpsCertificate() {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withoutHttpsCertificate()), this._client);
    }
    withRelationship(resourceBuilder, type) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withRelationship(resourceBuilder, type)), this._client);
    }
    withParentRelationship(parent) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withParentRelationship(parent)), this._client);
    }
    withChildRelationship(child) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withChildRelationship(child)), this._client);
    }
    withIconName(iconName, options) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withIconName(iconName, options)), this._client);
    }
    withComputeEnvironment(computeEnvironmentResource) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withComputeEnvironment(computeEnvironmentResource)), this._client);
    }
    withHttpProbe(probeType, options) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpProbe(probeType, options)), this._client);
    }
    excludeFromMcp() {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.excludeFromMcp()), this._client);
    }
    withHidden() {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withHidden()), this._client);
    }
    withHiddenOnCompletion(options) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withHiddenOnCompletion(options)), this._client);
    }
    withImagePushOptions(callback) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withImagePushOptions(callback)), this._client);
    }
    withRemoteImageName(remoteImageName) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withRemoteImageName(remoteImageName)), this._client);
    }
    withRemoteImageTag(remoteImageTag) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withRemoteImageTag(remoteImageTag)), this._client);
    }
    withPipelineStepFactory(stepName, callback, options) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)), this._client);
    }
    withPipelineConfiguration(callback) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.withPipelineConfiguration(callback)), this._client);
    }
    getResourceName() {
        return this._promise.then(obj => obj.getResourceName());
    }
    onBeforeResourceStarted(callback) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.onBeforeResourceStarted(callback)), this._client);
    }
    onResourceStopped(callback) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.onResourceStopped(callback)), this._client);
    }
    onInitializeResource(callback) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.onInitializeResource(callback)), this._client);
    }
    onResourceEndpointsAllocated(callback) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.onResourceEndpointsAllocated(callback)), this._client);
    }
    onResourceReady(callback) {
        return new CSharpAppResourcePromiseImpl(this._promise.then(obj => obj.onResourceReady(callback)), this._client);
    }
    createExecutionConfiguration() {
        return new ExecutionConfigurationBuilderPromiseImpl(this._promise.then(obj => obj.createExecutionConfiguration()), this._client);
    }
}
// ============================================================================
// DotnetToolResourceImpl
// ============================================================================
class DotnetToolResourceImpl extends ResourceBuilderBase {
    constructor(handle, client) {
        super(handle, client);
    }
    /** @internal */
    async _withContainerRegistryInternal(registry) {
        registry = isPromiseLike(registry) ? await registry : registry;
        const rpcArgs = { builder: this._handle, registry };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerRegistry', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Configures the resource to use the specified container registry for container image operations.
     *
     * This method adds a `ContainerRegistryReferenceAnnotation` to the resource,
     * indicating that the resource should use the specified container registry for container image operations.
     * @param registry The container registry resource builder.
     * @returns The resource builder for chaining.
     */
    withContainerRegistry(registry) {
        return new DotnetToolResourcePromiseImpl(this._withContainerRegistryInternal(registry), this._client);
    }
    /** @internal */
    async _withDockerfileBaseImageInternal(buildImage, runtimeImage) {
        const rpcArgs = { builder: this._handle };
        if (buildImage !== undefined)
            rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined)
            rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileBaseImage', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Configures custom base images for generated Dockerfiles.
     *
     * This extension method allows customization of the base images used in generated Dockerfiles.
     * For multi-stage Dockerfiles (e.g., Python with UV), you can specify separate build and runtime images.
     * Specify custom base images for a Python application:
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddPythonApp("myapp", "path/to/app", "main.py")
     * .WithDockerfileBaseImage(
     * buildImage: "ghcr.io/astral-sh/uv:python3.12-bookworm-slim",
     * runtimeImage: "python:3.12-slim-bookworm");
     * builder.Build().Run();
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileBaseImage(options) {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new DotnetToolResourcePromiseImpl(this._withDockerfileBaseImageInternal(buildImage, runtimeImage), this._client);
    }
    /** @internal */
    async _withToolPackageInternal(packageId) {
        const rpcArgs = { builder: this._handle, packageId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withToolPackage', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Sets the package identifier for the tool configuration associated with the resource builder.
     * @param packageId The package identifier to assign to the tool configuration. Cannot be null.
     * @returns The resource builder.
     */
    withToolPackage(packageId) {
        return new DotnetToolResourcePromiseImpl(this._withToolPackageInternal(packageId), this._client);
    }
    /** @internal */
    async _withToolVersionInternal(version) {
        const rpcArgs = { builder: this._handle, version };
        const result = await this._client.invokeCapability('Aspire.Hosting/withToolVersion', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Sets the package version for a tool to use.
     * @param version The package version to use
     * @returns The resource builder.
     */
    withToolVersion(version) {
        return new DotnetToolResourcePromiseImpl(this._withToolVersionInternal(version), this._client);
    }
    /** @internal */
    async _withToolPrereleaseInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withToolPrerelease', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Allows prerelease versions of the tool to be used
     * @returns The resource builder.
     */
    withToolPrerelease() {
        return new DotnetToolResourcePromiseImpl(this._withToolPrereleaseInternal(), this._client);
    }
    /** @internal */
    async _withToolSourceInternal(source) {
        const rpcArgs = { builder: this._handle, source };
        const result = await this._client.invokeCapability('Aspire.Hosting/withToolSource', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Adds a NuGet package source for tool acquisition.
     * @param source The source to add.
     * @returns The resource builder.
     */
    withToolSource(source) {
        return new DotnetToolResourcePromiseImpl(this._withToolSourceInternal(source), this._client);
    }
    /** @internal */
    async _withToolIgnoreExistingFeedsInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withToolIgnoreExistingFeeds', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Configures the tool to use only the specified package sources, ignoring existing NuGet configuration.
     * @returns The resource builder.
     */
    withToolIgnoreExistingFeeds() {
        return new DotnetToolResourcePromiseImpl(this._withToolIgnoreExistingFeedsInternal(), this._client);
    }
    /** @internal */
    async _withToolIgnoreFailedSourcesInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withToolIgnoreFailedSources', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Configures the resource to treat package source failures as warnings.
     * @returns The resource builder.
     */
    withToolIgnoreFailedSources() {
        return new DotnetToolResourcePromiseImpl(this._withToolIgnoreFailedSourcesInternal(), this._client);
    }
    /** @internal */
    async _publishAsDockerFileInternal(configure) {
        const configureId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new ContainerResourceImpl(objHandle, this._client);
            await configure(obj);
        });
        const rpcArgs = { builder: this._handle, configure: configureId };
        const result = await this._client.invokeCapability('Aspire.Hosting/publishAsDockerFile', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Publishes an executable as a Docker file
     *
     * When the executable resource is converted to a container resource, the arguments to the executable
     * are not used. This is because arguments to the executable often contain physical paths that are not valid
     * in the container. The container can be set up with the correct arguments using the `configure` action.
     * @param configure Optional action to configure the container resource
     * @returns The resource builder.
     */
    publishAsDockerFile(configure) {
        return new DotnetToolResourcePromiseImpl(this._publishAsDockerFileInternal(configure), this._client);
    }
    /** @internal */
    async _withExecutableCommandInternal(command) {
        const rpcArgs = { builder: this._handle, command };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExecutableCommand', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Sets the command for the executable resource.
     * @param command Command.
     * @returns The resource builder.
     */
    withExecutableCommand(command) {
        return new DotnetToolResourcePromiseImpl(this._withExecutableCommandInternal(command), this._client);
    }
    /** @internal */
    async _withWorkingDirectoryInternal(workingDirectory) {
        const rpcArgs = { builder: this._handle, workingDirectory };
        const result = await this._client.invokeCapability('Aspire.Hosting/withWorkingDirectory', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Sets the working directory for the executable resource.
     * @param workingDirectory Working directory.
     * @returns The resource builder.
     */
    withWorkingDirectory(workingDirectory) {
        return new DotnetToolResourcePromiseImpl(this._withWorkingDirectoryInternal(workingDirectory), this._client);
    }
    /** @internal */
    async _withMcpServerInternal(path, endpointName) {
        const rpcArgs = { builder: this._handle };
        if (path !== undefined)
            rpcArgs.path = path;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withMcpServer', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Marks the resource as hosting a Model Context Protocol (MCP) server on the specified endpoint.
     *
     * This method adds an `McpServerEndpointAnnotation` to the resource, enabling the Aspire tooling
     * to discover and proxy the MCP server exposed by the resource.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withMcpServer(options) {
        const path = options?.path;
        const endpointName = options?.endpointName;
        return new DotnetToolResourcePromiseImpl(this._withMcpServerInternal(path, endpointName), this._client);
    }
    /** @internal */
    async _withOtlpExporterInternal(protocol) {
        const rpcArgs = { builder: this._handle };
        if (protocol !== undefined)
            rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability('Aspire.Hosting/withOtlpExporter', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Configures OTLP telemetry export
     * @param options Additional options.
     */
    withOtlpExporter(options) {
        const protocol = options?.protocol;
        return new DotnetToolResourcePromiseImpl(this._withOtlpExporterInternal(protocol), this._client);
    }
    /** @internal */
    async _withRequiredCommandInternal(command, helpLink) {
        const rpcArgs = { builder: this._handle, command };
        if (helpLink !== undefined)
            rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability('Aspire.Hosting/withRequiredCommand', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Declares that a resource requires a specific command/executable to be available on the local machine PATH before it can start.
     *
     * The command is considered valid if either:
     * 1. It is an absolute or relative path (contains a directory separator) that points to an existing file, or
     * 2. It is discoverable on the current process PATH (respecting PATHEXT on Windows).
     * If the command is not found, a warning message will be logged but the resource will be allowed to attempt to start.
     * @param command The command string (file name or path) that should be validated.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withRequiredCommand(command, options) {
        const helpLink = options?.helpLink;
        return new DotnetToolResourcePromiseImpl(this._withRequiredCommandInternal(command, helpLink), this._client);
    }
    /** @internal */
    async _withSessionLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withSessionLifetime', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a session lifetime.
     * @returns The resource builder.
     */
    withSessionLifetime() {
        return new DotnetToolResourcePromiseImpl(this._withSessionLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withPersistentLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPersistentLifetime', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime.
     * @returns The resource builder.
     */
    withPersistentLifetime() {
        return new DotnetToolResourcePromiseImpl(this._withPersistentLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withLifetimeOfInternal(sourceBuilder) {
        sourceBuilder = isPromiseLike(sourceBuilder) ? await sourceBuilder : sourceBuilder;
        const rpcArgs = { builder: this._handle, sourceBuilder };
        const result = await this._client.invokeCapability('Aspire.Hosting/withLifetimeOf', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to match the lifetime of another resource.
     *
     * The resource lifetime is evaluated from `sourceBuilder` when the application model is prepared, so later lifetime
     * changes to the source resource are reflected by this resource.
     * @param sourceBuilder The resource builder whose lifetime should be used.
     * @returns The resource builder.
     */
    withLifetimeOf(sourceBuilder) {
        return new DotnetToolResourcePromiseImpl(this._withLifetimeOfInternal(sourceBuilder), this._client);
    }
    /** @internal */
    async _withParentProcessLifetimeInternal(parentProcessId) {
        const rpcArgs = { builder: this._handle, parentProcessId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withParentProcessLifetime', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime that ends when a parent process exits.
     * @param parentProcessId The ID of the parent process to monitor.
     * @returns The resource builder.
     */
    withParentProcessLifetime(parentProcessId) {
        return new DotnetToolResourcePromiseImpl(this._withParentProcessLifetimeInternal(parentProcessId), this._client);
    }
    /** @internal */
    async _withEnvironmentInternal(name, value) {
        value = isPromiseLike(value) ? await value : value;
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEnvironment', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /** Sets an environment variable */
    withEnvironment(name, value) {
        return new DotnetToolResourcePromiseImpl(this._withEnvironmentInternal(name, value), this._client);
    }
    /** @internal */
    async _withEnvironmentCallbackInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new EnvironmentCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEnvironmentCallback', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Allows for the population of environment variables on a resource.
     * @param callback A callback that allows for deferred execution for computing many environment variables. This runs after resources have been allocated by the orchestrator and allows access to other resources to resolve computed data, e.g. connection strings, ports.
     * @returns The resource builder.
     */
    withEnvironmentCallback(callback) {
        return new DotnetToolResourcePromiseImpl(this._withEnvironmentCallbackInternal(callback), this._client);
    }
    /** @internal */
    async _withArgsInternal(args) {
        const rpcArgs = { builder: this._handle, args };
        const result = await this._client.invokeCapability('Aspire.Hosting/withArgs', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Adds arguments to be passed to a resource that supports arguments when it is launched.
     * @param args The arguments to be passed to the resource when it is started.
     * @returns The resource builder.
     */
    withArgs(args) {
        return new DotnetToolResourcePromiseImpl(this._withArgsInternal(args), this._client);
    }
    /** @internal */
    async _withArgsCallbackInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new CommandLineArgsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withArgsCallback', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Adds a callback to be executed with a list of command-line arguments when a resource is started.
     * @param callback A callback that allows for deferred execution for computing arguments. This runs after resources have been allocated by the orchestrator and allows access to other resources to resolve computed data, e.g. connection strings, ports.
     * @returns The resource builder.
     */
    withArgsCallback(callback) {
        return new DotnetToolResourcePromiseImpl(this._withArgsCallbackInternal(callback), this._client);
    }
    /** @internal */
    async _withReferenceEnvironmentInternal(options) {
        const rpcArgs = { builder: this._handle, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withReferenceEnvironment', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Configures how information is injected into environment variables when the resource references other resources.
     * @param options Options controlling which reference information is emitted.
     * @returns The resource builder.
     */
    withReferenceEnvironment(options) {
        return new DotnetToolResourcePromiseImpl(this._withReferenceEnvironmentInternal(options), this._client);
    }
    /** @internal */
    async _withReferenceInternal(source, connectionName, optional, name) {
        source = isPromiseLike(source) ? await source : source;
        const rpcArgs = { builder: this._handle, source };
        if (connectionName !== undefined)
            rpcArgs.connectionName = connectionName;
        if (optional !== undefined)
            rpcArgs.optional = optional;
        if (name !== undefined)
            rpcArgs.name = name;
        const result = await this._client.invokeCapability('Aspire.Hosting/withReference', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Adds a reference to another resource
     * @param options Additional options.
     */
    withReference(source, options) {
        const connectionName = options?.connectionName;
        const optional = options?.optional;
        const name = options?.name;
        return new DotnetToolResourcePromiseImpl(this._withReferenceInternal(source, connectionName, optional, name), this._client);
    }
    /** @internal */
    async _withEndpointCallbackInternal(endpointName, callback, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpointCallback', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Updates a named endpoint via callback
     * @param options Additional options.
     */
    withEndpointCallback(endpointName, callback, options) {
        const createIfNotExists = options?.createIfNotExists;
        return new DotnetToolResourcePromiseImpl(this._withEndpointCallbackInternal(endpointName, callback, createIfNotExists), this._client);
    }
    /** @internal */
    async _withHttpEndpointCallbackInternal(callback, name, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        if (name !== undefined)
            rpcArgs.name = name;
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpEndpointCallback', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Updates an HTTP endpoint via callback
     * @param options Additional options.
     */
    withHttpEndpointCallback(callback, options) {
        const name = options?.name;
        const createIfNotExists = options?.createIfNotExists;
        return new DotnetToolResourcePromiseImpl(this._withHttpEndpointCallbackInternal(callback, name, createIfNotExists), this._client);
    }
    /** @internal */
    async _withHttpsEndpointCallbackInternal(callback, name, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        if (name !== undefined)
            rpcArgs.name = name;
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpsEndpointCallback', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Updates an HTTPS endpoint via callback
     * @param options Additional options.
     */
    withHttpsEndpointCallback(callback, options) {
        const name = options?.name;
        const createIfNotExists = options?.createIfNotExists;
        return new DotnetToolResourcePromiseImpl(this._withHttpsEndpointCallbackInternal(callback, name, createIfNotExists), this._client);
    }
    /** @internal */
    async _withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (scheme !== undefined)
            rpcArgs.scheme = scheme;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        if (isExternal !== undefined)
            rpcArgs.isExternal = isExternal;
        if (protocol !== undefined)
            rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpoint', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Adds a network endpoint
     * @param options Additional options.
     * @returns The resource builder.
     */
    withEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const scheme = options?.scheme;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        const isExternal = options?.isExternal;
        const protocol = options?.protocol;
        return new DotnetToolResourcePromiseImpl(this._withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol), this._client);
    }
    /** @internal */
    async _withEndpointProxySupportInternal(proxyEnabled) {
        const rpcArgs = { builder: this._handle, proxyEnabled };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpointProxySupport', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Set whether a resource can use proxied endpoints or whether they should be disabled for all endpoints belonging to the resource. If set to `false`, endpoints belonging to the resource will ignore the configured proxy settings and run proxy-less.
     *
     * This method is intended to support scenarios with persistent lifetime resources where it is desirable for the resource to be accessible over the same
     * port whether the Aspire application is running or not. Proxied endpoints bind ports that are only accessible while the Aspire application is running.
     * The user needs to be careful to ensure that endpoints are using unique ports when disabling proxy support as by default for proxy-less
     * endpoints, Aspire will allocate the target port as the host port, which will increase the chance of port conflicts.
     * @param proxyEnabled Should endpoints for the resource support using a proxy?
     * @returns The resource builder.
     */
    withEndpointProxySupport(proxyEnabled) {
        return new DotnetToolResourcePromiseImpl(this._withEndpointProxySupportInternal(proxyEnabled), this._client);
    }
    /** @internal */
    async _withHttpEndpointInternal(port, targetPort, name, env, isProxied) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpEndpoint', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP endpoint
     *
     * If an endpoint with the same name already exists on the resource, the existing endpoint is updated
     * with any non-null parameter values. Parameters left as `null` will not modify the existing endpoint's values.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new DotnetToolResourcePromiseImpl(this._withHttpEndpointInternal(port, targetPort, name, env, isProxied), this._client);
    }
    /** @internal */
    async _withHttpsEndpointInternal(port, targetPort, name, env, isProxied) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpsEndpoint', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTPS endpoint
     *
     * If an endpoint with the same name already exists on the resource, the existing endpoint is updated
     * with any non-null parameter values. Parameters left as `null` will not modify the existing endpoint's values.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpsEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new DotnetToolResourcePromiseImpl(this._withHttpsEndpointInternal(port, targetPort, name, env, isProxied), this._client);
    }
    /** @internal */
    async _withExternalHttpEndpointsInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExternalHttpEndpoints', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Marks existing http or https endpoints on a resource as external.
     * @returns The resource builder.
     */
    withExternalHttpEndpoints() {
        return new DotnetToolResourcePromiseImpl(this._withExternalHttpEndpointsInternal(), this._client);
    }
    /**
     * Gets an endpoint reference
     * @param name The name of the endpoint.
     * @returns An `EndpointReference` that can be used to resolve the address of the endpoint after resource allocation has occurred.
     */
    getEndpoint(name) {
        const promise = (async () => {
            const rpcArgs = { builder: this._handle, name };
            const handle = await this._client.invokeCapability('Aspire.Hosting/getEndpoint', rpcArgs);
            return new EndpointReferenceImpl(handle, this._client);
        })();
        return new EndpointReferencePromiseImpl(promise, this._client);
    }
    /** @internal */
    async _asHttp2ServiceInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/asHttp2Service', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to mark all endpoints' transport as HTTP/2. This is useful for HTTP/2 services that need prior knowledge.
     * @returns The resource builder.
     */
    asHttp2Service() {
        return new DotnetToolResourcePromiseImpl(this._asHttp2ServiceInternal(), this._client);
    }
    /** @internal */
    async _withUrlsInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new ResourceUrlsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrls', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to customize the URLs displayed for the resource.
     * @param callback The callback that will customize URLs for the resource.
     * @returns The resource builder.
     */
    withUrls(callback) {
        return new DotnetToolResourcePromiseImpl(this._withUrlsInternal(callback), this._client);
    }
    /** @internal */
    async _withUrlInternal(url, displayText) {
        const rpcArgs = { builder: this._handle, url };
        if (displayText !== undefined)
            rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrl', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Adds or modifies displayed URLs
     * @param options Additional options.
     */
    withUrl(url, options) {
        const displayText = options?.displayText;
        return new DotnetToolResourcePromiseImpl(this._withUrlInternal(url, displayText), this._client);
    }
    /** @internal */
    async _withUrlForEndpointInternal(endpointName, callback) {
        const callbackId = registerCallback(async (objData) => {
            const obj = wrapIfHandle(objData);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrlForEndpoint', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to update the URL displayed for the endpoint with the specified name.
     * @param endpointName The name of the endpoint to customize the URL for.
     * @param callback The callback that will customize the URL.
     * @returns The resource builder.
     */
    withUrlForEndpoint(endpointName, callback) {
        return new DotnetToolResourcePromiseImpl(this._withUrlForEndpointInternal(endpointName, callback), this._client);
    }
    /** @internal */
    async _excludeFromManifestInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromManifest', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Excludes a resource from being published to the manifest.
     * @returns The resource builder.
     */
    excludeFromManifest() {
        return new DotnetToolResourcePromiseImpl(this._excludeFromManifestInternal(), this._client);
    }
    /** @internal */
    async _waitForInternal(dependency, waitBehavior) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (waitBehavior !== undefined)
            rpcArgs.waitBehavior = waitBehavior;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitFor', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Waits for another resource to be ready
     * @param options Additional options.
     */
    waitFor(dependency, options) {
        const waitBehavior = options?.waitBehavior;
        return new DotnetToolResourcePromiseImpl(this._waitForInternal(dependency, waitBehavior), this._client);
    }
    /** @internal */
    async _waitForStartInternal(dependency, waitBehavior) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (waitBehavior !== undefined)
            rpcArgs.waitBehavior = waitBehavior;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitForStart', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Waits for another resource to start
     * @param options Additional options.
     */
    waitForStart(dependency, options) {
        const waitBehavior = options?.waitBehavior;
        return new DotnetToolResourcePromiseImpl(this._waitForStartInternal(dependency, waitBehavior), this._client);
    }
    /** @internal */
    async _withExplicitStartInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExplicitStart', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Prevents resource from starting automatically
     * @returns The resource builder.
     */
    withExplicitStart() {
        return new DotnetToolResourcePromiseImpl(this._withExplicitStartInternal(), this._client);
    }
    /** @internal */
    async _waitForCompletionInternal(dependency, exitCode) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitForResourceCompletion', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Waits for the dependency resource to enter the Exited or Finished state before starting the resource.
     * @param dependency The resource builder for the dependency resource.
     * @param options Additional options.
     * @returns The resource builder.
     */
    waitForCompletion(dependency, options) {
        const exitCode = options?.exitCode;
        return new DotnetToolResourcePromiseImpl(this._waitForCompletionInternal(dependency, exitCode), this._client);
    }
    /** @internal */
    async _withHealthCheckInternal(key) {
        const rpcArgs = { builder: this._handle, key };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHealthCheck', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Adds a health check by key
     * @param key The key for the health check.
     * @returns The resource builder.
     */
    withHealthCheck(key) {
        return new DotnetToolResourcePromiseImpl(this._withHealthCheckInternal(key), this._client);
    }
    /** @internal */
    async _withHttpHealthCheckInternal(path, statusCode, endpointName) {
        const rpcArgs = { builder: this._handle };
        if (path !== undefined)
            rpcArgs.path = path;
        if (statusCode !== undefined)
            rpcArgs.statusCode = statusCode;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpHealthCheck', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Adds a health check to the resource which is mapped to a specific endpoint.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpHealthCheck(options) {
        const path = options?.path;
        const statusCode = options?.statusCode;
        const endpointName = options?.endpointName;
        return new DotnetToolResourcePromiseImpl(this._withHttpHealthCheckInternal(path, statusCode, endpointName), this._client);
    }
    /** @internal */
    async _withCommandInternal(name, displayName, executeCommand, commandOptions) {
        const executeCommandId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined)
            rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability('Aspire.Hosting/withCommand', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Adds a resource command
     *
     * The `WithCommand` method is used to add commands to the resource. Commands are displayed in the dashboard
     * and can be executed by a user using the dashboard UI.
     * When a command is executed, the `executeCommand` callback is called and is run inside the Aspire host.
     * @param name The name of the command. The name uniquely identifies the command.
     * @param displayName The display name visible in UI.
     * @param executeCommand A callback that is executed when the command is executed. The callback is run inside the Aspire host. The callback result is used to indicate success or failure in the UI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withCommand(name, displayName, executeCommand, options) {
        const commandOptions = options?.commandOptions;
        return new DotnetToolResourcePromiseImpl(this._withCommandInternal(name, displayName, executeCommand, commandOptions), this._client);
    }
    /** @internal */
    async _withProcessCommandInternal(commandName, displayName, options) {
        const rpcArgs = { builder: this._handle, commandName, displayName, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommand', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /** Adds a command to the resource that starts a local process when invoked. */
    withProcessCommand(commandName, displayName, options) {
        return new DotnetToolResourcePromiseImpl(this._withProcessCommandInternal(commandName, displayName, options), this._client);
    }
    /** @internal */
    async _withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options) {
        const createProcessSpecId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await createProcessSpec(arg);
        });
        const rpcArgs = { builder: this._handle, commandName, displayName, createProcessSpec: createProcessSpecId };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommandFactory', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Adds a command to the resource that starts a local process created by a callback when invoked.
     * @param options Additional options.
     */
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new DotnetToolResourcePromiseImpl(this._withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options), this._client);
    }
    /** @internal */
    async _withHttpCommandInternal(path, displayName, options) {
        const rpcArgs = { builder: this._handle, path, displayName };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpCommand', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP resource command
     * @param options Additional options.
     */
    withHttpCommand(path, displayName, options) {
        return new DotnetToolResourcePromiseImpl(this._withHttpCommandInternal(path, displayName, options), this._client);
    }
    /** @internal */
    async _withDeveloperCertificateTrustInternal(trust) {
        const rpcArgs = { builder: this._handle, trust };
        const result = await this._client.invokeCapability('Aspire.Hosting/withDeveloperCertificateTrust', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Indicates whether developer certificates should be treated as trusted certificate authorities for the resource at run time. Currently this indicates trust for the ASP.NET Core developer certificate. The developer certificate will only be trusted when running in local development scenarios; in publish mode resources will use their default certificate trust.
     *
     * Disable trust for app host managed developer certificate(s) for a container resource.
     * ```
     * var container = builder.AddContainer("my-service", "my-service:latest")
     * .WithDeveloperCertificateTrust(false);
     * ```
     * Disable automatic trust for app host managed developer certificate(s), but explicitly enable it for a specific resource.
     * ```
     * var builder = DistributedApplication.CreateBuilder(new DistributedApplicationOptions()
     * {
     * Args = args,
     * TrustDeveloperCertificate = false,
     * });
     * var project = builder.AddProject<MyService>("my-service")
     * .WithDeveloperCertificateTrust(true);
     * ```
     * @param trust Indicates whether the developer certificate should be treated as trusted.
     * @returns The resource builder.
     */
    withDeveloperCertificateTrust(trust) {
        return new DotnetToolResourcePromiseImpl(this._withDeveloperCertificateTrustInternal(trust), this._client);
    }
    /** @internal */
    async _withCertificateTrustScopeInternal(scope) {
        const rpcArgs = { builder: this._handle, scope };
        const result = await this._client.invokeCapability('Aspire.Hosting/withCertificateTrustScope', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Sets the certificate trust scope
     *
     * The default scope if not overridden is `Append` which means that custom certificate
     * authorities should be appended to the default trusted certificate authorities for the resource. Setting the scope to
     * `Override` indicates the set of certificates in referenced
     * `CertificateAuthorityCollection` (and optionally Aspire developer certificiates) should be used as the
     * exclusive source of trust for a resource.
     * In all cases, this is a best effort implementation as not all resources support full customization of certificate
     * trust.
     * Set the scope for custom certificate authorities to override the default trusted certificate authorities for a container resource.
     * ```
     * var caCollection = builder.AddCertificateAuthorityCollection("my-cas")
     * .WithCertificate(new X509Certificate2("my-ca.pem"));
     * var container = builder.AddContainer("my-service", "my-service:latest")
     * .WithCertificateAuthorityCollection(caCollection)
     * .WithCertificateTrustScope(CertificateTrustScope.Override);
     * ```
     * @param scope The scope to apply to custom certificate authorities associated with the resource.
     * @returns The resource builder.
     */
    withCertificateTrustScope(scope) {
        return new DotnetToolResourcePromiseImpl(this._withCertificateTrustScopeInternal(scope), this._client);
    }
    /** @internal */
    async _withHttpsDeveloperCertificateInternal(password) {
        password = isPromiseLike(password) ? await password : password;
        const rpcArgs = { builder: this._handle };
        if (password !== undefined)
            rpcArgs.password = password;
        const result = await this._client.invokeCapability('Aspire.Hosting/withParameterHttpsDeveloperCertificate', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Indicates that a resource should use the developer certificate key pair for HTTPS endpoints at run time. Currently this indicates use of the ASP.NET Core developer certificate. The developer certificate will only be used when running in local development scenarios; in publish mode resources will use their default certificate configuration.
     *
     * Use the developer certificate for HTTPS/TLS endpoints on a container resource:
     * ```
     * builder.AddContainer("my-service", "my-image")
     * .WithHttpsDeveloperCertificate()
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpsDeveloperCertificate(options) {
        let password = options?.password;
        return new DotnetToolResourcePromiseImpl(this._withHttpsDeveloperCertificateInternal(password), this._client);
    }
    /** @internal */
    async _withoutHttpsCertificateInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withoutHttpsCertificate', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Disable HTTPS/TLS server certificate configuration for the resource. No HTTPS/TLS termination configuration will be applied.
     *
     * Disable HTTPS certificate configuration for a Redis resource:
     * ```
     * var redis = builder.AddRedis("cache")
     * .WithoutHttpsCertificate();
     * ```
     * @returns The resource builder.
     */
    withoutHttpsCertificate() {
        return new DotnetToolResourcePromiseImpl(this._withoutHttpsCertificateInternal(), this._client);
    }
    /** @internal */
    async _withRelationshipInternal(resourceBuilder, type) {
        resourceBuilder = isPromiseLike(resourceBuilder) ? await resourceBuilder : resourceBuilder;
        const rpcArgs = { builder: this._handle, resourceBuilder, type };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderRelationship', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Adds a relationship to another resource using its builder.
     * @param resourceBuilder The resource builder that the relationship is to.
     * @param type The relationship type.
     * @returns A resource builder.
     */
    withRelationship(resourceBuilder, type) {
        return new DotnetToolResourcePromiseImpl(this._withRelationshipInternal(resourceBuilder, type), this._client);
    }
    /** @internal */
    async _withParentRelationshipInternal(parent) {
        parent = isPromiseLike(parent) ? await parent : parent;
        const rpcArgs = { builder: this._handle, parent };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderParentRelationship', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Sets the parent relationship
     * @param parent The parent of `builder`.
     * @returns A resource builder.
     */
    withParentRelationship(parent) {
        return new DotnetToolResourcePromiseImpl(this._withParentRelationshipInternal(parent), this._client);
    }
    /** @internal */
    async _withChildRelationshipInternal(child) {
        child = isPromiseLike(child) ? await child : child;
        const rpcArgs = { builder: this._handle, child };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderChildRelationship', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Sets a child relationship
     * @param child The child of `builder`.
     * @returns A resource builder.
     */
    withChildRelationship(child) {
        return new DotnetToolResourcePromiseImpl(this._withChildRelationshipInternal(child), this._client);
    }
    /** @internal */
    async _withIconNameInternal(iconName, iconVariant) {
        const rpcArgs = { builder: this._handle, iconName };
        if (iconVariant !== undefined)
            rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability('Aspire.Hosting/withIconName', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Specifies the icon to use when displaying the resource in the dashboard.
     * @param iconName The name of the FluentUI icon to use. See https://aka.ms/fluentui-system-icons for available icons.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withIconName(iconName, options) {
        const iconVariant = options?.iconVariant;
        return new DotnetToolResourcePromiseImpl(this._withIconNameInternal(iconName, iconVariant), this._client);
    }
    /** @internal */
    async _withComputeEnvironmentInternal(computeEnvironmentResource) {
        computeEnvironmentResource = isPromiseLike(computeEnvironmentResource) ? await computeEnvironmentResource : computeEnvironmentResource;
        const rpcArgs = { builder: this._handle, computeEnvironmentResource };
        const result = await this._client.invokeCapability('Aspire.Hosting/withComputeEnvironment', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Configures the compute environment for the compute resource.
     *
     * This method allows associating a specific compute environment with the compute resource.
     * @param computeEnvironmentResource The compute environment resource to associate with the compute resource.
     * @returns The resource builder.
     */
    withComputeEnvironment(computeEnvironmentResource) {
        return new DotnetToolResourcePromiseImpl(this._withComputeEnvironmentInternal(computeEnvironmentResource), this._client);
    }
    /** @internal */
    async _withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName) {
        const rpcArgs = { builder: this._handle, probeType };
        if (path !== undefined)
            rpcArgs.path = path;
        if (initialDelaySeconds !== undefined)
            rpcArgs.initialDelaySeconds = initialDelaySeconds;
        if (periodSeconds !== undefined)
            rpcArgs.periodSeconds = periodSeconds;
        if (timeoutSeconds !== undefined)
            rpcArgs.timeoutSeconds = timeoutSeconds;
        if (failureThreshold !== undefined)
            rpcArgs.failureThreshold = failureThreshold;
        if (successThreshold !== undefined)
            rpcArgs.successThreshold = successThreshold;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpProbe', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP health probe to the resource
     * @param options Additional options.
     */
    withHttpProbe(probeType, options) {
        const path = options?.path;
        const initialDelaySeconds = options?.initialDelaySeconds;
        const periodSeconds = options?.periodSeconds;
        const timeoutSeconds = options?.timeoutSeconds;
        const failureThreshold = options?.failureThreshold;
        const successThreshold = options?.successThreshold;
        const endpointName = options?.endpointName;
        return new DotnetToolResourcePromiseImpl(this._withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName), this._client);
    }
    /** @internal */
    async _excludeFromMcpInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromMcp', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Exclude the resource from MCP operations using the Aspire MCP server. The resource is excluded from results that return resources, console logs and telemetry.
     * @returns The resource builder.
     */
    excludeFromMcp() {
        return new DotnetToolResourcePromiseImpl(this._excludeFromMcpInternal(), this._client);
    }
    /** @internal */
    async _withHiddenInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHidden', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists
     *
     * Use this method to hide resources that are implementation details and should never be displayed by default.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @returns The resource builder.
     */
    withHidden() {
        return new DotnetToolResourcePromiseImpl(this._withHiddenInternal(), this._client);
    }
    /** @internal */
    async _withHiddenOnCompletionInternal(exitCode, exitCodes) {
        const rpcArgs = { builder: this._handle };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        if (exitCodes !== undefined)
            rpcArgs.exitCodes = exitCodes;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHiddenOnCompletion', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists after successful completion
     *
     * This method is useful for one-off resources such as setup scripts, migrations, or build steps that should remain visible while running
     * and then be hidden after successful completion.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHiddenOnCompletion(options) {
        const exitCode = options?.exitCode;
        const exitCodes = options?.exitCodes;
        return new DotnetToolResourcePromiseImpl(this._withHiddenOnCompletionInternal(exitCode, exitCodes), this._client);
    }
    /** @internal */
    async _withImagePushOptionsInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ContainerImagePushOptionsCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImagePushOptions', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Adds an asynchronous callback to configure container image push options for the resource.
     *
     * This method allows customization of how container images are named and tagged when pushed to a registry using an asynchronous callback.
     * Use this overload when the callback needs to perform asynchronous operations such as retrieving configuration values from external sources.
     * The callback receives a `ContainerImagePushOptionsCallbackContext` that provides access to the resource
     * and the `ContainerImagePushOptions` that can be modified.
     * Multiple callbacks can be registered on the same resource, and they will be invoked in the order they were added.
     * @param callback The asynchronous callback to configure push options.
     * @returns The resource builder.
     */
    withImagePushOptions(callback) {
        return new DotnetToolResourcePromiseImpl(this._withImagePushOptionsInternal(callback), this._client);
    }
    /** @internal */
    async _withRemoteImageNameInternal(remoteImageName) {
        const rpcArgs = { builder: this._handle, remoteImageName };
        const result = await this._client.invokeCapability('Aspire.Hosting/withRemoteImageName', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Sets the remote image name (without registry endpoint or tag) for container push operations.
     *
     * Use this with `withRemoteImageTag` to fully customize the image reference used for container push operations.
     * @param remoteImageName The remote image name (e.g., "myapp" or "myorg/myapp").
     * @returns The resource builder.
     */
    withRemoteImageName(remoteImageName) {
        return new DotnetToolResourcePromiseImpl(this._withRemoteImageNameInternal(remoteImageName), this._client);
    }
    /** @internal */
    async _withRemoteImageTagInternal(remoteImageTag) {
        const rpcArgs = { builder: this._handle, remoteImageTag };
        const result = await this._client.invokeCapability('Aspire.Hosting/withRemoteImageTag', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Sets the remote image tag for container push operations.
     *
     * Use this with `withRemoteImageName` to fully customize the image reference used for container push operations.
     * @param remoteImageTag The remote image tag (e.g., "latest", "v1.0.0").
     * @returns The resource builder.
     */
    withRemoteImageTag(remoteImageTag) {
        return new DotnetToolResourcePromiseImpl(this._withRemoteImageTagInternal(remoteImageTag), this._client);
    }
    /** @internal */
    async _withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new PipelineStepContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined)
            rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined)
            rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined)
            rpcArgs.tags = tags;
        if (description !== undefined)
            rpcArgs.description = description;
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineStepFactory', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Adds a pipeline step to the resource that will be executed during deployment.
     * @param stepName The unique name of the pipeline step.
     * @param callback The callback to execute when the step runs.
     * @param options Additional options.
     * @returns The resource builder for chaining.
     */
    withPipelineStepFactory(stepName, callback, options) {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new DotnetToolResourcePromiseImpl(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description), this._client);
    }
    /** @internal */
    async _withPipelineConfigurationInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new PipelineConfigurationContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineConfiguration', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to be executed during the pipeline configuration phase, allowing modification of step dependencies and relationships.
     * @param callback The callback function to execute during the configuration phase.
     * @returns The resource builder for chaining.
     */
    withPipelineConfiguration(callback) {
        return new DotnetToolResourcePromiseImpl(this._withPipelineConfigurationInternal(callback), this._client);
    }
    /**
     * Gets the name of the resource from a builder.
     *
     * Why this wrapper exists: This capability accesses a nested property
     * (`resource.Resource.Name`) which requires a wrapper method. There is no single
     * .NET method that returns just the resource name that could be annotated directly.
     * @returns The resource name.
     */
    async getResourceName() {
        const rpcArgs = { resource: this._handle };
        return await this._client.invokeCapability('Aspire.Hosting/getResourceName', rpcArgs);
    }
    /** @internal */
    async _onBeforeResourceStartedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new BeforeResourceStartedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onBeforeResourceStarted', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the BeforeResourceStarted event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onBeforeResourceStarted(callback) {
        return new DotnetToolResourcePromiseImpl(this._onBeforeResourceStartedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceStoppedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceStoppedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceStopped', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceStopped event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceStopped(callback) {
        return new DotnetToolResourcePromiseImpl(this._onResourceStoppedInternal(callback), this._client);
    }
    /** @internal */
    async _onInitializeResourceInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new InitializeResourceEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onInitializeResource', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the InitializeResource event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onInitializeResource(callback) {
        return new DotnetToolResourcePromiseImpl(this._onInitializeResourceInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceEndpointsAllocatedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceEndpointsAllocatedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceEndpointsAllocated', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceEndpointsAllocated event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceEndpointsAllocated(callback) {
        return new DotnetToolResourcePromiseImpl(this._onResourceEndpointsAllocatedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceReadyInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceReadyEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceReady', rpcArgs);
        return new DotnetToolResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceReady event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceReady(callback) {
        return new DotnetToolResourcePromiseImpl(this._onResourceReadyInternal(callback), this._client);
    }
    /**
     * Creates an execution configuration builder for the specified resource.
     * @returns The execution configuration builder.
     */
    createExecutionConfiguration() {
        const promise = (async () => {
            const rpcArgs = { resource: this._handle };
            const handle = await this._client.invokeCapability('Aspire.Hosting/createExecutionConfiguration', rpcArgs);
            return new ExecutionConfigurationBuilderImpl(handle, this._client);
        })();
        return new ExecutionConfigurationBuilderPromiseImpl(promise, this._client);
    }
}
/**
 * Thenable wrapper for DotnetToolResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
class DotnetToolResourcePromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    withContainerRegistry(registry) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withContainerRegistry(registry)), this._client);
    }
    withDockerfileBaseImage(options) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileBaseImage(options)), this._client);
    }
    withToolPackage(packageId) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withToolPackage(packageId)), this._client);
    }
    withToolVersion(version) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withToolVersion(version)), this._client);
    }
    withToolPrerelease() {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withToolPrerelease()), this._client);
    }
    withToolSource(source) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withToolSource(source)), this._client);
    }
    withToolIgnoreExistingFeeds() {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withToolIgnoreExistingFeeds()), this._client);
    }
    withToolIgnoreFailedSources() {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withToolIgnoreFailedSources()), this._client);
    }
    publishAsDockerFile(configure) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.publishAsDockerFile(configure)), this._client);
    }
    withExecutableCommand(command) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withExecutableCommand(command)), this._client);
    }
    withWorkingDirectory(workingDirectory) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withWorkingDirectory(workingDirectory)), this._client);
    }
    withMcpServer(options) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withMcpServer(options)), this._client);
    }
    withOtlpExporter(options) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withOtlpExporter(options)), this._client);
    }
    withRequiredCommand(command, options) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withRequiredCommand(command, options)), this._client);
    }
    withSessionLifetime() {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withSessionLifetime()), this._client);
    }
    withPersistentLifetime() {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withPersistentLifetime()), this._client);
    }
    withLifetimeOf(sourceBuilder) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withLifetimeOf(sourceBuilder)), this._client);
    }
    withParentProcessLifetime(parentProcessId) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withParentProcessLifetime(parentProcessId)), this._client);
    }
    withEnvironment(name, value) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withEnvironment(name, value)), this._client);
    }
    withEnvironmentCallback(callback) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withEnvironmentCallback(callback)), this._client);
    }
    withArgs(args) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withArgs(args)), this._client);
    }
    withArgsCallback(callback) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withArgsCallback(callback)), this._client);
    }
    withReferenceEnvironment(options) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withReferenceEnvironment(options)), this._client);
    }
    withReference(source, options) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withReference(source, options)), this._client);
    }
    withEndpointCallback(endpointName, callback, options) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withEndpointCallback(endpointName, callback, options)), this._client);
    }
    withHttpEndpointCallback(callback, options) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withHttpEndpointCallback(callback, options)), this._client);
    }
    withHttpsEndpointCallback(callback, options) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withHttpsEndpointCallback(callback, options)), this._client);
    }
    withEndpoint(options) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withEndpoint(options)), this._client);
    }
    withEndpointProxySupport(proxyEnabled) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withEndpointProxySupport(proxyEnabled)), this._client);
    }
    withHttpEndpoint(options) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withHttpEndpoint(options)), this._client);
    }
    withHttpsEndpoint(options) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withHttpsEndpoint(options)), this._client);
    }
    withExternalHttpEndpoints() {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withExternalHttpEndpoints()), this._client);
    }
    getEndpoint(name) {
        return new EndpointReferencePromiseImpl(this._promise.then(obj => obj.getEndpoint(name)), this._client);
    }
    asHttp2Service() {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.asHttp2Service()), this._client);
    }
    withUrls(callback) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withUrls(callback)), this._client);
    }
    withUrl(url, options) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withUrl(url, options)), this._client);
    }
    withUrlForEndpoint(endpointName, callback) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)), this._client);
    }
    excludeFromManifest() {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.excludeFromManifest()), this._client);
    }
    waitFor(dependency, options) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.waitFor(dependency, options)), this._client);
    }
    waitForStart(dependency, options) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.waitForStart(dependency, options)), this._client);
    }
    withExplicitStart() {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withExplicitStart()), this._client);
    }
    waitForCompletion(dependency, options) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.waitForCompletion(dependency, options)), this._client);
    }
    withHealthCheck(key) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withHealthCheck(key)), this._client);
    }
    withHttpHealthCheck(options) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withHttpHealthCheck(options)), this._client);
    }
    withCommand(name, displayName, executeCommand, options) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)), this._client);
    }
    withProcessCommand(commandName, displayName, options) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommand(commandName, displayName, options)), this._client);
    }
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommandFactory(commandName, displayName, createProcessSpec, options)), this._client);
    }
    withHttpCommand(path, displayName, options) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withHttpCommand(path, displayName, options)), this._client);
    }
    withDeveloperCertificateTrust(trust) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withDeveloperCertificateTrust(trust)), this._client);
    }
    withCertificateTrustScope(scope) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withCertificateTrustScope(scope)), this._client);
    }
    withHttpsDeveloperCertificate(options) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withHttpsDeveloperCertificate(options)), this._client);
    }
    withoutHttpsCertificate() {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withoutHttpsCertificate()), this._client);
    }
    withRelationship(resourceBuilder, type) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withRelationship(resourceBuilder, type)), this._client);
    }
    withParentRelationship(parent) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withParentRelationship(parent)), this._client);
    }
    withChildRelationship(child) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withChildRelationship(child)), this._client);
    }
    withIconName(iconName, options) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withIconName(iconName, options)), this._client);
    }
    withComputeEnvironment(computeEnvironmentResource) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withComputeEnvironment(computeEnvironmentResource)), this._client);
    }
    withHttpProbe(probeType, options) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withHttpProbe(probeType, options)), this._client);
    }
    excludeFromMcp() {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.excludeFromMcp()), this._client);
    }
    withHidden() {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withHidden()), this._client);
    }
    withHiddenOnCompletion(options) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withHiddenOnCompletion(options)), this._client);
    }
    withImagePushOptions(callback) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withImagePushOptions(callback)), this._client);
    }
    withRemoteImageName(remoteImageName) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withRemoteImageName(remoteImageName)), this._client);
    }
    withRemoteImageTag(remoteImageTag) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withRemoteImageTag(remoteImageTag)), this._client);
    }
    withPipelineStepFactory(stepName, callback, options) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)), this._client);
    }
    withPipelineConfiguration(callback) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.withPipelineConfiguration(callback)), this._client);
    }
    getResourceName() {
        return this._promise.then(obj => obj.getResourceName());
    }
    onBeforeResourceStarted(callback) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.onBeforeResourceStarted(callback)), this._client);
    }
    onResourceStopped(callback) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.onResourceStopped(callback)), this._client);
    }
    onInitializeResource(callback) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.onInitializeResource(callback)), this._client);
    }
    onResourceEndpointsAllocated(callback) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.onResourceEndpointsAllocated(callback)), this._client);
    }
    onResourceReady(callback) {
        return new DotnetToolResourcePromiseImpl(this._promise.then(obj => obj.onResourceReady(callback)), this._client);
    }
    createExecutionConfiguration() {
        return new ExecutionConfigurationBuilderPromiseImpl(this._promise.then(obj => obj.createExecutionConfiguration()), this._client);
    }
}
// ============================================================================
// ExecutableResourceImpl
// ============================================================================
/**
 * A resource that represents a specified executable process.
 *
 * You can run any executable command using its full path.
 * As a security feature, Aspire doesn't run executable unless the command is located in a path listed in the PATH environment variable.
 * To run an executable file that's in the current directory, specify the full path or use the relative path `./` to represent the current directory.
 */
class ExecutableResourceImpl extends ResourceBuilderBase {
    constructor(handle, client) {
        super(handle, client);
    }
    /** @internal */
    async _withContainerRegistryInternal(registry) {
        registry = isPromiseLike(registry) ? await registry : registry;
        const rpcArgs = { builder: this._handle, registry };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerRegistry', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Configures the resource to use the specified container registry for container image operations.
     *
     * This method adds a `ContainerRegistryReferenceAnnotation` to the resource,
     * indicating that the resource should use the specified container registry for container image operations.
     * @param registry The container registry resource builder.
     * @returns The resource builder for chaining.
     */
    withContainerRegistry(registry) {
        return new ExecutableResourcePromiseImpl(this._withContainerRegistryInternal(registry), this._client);
    }
    /** @internal */
    async _withDockerfileBaseImageInternal(buildImage, runtimeImage) {
        const rpcArgs = { builder: this._handle };
        if (buildImage !== undefined)
            rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined)
            rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileBaseImage', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Configures custom base images for generated Dockerfiles.
     *
     * This extension method allows customization of the base images used in generated Dockerfiles.
     * For multi-stage Dockerfiles (e.g., Python with UV), you can specify separate build and runtime images.
     * Specify custom base images for a Python application:
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddPythonApp("myapp", "path/to/app", "main.py")
     * .WithDockerfileBaseImage(
     * buildImage: "ghcr.io/astral-sh/uv:python3.12-bookworm-slim",
     * runtimeImage: "python:3.12-slim-bookworm");
     * builder.Build().Run();
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileBaseImage(options) {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new ExecutableResourcePromiseImpl(this._withDockerfileBaseImageInternal(buildImage, runtimeImage), this._client);
    }
    /** @internal */
    async _publishAsDockerFileInternal(configure) {
        const configureId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new ContainerResourceImpl(objHandle, this._client);
            await configure(obj);
        });
        const rpcArgs = { builder: this._handle, configure: configureId };
        const result = await this._client.invokeCapability('Aspire.Hosting/publishAsDockerFile', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Publishes an executable as a Docker file
     *
     * When the executable resource is converted to a container resource, the arguments to the executable
     * are not used. This is because arguments to the executable often contain physical paths that are not valid
     * in the container. The container can be set up with the correct arguments using the `configure` action.
     * @param configure Optional action to configure the container resource
     * @returns The resource builder.
     */
    publishAsDockerFile(configure) {
        return new ExecutableResourcePromiseImpl(this._publishAsDockerFileInternal(configure), this._client);
    }
    /** @internal */
    async _withExecutableCommandInternal(command) {
        const rpcArgs = { builder: this._handle, command };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExecutableCommand', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Sets the command for the executable resource.
     * @param command Command.
     * @returns The resource builder.
     */
    withExecutableCommand(command) {
        return new ExecutableResourcePromiseImpl(this._withExecutableCommandInternal(command), this._client);
    }
    /** @internal */
    async _withWorkingDirectoryInternal(workingDirectory) {
        const rpcArgs = { builder: this._handle, workingDirectory };
        const result = await this._client.invokeCapability('Aspire.Hosting/withWorkingDirectory', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Sets the working directory for the executable resource.
     * @param workingDirectory Working directory.
     * @returns The resource builder.
     */
    withWorkingDirectory(workingDirectory) {
        return new ExecutableResourcePromiseImpl(this._withWorkingDirectoryInternal(workingDirectory), this._client);
    }
    /** @internal */
    async _withMcpServerInternal(path, endpointName) {
        const rpcArgs = { builder: this._handle };
        if (path !== undefined)
            rpcArgs.path = path;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withMcpServer', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Marks the resource as hosting a Model Context Protocol (MCP) server on the specified endpoint.
     *
     * This method adds an `McpServerEndpointAnnotation` to the resource, enabling the Aspire tooling
     * to discover and proxy the MCP server exposed by the resource.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withMcpServer(options) {
        const path = options?.path;
        const endpointName = options?.endpointName;
        return new ExecutableResourcePromiseImpl(this._withMcpServerInternal(path, endpointName), this._client);
    }
    /** @internal */
    async _withOtlpExporterInternal(protocol) {
        const rpcArgs = { builder: this._handle };
        if (protocol !== undefined)
            rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability('Aspire.Hosting/withOtlpExporter', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Configures OTLP telemetry export
     * @param options Additional options.
     */
    withOtlpExporter(options) {
        const protocol = options?.protocol;
        return new ExecutableResourcePromiseImpl(this._withOtlpExporterInternal(protocol), this._client);
    }
    /** @internal */
    async _withRequiredCommandInternal(command, helpLink) {
        const rpcArgs = { builder: this._handle, command };
        if (helpLink !== undefined)
            rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability('Aspire.Hosting/withRequiredCommand', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Declares that a resource requires a specific command/executable to be available on the local machine PATH before it can start.
     *
     * The command is considered valid if either:
     * 1. It is an absolute or relative path (contains a directory separator) that points to an existing file, or
     * 2. It is discoverable on the current process PATH (respecting PATHEXT on Windows).
     * If the command is not found, a warning message will be logged but the resource will be allowed to attempt to start.
     * @param command The command string (file name or path) that should be validated.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withRequiredCommand(command, options) {
        const helpLink = options?.helpLink;
        return new ExecutableResourcePromiseImpl(this._withRequiredCommandInternal(command, helpLink), this._client);
    }
    /** @internal */
    async _withSessionLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withSessionLifetime', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a session lifetime.
     * @returns The resource builder.
     */
    withSessionLifetime() {
        return new ExecutableResourcePromiseImpl(this._withSessionLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withPersistentLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPersistentLifetime', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime.
     * @returns The resource builder.
     */
    withPersistentLifetime() {
        return new ExecutableResourcePromiseImpl(this._withPersistentLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withLifetimeOfInternal(sourceBuilder) {
        sourceBuilder = isPromiseLike(sourceBuilder) ? await sourceBuilder : sourceBuilder;
        const rpcArgs = { builder: this._handle, sourceBuilder };
        const result = await this._client.invokeCapability('Aspire.Hosting/withLifetimeOf', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to match the lifetime of another resource.
     *
     * The resource lifetime is evaluated from `sourceBuilder` when the application model is prepared, so later lifetime
     * changes to the source resource are reflected by this resource.
     * @param sourceBuilder The resource builder whose lifetime should be used.
     * @returns The resource builder.
     */
    withLifetimeOf(sourceBuilder) {
        return new ExecutableResourcePromiseImpl(this._withLifetimeOfInternal(sourceBuilder), this._client);
    }
    /** @internal */
    async _withParentProcessLifetimeInternal(parentProcessId) {
        const rpcArgs = { builder: this._handle, parentProcessId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withParentProcessLifetime', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime that ends when a parent process exits.
     * @param parentProcessId The ID of the parent process to monitor.
     * @returns The resource builder.
     */
    withParentProcessLifetime(parentProcessId) {
        return new ExecutableResourcePromiseImpl(this._withParentProcessLifetimeInternal(parentProcessId), this._client);
    }
    /** @internal */
    async _withEnvironmentInternal(name, value) {
        value = isPromiseLike(value) ? await value : value;
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEnvironment', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /** Sets an environment variable */
    withEnvironment(name, value) {
        return new ExecutableResourcePromiseImpl(this._withEnvironmentInternal(name, value), this._client);
    }
    /** @internal */
    async _withEnvironmentCallbackInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new EnvironmentCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEnvironmentCallback', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Allows for the population of environment variables on a resource.
     * @param callback A callback that allows for deferred execution for computing many environment variables. This runs after resources have been allocated by the orchestrator and allows access to other resources to resolve computed data, e.g. connection strings, ports.
     * @returns The resource builder.
     */
    withEnvironmentCallback(callback) {
        return new ExecutableResourcePromiseImpl(this._withEnvironmentCallbackInternal(callback), this._client);
    }
    /** @internal */
    async _withArgsInternal(args) {
        const rpcArgs = { builder: this._handle, args };
        const result = await this._client.invokeCapability('Aspire.Hosting/withArgs', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Adds arguments to be passed to a resource that supports arguments when it is launched.
     * @param args The arguments to be passed to the resource when it is started.
     * @returns The resource builder.
     */
    withArgs(args) {
        return new ExecutableResourcePromiseImpl(this._withArgsInternal(args), this._client);
    }
    /** @internal */
    async _withArgsCallbackInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new CommandLineArgsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withArgsCallback', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Adds a callback to be executed with a list of command-line arguments when a resource is started.
     * @param callback A callback that allows for deferred execution for computing arguments. This runs after resources have been allocated by the orchestrator and allows access to other resources to resolve computed data, e.g. connection strings, ports.
     * @returns The resource builder.
     */
    withArgsCallback(callback) {
        return new ExecutableResourcePromiseImpl(this._withArgsCallbackInternal(callback), this._client);
    }
    /** @internal */
    async _withReferenceEnvironmentInternal(options) {
        const rpcArgs = { builder: this._handle, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withReferenceEnvironment', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Configures how information is injected into environment variables when the resource references other resources.
     * @param options Options controlling which reference information is emitted.
     * @returns The resource builder.
     */
    withReferenceEnvironment(options) {
        return new ExecutableResourcePromiseImpl(this._withReferenceEnvironmentInternal(options), this._client);
    }
    /** @internal */
    async _withReferenceInternal(source, connectionName, optional, name) {
        source = isPromiseLike(source) ? await source : source;
        const rpcArgs = { builder: this._handle, source };
        if (connectionName !== undefined)
            rpcArgs.connectionName = connectionName;
        if (optional !== undefined)
            rpcArgs.optional = optional;
        if (name !== undefined)
            rpcArgs.name = name;
        const result = await this._client.invokeCapability('Aspire.Hosting/withReference', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Adds a reference to another resource
     * @param options Additional options.
     */
    withReference(source, options) {
        const connectionName = options?.connectionName;
        const optional = options?.optional;
        const name = options?.name;
        return new ExecutableResourcePromiseImpl(this._withReferenceInternal(source, connectionName, optional, name), this._client);
    }
    /** @internal */
    async _withEndpointCallbackInternal(endpointName, callback, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpointCallback', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Updates a named endpoint via callback
     * @param options Additional options.
     */
    withEndpointCallback(endpointName, callback, options) {
        const createIfNotExists = options?.createIfNotExists;
        return new ExecutableResourcePromiseImpl(this._withEndpointCallbackInternal(endpointName, callback, createIfNotExists), this._client);
    }
    /** @internal */
    async _withHttpEndpointCallbackInternal(callback, name, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        if (name !== undefined)
            rpcArgs.name = name;
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpEndpointCallback', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Updates an HTTP endpoint via callback
     * @param options Additional options.
     */
    withHttpEndpointCallback(callback, options) {
        const name = options?.name;
        const createIfNotExists = options?.createIfNotExists;
        return new ExecutableResourcePromiseImpl(this._withHttpEndpointCallbackInternal(callback, name, createIfNotExists), this._client);
    }
    /** @internal */
    async _withHttpsEndpointCallbackInternal(callback, name, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        if (name !== undefined)
            rpcArgs.name = name;
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpsEndpointCallback', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Updates an HTTPS endpoint via callback
     * @param options Additional options.
     */
    withHttpsEndpointCallback(callback, options) {
        const name = options?.name;
        const createIfNotExists = options?.createIfNotExists;
        return new ExecutableResourcePromiseImpl(this._withHttpsEndpointCallbackInternal(callback, name, createIfNotExists), this._client);
    }
    /** @internal */
    async _withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (scheme !== undefined)
            rpcArgs.scheme = scheme;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        if (isExternal !== undefined)
            rpcArgs.isExternal = isExternal;
        if (protocol !== undefined)
            rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpoint', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Adds a network endpoint
     * @param options Additional options.
     * @returns The resource builder.
     */
    withEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const scheme = options?.scheme;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        const isExternal = options?.isExternal;
        const protocol = options?.protocol;
        return new ExecutableResourcePromiseImpl(this._withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol), this._client);
    }
    /** @internal */
    async _withEndpointProxySupportInternal(proxyEnabled) {
        const rpcArgs = { builder: this._handle, proxyEnabled };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpointProxySupport', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Set whether a resource can use proxied endpoints or whether they should be disabled for all endpoints belonging to the resource. If set to `false`, endpoints belonging to the resource will ignore the configured proxy settings and run proxy-less.
     *
     * This method is intended to support scenarios with persistent lifetime resources where it is desirable for the resource to be accessible over the same
     * port whether the Aspire application is running or not. Proxied endpoints bind ports that are only accessible while the Aspire application is running.
     * The user needs to be careful to ensure that endpoints are using unique ports when disabling proxy support as by default for proxy-less
     * endpoints, Aspire will allocate the target port as the host port, which will increase the chance of port conflicts.
     * @param proxyEnabled Should endpoints for the resource support using a proxy?
     * @returns The resource builder.
     */
    withEndpointProxySupport(proxyEnabled) {
        return new ExecutableResourcePromiseImpl(this._withEndpointProxySupportInternal(proxyEnabled), this._client);
    }
    /** @internal */
    async _withHttpEndpointInternal(port, targetPort, name, env, isProxied) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpEndpoint', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP endpoint
     *
     * If an endpoint with the same name already exists on the resource, the existing endpoint is updated
     * with any non-null parameter values. Parameters left as `null` will not modify the existing endpoint's values.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new ExecutableResourcePromiseImpl(this._withHttpEndpointInternal(port, targetPort, name, env, isProxied), this._client);
    }
    /** @internal */
    async _withHttpsEndpointInternal(port, targetPort, name, env, isProxied) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpsEndpoint', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTPS endpoint
     *
     * If an endpoint with the same name already exists on the resource, the existing endpoint is updated
     * with any non-null parameter values. Parameters left as `null` will not modify the existing endpoint's values.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpsEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new ExecutableResourcePromiseImpl(this._withHttpsEndpointInternal(port, targetPort, name, env, isProxied), this._client);
    }
    /** @internal */
    async _withExternalHttpEndpointsInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExternalHttpEndpoints', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Marks existing http or https endpoints on a resource as external.
     * @returns The resource builder.
     */
    withExternalHttpEndpoints() {
        return new ExecutableResourcePromiseImpl(this._withExternalHttpEndpointsInternal(), this._client);
    }
    /**
     * Gets an endpoint reference
     * @param name The name of the endpoint.
     * @returns An `EndpointReference` that can be used to resolve the address of the endpoint after resource allocation has occurred.
     */
    getEndpoint(name) {
        const promise = (async () => {
            const rpcArgs = { builder: this._handle, name };
            const handle = await this._client.invokeCapability('Aspire.Hosting/getEndpoint', rpcArgs);
            return new EndpointReferenceImpl(handle, this._client);
        })();
        return new EndpointReferencePromiseImpl(promise, this._client);
    }
    /** @internal */
    async _asHttp2ServiceInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/asHttp2Service', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to mark all endpoints' transport as HTTP/2. This is useful for HTTP/2 services that need prior knowledge.
     * @returns The resource builder.
     */
    asHttp2Service() {
        return new ExecutableResourcePromiseImpl(this._asHttp2ServiceInternal(), this._client);
    }
    /** @internal */
    async _withUrlsInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new ResourceUrlsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrls', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to customize the URLs displayed for the resource.
     * @param callback The callback that will customize URLs for the resource.
     * @returns The resource builder.
     */
    withUrls(callback) {
        return new ExecutableResourcePromiseImpl(this._withUrlsInternal(callback), this._client);
    }
    /** @internal */
    async _withUrlInternal(url, displayText) {
        const rpcArgs = { builder: this._handle, url };
        if (displayText !== undefined)
            rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrl', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Adds or modifies displayed URLs
     * @param options Additional options.
     */
    withUrl(url, options) {
        const displayText = options?.displayText;
        return new ExecutableResourcePromiseImpl(this._withUrlInternal(url, displayText), this._client);
    }
    /** @internal */
    async _withUrlForEndpointInternal(endpointName, callback) {
        const callbackId = registerCallback(async (objData) => {
            const obj = wrapIfHandle(objData);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrlForEndpoint', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to update the URL displayed for the endpoint with the specified name.
     * @param endpointName The name of the endpoint to customize the URL for.
     * @param callback The callback that will customize the URL.
     * @returns The resource builder.
     */
    withUrlForEndpoint(endpointName, callback) {
        return new ExecutableResourcePromiseImpl(this._withUrlForEndpointInternal(endpointName, callback), this._client);
    }
    /** @internal */
    async _excludeFromManifestInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromManifest', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Excludes a resource from being published to the manifest.
     * @returns The resource builder.
     */
    excludeFromManifest() {
        return new ExecutableResourcePromiseImpl(this._excludeFromManifestInternal(), this._client);
    }
    /** @internal */
    async _waitForInternal(dependency, waitBehavior) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (waitBehavior !== undefined)
            rpcArgs.waitBehavior = waitBehavior;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitFor', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Waits for another resource to be ready
     * @param options Additional options.
     */
    waitFor(dependency, options) {
        const waitBehavior = options?.waitBehavior;
        return new ExecutableResourcePromiseImpl(this._waitForInternal(dependency, waitBehavior), this._client);
    }
    /** @internal */
    async _waitForStartInternal(dependency, waitBehavior) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (waitBehavior !== undefined)
            rpcArgs.waitBehavior = waitBehavior;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitForStart', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Waits for another resource to start
     * @param options Additional options.
     */
    waitForStart(dependency, options) {
        const waitBehavior = options?.waitBehavior;
        return new ExecutableResourcePromiseImpl(this._waitForStartInternal(dependency, waitBehavior), this._client);
    }
    /** @internal */
    async _withExplicitStartInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExplicitStart', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Prevents resource from starting automatically
     * @returns The resource builder.
     */
    withExplicitStart() {
        return new ExecutableResourcePromiseImpl(this._withExplicitStartInternal(), this._client);
    }
    /** @internal */
    async _waitForCompletionInternal(dependency, exitCode) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitForResourceCompletion', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Waits for the dependency resource to enter the Exited or Finished state before starting the resource.
     * @param dependency The resource builder for the dependency resource.
     * @param options Additional options.
     * @returns The resource builder.
     */
    waitForCompletion(dependency, options) {
        const exitCode = options?.exitCode;
        return new ExecutableResourcePromiseImpl(this._waitForCompletionInternal(dependency, exitCode), this._client);
    }
    /** @internal */
    async _withHealthCheckInternal(key) {
        const rpcArgs = { builder: this._handle, key };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHealthCheck', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Adds a health check by key
     * @param key The key for the health check.
     * @returns The resource builder.
     */
    withHealthCheck(key) {
        return new ExecutableResourcePromiseImpl(this._withHealthCheckInternal(key), this._client);
    }
    /** @internal */
    async _withHttpHealthCheckInternal(path, statusCode, endpointName) {
        const rpcArgs = { builder: this._handle };
        if (path !== undefined)
            rpcArgs.path = path;
        if (statusCode !== undefined)
            rpcArgs.statusCode = statusCode;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpHealthCheck', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Adds a health check to the resource which is mapped to a specific endpoint.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpHealthCheck(options) {
        const path = options?.path;
        const statusCode = options?.statusCode;
        const endpointName = options?.endpointName;
        return new ExecutableResourcePromiseImpl(this._withHttpHealthCheckInternal(path, statusCode, endpointName), this._client);
    }
    /** @internal */
    async _withCommandInternal(name, displayName, executeCommand, commandOptions) {
        const executeCommandId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined)
            rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability('Aspire.Hosting/withCommand', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Adds a resource command
     *
     * The `WithCommand` method is used to add commands to the resource. Commands are displayed in the dashboard
     * and can be executed by a user using the dashboard UI.
     * When a command is executed, the `executeCommand` callback is called and is run inside the Aspire host.
     * @param name The name of the command. The name uniquely identifies the command.
     * @param displayName The display name visible in UI.
     * @param executeCommand A callback that is executed when the command is executed. The callback is run inside the Aspire host. The callback result is used to indicate success or failure in the UI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withCommand(name, displayName, executeCommand, options) {
        const commandOptions = options?.commandOptions;
        return new ExecutableResourcePromiseImpl(this._withCommandInternal(name, displayName, executeCommand, commandOptions), this._client);
    }
    /** @internal */
    async _withProcessCommandInternal(commandName, displayName, options) {
        const rpcArgs = { builder: this._handle, commandName, displayName, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommand', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /** Adds a command to the resource that starts a local process when invoked. */
    withProcessCommand(commandName, displayName, options) {
        return new ExecutableResourcePromiseImpl(this._withProcessCommandInternal(commandName, displayName, options), this._client);
    }
    /** @internal */
    async _withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options) {
        const createProcessSpecId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await createProcessSpec(arg);
        });
        const rpcArgs = { builder: this._handle, commandName, displayName, createProcessSpec: createProcessSpecId };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommandFactory', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Adds a command to the resource that starts a local process created by a callback when invoked.
     * @param options Additional options.
     */
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new ExecutableResourcePromiseImpl(this._withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options), this._client);
    }
    /** @internal */
    async _withHttpCommandInternal(path, displayName, options) {
        const rpcArgs = { builder: this._handle, path, displayName };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpCommand', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP resource command
     * @param options Additional options.
     */
    withHttpCommand(path, displayName, options) {
        return new ExecutableResourcePromiseImpl(this._withHttpCommandInternal(path, displayName, options), this._client);
    }
    /** @internal */
    async _withDeveloperCertificateTrustInternal(trust) {
        const rpcArgs = { builder: this._handle, trust };
        const result = await this._client.invokeCapability('Aspire.Hosting/withDeveloperCertificateTrust', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Indicates whether developer certificates should be treated as trusted certificate authorities for the resource at run time. Currently this indicates trust for the ASP.NET Core developer certificate. The developer certificate will only be trusted when running in local development scenarios; in publish mode resources will use their default certificate trust.
     *
     * Disable trust for app host managed developer certificate(s) for a container resource.
     * ```
     * var container = builder.AddContainer("my-service", "my-service:latest")
     * .WithDeveloperCertificateTrust(false);
     * ```
     * Disable automatic trust for app host managed developer certificate(s), but explicitly enable it for a specific resource.
     * ```
     * var builder = DistributedApplication.CreateBuilder(new DistributedApplicationOptions()
     * {
     * Args = args,
     * TrustDeveloperCertificate = false,
     * });
     * var project = builder.AddProject<MyService>("my-service")
     * .WithDeveloperCertificateTrust(true);
     * ```
     * @param trust Indicates whether the developer certificate should be treated as trusted.
     * @returns The resource builder.
     */
    withDeveloperCertificateTrust(trust) {
        return new ExecutableResourcePromiseImpl(this._withDeveloperCertificateTrustInternal(trust), this._client);
    }
    /** @internal */
    async _withCertificateTrustScopeInternal(scope) {
        const rpcArgs = { builder: this._handle, scope };
        const result = await this._client.invokeCapability('Aspire.Hosting/withCertificateTrustScope', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Sets the certificate trust scope
     *
     * The default scope if not overridden is `Append` which means that custom certificate
     * authorities should be appended to the default trusted certificate authorities for the resource. Setting the scope to
     * `Override` indicates the set of certificates in referenced
     * `CertificateAuthorityCollection` (and optionally Aspire developer certificiates) should be used as the
     * exclusive source of trust for a resource.
     * In all cases, this is a best effort implementation as not all resources support full customization of certificate
     * trust.
     * Set the scope for custom certificate authorities to override the default trusted certificate authorities for a container resource.
     * ```
     * var caCollection = builder.AddCertificateAuthorityCollection("my-cas")
     * .WithCertificate(new X509Certificate2("my-ca.pem"));
     * var container = builder.AddContainer("my-service", "my-service:latest")
     * .WithCertificateAuthorityCollection(caCollection)
     * .WithCertificateTrustScope(CertificateTrustScope.Override);
     * ```
     * @param scope The scope to apply to custom certificate authorities associated with the resource.
     * @returns The resource builder.
     */
    withCertificateTrustScope(scope) {
        return new ExecutableResourcePromiseImpl(this._withCertificateTrustScopeInternal(scope), this._client);
    }
    /** @internal */
    async _withHttpsDeveloperCertificateInternal(password) {
        password = isPromiseLike(password) ? await password : password;
        const rpcArgs = { builder: this._handle };
        if (password !== undefined)
            rpcArgs.password = password;
        const result = await this._client.invokeCapability('Aspire.Hosting/withParameterHttpsDeveloperCertificate', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Indicates that a resource should use the developer certificate key pair for HTTPS endpoints at run time. Currently this indicates use of the ASP.NET Core developer certificate. The developer certificate will only be used when running in local development scenarios; in publish mode resources will use their default certificate configuration.
     *
     * Use the developer certificate for HTTPS/TLS endpoints on a container resource:
     * ```
     * builder.AddContainer("my-service", "my-image")
     * .WithHttpsDeveloperCertificate()
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpsDeveloperCertificate(options) {
        let password = options?.password;
        return new ExecutableResourcePromiseImpl(this._withHttpsDeveloperCertificateInternal(password), this._client);
    }
    /** @internal */
    async _withoutHttpsCertificateInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withoutHttpsCertificate', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Disable HTTPS/TLS server certificate configuration for the resource. No HTTPS/TLS termination configuration will be applied.
     *
     * Disable HTTPS certificate configuration for a Redis resource:
     * ```
     * var redis = builder.AddRedis("cache")
     * .WithoutHttpsCertificate();
     * ```
     * @returns The resource builder.
     */
    withoutHttpsCertificate() {
        return new ExecutableResourcePromiseImpl(this._withoutHttpsCertificateInternal(), this._client);
    }
    /** @internal */
    async _withRelationshipInternal(resourceBuilder, type) {
        resourceBuilder = isPromiseLike(resourceBuilder) ? await resourceBuilder : resourceBuilder;
        const rpcArgs = { builder: this._handle, resourceBuilder, type };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderRelationship', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Adds a relationship to another resource using its builder.
     * @param resourceBuilder The resource builder that the relationship is to.
     * @param type The relationship type.
     * @returns A resource builder.
     */
    withRelationship(resourceBuilder, type) {
        return new ExecutableResourcePromiseImpl(this._withRelationshipInternal(resourceBuilder, type), this._client);
    }
    /** @internal */
    async _withParentRelationshipInternal(parent) {
        parent = isPromiseLike(parent) ? await parent : parent;
        const rpcArgs = { builder: this._handle, parent };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderParentRelationship', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Sets the parent relationship
     * @param parent The parent of `builder`.
     * @returns A resource builder.
     */
    withParentRelationship(parent) {
        return new ExecutableResourcePromiseImpl(this._withParentRelationshipInternal(parent), this._client);
    }
    /** @internal */
    async _withChildRelationshipInternal(child) {
        child = isPromiseLike(child) ? await child : child;
        const rpcArgs = { builder: this._handle, child };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderChildRelationship', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Sets a child relationship
     * @param child The child of `builder`.
     * @returns A resource builder.
     */
    withChildRelationship(child) {
        return new ExecutableResourcePromiseImpl(this._withChildRelationshipInternal(child), this._client);
    }
    /** @internal */
    async _withIconNameInternal(iconName, iconVariant) {
        const rpcArgs = { builder: this._handle, iconName };
        if (iconVariant !== undefined)
            rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability('Aspire.Hosting/withIconName', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Specifies the icon to use when displaying the resource in the dashboard.
     * @param iconName The name of the FluentUI icon to use. See https://aka.ms/fluentui-system-icons for available icons.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withIconName(iconName, options) {
        const iconVariant = options?.iconVariant;
        return new ExecutableResourcePromiseImpl(this._withIconNameInternal(iconName, iconVariant), this._client);
    }
    /** @internal */
    async _withComputeEnvironmentInternal(computeEnvironmentResource) {
        computeEnvironmentResource = isPromiseLike(computeEnvironmentResource) ? await computeEnvironmentResource : computeEnvironmentResource;
        const rpcArgs = { builder: this._handle, computeEnvironmentResource };
        const result = await this._client.invokeCapability('Aspire.Hosting/withComputeEnvironment', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Configures the compute environment for the compute resource.
     *
     * This method allows associating a specific compute environment with the compute resource.
     * @param computeEnvironmentResource The compute environment resource to associate with the compute resource.
     * @returns The resource builder.
     */
    withComputeEnvironment(computeEnvironmentResource) {
        return new ExecutableResourcePromiseImpl(this._withComputeEnvironmentInternal(computeEnvironmentResource), this._client);
    }
    /** @internal */
    async _withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName) {
        const rpcArgs = { builder: this._handle, probeType };
        if (path !== undefined)
            rpcArgs.path = path;
        if (initialDelaySeconds !== undefined)
            rpcArgs.initialDelaySeconds = initialDelaySeconds;
        if (periodSeconds !== undefined)
            rpcArgs.periodSeconds = periodSeconds;
        if (timeoutSeconds !== undefined)
            rpcArgs.timeoutSeconds = timeoutSeconds;
        if (failureThreshold !== undefined)
            rpcArgs.failureThreshold = failureThreshold;
        if (successThreshold !== undefined)
            rpcArgs.successThreshold = successThreshold;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpProbe', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP health probe to the resource
     * @param options Additional options.
     */
    withHttpProbe(probeType, options) {
        const path = options?.path;
        const initialDelaySeconds = options?.initialDelaySeconds;
        const periodSeconds = options?.periodSeconds;
        const timeoutSeconds = options?.timeoutSeconds;
        const failureThreshold = options?.failureThreshold;
        const successThreshold = options?.successThreshold;
        const endpointName = options?.endpointName;
        return new ExecutableResourcePromiseImpl(this._withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName), this._client);
    }
    /** @internal */
    async _excludeFromMcpInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromMcp', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Exclude the resource from MCP operations using the Aspire MCP server. The resource is excluded from results that return resources, console logs and telemetry.
     * @returns The resource builder.
     */
    excludeFromMcp() {
        return new ExecutableResourcePromiseImpl(this._excludeFromMcpInternal(), this._client);
    }
    /** @internal */
    async _withHiddenInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHidden', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists
     *
     * Use this method to hide resources that are implementation details and should never be displayed by default.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @returns The resource builder.
     */
    withHidden() {
        return new ExecutableResourcePromiseImpl(this._withHiddenInternal(), this._client);
    }
    /** @internal */
    async _withHiddenOnCompletionInternal(exitCode, exitCodes) {
        const rpcArgs = { builder: this._handle };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        if (exitCodes !== undefined)
            rpcArgs.exitCodes = exitCodes;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHiddenOnCompletion', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists after successful completion
     *
     * This method is useful for one-off resources such as setup scripts, migrations, or build steps that should remain visible while running
     * and then be hidden after successful completion.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHiddenOnCompletion(options) {
        const exitCode = options?.exitCode;
        const exitCodes = options?.exitCodes;
        return new ExecutableResourcePromiseImpl(this._withHiddenOnCompletionInternal(exitCode, exitCodes), this._client);
    }
    /** @internal */
    async _withImagePushOptionsInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ContainerImagePushOptionsCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImagePushOptions', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Adds an asynchronous callback to configure container image push options for the resource.
     *
     * This method allows customization of how container images are named and tagged when pushed to a registry using an asynchronous callback.
     * Use this overload when the callback needs to perform asynchronous operations such as retrieving configuration values from external sources.
     * The callback receives a `ContainerImagePushOptionsCallbackContext` that provides access to the resource
     * and the `ContainerImagePushOptions` that can be modified.
     * Multiple callbacks can be registered on the same resource, and they will be invoked in the order they were added.
     * @param callback The asynchronous callback to configure push options.
     * @returns The resource builder.
     */
    withImagePushOptions(callback) {
        return new ExecutableResourcePromiseImpl(this._withImagePushOptionsInternal(callback), this._client);
    }
    /** @internal */
    async _withRemoteImageNameInternal(remoteImageName) {
        const rpcArgs = { builder: this._handle, remoteImageName };
        const result = await this._client.invokeCapability('Aspire.Hosting/withRemoteImageName', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Sets the remote image name (without registry endpoint or tag) for container push operations.
     *
     * Use this with `withRemoteImageTag` to fully customize the image reference used for container push operations.
     * @param remoteImageName The remote image name (e.g., "myapp" or "myorg/myapp").
     * @returns The resource builder.
     */
    withRemoteImageName(remoteImageName) {
        return new ExecutableResourcePromiseImpl(this._withRemoteImageNameInternal(remoteImageName), this._client);
    }
    /** @internal */
    async _withRemoteImageTagInternal(remoteImageTag) {
        const rpcArgs = { builder: this._handle, remoteImageTag };
        const result = await this._client.invokeCapability('Aspire.Hosting/withRemoteImageTag', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Sets the remote image tag for container push operations.
     *
     * Use this with `withRemoteImageName` to fully customize the image reference used for container push operations.
     * @param remoteImageTag The remote image tag (e.g., "latest", "v1.0.0").
     * @returns The resource builder.
     */
    withRemoteImageTag(remoteImageTag) {
        return new ExecutableResourcePromiseImpl(this._withRemoteImageTagInternal(remoteImageTag), this._client);
    }
    /** @internal */
    async _withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new PipelineStepContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined)
            rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined)
            rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined)
            rpcArgs.tags = tags;
        if (description !== undefined)
            rpcArgs.description = description;
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineStepFactory', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Adds a pipeline step to the resource that will be executed during deployment.
     * @param stepName The unique name of the pipeline step.
     * @param callback The callback to execute when the step runs.
     * @param options Additional options.
     * @returns The resource builder for chaining.
     */
    withPipelineStepFactory(stepName, callback, options) {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new ExecutableResourcePromiseImpl(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description), this._client);
    }
    /** @internal */
    async _withPipelineConfigurationInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new PipelineConfigurationContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineConfiguration', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to be executed during the pipeline configuration phase, allowing modification of step dependencies and relationships.
     * @param callback The callback function to execute during the configuration phase.
     * @returns The resource builder for chaining.
     */
    withPipelineConfiguration(callback) {
        return new ExecutableResourcePromiseImpl(this._withPipelineConfigurationInternal(callback), this._client);
    }
    /**
     * Gets the name of the resource from a builder.
     *
     * Why this wrapper exists: This capability accesses a nested property
     * (`resource.Resource.Name`) which requires a wrapper method. There is no single
     * .NET method that returns just the resource name that could be annotated directly.
     * @returns The resource name.
     */
    async getResourceName() {
        const rpcArgs = { resource: this._handle };
        return await this._client.invokeCapability('Aspire.Hosting/getResourceName', rpcArgs);
    }
    /** @internal */
    async _onBeforeResourceStartedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new BeforeResourceStartedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onBeforeResourceStarted', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the BeforeResourceStarted event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onBeforeResourceStarted(callback) {
        return new ExecutableResourcePromiseImpl(this._onBeforeResourceStartedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceStoppedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceStoppedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceStopped', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceStopped event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceStopped(callback) {
        return new ExecutableResourcePromiseImpl(this._onResourceStoppedInternal(callback), this._client);
    }
    /** @internal */
    async _onInitializeResourceInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new InitializeResourceEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onInitializeResource', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the InitializeResource event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onInitializeResource(callback) {
        return new ExecutableResourcePromiseImpl(this._onInitializeResourceInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceEndpointsAllocatedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceEndpointsAllocatedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceEndpointsAllocated', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceEndpointsAllocated event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceEndpointsAllocated(callback) {
        return new ExecutableResourcePromiseImpl(this._onResourceEndpointsAllocatedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceReadyInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceReadyEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceReady', rpcArgs);
        return new ExecutableResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceReady event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceReady(callback) {
        return new ExecutableResourcePromiseImpl(this._onResourceReadyInternal(callback), this._client);
    }
    /**
     * Creates an execution configuration builder for the specified resource.
     * @returns The execution configuration builder.
     */
    createExecutionConfiguration() {
        const promise = (async () => {
            const rpcArgs = { resource: this._handle };
            const handle = await this._client.invokeCapability('Aspire.Hosting/createExecutionConfiguration', rpcArgs);
            return new ExecutionConfigurationBuilderImpl(handle, this._client);
        })();
        return new ExecutionConfigurationBuilderPromiseImpl(promise, this._client);
    }
}
/**
 * Thenable wrapper for ExecutableResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
class ExecutableResourcePromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    withContainerRegistry(registry) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withContainerRegistry(registry)), this._client);
    }
    withDockerfileBaseImage(options) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileBaseImage(options)), this._client);
    }
    publishAsDockerFile(configure) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.publishAsDockerFile(configure)), this._client);
    }
    withExecutableCommand(command) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withExecutableCommand(command)), this._client);
    }
    withWorkingDirectory(workingDirectory) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withWorkingDirectory(workingDirectory)), this._client);
    }
    withMcpServer(options) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withMcpServer(options)), this._client);
    }
    withOtlpExporter(options) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withOtlpExporter(options)), this._client);
    }
    withRequiredCommand(command, options) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withRequiredCommand(command, options)), this._client);
    }
    withSessionLifetime() {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withSessionLifetime()), this._client);
    }
    withPersistentLifetime() {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withPersistentLifetime()), this._client);
    }
    withLifetimeOf(sourceBuilder) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withLifetimeOf(sourceBuilder)), this._client);
    }
    withParentProcessLifetime(parentProcessId) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withParentProcessLifetime(parentProcessId)), this._client);
    }
    withEnvironment(name, value) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withEnvironment(name, value)), this._client);
    }
    withEnvironmentCallback(callback) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withEnvironmentCallback(callback)), this._client);
    }
    withArgs(args) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withArgs(args)), this._client);
    }
    withArgsCallback(callback) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withArgsCallback(callback)), this._client);
    }
    withReferenceEnvironment(options) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withReferenceEnvironment(options)), this._client);
    }
    withReference(source, options) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withReference(source, options)), this._client);
    }
    withEndpointCallback(endpointName, callback, options) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withEndpointCallback(endpointName, callback, options)), this._client);
    }
    withHttpEndpointCallback(callback, options) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withHttpEndpointCallback(callback, options)), this._client);
    }
    withHttpsEndpointCallback(callback, options) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withHttpsEndpointCallback(callback, options)), this._client);
    }
    withEndpoint(options) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withEndpoint(options)), this._client);
    }
    withEndpointProxySupport(proxyEnabled) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withEndpointProxySupport(proxyEnabled)), this._client);
    }
    withHttpEndpoint(options) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withHttpEndpoint(options)), this._client);
    }
    withHttpsEndpoint(options) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withHttpsEndpoint(options)), this._client);
    }
    withExternalHttpEndpoints() {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withExternalHttpEndpoints()), this._client);
    }
    getEndpoint(name) {
        return new EndpointReferencePromiseImpl(this._promise.then(obj => obj.getEndpoint(name)), this._client);
    }
    asHttp2Service() {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.asHttp2Service()), this._client);
    }
    withUrls(callback) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withUrls(callback)), this._client);
    }
    withUrl(url, options) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withUrl(url, options)), this._client);
    }
    withUrlForEndpoint(endpointName, callback) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)), this._client);
    }
    excludeFromManifest() {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.excludeFromManifest()), this._client);
    }
    waitFor(dependency, options) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.waitFor(dependency, options)), this._client);
    }
    waitForStart(dependency, options) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.waitForStart(dependency, options)), this._client);
    }
    withExplicitStart() {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withExplicitStart()), this._client);
    }
    waitForCompletion(dependency, options) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.waitForCompletion(dependency, options)), this._client);
    }
    withHealthCheck(key) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withHealthCheck(key)), this._client);
    }
    withHttpHealthCheck(options) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withHttpHealthCheck(options)), this._client);
    }
    withCommand(name, displayName, executeCommand, options) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)), this._client);
    }
    withProcessCommand(commandName, displayName, options) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommand(commandName, displayName, options)), this._client);
    }
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommandFactory(commandName, displayName, createProcessSpec, options)), this._client);
    }
    withHttpCommand(path, displayName, options) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withHttpCommand(path, displayName, options)), this._client);
    }
    withDeveloperCertificateTrust(trust) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withDeveloperCertificateTrust(trust)), this._client);
    }
    withCertificateTrustScope(scope) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withCertificateTrustScope(scope)), this._client);
    }
    withHttpsDeveloperCertificate(options) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withHttpsDeveloperCertificate(options)), this._client);
    }
    withoutHttpsCertificate() {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withoutHttpsCertificate()), this._client);
    }
    withRelationship(resourceBuilder, type) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withRelationship(resourceBuilder, type)), this._client);
    }
    withParentRelationship(parent) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withParentRelationship(parent)), this._client);
    }
    withChildRelationship(child) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withChildRelationship(child)), this._client);
    }
    withIconName(iconName, options) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withIconName(iconName, options)), this._client);
    }
    withComputeEnvironment(computeEnvironmentResource) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withComputeEnvironment(computeEnvironmentResource)), this._client);
    }
    withHttpProbe(probeType, options) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withHttpProbe(probeType, options)), this._client);
    }
    excludeFromMcp() {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.excludeFromMcp()), this._client);
    }
    withHidden() {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withHidden()), this._client);
    }
    withHiddenOnCompletion(options) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withHiddenOnCompletion(options)), this._client);
    }
    withImagePushOptions(callback) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withImagePushOptions(callback)), this._client);
    }
    withRemoteImageName(remoteImageName) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withRemoteImageName(remoteImageName)), this._client);
    }
    withRemoteImageTag(remoteImageTag) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withRemoteImageTag(remoteImageTag)), this._client);
    }
    withPipelineStepFactory(stepName, callback, options) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)), this._client);
    }
    withPipelineConfiguration(callback) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.withPipelineConfiguration(callback)), this._client);
    }
    getResourceName() {
        return this._promise.then(obj => obj.getResourceName());
    }
    onBeforeResourceStarted(callback) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.onBeforeResourceStarted(callback)), this._client);
    }
    onResourceStopped(callback) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.onResourceStopped(callback)), this._client);
    }
    onInitializeResource(callback) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.onInitializeResource(callback)), this._client);
    }
    onResourceEndpointsAllocated(callback) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.onResourceEndpointsAllocated(callback)), this._client);
    }
    onResourceReady(callback) {
        return new ExecutableResourcePromiseImpl(this._promise.then(obj => obj.onResourceReady(callback)), this._client);
    }
    createExecutionConfiguration() {
        return new ExecutionConfigurationBuilderPromiseImpl(this._promise.then(obj => obj.createExecutionConfiguration()), this._client);
    }
}
// ============================================================================
// ExternalServiceResourceImpl
// ============================================================================
class ExternalServiceResourceImpl extends ResourceBuilderBase {
    constructor(handle, client) {
        super(handle, client);
    }
    /** @internal */
    async _withContainerRegistryInternal(registry) {
        registry = isPromiseLike(registry) ? await registry : registry;
        const rpcArgs = { builder: this._handle, registry };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerRegistry', rpcArgs);
        return new ExternalServiceResourceImpl(result, this._client);
    }
    /**
     * Configures the resource to use the specified container registry for container image operations.
     *
     * This method adds a `ContainerRegistryReferenceAnnotation` to the resource,
     * indicating that the resource should use the specified container registry for container image operations.
     * @param registry The container registry resource builder.
     * @returns The resource builder for chaining.
     */
    withContainerRegistry(registry) {
        return new ExternalServiceResourcePromiseImpl(this._withContainerRegistryInternal(registry), this._client);
    }
    /** @internal */
    async _withDockerfileBaseImageInternal(buildImage, runtimeImage) {
        const rpcArgs = { builder: this._handle };
        if (buildImage !== undefined)
            rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined)
            rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileBaseImage', rpcArgs);
        return new ExternalServiceResourceImpl(result, this._client);
    }
    /**
     * Configures custom base images for generated Dockerfiles.
     *
     * This extension method allows customization of the base images used in generated Dockerfiles.
     * For multi-stage Dockerfiles (e.g., Python with UV), you can specify separate build and runtime images.
     * Specify custom base images for a Python application:
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddPythonApp("myapp", "path/to/app", "main.py")
     * .WithDockerfileBaseImage(
     * buildImage: "ghcr.io/astral-sh/uv:python3.12-bookworm-slim",
     * runtimeImage: "python:3.12-slim-bookworm");
     * builder.Build().Run();
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileBaseImage(options) {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new ExternalServiceResourcePromiseImpl(this._withDockerfileBaseImageInternal(buildImage, runtimeImage), this._client);
    }
    /** @internal */
    async _withHttpHealthCheckInternal(path, statusCode, endpointName) {
        const rpcArgs = { builder: this._handle };
        if (path !== undefined)
            rpcArgs.path = path;
        if (statusCode !== undefined)
            rpcArgs.statusCode = statusCode;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withExternalServiceHttpHealthCheck', rpcArgs);
        return new ExternalServiceResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP health check to the external service for polyglot app hosts.
     * @param options Additional options.
     */
    withHttpHealthCheck(options) {
        const path = options?.path;
        const statusCode = options?.statusCode;
        const endpointName = options?.endpointName;
        return new ExternalServiceResourcePromiseImpl(this._withHttpHealthCheckInternal(path, statusCode, endpointName), this._client);
    }
    /** @internal */
    async _withRequiredCommandInternal(command, helpLink) {
        const rpcArgs = { builder: this._handle, command };
        if (helpLink !== undefined)
            rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability('Aspire.Hosting/withRequiredCommand', rpcArgs);
        return new ExternalServiceResourceImpl(result, this._client);
    }
    /**
     * Declares that a resource requires a specific command/executable to be available on the local machine PATH before it can start.
     *
     * The command is considered valid if either:
     * 1. It is an absolute or relative path (contains a directory separator) that points to an existing file, or
     * 2. It is discoverable on the current process PATH (respecting PATHEXT on Windows).
     * If the command is not found, a warning message will be logged but the resource will be allowed to attempt to start.
     * @param command The command string (file name or path) that should be validated.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withRequiredCommand(command, options) {
        const helpLink = options?.helpLink;
        return new ExternalServiceResourcePromiseImpl(this._withRequiredCommandInternal(command, helpLink), this._client);
    }
    /** @internal */
    async _withSessionLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withSessionLifetime', rpcArgs);
        return new ExternalServiceResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a session lifetime.
     * @returns The resource builder.
     */
    withSessionLifetime() {
        return new ExternalServiceResourcePromiseImpl(this._withSessionLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withPersistentLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPersistentLifetime', rpcArgs);
        return new ExternalServiceResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime.
     * @returns The resource builder.
     */
    withPersistentLifetime() {
        return new ExternalServiceResourcePromiseImpl(this._withPersistentLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withLifetimeOfInternal(sourceBuilder) {
        sourceBuilder = isPromiseLike(sourceBuilder) ? await sourceBuilder : sourceBuilder;
        const rpcArgs = { builder: this._handle, sourceBuilder };
        const result = await this._client.invokeCapability('Aspire.Hosting/withLifetimeOf', rpcArgs);
        return new ExternalServiceResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to match the lifetime of another resource.
     *
     * The resource lifetime is evaluated from `sourceBuilder` when the application model is prepared, so later lifetime
     * changes to the source resource are reflected by this resource.
     * @param sourceBuilder The resource builder whose lifetime should be used.
     * @returns The resource builder.
     */
    withLifetimeOf(sourceBuilder) {
        return new ExternalServiceResourcePromiseImpl(this._withLifetimeOfInternal(sourceBuilder), this._client);
    }
    /** @internal */
    async _withParentProcessLifetimeInternal(parentProcessId) {
        const rpcArgs = { builder: this._handle, parentProcessId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withParentProcessLifetime', rpcArgs);
        return new ExternalServiceResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime that ends when a parent process exits.
     * @param parentProcessId The ID of the parent process to monitor.
     * @returns The resource builder.
     */
    withParentProcessLifetime(parentProcessId) {
        return new ExternalServiceResourcePromiseImpl(this._withParentProcessLifetimeInternal(parentProcessId), this._client);
    }
    /** @internal */
    async _withUrlsInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new ResourceUrlsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrls', rpcArgs);
        return new ExternalServiceResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to customize the URLs displayed for the resource.
     * @param callback The callback that will customize URLs for the resource.
     * @returns The resource builder.
     */
    withUrls(callback) {
        return new ExternalServiceResourcePromiseImpl(this._withUrlsInternal(callback), this._client);
    }
    /** @internal */
    async _withUrlInternal(url, displayText) {
        const rpcArgs = { builder: this._handle, url };
        if (displayText !== undefined)
            rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrl', rpcArgs);
        return new ExternalServiceResourceImpl(result, this._client);
    }
    /**
     * Adds or modifies displayed URLs
     * @param options Additional options.
     */
    withUrl(url, options) {
        const displayText = options?.displayText;
        return new ExternalServiceResourcePromiseImpl(this._withUrlInternal(url, displayText), this._client);
    }
    /** @internal */
    async _withUrlForEndpointInternal(endpointName, callback) {
        const callbackId = registerCallback(async (objData) => {
            const obj = wrapIfHandle(objData);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrlForEndpoint', rpcArgs);
        return new ExternalServiceResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to update the URL displayed for the endpoint with the specified name.
     * @param endpointName The name of the endpoint to customize the URL for.
     * @param callback The callback that will customize the URL.
     * @returns The resource builder.
     */
    withUrlForEndpoint(endpointName, callback) {
        return new ExternalServiceResourcePromiseImpl(this._withUrlForEndpointInternal(endpointName, callback), this._client);
    }
    /** @internal */
    async _excludeFromManifestInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromManifest', rpcArgs);
        return new ExternalServiceResourceImpl(result, this._client);
    }
    /**
     * Excludes a resource from being published to the manifest.
     * @returns The resource builder.
     */
    excludeFromManifest() {
        return new ExternalServiceResourcePromiseImpl(this._excludeFromManifestInternal(), this._client);
    }
    /** @internal */
    async _withExplicitStartInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExplicitStart', rpcArgs);
        return new ExternalServiceResourceImpl(result, this._client);
    }
    /**
     * Prevents resource from starting automatically
     * @returns The resource builder.
     */
    withExplicitStart() {
        return new ExternalServiceResourcePromiseImpl(this._withExplicitStartInternal(), this._client);
    }
    /** @internal */
    async _withHealthCheckInternal(key) {
        const rpcArgs = { builder: this._handle, key };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHealthCheck', rpcArgs);
        return new ExternalServiceResourceImpl(result, this._client);
    }
    /**
     * Adds a health check by key
     * @param key The key for the health check.
     * @returns The resource builder.
     */
    withHealthCheck(key) {
        return new ExternalServiceResourcePromiseImpl(this._withHealthCheckInternal(key), this._client);
    }
    /** @internal */
    async _withCommandInternal(name, displayName, executeCommand, commandOptions) {
        const executeCommandId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined)
            rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability('Aspire.Hosting/withCommand', rpcArgs);
        return new ExternalServiceResourceImpl(result, this._client);
    }
    /**
     * Adds a resource command
     *
     * The `WithCommand` method is used to add commands to the resource. Commands are displayed in the dashboard
     * and can be executed by a user using the dashboard UI.
     * When a command is executed, the `executeCommand` callback is called and is run inside the Aspire host.
     * @param name The name of the command. The name uniquely identifies the command.
     * @param displayName The display name visible in UI.
     * @param executeCommand A callback that is executed when the command is executed. The callback is run inside the Aspire host. The callback result is used to indicate success or failure in the UI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withCommand(name, displayName, executeCommand, options) {
        const commandOptions = options?.commandOptions;
        return new ExternalServiceResourcePromiseImpl(this._withCommandInternal(name, displayName, executeCommand, commandOptions), this._client);
    }
    /** @internal */
    async _withProcessCommandInternal(commandName, displayName, options) {
        const rpcArgs = { builder: this._handle, commandName, displayName, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommand', rpcArgs);
        return new ExternalServiceResourceImpl(result, this._client);
    }
    /** Adds a command to the resource that starts a local process when invoked. */
    withProcessCommand(commandName, displayName, options) {
        return new ExternalServiceResourcePromiseImpl(this._withProcessCommandInternal(commandName, displayName, options), this._client);
    }
    /** @internal */
    async _withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options) {
        const createProcessSpecId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await createProcessSpec(arg);
        });
        const rpcArgs = { builder: this._handle, commandName, displayName, createProcessSpec: createProcessSpecId };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommandFactory', rpcArgs);
        return new ExternalServiceResourceImpl(result, this._client);
    }
    /**
     * Adds a command to the resource that starts a local process created by a callback when invoked.
     * @param options Additional options.
     */
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new ExternalServiceResourcePromiseImpl(this._withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options), this._client);
    }
    /** @internal */
    async _withRelationshipInternal(resourceBuilder, type) {
        resourceBuilder = isPromiseLike(resourceBuilder) ? await resourceBuilder : resourceBuilder;
        const rpcArgs = { builder: this._handle, resourceBuilder, type };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderRelationship', rpcArgs);
        return new ExternalServiceResourceImpl(result, this._client);
    }
    /**
     * Adds a relationship to another resource using its builder.
     * @param resourceBuilder The resource builder that the relationship is to.
     * @param type The relationship type.
     * @returns A resource builder.
     */
    withRelationship(resourceBuilder, type) {
        return new ExternalServiceResourcePromiseImpl(this._withRelationshipInternal(resourceBuilder, type), this._client);
    }
    /** @internal */
    async _withParentRelationshipInternal(parent) {
        parent = isPromiseLike(parent) ? await parent : parent;
        const rpcArgs = { builder: this._handle, parent };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderParentRelationship', rpcArgs);
        return new ExternalServiceResourceImpl(result, this._client);
    }
    /**
     * Sets the parent relationship
     * @param parent The parent of `builder`.
     * @returns A resource builder.
     */
    withParentRelationship(parent) {
        return new ExternalServiceResourcePromiseImpl(this._withParentRelationshipInternal(parent), this._client);
    }
    /** @internal */
    async _withChildRelationshipInternal(child) {
        child = isPromiseLike(child) ? await child : child;
        const rpcArgs = { builder: this._handle, child };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderChildRelationship', rpcArgs);
        return new ExternalServiceResourceImpl(result, this._client);
    }
    /**
     * Sets a child relationship
     * @param child The child of `builder`.
     * @returns A resource builder.
     */
    withChildRelationship(child) {
        return new ExternalServiceResourcePromiseImpl(this._withChildRelationshipInternal(child), this._client);
    }
    /** @internal */
    async _withIconNameInternal(iconName, iconVariant) {
        const rpcArgs = { builder: this._handle, iconName };
        if (iconVariant !== undefined)
            rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability('Aspire.Hosting/withIconName', rpcArgs);
        return new ExternalServiceResourceImpl(result, this._client);
    }
    /**
     * Specifies the icon to use when displaying the resource in the dashboard.
     * @param iconName The name of the FluentUI icon to use. See https://aka.ms/fluentui-system-icons for available icons.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withIconName(iconName, options) {
        const iconVariant = options?.iconVariant;
        return new ExternalServiceResourcePromiseImpl(this._withIconNameInternal(iconName, iconVariant), this._client);
    }
    /** @internal */
    async _excludeFromMcpInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromMcp', rpcArgs);
        return new ExternalServiceResourceImpl(result, this._client);
    }
    /**
     * Exclude the resource from MCP operations using the Aspire MCP server. The resource is excluded from results that return resources, console logs and telemetry.
     * @returns The resource builder.
     */
    excludeFromMcp() {
        return new ExternalServiceResourcePromiseImpl(this._excludeFromMcpInternal(), this._client);
    }
    /** @internal */
    async _withHiddenInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHidden', rpcArgs);
        return new ExternalServiceResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists
     *
     * Use this method to hide resources that are implementation details and should never be displayed by default.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @returns The resource builder.
     */
    withHidden() {
        return new ExternalServiceResourcePromiseImpl(this._withHiddenInternal(), this._client);
    }
    /** @internal */
    async _withHiddenOnCompletionInternal(exitCode, exitCodes) {
        const rpcArgs = { builder: this._handle };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        if (exitCodes !== undefined)
            rpcArgs.exitCodes = exitCodes;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHiddenOnCompletion', rpcArgs);
        return new ExternalServiceResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists after successful completion
     *
     * This method is useful for one-off resources such as setup scripts, migrations, or build steps that should remain visible while running
     * and then be hidden after successful completion.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHiddenOnCompletion(options) {
        const exitCode = options?.exitCode;
        const exitCodes = options?.exitCodes;
        return new ExternalServiceResourcePromiseImpl(this._withHiddenOnCompletionInternal(exitCode, exitCodes), this._client);
    }
    /** @internal */
    async _withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new PipelineStepContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined)
            rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined)
            rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined)
            rpcArgs.tags = tags;
        if (description !== undefined)
            rpcArgs.description = description;
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineStepFactory', rpcArgs);
        return new ExternalServiceResourceImpl(result, this._client);
    }
    /**
     * Adds a pipeline step to the resource that will be executed during deployment.
     * @param stepName The unique name of the pipeline step.
     * @param callback The callback to execute when the step runs.
     * @param options Additional options.
     * @returns The resource builder for chaining.
     */
    withPipelineStepFactory(stepName, callback, options) {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new ExternalServiceResourcePromiseImpl(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description), this._client);
    }
    /** @internal */
    async _withPipelineConfigurationInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new PipelineConfigurationContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineConfiguration', rpcArgs);
        return new ExternalServiceResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to be executed during the pipeline configuration phase, allowing modification of step dependencies and relationships.
     * @param callback The callback function to execute during the configuration phase.
     * @returns The resource builder for chaining.
     */
    withPipelineConfiguration(callback) {
        return new ExternalServiceResourcePromiseImpl(this._withPipelineConfigurationInternal(callback), this._client);
    }
    /**
     * Gets the name of the resource from a builder.
     *
     * Why this wrapper exists: This capability accesses a nested property
     * (`resource.Resource.Name`) which requires a wrapper method. There is no single
     * .NET method that returns just the resource name that could be annotated directly.
     * @returns The resource name.
     */
    async getResourceName() {
        const rpcArgs = { resource: this._handle };
        return await this._client.invokeCapability('Aspire.Hosting/getResourceName', rpcArgs);
    }
    /** @internal */
    async _onBeforeResourceStartedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new BeforeResourceStartedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onBeforeResourceStarted', rpcArgs);
        return new ExternalServiceResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the BeforeResourceStarted event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onBeforeResourceStarted(callback) {
        return new ExternalServiceResourcePromiseImpl(this._onBeforeResourceStartedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceStoppedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceStoppedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceStopped', rpcArgs);
        return new ExternalServiceResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceStopped event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceStopped(callback) {
        return new ExternalServiceResourcePromiseImpl(this._onResourceStoppedInternal(callback), this._client);
    }
    /** @internal */
    async _onInitializeResourceInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new InitializeResourceEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onInitializeResource', rpcArgs);
        return new ExternalServiceResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the InitializeResource event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onInitializeResource(callback) {
        return new ExternalServiceResourcePromiseImpl(this._onInitializeResourceInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceReadyInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceReadyEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceReady', rpcArgs);
        return new ExternalServiceResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceReady event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceReady(callback) {
        return new ExternalServiceResourcePromiseImpl(this._onResourceReadyInternal(callback), this._client);
    }
    /**
     * Creates an execution configuration builder for the specified resource.
     * @returns The execution configuration builder.
     */
    createExecutionConfiguration() {
        const promise = (async () => {
            const rpcArgs = { resource: this._handle };
            const handle = await this._client.invokeCapability('Aspire.Hosting/createExecutionConfiguration', rpcArgs);
            return new ExecutionConfigurationBuilderImpl(handle, this._client);
        })();
        return new ExecutionConfigurationBuilderPromiseImpl(promise, this._client);
    }
}
/**
 * Thenable wrapper for ExternalServiceResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
class ExternalServiceResourcePromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    withContainerRegistry(registry) {
        return new ExternalServiceResourcePromiseImpl(this._promise.then(obj => obj.withContainerRegistry(registry)), this._client);
    }
    withDockerfileBaseImage(options) {
        return new ExternalServiceResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileBaseImage(options)), this._client);
    }
    withHttpHealthCheck(options) {
        return new ExternalServiceResourcePromiseImpl(this._promise.then(obj => obj.withHttpHealthCheck(options)), this._client);
    }
    withRequiredCommand(command, options) {
        return new ExternalServiceResourcePromiseImpl(this._promise.then(obj => obj.withRequiredCommand(command, options)), this._client);
    }
    withSessionLifetime() {
        return new ExternalServiceResourcePromiseImpl(this._promise.then(obj => obj.withSessionLifetime()), this._client);
    }
    withPersistentLifetime() {
        return new ExternalServiceResourcePromiseImpl(this._promise.then(obj => obj.withPersistentLifetime()), this._client);
    }
    withLifetimeOf(sourceBuilder) {
        return new ExternalServiceResourcePromiseImpl(this._promise.then(obj => obj.withLifetimeOf(sourceBuilder)), this._client);
    }
    withParentProcessLifetime(parentProcessId) {
        return new ExternalServiceResourcePromiseImpl(this._promise.then(obj => obj.withParentProcessLifetime(parentProcessId)), this._client);
    }
    withUrls(callback) {
        return new ExternalServiceResourcePromiseImpl(this._promise.then(obj => obj.withUrls(callback)), this._client);
    }
    withUrl(url, options) {
        return new ExternalServiceResourcePromiseImpl(this._promise.then(obj => obj.withUrl(url, options)), this._client);
    }
    withUrlForEndpoint(endpointName, callback) {
        return new ExternalServiceResourcePromiseImpl(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)), this._client);
    }
    excludeFromManifest() {
        return new ExternalServiceResourcePromiseImpl(this._promise.then(obj => obj.excludeFromManifest()), this._client);
    }
    withExplicitStart() {
        return new ExternalServiceResourcePromiseImpl(this._promise.then(obj => obj.withExplicitStart()), this._client);
    }
    withHealthCheck(key) {
        return new ExternalServiceResourcePromiseImpl(this._promise.then(obj => obj.withHealthCheck(key)), this._client);
    }
    withCommand(name, displayName, executeCommand, options) {
        return new ExternalServiceResourcePromiseImpl(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)), this._client);
    }
    withProcessCommand(commandName, displayName, options) {
        return new ExternalServiceResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommand(commandName, displayName, options)), this._client);
    }
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new ExternalServiceResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommandFactory(commandName, displayName, createProcessSpec, options)), this._client);
    }
    withRelationship(resourceBuilder, type) {
        return new ExternalServiceResourcePromiseImpl(this._promise.then(obj => obj.withRelationship(resourceBuilder, type)), this._client);
    }
    withParentRelationship(parent) {
        return new ExternalServiceResourcePromiseImpl(this._promise.then(obj => obj.withParentRelationship(parent)), this._client);
    }
    withChildRelationship(child) {
        return new ExternalServiceResourcePromiseImpl(this._promise.then(obj => obj.withChildRelationship(child)), this._client);
    }
    withIconName(iconName, options) {
        return new ExternalServiceResourcePromiseImpl(this._promise.then(obj => obj.withIconName(iconName, options)), this._client);
    }
    excludeFromMcp() {
        return new ExternalServiceResourcePromiseImpl(this._promise.then(obj => obj.excludeFromMcp()), this._client);
    }
    withHidden() {
        return new ExternalServiceResourcePromiseImpl(this._promise.then(obj => obj.withHidden()), this._client);
    }
    withHiddenOnCompletion(options) {
        return new ExternalServiceResourcePromiseImpl(this._promise.then(obj => obj.withHiddenOnCompletion(options)), this._client);
    }
    withPipelineStepFactory(stepName, callback, options) {
        return new ExternalServiceResourcePromiseImpl(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)), this._client);
    }
    withPipelineConfiguration(callback) {
        return new ExternalServiceResourcePromiseImpl(this._promise.then(obj => obj.withPipelineConfiguration(callback)), this._client);
    }
    getResourceName() {
        return this._promise.then(obj => obj.getResourceName());
    }
    onBeforeResourceStarted(callback) {
        return new ExternalServiceResourcePromiseImpl(this._promise.then(obj => obj.onBeforeResourceStarted(callback)), this._client);
    }
    onResourceStopped(callback) {
        return new ExternalServiceResourcePromiseImpl(this._promise.then(obj => obj.onResourceStopped(callback)), this._client);
    }
    onInitializeResource(callback) {
        return new ExternalServiceResourcePromiseImpl(this._promise.then(obj => obj.onInitializeResource(callback)), this._client);
    }
    onResourceReady(callback) {
        return new ExternalServiceResourcePromiseImpl(this._promise.then(obj => obj.onResourceReady(callback)), this._client);
    }
    createExecutionConfiguration() {
        return new ExecutionConfigurationBuilderPromiseImpl(this._promise.then(obj => obj.createExecutionConfiguration()), this._client);
    }
}
// ============================================================================
// JavaScriptAppResourceImpl
// ============================================================================
/** A resource that represents a JavaScript application. */
class JavaScriptAppResourceImpl extends ResourceBuilderBase {
    constructor(handle, client) {
        super(handle, client);
    }
    async command() {
        return await this._client.invokeCapability('Aspire.Hosting.JavaScript/JavaScriptAppResource.command', { context: this._handle });
    }
    async workingDirectory() {
        return await this._client.invokeCapability('Aspire.Hosting.JavaScript/JavaScriptAppResource.workingDirectory', { context: this._handle });
    }
    async name() {
        return await this._client.invokeCapability('Aspire.Hosting.JavaScript/JavaScriptAppResource.name', { context: this._handle });
    }
    /** @internal */
    async _withContainerRegistryInternal(registry) {
        registry = isPromiseLike(registry) ? await registry : registry;
        const rpcArgs = { builder: this._handle, registry };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerRegistry', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Configures the resource to use the specified container registry for container image operations.
     *
     * This method adds a `ContainerRegistryReferenceAnnotation` to the resource,
     * indicating that the resource should use the specified container registry for container image operations.
     * @param registry The container registry resource builder.
     * @returns The resource builder for chaining.
     */
    withContainerRegistry(registry) {
        return new JavaScriptAppResourcePromiseImpl(this._withContainerRegistryInternal(registry), this._client);
    }
    /** @internal */
    async _withDockerfileBaseImageInternal(buildImage, runtimeImage) {
        const rpcArgs = { builder: this._handle };
        if (buildImage !== undefined)
            rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined)
            rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileBaseImage', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Configures custom base images for generated Dockerfiles.
     *
     * This extension method allows customization of the base images used in generated Dockerfiles.
     * For multi-stage Dockerfiles (e.g., Python with UV), you can specify separate build and runtime images.
     * Specify custom base images for a Python application:
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddPythonApp("myapp", "path/to/app", "main.py")
     * .WithDockerfileBaseImage(
     * buildImage: "ghcr.io/astral-sh/uv:python3.12-bookworm-slim",
     * runtimeImage: "python:3.12-slim-bookworm");
     * builder.Build().Run();
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileBaseImage(options) {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new JavaScriptAppResourcePromiseImpl(this._withDockerfileBaseImageInternal(buildImage, runtimeImage), this._client);
    }
    /** @internal */
    async _publishAsDockerFileInternal(configure) {
        const configureId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new ContainerResourceImpl(objHandle, this._client);
            await configure(obj);
        });
        const rpcArgs = { builder: this._handle, configure: configureId };
        const result = await this._client.invokeCapability('Aspire.Hosting/publishAsDockerFile', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Publishes an executable as a Docker file
     *
     * When the executable resource is converted to a container resource, the arguments to the executable
     * are not used. This is because arguments to the executable often contain physical paths that are not valid
     * in the container. The container can be set up with the correct arguments using the `configure` action.
     * @param configure Optional action to configure the container resource
     * @returns The resource builder.
     */
    publishAsDockerFile(configure) {
        return new JavaScriptAppResourcePromiseImpl(this._publishAsDockerFileInternal(configure), this._client);
    }
    /** @internal */
    async _withExecutableCommandInternal(command) {
        const rpcArgs = { builder: this._handle, command };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExecutableCommand', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Sets the command for the executable resource.
     * @param command Command.
     * @returns The resource builder.
     */
    withExecutableCommand(command) {
        return new JavaScriptAppResourcePromiseImpl(this._withExecutableCommandInternal(command), this._client);
    }
    /** @internal */
    async _withWorkingDirectoryInternal(workingDirectory) {
        const rpcArgs = { builder: this._handle, workingDirectory };
        const result = await this._client.invokeCapability('Aspire.Hosting/withWorkingDirectory', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Sets the working directory for the executable resource.
     * @param workingDirectory Working directory.
     * @returns The resource builder.
     */
    withWorkingDirectory(workingDirectory) {
        return new JavaScriptAppResourcePromiseImpl(this._withWorkingDirectoryInternal(workingDirectory), this._client);
    }
    /** @internal */
    async _withMcpServerInternal(path, endpointName) {
        const rpcArgs = { builder: this._handle };
        if (path !== undefined)
            rpcArgs.path = path;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withMcpServer', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Marks the resource as hosting a Model Context Protocol (MCP) server on the specified endpoint.
     *
     * This method adds an `McpServerEndpointAnnotation` to the resource, enabling the Aspire tooling
     * to discover and proxy the MCP server exposed by the resource.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withMcpServer(options) {
        const path = options?.path;
        const endpointName = options?.endpointName;
        return new JavaScriptAppResourcePromiseImpl(this._withMcpServerInternal(path, endpointName), this._client);
    }
    /** @internal */
    async _withOtlpExporterInternal(protocol) {
        const rpcArgs = { builder: this._handle };
        if (protocol !== undefined)
            rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability('Aspire.Hosting/withOtlpExporter', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Configures OTLP telemetry export
     * @param options Additional options.
     */
    withOtlpExporter(options) {
        const protocol = options?.protocol;
        return new JavaScriptAppResourcePromiseImpl(this._withOtlpExporterInternal(protocol), this._client);
    }
    /** @internal */
    async _withRequiredCommandInternal(command, helpLink) {
        const rpcArgs = { builder: this._handle, command };
        if (helpLink !== undefined)
            rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability('Aspire.Hosting/withRequiredCommand', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Declares that a resource requires a specific command/executable to be available on the local machine PATH before it can start.
     *
     * The command is considered valid if either:
     * 1. It is an absolute or relative path (contains a directory separator) that points to an existing file, or
     * 2. It is discoverable on the current process PATH (respecting PATHEXT on Windows).
     * If the command is not found, a warning message will be logged but the resource will be allowed to attempt to start.
     * @param command The command string (file name or path) that should be validated.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withRequiredCommand(command, options) {
        const helpLink = options?.helpLink;
        return new JavaScriptAppResourcePromiseImpl(this._withRequiredCommandInternal(command, helpLink), this._client);
    }
    /** @internal */
    async _withSessionLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withSessionLifetime', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a session lifetime.
     * @returns The resource builder.
     */
    withSessionLifetime() {
        return new JavaScriptAppResourcePromiseImpl(this._withSessionLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withPersistentLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPersistentLifetime', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime.
     * @returns The resource builder.
     */
    withPersistentLifetime() {
        return new JavaScriptAppResourcePromiseImpl(this._withPersistentLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withLifetimeOfInternal(sourceBuilder) {
        sourceBuilder = isPromiseLike(sourceBuilder) ? await sourceBuilder : sourceBuilder;
        const rpcArgs = { builder: this._handle, sourceBuilder };
        const result = await this._client.invokeCapability('Aspire.Hosting/withLifetimeOf', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to match the lifetime of another resource.
     *
     * The resource lifetime is evaluated from `sourceBuilder` when the application model is prepared, so later lifetime
     * changes to the source resource are reflected by this resource.
     * @param sourceBuilder The resource builder whose lifetime should be used.
     * @returns The resource builder.
     */
    withLifetimeOf(sourceBuilder) {
        return new JavaScriptAppResourcePromiseImpl(this._withLifetimeOfInternal(sourceBuilder), this._client);
    }
    /** @internal */
    async _withParentProcessLifetimeInternal(parentProcessId) {
        const rpcArgs = { builder: this._handle, parentProcessId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withParentProcessLifetime', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime that ends when a parent process exits.
     * @param parentProcessId The ID of the parent process to monitor.
     * @returns The resource builder.
     */
    withParentProcessLifetime(parentProcessId) {
        return new JavaScriptAppResourcePromiseImpl(this._withParentProcessLifetimeInternal(parentProcessId), this._client);
    }
    /** @internal */
    async _withEnvironmentInternal(name, value) {
        value = isPromiseLike(value) ? await value : value;
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEnvironment', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /** Sets an environment variable */
    withEnvironment(name, value) {
        return new JavaScriptAppResourcePromiseImpl(this._withEnvironmentInternal(name, value), this._client);
    }
    /** @internal */
    async _withEnvironmentCallbackInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new EnvironmentCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEnvironmentCallback', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Allows for the population of environment variables on a resource.
     * @param callback A callback that allows for deferred execution for computing many environment variables. This runs after resources have been allocated by the orchestrator and allows access to other resources to resolve computed data, e.g. connection strings, ports.
     * @returns The resource builder.
     */
    withEnvironmentCallback(callback) {
        return new JavaScriptAppResourcePromiseImpl(this._withEnvironmentCallbackInternal(callback), this._client);
    }
    /** @internal */
    async _withArgsInternal(args) {
        const rpcArgs = { builder: this._handle, args };
        const result = await this._client.invokeCapability('Aspire.Hosting/withArgs', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Adds arguments to be passed to a resource that supports arguments when it is launched.
     * @param args The arguments to be passed to the resource when it is started.
     * @returns The resource builder.
     */
    withArgs(args) {
        return new JavaScriptAppResourcePromiseImpl(this._withArgsInternal(args), this._client);
    }
    /** @internal */
    async _withArgsCallbackInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new CommandLineArgsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withArgsCallback', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Adds a callback to be executed with a list of command-line arguments when a resource is started.
     * @param callback A callback that allows for deferred execution for computing arguments. This runs after resources have been allocated by the orchestrator and allows access to other resources to resolve computed data, e.g. connection strings, ports.
     * @returns The resource builder.
     */
    withArgsCallback(callback) {
        return new JavaScriptAppResourcePromiseImpl(this._withArgsCallbackInternal(callback), this._client);
    }
    /** @internal */
    async _withReferenceEnvironmentInternal(options) {
        const rpcArgs = { builder: this._handle, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withReferenceEnvironment', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Configures how information is injected into environment variables when the resource references other resources.
     * @param options Options controlling which reference information is emitted.
     * @returns The resource builder.
     */
    withReferenceEnvironment(options) {
        return new JavaScriptAppResourcePromiseImpl(this._withReferenceEnvironmentInternal(options), this._client);
    }
    /** @internal */
    async _withReferenceInternal(source, connectionName, optional, name) {
        source = isPromiseLike(source) ? await source : source;
        const rpcArgs = { builder: this._handle, source };
        if (connectionName !== undefined)
            rpcArgs.connectionName = connectionName;
        if (optional !== undefined)
            rpcArgs.optional = optional;
        if (name !== undefined)
            rpcArgs.name = name;
        const result = await this._client.invokeCapability('Aspire.Hosting/withReference', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Adds a reference to another resource
     * @param options Additional options.
     */
    withReference(source, options) {
        const connectionName = options?.connectionName;
        const optional = options?.optional;
        const name = options?.name;
        return new JavaScriptAppResourcePromiseImpl(this._withReferenceInternal(source, connectionName, optional, name), this._client);
    }
    /** @internal */
    async _withEndpointCallbackInternal(endpointName, callback, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpointCallback', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Updates a named endpoint via callback
     * @param options Additional options.
     */
    withEndpointCallback(endpointName, callback, options) {
        const createIfNotExists = options?.createIfNotExists;
        return new JavaScriptAppResourcePromiseImpl(this._withEndpointCallbackInternal(endpointName, callback, createIfNotExists), this._client);
    }
    /** @internal */
    async _withHttpEndpointCallbackInternal(callback, name, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        if (name !== undefined)
            rpcArgs.name = name;
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpEndpointCallback', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Updates an HTTP endpoint via callback
     * @param options Additional options.
     */
    withHttpEndpointCallback(callback, options) {
        const name = options?.name;
        const createIfNotExists = options?.createIfNotExists;
        return new JavaScriptAppResourcePromiseImpl(this._withHttpEndpointCallbackInternal(callback, name, createIfNotExists), this._client);
    }
    /** @internal */
    async _withHttpsEndpointCallbackInternal(callback, name, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        if (name !== undefined)
            rpcArgs.name = name;
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpsEndpointCallback', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Updates an HTTPS endpoint via callback
     * @param options Additional options.
     */
    withHttpsEndpointCallback(callback, options) {
        const name = options?.name;
        const createIfNotExists = options?.createIfNotExists;
        return new JavaScriptAppResourcePromiseImpl(this._withHttpsEndpointCallbackInternal(callback, name, createIfNotExists), this._client);
    }
    /** @internal */
    async _withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (scheme !== undefined)
            rpcArgs.scheme = scheme;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        if (isExternal !== undefined)
            rpcArgs.isExternal = isExternal;
        if (protocol !== undefined)
            rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpoint', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Adds a network endpoint
     * @param options Additional options.
     * @returns The resource builder.
     */
    withEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const scheme = options?.scheme;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        const isExternal = options?.isExternal;
        const protocol = options?.protocol;
        return new JavaScriptAppResourcePromiseImpl(this._withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol), this._client);
    }
    /** @internal */
    async _withEndpointProxySupportInternal(proxyEnabled) {
        const rpcArgs = { builder: this._handle, proxyEnabled };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpointProxySupport', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Set whether a resource can use proxied endpoints or whether they should be disabled for all endpoints belonging to the resource. If set to `false`, endpoints belonging to the resource will ignore the configured proxy settings and run proxy-less.
     *
     * This method is intended to support scenarios with persistent lifetime resources where it is desirable for the resource to be accessible over the same
     * port whether the Aspire application is running or not. Proxied endpoints bind ports that are only accessible while the Aspire application is running.
     * The user needs to be careful to ensure that endpoints are using unique ports when disabling proxy support as by default for proxy-less
     * endpoints, Aspire will allocate the target port as the host port, which will increase the chance of port conflicts.
     * @param proxyEnabled Should endpoints for the resource support using a proxy?
     * @returns The resource builder.
     */
    withEndpointProxySupport(proxyEnabled) {
        return new JavaScriptAppResourcePromiseImpl(this._withEndpointProxySupportInternal(proxyEnabled), this._client);
    }
    /** @internal */
    async _withHttpEndpointInternal(port, targetPort, name, env, isProxied) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpEndpoint', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP endpoint
     *
     * If an endpoint with the same name already exists on the resource, the existing endpoint is updated
     * with any non-null parameter values. Parameters left as `null` will not modify the existing endpoint's values.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new JavaScriptAppResourcePromiseImpl(this._withHttpEndpointInternal(port, targetPort, name, env, isProxied), this._client);
    }
    /** @internal */
    async _withHttpsEndpointInternal(port, targetPort, name, env, isProxied) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpsEndpoint', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTPS endpoint
     *
     * If an endpoint with the same name already exists on the resource, the existing endpoint is updated
     * with any non-null parameter values. Parameters left as `null` will not modify the existing endpoint's values.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpsEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new JavaScriptAppResourcePromiseImpl(this._withHttpsEndpointInternal(port, targetPort, name, env, isProxied), this._client);
    }
    /** @internal */
    async _withExternalHttpEndpointsInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExternalHttpEndpoints', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Marks existing http or https endpoints on a resource as external.
     * @returns The resource builder.
     */
    withExternalHttpEndpoints() {
        return new JavaScriptAppResourcePromiseImpl(this._withExternalHttpEndpointsInternal(), this._client);
    }
    /**
     * Gets an endpoint reference
     * @param name The name of the endpoint.
     * @returns An `EndpointReference` that can be used to resolve the address of the endpoint after resource allocation has occurred.
     */
    getEndpoint(name) {
        const promise = (async () => {
            const rpcArgs = { builder: this._handle, name };
            const handle = await this._client.invokeCapability('Aspire.Hosting/getEndpoint', rpcArgs);
            return new EndpointReferenceImpl(handle, this._client);
        })();
        return new EndpointReferencePromiseImpl(promise, this._client);
    }
    /** @internal */
    async _asHttp2ServiceInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/asHttp2Service', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to mark all endpoints' transport as HTTP/2. This is useful for HTTP/2 services that need prior knowledge.
     * @returns The resource builder.
     */
    asHttp2Service() {
        return new JavaScriptAppResourcePromiseImpl(this._asHttp2ServiceInternal(), this._client);
    }
    /** @internal */
    async _withUrlsInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new ResourceUrlsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrls', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to customize the URLs displayed for the resource.
     * @param callback The callback that will customize URLs for the resource.
     * @returns The resource builder.
     */
    withUrls(callback) {
        return new JavaScriptAppResourcePromiseImpl(this._withUrlsInternal(callback), this._client);
    }
    /** @internal */
    async _withUrlInternal(url, displayText) {
        const rpcArgs = { builder: this._handle, url };
        if (displayText !== undefined)
            rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrl', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Adds or modifies displayed URLs
     * @param options Additional options.
     */
    withUrl(url, options) {
        const displayText = options?.displayText;
        return new JavaScriptAppResourcePromiseImpl(this._withUrlInternal(url, displayText), this._client);
    }
    /** @internal */
    async _withUrlForEndpointInternal(endpointName, callback) {
        const callbackId = registerCallback(async (objData) => {
            const obj = wrapIfHandle(objData);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrlForEndpoint', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to update the URL displayed for the endpoint with the specified name.
     * @param endpointName The name of the endpoint to customize the URL for.
     * @param callback The callback that will customize the URL.
     * @returns The resource builder.
     */
    withUrlForEndpoint(endpointName, callback) {
        return new JavaScriptAppResourcePromiseImpl(this._withUrlForEndpointInternal(endpointName, callback), this._client);
    }
    /** @internal */
    async _withContainerFilesSourceInternal(sourcePath) {
        const rpcArgs = { builder: this._handle, sourcePath };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerFilesSource', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Adds a container files source annotation to the resource being built, specifying the path to the container files source.
     * @param sourcePath The path to the container files source to associate with the resource. Cannot be null.
     * @returns The resource builder instance with the container files source annotation applied.
     */
    withContainerFilesSource(sourcePath) {
        return new JavaScriptAppResourcePromiseImpl(this._withContainerFilesSourceInternal(sourcePath), this._client);
    }
    /** @internal */
    async _clearContainerFilesSourcesInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/clearContainerFilesSources', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Removes any container files source annotation from the resource being built.
     * @returns The resource builder instance with the container files source annotation applied.
     */
    clearContainerFilesSources() {
        return new JavaScriptAppResourcePromiseImpl(this._clearContainerFilesSourcesInternal(), this._client);
    }
    /** @internal */
    async _excludeFromManifestInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromManifest', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Excludes a resource from being published to the manifest.
     * @returns The resource builder.
     */
    excludeFromManifest() {
        return new JavaScriptAppResourcePromiseImpl(this._excludeFromManifestInternal(), this._client);
    }
    /** @internal */
    async _waitForInternal(dependency, waitBehavior) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (waitBehavior !== undefined)
            rpcArgs.waitBehavior = waitBehavior;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitFor', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Waits for another resource to be ready
     * @param options Additional options.
     */
    waitFor(dependency, options) {
        const waitBehavior = options?.waitBehavior;
        return new JavaScriptAppResourcePromiseImpl(this._waitForInternal(dependency, waitBehavior), this._client);
    }
    /** @internal */
    async _waitForStartInternal(dependency, waitBehavior) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (waitBehavior !== undefined)
            rpcArgs.waitBehavior = waitBehavior;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitForStart', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Waits for another resource to start
     * @param options Additional options.
     */
    waitForStart(dependency, options) {
        const waitBehavior = options?.waitBehavior;
        return new JavaScriptAppResourcePromiseImpl(this._waitForStartInternal(dependency, waitBehavior), this._client);
    }
    /** @internal */
    async _withExplicitStartInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExplicitStart', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Prevents resource from starting automatically
     * @returns The resource builder.
     */
    withExplicitStart() {
        return new JavaScriptAppResourcePromiseImpl(this._withExplicitStartInternal(), this._client);
    }
    /** @internal */
    async _waitForCompletionInternal(dependency, exitCode) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitForResourceCompletion', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Waits for the dependency resource to enter the Exited or Finished state before starting the resource.
     * @param dependency The resource builder for the dependency resource.
     * @param options Additional options.
     * @returns The resource builder.
     */
    waitForCompletion(dependency, options) {
        const exitCode = options?.exitCode;
        return new JavaScriptAppResourcePromiseImpl(this._waitForCompletionInternal(dependency, exitCode), this._client);
    }
    /** @internal */
    async _withHealthCheckInternal(key) {
        const rpcArgs = { builder: this._handle, key };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHealthCheck', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Adds a health check by key
     * @param key The key for the health check.
     * @returns The resource builder.
     */
    withHealthCheck(key) {
        return new JavaScriptAppResourcePromiseImpl(this._withHealthCheckInternal(key), this._client);
    }
    /** @internal */
    async _withHttpHealthCheckInternal(path, statusCode, endpointName) {
        const rpcArgs = { builder: this._handle };
        if (path !== undefined)
            rpcArgs.path = path;
        if (statusCode !== undefined)
            rpcArgs.statusCode = statusCode;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpHealthCheck', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Adds a health check to the resource which is mapped to a specific endpoint.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpHealthCheck(options) {
        const path = options?.path;
        const statusCode = options?.statusCode;
        const endpointName = options?.endpointName;
        return new JavaScriptAppResourcePromiseImpl(this._withHttpHealthCheckInternal(path, statusCode, endpointName), this._client);
    }
    /** @internal */
    async _withCommandInternal(name, displayName, executeCommand, commandOptions) {
        const executeCommandId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined)
            rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability('Aspire.Hosting/withCommand', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Adds a resource command
     *
     * The `WithCommand` method is used to add commands to the resource. Commands are displayed in the dashboard
     * and can be executed by a user using the dashboard UI.
     * When a command is executed, the `executeCommand` callback is called and is run inside the Aspire host.
     * @param name The name of the command. The name uniquely identifies the command.
     * @param displayName The display name visible in UI.
     * @param executeCommand A callback that is executed when the command is executed. The callback is run inside the Aspire host. The callback result is used to indicate success or failure in the UI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withCommand(name, displayName, executeCommand, options) {
        const commandOptions = options?.commandOptions;
        return new JavaScriptAppResourcePromiseImpl(this._withCommandInternal(name, displayName, executeCommand, commandOptions), this._client);
    }
    /** @internal */
    async _withProcessCommandInternal(commandName, displayName, options) {
        const rpcArgs = { builder: this._handle, commandName, displayName, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommand', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /** Adds a command to the resource that starts a local process when invoked. */
    withProcessCommand(commandName, displayName, options) {
        return new JavaScriptAppResourcePromiseImpl(this._withProcessCommandInternal(commandName, displayName, options), this._client);
    }
    /** @internal */
    async _withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options) {
        const createProcessSpecId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await createProcessSpec(arg);
        });
        const rpcArgs = { builder: this._handle, commandName, displayName, createProcessSpec: createProcessSpecId };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommandFactory', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Adds a command to the resource that starts a local process created by a callback when invoked.
     * @param options Additional options.
     */
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new JavaScriptAppResourcePromiseImpl(this._withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options), this._client);
    }
    /** @internal */
    async _withHttpCommandInternal(path, displayName, options) {
        const rpcArgs = { builder: this._handle, path, displayName };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpCommand', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP resource command
     * @param options Additional options.
     */
    withHttpCommand(path, displayName, options) {
        return new JavaScriptAppResourcePromiseImpl(this._withHttpCommandInternal(path, displayName, options), this._client);
    }
    /** @internal */
    async _withDeveloperCertificateTrustInternal(trust) {
        const rpcArgs = { builder: this._handle, trust };
        const result = await this._client.invokeCapability('Aspire.Hosting/withDeveloperCertificateTrust', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Indicates whether developer certificates should be treated as trusted certificate authorities for the resource at run time. Currently this indicates trust for the ASP.NET Core developer certificate. The developer certificate will only be trusted when running in local development scenarios; in publish mode resources will use their default certificate trust.
     *
     * Disable trust for app host managed developer certificate(s) for a container resource.
     * ```
     * var container = builder.AddContainer("my-service", "my-service:latest")
     * .WithDeveloperCertificateTrust(false);
     * ```
     * Disable automatic trust for app host managed developer certificate(s), but explicitly enable it for a specific resource.
     * ```
     * var builder = DistributedApplication.CreateBuilder(new DistributedApplicationOptions()
     * {
     * Args = args,
     * TrustDeveloperCertificate = false,
     * });
     * var project = builder.AddProject<MyService>("my-service")
     * .WithDeveloperCertificateTrust(true);
     * ```
     * @param trust Indicates whether the developer certificate should be treated as trusted.
     * @returns The resource builder.
     */
    withDeveloperCertificateTrust(trust) {
        return new JavaScriptAppResourcePromiseImpl(this._withDeveloperCertificateTrustInternal(trust), this._client);
    }
    /** @internal */
    async _withCertificateTrustScopeInternal(scope) {
        const rpcArgs = { builder: this._handle, scope };
        const result = await this._client.invokeCapability('Aspire.Hosting/withCertificateTrustScope', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Sets the certificate trust scope
     *
     * The default scope if not overridden is `Append` which means that custom certificate
     * authorities should be appended to the default trusted certificate authorities for the resource. Setting the scope to
     * `Override` indicates the set of certificates in referenced
     * `CertificateAuthorityCollection` (and optionally Aspire developer certificiates) should be used as the
     * exclusive source of trust for a resource.
     * In all cases, this is a best effort implementation as not all resources support full customization of certificate
     * trust.
     * Set the scope for custom certificate authorities to override the default trusted certificate authorities for a container resource.
     * ```
     * var caCollection = builder.AddCertificateAuthorityCollection("my-cas")
     * .WithCertificate(new X509Certificate2("my-ca.pem"));
     * var container = builder.AddContainer("my-service", "my-service:latest")
     * .WithCertificateAuthorityCollection(caCollection)
     * .WithCertificateTrustScope(CertificateTrustScope.Override);
     * ```
     * @param scope The scope to apply to custom certificate authorities associated with the resource.
     * @returns The resource builder.
     */
    withCertificateTrustScope(scope) {
        return new JavaScriptAppResourcePromiseImpl(this._withCertificateTrustScopeInternal(scope), this._client);
    }
    /** @internal */
    async _withHttpsDeveloperCertificateInternal(password) {
        password = isPromiseLike(password) ? await password : password;
        const rpcArgs = { builder: this._handle };
        if (password !== undefined)
            rpcArgs.password = password;
        const result = await this._client.invokeCapability('Aspire.Hosting/withParameterHttpsDeveloperCertificate', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Indicates that a resource should use the developer certificate key pair for HTTPS endpoints at run time. Currently this indicates use of the ASP.NET Core developer certificate. The developer certificate will only be used when running in local development scenarios; in publish mode resources will use their default certificate configuration.
     *
     * Use the developer certificate for HTTPS/TLS endpoints on a container resource:
     * ```
     * builder.AddContainer("my-service", "my-image")
     * .WithHttpsDeveloperCertificate()
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpsDeveloperCertificate(options) {
        let password = options?.password;
        return new JavaScriptAppResourcePromiseImpl(this._withHttpsDeveloperCertificateInternal(password), this._client);
    }
    /** @internal */
    async _withoutHttpsCertificateInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withoutHttpsCertificate', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Disable HTTPS/TLS server certificate configuration for the resource. No HTTPS/TLS termination configuration will be applied.
     *
     * Disable HTTPS certificate configuration for a Redis resource:
     * ```
     * var redis = builder.AddRedis("cache")
     * .WithoutHttpsCertificate();
     * ```
     * @returns The resource builder.
     */
    withoutHttpsCertificate() {
        return new JavaScriptAppResourcePromiseImpl(this._withoutHttpsCertificateInternal(), this._client);
    }
    /** @internal */
    async _withRelationshipInternal(resourceBuilder, type) {
        resourceBuilder = isPromiseLike(resourceBuilder) ? await resourceBuilder : resourceBuilder;
        const rpcArgs = { builder: this._handle, resourceBuilder, type };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderRelationship', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Adds a relationship to another resource using its builder.
     * @param resourceBuilder The resource builder that the relationship is to.
     * @param type The relationship type.
     * @returns A resource builder.
     */
    withRelationship(resourceBuilder, type) {
        return new JavaScriptAppResourcePromiseImpl(this._withRelationshipInternal(resourceBuilder, type), this._client);
    }
    /** @internal */
    async _withParentRelationshipInternal(parent) {
        parent = isPromiseLike(parent) ? await parent : parent;
        const rpcArgs = { builder: this._handle, parent };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderParentRelationship', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Sets the parent relationship
     * @param parent The parent of `builder`.
     * @returns A resource builder.
     */
    withParentRelationship(parent) {
        return new JavaScriptAppResourcePromiseImpl(this._withParentRelationshipInternal(parent), this._client);
    }
    /** @internal */
    async _withChildRelationshipInternal(child) {
        child = isPromiseLike(child) ? await child : child;
        const rpcArgs = { builder: this._handle, child };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderChildRelationship', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Sets a child relationship
     * @param child The child of `builder`.
     * @returns A resource builder.
     */
    withChildRelationship(child) {
        return new JavaScriptAppResourcePromiseImpl(this._withChildRelationshipInternal(child), this._client);
    }
    /** @internal */
    async _withIconNameInternal(iconName, iconVariant) {
        const rpcArgs = { builder: this._handle, iconName };
        if (iconVariant !== undefined)
            rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability('Aspire.Hosting/withIconName', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Specifies the icon to use when displaying the resource in the dashboard.
     * @param iconName The name of the FluentUI icon to use. See https://aka.ms/fluentui-system-icons for available icons.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withIconName(iconName, options) {
        const iconVariant = options?.iconVariant;
        return new JavaScriptAppResourcePromiseImpl(this._withIconNameInternal(iconName, iconVariant), this._client);
    }
    /** @internal */
    async _withComputeEnvironmentInternal(computeEnvironmentResource) {
        computeEnvironmentResource = isPromiseLike(computeEnvironmentResource) ? await computeEnvironmentResource : computeEnvironmentResource;
        const rpcArgs = { builder: this._handle, computeEnvironmentResource };
        const result = await this._client.invokeCapability('Aspire.Hosting/withComputeEnvironment', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Configures the compute environment for the compute resource.
     *
     * This method allows associating a specific compute environment with the compute resource.
     * @param computeEnvironmentResource The compute environment resource to associate with the compute resource.
     * @returns The resource builder.
     */
    withComputeEnvironment(computeEnvironmentResource) {
        return new JavaScriptAppResourcePromiseImpl(this._withComputeEnvironmentInternal(computeEnvironmentResource), this._client);
    }
    /** @internal */
    async _withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName) {
        const rpcArgs = { builder: this._handle, probeType };
        if (path !== undefined)
            rpcArgs.path = path;
        if (initialDelaySeconds !== undefined)
            rpcArgs.initialDelaySeconds = initialDelaySeconds;
        if (periodSeconds !== undefined)
            rpcArgs.periodSeconds = periodSeconds;
        if (timeoutSeconds !== undefined)
            rpcArgs.timeoutSeconds = timeoutSeconds;
        if (failureThreshold !== undefined)
            rpcArgs.failureThreshold = failureThreshold;
        if (successThreshold !== undefined)
            rpcArgs.successThreshold = successThreshold;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpProbe', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP health probe to the resource
     * @param options Additional options.
     */
    withHttpProbe(probeType, options) {
        const path = options?.path;
        const initialDelaySeconds = options?.initialDelaySeconds;
        const periodSeconds = options?.periodSeconds;
        const timeoutSeconds = options?.timeoutSeconds;
        const failureThreshold = options?.failureThreshold;
        const successThreshold = options?.successThreshold;
        const endpointName = options?.endpointName;
        return new JavaScriptAppResourcePromiseImpl(this._withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName), this._client);
    }
    /** @internal */
    async _excludeFromMcpInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromMcp', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Exclude the resource from MCP operations using the Aspire MCP server. The resource is excluded from results that return resources, console logs and telemetry.
     * @returns The resource builder.
     */
    excludeFromMcp() {
        return new JavaScriptAppResourcePromiseImpl(this._excludeFromMcpInternal(), this._client);
    }
    /** @internal */
    async _withHiddenInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHidden', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists
     *
     * Use this method to hide resources that are implementation details and should never be displayed by default.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @returns The resource builder.
     */
    withHidden() {
        return new JavaScriptAppResourcePromiseImpl(this._withHiddenInternal(), this._client);
    }
    /** @internal */
    async _withHiddenOnCompletionInternal(exitCode, exitCodes) {
        const rpcArgs = { builder: this._handle };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        if (exitCodes !== undefined)
            rpcArgs.exitCodes = exitCodes;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHiddenOnCompletion', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists after successful completion
     *
     * This method is useful for one-off resources such as setup scripts, migrations, or build steps that should remain visible while running
     * and then be hidden after successful completion.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHiddenOnCompletion(options) {
        const exitCode = options?.exitCode;
        const exitCodes = options?.exitCodes;
        return new JavaScriptAppResourcePromiseImpl(this._withHiddenOnCompletionInternal(exitCode, exitCodes), this._client);
    }
    /** @internal */
    async _withImagePushOptionsInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ContainerImagePushOptionsCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImagePushOptions', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Adds an asynchronous callback to configure container image push options for the resource.
     *
     * This method allows customization of how container images are named and tagged when pushed to a registry using an asynchronous callback.
     * Use this overload when the callback needs to perform asynchronous operations such as retrieving configuration values from external sources.
     * The callback receives a `ContainerImagePushOptionsCallbackContext` that provides access to the resource
     * and the `ContainerImagePushOptions` that can be modified.
     * Multiple callbacks can be registered on the same resource, and they will be invoked in the order they were added.
     * @param callback The asynchronous callback to configure push options.
     * @returns The resource builder.
     */
    withImagePushOptions(callback) {
        return new JavaScriptAppResourcePromiseImpl(this._withImagePushOptionsInternal(callback), this._client);
    }
    /** @internal */
    async _withRemoteImageNameInternal(remoteImageName) {
        const rpcArgs = { builder: this._handle, remoteImageName };
        const result = await this._client.invokeCapability('Aspire.Hosting/withRemoteImageName', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Sets the remote image name (without registry endpoint or tag) for container push operations.
     *
     * Use this with `withRemoteImageTag` to fully customize the image reference used for container push operations.
     * @param remoteImageName The remote image name (e.g., "myapp" or "myorg/myapp").
     * @returns The resource builder.
     */
    withRemoteImageName(remoteImageName) {
        return new JavaScriptAppResourcePromiseImpl(this._withRemoteImageNameInternal(remoteImageName), this._client);
    }
    /** @internal */
    async _withRemoteImageTagInternal(remoteImageTag) {
        const rpcArgs = { builder: this._handle, remoteImageTag };
        const result = await this._client.invokeCapability('Aspire.Hosting/withRemoteImageTag', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Sets the remote image tag for container push operations.
     *
     * Use this with `withRemoteImageName` to fully customize the image reference used for container push operations.
     * @param remoteImageTag The remote image tag (e.g., "latest", "v1.0.0").
     * @returns The resource builder.
     */
    withRemoteImageTag(remoteImageTag) {
        return new JavaScriptAppResourcePromiseImpl(this._withRemoteImageTagInternal(remoteImageTag), this._client);
    }
    /** @internal */
    async _withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new PipelineStepContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined)
            rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined)
            rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined)
            rpcArgs.tags = tags;
        if (description !== undefined)
            rpcArgs.description = description;
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineStepFactory', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Adds a pipeline step to the resource that will be executed during deployment.
     * @param stepName The unique name of the pipeline step.
     * @param callback The callback to execute when the step runs.
     * @param options Additional options.
     * @returns The resource builder for chaining.
     */
    withPipelineStepFactory(stepName, callback, options) {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new JavaScriptAppResourcePromiseImpl(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description), this._client);
    }
    /** @internal */
    async _withPipelineConfigurationInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new PipelineConfigurationContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineConfiguration', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to be executed during the pipeline configuration phase, allowing modification of step dependencies and relationships.
     * @param callback The callback function to execute during the configuration phase.
     * @returns The resource builder for chaining.
     */
    withPipelineConfiguration(callback) {
        return new JavaScriptAppResourcePromiseImpl(this._withPipelineConfigurationInternal(callback), this._client);
    }
    /**
     * Gets the name of the resource from a builder.
     *
     * Why this wrapper exists: This capability accesses a nested property
     * (`resource.Resource.Name`) which requires a wrapper method. There is no single
     * .NET method that returns just the resource name that could be annotated directly.
     * @returns The resource name.
     */
    async getResourceName() {
        const rpcArgs = { resource: this._handle };
        return await this._client.invokeCapability('Aspire.Hosting/getResourceName', rpcArgs);
    }
    /** @internal */
    async _onBeforeResourceStartedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new BeforeResourceStartedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onBeforeResourceStarted', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the BeforeResourceStarted event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onBeforeResourceStarted(callback) {
        return new JavaScriptAppResourcePromiseImpl(this._onBeforeResourceStartedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceStoppedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceStoppedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceStopped', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceStopped event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceStopped(callback) {
        return new JavaScriptAppResourcePromiseImpl(this._onResourceStoppedInternal(callback), this._client);
    }
    /** @internal */
    async _onInitializeResourceInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new InitializeResourceEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onInitializeResource', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the InitializeResource event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onInitializeResource(callback) {
        return new JavaScriptAppResourcePromiseImpl(this._onInitializeResourceInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceEndpointsAllocatedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceEndpointsAllocatedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceEndpointsAllocated', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceEndpointsAllocated event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceEndpointsAllocated(callback) {
        return new JavaScriptAppResourcePromiseImpl(this._onResourceEndpointsAllocatedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceReadyInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceReadyEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceReady', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceReady event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceReady(callback) {
        return new JavaScriptAppResourcePromiseImpl(this._onResourceReadyInternal(callback), this._client);
    }
    /**
     * Creates an execution configuration builder for the specified resource.
     * @returns The execution configuration builder.
     */
    createExecutionConfiguration() {
        const promise = (async () => {
            const rpcArgs = { resource: this._handle };
            const handle = await this._client.invokeCapability('Aspire.Hosting/createExecutionConfiguration', rpcArgs);
            return new ExecutionConfigurationBuilderImpl(handle, this._client);
        })();
        return new ExecutionConfigurationBuilderPromiseImpl(promise, this._client);
    }
    /** @internal */
    async _publishAsStaticWebsiteInternal(apiPath, apiTarget, outputPath, stripPrefix, targetEndpointName) {
        apiTarget = isPromiseLike(apiTarget) ? await apiTarget : apiTarget;
        const rpcArgs = { builder: this._handle };
        if (apiPath !== undefined)
            rpcArgs.apiPath = apiPath;
        if (apiTarget !== undefined)
            rpcArgs.apiTarget = apiTarget;
        if (outputPath !== undefined)
            rpcArgs.outputPath = outputPath;
        if (stripPrefix !== undefined)
            rpcArgs.stripPrefix = stripPrefix;
        if (targetEndpointName !== undefined)
            rpcArgs.targetEndpointName = targetEndpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/publishAsStaticWebsite', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Publishes the JavaScript application as a standalone static website using YARP.
     * @param options Additional options.
     */
    publishAsStaticWebsite(options) {
        const apiPath = options?.apiPath;
        let apiTarget = options?.apiTarget;
        const outputPath = options?.outputPath;
        const stripPrefix = options?.stripPrefix;
        const targetEndpointName = options?.targetEndpointName;
        return new JavaScriptAppResourcePromiseImpl(this._publishAsStaticWebsiteInternal(apiPath, apiTarget, outputPath, stripPrefix, targetEndpointName), this._client);
    }
    /** @internal */
    async _publishAsNodeServerInternal(entryPoint, outputPath) {
        const rpcArgs = { builder: this._handle, entryPoint };
        if (outputPath !== undefined)
            rpcArgs.outputPath = outputPath;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/publishAsNodeServer', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Configures the JavaScript application to publish as a standalone Node.js server that runs a built artifact directly.
     *
     * Use this method for frameworks that produce a Node.js server artifact during the build and recommend
     * running that artifact directly in production rather than invoking a package manager script at runtime.
     * The application source is still built using the configured package manager and build script; this method
     * only changes the publish-time runtime container shape.
     * The container files source path is automatically set to `outputPath` so that only
     * the built output directory is copied into the runtime container, not the full application source.
     * @param entryPoint The relative path to the Node.js entry point to execute in the published container after the build completes, such as `.output/server/index.js` or `build/index.js`.
     * @param options Additional options.
     * @returns The updated resource builder.
     */
    publishAsNodeServer(entryPoint, options) {
        const outputPath = options?.outputPath;
        return new JavaScriptAppResourcePromiseImpl(this._publishAsNodeServerInternal(entryPoint, outputPath), this._client);
    }
    /** @internal */
    async _publishAsPackageScriptInternal(scriptName, runScriptArguments) {
        const rpcArgs = { builder: this._handle };
        if (scriptName !== undefined)
            rpcArgs.scriptName = scriptName;
        if (runScriptArguments !== undefined)
            rpcArgs.runScriptArguments = runScriptArguments;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/publishAsPackageScript', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Configures the JavaScript application to publish as a Node.js server that uses a `package.json` script at runtime.
     *
     * Use this method for frameworks where the production server depends on packages in `node_modules` at runtime.
     * The resulting container includes the full application with production dependencies installed.
     * This method is appropriate for frameworks like Nuxt (where `useAsyncData`/`useFetch` requires the
     * full Nitro environment), Remix (where `react-router-serve` is an npm dependency), and Astro SSR
     * (where the built entry point imports unbundled `@astrojs/*` packages).
     * For frameworks that produce a self-contained server artifact that does not require `node_modules`,
     * use `PublishAsNodeServer``1` instead for a smaller runtime image.
     * @param options Additional options.
     * @returns The updated resource builder.
     */
    publishAsPackageScript(options) {
        const scriptName = options?.scriptName;
        const runScriptArguments = options?.runScriptArguments;
        return new JavaScriptAppResourcePromiseImpl(this._publishAsPackageScriptInternal(scriptName, runScriptArguments), this._client);
    }
    /** @internal */
    async _withNpmInternal(install, installCommand, installArgs) {
        const rpcArgs = { resource: this._handle };
        if (install !== undefined)
            rpcArgs.install = install;
        if (installCommand !== undefined)
            rpcArgs.installCommand = installCommand;
        if (installArgs !== undefined)
            rpcArgs.installArgs = installArgs;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/withNpm', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Configures the Node.js resource to use npm as the package manager and optionally installs packages before the application starts.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withNpm(options) {
        const install = options?.install;
        const installCommand = options?.installCommand;
        const installArgs = options?.installArgs;
        return new JavaScriptAppResourcePromiseImpl(this._withNpmInternal(install, installCommand, installArgs), this._client);
    }
    /** @internal */
    async _withBunInternal(install, installArgs) {
        const rpcArgs = { resource: this._handle };
        if (install !== undefined)
            rpcArgs.install = install;
        if (installArgs !== undefined)
            rpcArgs.installArgs = installArgs;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/withBun', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Configures the JavaScript resource to use Bun as the package manager and optionally installs packages before the application starts.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withBun(options) {
        const install = options?.install;
        const installArgs = options?.installArgs;
        return new JavaScriptAppResourcePromiseImpl(this._withBunInternal(install, installArgs), this._client);
    }
    /** @internal */
    async _withYarnInternal(install, installArgs) {
        const rpcArgs = { resource: this._handle };
        if (install !== undefined)
            rpcArgs.install = install;
        if (installArgs !== undefined)
            rpcArgs.installArgs = installArgs;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/withYarn', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Configures the Node.js resource to use yarn as the package manager and optionally installs packages before the application starts.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withYarn(options) {
        const install = options?.install;
        const installArgs = options?.installArgs;
        return new JavaScriptAppResourcePromiseImpl(this._withYarnInternal(install, installArgs), this._client);
    }
    /** @internal */
    async _withPnpmInternal(install, installArgs) {
        const rpcArgs = { resource: this._handle };
        if (install !== undefined)
            rpcArgs.install = install;
        if (installArgs !== undefined)
            rpcArgs.installArgs = installArgs;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/withPnpm', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Configures the Node.js resource to use pnpm as the package manager and optionally installs packages before the application starts.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withPnpm(options) {
        const install = options?.install;
        const installArgs = options?.installArgs;
        return new JavaScriptAppResourcePromiseImpl(this._withPnpmInternal(install, installArgs), this._client);
    }
    /** @internal */
    async _withBuildScriptInternal(scriptName, args) {
        const rpcArgs = { resource: this._handle, scriptName };
        if (args !== undefined)
            rpcArgs.args = args;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/withBuildScript', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Adds a build script annotation to the resource builder using the specified command-line arguments.
     *
     * Use this method to specify custom build scripts for JavaScript application resources during
     * deployment.
     * @param scriptName The name of the script to be executed when the resource is built.
     * @param options Additional options.
     * @returns The same resource builder instance with the build script annotation applied.
     */
    withBuildScript(scriptName, options) {
        const args = options?.args;
        return new JavaScriptAppResourcePromiseImpl(this._withBuildScriptInternal(scriptName, args), this._client);
    }
    /** @internal */
    async _withRunScriptInternal(scriptName, args) {
        const rpcArgs = { resource: this._handle, scriptName };
        if (args !== undefined)
            rpcArgs.args = args;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/withRunScript', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Adds a run script annotation to the specified JavaScript application resource builder, specifying the script to execute and its arguments during run mode.
     *
     * Use this method to specify a custom script and its arguments that should be executed when the resource is executed
     * in RunMode.
     * @param scriptName The name of the script to be executed when the resource is run.
     * @param options Additional options.
     * @returns The same resource builder instance with the run script annotation applied, enabling further configuration.
     */
    withRunScript(scriptName, options) {
        const args = options?.args;
        return new JavaScriptAppResourcePromiseImpl(this._withRunScriptInternal(scriptName, args), this._client);
    }
    /** @internal */
    async _withBrowserDebuggerInternal(browser) {
        const rpcArgs = { builder: this._handle };
        if (browser !== undefined)
            rpcArgs.browser = browser;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/withBrowserDebugger', rpcArgs);
        return new JavaScriptAppResourceImpl(result, this._client);
    }
    /**
     * Configures a browser debugger for the JavaScript application resource, enabling browser-based debugging through a child resource that launches when the parent application is ready.
     *
     * This method creates a child `BrowserDebuggerResource` that waits for the parent JavaScript
     * application to start, then launches a browser debug session targeting the parent's HTTP or HTTPS endpoint.
     * The parent resource must have at least one HTTP or HTTPS endpoint configured.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withBrowserDebugger(options) {
        const browser = options?.browser;
        return new JavaScriptAppResourcePromiseImpl(this._withBrowserDebuggerInternal(browser), this._client);
    }
}
/**
 * Thenable wrapper for JavaScriptAppResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
class JavaScriptAppResourcePromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    command() {
        return this._promise.then(obj => obj.command());
    }
    workingDirectory() {
        return this._promise.then(obj => obj.workingDirectory());
    }
    name() {
        return this._promise.then(obj => obj.name());
    }
    withContainerRegistry(registry) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withContainerRegistry(registry)), this._client);
    }
    withDockerfileBaseImage(options) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileBaseImage(options)), this._client);
    }
    publishAsDockerFile(configure) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.publishAsDockerFile(configure)), this._client);
    }
    withExecutableCommand(command) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withExecutableCommand(command)), this._client);
    }
    withWorkingDirectory(workingDirectory) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withWorkingDirectory(workingDirectory)), this._client);
    }
    withMcpServer(options) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withMcpServer(options)), this._client);
    }
    withOtlpExporter(options) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withOtlpExporter(options)), this._client);
    }
    withRequiredCommand(command, options) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withRequiredCommand(command, options)), this._client);
    }
    withSessionLifetime() {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withSessionLifetime()), this._client);
    }
    withPersistentLifetime() {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withPersistentLifetime()), this._client);
    }
    withLifetimeOf(sourceBuilder) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withLifetimeOf(sourceBuilder)), this._client);
    }
    withParentProcessLifetime(parentProcessId) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withParentProcessLifetime(parentProcessId)), this._client);
    }
    withEnvironment(name, value) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withEnvironment(name, value)), this._client);
    }
    withEnvironmentCallback(callback) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withEnvironmentCallback(callback)), this._client);
    }
    withArgs(args) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withArgs(args)), this._client);
    }
    withArgsCallback(callback) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withArgsCallback(callback)), this._client);
    }
    withReferenceEnvironment(options) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withReferenceEnvironment(options)), this._client);
    }
    withReference(source, options) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withReference(source, options)), this._client);
    }
    withEndpointCallback(endpointName, callback, options) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withEndpointCallback(endpointName, callback, options)), this._client);
    }
    withHttpEndpointCallback(callback, options) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpEndpointCallback(callback, options)), this._client);
    }
    withHttpsEndpointCallback(callback, options) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpsEndpointCallback(callback, options)), this._client);
    }
    withEndpoint(options) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withEndpoint(options)), this._client);
    }
    withEndpointProxySupport(proxyEnabled) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withEndpointProxySupport(proxyEnabled)), this._client);
    }
    withHttpEndpoint(options) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpEndpoint(options)), this._client);
    }
    withHttpsEndpoint(options) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpsEndpoint(options)), this._client);
    }
    withExternalHttpEndpoints() {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withExternalHttpEndpoints()), this._client);
    }
    getEndpoint(name) {
        return new EndpointReferencePromiseImpl(this._promise.then(obj => obj.getEndpoint(name)), this._client);
    }
    asHttp2Service() {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.asHttp2Service()), this._client);
    }
    withUrls(callback) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withUrls(callback)), this._client);
    }
    withUrl(url, options) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withUrl(url, options)), this._client);
    }
    withUrlForEndpoint(endpointName, callback) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)), this._client);
    }
    withContainerFilesSource(sourcePath) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withContainerFilesSource(sourcePath)), this._client);
    }
    clearContainerFilesSources() {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.clearContainerFilesSources()), this._client);
    }
    excludeFromManifest() {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.excludeFromManifest()), this._client);
    }
    waitFor(dependency, options) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.waitFor(dependency, options)), this._client);
    }
    waitForStart(dependency, options) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.waitForStart(dependency, options)), this._client);
    }
    withExplicitStart() {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withExplicitStart()), this._client);
    }
    waitForCompletion(dependency, options) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.waitForCompletion(dependency, options)), this._client);
    }
    withHealthCheck(key) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withHealthCheck(key)), this._client);
    }
    withHttpHealthCheck(options) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpHealthCheck(options)), this._client);
    }
    withCommand(name, displayName, executeCommand, options) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)), this._client);
    }
    withProcessCommand(commandName, displayName, options) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommand(commandName, displayName, options)), this._client);
    }
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommandFactory(commandName, displayName, createProcessSpec, options)), this._client);
    }
    withHttpCommand(path, displayName, options) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpCommand(path, displayName, options)), this._client);
    }
    withDeveloperCertificateTrust(trust) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withDeveloperCertificateTrust(trust)), this._client);
    }
    withCertificateTrustScope(scope) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withCertificateTrustScope(scope)), this._client);
    }
    withHttpsDeveloperCertificate(options) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpsDeveloperCertificate(options)), this._client);
    }
    withoutHttpsCertificate() {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withoutHttpsCertificate()), this._client);
    }
    withRelationship(resourceBuilder, type) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withRelationship(resourceBuilder, type)), this._client);
    }
    withParentRelationship(parent) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withParentRelationship(parent)), this._client);
    }
    withChildRelationship(child) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withChildRelationship(child)), this._client);
    }
    withIconName(iconName, options) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withIconName(iconName, options)), this._client);
    }
    withComputeEnvironment(computeEnvironmentResource) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withComputeEnvironment(computeEnvironmentResource)), this._client);
    }
    withHttpProbe(probeType, options) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpProbe(probeType, options)), this._client);
    }
    excludeFromMcp() {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.excludeFromMcp()), this._client);
    }
    withHidden() {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withHidden()), this._client);
    }
    withHiddenOnCompletion(options) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withHiddenOnCompletion(options)), this._client);
    }
    withImagePushOptions(callback) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withImagePushOptions(callback)), this._client);
    }
    withRemoteImageName(remoteImageName) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withRemoteImageName(remoteImageName)), this._client);
    }
    withRemoteImageTag(remoteImageTag) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withRemoteImageTag(remoteImageTag)), this._client);
    }
    withPipelineStepFactory(stepName, callback, options) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)), this._client);
    }
    withPipelineConfiguration(callback) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withPipelineConfiguration(callback)), this._client);
    }
    getResourceName() {
        return this._promise.then(obj => obj.getResourceName());
    }
    onBeforeResourceStarted(callback) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.onBeforeResourceStarted(callback)), this._client);
    }
    onResourceStopped(callback) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.onResourceStopped(callback)), this._client);
    }
    onInitializeResource(callback) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.onInitializeResource(callback)), this._client);
    }
    onResourceEndpointsAllocated(callback) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.onResourceEndpointsAllocated(callback)), this._client);
    }
    onResourceReady(callback) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.onResourceReady(callback)), this._client);
    }
    createExecutionConfiguration() {
        return new ExecutionConfigurationBuilderPromiseImpl(this._promise.then(obj => obj.createExecutionConfiguration()), this._client);
    }
    publishAsStaticWebsite(options) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.publishAsStaticWebsite(options)), this._client);
    }
    publishAsNodeServer(entryPoint, options) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.publishAsNodeServer(entryPoint, options)), this._client);
    }
    publishAsPackageScript(options) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.publishAsPackageScript(options)), this._client);
    }
    withNpm(options) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withNpm(options)), this._client);
    }
    withBun(options) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withBun(options)), this._client);
    }
    withYarn(options) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withYarn(options)), this._client);
    }
    withPnpm(options) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withPnpm(options)), this._client);
    }
    withBuildScript(scriptName, options) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withBuildScript(scriptName, options)), this._client);
    }
    withRunScript(scriptName, options) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withRunScript(scriptName, options)), this._client);
    }
    withBrowserDebugger(options) {
        return new JavaScriptAppResourcePromiseImpl(this._promise.then(obj => obj.withBrowserDebugger(options)), this._client);
    }
}
// ============================================================================
// NextJsAppResourceImpl
// ============================================================================
/** Represents a Next.js application resource. */
class NextJsAppResourceImpl extends ResourceBuilderBase {
    constructor(handle, client) {
        super(handle, client);
    }
    async command() {
        return await this._client.invokeCapability('Aspire.Hosting.JavaScript/JavaScriptAppResource.command', { context: this._handle });
    }
    async workingDirectory() {
        return await this._client.invokeCapability('Aspire.Hosting.JavaScript/JavaScriptAppResource.workingDirectory', { context: this._handle });
    }
    async name() {
        return await this._client.invokeCapability('Aspire.Hosting.JavaScript/JavaScriptAppResource.name', { context: this._handle });
    }
    /** @internal */
    async _withContainerRegistryInternal(registry) {
        registry = isPromiseLike(registry) ? await registry : registry;
        const rpcArgs = { builder: this._handle, registry };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerRegistry', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Configures the resource to use the specified container registry for container image operations.
     *
     * This method adds a `ContainerRegistryReferenceAnnotation` to the resource,
     * indicating that the resource should use the specified container registry for container image operations.
     * @param registry The container registry resource builder.
     * @returns The resource builder for chaining.
     */
    withContainerRegistry(registry) {
        return new NextJsAppResourcePromiseImpl(this._withContainerRegistryInternal(registry), this._client);
    }
    /** @internal */
    async _withDockerfileBaseImageInternal(buildImage, runtimeImage) {
        const rpcArgs = { builder: this._handle };
        if (buildImage !== undefined)
            rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined)
            rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileBaseImage', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Configures custom base images for generated Dockerfiles.
     *
     * This extension method allows customization of the base images used in generated Dockerfiles.
     * For multi-stage Dockerfiles (e.g., Python with UV), you can specify separate build and runtime images.
     * Specify custom base images for a Python application:
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddPythonApp("myapp", "path/to/app", "main.py")
     * .WithDockerfileBaseImage(
     * buildImage: "ghcr.io/astral-sh/uv:python3.12-bookworm-slim",
     * runtimeImage: "python:3.12-slim-bookworm");
     * builder.Build().Run();
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileBaseImage(options) {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new NextJsAppResourcePromiseImpl(this._withDockerfileBaseImageInternal(buildImage, runtimeImage), this._client);
    }
    /** @internal */
    async _publishAsDockerFileInternal(configure) {
        const configureId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new ContainerResourceImpl(objHandle, this._client);
            await configure(obj);
        });
        const rpcArgs = { builder: this._handle, configure: configureId };
        const result = await this._client.invokeCapability('Aspire.Hosting/publishAsDockerFile', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Publishes an executable as a Docker file
     *
     * When the executable resource is converted to a container resource, the arguments to the executable
     * are not used. This is because arguments to the executable often contain physical paths that are not valid
     * in the container. The container can be set up with the correct arguments using the `configure` action.
     * @param configure Optional action to configure the container resource
     * @returns The resource builder.
     */
    publishAsDockerFile(configure) {
        return new NextJsAppResourcePromiseImpl(this._publishAsDockerFileInternal(configure), this._client);
    }
    /** @internal */
    async _withExecutableCommandInternal(command) {
        const rpcArgs = { builder: this._handle, command };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExecutableCommand', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Sets the command for the executable resource.
     * @param command Command.
     * @returns The resource builder.
     */
    withExecutableCommand(command) {
        return new NextJsAppResourcePromiseImpl(this._withExecutableCommandInternal(command), this._client);
    }
    /** @internal */
    async _withWorkingDirectoryInternal(workingDirectory) {
        const rpcArgs = { builder: this._handle, workingDirectory };
        const result = await this._client.invokeCapability('Aspire.Hosting/withWorkingDirectory', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Sets the working directory for the executable resource.
     * @param workingDirectory Working directory.
     * @returns The resource builder.
     */
    withWorkingDirectory(workingDirectory) {
        return new NextJsAppResourcePromiseImpl(this._withWorkingDirectoryInternal(workingDirectory), this._client);
    }
    /** @internal */
    async _withMcpServerInternal(path, endpointName) {
        const rpcArgs = { builder: this._handle };
        if (path !== undefined)
            rpcArgs.path = path;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withMcpServer', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Marks the resource as hosting a Model Context Protocol (MCP) server on the specified endpoint.
     *
     * This method adds an `McpServerEndpointAnnotation` to the resource, enabling the Aspire tooling
     * to discover and proxy the MCP server exposed by the resource.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withMcpServer(options) {
        const path = options?.path;
        const endpointName = options?.endpointName;
        return new NextJsAppResourcePromiseImpl(this._withMcpServerInternal(path, endpointName), this._client);
    }
    /** @internal */
    async _withOtlpExporterInternal(protocol) {
        const rpcArgs = { builder: this._handle };
        if (protocol !== undefined)
            rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability('Aspire.Hosting/withOtlpExporter', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Configures OTLP telemetry export
     * @param options Additional options.
     */
    withOtlpExporter(options) {
        const protocol = options?.protocol;
        return new NextJsAppResourcePromiseImpl(this._withOtlpExporterInternal(protocol), this._client);
    }
    /** @internal */
    async _withRequiredCommandInternal(command, helpLink) {
        const rpcArgs = { builder: this._handle, command };
        if (helpLink !== undefined)
            rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability('Aspire.Hosting/withRequiredCommand', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Declares that a resource requires a specific command/executable to be available on the local machine PATH before it can start.
     *
     * The command is considered valid if either:
     * 1. It is an absolute or relative path (contains a directory separator) that points to an existing file, or
     * 2. It is discoverable on the current process PATH (respecting PATHEXT on Windows).
     * If the command is not found, a warning message will be logged but the resource will be allowed to attempt to start.
     * @param command The command string (file name or path) that should be validated.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withRequiredCommand(command, options) {
        const helpLink = options?.helpLink;
        return new NextJsAppResourcePromiseImpl(this._withRequiredCommandInternal(command, helpLink), this._client);
    }
    /** @internal */
    async _withSessionLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withSessionLifetime', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a session lifetime.
     * @returns The resource builder.
     */
    withSessionLifetime() {
        return new NextJsAppResourcePromiseImpl(this._withSessionLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withPersistentLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPersistentLifetime', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime.
     * @returns The resource builder.
     */
    withPersistentLifetime() {
        return new NextJsAppResourcePromiseImpl(this._withPersistentLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withLifetimeOfInternal(sourceBuilder) {
        sourceBuilder = isPromiseLike(sourceBuilder) ? await sourceBuilder : sourceBuilder;
        const rpcArgs = { builder: this._handle, sourceBuilder };
        const result = await this._client.invokeCapability('Aspire.Hosting/withLifetimeOf', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to match the lifetime of another resource.
     *
     * The resource lifetime is evaluated from `sourceBuilder` when the application model is prepared, so later lifetime
     * changes to the source resource are reflected by this resource.
     * @param sourceBuilder The resource builder whose lifetime should be used.
     * @returns The resource builder.
     */
    withLifetimeOf(sourceBuilder) {
        return new NextJsAppResourcePromiseImpl(this._withLifetimeOfInternal(sourceBuilder), this._client);
    }
    /** @internal */
    async _withParentProcessLifetimeInternal(parentProcessId) {
        const rpcArgs = { builder: this._handle, parentProcessId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withParentProcessLifetime', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime that ends when a parent process exits.
     * @param parentProcessId The ID of the parent process to monitor.
     * @returns The resource builder.
     */
    withParentProcessLifetime(parentProcessId) {
        return new NextJsAppResourcePromiseImpl(this._withParentProcessLifetimeInternal(parentProcessId), this._client);
    }
    /** @internal */
    async _withEnvironmentInternal(name, value) {
        value = isPromiseLike(value) ? await value : value;
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEnvironment', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /** Sets an environment variable */
    withEnvironment(name, value) {
        return new NextJsAppResourcePromiseImpl(this._withEnvironmentInternal(name, value), this._client);
    }
    /** @internal */
    async _withEnvironmentCallbackInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new EnvironmentCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEnvironmentCallback', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Allows for the population of environment variables on a resource.
     * @param callback A callback that allows for deferred execution for computing many environment variables. This runs after resources have been allocated by the orchestrator and allows access to other resources to resolve computed data, e.g. connection strings, ports.
     * @returns The resource builder.
     */
    withEnvironmentCallback(callback) {
        return new NextJsAppResourcePromiseImpl(this._withEnvironmentCallbackInternal(callback), this._client);
    }
    /** @internal */
    async _withArgsInternal(args) {
        const rpcArgs = { builder: this._handle, args };
        const result = await this._client.invokeCapability('Aspire.Hosting/withArgs', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Adds arguments to be passed to a resource that supports arguments when it is launched.
     * @param args The arguments to be passed to the resource when it is started.
     * @returns The resource builder.
     */
    withArgs(args) {
        return new NextJsAppResourcePromiseImpl(this._withArgsInternal(args), this._client);
    }
    /** @internal */
    async _withArgsCallbackInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new CommandLineArgsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withArgsCallback', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Adds a callback to be executed with a list of command-line arguments when a resource is started.
     * @param callback A callback that allows for deferred execution for computing arguments. This runs after resources have been allocated by the orchestrator and allows access to other resources to resolve computed data, e.g. connection strings, ports.
     * @returns The resource builder.
     */
    withArgsCallback(callback) {
        return new NextJsAppResourcePromiseImpl(this._withArgsCallbackInternal(callback), this._client);
    }
    /** @internal */
    async _withReferenceEnvironmentInternal(options) {
        const rpcArgs = { builder: this._handle, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withReferenceEnvironment', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Configures how information is injected into environment variables when the resource references other resources.
     * @param options Options controlling which reference information is emitted.
     * @returns The resource builder.
     */
    withReferenceEnvironment(options) {
        return new NextJsAppResourcePromiseImpl(this._withReferenceEnvironmentInternal(options), this._client);
    }
    /** @internal */
    async _withReferenceInternal(source, connectionName, optional, name) {
        source = isPromiseLike(source) ? await source : source;
        const rpcArgs = { builder: this._handle, source };
        if (connectionName !== undefined)
            rpcArgs.connectionName = connectionName;
        if (optional !== undefined)
            rpcArgs.optional = optional;
        if (name !== undefined)
            rpcArgs.name = name;
        const result = await this._client.invokeCapability('Aspire.Hosting/withReference', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Adds a reference to another resource
     * @param options Additional options.
     */
    withReference(source, options) {
        const connectionName = options?.connectionName;
        const optional = options?.optional;
        const name = options?.name;
        return new NextJsAppResourcePromiseImpl(this._withReferenceInternal(source, connectionName, optional, name), this._client);
    }
    /** @internal */
    async _withEndpointCallbackInternal(endpointName, callback, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpointCallback', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Updates a named endpoint via callback
     * @param options Additional options.
     */
    withEndpointCallback(endpointName, callback, options) {
        const createIfNotExists = options?.createIfNotExists;
        return new NextJsAppResourcePromiseImpl(this._withEndpointCallbackInternal(endpointName, callback, createIfNotExists), this._client);
    }
    /** @internal */
    async _withHttpEndpointCallbackInternal(callback, name, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        if (name !== undefined)
            rpcArgs.name = name;
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpEndpointCallback', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Updates an HTTP endpoint via callback
     * @param options Additional options.
     */
    withHttpEndpointCallback(callback, options) {
        const name = options?.name;
        const createIfNotExists = options?.createIfNotExists;
        return new NextJsAppResourcePromiseImpl(this._withHttpEndpointCallbackInternal(callback, name, createIfNotExists), this._client);
    }
    /** @internal */
    async _withHttpsEndpointCallbackInternal(callback, name, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        if (name !== undefined)
            rpcArgs.name = name;
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpsEndpointCallback', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Updates an HTTPS endpoint via callback
     * @param options Additional options.
     */
    withHttpsEndpointCallback(callback, options) {
        const name = options?.name;
        const createIfNotExists = options?.createIfNotExists;
        return new NextJsAppResourcePromiseImpl(this._withHttpsEndpointCallbackInternal(callback, name, createIfNotExists), this._client);
    }
    /** @internal */
    async _withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (scheme !== undefined)
            rpcArgs.scheme = scheme;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        if (isExternal !== undefined)
            rpcArgs.isExternal = isExternal;
        if (protocol !== undefined)
            rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpoint', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Adds a network endpoint
     * @param options Additional options.
     * @returns The resource builder.
     */
    withEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const scheme = options?.scheme;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        const isExternal = options?.isExternal;
        const protocol = options?.protocol;
        return new NextJsAppResourcePromiseImpl(this._withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol), this._client);
    }
    /** @internal */
    async _withEndpointProxySupportInternal(proxyEnabled) {
        const rpcArgs = { builder: this._handle, proxyEnabled };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpointProxySupport', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Set whether a resource can use proxied endpoints or whether they should be disabled for all endpoints belonging to the resource. If set to `false`, endpoints belonging to the resource will ignore the configured proxy settings and run proxy-less.
     *
     * This method is intended to support scenarios with persistent lifetime resources where it is desirable for the resource to be accessible over the same
     * port whether the Aspire application is running or not. Proxied endpoints bind ports that are only accessible while the Aspire application is running.
     * The user needs to be careful to ensure that endpoints are using unique ports when disabling proxy support as by default for proxy-less
     * endpoints, Aspire will allocate the target port as the host port, which will increase the chance of port conflicts.
     * @param proxyEnabled Should endpoints for the resource support using a proxy?
     * @returns The resource builder.
     */
    withEndpointProxySupport(proxyEnabled) {
        return new NextJsAppResourcePromiseImpl(this._withEndpointProxySupportInternal(proxyEnabled), this._client);
    }
    /** @internal */
    async _withHttpEndpointInternal(port, targetPort, name, env, isProxied) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpEndpoint', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP endpoint
     *
     * If an endpoint with the same name already exists on the resource, the existing endpoint is updated
     * with any non-null parameter values. Parameters left as `null` will not modify the existing endpoint's values.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new NextJsAppResourcePromiseImpl(this._withHttpEndpointInternal(port, targetPort, name, env, isProxied), this._client);
    }
    /** @internal */
    async _withHttpsEndpointInternal(port, targetPort, name, env, isProxied) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpsEndpoint', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTPS endpoint
     *
     * If an endpoint with the same name already exists on the resource, the existing endpoint is updated
     * with any non-null parameter values. Parameters left as `null` will not modify the existing endpoint's values.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpsEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new NextJsAppResourcePromiseImpl(this._withHttpsEndpointInternal(port, targetPort, name, env, isProxied), this._client);
    }
    /** @internal */
    async _withExternalHttpEndpointsInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExternalHttpEndpoints', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Marks existing http or https endpoints on a resource as external.
     * @returns The resource builder.
     */
    withExternalHttpEndpoints() {
        return new NextJsAppResourcePromiseImpl(this._withExternalHttpEndpointsInternal(), this._client);
    }
    /**
     * Gets an endpoint reference
     * @param name The name of the endpoint.
     * @returns An `EndpointReference` that can be used to resolve the address of the endpoint after resource allocation has occurred.
     */
    getEndpoint(name) {
        const promise = (async () => {
            const rpcArgs = { builder: this._handle, name };
            const handle = await this._client.invokeCapability('Aspire.Hosting/getEndpoint', rpcArgs);
            return new EndpointReferenceImpl(handle, this._client);
        })();
        return new EndpointReferencePromiseImpl(promise, this._client);
    }
    /** @internal */
    async _asHttp2ServiceInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/asHttp2Service', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to mark all endpoints' transport as HTTP/2. This is useful for HTTP/2 services that need prior knowledge.
     * @returns The resource builder.
     */
    asHttp2Service() {
        return new NextJsAppResourcePromiseImpl(this._asHttp2ServiceInternal(), this._client);
    }
    /** @internal */
    async _withUrlsInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new ResourceUrlsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrls', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to customize the URLs displayed for the resource.
     * @param callback The callback that will customize URLs for the resource.
     * @returns The resource builder.
     */
    withUrls(callback) {
        return new NextJsAppResourcePromiseImpl(this._withUrlsInternal(callback), this._client);
    }
    /** @internal */
    async _withUrlInternal(url, displayText) {
        const rpcArgs = { builder: this._handle, url };
        if (displayText !== undefined)
            rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrl', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Adds or modifies displayed URLs
     * @param options Additional options.
     */
    withUrl(url, options) {
        const displayText = options?.displayText;
        return new NextJsAppResourcePromiseImpl(this._withUrlInternal(url, displayText), this._client);
    }
    /** @internal */
    async _withUrlForEndpointInternal(endpointName, callback) {
        const callbackId = registerCallback(async (objData) => {
            const obj = wrapIfHandle(objData);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrlForEndpoint', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to update the URL displayed for the endpoint with the specified name.
     * @param endpointName The name of the endpoint to customize the URL for.
     * @param callback The callback that will customize the URL.
     * @returns The resource builder.
     */
    withUrlForEndpoint(endpointName, callback) {
        return new NextJsAppResourcePromiseImpl(this._withUrlForEndpointInternal(endpointName, callback), this._client);
    }
    /** @internal */
    async _withContainerFilesSourceInternal(sourcePath) {
        const rpcArgs = { builder: this._handle, sourcePath };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerFilesSource', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Adds a container files source annotation to the resource being built, specifying the path to the container files source.
     * @param sourcePath The path to the container files source to associate with the resource. Cannot be null.
     * @returns The resource builder instance with the container files source annotation applied.
     */
    withContainerFilesSource(sourcePath) {
        return new NextJsAppResourcePromiseImpl(this._withContainerFilesSourceInternal(sourcePath), this._client);
    }
    /** @internal */
    async _clearContainerFilesSourcesInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/clearContainerFilesSources', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Removes any container files source annotation from the resource being built.
     * @returns The resource builder instance with the container files source annotation applied.
     */
    clearContainerFilesSources() {
        return new NextJsAppResourcePromiseImpl(this._clearContainerFilesSourcesInternal(), this._client);
    }
    /** @internal */
    async _excludeFromManifestInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromManifest', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Excludes a resource from being published to the manifest.
     * @returns The resource builder.
     */
    excludeFromManifest() {
        return new NextJsAppResourcePromiseImpl(this._excludeFromManifestInternal(), this._client);
    }
    /** @internal */
    async _waitForInternal(dependency, waitBehavior) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (waitBehavior !== undefined)
            rpcArgs.waitBehavior = waitBehavior;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitFor', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Waits for another resource to be ready
     * @param options Additional options.
     */
    waitFor(dependency, options) {
        const waitBehavior = options?.waitBehavior;
        return new NextJsAppResourcePromiseImpl(this._waitForInternal(dependency, waitBehavior), this._client);
    }
    /** @internal */
    async _waitForStartInternal(dependency, waitBehavior) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (waitBehavior !== undefined)
            rpcArgs.waitBehavior = waitBehavior;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitForStart', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Waits for another resource to start
     * @param options Additional options.
     */
    waitForStart(dependency, options) {
        const waitBehavior = options?.waitBehavior;
        return new NextJsAppResourcePromiseImpl(this._waitForStartInternal(dependency, waitBehavior), this._client);
    }
    /** @internal */
    async _withExplicitStartInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExplicitStart', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Prevents resource from starting automatically
     * @returns The resource builder.
     */
    withExplicitStart() {
        return new NextJsAppResourcePromiseImpl(this._withExplicitStartInternal(), this._client);
    }
    /** @internal */
    async _waitForCompletionInternal(dependency, exitCode) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitForResourceCompletion', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Waits for the dependency resource to enter the Exited or Finished state before starting the resource.
     * @param dependency The resource builder for the dependency resource.
     * @param options Additional options.
     * @returns The resource builder.
     */
    waitForCompletion(dependency, options) {
        const exitCode = options?.exitCode;
        return new NextJsAppResourcePromiseImpl(this._waitForCompletionInternal(dependency, exitCode), this._client);
    }
    /** @internal */
    async _withHealthCheckInternal(key) {
        const rpcArgs = { builder: this._handle, key };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHealthCheck', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Adds a health check by key
     * @param key The key for the health check.
     * @returns The resource builder.
     */
    withHealthCheck(key) {
        return new NextJsAppResourcePromiseImpl(this._withHealthCheckInternal(key), this._client);
    }
    /** @internal */
    async _withHttpHealthCheckInternal(path, statusCode, endpointName) {
        const rpcArgs = { builder: this._handle };
        if (path !== undefined)
            rpcArgs.path = path;
        if (statusCode !== undefined)
            rpcArgs.statusCode = statusCode;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpHealthCheck', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Adds a health check to the resource which is mapped to a specific endpoint.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpHealthCheck(options) {
        const path = options?.path;
        const statusCode = options?.statusCode;
        const endpointName = options?.endpointName;
        return new NextJsAppResourcePromiseImpl(this._withHttpHealthCheckInternal(path, statusCode, endpointName), this._client);
    }
    /** @internal */
    async _withCommandInternal(name, displayName, executeCommand, commandOptions) {
        const executeCommandId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined)
            rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability('Aspire.Hosting/withCommand', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Adds a resource command
     *
     * The `WithCommand` method is used to add commands to the resource. Commands are displayed in the dashboard
     * and can be executed by a user using the dashboard UI.
     * When a command is executed, the `executeCommand` callback is called and is run inside the Aspire host.
     * @param name The name of the command. The name uniquely identifies the command.
     * @param displayName The display name visible in UI.
     * @param executeCommand A callback that is executed when the command is executed. The callback is run inside the Aspire host. The callback result is used to indicate success or failure in the UI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withCommand(name, displayName, executeCommand, options) {
        const commandOptions = options?.commandOptions;
        return new NextJsAppResourcePromiseImpl(this._withCommandInternal(name, displayName, executeCommand, commandOptions), this._client);
    }
    /** @internal */
    async _withProcessCommandInternal(commandName, displayName, options) {
        const rpcArgs = { builder: this._handle, commandName, displayName, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommand', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /** Adds a command to the resource that starts a local process when invoked. */
    withProcessCommand(commandName, displayName, options) {
        return new NextJsAppResourcePromiseImpl(this._withProcessCommandInternal(commandName, displayName, options), this._client);
    }
    /** @internal */
    async _withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options) {
        const createProcessSpecId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await createProcessSpec(arg);
        });
        const rpcArgs = { builder: this._handle, commandName, displayName, createProcessSpec: createProcessSpecId };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommandFactory', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Adds a command to the resource that starts a local process created by a callback when invoked.
     * @param options Additional options.
     */
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new NextJsAppResourcePromiseImpl(this._withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options), this._client);
    }
    /** @internal */
    async _withHttpCommandInternal(path, displayName, options) {
        const rpcArgs = { builder: this._handle, path, displayName };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpCommand', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP resource command
     * @param options Additional options.
     */
    withHttpCommand(path, displayName, options) {
        return new NextJsAppResourcePromiseImpl(this._withHttpCommandInternal(path, displayName, options), this._client);
    }
    /** @internal */
    async _withDeveloperCertificateTrustInternal(trust) {
        const rpcArgs = { builder: this._handle, trust };
        const result = await this._client.invokeCapability('Aspire.Hosting/withDeveloperCertificateTrust', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Indicates whether developer certificates should be treated as trusted certificate authorities for the resource at run time. Currently this indicates trust for the ASP.NET Core developer certificate. The developer certificate will only be trusted when running in local development scenarios; in publish mode resources will use their default certificate trust.
     *
     * Disable trust for app host managed developer certificate(s) for a container resource.
     * ```
     * var container = builder.AddContainer("my-service", "my-service:latest")
     * .WithDeveloperCertificateTrust(false);
     * ```
     * Disable automatic trust for app host managed developer certificate(s), but explicitly enable it for a specific resource.
     * ```
     * var builder = DistributedApplication.CreateBuilder(new DistributedApplicationOptions()
     * {
     * Args = args,
     * TrustDeveloperCertificate = false,
     * });
     * var project = builder.AddProject<MyService>("my-service")
     * .WithDeveloperCertificateTrust(true);
     * ```
     * @param trust Indicates whether the developer certificate should be treated as trusted.
     * @returns The resource builder.
     */
    withDeveloperCertificateTrust(trust) {
        return new NextJsAppResourcePromiseImpl(this._withDeveloperCertificateTrustInternal(trust), this._client);
    }
    /** @internal */
    async _withCertificateTrustScopeInternal(scope) {
        const rpcArgs = { builder: this._handle, scope };
        const result = await this._client.invokeCapability('Aspire.Hosting/withCertificateTrustScope', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Sets the certificate trust scope
     *
     * The default scope if not overridden is `Append` which means that custom certificate
     * authorities should be appended to the default trusted certificate authorities for the resource. Setting the scope to
     * `Override` indicates the set of certificates in referenced
     * `CertificateAuthorityCollection` (and optionally Aspire developer certificiates) should be used as the
     * exclusive source of trust for a resource.
     * In all cases, this is a best effort implementation as not all resources support full customization of certificate
     * trust.
     * Set the scope for custom certificate authorities to override the default trusted certificate authorities for a container resource.
     * ```
     * var caCollection = builder.AddCertificateAuthorityCollection("my-cas")
     * .WithCertificate(new X509Certificate2("my-ca.pem"));
     * var container = builder.AddContainer("my-service", "my-service:latest")
     * .WithCertificateAuthorityCollection(caCollection)
     * .WithCertificateTrustScope(CertificateTrustScope.Override);
     * ```
     * @param scope The scope to apply to custom certificate authorities associated with the resource.
     * @returns The resource builder.
     */
    withCertificateTrustScope(scope) {
        return new NextJsAppResourcePromiseImpl(this._withCertificateTrustScopeInternal(scope), this._client);
    }
    /** @internal */
    async _withHttpsDeveloperCertificateInternal(password) {
        password = isPromiseLike(password) ? await password : password;
        const rpcArgs = { builder: this._handle };
        if (password !== undefined)
            rpcArgs.password = password;
        const result = await this._client.invokeCapability('Aspire.Hosting/withParameterHttpsDeveloperCertificate', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Indicates that a resource should use the developer certificate key pair for HTTPS endpoints at run time. Currently this indicates use of the ASP.NET Core developer certificate. The developer certificate will only be used when running in local development scenarios; in publish mode resources will use their default certificate configuration.
     *
     * Use the developer certificate for HTTPS/TLS endpoints on a container resource:
     * ```
     * builder.AddContainer("my-service", "my-image")
     * .WithHttpsDeveloperCertificate()
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpsDeveloperCertificate(options) {
        let password = options?.password;
        return new NextJsAppResourcePromiseImpl(this._withHttpsDeveloperCertificateInternal(password), this._client);
    }
    /** @internal */
    async _withoutHttpsCertificateInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withoutHttpsCertificate', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Disable HTTPS/TLS server certificate configuration for the resource. No HTTPS/TLS termination configuration will be applied.
     *
     * Disable HTTPS certificate configuration for a Redis resource:
     * ```
     * var redis = builder.AddRedis("cache")
     * .WithoutHttpsCertificate();
     * ```
     * @returns The resource builder.
     */
    withoutHttpsCertificate() {
        return new NextJsAppResourcePromiseImpl(this._withoutHttpsCertificateInternal(), this._client);
    }
    /** @internal */
    async _withRelationshipInternal(resourceBuilder, type) {
        resourceBuilder = isPromiseLike(resourceBuilder) ? await resourceBuilder : resourceBuilder;
        const rpcArgs = { builder: this._handle, resourceBuilder, type };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderRelationship', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Adds a relationship to another resource using its builder.
     * @param resourceBuilder The resource builder that the relationship is to.
     * @param type The relationship type.
     * @returns A resource builder.
     */
    withRelationship(resourceBuilder, type) {
        return new NextJsAppResourcePromiseImpl(this._withRelationshipInternal(resourceBuilder, type), this._client);
    }
    /** @internal */
    async _withParentRelationshipInternal(parent) {
        parent = isPromiseLike(parent) ? await parent : parent;
        const rpcArgs = { builder: this._handle, parent };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderParentRelationship', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Sets the parent relationship
     * @param parent The parent of `builder`.
     * @returns A resource builder.
     */
    withParentRelationship(parent) {
        return new NextJsAppResourcePromiseImpl(this._withParentRelationshipInternal(parent), this._client);
    }
    /** @internal */
    async _withChildRelationshipInternal(child) {
        child = isPromiseLike(child) ? await child : child;
        const rpcArgs = { builder: this._handle, child };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderChildRelationship', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Sets a child relationship
     * @param child The child of `builder`.
     * @returns A resource builder.
     */
    withChildRelationship(child) {
        return new NextJsAppResourcePromiseImpl(this._withChildRelationshipInternal(child), this._client);
    }
    /** @internal */
    async _withIconNameInternal(iconName, iconVariant) {
        const rpcArgs = { builder: this._handle, iconName };
        if (iconVariant !== undefined)
            rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability('Aspire.Hosting/withIconName', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Specifies the icon to use when displaying the resource in the dashboard.
     * @param iconName The name of the FluentUI icon to use. See https://aka.ms/fluentui-system-icons for available icons.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withIconName(iconName, options) {
        const iconVariant = options?.iconVariant;
        return new NextJsAppResourcePromiseImpl(this._withIconNameInternal(iconName, iconVariant), this._client);
    }
    /** @internal */
    async _withComputeEnvironmentInternal(computeEnvironmentResource) {
        computeEnvironmentResource = isPromiseLike(computeEnvironmentResource) ? await computeEnvironmentResource : computeEnvironmentResource;
        const rpcArgs = { builder: this._handle, computeEnvironmentResource };
        const result = await this._client.invokeCapability('Aspire.Hosting/withComputeEnvironment', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Configures the compute environment for the compute resource.
     *
     * This method allows associating a specific compute environment with the compute resource.
     * @param computeEnvironmentResource The compute environment resource to associate with the compute resource.
     * @returns The resource builder.
     */
    withComputeEnvironment(computeEnvironmentResource) {
        return new NextJsAppResourcePromiseImpl(this._withComputeEnvironmentInternal(computeEnvironmentResource), this._client);
    }
    /** @internal */
    async _withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName) {
        const rpcArgs = { builder: this._handle, probeType };
        if (path !== undefined)
            rpcArgs.path = path;
        if (initialDelaySeconds !== undefined)
            rpcArgs.initialDelaySeconds = initialDelaySeconds;
        if (periodSeconds !== undefined)
            rpcArgs.periodSeconds = periodSeconds;
        if (timeoutSeconds !== undefined)
            rpcArgs.timeoutSeconds = timeoutSeconds;
        if (failureThreshold !== undefined)
            rpcArgs.failureThreshold = failureThreshold;
        if (successThreshold !== undefined)
            rpcArgs.successThreshold = successThreshold;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpProbe', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP health probe to the resource
     * @param options Additional options.
     */
    withHttpProbe(probeType, options) {
        const path = options?.path;
        const initialDelaySeconds = options?.initialDelaySeconds;
        const periodSeconds = options?.periodSeconds;
        const timeoutSeconds = options?.timeoutSeconds;
        const failureThreshold = options?.failureThreshold;
        const successThreshold = options?.successThreshold;
        const endpointName = options?.endpointName;
        return new NextJsAppResourcePromiseImpl(this._withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName), this._client);
    }
    /** @internal */
    async _excludeFromMcpInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromMcp', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Exclude the resource from MCP operations using the Aspire MCP server. The resource is excluded from results that return resources, console logs and telemetry.
     * @returns The resource builder.
     */
    excludeFromMcp() {
        return new NextJsAppResourcePromiseImpl(this._excludeFromMcpInternal(), this._client);
    }
    /** @internal */
    async _withHiddenInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHidden', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists
     *
     * Use this method to hide resources that are implementation details and should never be displayed by default.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @returns The resource builder.
     */
    withHidden() {
        return new NextJsAppResourcePromiseImpl(this._withHiddenInternal(), this._client);
    }
    /** @internal */
    async _withHiddenOnCompletionInternal(exitCode, exitCodes) {
        const rpcArgs = { builder: this._handle };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        if (exitCodes !== undefined)
            rpcArgs.exitCodes = exitCodes;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHiddenOnCompletion', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists after successful completion
     *
     * This method is useful for one-off resources such as setup scripts, migrations, or build steps that should remain visible while running
     * and then be hidden after successful completion.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHiddenOnCompletion(options) {
        const exitCode = options?.exitCode;
        const exitCodes = options?.exitCodes;
        return new NextJsAppResourcePromiseImpl(this._withHiddenOnCompletionInternal(exitCode, exitCodes), this._client);
    }
    /** @internal */
    async _withImagePushOptionsInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ContainerImagePushOptionsCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImagePushOptions', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Adds an asynchronous callback to configure container image push options for the resource.
     *
     * This method allows customization of how container images are named and tagged when pushed to a registry using an asynchronous callback.
     * Use this overload when the callback needs to perform asynchronous operations such as retrieving configuration values from external sources.
     * The callback receives a `ContainerImagePushOptionsCallbackContext` that provides access to the resource
     * and the `ContainerImagePushOptions` that can be modified.
     * Multiple callbacks can be registered on the same resource, and they will be invoked in the order they were added.
     * @param callback The asynchronous callback to configure push options.
     * @returns The resource builder.
     */
    withImagePushOptions(callback) {
        return new NextJsAppResourcePromiseImpl(this._withImagePushOptionsInternal(callback), this._client);
    }
    /** @internal */
    async _withRemoteImageNameInternal(remoteImageName) {
        const rpcArgs = { builder: this._handle, remoteImageName };
        const result = await this._client.invokeCapability('Aspire.Hosting/withRemoteImageName', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Sets the remote image name (without registry endpoint or tag) for container push operations.
     *
     * Use this with `withRemoteImageTag` to fully customize the image reference used for container push operations.
     * @param remoteImageName The remote image name (e.g., "myapp" or "myorg/myapp").
     * @returns The resource builder.
     */
    withRemoteImageName(remoteImageName) {
        return new NextJsAppResourcePromiseImpl(this._withRemoteImageNameInternal(remoteImageName), this._client);
    }
    /** @internal */
    async _withRemoteImageTagInternal(remoteImageTag) {
        const rpcArgs = { builder: this._handle, remoteImageTag };
        const result = await this._client.invokeCapability('Aspire.Hosting/withRemoteImageTag', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Sets the remote image tag for container push operations.
     *
     * Use this with `withRemoteImageName` to fully customize the image reference used for container push operations.
     * @param remoteImageTag The remote image tag (e.g., "latest", "v1.0.0").
     * @returns The resource builder.
     */
    withRemoteImageTag(remoteImageTag) {
        return new NextJsAppResourcePromiseImpl(this._withRemoteImageTagInternal(remoteImageTag), this._client);
    }
    /** @internal */
    async _withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new PipelineStepContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined)
            rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined)
            rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined)
            rpcArgs.tags = tags;
        if (description !== undefined)
            rpcArgs.description = description;
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineStepFactory', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Adds a pipeline step to the resource that will be executed during deployment.
     * @param stepName The unique name of the pipeline step.
     * @param callback The callback to execute when the step runs.
     * @param options Additional options.
     * @returns The resource builder for chaining.
     */
    withPipelineStepFactory(stepName, callback, options) {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new NextJsAppResourcePromiseImpl(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description), this._client);
    }
    /** @internal */
    async _withPipelineConfigurationInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new PipelineConfigurationContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineConfiguration', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to be executed during the pipeline configuration phase, allowing modification of step dependencies and relationships.
     * @param callback The callback function to execute during the configuration phase.
     * @returns The resource builder for chaining.
     */
    withPipelineConfiguration(callback) {
        return new NextJsAppResourcePromiseImpl(this._withPipelineConfigurationInternal(callback), this._client);
    }
    /**
     * Gets the name of the resource from a builder.
     *
     * Why this wrapper exists: This capability accesses a nested property
     * (`resource.Resource.Name`) which requires a wrapper method. There is no single
     * .NET method that returns just the resource name that could be annotated directly.
     * @returns The resource name.
     */
    async getResourceName() {
        const rpcArgs = { resource: this._handle };
        return await this._client.invokeCapability('Aspire.Hosting/getResourceName', rpcArgs);
    }
    /** @internal */
    async _onBeforeResourceStartedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new BeforeResourceStartedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onBeforeResourceStarted', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the BeforeResourceStarted event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onBeforeResourceStarted(callback) {
        return new NextJsAppResourcePromiseImpl(this._onBeforeResourceStartedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceStoppedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceStoppedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceStopped', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceStopped event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceStopped(callback) {
        return new NextJsAppResourcePromiseImpl(this._onResourceStoppedInternal(callback), this._client);
    }
    /** @internal */
    async _onInitializeResourceInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new InitializeResourceEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onInitializeResource', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the InitializeResource event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onInitializeResource(callback) {
        return new NextJsAppResourcePromiseImpl(this._onInitializeResourceInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceEndpointsAllocatedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceEndpointsAllocatedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceEndpointsAllocated', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceEndpointsAllocated event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceEndpointsAllocated(callback) {
        return new NextJsAppResourcePromiseImpl(this._onResourceEndpointsAllocatedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceReadyInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceReadyEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceReady', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceReady event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceReady(callback) {
        return new NextJsAppResourcePromiseImpl(this._onResourceReadyInternal(callback), this._client);
    }
    /**
     * Creates an execution configuration builder for the specified resource.
     * @returns The execution configuration builder.
     */
    createExecutionConfiguration() {
        const promise = (async () => {
            const rpcArgs = { resource: this._handle };
            const handle = await this._client.invokeCapability('Aspire.Hosting/createExecutionConfiguration', rpcArgs);
            return new ExecutionConfigurationBuilderImpl(handle, this._client);
        })();
        return new ExecutionConfigurationBuilderPromiseImpl(promise, this._client);
    }
    /** @internal */
    async _publishAsStaticWebsiteInternal(apiPath, apiTarget, outputPath, stripPrefix, targetEndpointName) {
        apiTarget = isPromiseLike(apiTarget) ? await apiTarget : apiTarget;
        const rpcArgs = { builder: this._handle };
        if (apiPath !== undefined)
            rpcArgs.apiPath = apiPath;
        if (apiTarget !== undefined)
            rpcArgs.apiTarget = apiTarget;
        if (outputPath !== undefined)
            rpcArgs.outputPath = outputPath;
        if (stripPrefix !== undefined)
            rpcArgs.stripPrefix = stripPrefix;
        if (targetEndpointName !== undefined)
            rpcArgs.targetEndpointName = targetEndpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/publishAsStaticWebsite', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Publishes the JavaScript application as a standalone static website using YARP.
     * @param options Additional options.
     */
    publishAsStaticWebsite(options) {
        const apiPath = options?.apiPath;
        let apiTarget = options?.apiTarget;
        const outputPath = options?.outputPath;
        const stripPrefix = options?.stripPrefix;
        const targetEndpointName = options?.targetEndpointName;
        return new NextJsAppResourcePromiseImpl(this._publishAsStaticWebsiteInternal(apiPath, apiTarget, outputPath, stripPrefix, targetEndpointName), this._client);
    }
    /** @internal */
    async _publishAsNodeServerInternal(entryPoint, outputPath) {
        const rpcArgs = { builder: this._handle, entryPoint };
        if (outputPath !== undefined)
            rpcArgs.outputPath = outputPath;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/publishAsNodeServer', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Configures the JavaScript application to publish as a standalone Node.js server that runs a built artifact directly.
     *
     * Use this method for frameworks that produce a Node.js server artifact during the build and recommend
     * running that artifact directly in production rather than invoking a package manager script at runtime.
     * The application source is still built using the configured package manager and build script; this method
     * only changes the publish-time runtime container shape.
     * The container files source path is automatically set to `outputPath` so that only
     * the built output directory is copied into the runtime container, not the full application source.
     * @param entryPoint The relative path to the Node.js entry point to execute in the published container after the build completes, such as `.output/server/index.js` or `build/index.js`.
     * @param options Additional options.
     * @returns The updated resource builder.
     */
    publishAsNodeServer(entryPoint, options) {
        const outputPath = options?.outputPath;
        return new NextJsAppResourcePromiseImpl(this._publishAsNodeServerInternal(entryPoint, outputPath), this._client);
    }
    /** @internal */
    async _publishAsPackageScriptInternal(scriptName, runScriptArguments) {
        const rpcArgs = { builder: this._handle };
        if (scriptName !== undefined)
            rpcArgs.scriptName = scriptName;
        if (runScriptArguments !== undefined)
            rpcArgs.runScriptArguments = runScriptArguments;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/publishAsPackageScript', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Configures the JavaScript application to publish as a Node.js server that uses a `package.json` script at runtime.
     *
     * Use this method for frameworks where the production server depends on packages in `node_modules` at runtime.
     * The resulting container includes the full application with production dependencies installed.
     * This method is appropriate for frameworks like Nuxt (where `useAsyncData`/`useFetch` requires the
     * full Nitro environment), Remix (where `react-router-serve` is an npm dependency), and Astro SSR
     * (where the built entry point imports unbundled `@astrojs/*` packages).
     * For frameworks that produce a self-contained server artifact that does not require `node_modules`,
     * use `PublishAsNodeServer``1` instead for a smaller runtime image.
     * @param options Additional options.
     * @returns The updated resource builder.
     */
    publishAsPackageScript(options) {
        const scriptName = options?.scriptName;
        const runScriptArguments = options?.runScriptArguments;
        return new NextJsAppResourcePromiseImpl(this._publishAsPackageScriptInternal(scriptName, runScriptArguments), this._client);
    }
    /** @internal */
    async _disableBuildValidationInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/disableBuildValidation', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Disables deploy-time build validation checks for the Next.js application.
     *
     * By default, `AddNextJsApp` adds publish prerequisite steps that verify
     * the Next.js configuration (e.g. that `output: "standalone"` is set). Use this method
     * to suppress those checks when the configuration is set dynamically or via an external
     * mechanism that cannot be detected by static file inspection.
     * @returns The resource builder for chaining.
     */
    disableBuildValidation() {
        return new NextJsAppResourcePromiseImpl(this._disableBuildValidationInternal(), this._client);
    }
    /** @internal */
    async _withNpmInternal(install, installCommand, installArgs) {
        const rpcArgs = { resource: this._handle };
        if (install !== undefined)
            rpcArgs.install = install;
        if (installCommand !== undefined)
            rpcArgs.installCommand = installCommand;
        if (installArgs !== undefined)
            rpcArgs.installArgs = installArgs;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/withNpm', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Configures the Node.js resource to use npm as the package manager and optionally installs packages before the application starts.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withNpm(options) {
        const install = options?.install;
        const installCommand = options?.installCommand;
        const installArgs = options?.installArgs;
        return new NextJsAppResourcePromiseImpl(this._withNpmInternal(install, installCommand, installArgs), this._client);
    }
    /** @internal */
    async _withBunInternal(install, installArgs) {
        const rpcArgs = { resource: this._handle };
        if (install !== undefined)
            rpcArgs.install = install;
        if (installArgs !== undefined)
            rpcArgs.installArgs = installArgs;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/withBun', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Configures the JavaScript resource to use Bun as the package manager and optionally installs packages before the application starts.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withBun(options) {
        const install = options?.install;
        const installArgs = options?.installArgs;
        return new NextJsAppResourcePromiseImpl(this._withBunInternal(install, installArgs), this._client);
    }
    /** @internal */
    async _withYarnInternal(install, installArgs) {
        const rpcArgs = { resource: this._handle };
        if (install !== undefined)
            rpcArgs.install = install;
        if (installArgs !== undefined)
            rpcArgs.installArgs = installArgs;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/withYarn', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Configures the Node.js resource to use yarn as the package manager and optionally installs packages before the application starts.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withYarn(options) {
        const install = options?.install;
        const installArgs = options?.installArgs;
        return new NextJsAppResourcePromiseImpl(this._withYarnInternal(install, installArgs), this._client);
    }
    /** @internal */
    async _withPnpmInternal(install, installArgs) {
        const rpcArgs = { resource: this._handle };
        if (install !== undefined)
            rpcArgs.install = install;
        if (installArgs !== undefined)
            rpcArgs.installArgs = installArgs;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/withPnpm', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Configures the Node.js resource to use pnpm as the package manager and optionally installs packages before the application starts.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withPnpm(options) {
        const install = options?.install;
        const installArgs = options?.installArgs;
        return new NextJsAppResourcePromiseImpl(this._withPnpmInternal(install, installArgs), this._client);
    }
    /** @internal */
    async _withBuildScriptInternal(scriptName, args) {
        const rpcArgs = { resource: this._handle, scriptName };
        if (args !== undefined)
            rpcArgs.args = args;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/withBuildScript', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Adds a build script annotation to the resource builder using the specified command-line arguments.
     *
     * Use this method to specify custom build scripts for JavaScript application resources during
     * deployment.
     * @param scriptName The name of the script to be executed when the resource is built.
     * @param options Additional options.
     * @returns The same resource builder instance with the build script annotation applied.
     */
    withBuildScript(scriptName, options) {
        const args = options?.args;
        return new NextJsAppResourcePromiseImpl(this._withBuildScriptInternal(scriptName, args), this._client);
    }
    /** @internal */
    async _withRunScriptInternal(scriptName, args) {
        const rpcArgs = { resource: this._handle, scriptName };
        if (args !== undefined)
            rpcArgs.args = args;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/withRunScript', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Adds a run script annotation to the specified JavaScript application resource builder, specifying the script to execute and its arguments during run mode.
     *
     * Use this method to specify a custom script and its arguments that should be executed when the resource is executed
     * in RunMode.
     * @param scriptName The name of the script to be executed when the resource is run.
     * @param options Additional options.
     * @returns The same resource builder instance with the run script annotation applied, enabling further configuration.
     */
    withRunScript(scriptName, options) {
        const args = options?.args;
        return new NextJsAppResourcePromiseImpl(this._withRunScriptInternal(scriptName, args), this._client);
    }
    /** @internal */
    async _withBrowserDebuggerInternal(browser) {
        const rpcArgs = { builder: this._handle };
        if (browser !== undefined)
            rpcArgs.browser = browser;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/withBrowserDebugger', rpcArgs);
        return new NextJsAppResourceImpl(result, this._client);
    }
    /**
     * Configures a browser debugger for the JavaScript application resource, enabling browser-based debugging through a child resource that launches when the parent application is ready.
     *
     * This method creates a child `BrowserDebuggerResource` that waits for the parent JavaScript
     * application to start, then launches a browser debug session targeting the parent's HTTP or HTTPS endpoint.
     * The parent resource must have at least one HTTP or HTTPS endpoint configured.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withBrowserDebugger(options) {
        const browser = options?.browser;
        return new NextJsAppResourcePromiseImpl(this._withBrowserDebuggerInternal(browser), this._client);
    }
}
/**
 * Thenable wrapper for NextJsAppResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
class NextJsAppResourcePromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    command() {
        return this._promise.then(obj => obj.command());
    }
    workingDirectory() {
        return this._promise.then(obj => obj.workingDirectory());
    }
    name() {
        return this._promise.then(obj => obj.name());
    }
    withContainerRegistry(registry) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withContainerRegistry(registry)), this._client);
    }
    withDockerfileBaseImage(options) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileBaseImage(options)), this._client);
    }
    publishAsDockerFile(configure) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.publishAsDockerFile(configure)), this._client);
    }
    withExecutableCommand(command) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withExecutableCommand(command)), this._client);
    }
    withWorkingDirectory(workingDirectory) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withWorkingDirectory(workingDirectory)), this._client);
    }
    withMcpServer(options) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withMcpServer(options)), this._client);
    }
    withOtlpExporter(options) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withOtlpExporter(options)), this._client);
    }
    withRequiredCommand(command, options) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withRequiredCommand(command, options)), this._client);
    }
    withSessionLifetime() {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withSessionLifetime()), this._client);
    }
    withPersistentLifetime() {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withPersistentLifetime()), this._client);
    }
    withLifetimeOf(sourceBuilder) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withLifetimeOf(sourceBuilder)), this._client);
    }
    withParentProcessLifetime(parentProcessId) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withParentProcessLifetime(parentProcessId)), this._client);
    }
    withEnvironment(name, value) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withEnvironment(name, value)), this._client);
    }
    withEnvironmentCallback(callback) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withEnvironmentCallback(callback)), this._client);
    }
    withArgs(args) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withArgs(args)), this._client);
    }
    withArgsCallback(callback) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withArgsCallback(callback)), this._client);
    }
    withReferenceEnvironment(options) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withReferenceEnvironment(options)), this._client);
    }
    withReference(source, options) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withReference(source, options)), this._client);
    }
    withEndpointCallback(endpointName, callback, options) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withEndpointCallback(endpointName, callback, options)), this._client);
    }
    withHttpEndpointCallback(callback, options) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpEndpointCallback(callback, options)), this._client);
    }
    withHttpsEndpointCallback(callback, options) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpsEndpointCallback(callback, options)), this._client);
    }
    withEndpoint(options) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withEndpoint(options)), this._client);
    }
    withEndpointProxySupport(proxyEnabled) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withEndpointProxySupport(proxyEnabled)), this._client);
    }
    withHttpEndpoint(options) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpEndpoint(options)), this._client);
    }
    withHttpsEndpoint(options) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpsEndpoint(options)), this._client);
    }
    withExternalHttpEndpoints() {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withExternalHttpEndpoints()), this._client);
    }
    getEndpoint(name) {
        return new EndpointReferencePromiseImpl(this._promise.then(obj => obj.getEndpoint(name)), this._client);
    }
    asHttp2Service() {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.asHttp2Service()), this._client);
    }
    withUrls(callback) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withUrls(callback)), this._client);
    }
    withUrl(url, options) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withUrl(url, options)), this._client);
    }
    withUrlForEndpoint(endpointName, callback) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)), this._client);
    }
    withContainerFilesSource(sourcePath) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withContainerFilesSource(sourcePath)), this._client);
    }
    clearContainerFilesSources() {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.clearContainerFilesSources()), this._client);
    }
    excludeFromManifest() {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.excludeFromManifest()), this._client);
    }
    waitFor(dependency, options) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.waitFor(dependency, options)), this._client);
    }
    waitForStart(dependency, options) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.waitForStart(dependency, options)), this._client);
    }
    withExplicitStart() {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withExplicitStart()), this._client);
    }
    waitForCompletion(dependency, options) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.waitForCompletion(dependency, options)), this._client);
    }
    withHealthCheck(key) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withHealthCheck(key)), this._client);
    }
    withHttpHealthCheck(options) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpHealthCheck(options)), this._client);
    }
    withCommand(name, displayName, executeCommand, options) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)), this._client);
    }
    withProcessCommand(commandName, displayName, options) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommand(commandName, displayName, options)), this._client);
    }
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommandFactory(commandName, displayName, createProcessSpec, options)), this._client);
    }
    withHttpCommand(path, displayName, options) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpCommand(path, displayName, options)), this._client);
    }
    withDeveloperCertificateTrust(trust) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withDeveloperCertificateTrust(trust)), this._client);
    }
    withCertificateTrustScope(scope) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withCertificateTrustScope(scope)), this._client);
    }
    withHttpsDeveloperCertificate(options) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpsDeveloperCertificate(options)), this._client);
    }
    withoutHttpsCertificate() {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withoutHttpsCertificate()), this._client);
    }
    withRelationship(resourceBuilder, type) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withRelationship(resourceBuilder, type)), this._client);
    }
    withParentRelationship(parent) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withParentRelationship(parent)), this._client);
    }
    withChildRelationship(child) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withChildRelationship(child)), this._client);
    }
    withIconName(iconName, options) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withIconName(iconName, options)), this._client);
    }
    withComputeEnvironment(computeEnvironmentResource) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withComputeEnvironment(computeEnvironmentResource)), this._client);
    }
    withHttpProbe(probeType, options) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpProbe(probeType, options)), this._client);
    }
    excludeFromMcp() {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.excludeFromMcp()), this._client);
    }
    withHidden() {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withHidden()), this._client);
    }
    withHiddenOnCompletion(options) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withHiddenOnCompletion(options)), this._client);
    }
    withImagePushOptions(callback) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withImagePushOptions(callback)), this._client);
    }
    withRemoteImageName(remoteImageName) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withRemoteImageName(remoteImageName)), this._client);
    }
    withRemoteImageTag(remoteImageTag) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withRemoteImageTag(remoteImageTag)), this._client);
    }
    withPipelineStepFactory(stepName, callback, options) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)), this._client);
    }
    withPipelineConfiguration(callback) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withPipelineConfiguration(callback)), this._client);
    }
    getResourceName() {
        return this._promise.then(obj => obj.getResourceName());
    }
    onBeforeResourceStarted(callback) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.onBeforeResourceStarted(callback)), this._client);
    }
    onResourceStopped(callback) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.onResourceStopped(callback)), this._client);
    }
    onInitializeResource(callback) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.onInitializeResource(callback)), this._client);
    }
    onResourceEndpointsAllocated(callback) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.onResourceEndpointsAllocated(callback)), this._client);
    }
    onResourceReady(callback) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.onResourceReady(callback)), this._client);
    }
    createExecutionConfiguration() {
        return new ExecutionConfigurationBuilderPromiseImpl(this._promise.then(obj => obj.createExecutionConfiguration()), this._client);
    }
    publishAsStaticWebsite(options) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.publishAsStaticWebsite(options)), this._client);
    }
    publishAsNodeServer(entryPoint, options) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.publishAsNodeServer(entryPoint, options)), this._client);
    }
    publishAsPackageScript(options) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.publishAsPackageScript(options)), this._client);
    }
    disableBuildValidation() {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.disableBuildValidation()), this._client);
    }
    withNpm(options) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withNpm(options)), this._client);
    }
    withBun(options) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withBun(options)), this._client);
    }
    withYarn(options) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withYarn(options)), this._client);
    }
    withPnpm(options) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withPnpm(options)), this._client);
    }
    withBuildScript(scriptName, options) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withBuildScript(scriptName, options)), this._client);
    }
    withRunScript(scriptName, options) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withRunScript(scriptName, options)), this._client);
    }
    withBrowserDebugger(options) {
        return new NextJsAppResourcePromiseImpl(this._promise.then(obj => obj.withBrowserDebugger(options)), this._client);
    }
}
// ============================================================================
// NodeAppResourceImpl
// ============================================================================
/** A resource that represents a node application. */
class NodeAppResourceImpl extends ResourceBuilderBase {
    constructor(handle, client) {
        super(handle, client);
    }
    async command() {
        return await this._client.invokeCapability('Aspire.Hosting.JavaScript/JavaScriptAppResource.command', { context: this._handle });
    }
    async workingDirectory() {
        return await this._client.invokeCapability('Aspire.Hosting.JavaScript/JavaScriptAppResource.workingDirectory', { context: this._handle });
    }
    async name() {
        return await this._client.invokeCapability('Aspire.Hosting.JavaScript/JavaScriptAppResource.name', { context: this._handle });
    }
    /** @internal */
    async _withContainerRegistryInternal(registry) {
        registry = isPromiseLike(registry) ? await registry : registry;
        const rpcArgs = { builder: this._handle, registry };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerRegistry', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Configures the resource to use the specified container registry for container image operations.
     *
     * This method adds a `ContainerRegistryReferenceAnnotation` to the resource,
     * indicating that the resource should use the specified container registry for container image operations.
     * @param registry The container registry resource builder.
     * @returns The resource builder for chaining.
     */
    withContainerRegistry(registry) {
        return new NodeAppResourcePromiseImpl(this._withContainerRegistryInternal(registry), this._client);
    }
    /** @internal */
    async _withDockerfileBaseImageInternal(buildImage, runtimeImage) {
        const rpcArgs = { builder: this._handle };
        if (buildImage !== undefined)
            rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined)
            rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileBaseImage', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Configures custom base images for generated Dockerfiles.
     *
     * This extension method allows customization of the base images used in generated Dockerfiles.
     * For multi-stage Dockerfiles (e.g., Python with UV), you can specify separate build and runtime images.
     * Specify custom base images for a Python application:
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddPythonApp("myapp", "path/to/app", "main.py")
     * .WithDockerfileBaseImage(
     * buildImage: "ghcr.io/astral-sh/uv:python3.12-bookworm-slim",
     * runtimeImage: "python:3.12-slim-bookworm");
     * builder.Build().Run();
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileBaseImage(options) {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new NodeAppResourcePromiseImpl(this._withDockerfileBaseImageInternal(buildImage, runtimeImage), this._client);
    }
    /** @internal */
    async _publishAsDockerFileInternal(configure) {
        const configureId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new ContainerResourceImpl(objHandle, this._client);
            await configure(obj);
        });
        const rpcArgs = { builder: this._handle, configure: configureId };
        const result = await this._client.invokeCapability('Aspire.Hosting/publishAsDockerFile', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Publishes an executable as a Docker file
     *
     * When the executable resource is converted to a container resource, the arguments to the executable
     * are not used. This is because arguments to the executable often contain physical paths that are not valid
     * in the container. The container can be set up with the correct arguments using the `configure` action.
     * @param configure Optional action to configure the container resource
     * @returns The resource builder.
     */
    publishAsDockerFile(configure) {
        return new NodeAppResourcePromiseImpl(this._publishAsDockerFileInternal(configure), this._client);
    }
    /** @internal */
    async _withExecutableCommandInternal(command) {
        const rpcArgs = { builder: this._handle, command };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExecutableCommand', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Sets the command for the executable resource.
     * @param command Command.
     * @returns The resource builder.
     */
    withExecutableCommand(command) {
        return new NodeAppResourcePromiseImpl(this._withExecutableCommandInternal(command), this._client);
    }
    /** @internal */
    async _withWorkingDirectoryInternal(workingDirectory) {
        const rpcArgs = { builder: this._handle, workingDirectory };
        const result = await this._client.invokeCapability('Aspire.Hosting/withWorkingDirectory', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Sets the working directory for the executable resource.
     * @param workingDirectory Working directory.
     * @returns The resource builder.
     */
    withWorkingDirectory(workingDirectory) {
        return new NodeAppResourcePromiseImpl(this._withWorkingDirectoryInternal(workingDirectory), this._client);
    }
    /** @internal */
    async _withMcpServerInternal(path, endpointName) {
        const rpcArgs = { builder: this._handle };
        if (path !== undefined)
            rpcArgs.path = path;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withMcpServer', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Marks the resource as hosting a Model Context Protocol (MCP) server on the specified endpoint.
     *
     * This method adds an `McpServerEndpointAnnotation` to the resource, enabling the Aspire tooling
     * to discover and proxy the MCP server exposed by the resource.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withMcpServer(options) {
        const path = options?.path;
        const endpointName = options?.endpointName;
        return new NodeAppResourcePromiseImpl(this._withMcpServerInternal(path, endpointName), this._client);
    }
    /** @internal */
    async _withOtlpExporterInternal(protocol) {
        const rpcArgs = { builder: this._handle };
        if (protocol !== undefined)
            rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability('Aspire.Hosting/withOtlpExporter', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Configures OTLP telemetry export
     * @param options Additional options.
     */
    withOtlpExporter(options) {
        const protocol = options?.protocol;
        return new NodeAppResourcePromiseImpl(this._withOtlpExporterInternal(protocol), this._client);
    }
    /** @internal */
    async _withRequiredCommandInternal(command, helpLink) {
        const rpcArgs = { builder: this._handle, command };
        if (helpLink !== undefined)
            rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability('Aspire.Hosting/withRequiredCommand', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Declares that a resource requires a specific command/executable to be available on the local machine PATH before it can start.
     *
     * The command is considered valid if either:
     * 1. It is an absolute or relative path (contains a directory separator) that points to an existing file, or
     * 2. It is discoverable on the current process PATH (respecting PATHEXT on Windows).
     * If the command is not found, a warning message will be logged but the resource will be allowed to attempt to start.
     * @param command The command string (file name or path) that should be validated.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withRequiredCommand(command, options) {
        const helpLink = options?.helpLink;
        return new NodeAppResourcePromiseImpl(this._withRequiredCommandInternal(command, helpLink), this._client);
    }
    /** @internal */
    async _withSessionLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withSessionLifetime', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a session lifetime.
     * @returns The resource builder.
     */
    withSessionLifetime() {
        return new NodeAppResourcePromiseImpl(this._withSessionLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withPersistentLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPersistentLifetime', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime.
     * @returns The resource builder.
     */
    withPersistentLifetime() {
        return new NodeAppResourcePromiseImpl(this._withPersistentLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withLifetimeOfInternal(sourceBuilder) {
        sourceBuilder = isPromiseLike(sourceBuilder) ? await sourceBuilder : sourceBuilder;
        const rpcArgs = { builder: this._handle, sourceBuilder };
        const result = await this._client.invokeCapability('Aspire.Hosting/withLifetimeOf', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to match the lifetime of another resource.
     *
     * The resource lifetime is evaluated from `sourceBuilder` when the application model is prepared, so later lifetime
     * changes to the source resource are reflected by this resource.
     * @param sourceBuilder The resource builder whose lifetime should be used.
     * @returns The resource builder.
     */
    withLifetimeOf(sourceBuilder) {
        return new NodeAppResourcePromiseImpl(this._withLifetimeOfInternal(sourceBuilder), this._client);
    }
    /** @internal */
    async _withParentProcessLifetimeInternal(parentProcessId) {
        const rpcArgs = { builder: this._handle, parentProcessId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withParentProcessLifetime', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime that ends when a parent process exits.
     * @param parentProcessId The ID of the parent process to monitor.
     * @returns The resource builder.
     */
    withParentProcessLifetime(parentProcessId) {
        return new NodeAppResourcePromiseImpl(this._withParentProcessLifetimeInternal(parentProcessId), this._client);
    }
    /** @internal */
    async _withEnvironmentInternal(name, value) {
        value = isPromiseLike(value) ? await value : value;
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEnvironment', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /** Sets an environment variable */
    withEnvironment(name, value) {
        return new NodeAppResourcePromiseImpl(this._withEnvironmentInternal(name, value), this._client);
    }
    /** @internal */
    async _withEnvironmentCallbackInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new EnvironmentCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEnvironmentCallback', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Allows for the population of environment variables on a resource.
     * @param callback A callback that allows for deferred execution for computing many environment variables. This runs after resources have been allocated by the orchestrator and allows access to other resources to resolve computed data, e.g. connection strings, ports.
     * @returns The resource builder.
     */
    withEnvironmentCallback(callback) {
        return new NodeAppResourcePromiseImpl(this._withEnvironmentCallbackInternal(callback), this._client);
    }
    /** @internal */
    async _withArgsInternal(args) {
        const rpcArgs = { builder: this._handle, args };
        const result = await this._client.invokeCapability('Aspire.Hosting/withArgs', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Adds arguments to be passed to a resource that supports arguments when it is launched.
     * @param args The arguments to be passed to the resource when it is started.
     * @returns The resource builder.
     */
    withArgs(args) {
        return new NodeAppResourcePromiseImpl(this._withArgsInternal(args), this._client);
    }
    /** @internal */
    async _withArgsCallbackInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new CommandLineArgsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withArgsCallback', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Adds a callback to be executed with a list of command-line arguments when a resource is started.
     * @param callback A callback that allows for deferred execution for computing arguments. This runs after resources have been allocated by the orchestrator and allows access to other resources to resolve computed data, e.g. connection strings, ports.
     * @returns The resource builder.
     */
    withArgsCallback(callback) {
        return new NodeAppResourcePromiseImpl(this._withArgsCallbackInternal(callback), this._client);
    }
    /** @internal */
    async _withReferenceEnvironmentInternal(options) {
        const rpcArgs = { builder: this._handle, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withReferenceEnvironment', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Configures how information is injected into environment variables when the resource references other resources.
     * @param options Options controlling which reference information is emitted.
     * @returns The resource builder.
     */
    withReferenceEnvironment(options) {
        return new NodeAppResourcePromiseImpl(this._withReferenceEnvironmentInternal(options), this._client);
    }
    /** @internal */
    async _withReferenceInternal(source, connectionName, optional, name) {
        source = isPromiseLike(source) ? await source : source;
        const rpcArgs = { builder: this._handle, source };
        if (connectionName !== undefined)
            rpcArgs.connectionName = connectionName;
        if (optional !== undefined)
            rpcArgs.optional = optional;
        if (name !== undefined)
            rpcArgs.name = name;
        const result = await this._client.invokeCapability('Aspire.Hosting/withReference', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Adds a reference to another resource
     * @param options Additional options.
     */
    withReference(source, options) {
        const connectionName = options?.connectionName;
        const optional = options?.optional;
        const name = options?.name;
        return new NodeAppResourcePromiseImpl(this._withReferenceInternal(source, connectionName, optional, name), this._client);
    }
    /** @internal */
    async _withEndpointCallbackInternal(endpointName, callback, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpointCallback', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Updates a named endpoint via callback
     * @param options Additional options.
     */
    withEndpointCallback(endpointName, callback, options) {
        const createIfNotExists = options?.createIfNotExists;
        return new NodeAppResourcePromiseImpl(this._withEndpointCallbackInternal(endpointName, callback, createIfNotExists), this._client);
    }
    /** @internal */
    async _withHttpEndpointCallbackInternal(callback, name, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        if (name !== undefined)
            rpcArgs.name = name;
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpEndpointCallback', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Updates an HTTP endpoint via callback
     * @param options Additional options.
     */
    withHttpEndpointCallback(callback, options) {
        const name = options?.name;
        const createIfNotExists = options?.createIfNotExists;
        return new NodeAppResourcePromiseImpl(this._withHttpEndpointCallbackInternal(callback, name, createIfNotExists), this._client);
    }
    /** @internal */
    async _withHttpsEndpointCallbackInternal(callback, name, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        if (name !== undefined)
            rpcArgs.name = name;
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpsEndpointCallback', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Updates an HTTPS endpoint via callback
     * @param options Additional options.
     */
    withHttpsEndpointCallback(callback, options) {
        const name = options?.name;
        const createIfNotExists = options?.createIfNotExists;
        return new NodeAppResourcePromiseImpl(this._withHttpsEndpointCallbackInternal(callback, name, createIfNotExists), this._client);
    }
    /** @internal */
    async _withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (scheme !== undefined)
            rpcArgs.scheme = scheme;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        if (isExternal !== undefined)
            rpcArgs.isExternal = isExternal;
        if (protocol !== undefined)
            rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpoint', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Adds a network endpoint
     * @param options Additional options.
     * @returns The resource builder.
     */
    withEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const scheme = options?.scheme;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        const isExternal = options?.isExternal;
        const protocol = options?.protocol;
        return new NodeAppResourcePromiseImpl(this._withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol), this._client);
    }
    /** @internal */
    async _withEndpointProxySupportInternal(proxyEnabled) {
        const rpcArgs = { builder: this._handle, proxyEnabled };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpointProxySupport', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Set whether a resource can use proxied endpoints or whether they should be disabled for all endpoints belonging to the resource. If set to `false`, endpoints belonging to the resource will ignore the configured proxy settings and run proxy-less.
     *
     * This method is intended to support scenarios with persistent lifetime resources where it is desirable for the resource to be accessible over the same
     * port whether the Aspire application is running or not. Proxied endpoints bind ports that are only accessible while the Aspire application is running.
     * The user needs to be careful to ensure that endpoints are using unique ports when disabling proxy support as by default for proxy-less
     * endpoints, Aspire will allocate the target port as the host port, which will increase the chance of port conflicts.
     * @param proxyEnabled Should endpoints for the resource support using a proxy?
     * @returns The resource builder.
     */
    withEndpointProxySupport(proxyEnabled) {
        return new NodeAppResourcePromiseImpl(this._withEndpointProxySupportInternal(proxyEnabled), this._client);
    }
    /** @internal */
    async _withHttpEndpointInternal(port, targetPort, name, env, isProxied) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpEndpoint', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP endpoint
     *
     * If an endpoint with the same name already exists on the resource, the existing endpoint is updated
     * with any non-null parameter values. Parameters left as `null` will not modify the existing endpoint's values.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new NodeAppResourcePromiseImpl(this._withHttpEndpointInternal(port, targetPort, name, env, isProxied), this._client);
    }
    /** @internal */
    async _withHttpsEndpointInternal(port, targetPort, name, env, isProxied) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpsEndpoint', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTPS endpoint
     *
     * If an endpoint with the same name already exists on the resource, the existing endpoint is updated
     * with any non-null parameter values. Parameters left as `null` will not modify the existing endpoint's values.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpsEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new NodeAppResourcePromiseImpl(this._withHttpsEndpointInternal(port, targetPort, name, env, isProxied), this._client);
    }
    /** @internal */
    async _withExternalHttpEndpointsInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExternalHttpEndpoints', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Marks existing http or https endpoints on a resource as external.
     * @returns The resource builder.
     */
    withExternalHttpEndpoints() {
        return new NodeAppResourcePromiseImpl(this._withExternalHttpEndpointsInternal(), this._client);
    }
    /**
     * Gets an endpoint reference
     * @param name The name of the endpoint.
     * @returns An `EndpointReference` that can be used to resolve the address of the endpoint after resource allocation has occurred.
     */
    getEndpoint(name) {
        const promise = (async () => {
            const rpcArgs = { builder: this._handle, name };
            const handle = await this._client.invokeCapability('Aspire.Hosting/getEndpoint', rpcArgs);
            return new EndpointReferenceImpl(handle, this._client);
        })();
        return new EndpointReferencePromiseImpl(promise, this._client);
    }
    /** @internal */
    async _asHttp2ServiceInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/asHttp2Service', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to mark all endpoints' transport as HTTP/2. This is useful for HTTP/2 services that need prior knowledge.
     * @returns The resource builder.
     */
    asHttp2Service() {
        return new NodeAppResourcePromiseImpl(this._asHttp2ServiceInternal(), this._client);
    }
    /** @internal */
    async _withUrlsInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new ResourceUrlsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrls', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to customize the URLs displayed for the resource.
     * @param callback The callback that will customize URLs for the resource.
     * @returns The resource builder.
     */
    withUrls(callback) {
        return new NodeAppResourcePromiseImpl(this._withUrlsInternal(callback), this._client);
    }
    /** @internal */
    async _withUrlInternal(url, displayText) {
        const rpcArgs = { builder: this._handle, url };
        if (displayText !== undefined)
            rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrl', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Adds or modifies displayed URLs
     * @param options Additional options.
     */
    withUrl(url, options) {
        const displayText = options?.displayText;
        return new NodeAppResourcePromiseImpl(this._withUrlInternal(url, displayText), this._client);
    }
    /** @internal */
    async _withUrlForEndpointInternal(endpointName, callback) {
        const callbackId = registerCallback(async (objData) => {
            const obj = wrapIfHandle(objData);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrlForEndpoint', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to update the URL displayed for the endpoint with the specified name.
     * @param endpointName The name of the endpoint to customize the URL for.
     * @param callback The callback that will customize the URL.
     * @returns The resource builder.
     */
    withUrlForEndpoint(endpointName, callback) {
        return new NodeAppResourcePromiseImpl(this._withUrlForEndpointInternal(endpointName, callback), this._client);
    }
    /** @internal */
    async _publishWithContainerFilesInternal(source, destinationPath) {
        source = isPromiseLike(source) ? await source : source;
        const rpcArgs = { builder: this._handle, source, destinationPath };
        const result = await this._client.invokeCapability('Aspire.Hosting/publishWithContainerFilesFromResource', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Configures the resource to copy container files from the specified source resource during publishing.
     * @param source The resource which contains the container files to be copied.
     * @param destinationPath The destination path within the resource's container where the files will be copied.
     */
    publishWithContainerFiles(source, destinationPath) {
        return new NodeAppResourcePromiseImpl(this._publishWithContainerFilesInternal(source, destinationPath), this._client);
    }
    /** @internal */
    async _withContainerFilesSourceInternal(sourcePath) {
        const rpcArgs = { builder: this._handle, sourcePath };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerFilesSource', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Adds a container files source annotation to the resource being built, specifying the path to the container files source.
     * @param sourcePath The path to the container files source to associate with the resource. Cannot be null.
     * @returns The resource builder instance with the container files source annotation applied.
     */
    withContainerFilesSource(sourcePath) {
        return new NodeAppResourcePromiseImpl(this._withContainerFilesSourceInternal(sourcePath), this._client);
    }
    /** @internal */
    async _clearContainerFilesSourcesInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/clearContainerFilesSources', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Removes any container files source annotation from the resource being built.
     * @returns The resource builder instance with the container files source annotation applied.
     */
    clearContainerFilesSources() {
        return new NodeAppResourcePromiseImpl(this._clearContainerFilesSourcesInternal(), this._client);
    }
    /** @internal */
    async _excludeFromManifestInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromManifest', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Excludes a resource from being published to the manifest.
     * @returns The resource builder.
     */
    excludeFromManifest() {
        return new NodeAppResourcePromiseImpl(this._excludeFromManifestInternal(), this._client);
    }
    /** @internal */
    async _waitForInternal(dependency, waitBehavior) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (waitBehavior !== undefined)
            rpcArgs.waitBehavior = waitBehavior;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitFor', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Waits for another resource to be ready
     * @param options Additional options.
     */
    waitFor(dependency, options) {
        const waitBehavior = options?.waitBehavior;
        return new NodeAppResourcePromiseImpl(this._waitForInternal(dependency, waitBehavior), this._client);
    }
    /** @internal */
    async _waitForStartInternal(dependency, waitBehavior) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (waitBehavior !== undefined)
            rpcArgs.waitBehavior = waitBehavior;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitForStart', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Waits for another resource to start
     * @param options Additional options.
     */
    waitForStart(dependency, options) {
        const waitBehavior = options?.waitBehavior;
        return new NodeAppResourcePromiseImpl(this._waitForStartInternal(dependency, waitBehavior), this._client);
    }
    /** @internal */
    async _withExplicitStartInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExplicitStart', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Prevents resource from starting automatically
     * @returns The resource builder.
     */
    withExplicitStart() {
        return new NodeAppResourcePromiseImpl(this._withExplicitStartInternal(), this._client);
    }
    /** @internal */
    async _waitForCompletionInternal(dependency, exitCode) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitForResourceCompletion', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Waits for the dependency resource to enter the Exited or Finished state before starting the resource.
     * @param dependency The resource builder for the dependency resource.
     * @param options Additional options.
     * @returns The resource builder.
     */
    waitForCompletion(dependency, options) {
        const exitCode = options?.exitCode;
        return new NodeAppResourcePromiseImpl(this._waitForCompletionInternal(dependency, exitCode), this._client);
    }
    /** @internal */
    async _withHealthCheckInternal(key) {
        const rpcArgs = { builder: this._handle, key };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHealthCheck', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Adds a health check by key
     * @param key The key for the health check.
     * @returns The resource builder.
     */
    withHealthCheck(key) {
        return new NodeAppResourcePromiseImpl(this._withHealthCheckInternal(key), this._client);
    }
    /** @internal */
    async _withHttpHealthCheckInternal(path, statusCode, endpointName) {
        const rpcArgs = { builder: this._handle };
        if (path !== undefined)
            rpcArgs.path = path;
        if (statusCode !== undefined)
            rpcArgs.statusCode = statusCode;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpHealthCheck', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Adds a health check to the resource which is mapped to a specific endpoint.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpHealthCheck(options) {
        const path = options?.path;
        const statusCode = options?.statusCode;
        const endpointName = options?.endpointName;
        return new NodeAppResourcePromiseImpl(this._withHttpHealthCheckInternal(path, statusCode, endpointName), this._client);
    }
    /** @internal */
    async _withCommandInternal(name, displayName, executeCommand, commandOptions) {
        const executeCommandId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined)
            rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability('Aspire.Hosting/withCommand', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Adds a resource command
     *
     * The `WithCommand` method is used to add commands to the resource. Commands are displayed in the dashboard
     * and can be executed by a user using the dashboard UI.
     * When a command is executed, the `executeCommand` callback is called and is run inside the Aspire host.
     * @param name The name of the command. The name uniquely identifies the command.
     * @param displayName The display name visible in UI.
     * @param executeCommand A callback that is executed when the command is executed. The callback is run inside the Aspire host. The callback result is used to indicate success or failure in the UI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withCommand(name, displayName, executeCommand, options) {
        const commandOptions = options?.commandOptions;
        return new NodeAppResourcePromiseImpl(this._withCommandInternal(name, displayName, executeCommand, commandOptions), this._client);
    }
    /** @internal */
    async _withProcessCommandInternal(commandName, displayName, options) {
        const rpcArgs = { builder: this._handle, commandName, displayName, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommand', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /** Adds a command to the resource that starts a local process when invoked. */
    withProcessCommand(commandName, displayName, options) {
        return new NodeAppResourcePromiseImpl(this._withProcessCommandInternal(commandName, displayName, options), this._client);
    }
    /** @internal */
    async _withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options) {
        const createProcessSpecId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await createProcessSpec(arg);
        });
        const rpcArgs = { builder: this._handle, commandName, displayName, createProcessSpec: createProcessSpecId };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommandFactory', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Adds a command to the resource that starts a local process created by a callback when invoked.
     * @param options Additional options.
     */
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new NodeAppResourcePromiseImpl(this._withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options), this._client);
    }
    /** @internal */
    async _withHttpCommandInternal(path, displayName, options) {
        const rpcArgs = { builder: this._handle, path, displayName };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpCommand', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP resource command
     * @param options Additional options.
     */
    withHttpCommand(path, displayName, options) {
        return new NodeAppResourcePromiseImpl(this._withHttpCommandInternal(path, displayName, options), this._client);
    }
    /** @internal */
    async _withDeveloperCertificateTrustInternal(trust) {
        const rpcArgs = { builder: this._handle, trust };
        const result = await this._client.invokeCapability('Aspire.Hosting/withDeveloperCertificateTrust', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Indicates whether developer certificates should be treated as trusted certificate authorities for the resource at run time. Currently this indicates trust for the ASP.NET Core developer certificate. The developer certificate will only be trusted when running in local development scenarios; in publish mode resources will use their default certificate trust.
     *
     * Disable trust for app host managed developer certificate(s) for a container resource.
     * ```
     * var container = builder.AddContainer("my-service", "my-service:latest")
     * .WithDeveloperCertificateTrust(false);
     * ```
     * Disable automatic trust for app host managed developer certificate(s), but explicitly enable it for a specific resource.
     * ```
     * var builder = DistributedApplication.CreateBuilder(new DistributedApplicationOptions()
     * {
     * Args = args,
     * TrustDeveloperCertificate = false,
     * });
     * var project = builder.AddProject<MyService>("my-service")
     * .WithDeveloperCertificateTrust(true);
     * ```
     * @param trust Indicates whether the developer certificate should be treated as trusted.
     * @returns The resource builder.
     */
    withDeveloperCertificateTrust(trust) {
        return new NodeAppResourcePromiseImpl(this._withDeveloperCertificateTrustInternal(trust), this._client);
    }
    /** @internal */
    async _withCertificateTrustScopeInternal(scope) {
        const rpcArgs = { builder: this._handle, scope };
        const result = await this._client.invokeCapability('Aspire.Hosting/withCertificateTrustScope', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Sets the certificate trust scope
     *
     * The default scope if not overridden is `Append` which means that custom certificate
     * authorities should be appended to the default trusted certificate authorities for the resource. Setting the scope to
     * `Override` indicates the set of certificates in referenced
     * `CertificateAuthorityCollection` (and optionally Aspire developer certificiates) should be used as the
     * exclusive source of trust for a resource.
     * In all cases, this is a best effort implementation as not all resources support full customization of certificate
     * trust.
     * Set the scope for custom certificate authorities to override the default trusted certificate authorities for a container resource.
     * ```
     * var caCollection = builder.AddCertificateAuthorityCollection("my-cas")
     * .WithCertificate(new X509Certificate2("my-ca.pem"));
     * var container = builder.AddContainer("my-service", "my-service:latest")
     * .WithCertificateAuthorityCollection(caCollection)
     * .WithCertificateTrustScope(CertificateTrustScope.Override);
     * ```
     * @param scope The scope to apply to custom certificate authorities associated with the resource.
     * @returns The resource builder.
     */
    withCertificateTrustScope(scope) {
        return new NodeAppResourcePromiseImpl(this._withCertificateTrustScopeInternal(scope), this._client);
    }
    /** @internal */
    async _withHttpsDeveloperCertificateInternal(password) {
        password = isPromiseLike(password) ? await password : password;
        const rpcArgs = { builder: this._handle };
        if (password !== undefined)
            rpcArgs.password = password;
        const result = await this._client.invokeCapability('Aspire.Hosting/withParameterHttpsDeveloperCertificate', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Indicates that a resource should use the developer certificate key pair for HTTPS endpoints at run time. Currently this indicates use of the ASP.NET Core developer certificate. The developer certificate will only be used when running in local development scenarios; in publish mode resources will use their default certificate configuration.
     *
     * Use the developer certificate for HTTPS/TLS endpoints on a container resource:
     * ```
     * builder.AddContainer("my-service", "my-image")
     * .WithHttpsDeveloperCertificate()
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpsDeveloperCertificate(options) {
        let password = options?.password;
        return new NodeAppResourcePromiseImpl(this._withHttpsDeveloperCertificateInternal(password), this._client);
    }
    /** @internal */
    async _withoutHttpsCertificateInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withoutHttpsCertificate', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Disable HTTPS/TLS server certificate configuration for the resource. No HTTPS/TLS termination configuration will be applied.
     *
     * Disable HTTPS certificate configuration for a Redis resource:
     * ```
     * var redis = builder.AddRedis("cache")
     * .WithoutHttpsCertificate();
     * ```
     * @returns The resource builder.
     */
    withoutHttpsCertificate() {
        return new NodeAppResourcePromiseImpl(this._withoutHttpsCertificateInternal(), this._client);
    }
    /** @internal */
    async _withRelationshipInternal(resourceBuilder, type) {
        resourceBuilder = isPromiseLike(resourceBuilder) ? await resourceBuilder : resourceBuilder;
        const rpcArgs = { builder: this._handle, resourceBuilder, type };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderRelationship', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Adds a relationship to another resource using its builder.
     * @param resourceBuilder The resource builder that the relationship is to.
     * @param type The relationship type.
     * @returns A resource builder.
     */
    withRelationship(resourceBuilder, type) {
        return new NodeAppResourcePromiseImpl(this._withRelationshipInternal(resourceBuilder, type), this._client);
    }
    /** @internal */
    async _withParentRelationshipInternal(parent) {
        parent = isPromiseLike(parent) ? await parent : parent;
        const rpcArgs = { builder: this._handle, parent };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderParentRelationship', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Sets the parent relationship
     * @param parent The parent of `builder`.
     * @returns A resource builder.
     */
    withParentRelationship(parent) {
        return new NodeAppResourcePromiseImpl(this._withParentRelationshipInternal(parent), this._client);
    }
    /** @internal */
    async _withChildRelationshipInternal(child) {
        child = isPromiseLike(child) ? await child : child;
        const rpcArgs = { builder: this._handle, child };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderChildRelationship', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Sets a child relationship
     * @param child The child of `builder`.
     * @returns A resource builder.
     */
    withChildRelationship(child) {
        return new NodeAppResourcePromiseImpl(this._withChildRelationshipInternal(child), this._client);
    }
    /** @internal */
    async _withIconNameInternal(iconName, iconVariant) {
        const rpcArgs = { builder: this._handle, iconName };
        if (iconVariant !== undefined)
            rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability('Aspire.Hosting/withIconName', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Specifies the icon to use when displaying the resource in the dashboard.
     * @param iconName The name of the FluentUI icon to use. See https://aka.ms/fluentui-system-icons for available icons.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withIconName(iconName, options) {
        const iconVariant = options?.iconVariant;
        return new NodeAppResourcePromiseImpl(this._withIconNameInternal(iconName, iconVariant), this._client);
    }
    /** @internal */
    async _withComputeEnvironmentInternal(computeEnvironmentResource) {
        computeEnvironmentResource = isPromiseLike(computeEnvironmentResource) ? await computeEnvironmentResource : computeEnvironmentResource;
        const rpcArgs = { builder: this._handle, computeEnvironmentResource };
        const result = await this._client.invokeCapability('Aspire.Hosting/withComputeEnvironment', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Configures the compute environment for the compute resource.
     *
     * This method allows associating a specific compute environment with the compute resource.
     * @param computeEnvironmentResource The compute environment resource to associate with the compute resource.
     * @returns The resource builder.
     */
    withComputeEnvironment(computeEnvironmentResource) {
        return new NodeAppResourcePromiseImpl(this._withComputeEnvironmentInternal(computeEnvironmentResource), this._client);
    }
    /** @internal */
    async _withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName) {
        const rpcArgs = { builder: this._handle, probeType };
        if (path !== undefined)
            rpcArgs.path = path;
        if (initialDelaySeconds !== undefined)
            rpcArgs.initialDelaySeconds = initialDelaySeconds;
        if (periodSeconds !== undefined)
            rpcArgs.periodSeconds = periodSeconds;
        if (timeoutSeconds !== undefined)
            rpcArgs.timeoutSeconds = timeoutSeconds;
        if (failureThreshold !== undefined)
            rpcArgs.failureThreshold = failureThreshold;
        if (successThreshold !== undefined)
            rpcArgs.successThreshold = successThreshold;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpProbe', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP health probe to the resource
     * @param options Additional options.
     */
    withHttpProbe(probeType, options) {
        const path = options?.path;
        const initialDelaySeconds = options?.initialDelaySeconds;
        const periodSeconds = options?.periodSeconds;
        const timeoutSeconds = options?.timeoutSeconds;
        const failureThreshold = options?.failureThreshold;
        const successThreshold = options?.successThreshold;
        const endpointName = options?.endpointName;
        return new NodeAppResourcePromiseImpl(this._withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName), this._client);
    }
    /** @internal */
    async _excludeFromMcpInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromMcp', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Exclude the resource from MCP operations using the Aspire MCP server. The resource is excluded from results that return resources, console logs and telemetry.
     * @returns The resource builder.
     */
    excludeFromMcp() {
        return new NodeAppResourcePromiseImpl(this._excludeFromMcpInternal(), this._client);
    }
    /** @internal */
    async _withHiddenInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHidden', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists
     *
     * Use this method to hide resources that are implementation details and should never be displayed by default.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @returns The resource builder.
     */
    withHidden() {
        return new NodeAppResourcePromiseImpl(this._withHiddenInternal(), this._client);
    }
    /** @internal */
    async _withHiddenOnCompletionInternal(exitCode, exitCodes) {
        const rpcArgs = { builder: this._handle };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        if (exitCodes !== undefined)
            rpcArgs.exitCodes = exitCodes;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHiddenOnCompletion', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists after successful completion
     *
     * This method is useful for one-off resources such as setup scripts, migrations, or build steps that should remain visible while running
     * and then be hidden after successful completion.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHiddenOnCompletion(options) {
        const exitCode = options?.exitCode;
        const exitCodes = options?.exitCodes;
        return new NodeAppResourcePromiseImpl(this._withHiddenOnCompletionInternal(exitCode, exitCodes), this._client);
    }
    /** @internal */
    async _withImagePushOptionsInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ContainerImagePushOptionsCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImagePushOptions', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Adds an asynchronous callback to configure container image push options for the resource.
     *
     * This method allows customization of how container images are named and tagged when pushed to a registry using an asynchronous callback.
     * Use this overload when the callback needs to perform asynchronous operations such as retrieving configuration values from external sources.
     * The callback receives a `ContainerImagePushOptionsCallbackContext` that provides access to the resource
     * and the `ContainerImagePushOptions` that can be modified.
     * Multiple callbacks can be registered on the same resource, and they will be invoked in the order they were added.
     * @param callback The asynchronous callback to configure push options.
     * @returns The resource builder.
     */
    withImagePushOptions(callback) {
        return new NodeAppResourcePromiseImpl(this._withImagePushOptionsInternal(callback), this._client);
    }
    /** @internal */
    async _withRemoteImageNameInternal(remoteImageName) {
        const rpcArgs = { builder: this._handle, remoteImageName };
        const result = await this._client.invokeCapability('Aspire.Hosting/withRemoteImageName', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Sets the remote image name (without registry endpoint or tag) for container push operations.
     *
     * Use this with `withRemoteImageTag` to fully customize the image reference used for container push operations.
     * @param remoteImageName The remote image name (e.g., "myapp" or "myorg/myapp").
     * @returns The resource builder.
     */
    withRemoteImageName(remoteImageName) {
        return new NodeAppResourcePromiseImpl(this._withRemoteImageNameInternal(remoteImageName), this._client);
    }
    /** @internal */
    async _withRemoteImageTagInternal(remoteImageTag) {
        const rpcArgs = { builder: this._handle, remoteImageTag };
        const result = await this._client.invokeCapability('Aspire.Hosting/withRemoteImageTag', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Sets the remote image tag for container push operations.
     *
     * Use this with `withRemoteImageName` to fully customize the image reference used for container push operations.
     * @param remoteImageTag The remote image tag (e.g., "latest", "v1.0.0").
     * @returns The resource builder.
     */
    withRemoteImageTag(remoteImageTag) {
        return new NodeAppResourcePromiseImpl(this._withRemoteImageTagInternal(remoteImageTag), this._client);
    }
    /** @internal */
    async _withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new PipelineStepContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined)
            rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined)
            rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined)
            rpcArgs.tags = tags;
        if (description !== undefined)
            rpcArgs.description = description;
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineStepFactory', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Adds a pipeline step to the resource that will be executed during deployment.
     * @param stepName The unique name of the pipeline step.
     * @param callback The callback to execute when the step runs.
     * @param options Additional options.
     * @returns The resource builder for chaining.
     */
    withPipelineStepFactory(stepName, callback, options) {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new NodeAppResourcePromiseImpl(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description), this._client);
    }
    /** @internal */
    async _withPipelineConfigurationInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new PipelineConfigurationContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineConfiguration', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to be executed during the pipeline configuration phase, allowing modification of step dependencies and relationships.
     * @param callback The callback function to execute during the configuration phase.
     * @returns The resource builder for chaining.
     */
    withPipelineConfiguration(callback) {
        return new NodeAppResourcePromiseImpl(this._withPipelineConfigurationInternal(callback), this._client);
    }
    /**
     * Gets the name of the resource from a builder.
     *
     * Why this wrapper exists: This capability accesses a nested property
     * (`resource.Resource.Name`) which requires a wrapper method. There is no single
     * .NET method that returns just the resource name that could be annotated directly.
     * @returns The resource name.
     */
    async getResourceName() {
        const rpcArgs = { resource: this._handle };
        return await this._client.invokeCapability('Aspire.Hosting/getResourceName', rpcArgs);
    }
    /** @internal */
    async _onBeforeResourceStartedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new BeforeResourceStartedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onBeforeResourceStarted', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the BeforeResourceStarted event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onBeforeResourceStarted(callback) {
        return new NodeAppResourcePromiseImpl(this._onBeforeResourceStartedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceStoppedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceStoppedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceStopped', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceStopped event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceStopped(callback) {
        return new NodeAppResourcePromiseImpl(this._onResourceStoppedInternal(callback), this._client);
    }
    /** @internal */
    async _onInitializeResourceInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new InitializeResourceEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onInitializeResource', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the InitializeResource event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onInitializeResource(callback) {
        return new NodeAppResourcePromiseImpl(this._onInitializeResourceInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceEndpointsAllocatedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceEndpointsAllocatedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceEndpointsAllocated', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceEndpointsAllocated event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceEndpointsAllocated(callback) {
        return new NodeAppResourcePromiseImpl(this._onResourceEndpointsAllocatedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceReadyInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceReadyEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceReady', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceReady event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceReady(callback) {
        return new NodeAppResourcePromiseImpl(this._onResourceReadyInternal(callback), this._client);
    }
    /**
     * Creates an execution configuration builder for the specified resource.
     * @returns The execution configuration builder.
     */
    createExecutionConfiguration() {
        const promise = (async () => {
            const rpcArgs = { resource: this._handle };
            const handle = await this._client.invokeCapability('Aspire.Hosting/createExecutionConfiguration', rpcArgs);
            return new ExecutionConfigurationBuilderImpl(handle, this._client);
        })();
        return new ExecutionConfigurationBuilderPromiseImpl(promise, this._client);
    }
    /** @internal */
    async _publishAsStaticWebsiteInternal(apiPath, apiTarget, outputPath, stripPrefix, targetEndpointName) {
        apiTarget = isPromiseLike(apiTarget) ? await apiTarget : apiTarget;
        const rpcArgs = { builder: this._handle };
        if (apiPath !== undefined)
            rpcArgs.apiPath = apiPath;
        if (apiTarget !== undefined)
            rpcArgs.apiTarget = apiTarget;
        if (outputPath !== undefined)
            rpcArgs.outputPath = outputPath;
        if (stripPrefix !== undefined)
            rpcArgs.stripPrefix = stripPrefix;
        if (targetEndpointName !== undefined)
            rpcArgs.targetEndpointName = targetEndpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/publishAsStaticWebsite', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Publishes the JavaScript application as a standalone static website using YARP.
     * @param options Additional options.
     */
    publishAsStaticWebsite(options) {
        const apiPath = options?.apiPath;
        let apiTarget = options?.apiTarget;
        const outputPath = options?.outputPath;
        const stripPrefix = options?.stripPrefix;
        const targetEndpointName = options?.targetEndpointName;
        return new NodeAppResourcePromiseImpl(this._publishAsStaticWebsiteInternal(apiPath, apiTarget, outputPath, stripPrefix, targetEndpointName), this._client);
    }
    /** @internal */
    async _publishAsNodeServerInternal(entryPoint, outputPath) {
        const rpcArgs = { builder: this._handle, entryPoint };
        if (outputPath !== undefined)
            rpcArgs.outputPath = outputPath;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/publishAsNodeServer', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Configures the JavaScript application to publish as a standalone Node.js server that runs a built artifact directly.
     *
     * Use this method for frameworks that produce a Node.js server artifact during the build and recommend
     * running that artifact directly in production rather than invoking a package manager script at runtime.
     * The application source is still built using the configured package manager and build script; this method
     * only changes the publish-time runtime container shape.
     * The container files source path is automatically set to `outputPath` so that only
     * the built output directory is copied into the runtime container, not the full application source.
     * @param entryPoint The relative path to the Node.js entry point to execute in the published container after the build completes, such as `.output/server/index.js` or `build/index.js`.
     * @param options Additional options.
     * @returns The updated resource builder.
     */
    publishAsNodeServer(entryPoint, options) {
        const outputPath = options?.outputPath;
        return new NodeAppResourcePromiseImpl(this._publishAsNodeServerInternal(entryPoint, outputPath), this._client);
    }
    /** @internal */
    async _publishAsPackageScriptInternal(scriptName, runScriptArguments) {
        const rpcArgs = { builder: this._handle };
        if (scriptName !== undefined)
            rpcArgs.scriptName = scriptName;
        if (runScriptArguments !== undefined)
            rpcArgs.runScriptArguments = runScriptArguments;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/publishAsPackageScript', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Configures the JavaScript application to publish as a Node.js server that uses a `package.json` script at runtime.
     *
     * Use this method for frameworks where the production server depends on packages in `node_modules` at runtime.
     * The resulting container includes the full application with production dependencies installed.
     * This method is appropriate for frameworks like Nuxt (where `useAsyncData`/`useFetch` requires the
     * full Nitro environment), Remix (where `react-router-serve` is an npm dependency), and Astro SSR
     * (where the built entry point imports unbundled `@astrojs/*` packages).
     * For frameworks that produce a self-contained server artifact that does not require `node_modules`,
     * use `PublishAsNodeServer``1` instead for a smaller runtime image.
     * @param options Additional options.
     * @returns The updated resource builder.
     */
    publishAsPackageScript(options) {
        const scriptName = options?.scriptName;
        const runScriptArguments = options?.runScriptArguments;
        return new NodeAppResourcePromiseImpl(this._publishAsPackageScriptInternal(scriptName, runScriptArguments), this._client);
    }
    /** @internal */
    async _withNpmInternal(install, installCommand, installArgs) {
        const rpcArgs = { resource: this._handle };
        if (install !== undefined)
            rpcArgs.install = install;
        if (installCommand !== undefined)
            rpcArgs.installCommand = installCommand;
        if (installArgs !== undefined)
            rpcArgs.installArgs = installArgs;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/withNpm', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Configures the Node.js resource to use npm as the package manager and optionally installs packages before the application starts.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withNpm(options) {
        const install = options?.install;
        const installCommand = options?.installCommand;
        const installArgs = options?.installArgs;
        return new NodeAppResourcePromiseImpl(this._withNpmInternal(install, installCommand, installArgs), this._client);
    }
    /** @internal */
    async _withBunInternal(install, installArgs) {
        const rpcArgs = { resource: this._handle };
        if (install !== undefined)
            rpcArgs.install = install;
        if (installArgs !== undefined)
            rpcArgs.installArgs = installArgs;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/withBun', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Configures the JavaScript resource to use Bun as the package manager and optionally installs packages before the application starts.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withBun(options) {
        const install = options?.install;
        const installArgs = options?.installArgs;
        return new NodeAppResourcePromiseImpl(this._withBunInternal(install, installArgs), this._client);
    }
    /** @internal */
    async _withYarnInternal(install, installArgs) {
        const rpcArgs = { resource: this._handle };
        if (install !== undefined)
            rpcArgs.install = install;
        if (installArgs !== undefined)
            rpcArgs.installArgs = installArgs;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/withYarn', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Configures the Node.js resource to use yarn as the package manager and optionally installs packages before the application starts.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withYarn(options) {
        const install = options?.install;
        const installArgs = options?.installArgs;
        return new NodeAppResourcePromiseImpl(this._withYarnInternal(install, installArgs), this._client);
    }
    /** @internal */
    async _withPnpmInternal(install, installArgs) {
        const rpcArgs = { resource: this._handle };
        if (install !== undefined)
            rpcArgs.install = install;
        if (installArgs !== undefined)
            rpcArgs.installArgs = installArgs;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/withPnpm', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Configures the Node.js resource to use pnpm as the package manager and optionally installs packages before the application starts.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withPnpm(options) {
        const install = options?.install;
        const installArgs = options?.installArgs;
        return new NodeAppResourcePromiseImpl(this._withPnpmInternal(install, installArgs), this._client);
    }
    /** @internal */
    async _withBuildScriptInternal(scriptName, args) {
        const rpcArgs = { resource: this._handle, scriptName };
        if (args !== undefined)
            rpcArgs.args = args;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/withBuildScript', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Adds a build script annotation to the resource builder using the specified command-line arguments.
     *
     * Use this method to specify custom build scripts for JavaScript application resources during
     * deployment.
     * @param scriptName The name of the script to be executed when the resource is built.
     * @param options Additional options.
     * @returns The same resource builder instance with the build script annotation applied.
     */
    withBuildScript(scriptName, options) {
        const args = options?.args;
        return new NodeAppResourcePromiseImpl(this._withBuildScriptInternal(scriptName, args), this._client);
    }
    /** @internal */
    async _withRunScriptInternal(scriptName, args) {
        const rpcArgs = { resource: this._handle, scriptName };
        if (args !== undefined)
            rpcArgs.args = args;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/withRunScript', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Adds a run script annotation to the specified JavaScript application resource builder, specifying the script to execute and its arguments during run mode.
     *
     * Use this method to specify a custom script and its arguments that should be executed when the resource is executed
     * in RunMode.
     * @param scriptName The name of the script to be executed when the resource is run.
     * @param options Additional options.
     * @returns The same resource builder instance with the run script annotation applied, enabling further configuration.
     */
    withRunScript(scriptName, options) {
        const args = options?.args;
        return new NodeAppResourcePromiseImpl(this._withRunScriptInternal(scriptName, args), this._client);
    }
    /** @internal */
    async _withBrowserDebuggerInternal(browser) {
        const rpcArgs = { builder: this._handle };
        if (browser !== undefined)
            rpcArgs.browser = browser;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/withBrowserDebugger', rpcArgs);
        return new NodeAppResourceImpl(result, this._client);
    }
    /**
     * Configures a browser debugger for the JavaScript application resource, enabling browser-based debugging through a child resource that launches when the parent application is ready.
     *
     * This method creates a child `BrowserDebuggerResource` that waits for the parent JavaScript
     * application to start, then launches a browser debug session targeting the parent's HTTP or HTTPS endpoint.
     * The parent resource must have at least one HTTP or HTTPS endpoint configured.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withBrowserDebugger(options) {
        const browser = options?.browser;
        return new NodeAppResourcePromiseImpl(this._withBrowserDebuggerInternal(browser), this._client);
    }
}
/**
 * Thenable wrapper for NodeAppResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
class NodeAppResourcePromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    command() {
        return this._promise.then(obj => obj.command());
    }
    workingDirectory() {
        return this._promise.then(obj => obj.workingDirectory());
    }
    name() {
        return this._promise.then(obj => obj.name());
    }
    withContainerRegistry(registry) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withContainerRegistry(registry)), this._client);
    }
    withDockerfileBaseImage(options) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileBaseImage(options)), this._client);
    }
    publishAsDockerFile(configure) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.publishAsDockerFile(configure)), this._client);
    }
    withExecutableCommand(command) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withExecutableCommand(command)), this._client);
    }
    withWorkingDirectory(workingDirectory) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withWorkingDirectory(workingDirectory)), this._client);
    }
    withMcpServer(options) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withMcpServer(options)), this._client);
    }
    withOtlpExporter(options) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withOtlpExporter(options)), this._client);
    }
    withRequiredCommand(command, options) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withRequiredCommand(command, options)), this._client);
    }
    withSessionLifetime() {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withSessionLifetime()), this._client);
    }
    withPersistentLifetime() {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withPersistentLifetime()), this._client);
    }
    withLifetimeOf(sourceBuilder) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withLifetimeOf(sourceBuilder)), this._client);
    }
    withParentProcessLifetime(parentProcessId) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withParentProcessLifetime(parentProcessId)), this._client);
    }
    withEnvironment(name, value) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withEnvironment(name, value)), this._client);
    }
    withEnvironmentCallback(callback) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withEnvironmentCallback(callback)), this._client);
    }
    withArgs(args) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withArgs(args)), this._client);
    }
    withArgsCallback(callback) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withArgsCallback(callback)), this._client);
    }
    withReferenceEnvironment(options) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withReferenceEnvironment(options)), this._client);
    }
    withReference(source, options) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withReference(source, options)), this._client);
    }
    withEndpointCallback(endpointName, callback, options) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withEndpointCallback(endpointName, callback, options)), this._client);
    }
    withHttpEndpointCallback(callback, options) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpEndpointCallback(callback, options)), this._client);
    }
    withHttpsEndpointCallback(callback, options) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpsEndpointCallback(callback, options)), this._client);
    }
    withEndpoint(options) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withEndpoint(options)), this._client);
    }
    withEndpointProxySupport(proxyEnabled) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withEndpointProxySupport(proxyEnabled)), this._client);
    }
    withHttpEndpoint(options) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpEndpoint(options)), this._client);
    }
    withHttpsEndpoint(options) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpsEndpoint(options)), this._client);
    }
    withExternalHttpEndpoints() {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withExternalHttpEndpoints()), this._client);
    }
    getEndpoint(name) {
        return new EndpointReferencePromiseImpl(this._promise.then(obj => obj.getEndpoint(name)), this._client);
    }
    asHttp2Service() {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.asHttp2Service()), this._client);
    }
    withUrls(callback) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withUrls(callback)), this._client);
    }
    withUrl(url, options) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withUrl(url, options)), this._client);
    }
    withUrlForEndpoint(endpointName, callback) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)), this._client);
    }
    publishWithContainerFiles(source, destinationPath) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.publishWithContainerFiles(source, destinationPath)), this._client);
    }
    withContainerFilesSource(sourcePath) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withContainerFilesSource(sourcePath)), this._client);
    }
    clearContainerFilesSources() {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.clearContainerFilesSources()), this._client);
    }
    excludeFromManifest() {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.excludeFromManifest()), this._client);
    }
    waitFor(dependency, options) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.waitFor(dependency, options)), this._client);
    }
    waitForStart(dependency, options) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.waitForStart(dependency, options)), this._client);
    }
    withExplicitStart() {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withExplicitStart()), this._client);
    }
    waitForCompletion(dependency, options) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.waitForCompletion(dependency, options)), this._client);
    }
    withHealthCheck(key) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withHealthCheck(key)), this._client);
    }
    withHttpHealthCheck(options) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpHealthCheck(options)), this._client);
    }
    withCommand(name, displayName, executeCommand, options) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)), this._client);
    }
    withProcessCommand(commandName, displayName, options) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommand(commandName, displayName, options)), this._client);
    }
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommandFactory(commandName, displayName, createProcessSpec, options)), this._client);
    }
    withHttpCommand(path, displayName, options) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpCommand(path, displayName, options)), this._client);
    }
    withDeveloperCertificateTrust(trust) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withDeveloperCertificateTrust(trust)), this._client);
    }
    withCertificateTrustScope(scope) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withCertificateTrustScope(scope)), this._client);
    }
    withHttpsDeveloperCertificate(options) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpsDeveloperCertificate(options)), this._client);
    }
    withoutHttpsCertificate() {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withoutHttpsCertificate()), this._client);
    }
    withRelationship(resourceBuilder, type) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withRelationship(resourceBuilder, type)), this._client);
    }
    withParentRelationship(parent) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withParentRelationship(parent)), this._client);
    }
    withChildRelationship(child) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withChildRelationship(child)), this._client);
    }
    withIconName(iconName, options) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withIconName(iconName, options)), this._client);
    }
    withComputeEnvironment(computeEnvironmentResource) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withComputeEnvironment(computeEnvironmentResource)), this._client);
    }
    withHttpProbe(probeType, options) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpProbe(probeType, options)), this._client);
    }
    excludeFromMcp() {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.excludeFromMcp()), this._client);
    }
    withHidden() {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withHidden()), this._client);
    }
    withHiddenOnCompletion(options) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withHiddenOnCompletion(options)), this._client);
    }
    withImagePushOptions(callback) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withImagePushOptions(callback)), this._client);
    }
    withRemoteImageName(remoteImageName) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withRemoteImageName(remoteImageName)), this._client);
    }
    withRemoteImageTag(remoteImageTag) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withRemoteImageTag(remoteImageTag)), this._client);
    }
    withPipelineStepFactory(stepName, callback, options) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)), this._client);
    }
    withPipelineConfiguration(callback) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withPipelineConfiguration(callback)), this._client);
    }
    getResourceName() {
        return this._promise.then(obj => obj.getResourceName());
    }
    onBeforeResourceStarted(callback) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.onBeforeResourceStarted(callback)), this._client);
    }
    onResourceStopped(callback) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.onResourceStopped(callback)), this._client);
    }
    onInitializeResource(callback) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.onInitializeResource(callback)), this._client);
    }
    onResourceEndpointsAllocated(callback) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.onResourceEndpointsAllocated(callback)), this._client);
    }
    onResourceReady(callback) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.onResourceReady(callback)), this._client);
    }
    createExecutionConfiguration() {
        return new ExecutionConfigurationBuilderPromiseImpl(this._promise.then(obj => obj.createExecutionConfiguration()), this._client);
    }
    publishAsStaticWebsite(options) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.publishAsStaticWebsite(options)), this._client);
    }
    publishAsNodeServer(entryPoint, options) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.publishAsNodeServer(entryPoint, options)), this._client);
    }
    publishAsPackageScript(options) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.publishAsPackageScript(options)), this._client);
    }
    withNpm(options) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withNpm(options)), this._client);
    }
    withBun(options) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withBun(options)), this._client);
    }
    withYarn(options) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withYarn(options)), this._client);
    }
    withPnpm(options) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withPnpm(options)), this._client);
    }
    withBuildScript(scriptName, options) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withBuildScript(scriptName, options)), this._client);
    }
    withRunScript(scriptName, options) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withRunScript(scriptName, options)), this._client);
    }
    withBrowserDebugger(options) {
        return new NodeAppResourcePromiseImpl(this._promise.then(obj => obj.withBrowserDebugger(options)), this._client);
    }
}
// ============================================================================
// ParameterResourceImpl
// ============================================================================
/** Represents a parameter resource. */
class ParameterResourceImpl extends ResourceBuilderBase {
    constructor(handle, client) {
        super(handle, client);
    }
    /** @internal */
    async _withContainerRegistryInternal(registry) {
        registry = isPromiseLike(registry) ? await registry : registry;
        const rpcArgs = { builder: this._handle, registry };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerRegistry', rpcArgs);
        return new ParameterResourceImpl(result, this._client);
    }
    /**
     * Configures the resource to use the specified container registry for container image operations.
     *
     * This method adds a `ContainerRegistryReferenceAnnotation` to the resource,
     * indicating that the resource should use the specified container registry for container image operations.
     * @param registry The container registry resource builder.
     * @returns The resource builder for chaining.
     */
    withContainerRegistry(registry) {
        return new ParameterResourcePromiseImpl(this._withContainerRegistryInternal(registry), this._client);
    }
    /** @internal */
    async _withDockerfileBaseImageInternal(buildImage, runtimeImage) {
        const rpcArgs = { builder: this._handle };
        if (buildImage !== undefined)
            rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined)
            rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileBaseImage', rpcArgs);
        return new ParameterResourceImpl(result, this._client);
    }
    /**
     * Configures custom base images for generated Dockerfiles.
     *
     * This extension method allows customization of the base images used in generated Dockerfiles.
     * For multi-stage Dockerfiles (e.g., Python with UV), you can specify separate build and runtime images.
     * Specify custom base images for a Python application:
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddPythonApp("myapp", "path/to/app", "main.py")
     * .WithDockerfileBaseImage(
     * buildImage: "ghcr.io/astral-sh/uv:python3.12-bookworm-slim",
     * runtimeImage: "python:3.12-slim-bookworm");
     * builder.Build().Run();
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileBaseImage(options) {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new ParameterResourcePromiseImpl(this._withDockerfileBaseImageInternal(buildImage, runtimeImage), this._client);
    }
    /** @internal */
    async _withDescriptionInternal(description, enableMarkdown) {
        const rpcArgs = { builder: this._handle, description };
        if (enableMarkdown !== undefined)
            rpcArgs.enableMarkdown = enableMarkdown;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDescription', rpcArgs);
        return new ParameterResourceImpl(result, this._client);
    }
    /**
     * Sets the description of the parameter resource.
     * @param description The parameter description.
     * @param options Additional options.
     * @returns Resource builder for the parameter.
     */
    withDescription(description, options) {
        const enableMarkdown = options?.enableMarkdown;
        return new ParameterResourcePromiseImpl(this._withDescriptionInternal(description, enableMarkdown), this._client);
    }
    /** @internal */
    async _withCustomInputInternal(options) {
        const rpcArgs = { builder: this._handle, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withCustomInput', rpcArgs);
        return new ParameterResourceImpl(result, this._client);
    }
    /**
     * Sets a custom input for the parameter resource from a polyglot app host.
     * @param options Options used to customize the input for the parameter.
     * @returns Resource builder for the parameter.
     */
    withCustomInput(options) {
        return new ParameterResourcePromiseImpl(this._withCustomInputInternal(options), this._client);
    }
    /** @internal */
    async _withRequiredCommandInternal(command, helpLink) {
        const rpcArgs = { builder: this._handle, command };
        if (helpLink !== undefined)
            rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability('Aspire.Hosting/withRequiredCommand', rpcArgs);
        return new ParameterResourceImpl(result, this._client);
    }
    /**
     * Declares that a resource requires a specific command/executable to be available on the local machine PATH before it can start.
     *
     * The command is considered valid if either:
     * 1. It is an absolute or relative path (contains a directory separator) that points to an existing file, or
     * 2. It is discoverable on the current process PATH (respecting PATHEXT on Windows).
     * If the command is not found, a warning message will be logged but the resource will be allowed to attempt to start.
     * @param command The command string (file name or path) that should be validated.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withRequiredCommand(command, options) {
        const helpLink = options?.helpLink;
        return new ParameterResourcePromiseImpl(this._withRequiredCommandInternal(command, helpLink), this._client);
    }
    /** @internal */
    async _withSessionLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withSessionLifetime', rpcArgs);
        return new ParameterResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a session lifetime.
     * @returns The resource builder.
     */
    withSessionLifetime() {
        return new ParameterResourcePromiseImpl(this._withSessionLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withPersistentLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPersistentLifetime', rpcArgs);
        return new ParameterResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime.
     * @returns The resource builder.
     */
    withPersistentLifetime() {
        return new ParameterResourcePromiseImpl(this._withPersistentLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withLifetimeOfInternal(sourceBuilder) {
        sourceBuilder = isPromiseLike(sourceBuilder) ? await sourceBuilder : sourceBuilder;
        const rpcArgs = { builder: this._handle, sourceBuilder };
        const result = await this._client.invokeCapability('Aspire.Hosting/withLifetimeOf', rpcArgs);
        return new ParameterResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to match the lifetime of another resource.
     *
     * The resource lifetime is evaluated from `sourceBuilder` when the application model is prepared, so later lifetime
     * changes to the source resource are reflected by this resource.
     * @param sourceBuilder The resource builder whose lifetime should be used.
     * @returns The resource builder.
     */
    withLifetimeOf(sourceBuilder) {
        return new ParameterResourcePromiseImpl(this._withLifetimeOfInternal(sourceBuilder), this._client);
    }
    /** @internal */
    async _withParentProcessLifetimeInternal(parentProcessId) {
        const rpcArgs = { builder: this._handle, parentProcessId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withParentProcessLifetime', rpcArgs);
        return new ParameterResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime that ends when a parent process exits.
     * @param parentProcessId The ID of the parent process to monitor.
     * @returns The resource builder.
     */
    withParentProcessLifetime(parentProcessId) {
        return new ParameterResourcePromiseImpl(this._withParentProcessLifetimeInternal(parentProcessId), this._client);
    }
    /** @internal */
    async _withUrlsInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new ResourceUrlsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrls', rpcArgs);
        return new ParameterResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to customize the URLs displayed for the resource.
     * @param callback The callback that will customize URLs for the resource.
     * @returns The resource builder.
     */
    withUrls(callback) {
        return new ParameterResourcePromiseImpl(this._withUrlsInternal(callback), this._client);
    }
    /** @internal */
    async _withUrlInternal(url, displayText) {
        const rpcArgs = { builder: this._handle, url };
        if (displayText !== undefined)
            rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrl', rpcArgs);
        return new ParameterResourceImpl(result, this._client);
    }
    /**
     * Adds or modifies displayed URLs
     * @param options Additional options.
     */
    withUrl(url, options) {
        const displayText = options?.displayText;
        return new ParameterResourcePromiseImpl(this._withUrlInternal(url, displayText), this._client);
    }
    /** @internal */
    async _withUrlForEndpointInternal(endpointName, callback) {
        const callbackId = registerCallback(async (objData) => {
            const obj = wrapIfHandle(objData);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrlForEndpoint', rpcArgs);
        return new ParameterResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to update the URL displayed for the endpoint with the specified name.
     * @param endpointName The name of the endpoint to customize the URL for.
     * @param callback The callback that will customize the URL.
     * @returns The resource builder.
     */
    withUrlForEndpoint(endpointName, callback) {
        return new ParameterResourcePromiseImpl(this._withUrlForEndpointInternal(endpointName, callback), this._client);
    }
    /** @internal */
    async _excludeFromManifestInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromManifest', rpcArgs);
        return new ParameterResourceImpl(result, this._client);
    }
    /**
     * Excludes a resource from being published to the manifest.
     * @returns The resource builder.
     */
    excludeFromManifest() {
        return new ParameterResourcePromiseImpl(this._excludeFromManifestInternal(), this._client);
    }
    /** @internal */
    async _withExplicitStartInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExplicitStart', rpcArgs);
        return new ParameterResourceImpl(result, this._client);
    }
    /**
     * Prevents resource from starting automatically
     * @returns The resource builder.
     */
    withExplicitStart() {
        return new ParameterResourcePromiseImpl(this._withExplicitStartInternal(), this._client);
    }
    /** @internal */
    async _withHealthCheckInternal(key) {
        const rpcArgs = { builder: this._handle, key };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHealthCheck', rpcArgs);
        return new ParameterResourceImpl(result, this._client);
    }
    /**
     * Adds a health check by key
     * @param key The key for the health check.
     * @returns The resource builder.
     */
    withHealthCheck(key) {
        return new ParameterResourcePromiseImpl(this._withHealthCheckInternal(key), this._client);
    }
    /** @internal */
    async _withCommandInternal(name, displayName, executeCommand, commandOptions) {
        const executeCommandId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined)
            rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability('Aspire.Hosting/withCommand', rpcArgs);
        return new ParameterResourceImpl(result, this._client);
    }
    /**
     * Adds a resource command
     *
     * The `WithCommand` method is used to add commands to the resource. Commands are displayed in the dashboard
     * and can be executed by a user using the dashboard UI.
     * When a command is executed, the `executeCommand` callback is called and is run inside the Aspire host.
     * @param name The name of the command. The name uniquely identifies the command.
     * @param displayName The display name visible in UI.
     * @param executeCommand A callback that is executed when the command is executed. The callback is run inside the Aspire host. The callback result is used to indicate success or failure in the UI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withCommand(name, displayName, executeCommand, options) {
        const commandOptions = options?.commandOptions;
        return new ParameterResourcePromiseImpl(this._withCommandInternal(name, displayName, executeCommand, commandOptions), this._client);
    }
    /** @internal */
    async _withProcessCommandInternal(commandName, displayName, options) {
        const rpcArgs = { builder: this._handle, commandName, displayName, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommand', rpcArgs);
        return new ParameterResourceImpl(result, this._client);
    }
    /** Adds a command to the resource that starts a local process when invoked. */
    withProcessCommand(commandName, displayName, options) {
        return new ParameterResourcePromiseImpl(this._withProcessCommandInternal(commandName, displayName, options), this._client);
    }
    /** @internal */
    async _withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options) {
        const createProcessSpecId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await createProcessSpec(arg);
        });
        const rpcArgs = { builder: this._handle, commandName, displayName, createProcessSpec: createProcessSpecId };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommandFactory', rpcArgs);
        return new ParameterResourceImpl(result, this._client);
    }
    /**
     * Adds a command to the resource that starts a local process created by a callback when invoked.
     * @param options Additional options.
     */
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new ParameterResourcePromiseImpl(this._withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options), this._client);
    }
    /** @internal */
    async _withRelationshipInternal(resourceBuilder, type) {
        resourceBuilder = isPromiseLike(resourceBuilder) ? await resourceBuilder : resourceBuilder;
        const rpcArgs = { builder: this._handle, resourceBuilder, type };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderRelationship', rpcArgs);
        return new ParameterResourceImpl(result, this._client);
    }
    /**
     * Adds a relationship to another resource using its builder.
     * @param resourceBuilder The resource builder that the relationship is to.
     * @param type The relationship type.
     * @returns A resource builder.
     */
    withRelationship(resourceBuilder, type) {
        return new ParameterResourcePromiseImpl(this._withRelationshipInternal(resourceBuilder, type), this._client);
    }
    /** @internal */
    async _withParentRelationshipInternal(parent) {
        parent = isPromiseLike(parent) ? await parent : parent;
        const rpcArgs = { builder: this._handle, parent };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderParentRelationship', rpcArgs);
        return new ParameterResourceImpl(result, this._client);
    }
    /**
     * Sets the parent relationship
     * @param parent The parent of `builder`.
     * @returns A resource builder.
     */
    withParentRelationship(parent) {
        return new ParameterResourcePromiseImpl(this._withParentRelationshipInternal(parent), this._client);
    }
    /** @internal */
    async _withChildRelationshipInternal(child) {
        child = isPromiseLike(child) ? await child : child;
        const rpcArgs = { builder: this._handle, child };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderChildRelationship', rpcArgs);
        return new ParameterResourceImpl(result, this._client);
    }
    /**
     * Sets a child relationship
     * @param child The child of `builder`.
     * @returns A resource builder.
     */
    withChildRelationship(child) {
        return new ParameterResourcePromiseImpl(this._withChildRelationshipInternal(child), this._client);
    }
    /** @internal */
    async _withIconNameInternal(iconName, iconVariant) {
        const rpcArgs = { builder: this._handle, iconName };
        if (iconVariant !== undefined)
            rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability('Aspire.Hosting/withIconName', rpcArgs);
        return new ParameterResourceImpl(result, this._client);
    }
    /**
     * Specifies the icon to use when displaying the resource in the dashboard.
     * @param iconName The name of the FluentUI icon to use. See https://aka.ms/fluentui-system-icons for available icons.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withIconName(iconName, options) {
        const iconVariant = options?.iconVariant;
        return new ParameterResourcePromiseImpl(this._withIconNameInternal(iconName, iconVariant), this._client);
    }
    /** @internal */
    async _excludeFromMcpInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromMcp', rpcArgs);
        return new ParameterResourceImpl(result, this._client);
    }
    /**
     * Exclude the resource from MCP operations using the Aspire MCP server. The resource is excluded from results that return resources, console logs and telemetry.
     * @returns The resource builder.
     */
    excludeFromMcp() {
        return new ParameterResourcePromiseImpl(this._excludeFromMcpInternal(), this._client);
    }
    /** @internal */
    async _withHiddenInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHidden', rpcArgs);
        return new ParameterResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists
     *
     * Use this method to hide resources that are implementation details and should never be displayed by default.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @returns The resource builder.
     */
    withHidden() {
        return new ParameterResourcePromiseImpl(this._withHiddenInternal(), this._client);
    }
    /** @internal */
    async _withHiddenOnCompletionInternal(exitCode, exitCodes) {
        const rpcArgs = { builder: this._handle };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        if (exitCodes !== undefined)
            rpcArgs.exitCodes = exitCodes;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHiddenOnCompletion', rpcArgs);
        return new ParameterResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists after successful completion
     *
     * This method is useful for one-off resources such as setup scripts, migrations, or build steps that should remain visible while running
     * and then be hidden after successful completion.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHiddenOnCompletion(options) {
        const exitCode = options?.exitCode;
        const exitCodes = options?.exitCodes;
        return new ParameterResourcePromiseImpl(this._withHiddenOnCompletionInternal(exitCode, exitCodes), this._client);
    }
    /** @internal */
    async _withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new PipelineStepContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined)
            rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined)
            rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined)
            rpcArgs.tags = tags;
        if (description !== undefined)
            rpcArgs.description = description;
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineStepFactory', rpcArgs);
        return new ParameterResourceImpl(result, this._client);
    }
    /**
     * Adds a pipeline step to the resource that will be executed during deployment.
     * @param stepName The unique name of the pipeline step.
     * @param callback The callback to execute when the step runs.
     * @param options Additional options.
     * @returns The resource builder for chaining.
     */
    withPipelineStepFactory(stepName, callback, options) {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new ParameterResourcePromiseImpl(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description), this._client);
    }
    /** @internal */
    async _withPipelineConfigurationInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new PipelineConfigurationContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineConfiguration', rpcArgs);
        return new ParameterResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to be executed during the pipeline configuration phase, allowing modification of step dependencies and relationships.
     * @param callback The callback function to execute during the configuration phase.
     * @returns The resource builder for chaining.
     */
    withPipelineConfiguration(callback) {
        return new ParameterResourcePromiseImpl(this._withPipelineConfigurationInternal(callback), this._client);
    }
    /**
     * Gets the name of the resource from a builder.
     *
     * Why this wrapper exists: This capability accesses a nested property
     * (`resource.Resource.Name`) which requires a wrapper method. There is no single
     * .NET method that returns just the resource name that could be annotated directly.
     * @returns The resource name.
     */
    async getResourceName() {
        const rpcArgs = { resource: this._handle };
        return await this._client.invokeCapability('Aspire.Hosting/getResourceName', rpcArgs);
    }
    /** @internal */
    async _onBeforeResourceStartedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new BeforeResourceStartedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onBeforeResourceStarted', rpcArgs);
        return new ParameterResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the BeforeResourceStarted event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onBeforeResourceStarted(callback) {
        return new ParameterResourcePromiseImpl(this._onBeforeResourceStartedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceStoppedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceStoppedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceStopped', rpcArgs);
        return new ParameterResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceStopped event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceStopped(callback) {
        return new ParameterResourcePromiseImpl(this._onResourceStoppedInternal(callback), this._client);
    }
    /** @internal */
    async _onInitializeResourceInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new InitializeResourceEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onInitializeResource', rpcArgs);
        return new ParameterResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the InitializeResource event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onInitializeResource(callback) {
        return new ParameterResourcePromiseImpl(this._onInitializeResourceInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceReadyInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceReadyEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceReady', rpcArgs);
        return new ParameterResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceReady event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceReady(callback) {
        return new ParameterResourcePromiseImpl(this._onResourceReadyInternal(callback), this._client);
    }
    /**
     * Creates an execution configuration builder for the specified resource.
     * @returns The execution configuration builder.
     */
    createExecutionConfiguration() {
        const promise = (async () => {
            const rpcArgs = { resource: this._handle };
            const handle = await this._client.invokeCapability('Aspire.Hosting/createExecutionConfiguration', rpcArgs);
            return new ExecutionConfigurationBuilderImpl(handle, this._client);
        })();
        return new ExecutionConfigurationBuilderPromiseImpl(promise, this._client);
    }
}
/**
 * Thenable wrapper for ParameterResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
class ParameterResourcePromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    withContainerRegistry(registry) {
        return new ParameterResourcePromiseImpl(this._promise.then(obj => obj.withContainerRegistry(registry)), this._client);
    }
    withDockerfileBaseImage(options) {
        return new ParameterResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileBaseImage(options)), this._client);
    }
    withDescription(description, options) {
        return new ParameterResourcePromiseImpl(this._promise.then(obj => obj.withDescription(description, options)), this._client);
    }
    withCustomInput(options) {
        return new ParameterResourcePromiseImpl(this._promise.then(obj => obj.withCustomInput(options)), this._client);
    }
    withRequiredCommand(command, options) {
        return new ParameterResourcePromiseImpl(this._promise.then(obj => obj.withRequiredCommand(command, options)), this._client);
    }
    withSessionLifetime() {
        return new ParameterResourcePromiseImpl(this._promise.then(obj => obj.withSessionLifetime()), this._client);
    }
    withPersistentLifetime() {
        return new ParameterResourcePromiseImpl(this._promise.then(obj => obj.withPersistentLifetime()), this._client);
    }
    withLifetimeOf(sourceBuilder) {
        return new ParameterResourcePromiseImpl(this._promise.then(obj => obj.withLifetimeOf(sourceBuilder)), this._client);
    }
    withParentProcessLifetime(parentProcessId) {
        return new ParameterResourcePromiseImpl(this._promise.then(obj => obj.withParentProcessLifetime(parentProcessId)), this._client);
    }
    withUrls(callback) {
        return new ParameterResourcePromiseImpl(this._promise.then(obj => obj.withUrls(callback)), this._client);
    }
    withUrl(url, options) {
        return new ParameterResourcePromiseImpl(this._promise.then(obj => obj.withUrl(url, options)), this._client);
    }
    withUrlForEndpoint(endpointName, callback) {
        return new ParameterResourcePromiseImpl(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)), this._client);
    }
    excludeFromManifest() {
        return new ParameterResourcePromiseImpl(this._promise.then(obj => obj.excludeFromManifest()), this._client);
    }
    withExplicitStart() {
        return new ParameterResourcePromiseImpl(this._promise.then(obj => obj.withExplicitStart()), this._client);
    }
    withHealthCheck(key) {
        return new ParameterResourcePromiseImpl(this._promise.then(obj => obj.withHealthCheck(key)), this._client);
    }
    withCommand(name, displayName, executeCommand, options) {
        return new ParameterResourcePromiseImpl(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)), this._client);
    }
    withProcessCommand(commandName, displayName, options) {
        return new ParameterResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommand(commandName, displayName, options)), this._client);
    }
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new ParameterResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommandFactory(commandName, displayName, createProcessSpec, options)), this._client);
    }
    withRelationship(resourceBuilder, type) {
        return new ParameterResourcePromiseImpl(this._promise.then(obj => obj.withRelationship(resourceBuilder, type)), this._client);
    }
    withParentRelationship(parent) {
        return new ParameterResourcePromiseImpl(this._promise.then(obj => obj.withParentRelationship(parent)), this._client);
    }
    withChildRelationship(child) {
        return new ParameterResourcePromiseImpl(this._promise.then(obj => obj.withChildRelationship(child)), this._client);
    }
    withIconName(iconName, options) {
        return new ParameterResourcePromiseImpl(this._promise.then(obj => obj.withIconName(iconName, options)), this._client);
    }
    excludeFromMcp() {
        return new ParameterResourcePromiseImpl(this._promise.then(obj => obj.excludeFromMcp()), this._client);
    }
    withHidden() {
        return new ParameterResourcePromiseImpl(this._promise.then(obj => obj.withHidden()), this._client);
    }
    withHiddenOnCompletion(options) {
        return new ParameterResourcePromiseImpl(this._promise.then(obj => obj.withHiddenOnCompletion(options)), this._client);
    }
    withPipelineStepFactory(stepName, callback, options) {
        return new ParameterResourcePromiseImpl(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)), this._client);
    }
    withPipelineConfiguration(callback) {
        return new ParameterResourcePromiseImpl(this._promise.then(obj => obj.withPipelineConfiguration(callback)), this._client);
    }
    getResourceName() {
        return this._promise.then(obj => obj.getResourceName());
    }
    onBeforeResourceStarted(callback) {
        return new ParameterResourcePromiseImpl(this._promise.then(obj => obj.onBeforeResourceStarted(callback)), this._client);
    }
    onResourceStopped(callback) {
        return new ParameterResourcePromiseImpl(this._promise.then(obj => obj.onResourceStopped(callback)), this._client);
    }
    onInitializeResource(callback) {
        return new ParameterResourcePromiseImpl(this._promise.then(obj => obj.onInitializeResource(callback)), this._client);
    }
    onResourceReady(callback) {
        return new ParameterResourcePromiseImpl(this._promise.then(obj => obj.onResourceReady(callback)), this._client);
    }
    createExecutionConfiguration() {
        return new ExecutionConfigurationBuilderPromiseImpl(this._promise.then(obj => obj.createExecutionConfiguration()), this._client);
    }
}
// ============================================================================
// PgAdminContainerResourceImpl
// ============================================================================
class PgAdminContainerResourceImpl extends ResourceBuilderBase {
    constructor(handle, client) {
        super(handle, client);
    }
    /** @internal */
    async _withContainerRegistryInternal(registry) {
        registry = isPromiseLike(registry) ? await registry : registry;
        const rpcArgs = { builder: this._handle, registry };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerRegistry', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Configures the resource to use the specified container registry for container image operations.
     *
     * This method adds a `ContainerRegistryReferenceAnnotation` to the resource,
     * indicating that the resource should use the specified container registry for container image operations.
     * @param registry The container registry resource builder.
     * @returns The resource builder for chaining.
     */
    withContainerRegistry(registry) {
        return new PgAdminContainerResourcePromiseImpl(this._withContainerRegistryInternal(registry), this._client);
    }
    /** @internal */
    async _withBindMountInternal(source, target, isReadOnly) {
        const rpcArgs = { builder: this._handle, source, target };
        if (isReadOnly !== undefined)
            rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability('Aspire.Hosting/withBindMount', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a bind mount to a container resource.
     * @param source The source path of the mount. This is the path to the file or directory on the host, relative to the app host project directory.
     * @param target The target path where the file or directory is mounted in the container.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withBindMount(source, target, options) {
        const isReadOnly = options?.isReadOnly;
        return new PgAdminContainerResourcePromiseImpl(this._withBindMountInternal(source, target, isReadOnly), this._client);
    }
    /** @internal */
    async _withEntrypointInternal(entrypoint) {
        const rpcArgs = { builder: this._handle, entrypoint };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEntrypoint', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Sets the Entrypoint for the container.
     * @param entrypoint The new entrypoint for the container.
     * @returns The resource builder.
     */
    withEntrypoint(entrypoint) {
        return new PgAdminContainerResourcePromiseImpl(this._withEntrypointInternal(entrypoint), this._client);
    }
    /** @internal */
    async _withImageTagInternal(tag) {
        const rpcArgs = { builder: this._handle, tag };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImageTag', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Allows overriding the image tag on a container.
     * @param tag Tag value.
     * @returns The resource builder.
     */
    withImageTag(tag) {
        return new PgAdminContainerResourcePromiseImpl(this._withImageTagInternal(tag), this._client);
    }
    /** @internal */
    async _withImageRegistryInternal(registry) {
        const rpcArgs = { builder: this._handle, registry };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImageRegistry', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Allows overriding the image registry on a container.
     * @param registry Registry value.
     * @returns The resource builder.
     */
    withImageRegistry(registry) {
        return new PgAdminContainerResourcePromiseImpl(this._withImageRegistryInternal(registry), this._client);
    }
    /** @internal */
    async _withImageInternal(image, tag) {
        const rpcArgs = { builder: this._handle, image };
        if (tag !== undefined)
            rpcArgs.tag = tag;
        const result = await this._client.invokeCapability('Aspire.Hosting/withImage', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Allows overriding the image on a container.
     * @param image Image value.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withImage(image, options) {
        const tag = options?.tag;
        return new PgAdminContainerResourcePromiseImpl(this._withImageInternal(image, tag), this._client);
    }
    /** @internal */
    async _withImageSHA256Internal(sha256) {
        const rpcArgs = { builder: this._handle, sha256 };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImageSHA256', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Allows setting the image to a specific sha256 on a container.
     * @param sha256 Registry value.
     * @returns The resource builder.
     */
    withImageSHA256(sha256) {
        return new PgAdminContainerResourcePromiseImpl(this._withImageSHA256Internal(sha256), this._client);
    }
    /** @internal */
    async _withContainerRuntimeArgsInternal(args) {
        const rpcArgs = { builder: this._handle, args };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerRuntimeArgs', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a callback to be executed with a list of arguments to add to the container runtime run command when a container resource is started.
     * @param args The arguments to be passed to the container runtime run command when the container resource is started.
     * @returns The resource builder.
     */
    withContainerRuntimeArgs(args) {
        return new PgAdminContainerResourcePromiseImpl(this._withContainerRuntimeArgsInternal(args), this._client);
    }
    /** @internal */
    async _withLifetimeInternal(lifetime) {
        const rpcArgs = { builder: this._handle, lifetime };
        const result = await this._client.invokeCapability('Aspire.Hosting/withLifetime', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Sets the lifetime behavior of the container resource.
     *
     * Prefer `WithPersistentLifetime``1` or
     * `WithSessionLifetime``1` for new code.
     * Marking a container resource to have a `Persistent` lifetime.
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddContainer("mycontainer", "myimage")
     * .WithPersistentLifetime();
     * builder.Build().Run();
     * ```
     * @param lifetime The lifetime behavior of the container resource. The default behavior is `Session`.
     * @returns The resource builder.
     */
    withLifetime(lifetime) {
        return new PgAdminContainerResourcePromiseImpl(this._withLifetimeInternal(lifetime), this._client);
    }
    /** @internal */
    async _withImagePullPolicyInternal(pullPolicy) {
        const rpcArgs = { builder: this._handle, pullPolicy };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImagePullPolicy', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Sets the pull policy for the container resource.
     * @param pullPolicy The pull policy behavior for the container resource.
     * @returns The resource builder.
     */
    withImagePullPolicy(pullPolicy) {
        return new PgAdminContainerResourcePromiseImpl(this._withImagePullPolicyInternal(pullPolicy), this._client);
    }
    /** @internal */
    async _publishAsContainerInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/publishAsContainer', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Changes the resource to be published as a container in the manifest.
     * @returns The resource builder.
     */
    publishAsContainer() {
        return new PgAdminContainerResourcePromiseImpl(this._publishAsContainerInternal(), this._client);
    }
    /** @internal */
    async _withDockerfileInternal(contextPath, dockerfilePath, stage) {
        const rpcArgs = { builder: this._handle, contextPath };
        if (dockerfilePath !== undefined)
            rpcArgs.dockerfilePath = dockerfilePath;
        if (stage !== undefined)
            rpcArgs.stage = stage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfile', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Causes Aspire to build the specified container image from a Dockerfile.
     * @param contextPath Path to be used as the context for the container image build.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfile(contextPath, options) {
        const dockerfilePath = options?.dockerfilePath;
        const stage = options?.stage;
        return new PgAdminContainerResourcePromiseImpl(this._withDockerfileInternal(contextPath, dockerfilePath, stage), this._client);
    }
    /** @internal */
    async _withDockerfileFactoryInternal(contextPath, dockerfileFactory, stage) {
        const dockerfileFactoryId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new DockerfileFactoryContextImpl(argHandle, this._client);
            return await dockerfileFactory(arg);
        });
        const rpcArgs = { builder: this._handle, contextPath, dockerfileFactory: dockerfileFactoryId };
        if (stage !== undefined)
            rpcArgs.stage = stage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileFactory', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Builds the specified container image from a Dockerfile generated by an asynchronous factory function.
     *
     * When this method is called, an annotation is added to the `ContainerResource` that specifies the context path
     * and a factory function that generates Dockerfile content. The factory is invoked at build time to produce the Dockerfile,
     * which is then written to a temporary file and used by the orchestrator to build the container image.
     * The `contextPath` is relative to the AppHost directory unless it is fully qualified.
     * The factory function is invoked once during the build process to generate the Dockerfile content.
     * The output is trusted and not validated.
     * Creates a container called `mycontainer` with a dynamically generated Dockerfile.
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddContainer("mycontainer", "myimage")
     * .WithDockerfileFactory("path/to/context", async context =>
     * {
     * var template = await File.ReadAllTextAsync("template.dockerfile", context.CancellationToken);
     * return template.Replace("{{VERSION}}", "1.0");
     * });
     * builder.Build().Run();
     * ```
     * @param contextPath Path to be used as the context for the container image build.
     * @param dockerfileFactory An asynchronous function that returns the Dockerfile content as a string.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileFactory(contextPath, dockerfileFactory, options) {
        const stage = options?.stage;
        return new PgAdminContainerResourcePromiseImpl(this._withDockerfileFactoryInternal(contextPath, dockerfileFactory, stage), this._client);
    }
    /** @internal */
    async _withContainerNameInternal(name) {
        const rpcArgs = { builder: this._handle, name };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerName', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Overrides the default container name for this resource. By default Aspire generates a unique container name based on the resource name and a random postfix (or a postfix based on a hash of the AppHost project path for persistent container resources). This method allows you to override that behavior with a custom name, but could lead to naming conflicts if the specified name is not unique.
     *
     * Combining this with `Persistent` will allow Aspire to re-use an existing container that was not
     * created by an Aspire AppHost.
     * @param name The desired container name. Must be a valid container name or your runtime will report an error.
     * @returns The resource builder.
     */
    withContainerName(name) {
        return new PgAdminContainerResourcePromiseImpl(this._withContainerNameInternal(name), this._client);
    }
    /** @internal */
    async _withBuildArgInternal(name, value) {
        value = isPromiseLike(value) ? await value : value;
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuildArg', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a build argument when the container is built from a Dockerfile.
     * @param name The name of the build argument.
     * @param value The build argument value, either a string or a parameter resource.
     * @returns The resource builder.
     */
    withBuildArg(name, value) {
        return new PgAdminContainerResourcePromiseImpl(this._withBuildArgInternal(name, value), this._client);
    }
    /** @internal */
    async _withBuildSecretInternal(name, value) {
        value = isPromiseLike(value) ? await value : value;
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withParameterBuildSecret', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a secret build argument when the container is built from a Dockerfile.
     * @param name The name of the secret build argument.
     * @param value The resource builder for a parameter resource.
     * @returns The resource builder.
     */
    withBuildSecret(name, value) {
        return new PgAdminContainerResourcePromiseImpl(this._withBuildSecretInternal(name, value), this._client);
    }
    /** @internal */
    async _withContainerCertificatePathsInternal(customCertificatesDestination, defaultCertificateBundlePaths, defaultCertificateDirectoryPaths) {
        const rpcArgs = { builder: this._handle };
        if (customCertificatesDestination !== undefined)
            rpcArgs.customCertificatesDestination = customCertificatesDestination;
        if (defaultCertificateBundlePaths !== undefined)
            rpcArgs.defaultCertificateBundlePaths = defaultCertificateBundlePaths;
        if (defaultCertificateDirectoryPaths !== undefined)
            rpcArgs.defaultCertificateDirectoryPaths = defaultCertificateDirectoryPaths;
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerCertificatePaths', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Adds container certificate path overrides used for certificate trust at run time.
     * @param options Additional options.
     * @returns The updated resource builder.
     */
    withContainerCertificatePaths(options) {
        const customCertificatesDestination = options?.customCertificatesDestination;
        const defaultCertificateBundlePaths = options?.defaultCertificateBundlePaths;
        const defaultCertificateDirectoryPaths = options?.defaultCertificateDirectoryPaths;
        return new PgAdminContainerResourcePromiseImpl(this._withContainerCertificatePathsInternal(customCertificatesDestination, defaultCertificateBundlePaths, defaultCertificateDirectoryPaths), this._client);
    }
    /** @internal */
    async _withDockerfileBuilderInternal(contextPath, callback, stage) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new DockerfileBuilderCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, contextPath, callback: callbackId };
        if (stage !== undefined)
            rpcArgs.stage = stage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileBuilder', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Configures the resource to use a programmatically generated Dockerfile
     *
     * This method provides a programmatic way to build Dockerfiles using the `DockerfileBuilder` API
     * instead of string manipulation. Callbacks can be composed by calling this method multiple times - each callback will be invoked
     * in order to build up the final Dockerfile.
     * The `contextPath` is relative to the AppHost directory unless it is fully qualified.
     * Creates a container with a programmatically built Dockerfile using fluent API:
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddContainer("mycontainer", "myimage")
     * .WithDockerfileBuilder("path/to/context", context =>
     * {
     * context.Builder.From("alpine:latest")
     * .WorkDir("/app")
     * .Run("apk add curl")
     * .Copy(".", ".")
     * .Cmd(["./myapp"]);
     * return Task.CompletedTask;
     * });
     * builder.Build().Run();
     * ```
     * @param contextPath Path to be used as the context for the container image build.
     * @param callback A callback that uses the `DockerfileBuilder` API to construct the Dockerfile.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileBuilder(contextPath, callback, options) {
        const stage = options?.stage;
        return new PgAdminContainerResourcePromiseImpl(this._withDockerfileBuilderInternal(contextPath, callback, stage), this._client);
    }
    /** @internal */
    async _withDockerfileBaseImageInternal(buildImage, runtimeImage) {
        const rpcArgs = { builder: this._handle };
        if (buildImage !== undefined)
            rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined)
            rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileBaseImage', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Configures custom base images for generated Dockerfiles.
     *
     * This extension method allows customization of the base images used in generated Dockerfiles.
     * For multi-stage Dockerfiles (e.g., Python with UV), you can specify separate build and runtime images.
     * Specify custom base images for a Python application:
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddPythonApp("myapp", "path/to/app", "main.py")
     * .WithDockerfileBaseImage(
     * buildImage: "ghcr.io/astral-sh/uv:python3.12-bookworm-slim",
     * runtimeImage: "python:3.12-slim-bookworm");
     * builder.Build().Run();
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileBaseImage(options) {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new PgAdminContainerResourcePromiseImpl(this._withDockerfileBaseImageInternal(buildImage, runtimeImage), this._client);
    }
    /** @internal */
    async _withContainerNetworkAliasInternal(alias) {
        const rpcArgs = { builder: this._handle, alias };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerNetworkAlias', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a network alias to container resource.
     *
     * Network aliases enable DNS resolution of the container on the network by custom names.
     * By default, containers are accessible on the network using their resource name as a DNS alias.
     * This method allows adding additional aliases for the same container.
     * Multiple aliases can be added by calling this method multiple times.
     * @param alias The network alias for the container.
     * @returns The resource builder.
     */
    withContainerNetworkAlias(alias) {
        return new PgAdminContainerResourcePromiseImpl(this._withContainerNetworkAliasInternal(alias), this._client);
    }
    /** @internal */
    async _withMcpServerInternal(path, endpointName) {
        const rpcArgs = { builder: this._handle };
        if (path !== undefined)
            rpcArgs.path = path;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withMcpServer', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Marks the resource as hosting a Model Context Protocol (MCP) server on the specified endpoint.
     *
     * This method adds an `McpServerEndpointAnnotation` to the resource, enabling the Aspire tooling
     * to discover and proxy the MCP server exposed by the resource.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withMcpServer(options) {
        const path = options?.path;
        const endpointName = options?.endpointName;
        return new PgAdminContainerResourcePromiseImpl(this._withMcpServerInternal(path, endpointName), this._client);
    }
    /** @internal */
    async _withOtlpExporterInternal(protocol) {
        const rpcArgs = { builder: this._handle };
        if (protocol !== undefined)
            rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability('Aspire.Hosting/withOtlpExporter', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Configures OTLP telemetry export
     * @param options Additional options.
     */
    withOtlpExporter(options) {
        const protocol = options?.protocol;
        return new PgAdminContainerResourcePromiseImpl(this._withOtlpExporterInternal(protocol), this._client);
    }
    /** @internal */
    async _publishAsConnectionStringInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/publishAsConnectionString', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Changes the resource to be published as a connection string reference in the manifest.
     * @returns The resource builder.
     */
    publishAsConnectionString() {
        return new PgAdminContainerResourcePromiseImpl(this._publishAsConnectionStringInternal(), this._client);
    }
    /** @internal */
    async _withRequiredCommandInternal(command, helpLink) {
        const rpcArgs = { builder: this._handle, command };
        if (helpLink !== undefined)
            rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability('Aspire.Hosting/withRequiredCommand', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Declares that a resource requires a specific command/executable to be available on the local machine PATH before it can start.
     *
     * The command is considered valid if either:
     * 1. It is an absolute or relative path (contains a directory separator) that points to an existing file, or
     * 2. It is discoverable on the current process PATH (respecting PATHEXT on Windows).
     * If the command is not found, a warning message will be logged but the resource will be allowed to attempt to start.
     * @param command The command string (file name or path) that should be validated.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withRequiredCommand(command, options) {
        const helpLink = options?.helpLink;
        return new PgAdminContainerResourcePromiseImpl(this._withRequiredCommandInternal(command, helpLink), this._client);
    }
    /** @internal */
    async _withSessionLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withSessionLifetime', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a session lifetime.
     * @returns The resource builder.
     */
    withSessionLifetime() {
        return new PgAdminContainerResourcePromiseImpl(this._withSessionLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withPersistentLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPersistentLifetime', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime.
     * @returns The resource builder.
     */
    withPersistentLifetime() {
        return new PgAdminContainerResourcePromiseImpl(this._withPersistentLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withLifetimeOfInternal(sourceBuilder) {
        sourceBuilder = isPromiseLike(sourceBuilder) ? await sourceBuilder : sourceBuilder;
        const rpcArgs = { builder: this._handle, sourceBuilder };
        const result = await this._client.invokeCapability('Aspire.Hosting/withLifetimeOf', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to match the lifetime of another resource.
     *
     * The resource lifetime is evaluated from `sourceBuilder` when the application model is prepared, so later lifetime
     * changes to the source resource are reflected by this resource.
     * @param sourceBuilder The resource builder whose lifetime should be used.
     * @returns The resource builder.
     */
    withLifetimeOf(sourceBuilder) {
        return new PgAdminContainerResourcePromiseImpl(this._withLifetimeOfInternal(sourceBuilder), this._client);
    }
    /** @internal */
    async _withParentProcessLifetimeInternal(parentProcessId) {
        const rpcArgs = { builder: this._handle, parentProcessId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withParentProcessLifetime', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime that ends when a parent process exits.
     * @param parentProcessId The ID of the parent process to monitor.
     * @returns The resource builder.
     */
    withParentProcessLifetime(parentProcessId) {
        return new PgAdminContainerResourcePromiseImpl(this._withParentProcessLifetimeInternal(parentProcessId), this._client);
    }
    /** @internal */
    async _withEnvironmentInternal(name, value) {
        value = isPromiseLike(value) ? await value : value;
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEnvironment', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /** Sets an environment variable */
    withEnvironment(name, value) {
        return new PgAdminContainerResourcePromiseImpl(this._withEnvironmentInternal(name, value), this._client);
    }
    /** @internal */
    async _withEnvironmentCallbackInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new EnvironmentCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEnvironmentCallback', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Allows for the population of environment variables on a resource.
     * @param callback A callback that allows for deferred execution for computing many environment variables. This runs after resources have been allocated by the orchestrator and allows access to other resources to resolve computed data, e.g. connection strings, ports.
     * @returns The resource builder.
     */
    withEnvironmentCallback(callback) {
        return new PgAdminContainerResourcePromiseImpl(this._withEnvironmentCallbackInternal(callback), this._client);
    }
    /** @internal */
    async _withArgsInternal(args) {
        const rpcArgs = { builder: this._handle, args };
        const result = await this._client.invokeCapability('Aspire.Hosting/withArgs', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Adds arguments to be passed to a resource that supports arguments when it is launched.
     * @param args The arguments to be passed to the resource when it is started.
     * @returns The resource builder.
     */
    withArgs(args) {
        return new PgAdminContainerResourcePromiseImpl(this._withArgsInternal(args), this._client);
    }
    /** @internal */
    async _withArgsCallbackInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new CommandLineArgsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withArgsCallback', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a callback to be executed with a list of command-line arguments when a resource is started.
     * @param callback A callback that allows for deferred execution for computing arguments. This runs after resources have been allocated by the orchestrator and allows access to other resources to resolve computed data, e.g. connection strings, ports.
     * @returns The resource builder.
     */
    withArgsCallback(callback) {
        return new PgAdminContainerResourcePromiseImpl(this._withArgsCallbackInternal(callback), this._client);
    }
    /** @internal */
    async _withReferenceEnvironmentInternal(options) {
        const rpcArgs = { builder: this._handle, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withReferenceEnvironment', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Configures how information is injected into environment variables when the resource references other resources.
     * @param options Options controlling which reference information is emitted.
     * @returns The resource builder.
     */
    withReferenceEnvironment(options) {
        return new PgAdminContainerResourcePromiseImpl(this._withReferenceEnvironmentInternal(options), this._client);
    }
    /** @internal */
    async _withReferenceInternal(source, connectionName, optional, name) {
        source = isPromiseLike(source) ? await source : source;
        const rpcArgs = { builder: this._handle, source };
        if (connectionName !== undefined)
            rpcArgs.connectionName = connectionName;
        if (optional !== undefined)
            rpcArgs.optional = optional;
        if (name !== undefined)
            rpcArgs.name = name;
        const result = await this._client.invokeCapability('Aspire.Hosting/withReference', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a reference to another resource
     * @param options Additional options.
     */
    withReference(source, options) {
        const connectionName = options?.connectionName;
        const optional = options?.optional;
        const name = options?.name;
        return new PgAdminContainerResourcePromiseImpl(this._withReferenceInternal(source, connectionName, optional, name), this._client);
    }
    /** @internal */
    async _withEndpointCallbackInternal(endpointName, callback, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpointCallback', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Updates a named endpoint via callback
     * @param options Additional options.
     */
    withEndpointCallback(endpointName, callback, options) {
        const createIfNotExists = options?.createIfNotExists;
        return new PgAdminContainerResourcePromiseImpl(this._withEndpointCallbackInternal(endpointName, callback, createIfNotExists), this._client);
    }
    /** @internal */
    async _withHttpEndpointCallbackInternal(callback, name, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        if (name !== undefined)
            rpcArgs.name = name;
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpEndpointCallback', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Updates an HTTP endpoint via callback
     * @param options Additional options.
     */
    withHttpEndpointCallback(callback, options) {
        const name = options?.name;
        const createIfNotExists = options?.createIfNotExists;
        return new PgAdminContainerResourcePromiseImpl(this._withHttpEndpointCallbackInternal(callback, name, createIfNotExists), this._client);
    }
    /** @internal */
    async _withHttpsEndpointCallbackInternal(callback, name, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        if (name !== undefined)
            rpcArgs.name = name;
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpsEndpointCallback', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Updates an HTTPS endpoint via callback
     * @param options Additional options.
     */
    withHttpsEndpointCallback(callback, options) {
        const name = options?.name;
        const createIfNotExists = options?.createIfNotExists;
        return new PgAdminContainerResourcePromiseImpl(this._withHttpsEndpointCallbackInternal(callback, name, createIfNotExists), this._client);
    }
    /** @internal */
    async _withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (scheme !== undefined)
            rpcArgs.scheme = scheme;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        if (isExternal !== undefined)
            rpcArgs.isExternal = isExternal;
        if (protocol !== undefined)
            rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpoint', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a network endpoint
     * @param options Additional options.
     * @returns The resource builder.
     */
    withEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const scheme = options?.scheme;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        const isExternal = options?.isExternal;
        const protocol = options?.protocol;
        return new PgAdminContainerResourcePromiseImpl(this._withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol), this._client);
    }
    /** @internal */
    async _withEndpointProxySupportInternal(proxyEnabled) {
        const rpcArgs = { builder: this._handle, proxyEnabled };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpointProxySupport', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Set whether a resource can use proxied endpoints or whether they should be disabled for all endpoints belonging to the resource. If set to `false`, endpoints belonging to the resource will ignore the configured proxy settings and run proxy-less.
     *
     * This method is intended to support scenarios with persistent lifetime resources where it is desirable for the resource to be accessible over the same
     * port whether the Aspire application is running or not. Proxied endpoints bind ports that are only accessible while the Aspire application is running.
     * The user needs to be careful to ensure that endpoints are using unique ports when disabling proxy support as by default for proxy-less
     * endpoints, Aspire will allocate the target port as the host port, which will increase the chance of port conflicts.
     * @param proxyEnabled Should endpoints for the resource support using a proxy?
     * @returns The resource builder.
     */
    withEndpointProxySupport(proxyEnabled) {
        return new PgAdminContainerResourcePromiseImpl(this._withEndpointProxySupportInternal(proxyEnabled), this._client);
    }
    /** @internal */
    async _withHttpEndpointInternal(port, targetPort, name, env, isProxied) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpEndpoint', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP endpoint
     *
     * If an endpoint with the same name already exists on the resource, the existing endpoint is updated
     * with any non-null parameter values. Parameters left as `null` will not modify the existing endpoint's values.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new PgAdminContainerResourcePromiseImpl(this._withHttpEndpointInternal(port, targetPort, name, env, isProxied), this._client);
    }
    /** @internal */
    async _withHttpsEndpointInternal(port, targetPort, name, env, isProxied) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpsEndpoint', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTPS endpoint
     *
     * If an endpoint with the same name already exists on the resource, the existing endpoint is updated
     * with any non-null parameter values. Parameters left as `null` will not modify the existing endpoint's values.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpsEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new PgAdminContainerResourcePromiseImpl(this._withHttpsEndpointInternal(port, targetPort, name, env, isProxied), this._client);
    }
    /** @internal */
    async _withExternalHttpEndpointsInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExternalHttpEndpoints', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Marks existing http or https endpoints on a resource as external.
     * @returns The resource builder.
     */
    withExternalHttpEndpoints() {
        return new PgAdminContainerResourcePromiseImpl(this._withExternalHttpEndpointsInternal(), this._client);
    }
    /**
     * Gets an endpoint reference
     * @param name The name of the endpoint.
     * @returns An `EndpointReference` that can be used to resolve the address of the endpoint after resource allocation has occurred.
     */
    getEndpoint(name) {
        const promise = (async () => {
            const rpcArgs = { builder: this._handle, name };
            const handle = await this._client.invokeCapability('Aspire.Hosting/getEndpoint', rpcArgs);
            return new EndpointReferenceImpl(handle, this._client);
        })();
        return new EndpointReferencePromiseImpl(promise, this._client);
    }
    /** @internal */
    async _asHttp2ServiceInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/asHttp2Service', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to mark all endpoints' transport as HTTP/2. This is useful for HTTP/2 services that need prior knowledge.
     * @returns The resource builder.
     */
    asHttp2Service() {
        return new PgAdminContainerResourcePromiseImpl(this._asHttp2ServiceInternal(), this._client);
    }
    /** @internal */
    async _withUrlsInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new ResourceUrlsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrls', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to customize the URLs displayed for the resource.
     * @param callback The callback that will customize URLs for the resource.
     * @returns The resource builder.
     */
    withUrls(callback) {
        return new PgAdminContainerResourcePromiseImpl(this._withUrlsInternal(callback), this._client);
    }
    /** @internal */
    async _withUrlInternal(url, displayText) {
        const rpcArgs = { builder: this._handle, url };
        if (displayText !== undefined)
            rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrl', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Adds or modifies displayed URLs
     * @param options Additional options.
     */
    withUrl(url, options) {
        const displayText = options?.displayText;
        return new PgAdminContainerResourcePromiseImpl(this._withUrlInternal(url, displayText), this._client);
    }
    /** @internal */
    async _withUrlForEndpointInternal(endpointName, callback) {
        const callbackId = registerCallback(async (objData) => {
            const obj = wrapIfHandle(objData);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrlForEndpoint', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to update the URL displayed for the endpoint with the specified name.
     * @param endpointName The name of the endpoint to customize the URL for.
     * @param callback The callback that will customize the URL.
     * @returns The resource builder.
     */
    withUrlForEndpoint(endpointName, callback) {
        return new PgAdminContainerResourcePromiseImpl(this._withUrlForEndpointInternal(endpointName, callback), this._client);
    }
    /** @internal */
    async _excludeFromManifestInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromManifest', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Excludes a resource from being published to the manifest.
     * @returns The resource builder.
     */
    excludeFromManifest() {
        return new PgAdminContainerResourcePromiseImpl(this._excludeFromManifestInternal(), this._client);
    }
    /** @internal */
    async _waitForInternal(dependency, waitBehavior) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (waitBehavior !== undefined)
            rpcArgs.waitBehavior = waitBehavior;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitFor', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Waits for another resource to be ready
     * @param options Additional options.
     */
    waitFor(dependency, options) {
        const waitBehavior = options?.waitBehavior;
        return new PgAdminContainerResourcePromiseImpl(this._waitForInternal(dependency, waitBehavior), this._client);
    }
    /** @internal */
    async _waitForStartInternal(dependency, waitBehavior) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (waitBehavior !== undefined)
            rpcArgs.waitBehavior = waitBehavior;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitForStart', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Waits for another resource to start
     * @param options Additional options.
     */
    waitForStart(dependency, options) {
        const waitBehavior = options?.waitBehavior;
        return new PgAdminContainerResourcePromiseImpl(this._waitForStartInternal(dependency, waitBehavior), this._client);
    }
    /** @internal */
    async _withExplicitStartInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExplicitStart', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Prevents resource from starting automatically
     * @returns The resource builder.
     */
    withExplicitStart() {
        return new PgAdminContainerResourcePromiseImpl(this._withExplicitStartInternal(), this._client);
    }
    /** @internal */
    async _waitForCompletionInternal(dependency, exitCode) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitForResourceCompletion', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Waits for the dependency resource to enter the Exited or Finished state before starting the resource.
     * @param dependency The resource builder for the dependency resource.
     * @param options Additional options.
     * @returns The resource builder.
     */
    waitForCompletion(dependency, options) {
        const exitCode = options?.exitCode;
        return new PgAdminContainerResourcePromiseImpl(this._waitForCompletionInternal(dependency, exitCode), this._client);
    }
    /** @internal */
    async _withHealthCheckInternal(key) {
        const rpcArgs = { builder: this._handle, key };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHealthCheck', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a health check by key
     * @param key The key for the health check.
     * @returns The resource builder.
     */
    withHealthCheck(key) {
        return new PgAdminContainerResourcePromiseImpl(this._withHealthCheckInternal(key), this._client);
    }
    /** @internal */
    async _withHttpHealthCheckInternal(path, statusCode, endpointName) {
        const rpcArgs = { builder: this._handle };
        if (path !== undefined)
            rpcArgs.path = path;
        if (statusCode !== undefined)
            rpcArgs.statusCode = statusCode;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpHealthCheck', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a health check to the resource which is mapped to a specific endpoint.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpHealthCheck(options) {
        const path = options?.path;
        const statusCode = options?.statusCode;
        const endpointName = options?.endpointName;
        return new PgAdminContainerResourcePromiseImpl(this._withHttpHealthCheckInternal(path, statusCode, endpointName), this._client);
    }
    /** @internal */
    async _withCommandInternal(name, displayName, executeCommand, commandOptions) {
        const executeCommandId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined)
            rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability('Aspire.Hosting/withCommand', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a resource command
     *
     * The `WithCommand` method is used to add commands to the resource. Commands are displayed in the dashboard
     * and can be executed by a user using the dashboard UI.
     * When a command is executed, the `executeCommand` callback is called and is run inside the Aspire host.
     * @param name The name of the command. The name uniquely identifies the command.
     * @param displayName The display name visible in UI.
     * @param executeCommand A callback that is executed when the command is executed. The callback is run inside the Aspire host. The callback result is used to indicate success or failure in the UI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withCommand(name, displayName, executeCommand, options) {
        const commandOptions = options?.commandOptions;
        return new PgAdminContainerResourcePromiseImpl(this._withCommandInternal(name, displayName, executeCommand, commandOptions), this._client);
    }
    /** @internal */
    async _withProcessCommandInternal(commandName, displayName, options) {
        const rpcArgs = { builder: this._handle, commandName, displayName, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommand', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /** Adds a command to the resource that starts a local process when invoked. */
    withProcessCommand(commandName, displayName, options) {
        return new PgAdminContainerResourcePromiseImpl(this._withProcessCommandInternal(commandName, displayName, options), this._client);
    }
    /** @internal */
    async _withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options) {
        const createProcessSpecId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await createProcessSpec(arg);
        });
        const rpcArgs = { builder: this._handle, commandName, displayName, createProcessSpec: createProcessSpecId };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommandFactory', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a command to the resource that starts a local process created by a callback when invoked.
     * @param options Additional options.
     */
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new PgAdminContainerResourcePromiseImpl(this._withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options), this._client);
    }
    /** @internal */
    async _withHttpCommandInternal(path, displayName, options) {
        const rpcArgs = { builder: this._handle, path, displayName };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpCommand', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP resource command
     * @param options Additional options.
     */
    withHttpCommand(path, displayName, options) {
        return new PgAdminContainerResourcePromiseImpl(this._withHttpCommandInternal(path, displayName, options), this._client);
    }
    /** @internal */
    async _withDeveloperCertificateTrustInternal(trust) {
        const rpcArgs = { builder: this._handle, trust };
        const result = await this._client.invokeCapability('Aspire.Hosting/withDeveloperCertificateTrust', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Indicates whether developer certificates should be treated as trusted certificate authorities for the resource at run time. Currently this indicates trust for the ASP.NET Core developer certificate. The developer certificate will only be trusted when running in local development scenarios; in publish mode resources will use their default certificate trust.
     *
     * Disable trust for app host managed developer certificate(s) for a container resource.
     * ```
     * var container = builder.AddContainer("my-service", "my-service:latest")
     * .WithDeveloperCertificateTrust(false);
     * ```
     * Disable automatic trust for app host managed developer certificate(s), but explicitly enable it for a specific resource.
     * ```
     * var builder = DistributedApplication.CreateBuilder(new DistributedApplicationOptions()
     * {
     * Args = args,
     * TrustDeveloperCertificate = false,
     * });
     * var project = builder.AddProject<MyService>("my-service")
     * .WithDeveloperCertificateTrust(true);
     * ```
     * @param trust Indicates whether the developer certificate should be treated as trusted.
     * @returns The resource builder.
     */
    withDeveloperCertificateTrust(trust) {
        return new PgAdminContainerResourcePromiseImpl(this._withDeveloperCertificateTrustInternal(trust), this._client);
    }
    /** @internal */
    async _withCertificateTrustScopeInternal(scope) {
        const rpcArgs = { builder: this._handle, scope };
        const result = await this._client.invokeCapability('Aspire.Hosting/withCertificateTrustScope', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Sets the certificate trust scope
     *
     * The default scope if not overridden is `Append` which means that custom certificate
     * authorities should be appended to the default trusted certificate authorities for the resource. Setting the scope to
     * `Override` indicates the set of certificates in referenced
     * `CertificateAuthorityCollection` (and optionally Aspire developer certificiates) should be used as the
     * exclusive source of trust for a resource.
     * In all cases, this is a best effort implementation as not all resources support full customization of certificate
     * trust.
     * Set the scope for custom certificate authorities to override the default trusted certificate authorities for a container resource.
     * ```
     * var caCollection = builder.AddCertificateAuthorityCollection("my-cas")
     * .WithCertificate(new X509Certificate2("my-ca.pem"));
     * var container = builder.AddContainer("my-service", "my-service:latest")
     * .WithCertificateAuthorityCollection(caCollection)
     * .WithCertificateTrustScope(CertificateTrustScope.Override);
     * ```
     * @param scope The scope to apply to custom certificate authorities associated with the resource.
     * @returns The resource builder.
     */
    withCertificateTrustScope(scope) {
        return new PgAdminContainerResourcePromiseImpl(this._withCertificateTrustScopeInternal(scope), this._client);
    }
    /** @internal */
    async _withHttpsDeveloperCertificateInternal(password) {
        password = isPromiseLike(password) ? await password : password;
        const rpcArgs = { builder: this._handle };
        if (password !== undefined)
            rpcArgs.password = password;
        const result = await this._client.invokeCapability('Aspire.Hosting/withParameterHttpsDeveloperCertificate', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Indicates that a resource should use the developer certificate key pair for HTTPS endpoints at run time. Currently this indicates use of the ASP.NET Core developer certificate. The developer certificate will only be used when running in local development scenarios; in publish mode resources will use their default certificate configuration.
     *
     * Use the developer certificate for HTTPS/TLS endpoints on a container resource:
     * ```
     * builder.AddContainer("my-service", "my-image")
     * .WithHttpsDeveloperCertificate()
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpsDeveloperCertificate(options) {
        let password = options?.password;
        return new PgAdminContainerResourcePromiseImpl(this._withHttpsDeveloperCertificateInternal(password), this._client);
    }
    /** @internal */
    async _withoutHttpsCertificateInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withoutHttpsCertificate', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Disable HTTPS/TLS server certificate configuration for the resource. No HTTPS/TLS termination configuration will be applied.
     *
     * Disable HTTPS certificate configuration for a Redis resource:
     * ```
     * var redis = builder.AddRedis("cache")
     * .WithoutHttpsCertificate();
     * ```
     * @returns The resource builder.
     */
    withoutHttpsCertificate() {
        return new PgAdminContainerResourcePromiseImpl(this._withoutHttpsCertificateInternal(), this._client);
    }
    /** @internal */
    async _withRelationshipInternal(resourceBuilder, type) {
        resourceBuilder = isPromiseLike(resourceBuilder) ? await resourceBuilder : resourceBuilder;
        const rpcArgs = { builder: this._handle, resourceBuilder, type };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderRelationship', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a relationship to another resource using its builder.
     * @param resourceBuilder The resource builder that the relationship is to.
     * @param type The relationship type.
     * @returns A resource builder.
     */
    withRelationship(resourceBuilder, type) {
        return new PgAdminContainerResourcePromiseImpl(this._withRelationshipInternal(resourceBuilder, type), this._client);
    }
    /** @internal */
    async _withParentRelationshipInternal(parent) {
        parent = isPromiseLike(parent) ? await parent : parent;
        const rpcArgs = { builder: this._handle, parent };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderParentRelationship', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Sets the parent relationship
     * @param parent The parent of `builder`.
     * @returns A resource builder.
     */
    withParentRelationship(parent) {
        return new PgAdminContainerResourcePromiseImpl(this._withParentRelationshipInternal(parent), this._client);
    }
    /** @internal */
    async _withChildRelationshipInternal(child) {
        child = isPromiseLike(child) ? await child : child;
        const rpcArgs = { builder: this._handle, child };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderChildRelationship', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Sets a child relationship
     * @param child The child of `builder`.
     * @returns A resource builder.
     */
    withChildRelationship(child) {
        return new PgAdminContainerResourcePromiseImpl(this._withChildRelationshipInternal(child), this._client);
    }
    /** @internal */
    async _withIconNameInternal(iconName, iconVariant) {
        const rpcArgs = { builder: this._handle, iconName };
        if (iconVariant !== undefined)
            rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability('Aspire.Hosting/withIconName', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Specifies the icon to use when displaying the resource in the dashboard.
     * @param iconName The name of the FluentUI icon to use. See https://aka.ms/fluentui-system-icons for available icons.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withIconName(iconName, options) {
        const iconVariant = options?.iconVariant;
        return new PgAdminContainerResourcePromiseImpl(this._withIconNameInternal(iconName, iconVariant), this._client);
    }
    /** @internal */
    async _withComputeEnvironmentInternal(computeEnvironmentResource) {
        computeEnvironmentResource = isPromiseLike(computeEnvironmentResource) ? await computeEnvironmentResource : computeEnvironmentResource;
        const rpcArgs = { builder: this._handle, computeEnvironmentResource };
        const result = await this._client.invokeCapability('Aspire.Hosting/withComputeEnvironment', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Configures the compute environment for the compute resource.
     *
     * This method allows associating a specific compute environment with the compute resource.
     * @param computeEnvironmentResource The compute environment resource to associate with the compute resource.
     * @returns The resource builder.
     */
    withComputeEnvironment(computeEnvironmentResource) {
        return new PgAdminContainerResourcePromiseImpl(this._withComputeEnvironmentInternal(computeEnvironmentResource), this._client);
    }
    /** @internal */
    async _withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName) {
        const rpcArgs = { builder: this._handle, probeType };
        if (path !== undefined)
            rpcArgs.path = path;
        if (initialDelaySeconds !== undefined)
            rpcArgs.initialDelaySeconds = initialDelaySeconds;
        if (periodSeconds !== undefined)
            rpcArgs.periodSeconds = periodSeconds;
        if (timeoutSeconds !== undefined)
            rpcArgs.timeoutSeconds = timeoutSeconds;
        if (failureThreshold !== undefined)
            rpcArgs.failureThreshold = failureThreshold;
        if (successThreshold !== undefined)
            rpcArgs.successThreshold = successThreshold;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpProbe', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP health probe to the resource
     * @param options Additional options.
     */
    withHttpProbe(probeType, options) {
        const path = options?.path;
        const initialDelaySeconds = options?.initialDelaySeconds;
        const periodSeconds = options?.periodSeconds;
        const timeoutSeconds = options?.timeoutSeconds;
        const failureThreshold = options?.failureThreshold;
        const successThreshold = options?.successThreshold;
        const endpointName = options?.endpointName;
        return new PgAdminContainerResourcePromiseImpl(this._withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName), this._client);
    }
    /** @internal */
    async _excludeFromMcpInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromMcp', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Exclude the resource from MCP operations using the Aspire MCP server. The resource is excluded from results that return resources, console logs and telemetry.
     * @returns The resource builder.
     */
    excludeFromMcp() {
        return new PgAdminContainerResourcePromiseImpl(this._excludeFromMcpInternal(), this._client);
    }
    /** @internal */
    async _withHiddenInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHidden', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists
     *
     * Use this method to hide resources that are implementation details and should never be displayed by default.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @returns The resource builder.
     */
    withHidden() {
        return new PgAdminContainerResourcePromiseImpl(this._withHiddenInternal(), this._client);
    }
    /** @internal */
    async _withHiddenOnCompletionInternal(exitCode, exitCodes) {
        const rpcArgs = { builder: this._handle };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        if (exitCodes !== undefined)
            rpcArgs.exitCodes = exitCodes;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHiddenOnCompletion', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists after successful completion
     *
     * This method is useful for one-off resources such as setup scripts, migrations, or build steps that should remain visible while running
     * and then be hidden after successful completion.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHiddenOnCompletion(options) {
        const exitCode = options?.exitCode;
        const exitCodes = options?.exitCodes;
        return new PgAdminContainerResourcePromiseImpl(this._withHiddenOnCompletionInternal(exitCode, exitCodes), this._client);
    }
    /** @internal */
    async _withImagePushOptionsInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ContainerImagePushOptionsCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImagePushOptions', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Adds an asynchronous callback to configure container image push options for the resource.
     *
     * This method allows customization of how container images are named and tagged when pushed to a registry using an asynchronous callback.
     * Use this overload when the callback needs to perform asynchronous operations such as retrieving configuration values from external sources.
     * The callback receives a `ContainerImagePushOptionsCallbackContext` that provides access to the resource
     * and the `ContainerImagePushOptions` that can be modified.
     * Multiple callbacks can be registered on the same resource, and they will be invoked in the order they were added.
     * @param callback The asynchronous callback to configure push options.
     * @returns The resource builder.
     */
    withImagePushOptions(callback) {
        return new PgAdminContainerResourcePromiseImpl(this._withImagePushOptionsInternal(callback), this._client);
    }
    /** @internal */
    async _withRemoteImageNameInternal(remoteImageName) {
        const rpcArgs = { builder: this._handle, remoteImageName };
        const result = await this._client.invokeCapability('Aspire.Hosting/withRemoteImageName', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Sets the remote image name (without registry endpoint or tag) for container push operations.
     *
     * Use this with `withRemoteImageTag` to fully customize the image reference used for container push operations.
     * @param remoteImageName The remote image name (e.g., "myapp" or "myorg/myapp").
     * @returns The resource builder.
     */
    withRemoteImageName(remoteImageName) {
        return new PgAdminContainerResourcePromiseImpl(this._withRemoteImageNameInternal(remoteImageName), this._client);
    }
    /** @internal */
    async _withRemoteImageTagInternal(remoteImageTag) {
        const rpcArgs = { builder: this._handle, remoteImageTag };
        const result = await this._client.invokeCapability('Aspire.Hosting/withRemoteImageTag', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Sets the remote image tag for container push operations.
     *
     * Use this with `withRemoteImageName` to fully customize the image reference used for container push operations.
     * @param remoteImageTag The remote image tag (e.g., "latest", "v1.0.0").
     * @returns The resource builder.
     */
    withRemoteImageTag(remoteImageTag) {
        return new PgAdminContainerResourcePromiseImpl(this._withRemoteImageTagInternal(remoteImageTag), this._client);
    }
    /** @internal */
    async _withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new PipelineStepContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined)
            rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined)
            rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined)
            rpcArgs.tags = tags;
        if (description !== undefined)
            rpcArgs.description = description;
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineStepFactory', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a pipeline step to the resource that will be executed during deployment.
     * @param stepName The unique name of the pipeline step.
     * @param callback The callback to execute when the step runs.
     * @param options Additional options.
     * @returns The resource builder for chaining.
     */
    withPipelineStepFactory(stepName, callback, options) {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new PgAdminContainerResourcePromiseImpl(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description), this._client);
    }
    /** @internal */
    async _withPipelineConfigurationInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new PipelineConfigurationContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineConfiguration', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to be executed during the pipeline configuration phase, allowing modification of step dependencies and relationships.
     * @param callback The callback function to execute during the configuration phase.
     * @returns The resource builder for chaining.
     */
    withPipelineConfiguration(callback) {
        return new PgAdminContainerResourcePromiseImpl(this._withPipelineConfigurationInternal(callback), this._client);
    }
    /** @internal */
    async _withVolumeInternal(target, name, isReadOnly) {
        const rpcArgs = { resource: this._handle, target };
        if (name !== undefined)
            rpcArgs.name = name;
        if (isReadOnly !== undefined)
            rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability('Aspire.Hosting/withVolume', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a volume to a container resource.
     *
     * Volumes persist data across container restarts. Named volumes are managed
     * by Docker/Podman and stored in a system-managed location.
     * Why this wrapper exists: The original `ContainerResourceBuilderExtensions.WithVolume`
     * has parameter order `(name?, target, isReadOnly)` where the optional `name` comes first.
     * This wrapper reorders parameters to `(target, name?, isReadOnly)` so the required `target`
     * parameter comes first, providing a better API for polyglot consumers.
     * @param target The mount path inside the container.
     * @param options Additional options.
     * @returns The same resource builder handle for chaining.
     */
    withVolume(target, options) {
        const name = options?.name;
        const isReadOnly = options?.isReadOnly;
        return new PgAdminContainerResourcePromiseImpl(this._withVolumeInternal(target, name, isReadOnly), this._client);
    }
    /**
     * Gets the name of the resource from a builder.
     *
     * Why this wrapper exists: This capability accesses a nested property
     * (`resource.Resource.Name`) which requires a wrapper method. There is no single
     * .NET method that returns just the resource name that could be annotated directly.
     * @returns The resource name.
     */
    async getResourceName() {
        const rpcArgs = { resource: this._handle };
        return await this._client.invokeCapability('Aspire.Hosting/getResourceName', rpcArgs);
    }
    /** @internal */
    async _onBeforeResourceStartedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new BeforeResourceStartedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onBeforeResourceStarted', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the BeforeResourceStarted event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onBeforeResourceStarted(callback) {
        return new PgAdminContainerResourcePromiseImpl(this._onBeforeResourceStartedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceStoppedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceStoppedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceStopped', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceStopped event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceStopped(callback) {
        return new PgAdminContainerResourcePromiseImpl(this._onResourceStoppedInternal(callback), this._client);
    }
    /** @internal */
    async _onInitializeResourceInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new InitializeResourceEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onInitializeResource', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the InitializeResource event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onInitializeResource(callback) {
        return new PgAdminContainerResourcePromiseImpl(this._onInitializeResourceInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceEndpointsAllocatedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceEndpointsAllocatedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceEndpointsAllocated', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceEndpointsAllocated event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceEndpointsAllocated(callback) {
        return new PgAdminContainerResourcePromiseImpl(this._onResourceEndpointsAllocatedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceReadyInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceReadyEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceReady', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceReady event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceReady(callback) {
        return new PgAdminContainerResourcePromiseImpl(this._onResourceReadyInternal(callback), this._client);
    }
    /**
     * Creates an execution configuration builder for the specified resource.
     * @returns The execution configuration builder.
     */
    createExecutionConfiguration() {
        const promise = (async () => {
            const rpcArgs = { resource: this._handle };
            const handle = await this._client.invokeCapability('Aspire.Hosting/createExecutionConfiguration', rpcArgs);
            return new ExecutionConfigurationBuilderImpl(handle, this._client);
        })();
        return new ExecutionConfigurationBuilderPromiseImpl(promise, this._client);
    }
    /** @internal */
    async _withHostPortInternal(port) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        const result = await this._client.invokeCapability('Aspire.Hosting.PostgreSQL/withPgAdminHostPort', rpcArgs);
        return new PgAdminContainerResourceImpl(result, this._client);
    }
    /**
     * Configures the host port that the PGAdmin resource is exposed on instead of using randomly assigned port.
     * @param options Additional options.
     * @returns The resource builder for PGAdmin.
     */
    withHostPort(options) {
        const port = options?.port;
        return new PgAdminContainerResourcePromiseImpl(this._withHostPortInternal(port), this._client);
    }
}
/**
 * Thenable wrapper for PgAdminContainerResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
class PgAdminContainerResourcePromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    withContainerRegistry(registry) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withContainerRegistry(registry)), this._client);
    }
    withBindMount(source, target, options) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withBindMount(source, target, options)), this._client);
    }
    withEntrypoint(entrypoint) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withEntrypoint(entrypoint)), this._client);
    }
    withImageTag(tag) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withImageTag(tag)), this._client);
    }
    withImageRegistry(registry) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withImageRegistry(registry)), this._client);
    }
    withImage(image, options) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withImage(image, options)), this._client);
    }
    withImageSHA256(sha256) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withImageSHA256(sha256)), this._client);
    }
    withContainerRuntimeArgs(args) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withContainerRuntimeArgs(args)), this._client);
    }
    withLifetime(lifetime) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withLifetime(lifetime)), this._client);
    }
    withImagePullPolicy(pullPolicy) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withImagePullPolicy(pullPolicy)), this._client);
    }
    publishAsContainer() {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.publishAsContainer()), this._client);
    }
    withDockerfile(contextPath, options) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withDockerfile(contextPath, options)), this._client);
    }
    withDockerfileFactory(contextPath, dockerfileFactory, options) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileFactory(contextPath, dockerfileFactory, options)), this._client);
    }
    withContainerName(name) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withContainerName(name)), this._client);
    }
    withBuildArg(name, value) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withBuildArg(name, value)), this._client);
    }
    withBuildSecret(name, value) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withBuildSecret(name, value)), this._client);
    }
    withContainerCertificatePaths(options) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withContainerCertificatePaths(options)), this._client);
    }
    withDockerfileBuilder(contextPath, callback, options) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileBuilder(contextPath, callback, options)), this._client);
    }
    withDockerfileBaseImage(options) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileBaseImage(options)), this._client);
    }
    withContainerNetworkAlias(alias) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withContainerNetworkAlias(alias)), this._client);
    }
    withMcpServer(options) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withMcpServer(options)), this._client);
    }
    withOtlpExporter(options) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withOtlpExporter(options)), this._client);
    }
    publishAsConnectionString() {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.publishAsConnectionString()), this._client);
    }
    withRequiredCommand(command, options) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withRequiredCommand(command, options)), this._client);
    }
    withSessionLifetime() {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withSessionLifetime()), this._client);
    }
    withPersistentLifetime() {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withPersistentLifetime()), this._client);
    }
    withLifetimeOf(sourceBuilder) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withLifetimeOf(sourceBuilder)), this._client);
    }
    withParentProcessLifetime(parentProcessId) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withParentProcessLifetime(parentProcessId)), this._client);
    }
    withEnvironment(name, value) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withEnvironment(name, value)), this._client);
    }
    withEnvironmentCallback(callback) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withEnvironmentCallback(callback)), this._client);
    }
    withArgs(args) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withArgs(args)), this._client);
    }
    withArgsCallback(callback) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withArgsCallback(callback)), this._client);
    }
    withReferenceEnvironment(options) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withReferenceEnvironment(options)), this._client);
    }
    withReference(source, options) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withReference(source, options)), this._client);
    }
    withEndpointCallback(endpointName, callback, options) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withEndpointCallback(endpointName, callback, options)), this._client);
    }
    withHttpEndpointCallback(callback, options) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withHttpEndpointCallback(callback, options)), this._client);
    }
    withHttpsEndpointCallback(callback, options) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withHttpsEndpointCallback(callback, options)), this._client);
    }
    withEndpoint(options) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withEndpoint(options)), this._client);
    }
    withEndpointProxySupport(proxyEnabled) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withEndpointProxySupport(proxyEnabled)), this._client);
    }
    withHttpEndpoint(options) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withHttpEndpoint(options)), this._client);
    }
    withHttpsEndpoint(options) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withHttpsEndpoint(options)), this._client);
    }
    withExternalHttpEndpoints() {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withExternalHttpEndpoints()), this._client);
    }
    getEndpoint(name) {
        return new EndpointReferencePromiseImpl(this._promise.then(obj => obj.getEndpoint(name)), this._client);
    }
    asHttp2Service() {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.asHttp2Service()), this._client);
    }
    withUrls(callback) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withUrls(callback)), this._client);
    }
    withUrl(url, options) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withUrl(url, options)), this._client);
    }
    withUrlForEndpoint(endpointName, callback) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)), this._client);
    }
    excludeFromManifest() {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.excludeFromManifest()), this._client);
    }
    waitFor(dependency, options) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.waitFor(dependency, options)), this._client);
    }
    waitForStart(dependency, options) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.waitForStart(dependency, options)), this._client);
    }
    withExplicitStart() {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withExplicitStart()), this._client);
    }
    waitForCompletion(dependency, options) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.waitForCompletion(dependency, options)), this._client);
    }
    withHealthCheck(key) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withHealthCheck(key)), this._client);
    }
    withHttpHealthCheck(options) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withHttpHealthCheck(options)), this._client);
    }
    withCommand(name, displayName, executeCommand, options) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)), this._client);
    }
    withProcessCommand(commandName, displayName, options) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommand(commandName, displayName, options)), this._client);
    }
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommandFactory(commandName, displayName, createProcessSpec, options)), this._client);
    }
    withHttpCommand(path, displayName, options) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withHttpCommand(path, displayName, options)), this._client);
    }
    withDeveloperCertificateTrust(trust) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withDeveloperCertificateTrust(trust)), this._client);
    }
    withCertificateTrustScope(scope) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withCertificateTrustScope(scope)), this._client);
    }
    withHttpsDeveloperCertificate(options) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withHttpsDeveloperCertificate(options)), this._client);
    }
    withoutHttpsCertificate() {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withoutHttpsCertificate()), this._client);
    }
    withRelationship(resourceBuilder, type) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withRelationship(resourceBuilder, type)), this._client);
    }
    withParentRelationship(parent) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withParentRelationship(parent)), this._client);
    }
    withChildRelationship(child) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withChildRelationship(child)), this._client);
    }
    withIconName(iconName, options) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withIconName(iconName, options)), this._client);
    }
    withComputeEnvironment(computeEnvironmentResource) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withComputeEnvironment(computeEnvironmentResource)), this._client);
    }
    withHttpProbe(probeType, options) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withHttpProbe(probeType, options)), this._client);
    }
    excludeFromMcp() {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.excludeFromMcp()), this._client);
    }
    withHidden() {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withHidden()), this._client);
    }
    withHiddenOnCompletion(options) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withHiddenOnCompletion(options)), this._client);
    }
    withImagePushOptions(callback) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withImagePushOptions(callback)), this._client);
    }
    withRemoteImageName(remoteImageName) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withRemoteImageName(remoteImageName)), this._client);
    }
    withRemoteImageTag(remoteImageTag) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withRemoteImageTag(remoteImageTag)), this._client);
    }
    withPipelineStepFactory(stepName, callback, options) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)), this._client);
    }
    withPipelineConfiguration(callback) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withPipelineConfiguration(callback)), this._client);
    }
    withVolume(target, options) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withVolume(target, options)), this._client);
    }
    getResourceName() {
        return this._promise.then(obj => obj.getResourceName());
    }
    onBeforeResourceStarted(callback) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.onBeforeResourceStarted(callback)), this._client);
    }
    onResourceStopped(callback) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.onResourceStopped(callback)), this._client);
    }
    onInitializeResource(callback) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.onInitializeResource(callback)), this._client);
    }
    onResourceEndpointsAllocated(callback) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.onResourceEndpointsAllocated(callback)), this._client);
    }
    onResourceReady(callback) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.onResourceReady(callback)), this._client);
    }
    createExecutionConfiguration() {
        return new ExecutionConfigurationBuilderPromiseImpl(this._promise.then(obj => obj.createExecutionConfiguration()), this._client);
    }
    withHostPort(options) {
        return new PgAdminContainerResourcePromiseImpl(this._promise.then(obj => obj.withHostPort(options)), this._client);
    }
}
// ============================================================================
// PgWebContainerResourceImpl
// ============================================================================
class PgWebContainerResourceImpl extends ResourceBuilderBase {
    constructor(handle, client) {
        super(handle, client);
    }
    /** @internal */
    async _withContainerRegistryInternal(registry) {
        registry = isPromiseLike(registry) ? await registry : registry;
        const rpcArgs = { builder: this._handle, registry };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerRegistry', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Configures the resource to use the specified container registry for container image operations.
     *
     * This method adds a `ContainerRegistryReferenceAnnotation` to the resource,
     * indicating that the resource should use the specified container registry for container image operations.
     * @param registry The container registry resource builder.
     * @returns The resource builder for chaining.
     */
    withContainerRegistry(registry) {
        return new PgWebContainerResourcePromiseImpl(this._withContainerRegistryInternal(registry), this._client);
    }
    /** @internal */
    async _withBindMountInternal(source, target, isReadOnly) {
        const rpcArgs = { builder: this._handle, source, target };
        if (isReadOnly !== undefined)
            rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability('Aspire.Hosting/withBindMount', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a bind mount to a container resource.
     * @param source The source path of the mount. This is the path to the file or directory on the host, relative to the app host project directory.
     * @param target The target path where the file or directory is mounted in the container.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withBindMount(source, target, options) {
        const isReadOnly = options?.isReadOnly;
        return new PgWebContainerResourcePromiseImpl(this._withBindMountInternal(source, target, isReadOnly), this._client);
    }
    /** @internal */
    async _withEntrypointInternal(entrypoint) {
        const rpcArgs = { builder: this._handle, entrypoint };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEntrypoint', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Sets the Entrypoint for the container.
     * @param entrypoint The new entrypoint for the container.
     * @returns The resource builder.
     */
    withEntrypoint(entrypoint) {
        return new PgWebContainerResourcePromiseImpl(this._withEntrypointInternal(entrypoint), this._client);
    }
    /** @internal */
    async _withImageTagInternal(tag) {
        const rpcArgs = { builder: this._handle, tag };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImageTag', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Allows overriding the image tag on a container.
     * @param tag Tag value.
     * @returns The resource builder.
     */
    withImageTag(tag) {
        return new PgWebContainerResourcePromiseImpl(this._withImageTagInternal(tag), this._client);
    }
    /** @internal */
    async _withImageRegistryInternal(registry) {
        const rpcArgs = { builder: this._handle, registry };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImageRegistry', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Allows overriding the image registry on a container.
     * @param registry Registry value.
     * @returns The resource builder.
     */
    withImageRegistry(registry) {
        return new PgWebContainerResourcePromiseImpl(this._withImageRegistryInternal(registry), this._client);
    }
    /** @internal */
    async _withImageInternal(image, tag) {
        const rpcArgs = { builder: this._handle, image };
        if (tag !== undefined)
            rpcArgs.tag = tag;
        const result = await this._client.invokeCapability('Aspire.Hosting/withImage', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Allows overriding the image on a container.
     * @param image Image value.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withImage(image, options) {
        const tag = options?.tag;
        return new PgWebContainerResourcePromiseImpl(this._withImageInternal(image, tag), this._client);
    }
    /** @internal */
    async _withImageSHA256Internal(sha256) {
        const rpcArgs = { builder: this._handle, sha256 };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImageSHA256', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Allows setting the image to a specific sha256 on a container.
     * @param sha256 Registry value.
     * @returns The resource builder.
     */
    withImageSHA256(sha256) {
        return new PgWebContainerResourcePromiseImpl(this._withImageSHA256Internal(sha256), this._client);
    }
    /** @internal */
    async _withContainerRuntimeArgsInternal(args) {
        const rpcArgs = { builder: this._handle, args };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerRuntimeArgs', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a callback to be executed with a list of arguments to add to the container runtime run command when a container resource is started.
     * @param args The arguments to be passed to the container runtime run command when the container resource is started.
     * @returns The resource builder.
     */
    withContainerRuntimeArgs(args) {
        return new PgWebContainerResourcePromiseImpl(this._withContainerRuntimeArgsInternal(args), this._client);
    }
    /** @internal */
    async _withLifetimeInternal(lifetime) {
        const rpcArgs = { builder: this._handle, lifetime };
        const result = await this._client.invokeCapability('Aspire.Hosting/withLifetime', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Sets the lifetime behavior of the container resource.
     *
     * Prefer `WithPersistentLifetime``1` or
     * `WithSessionLifetime``1` for new code.
     * Marking a container resource to have a `Persistent` lifetime.
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddContainer("mycontainer", "myimage")
     * .WithPersistentLifetime();
     * builder.Build().Run();
     * ```
     * @param lifetime The lifetime behavior of the container resource. The default behavior is `Session`.
     * @returns The resource builder.
     */
    withLifetime(lifetime) {
        return new PgWebContainerResourcePromiseImpl(this._withLifetimeInternal(lifetime), this._client);
    }
    /** @internal */
    async _withImagePullPolicyInternal(pullPolicy) {
        const rpcArgs = { builder: this._handle, pullPolicy };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImagePullPolicy', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Sets the pull policy for the container resource.
     * @param pullPolicy The pull policy behavior for the container resource.
     * @returns The resource builder.
     */
    withImagePullPolicy(pullPolicy) {
        return new PgWebContainerResourcePromiseImpl(this._withImagePullPolicyInternal(pullPolicy), this._client);
    }
    /** @internal */
    async _publishAsContainerInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/publishAsContainer', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Changes the resource to be published as a container in the manifest.
     * @returns The resource builder.
     */
    publishAsContainer() {
        return new PgWebContainerResourcePromiseImpl(this._publishAsContainerInternal(), this._client);
    }
    /** @internal */
    async _withDockerfileInternal(contextPath, dockerfilePath, stage) {
        const rpcArgs = { builder: this._handle, contextPath };
        if (dockerfilePath !== undefined)
            rpcArgs.dockerfilePath = dockerfilePath;
        if (stage !== undefined)
            rpcArgs.stage = stage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfile', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Causes Aspire to build the specified container image from a Dockerfile.
     * @param contextPath Path to be used as the context for the container image build.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfile(contextPath, options) {
        const dockerfilePath = options?.dockerfilePath;
        const stage = options?.stage;
        return new PgWebContainerResourcePromiseImpl(this._withDockerfileInternal(contextPath, dockerfilePath, stage), this._client);
    }
    /** @internal */
    async _withDockerfileFactoryInternal(contextPath, dockerfileFactory, stage) {
        const dockerfileFactoryId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new DockerfileFactoryContextImpl(argHandle, this._client);
            return await dockerfileFactory(arg);
        });
        const rpcArgs = { builder: this._handle, contextPath, dockerfileFactory: dockerfileFactoryId };
        if (stage !== undefined)
            rpcArgs.stage = stage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileFactory', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Builds the specified container image from a Dockerfile generated by an asynchronous factory function.
     *
     * When this method is called, an annotation is added to the `ContainerResource` that specifies the context path
     * and a factory function that generates Dockerfile content. The factory is invoked at build time to produce the Dockerfile,
     * which is then written to a temporary file and used by the orchestrator to build the container image.
     * The `contextPath` is relative to the AppHost directory unless it is fully qualified.
     * The factory function is invoked once during the build process to generate the Dockerfile content.
     * The output is trusted and not validated.
     * Creates a container called `mycontainer` with a dynamically generated Dockerfile.
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddContainer("mycontainer", "myimage")
     * .WithDockerfileFactory("path/to/context", async context =>
     * {
     * var template = await File.ReadAllTextAsync("template.dockerfile", context.CancellationToken);
     * return template.Replace("{{VERSION}}", "1.0");
     * });
     * builder.Build().Run();
     * ```
     * @param contextPath Path to be used as the context for the container image build.
     * @param dockerfileFactory An asynchronous function that returns the Dockerfile content as a string.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileFactory(contextPath, dockerfileFactory, options) {
        const stage = options?.stage;
        return new PgWebContainerResourcePromiseImpl(this._withDockerfileFactoryInternal(contextPath, dockerfileFactory, stage), this._client);
    }
    /** @internal */
    async _withContainerNameInternal(name) {
        const rpcArgs = { builder: this._handle, name };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerName', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Overrides the default container name for this resource. By default Aspire generates a unique container name based on the resource name and a random postfix (or a postfix based on a hash of the AppHost project path for persistent container resources). This method allows you to override that behavior with a custom name, but could lead to naming conflicts if the specified name is not unique.
     *
     * Combining this with `Persistent` will allow Aspire to re-use an existing container that was not
     * created by an Aspire AppHost.
     * @param name The desired container name. Must be a valid container name or your runtime will report an error.
     * @returns The resource builder.
     */
    withContainerName(name) {
        return new PgWebContainerResourcePromiseImpl(this._withContainerNameInternal(name), this._client);
    }
    /** @internal */
    async _withBuildArgInternal(name, value) {
        value = isPromiseLike(value) ? await value : value;
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuildArg', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a build argument when the container is built from a Dockerfile.
     * @param name The name of the build argument.
     * @param value The build argument value, either a string or a parameter resource.
     * @returns The resource builder.
     */
    withBuildArg(name, value) {
        return new PgWebContainerResourcePromiseImpl(this._withBuildArgInternal(name, value), this._client);
    }
    /** @internal */
    async _withBuildSecretInternal(name, value) {
        value = isPromiseLike(value) ? await value : value;
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withParameterBuildSecret', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a secret build argument when the container is built from a Dockerfile.
     * @param name The name of the secret build argument.
     * @param value The resource builder for a parameter resource.
     * @returns The resource builder.
     */
    withBuildSecret(name, value) {
        return new PgWebContainerResourcePromiseImpl(this._withBuildSecretInternal(name, value), this._client);
    }
    /** @internal */
    async _withContainerCertificatePathsInternal(customCertificatesDestination, defaultCertificateBundlePaths, defaultCertificateDirectoryPaths) {
        const rpcArgs = { builder: this._handle };
        if (customCertificatesDestination !== undefined)
            rpcArgs.customCertificatesDestination = customCertificatesDestination;
        if (defaultCertificateBundlePaths !== undefined)
            rpcArgs.defaultCertificateBundlePaths = defaultCertificateBundlePaths;
        if (defaultCertificateDirectoryPaths !== undefined)
            rpcArgs.defaultCertificateDirectoryPaths = defaultCertificateDirectoryPaths;
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerCertificatePaths', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Adds container certificate path overrides used for certificate trust at run time.
     * @param options Additional options.
     * @returns The updated resource builder.
     */
    withContainerCertificatePaths(options) {
        const customCertificatesDestination = options?.customCertificatesDestination;
        const defaultCertificateBundlePaths = options?.defaultCertificateBundlePaths;
        const defaultCertificateDirectoryPaths = options?.defaultCertificateDirectoryPaths;
        return new PgWebContainerResourcePromiseImpl(this._withContainerCertificatePathsInternal(customCertificatesDestination, defaultCertificateBundlePaths, defaultCertificateDirectoryPaths), this._client);
    }
    /** @internal */
    async _withDockerfileBuilderInternal(contextPath, callback, stage) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new DockerfileBuilderCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, contextPath, callback: callbackId };
        if (stage !== undefined)
            rpcArgs.stage = stage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileBuilder', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Configures the resource to use a programmatically generated Dockerfile
     *
     * This method provides a programmatic way to build Dockerfiles using the `DockerfileBuilder` API
     * instead of string manipulation. Callbacks can be composed by calling this method multiple times - each callback will be invoked
     * in order to build up the final Dockerfile.
     * The `contextPath` is relative to the AppHost directory unless it is fully qualified.
     * Creates a container with a programmatically built Dockerfile using fluent API:
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddContainer("mycontainer", "myimage")
     * .WithDockerfileBuilder("path/to/context", context =>
     * {
     * context.Builder.From("alpine:latest")
     * .WorkDir("/app")
     * .Run("apk add curl")
     * .Copy(".", ".")
     * .Cmd(["./myapp"]);
     * return Task.CompletedTask;
     * });
     * builder.Build().Run();
     * ```
     * @param contextPath Path to be used as the context for the container image build.
     * @param callback A callback that uses the `DockerfileBuilder` API to construct the Dockerfile.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileBuilder(contextPath, callback, options) {
        const stage = options?.stage;
        return new PgWebContainerResourcePromiseImpl(this._withDockerfileBuilderInternal(contextPath, callback, stage), this._client);
    }
    /** @internal */
    async _withDockerfileBaseImageInternal(buildImage, runtimeImage) {
        const rpcArgs = { builder: this._handle };
        if (buildImage !== undefined)
            rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined)
            rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileBaseImage', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Configures custom base images for generated Dockerfiles.
     *
     * This extension method allows customization of the base images used in generated Dockerfiles.
     * For multi-stage Dockerfiles (e.g., Python with UV), you can specify separate build and runtime images.
     * Specify custom base images for a Python application:
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddPythonApp("myapp", "path/to/app", "main.py")
     * .WithDockerfileBaseImage(
     * buildImage: "ghcr.io/astral-sh/uv:python3.12-bookworm-slim",
     * runtimeImage: "python:3.12-slim-bookworm");
     * builder.Build().Run();
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileBaseImage(options) {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new PgWebContainerResourcePromiseImpl(this._withDockerfileBaseImageInternal(buildImage, runtimeImage), this._client);
    }
    /** @internal */
    async _withContainerNetworkAliasInternal(alias) {
        const rpcArgs = { builder: this._handle, alias };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerNetworkAlias', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a network alias to container resource.
     *
     * Network aliases enable DNS resolution of the container on the network by custom names.
     * By default, containers are accessible on the network using their resource name as a DNS alias.
     * This method allows adding additional aliases for the same container.
     * Multiple aliases can be added by calling this method multiple times.
     * @param alias The network alias for the container.
     * @returns The resource builder.
     */
    withContainerNetworkAlias(alias) {
        return new PgWebContainerResourcePromiseImpl(this._withContainerNetworkAliasInternal(alias), this._client);
    }
    /** @internal */
    async _withMcpServerInternal(path, endpointName) {
        const rpcArgs = { builder: this._handle };
        if (path !== undefined)
            rpcArgs.path = path;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withMcpServer', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Marks the resource as hosting a Model Context Protocol (MCP) server on the specified endpoint.
     *
     * This method adds an `McpServerEndpointAnnotation` to the resource, enabling the Aspire tooling
     * to discover and proxy the MCP server exposed by the resource.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withMcpServer(options) {
        const path = options?.path;
        const endpointName = options?.endpointName;
        return new PgWebContainerResourcePromiseImpl(this._withMcpServerInternal(path, endpointName), this._client);
    }
    /** @internal */
    async _withOtlpExporterInternal(protocol) {
        const rpcArgs = { builder: this._handle };
        if (protocol !== undefined)
            rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability('Aspire.Hosting/withOtlpExporter', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Configures OTLP telemetry export
     * @param options Additional options.
     */
    withOtlpExporter(options) {
        const protocol = options?.protocol;
        return new PgWebContainerResourcePromiseImpl(this._withOtlpExporterInternal(protocol), this._client);
    }
    /** @internal */
    async _publishAsConnectionStringInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/publishAsConnectionString', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Changes the resource to be published as a connection string reference in the manifest.
     * @returns The resource builder.
     */
    publishAsConnectionString() {
        return new PgWebContainerResourcePromiseImpl(this._publishAsConnectionStringInternal(), this._client);
    }
    /** @internal */
    async _withRequiredCommandInternal(command, helpLink) {
        const rpcArgs = { builder: this._handle, command };
        if (helpLink !== undefined)
            rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability('Aspire.Hosting/withRequiredCommand', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Declares that a resource requires a specific command/executable to be available on the local machine PATH before it can start.
     *
     * The command is considered valid if either:
     * 1. It is an absolute or relative path (contains a directory separator) that points to an existing file, or
     * 2. It is discoverable on the current process PATH (respecting PATHEXT on Windows).
     * If the command is not found, a warning message will be logged but the resource will be allowed to attempt to start.
     * @param command The command string (file name or path) that should be validated.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withRequiredCommand(command, options) {
        const helpLink = options?.helpLink;
        return new PgWebContainerResourcePromiseImpl(this._withRequiredCommandInternal(command, helpLink), this._client);
    }
    /** @internal */
    async _withSessionLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withSessionLifetime', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a session lifetime.
     * @returns The resource builder.
     */
    withSessionLifetime() {
        return new PgWebContainerResourcePromiseImpl(this._withSessionLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withPersistentLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPersistentLifetime', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime.
     * @returns The resource builder.
     */
    withPersistentLifetime() {
        return new PgWebContainerResourcePromiseImpl(this._withPersistentLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withLifetimeOfInternal(sourceBuilder) {
        sourceBuilder = isPromiseLike(sourceBuilder) ? await sourceBuilder : sourceBuilder;
        const rpcArgs = { builder: this._handle, sourceBuilder };
        const result = await this._client.invokeCapability('Aspire.Hosting/withLifetimeOf', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to match the lifetime of another resource.
     *
     * The resource lifetime is evaluated from `sourceBuilder` when the application model is prepared, so later lifetime
     * changes to the source resource are reflected by this resource.
     * @param sourceBuilder The resource builder whose lifetime should be used.
     * @returns The resource builder.
     */
    withLifetimeOf(sourceBuilder) {
        return new PgWebContainerResourcePromiseImpl(this._withLifetimeOfInternal(sourceBuilder), this._client);
    }
    /** @internal */
    async _withParentProcessLifetimeInternal(parentProcessId) {
        const rpcArgs = { builder: this._handle, parentProcessId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withParentProcessLifetime', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime that ends when a parent process exits.
     * @param parentProcessId The ID of the parent process to monitor.
     * @returns The resource builder.
     */
    withParentProcessLifetime(parentProcessId) {
        return new PgWebContainerResourcePromiseImpl(this._withParentProcessLifetimeInternal(parentProcessId), this._client);
    }
    /** @internal */
    async _withEnvironmentInternal(name, value) {
        value = isPromiseLike(value) ? await value : value;
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEnvironment', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /** Sets an environment variable */
    withEnvironment(name, value) {
        return new PgWebContainerResourcePromiseImpl(this._withEnvironmentInternal(name, value), this._client);
    }
    /** @internal */
    async _withEnvironmentCallbackInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new EnvironmentCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEnvironmentCallback', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Allows for the population of environment variables on a resource.
     * @param callback A callback that allows for deferred execution for computing many environment variables. This runs after resources have been allocated by the orchestrator and allows access to other resources to resolve computed data, e.g. connection strings, ports.
     * @returns The resource builder.
     */
    withEnvironmentCallback(callback) {
        return new PgWebContainerResourcePromiseImpl(this._withEnvironmentCallbackInternal(callback), this._client);
    }
    /** @internal */
    async _withArgsInternal(args) {
        const rpcArgs = { builder: this._handle, args };
        const result = await this._client.invokeCapability('Aspire.Hosting/withArgs', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Adds arguments to be passed to a resource that supports arguments when it is launched.
     * @param args The arguments to be passed to the resource when it is started.
     * @returns The resource builder.
     */
    withArgs(args) {
        return new PgWebContainerResourcePromiseImpl(this._withArgsInternal(args), this._client);
    }
    /** @internal */
    async _withArgsCallbackInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new CommandLineArgsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withArgsCallback', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a callback to be executed with a list of command-line arguments when a resource is started.
     * @param callback A callback that allows for deferred execution for computing arguments. This runs after resources have been allocated by the orchestrator and allows access to other resources to resolve computed data, e.g. connection strings, ports.
     * @returns The resource builder.
     */
    withArgsCallback(callback) {
        return new PgWebContainerResourcePromiseImpl(this._withArgsCallbackInternal(callback), this._client);
    }
    /** @internal */
    async _withReferenceEnvironmentInternal(options) {
        const rpcArgs = { builder: this._handle, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withReferenceEnvironment', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Configures how information is injected into environment variables when the resource references other resources.
     * @param options Options controlling which reference information is emitted.
     * @returns The resource builder.
     */
    withReferenceEnvironment(options) {
        return new PgWebContainerResourcePromiseImpl(this._withReferenceEnvironmentInternal(options), this._client);
    }
    /** @internal */
    async _withReferenceInternal(source, connectionName, optional, name) {
        source = isPromiseLike(source) ? await source : source;
        const rpcArgs = { builder: this._handle, source };
        if (connectionName !== undefined)
            rpcArgs.connectionName = connectionName;
        if (optional !== undefined)
            rpcArgs.optional = optional;
        if (name !== undefined)
            rpcArgs.name = name;
        const result = await this._client.invokeCapability('Aspire.Hosting/withReference', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a reference to another resource
     * @param options Additional options.
     */
    withReference(source, options) {
        const connectionName = options?.connectionName;
        const optional = options?.optional;
        const name = options?.name;
        return new PgWebContainerResourcePromiseImpl(this._withReferenceInternal(source, connectionName, optional, name), this._client);
    }
    /** @internal */
    async _withEndpointCallbackInternal(endpointName, callback, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpointCallback', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Updates a named endpoint via callback
     * @param options Additional options.
     */
    withEndpointCallback(endpointName, callback, options) {
        const createIfNotExists = options?.createIfNotExists;
        return new PgWebContainerResourcePromiseImpl(this._withEndpointCallbackInternal(endpointName, callback, createIfNotExists), this._client);
    }
    /** @internal */
    async _withHttpEndpointCallbackInternal(callback, name, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        if (name !== undefined)
            rpcArgs.name = name;
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpEndpointCallback', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Updates an HTTP endpoint via callback
     * @param options Additional options.
     */
    withHttpEndpointCallback(callback, options) {
        const name = options?.name;
        const createIfNotExists = options?.createIfNotExists;
        return new PgWebContainerResourcePromiseImpl(this._withHttpEndpointCallbackInternal(callback, name, createIfNotExists), this._client);
    }
    /** @internal */
    async _withHttpsEndpointCallbackInternal(callback, name, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        if (name !== undefined)
            rpcArgs.name = name;
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpsEndpointCallback', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Updates an HTTPS endpoint via callback
     * @param options Additional options.
     */
    withHttpsEndpointCallback(callback, options) {
        const name = options?.name;
        const createIfNotExists = options?.createIfNotExists;
        return new PgWebContainerResourcePromiseImpl(this._withHttpsEndpointCallbackInternal(callback, name, createIfNotExists), this._client);
    }
    /** @internal */
    async _withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (scheme !== undefined)
            rpcArgs.scheme = scheme;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        if (isExternal !== undefined)
            rpcArgs.isExternal = isExternal;
        if (protocol !== undefined)
            rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpoint', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a network endpoint
     * @param options Additional options.
     * @returns The resource builder.
     */
    withEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const scheme = options?.scheme;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        const isExternal = options?.isExternal;
        const protocol = options?.protocol;
        return new PgWebContainerResourcePromiseImpl(this._withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol), this._client);
    }
    /** @internal */
    async _withEndpointProxySupportInternal(proxyEnabled) {
        const rpcArgs = { builder: this._handle, proxyEnabled };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpointProxySupport', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Set whether a resource can use proxied endpoints or whether they should be disabled for all endpoints belonging to the resource. If set to `false`, endpoints belonging to the resource will ignore the configured proxy settings and run proxy-less.
     *
     * This method is intended to support scenarios with persistent lifetime resources where it is desirable for the resource to be accessible over the same
     * port whether the Aspire application is running or not. Proxied endpoints bind ports that are only accessible while the Aspire application is running.
     * The user needs to be careful to ensure that endpoints are using unique ports when disabling proxy support as by default for proxy-less
     * endpoints, Aspire will allocate the target port as the host port, which will increase the chance of port conflicts.
     * @param proxyEnabled Should endpoints for the resource support using a proxy?
     * @returns The resource builder.
     */
    withEndpointProxySupport(proxyEnabled) {
        return new PgWebContainerResourcePromiseImpl(this._withEndpointProxySupportInternal(proxyEnabled), this._client);
    }
    /** @internal */
    async _withHttpEndpointInternal(port, targetPort, name, env, isProxied) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpEndpoint', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP endpoint
     *
     * If an endpoint with the same name already exists on the resource, the existing endpoint is updated
     * with any non-null parameter values. Parameters left as `null` will not modify the existing endpoint's values.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new PgWebContainerResourcePromiseImpl(this._withHttpEndpointInternal(port, targetPort, name, env, isProxied), this._client);
    }
    /** @internal */
    async _withHttpsEndpointInternal(port, targetPort, name, env, isProxied) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpsEndpoint', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTPS endpoint
     *
     * If an endpoint with the same name already exists on the resource, the existing endpoint is updated
     * with any non-null parameter values. Parameters left as `null` will not modify the existing endpoint's values.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpsEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new PgWebContainerResourcePromiseImpl(this._withHttpsEndpointInternal(port, targetPort, name, env, isProxied), this._client);
    }
    /** @internal */
    async _withExternalHttpEndpointsInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExternalHttpEndpoints', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Marks existing http or https endpoints on a resource as external.
     * @returns The resource builder.
     */
    withExternalHttpEndpoints() {
        return new PgWebContainerResourcePromiseImpl(this._withExternalHttpEndpointsInternal(), this._client);
    }
    /**
     * Gets an endpoint reference
     * @param name The name of the endpoint.
     * @returns An `EndpointReference` that can be used to resolve the address of the endpoint after resource allocation has occurred.
     */
    getEndpoint(name) {
        const promise = (async () => {
            const rpcArgs = { builder: this._handle, name };
            const handle = await this._client.invokeCapability('Aspire.Hosting/getEndpoint', rpcArgs);
            return new EndpointReferenceImpl(handle, this._client);
        })();
        return new EndpointReferencePromiseImpl(promise, this._client);
    }
    /** @internal */
    async _asHttp2ServiceInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/asHttp2Service', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to mark all endpoints' transport as HTTP/2. This is useful for HTTP/2 services that need prior knowledge.
     * @returns The resource builder.
     */
    asHttp2Service() {
        return new PgWebContainerResourcePromiseImpl(this._asHttp2ServiceInternal(), this._client);
    }
    /** @internal */
    async _withUrlsInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new ResourceUrlsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrls', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to customize the URLs displayed for the resource.
     * @param callback The callback that will customize URLs for the resource.
     * @returns The resource builder.
     */
    withUrls(callback) {
        return new PgWebContainerResourcePromiseImpl(this._withUrlsInternal(callback), this._client);
    }
    /** @internal */
    async _withUrlInternal(url, displayText) {
        const rpcArgs = { builder: this._handle, url };
        if (displayText !== undefined)
            rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrl', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Adds or modifies displayed URLs
     * @param options Additional options.
     */
    withUrl(url, options) {
        const displayText = options?.displayText;
        return new PgWebContainerResourcePromiseImpl(this._withUrlInternal(url, displayText), this._client);
    }
    /** @internal */
    async _withUrlForEndpointInternal(endpointName, callback) {
        const callbackId = registerCallback(async (objData) => {
            const obj = wrapIfHandle(objData);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrlForEndpoint', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to update the URL displayed for the endpoint with the specified name.
     * @param endpointName The name of the endpoint to customize the URL for.
     * @param callback The callback that will customize the URL.
     * @returns The resource builder.
     */
    withUrlForEndpoint(endpointName, callback) {
        return new PgWebContainerResourcePromiseImpl(this._withUrlForEndpointInternal(endpointName, callback), this._client);
    }
    /** @internal */
    async _excludeFromManifestInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromManifest', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Excludes a resource from being published to the manifest.
     * @returns The resource builder.
     */
    excludeFromManifest() {
        return new PgWebContainerResourcePromiseImpl(this._excludeFromManifestInternal(), this._client);
    }
    /** @internal */
    async _waitForInternal(dependency, waitBehavior) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (waitBehavior !== undefined)
            rpcArgs.waitBehavior = waitBehavior;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitFor', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Waits for another resource to be ready
     * @param options Additional options.
     */
    waitFor(dependency, options) {
        const waitBehavior = options?.waitBehavior;
        return new PgWebContainerResourcePromiseImpl(this._waitForInternal(dependency, waitBehavior), this._client);
    }
    /** @internal */
    async _waitForStartInternal(dependency, waitBehavior) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (waitBehavior !== undefined)
            rpcArgs.waitBehavior = waitBehavior;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitForStart', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Waits for another resource to start
     * @param options Additional options.
     */
    waitForStart(dependency, options) {
        const waitBehavior = options?.waitBehavior;
        return new PgWebContainerResourcePromiseImpl(this._waitForStartInternal(dependency, waitBehavior), this._client);
    }
    /** @internal */
    async _withExplicitStartInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExplicitStart', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Prevents resource from starting automatically
     * @returns The resource builder.
     */
    withExplicitStart() {
        return new PgWebContainerResourcePromiseImpl(this._withExplicitStartInternal(), this._client);
    }
    /** @internal */
    async _waitForCompletionInternal(dependency, exitCode) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitForResourceCompletion', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Waits for the dependency resource to enter the Exited or Finished state before starting the resource.
     * @param dependency The resource builder for the dependency resource.
     * @param options Additional options.
     * @returns The resource builder.
     */
    waitForCompletion(dependency, options) {
        const exitCode = options?.exitCode;
        return new PgWebContainerResourcePromiseImpl(this._waitForCompletionInternal(dependency, exitCode), this._client);
    }
    /** @internal */
    async _withHealthCheckInternal(key) {
        const rpcArgs = { builder: this._handle, key };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHealthCheck', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a health check by key
     * @param key The key for the health check.
     * @returns The resource builder.
     */
    withHealthCheck(key) {
        return new PgWebContainerResourcePromiseImpl(this._withHealthCheckInternal(key), this._client);
    }
    /** @internal */
    async _withHttpHealthCheckInternal(path, statusCode, endpointName) {
        const rpcArgs = { builder: this._handle };
        if (path !== undefined)
            rpcArgs.path = path;
        if (statusCode !== undefined)
            rpcArgs.statusCode = statusCode;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpHealthCheck', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a health check to the resource which is mapped to a specific endpoint.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpHealthCheck(options) {
        const path = options?.path;
        const statusCode = options?.statusCode;
        const endpointName = options?.endpointName;
        return new PgWebContainerResourcePromiseImpl(this._withHttpHealthCheckInternal(path, statusCode, endpointName), this._client);
    }
    /** @internal */
    async _withCommandInternal(name, displayName, executeCommand, commandOptions) {
        const executeCommandId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined)
            rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability('Aspire.Hosting/withCommand', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a resource command
     *
     * The `WithCommand` method is used to add commands to the resource. Commands are displayed in the dashboard
     * and can be executed by a user using the dashboard UI.
     * When a command is executed, the `executeCommand` callback is called and is run inside the Aspire host.
     * @param name The name of the command. The name uniquely identifies the command.
     * @param displayName The display name visible in UI.
     * @param executeCommand A callback that is executed when the command is executed. The callback is run inside the Aspire host. The callback result is used to indicate success or failure in the UI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withCommand(name, displayName, executeCommand, options) {
        const commandOptions = options?.commandOptions;
        return new PgWebContainerResourcePromiseImpl(this._withCommandInternal(name, displayName, executeCommand, commandOptions), this._client);
    }
    /** @internal */
    async _withProcessCommandInternal(commandName, displayName, options) {
        const rpcArgs = { builder: this._handle, commandName, displayName, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommand', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /** Adds a command to the resource that starts a local process when invoked. */
    withProcessCommand(commandName, displayName, options) {
        return new PgWebContainerResourcePromiseImpl(this._withProcessCommandInternal(commandName, displayName, options), this._client);
    }
    /** @internal */
    async _withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options) {
        const createProcessSpecId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await createProcessSpec(arg);
        });
        const rpcArgs = { builder: this._handle, commandName, displayName, createProcessSpec: createProcessSpecId };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommandFactory', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a command to the resource that starts a local process created by a callback when invoked.
     * @param options Additional options.
     */
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new PgWebContainerResourcePromiseImpl(this._withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options), this._client);
    }
    /** @internal */
    async _withHttpCommandInternal(path, displayName, options) {
        const rpcArgs = { builder: this._handle, path, displayName };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpCommand', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP resource command
     * @param options Additional options.
     */
    withHttpCommand(path, displayName, options) {
        return new PgWebContainerResourcePromiseImpl(this._withHttpCommandInternal(path, displayName, options), this._client);
    }
    /** @internal */
    async _withDeveloperCertificateTrustInternal(trust) {
        const rpcArgs = { builder: this._handle, trust };
        const result = await this._client.invokeCapability('Aspire.Hosting/withDeveloperCertificateTrust', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Indicates whether developer certificates should be treated as trusted certificate authorities for the resource at run time. Currently this indicates trust for the ASP.NET Core developer certificate. The developer certificate will only be trusted when running in local development scenarios; in publish mode resources will use their default certificate trust.
     *
     * Disable trust for app host managed developer certificate(s) for a container resource.
     * ```
     * var container = builder.AddContainer("my-service", "my-service:latest")
     * .WithDeveloperCertificateTrust(false);
     * ```
     * Disable automatic trust for app host managed developer certificate(s), but explicitly enable it for a specific resource.
     * ```
     * var builder = DistributedApplication.CreateBuilder(new DistributedApplicationOptions()
     * {
     * Args = args,
     * TrustDeveloperCertificate = false,
     * });
     * var project = builder.AddProject<MyService>("my-service")
     * .WithDeveloperCertificateTrust(true);
     * ```
     * @param trust Indicates whether the developer certificate should be treated as trusted.
     * @returns The resource builder.
     */
    withDeveloperCertificateTrust(trust) {
        return new PgWebContainerResourcePromiseImpl(this._withDeveloperCertificateTrustInternal(trust), this._client);
    }
    /** @internal */
    async _withCertificateTrustScopeInternal(scope) {
        const rpcArgs = { builder: this._handle, scope };
        const result = await this._client.invokeCapability('Aspire.Hosting/withCertificateTrustScope', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Sets the certificate trust scope
     *
     * The default scope if not overridden is `Append` which means that custom certificate
     * authorities should be appended to the default trusted certificate authorities for the resource. Setting the scope to
     * `Override` indicates the set of certificates in referenced
     * `CertificateAuthorityCollection` (and optionally Aspire developer certificiates) should be used as the
     * exclusive source of trust for a resource.
     * In all cases, this is a best effort implementation as not all resources support full customization of certificate
     * trust.
     * Set the scope for custom certificate authorities to override the default trusted certificate authorities for a container resource.
     * ```
     * var caCollection = builder.AddCertificateAuthorityCollection("my-cas")
     * .WithCertificate(new X509Certificate2("my-ca.pem"));
     * var container = builder.AddContainer("my-service", "my-service:latest")
     * .WithCertificateAuthorityCollection(caCollection)
     * .WithCertificateTrustScope(CertificateTrustScope.Override);
     * ```
     * @param scope The scope to apply to custom certificate authorities associated with the resource.
     * @returns The resource builder.
     */
    withCertificateTrustScope(scope) {
        return new PgWebContainerResourcePromiseImpl(this._withCertificateTrustScopeInternal(scope), this._client);
    }
    /** @internal */
    async _withHttpsDeveloperCertificateInternal(password) {
        password = isPromiseLike(password) ? await password : password;
        const rpcArgs = { builder: this._handle };
        if (password !== undefined)
            rpcArgs.password = password;
        const result = await this._client.invokeCapability('Aspire.Hosting/withParameterHttpsDeveloperCertificate', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Indicates that a resource should use the developer certificate key pair for HTTPS endpoints at run time. Currently this indicates use of the ASP.NET Core developer certificate. The developer certificate will only be used when running in local development scenarios; in publish mode resources will use their default certificate configuration.
     *
     * Use the developer certificate for HTTPS/TLS endpoints on a container resource:
     * ```
     * builder.AddContainer("my-service", "my-image")
     * .WithHttpsDeveloperCertificate()
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpsDeveloperCertificate(options) {
        let password = options?.password;
        return new PgWebContainerResourcePromiseImpl(this._withHttpsDeveloperCertificateInternal(password), this._client);
    }
    /** @internal */
    async _withoutHttpsCertificateInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withoutHttpsCertificate', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Disable HTTPS/TLS server certificate configuration for the resource. No HTTPS/TLS termination configuration will be applied.
     *
     * Disable HTTPS certificate configuration for a Redis resource:
     * ```
     * var redis = builder.AddRedis("cache")
     * .WithoutHttpsCertificate();
     * ```
     * @returns The resource builder.
     */
    withoutHttpsCertificate() {
        return new PgWebContainerResourcePromiseImpl(this._withoutHttpsCertificateInternal(), this._client);
    }
    /** @internal */
    async _withRelationshipInternal(resourceBuilder, type) {
        resourceBuilder = isPromiseLike(resourceBuilder) ? await resourceBuilder : resourceBuilder;
        const rpcArgs = { builder: this._handle, resourceBuilder, type };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderRelationship', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a relationship to another resource using its builder.
     * @param resourceBuilder The resource builder that the relationship is to.
     * @param type The relationship type.
     * @returns A resource builder.
     */
    withRelationship(resourceBuilder, type) {
        return new PgWebContainerResourcePromiseImpl(this._withRelationshipInternal(resourceBuilder, type), this._client);
    }
    /** @internal */
    async _withParentRelationshipInternal(parent) {
        parent = isPromiseLike(parent) ? await parent : parent;
        const rpcArgs = { builder: this._handle, parent };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderParentRelationship', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Sets the parent relationship
     * @param parent The parent of `builder`.
     * @returns A resource builder.
     */
    withParentRelationship(parent) {
        return new PgWebContainerResourcePromiseImpl(this._withParentRelationshipInternal(parent), this._client);
    }
    /** @internal */
    async _withChildRelationshipInternal(child) {
        child = isPromiseLike(child) ? await child : child;
        const rpcArgs = { builder: this._handle, child };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderChildRelationship', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Sets a child relationship
     * @param child The child of `builder`.
     * @returns A resource builder.
     */
    withChildRelationship(child) {
        return new PgWebContainerResourcePromiseImpl(this._withChildRelationshipInternal(child), this._client);
    }
    /** @internal */
    async _withIconNameInternal(iconName, iconVariant) {
        const rpcArgs = { builder: this._handle, iconName };
        if (iconVariant !== undefined)
            rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability('Aspire.Hosting/withIconName', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Specifies the icon to use when displaying the resource in the dashboard.
     * @param iconName The name of the FluentUI icon to use. See https://aka.ms/fluentui-system-icons for available icons.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withIconName(iconName, options) {
        const iconVariant = options?.iconVariant;
        return new PgWebContainerResourcePromiseImpl(this._withIconNameInternal(iconName, iconVariant), this._client);
    }
    /** @internal */
    async _withComputeEnvironmentInternal(computeEnvironmentResource) {
        computeEnvironmentResource = isPromiseLike(computeEnvironmentResource) ? await computeEnvironmentResource : computeEnvironmentResource;
        const rpcArgs = { builder: this._handle, computeEnvironmentResource };
        const result = await this._client.invokeCapability('Aspire.Hosting/withComputeEnvironment', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Configures the compute environment for the compute resource.
     *
     * This method allows associating a specific compute environment with the compute resource.
     * @param computeEnvironmentResource The compute environment resource to associate with the compute resource.
     * @returns The resource builder.
     */
    withComputeEnvironment(computeEnvironmentResource) {
        return new PgWebContainerResourcePromiseImpl(this._withComputeEnvironmentInternal(computeEnvironmentResource), this._client);
    }
    /** @internal */
    async _withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName) {
        const rpcArgs = { builder: this._handle, probeType };
        if (path !== undefined)
            rpcArgs.path = path;
        if (initialDelaySeconds !== undefined)
            rpcArgs.initialDelaySeconds = initialDelaySeconds;
        if (periodSeconds !== undefined)
            rpcArgs.periodSeconds = periodSeconds;
        if (timeoutSeconds !== undefined)
            rpcArgs.timeoutSeconds = timeoutSeconds;
        if (failureThreshold !== undefined)
            rpcArgs.failureThreshold = failureThreshold;
        if (successThreshold !== undefined)
            rpcArgs.successThreshold = successThreshold;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpProbe', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP health probe to the resource
     * @param options Additional options.
     */
    withHttpProbe(probeType, options) {
        const path = options?.path;
        const initialDelaySeconds = options?.initialDelaySeconds;
        const periodSeconds = options?.periodSeconds;
        const timeoutSeconds = options?.timeoutSeconds;
        const failureThreshold = options?.failureThreshold;
        const successThreshold = options?.successThreshold;
        const endpointName = options?.endpointName;
        return new PgWebContainerResourcePromiseImpl(this._withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName), this._client);
    }
    /** @internal */
    async _excludeFromMcpInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromMcp', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Exclude the resource from MCP operations using the Aspire MCP server. The resource is excluded from results that return resources, console logs and telemetry.
     * @returns The resource builder.
     */
    excludeFromMcp() {
        return new PgWebContainerResourcePromiseImpl(this._excludeFromMcpInternal(), this._client);
    }
    /** @internal */
    async _withHiddenInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHidden', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists
     *
     * Use this method to hide resources that are implementation details and should never be displayed by default.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @returns The resource builder.
     */
    withHidden() {
        return new PgWebContainerResourcePromiseImpl(this._withHiddenInternal(), this._client);
    }
    /** @internal */
    async _withHiddenOnCompletionInternal(exitCode, exitCodes) {
        const rpcArgs = { builder: this._handle };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        if (exitCodes !== undefined)
            rpcArgs.exitCodes = exitCodes;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHiddenOnCompletion', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists after successful completion
     *
     * This method is useful for one-off resources such as setup scripts, migrations, or build steps that should remain visible while running
     * and then be hidden after successful completion.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHiddenOnCompletion(options) {
        const exitCode = options?.exitCode;
        const exitCodes = options?.exitCodes;
        return new PgWebContainerResourcePromiseImpl(this._withHiddenOnCompletionInternal(exitCode, exitCodes), this._client);
    }
    /** @internal */
    async _withImagePushOptionsInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ContainerImagePushOptionsCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImagePushOptions', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Adds an asynchronous callback to configure container image push options for the resource.
     *
     * This method allows customization of how container images are named and tagged when pushed to a registry using an asynchronous callback.
     * Use this overload when the callback needs to perform asynchronous operations such as retrieving configuration values from external sources.
     * The callback receives a `ContainerImagePushOptionsCallbackContext` that provides access to the resource
     * and the `ContainerImagePushOptions` that can be modified.
     * Multiple callbacks can be registered on the same resource, and they will be invoked in the order they were added.
     * @param callback The asynchronous callback to configure push options.
     * @returns The resource builder.
     */
    withImagePushOptions(callback) {
        return new PgWebContainerResourcePromiseImpl(this._withImagePushOptionsInternal(callback), this._client);
    }
    /** @internal */
    async _withRemoteImageNameInternal(remoteImageName) {
        const rpcArgs = { builder: this._handle, remoteImageName };
        const result = await this._client.invokeCapability('Aspire.Hosting/withRemoteImageName', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Sets the remote image name (without registry endpoint or tag) for container push operations.
     *
     * Use this with `withRemoteImageTag` to fully customize the image reference used for container push operations.
     * @param remoteImageName The remote image name (e.g., "myapp" or "myorg/myapp").
     * @returns The resource builder.
     */
    withRemoteImageName(remoteImageName) {
        return new PgWebContainerResourcePromiseImpl(this._withRemoteImageNameInternal(remoteImageName), this._client);
    }
    /** @internal */
    async _withRemoteImageTagInternal(remoteImageTag) {
        const rpcArgs = { builder: this._handle, remoteImageTag };
        const result = await this._client.invokeCapability('Aspire.Hosting/withRemoteImageTag', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Sets the remote image tag for container push operations.
     *
     * Use this with `withRemoteImageName` to fully customize the image reference used for container push operations.
     * @param remoteImageTag The remote image tag (e.g., "latest", "v1.0.0").
     * @returns The resource builder.
     */
    withRemoteImageTag(remoteImageTag) {
        return new PgWebContainerResourcePromiseImpl(this._withRemoteImageTagInternal(remoteImageTag), this._client);
    }
    /** @internal */
    async _withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new PipelineStepContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined)
            rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined)
            rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined)
            rpcArgs.tags = tags;
        if (description !== undefined)
            rpcArgs.description = description;
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineStepFactory', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a pipeline step to the resource that will be executed during deployment.
     * @param stepName The unique name of the pipeline step.
     * @param callback The callback to execute when the step runs.
     * @param options Additional options.
     * @returns The resource builder for chaining.
     */
    withPipelineStepFactory(stepName, callback, options) {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new PgWebContainerResourcePromiseImpl(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description), this._client);
    }
    /** @internal */
    async _withPipelineConfigurationInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new PipelineConfigurationContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineConfiguration', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to be executed during the pipeline configuration phase, allowing modification of step dependencies and relationships.
     * @param callback The callback function to execute during the configuration phase.
     * @returns The resource builder for chaining.
     */
    withPipelineConfiguration(callback) {
        return new PgWebContainerResourcePromiseImpl(this._withPipelineConfigurationInternal(callback), this._client);
    }
    /** @internal */
    async _withVolumeInternal(target, name, isReadOnly) {
        const rpcArgs = { resource: this._handle, target };
        if (name !== undefined)
            rpcArgs.name = name;
        if (isReadOnly !== undefined)
            rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability('Aspire.Hosting/withVolume', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a volume to a container resource.
     *
     * Volumes persist data across container restarts. Named volumes are managed
     * by Docker/Podman and stored in a system-managed location.
     * Why this wrapper exists: The original `ContainerResourceBuilderExtensions.WithVolume`
     * has parameter order `(name?, target, isReadOnly)` where the optional `name` comes first.
     * This wrapper reorders parameters to `(target, name?, isReadOnly)` so the required `target`
     * parameter comes first, providing a better API for polyglot consumers.
     * @param target The mount path inside the container.
     * @param options Additional options.
     * @returns The same resource builder handle for chaining.
     */
    withVolume(target, options) {
        const name = options?.name;
        const isReadOnly = options?.isReadOnly;
        return new PgWebContainerResourcePromiseImpl(this._withVolumeInternal(target, name, isReadOnly), this._client);
    }
    /**
     * Gets the name of the resource from a builder.
     *
     * Why this wrapper exists: This capability accesses a nested property
     * (`resource.Resource.Name`) which requires a wrapper method. There is no single
     * .NET method that returns just the resource name that could be annotated directly.
     * @returns The resource name.
     */
    async getResourceName() {
        const rpcArgs = { resource: this._handle };
        return await this._client.invokeCapability('Aspire.Hosting/getResourceName', rpcArgs);
    }
    /** @internal */
    async _onBeforeResourceStartedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new BeforeResourceStartedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onBeforeResourceStarted', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the BeforeResourceStarted event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onBeforeResourceStarted(callback) {
        return new PgWebContainerResourcePromiseImpl(this._onBeforeResourceStartedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceStoppedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceStoppedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceStopped', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceStopped event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceStopped(callback) {
        return new PgWebContainerResourcePromiseImpl(this._onResourceStoppedInternal(callback), this._client);
    }
    /** @internal */
    async _onInitializeResourceInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new InitializeResourceEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onInitializeResource', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the InitializeResource event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onInitializeResource(callback) {
        return new PgWebContainerResourcePromiseImpl(this._onInitializeResourceInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceEndpointsAllocatedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceEndpointsAllocatedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceEndpointsAllocated', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceEndpointsAllocated event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceEndpointsAllocated(callback) {
        return new PgWebContainerResourcePromiseImpl(this._onResourceEndpointsAllocatedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceReadyInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceReadyEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceReady', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceReady event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceReady(callback) {
        return new PgWebContainerResourcePromiseImpl(this._onResourceReadyInternal(callback), this._client);
    }
    /**
     * Creates an execution configuration builder for the specified resource.
     * @returns The execution configuration builder.
     */
    createExecutionConfiguration() {
        const promise = (async () => {
            const rpcArgs = { resource: this._handle };
            const handle = await this._client.invokeCapability('Aspire.Hosting/createExecutionConfiguration', rpcArgs);
            return new ExecutionConfigurationBuilderImpl(handle, this._client);
        })();
        return new ExecutionConfigurationBuilderPromiseImpl(promise, this._client);
    }
    /** @internal */
    async _withHostPortInternal(port) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        const result = await this._client.invokeCapability('Aspire.Hosting.PostgreSQL/withPgWebHostPort', rpcArgs);
        return new PgWebContainerResourceImpl(result, this._client);
    }
    /**
     * Configures the host port that the pgweb resource is exposed on instead of using randomly assigned port.
     * @param options Additional options.
     * @returns The resource builder for pgweb.
     */
    withHostPort(options) {
        const port = options?.port;
        return new PgWebContainerResourcePromiseImpl(this._withHostPortInternal(port), this._client);
    }
}
/**
 * Thenable wrapper for PgWebContainerResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
class PgWebContainerResourcePromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    withContainerRegistry(registry) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withContainerRegistry(registry)), this._client);
    }
    withBindMount(source, target, options) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withBindMount(source, target, options)), this._client);
    }
    withEntrypoint(entrypoint) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withEntrypoint(entrypoint)), this._client);
    }
    withImageTag(tag) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withImageTag(tag)), this._client);
    }
    withImageRegistry(registry) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withImageRegistry(registry)), this._client);
    }
    withImage(image, options) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withImage(image, options)), this._client);
    }
    withImageSHA256(sha256) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withImageSHA256(sha256)), this._client);
    }
    withContainerRuntimeArgs(args) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withContainerRuntimeArgs(args)), this._client);
    }
    withLifetime(lifetime) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withLifetime(lifetime)), this._client);
    }
    withImagePullPolicy(pullPolicy) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withImagePullPolicy(pullPolicy)), this._client);
    }
    publishAsContainer() {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.publishAsContainer()), this._client);
    }
    withDockerfile(contextPath, options) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withDockerfile(contextPath, options)), this._client);
    }
    withDockerfileFactory(contextPath, dockerfileFactory, options) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileFactory(contextPath, dockerfileFactory, options)), this._client);
    }
    withContainerName(name) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withContainerName(name)), this._client);
    }
    withBuildArg(name, value) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withBuildArg(name, value)), this._client);
    }
    withBuildSecret(name, value) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withBuildSecret(name, value)), this._client);
    }
    withContainerCertificatePaths(options) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withContainerCertificatePaths(options)), this._client);
    }
    withDockerfileBuilder(contextPath, callback, options) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileBuilder(contextPath, callback, options)), this._client);
    }
    withDockerfileBaseImage(options) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileBaseImage(options)), this._client);
    }
    withContainerNetworkAlias(alias) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withContainerNetworkAlias(alias)), this._client);
    }
    withMcpServer(options) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withMcpServer(options)), this._client);
    }
    withOtlpExporter(options) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withOtlpExporter(options)), this._client);
    }
    publishAsConnectionString() {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.publishAsConnectionString()), this._client);
    }
    withRequiredCommand(command, options) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withRequiredCommand(command, options)), this._client);
    }
    withSessionLifetime() {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withSessionLifetime()), this._client);
    }
    withPersistentLifetime() {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withPersistentLifetime()), this._client);
    }
    withLifetimeOf(sourceBuilder) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withLifetimeOf(sourceBuilder)), this._client);
    }
    withParentProcessLifetime(parentProcessId) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withParentProcessLifetime(parentProcessId)), this._client);
    }
    withEnvironment(name, value) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withEnvironment(name, value)), this._client);
    }
    withEnvironmentCallback(callback) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withEnvironmentCallback(callback)), this._client);
    }
    withArgs(args) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withArgs(args)), this._client);
    }
    withArgsCallback(callback) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withArgsCallback(callback)), this._client);
    }
    withReferenceEnvironment(options) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withReferenceEnvironment(options)), this._client);
    }
    withReference(source, options) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withReference(source, options)), this._client);
    }
    withEndpointCallback(endpointName, callback, options) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withEndpointCallback(endpointName, callback, options)), this._client);
    }
    withHttpEndpointCallback(callback, options) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withHttpEndpointCallback(callback, options)), this._client);
    }
    withHttpsEndpointCallback(callback, options) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withHttpsEndpointCallback(callback, options)), this._client);
    }
    withEndpoint(options) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withEndpoint(options)), this._client);
    }
    withEndpointProxySupport(proxyEnabled) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withEndpointProxySupport(proxyEnabled)), this._client);
    }
    withHttpEndpoint(options) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withHttpEndpoint(options)), this._client);
    }
    withHttpsEndpoint(options) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withHttpsEndpoint(options)), this._client);
    }
    withExternalHttpEndpoints() {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withExternalHttpEndpoints()), this._client);
    }
    getEndpoint(name) {
        return new EndpointReferencePromiseImpl(this._promise.then(obj => obj.getEndpoint(name)), this._client);
    }
    asHttp2Service() {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.asHttp2Service()), this._client);
    }
    withUrls(callback) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withUrls(callback)), this._client);
    }
    withUrl(url, options) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withUrl(url, options)), this._client);
    }
    withUrlForEndpoint(endpointName, callback) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)), this._client);
    }
    excludeFromManifest() {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.excludeFromManifest()), this._client);
    }
    waitFor(dependency, options) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.waitFor(dependency, options)), this._client);
    }
    waitForStart(dependency, options) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.waitForStart(dependency, options)), this._client);
    }
    withExplicitStart() {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withExplicitStart()), this._client);
    }
    waitForCompletion(dependency, options) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.waitForCompletion(dependency, options)), this._client);
    }
    withHealthCheck(key) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withHealthCheck(key)), this._client);
    }
    withHttpHealthCheck(options) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withHttpHealthCheck(options)), this._client);
    }
    withCommand(name, displayName, executeCommand, options) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)), this._client);
    }
    withProcessCommand(commandName, displayName, options) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommand(commandName, displayName, options)), this._client);
    }
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommandFactory(commandName, displayName, createProcessSpec, options)), this._client);
    }
    withHttpCommand(path, displayName, options) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withHttpCommand(path, displayName, options)), this._client);
    }
    withDeveloperCertificateTrust(trust) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withDeveloperCertificateTrust(trust)), this._client);
    }
    withCertificateTrustScope(scope) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withCertificateTrustScope(scope)), this._client);
    }
    withHttpsDeveloperCertificate(options) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withHttpsDeveloperCertificate(options)), this._client);
    }
    withoutHttpsCertificate() {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withoutHttpsCertificate()), this._client);
    }
    withRelationship(resourceBuilder, type) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withRelationship(resourceBuilder, type)), this._client);
    }
    withParentRelationship(parent) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withParentRelationship(parent)), this._client);
    }
    withChildRelationship(child) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withChildRelationship(child)), this._client);
    }
    withIconName(iconName, options) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withIconName(iconName, options)), this._client);
    }
    withComputeEnvironment(computeEnvironmentResource) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withComputeEnvironment(computeEnvironmentResource)), this._client);
    }
    withHttpProbe(probeType, options) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withHttpProbe(probeType, options)), this._client);
    }
    excludeFromMcp() {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.excludeFromMcp()), this._client);
    }
    withHidden() {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withHidden()), this._client);
    }
    withHiddenOnCompletion(options) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withHiddenOnCompletion(options)), this._client);
    }
    withImagePushOptions(callback) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withImagePushOptions(callback)), this._client);
    }
    withRemoteImageName(remoteImageName) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withRemoteImageName(remoteImageName)), this._client);
    }
    withRemoteImageTag(remoteImageTag) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withRemoteImageTag(remoteImageTag)), this._client);
    }
    withPipelineStepFactory(stepName, callback, options) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)), this._client);
    }
    withPipelineConfiguration(callback) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withPipelineConfiguration(callback)), this._client);
    }
    withVolume(target, options) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withVolume(target, options)), this._client);
    }
    getResourceName() {
        return this._promise.then(obj => obj.getResourceName());
    }
    onBeforeResourceStarted(callback) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.onBeforeResourceStarted(callback)), this._client);
    }
    onResourceStopped(callback) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.onResourceStopped(callback)), this._client);
    }
    onInitializeResource(callback) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.onInitializeResource(callback)), this._client);
    }
    onResourceEndpointsAllocated(callback) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.onResourceEndpointsAllocated(callback)), this._client);
    }
    onResourceReady(callback) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.onResourceReady(callback)), this._client);
    }
    createExecutionConfiguration() {
        return new ExecutionConfigurationBuilderPromiseImpl(this._promise.then(obj => obj.createExecutionConfiguration()), this._client);
    }
    withHostPort(options) {
        return new PgWebContainerResourcePromiseImpl(this._promise.then(obj => obj.withHostPort(options)), this._client);
    }
}
// ============================================================================
// PostgresDatabaseResourceImpl
// ============================================================================
/** A resource that represents a PostgreSQL database. This is a child resource of a {@link PostgresServerResource}. */
class PostgresDatabaseResourceImpl extends ResourceBuilderBase {
    constructor(handle, client) {
        super(handle, client);
    }
    parent() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/PostgresDatabaseResource.parent', { context: this._handle });
            return new PostgresServerResourceImpl(handle, this._client);
        })();
        return new PostgresServerResourcePromiseImpl(promise, this._client, false);
    }
    async connectionStringExpression() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/PostgresDatabaseResource.connectionStringExpression', { context: this._handle });
    }
    async databaseName() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/PostgresDatabaseResource.databaseName', { context: this._handle });
    }
    async uriExpression() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/PostgresDatabaseResource.uriExpression', { context: this._handle });
    }
    async jdbcConnectionString() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/PostgresDatabaseResource.jdbcConnectionString', { context: this._handle });
    }
    async name() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/PostgresDatabaseResource.name', { context: this._handle });
    }
    /** @internal */
    async _withContainerRegistryInternal(registry) {
        registry = isPromiseLike(registry) ? await registry : registry;
        const rpcArgs = { builder: this._handle, registry };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerRegistry', rpcArgs);
        return new PostgresDatabaseResourceImpl(result, this._client);
    }
    /**
     * Configures the resource to use the specified container registry for container image operations.
     *
     * This method adds a `ContainerRegistryReferenceAnnotation` to the resource,
     * indicating that the resource should use the specified container registry for container image operations.
     * @param registry The container registry resource builder.
     * @returns The resource builder for chaining.
     */
    withContainerRegistry(registry) {
        return new PostgresDatabaseResourcePromiseImpl(this._withContainerRegistryInternal(registry), this._client);
    }
    /** @internal */
    async _withDockerfileBaseImageInternal(buildImage, runtimeImage) {
        const rpcArgs = { builder: this._handle };
        if (buildImage !== undefined)
            rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined)
            rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileBaseImage', rpcArgs);
        return new PostgresDatabaseResourceImpl(result, this._client);
    }
    /**
     * Configures custom base images for generated Dockerfiles.
     *
     * This extension method allows customization of the base images used in generated Dockerfiles.
     * For multi-stage Dockerfiles (e.g., Python with UV), you can specify separate build and runtime images.
     * Specify custom base images for a Python application:
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddPythonApp("myapp", "path/to/app", "main.py")
     * .WithDockerfileBaseImage(
     * buildImage: "ghcr.io/astral-sh/uv:python3.12-bookworm-slim",
     * runtimeImage: "python:3.12-slim-bookworm");
     * builder.Build().Run();
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileBaseImage(options) {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new PostgresDatabaseResourcePromiseImpl(this._withDockerfileBaseImageInternal(buildImage, runtimeImage), this._client);
    }
    /** @internal */
    async _withRequiredCommandInternal(command, helpLink) {
        const rpcArgs = { builder: this._handle, command };
        if (helpLink !== undefined)
            rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability('Aspire.Hosting/withRequiredCommand', rpcArgs);
        return new PostgresDatabaseResourceImpl(result, this._client);
    }
    /**
     * Declares that a resource requires a specific command/executable to be available on the local machine PATH before it can start.
     *
     * The command is considered valid if either:
     * 1. It is an absolute or relative path (contains a directory separator) that points to an existing file, or
     * 2. It is discoverable on the current process PATH (respecting PATHEXT on Windows).
     * If the command is not found, a warning message will be logged but the resource will be allowed to attempt to start.
     * @param command The command string (file name or path) that should be validated.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withRequiredCommand(command, options) {
        const helpLink = options?.helpLink;
        return new PostgresDatabaseResourcePromiseImpl(this._withRequiredCommandInternal(command, helpLink), this._client);
    }
    /** @internal */
    async _withSessionLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withSessionLifetime', rpcArgs);
        return new PostgresDatabaseResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a session lifetime.
     * @returns The resource builder.
     */
    withSessionLifetime() {
        return new PostgresDatabaseResourcePromiseImpl(this._withSessionLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withPersistentLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPersistentLifetime', rpcArgs);
        return new PostgresDatabaseResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime.
     * @returns The resource builder.
     */
    withPersistentLifetime() {
        return new PostgresDatabaseResourcePromiseImpl(this._withPersistentLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withLifetimeOfInternal(sourceBuilder) {
        sourceBuilder = isPromiseLike(sourceBuilder) ? await sourceBuilder : sourceBuilder;
        const rpcArgs = { builder: this._handle, sourceBuilder };
        const result = await this._client.invokeCapability('Aspire.Hosting/withLifetimeOf', rpcArgs);
        return new PostgresDatabaseResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to match the lifetime of another resource.
     *
     * The resource lifetime is evaluated from `sourceBuilder` when the application model is prepared, so later lifetime
     * changes to the source resource are reflected by this resource.
     * @param sourceBuilder The resource builder whose lifetime should be used.
     * @returns The resource builder.
     */
    withLifetimeOf(sourceBuilder) {
        return new PostgresDatabaseResourcePromiseImpl(this._withLifetimeOfInternal(sourceBuilder), this._client);
    }
    /** @internal */
    async _withParentProcessLifetimeInternal(parentProcessId) {
        const rpcArgs = { builder: this._handle, parentProcessId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withParentProcessLifetime', rpcArgs);
        return new PostgresDatabaseResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime that ends when a parent process exits.
     * @param parentProcessId The ID of the parent process to monitor.
     * @returns The resource builder.
     */
    withParentProcessLifetime(parentProcessId) {
        return new PostgresDatabaseResourcePromiseImpl(this._withParentProcessLifetimeInternal(parentProcessId), this._client);
    }
    /** @internal */
    async _withConnectionPropertyInternal(name, value) {
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withConnectionProperty', rpcArgs);
        return new PostgresDatabaseResourceImpl(result, this._client);
    }
    /**
     * Adds a connection property annotation to the resource being built.
     * @param name The name of the connection property to add.
     * @param value The value to assign to the connection property, specified as a string or reference expression.
     * @returns The same resource builder instance with the specified connection property annotation applied.
     */
    withConnectionProperty(name, value) {
        return new PostgresDatabaseResourcePromiseImpl(this._withConnectionPropertyInternal(name, value), this._client);
    }
    /**
     * Retrieves the value of a specified connection property from the resource's connection properties.
     *
     * Throws a KeyNotFoundException if the specified key does not exist in the resource's
     * connection properties.
     * @param key The key of the connection property to retrieve. Cannot be null.
     * @returns The value associated with the specified connection property key.
     */
    async getConnectionProperty(key) {
        const rpcArgs = { resource: this._handle, key };
        return await this._client.invokeCapability('Aspire.Hosting/getConnectionProperty', rpcArgs);
    }
    /** @internal */
    async _withUrlsInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new ResourceUrlsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrls', rpcArgs);
        return new PostgresDatabaseResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to customize the URLs displayed for the resource.
     * @param callback The callback that will customize URLs for the resource.
     * @returns The resource builder.
     */
    withUrls(callback) {
        return new PostgresDatabaseResourcePromiseImpl(this._withUrlsInternal(callback), this._client);
    }
    /** @internal */
    async _withUrlInternal(url, displayText) {
        const rpcArgs = { builder: this._handle, url };
        if (displayText !== undefined)
            rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrl', rpcArgs);
        return new PostgresDatabaseResourceImpl(result, this._client);
    }
    /**
     * Adds or modifies displayed URLs
     * @param options Additional options.
     */
    withUrl(url, options) {
        const displayText = options?.displayText;
        return new PostgresDatabaseResourcePromiseImpl(this._withUrlInternal(url, displayText), this._client);
    }
    /** @internal */
    async _withUrlForEndpointInternal(endpointName, callback) {
        const callbackId = registerCallback(async (objData) => {
            const obj = wrapIfHandle(objData);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrlForEndpoint', rpcArgs);
        return new PostgresDatabaseResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to update the URL displayed for the endpoint with the specified name.
     * @param endpointName The name of the endpoint to customize the URL for.
     * @param callback The callback that will customize the URL.
     * @returns The resource builder.
     */
    withUrlForEndpoint(endpointName, callback) {
        return new PostgresDatabaseResourcePromiseImpl(this._withUrlForEndpointInternal(endpointName, callback), this._client);
    }
    /** @internal */
    async _excludeFromManifestInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromManifest', rpcArgs);
        return new PostgresDatabaseResourceImpl(result, this._client);
    }
    /**
     * Excludes a resource from being published to the manifest.
     * @returns The resource builder.
     */
    excludeFromManifest() {
        return new PostgresDatabaseResourcePromiseImpl(this._excludeFromManifestInternal(), this._client);
    }
    /** @internal */
    async _withExplicitStartInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExplicitStart', rpcArgs);
        return new PostgresDatabaseResourceImpl(result, this._client);
    }
    /**
     * Prevents resource from starting automatically
     * @returns The resource builder.
     */
    withExplicitStart() {
        return new PostgresDatabaseResourcePromiseImpl(this._withExplicitStartInternal(), this._client);
    }
    /** @internal */
    async _withHealthCheckInternal(key) {
        const rpcArgs = { builder: this._handle, key };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHealthCheck', rpcArgs);
        return new PostgresDatabaseResourceImpl(result, this._client);
    }
    /**
     * Adds a health check by key
     * @param key The key for the health check.
     * @returns The resource builder.
     */
    withHealthCheck(key) {
        return new PostgresDatabaseResourcePromiseImpl(this._withHealthCheckInternal(key), this._client);
    }
    /** @internal */
    async _withCommandInternal(name, displayName, executeCommand, commandOptions) {
        const executeCommandId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined)
            rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability('Aspire.Hosting/withCommand', rpcArgs);
        return new PostgresDatabaseResourceImpl(result, this._client);
    }
    /**
     * Adds a resource command
     *
     * The `WithCommand` method is used to add commands to the resource. Commands are displayed in the dashboard
     * and can be executed by a user using the dashboard UI.
     * When a command is executed, the `executeCommand` callback is called and is run inside the Aspire host.
     * @param name The name of the command. The name uniquely identifies the command.
     * @param displayName The display name visible in UI.
     * @param executeCommand A callback that is executed when the command is executed. The callback is run inside the Aspire host. The callback result is used to indicate success or failure in the UI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withCommand(name, displayName, executeCommand, options) {
        const commandOptions = options?.commandOptions;
        return new PostgresDatabaseResourcePromiseImpl(this._withCommandInternal(name, displayName, executeCommand, commandOptions), this._client);
    }
    /** @internal */
    async _withProcessCommandInternal(commandName, displayName, options) {
        const rpcArgs = { builder: this._handle, commandName, displayName, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommand', rpcArgs);
        return new PostgresDatabaseResourceImpl(result, this._client);
    }
    /** Adds a command to the resource that starts a local process when invoked. */
    withProcessCommand(commandName, displayName, options) {
        return new PostgresDatabaseResourcePromiseImpl(this._withProcessCommandInternal(commandName, displayName, options), this._client);
    }
    /** @internal */
    async _withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options) {
        const createProcessSpecId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await createProcessSpec(arg);
        });
        const rpcArgs = { builder: this._handle, commandName, displayName, createProcessSpec: createProcessSpecId };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommandFactory', rpcArgs);
        return new PostgresDatabaseResourceImpl(result, this._client);
    }
    /**
     * Adds a command to the resource that starts a local process created by a callback when invoked.
     * @param options Additional options.
     */
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new PostgresDatabaseResourcePromiseImpl(this._withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options), this._client);
    }
    /** @internal */
    async _withRelationshipInternal(resourceBuilder, type) {
        resourceBuilder = isPromiseLike(resourceBuilder) ? await resourceBuilder : resourceBuilder;
        const rpcArgs = { builder: this._handle, resourceBuilder, type };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderRelationship', rpcArgs);
        return new PostgresDatabaseResourceImpl(result, this._client);
    }
    /**
     * Adds a relationship to another resource using its builder.
     * @param resourceBuilder The resource builder that the relationship is to.
     * @param type The relationship type.
     * @returns A resource builder.
     */
    withRelationship(resourceBuilder, type) {
        return new PostgresDatabaseResourcePromiseImpl(this._withRelationshipInternal(resourceBuilder, type), this._client);
    }
    /** @internal */
    async _withParentRelationshipInternal(parent) {
        parent = isPromiseLike(parent) ? await parent : parent;
        const rpcArgs = { builder: this._handle, parent };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderParentRelationship', rpcArgs);
        return new PostgresDatabaseResourceImpl(result, this._client);
    }
    /**
     * Sets the parent relationship
     * @param parent The parent of `builder`.
     * @returns A resource builder.
     */
    withParentRelationship(parent) {
        return new PostgresDatabaseResourcePromiseImpl(this._withParentRelationshipInternal(parent), this._client);
    }
    /** @internal */
    async _withChildRelationshipInternal(child) {
        child = isPromiseLike(child) ? await child : child;
        const rpcArgs = { builder: this._handle, child };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderChildRelationship', rpcArgs);
        return new PostgresDatabaseResourceImpl(result, this._client);
    }
    /**
     * Sets a child relationship
     * @param child The child of `builder`.
     * @returns A resource builder.
     */
    withChildRelationship(child) {
        return new PostgresDatabaseResourcePromiseImpl(this._withChildRelationshipInternal(child), this._client);
    }
    /** @internal */
    async _withIconNameInternal(iconName, iconVariant) {
        const rpcArgs = { builder: this._handle, iconName };
        if (iconVariant !== undefined)
            rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability('Aspire.Hosting/withIconName', rpcArgs);
        return new PostgresDatabaseResourceImpl(result, this._client);
    }
    /**
     * Specifies the icon to use when displaying the resource in the dashboard.
     * @param iconName The name of the FluentUI icon to use. See https://aka.ms/fluentui-system-icons for available icons.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withIconName(iconName, options) {
        const iconVariant = options?.iconVariant;
        return new PostgresDatabaseResourcePromiseImpl(this._withIconNameInternal(iconName, iconVariant), this._client);
    }
    /** @internal */
    async _excludeFromMcpInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromMcp', rpcArgs);
        return new PostgresDatabaseResourceImpl(result, this._client);
    }
    /**
     * Exclude the resource from MCP operations using the Aspire MCP server. The resource is excluded from results that return resources, console logs and telemetry.
     * @returns The resource builder.
     */
    excludeFromMcp() {
        return new PostgresDatabaseResourcePromiseImpl(this._excludeFromMcpInternal(), this._client);
    }
    /** @internal */
    async _withHiddenInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHidden', rpcArgs);
        return new PostgresDatabaseResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists
     *
     * Use this method to hide resources that are implementation details and should never be displayed by default.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @returns The resource builder.
     */
    withHidden() {
        return new PostgresDatabaseResourcePromiseImpl(this._withHiddenInternal(), this._client);
    }
    /** @internal */
    async _withHiddenOnCompletionInternal(exitCode, exitCodes) {
        const rpcArgs = { builder: this._handle };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        if (exitCodes !== undefined)
            rpcArgs.exitCodes = exitCodes;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHiddenOnCompletion', rpcArgs);
        return new PostgresDatabaseResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists after successful completion
     *
     * This method is useful for one-off resources such as setup scripts, migrations, or build steps that should remain visible while running
     * and then be hidden after successful completion.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHiddenOnCompletion(options) {
        const exitCode = options?.exitCode;
        const exitCodes = options?.exitCodes;
        return new PostgresDatabaseResourcePromiseImpl(this._withHiddenOnCompletionInternal(exitCode, exitCodes), this._client);
    }
    /** @internal */
    async _withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new PipelineStepContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined)
            rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined)
            rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined)
            rpcArgs.tags = tags;
        if (description !== undefined)
            rpcArgs.description = description;
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineStepFactory', rpcArgs);
        return new PostgresDatabaseResourceImpl(result, this._client);
    }
    /**
     * Adds a pipeline step to the resource that will be executed during deployment.
     * @param stepName The unique name of the pipeline step.
     * @param callback The callback to execute when the step runs.
     * @param options Additional options.
     * @returns The resource builder for chaining.
     */
    withPipelineStepFactory(stepName, callback, options) {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new PostgresDatabaseResourcePromiseImpl(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description), this._client);
    }
    /** @internal */
    async _withPipelineConfigurationInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new PipelineConfigurationContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineConfiguration', rpcArgs);
        return new PostgresDatabaseResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to be executed during the pipeline configuration phase, allowing modification of step dependencies and relationships.
     * @param callback The callback function to execute during the configuration phase.
     * @returns The resource builder for chaining.
     */
    withPipelineConfiguration(callback) {
        return new PostgresDatabaseResourcePromiseImpl(this._withPipelineConfigurationInternal(callback), this._client);
    }
    /**
     * Gets the name of the resource from a builder.
     *
     * Why this wrapper exists: This capability accesses a nested property
     * (`resource.Resource.Name`) which requires a wrapper method. There is no single
     * .NET method that returns just the resource name that could be annotated directly.
     * @returns The resource name.
     */
    async getResourceName() {
        const rpcArgs = { resource: this._handle };
        return await this._client.invokeCapability('Aspire.Hosting/getResourceName', rpcArgs);
    }
    /** @internal */
    async _onBeforeResourceStartedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new BeforeResourceStartedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onBeforeResourceStarted', rpcArgs);
        return new PostgresDatabaseResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the BeforeResourceStarted event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onBeforeResourceStarted(callback) {
        return new PostgresDatabaseResourcePromiseImpl(this._onBeforeResourceStartedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceStoppedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceStoppedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceStopped', rpcArgs);
        return new PostgresDatabaseResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceStopped event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceStopped(callback) {
        return new PostgresDatabaseResourcePromiseImpl(this._onResourceStoppedInternal(callback), this._client);
    }
    /** @internal */
    async _onConnectionStringAvailableInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ConnectionStringAvailableEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onConnectionStringAvailable', rpcArgs);
        return new PostgresDatabaseResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ConnectionStringAvailable event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onConnectionStringAvailable(callback) {
        return new PostgresDatabaseResourcePromiseImpl(this._onConnectionStringAvailableInternal(callback), this._client);
    }
    /** @internal */
    async _onInitializeResourceInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new InitializeResourceEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onInitializeResource', rpcArgs);
        return new PostgresDatabaseResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the InitializeResource event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onInitializeResource(callback) {
        return new PostgresDatabaseResourcePromiseImpl(this._onInitializeResourceInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceReadyInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceReadyEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceReady', rpcArgs);
        return new PostgresDatabaseResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceReady event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceReady(callback) {
        return new PostgresDatabaseResourcePromiseImpl(this._onResourceReadyInternal(callback), this._client);
    }
    /**
     * Creates an execution configuration builder for the specified resource.
     * @returns The execution configuration builder.
     */
    createExecutionConfiguration() {
        const promise = (async () => {
            const rpcArgs = { resource: this._handle };
            const handle = await this._client.invokeCapability('Aspire.Hosting/createExecutionConfiguration', rpcArgs);
            return new ExecutionConfigurationBuilderImpl(handle, this._client);
        })();
        return new ExecutionConfigurationBuilderPromiseImpl(promise, this._client);
    }
    /** @internal */
    async _withPostgresMcpInternal(configureContainer, containerName) {
        const configureContainerId = configureContainer ? registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new PostgresMcpContainerResourceImpl(objHandle, this._client);
            await configureContainer(obj);
        }) : undefined;
        const rpcArgs = { builder: this._handle };
        if (configureContainer !== undefined)
            rpcArgs.configureContainer = configureContainerId;
        if (containerName !== undefined)
            rpcArgs.containerName = containerName;
        const result = await this._client.invokeCapability('Aspire.Hosting.PostgreSQL/withPostgresMcp', rpcArgs);
        return new PostgresDatabaseResourceImpl(result, this._client);
    }
    /**
     * Adds a Postgres MCP server container and configures it to connect to the database represented by `builder`.
     *
     * The Postgres MCP server is configured to use SSE transport and will expose an HTTP endpoint.
     * This version of the package defaults to the tag of the container image.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withPostgresMcp(options) {
        const configureContainer = options?.configureContainer;
        const containerName = options?.containerName;
        return new PostgresDatabaseResourcePromiseImpl(this._withPostgresMcpInternal(configureContainer, containerName), this._client);
    }
    /** @internal */
    async _withCreationScriptInternal(script) {
        const rpcArgs = { builder: this._handle, script };
        const result = await this._client.invokeCapability('Aspire.Hosting.PostgreSQL/withCreationScript', rpcArgs);
        return new PostgresDatabaseResourceImpl(result, this._client);
    }
    /**
     * Defines the SQL script used to create the database.
     *
     * The script can only contain SQL statements applying to the default database like CREATE DATABASE. Custom statements like table creation
     * and data insertion are not supported since they require a distinct connection to the newly created database.
     * Default script is
     * ```
     * CREATE DATABASE "<QUOTED_DATABASE_NAME>"
     * ```
     * @param script The SQL script used to create the database.
     * @returns The resource builder.
     */
    withCreationScript(script) {
        return new PostgresDatabaseResourcePromiseImpl(this._withCreationScriptInternal(script), this._client);
    }
}
/**
 * Thenable wrapper for PostgresDatabaseResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
class PostgresDatabaseResourcePromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    parent() {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.parent()), this._client, false);
    }
    connectionStringExpression() {
        return this._promise.then(obj => obj.connectionStringExpression());
    }
    databaseName() {
        return this._promise.then(obj => obj.databaseName());
    }
    uriExpression() {
        return this._promise.then(obj => obj.uriExpression());
    }
    jdbcConnectionString() {
        return this._promise.then(obj => obj.jdbcConnectionString());
    }
    name() {
        return this._promise.then(obj => obj.name());
    }
    withContainerRegistry(registry) {
        return new PostgresDatabaseResourcePromiseImpl(this._promise.then(obj => obj.withContainerRegistry(registry)), this._client);
    }
    withDockerfileBaseImage(options) {
        return new PostgresDatabaseResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileBaseImage(options)), this._client);
    }
    withRequiredCommand(command, options) {
        return new PostgresDatabaseResourcePromiseImpl(this._promise.then(obj => obj.withRequiredCommand(command, options)), this._client);
    }
    withSessionLifetime() {
        return new PostgresDatabaseResourcePromiseImpl(this._promise.then(obj => obj.withSessionLifetime()), this._client);
    }
    withPersistentLifetime() {
        return new PostgresDatabaseResourcePromiseImpl(this._promise.then(obj => obj.withPersistentLifetime()), this._client);
    }
    withLifetimeOf(sourceBuilder) {
        return new PostgresDatabaseResourcePromiseImpl(this._promise.then(obj => obj.withLifetimeOf(sourceBuilder)), this._client);
    }
    withParentProcessLifetime(parentProcessId) {
        return new PostgresDatabaseResourcePromiseImpl(this._promise.then(obj => obj.withParentProcessLifetime(parentProcessId)), this._client);
    }
    withConnectionProperty(name, value) {
        return new PostgresDatabaseResourcePromiseImpl(this._promise.then(obj => obj.withConnectionProperty(name, value)), this._client);
    }
    getConnectionProperty(key) {
        return this._promise.then(obj => obj.getConnectionProperty(key));
    }
    withUrls(callback) {
        return new PostgresDatabaseResourcePromiseImpl(this._promise.then(obj => obj.withUrls(callback)), this._client);
    }
    withUrl(url, options) {
        return new PostgresDatabaseResourcePromiseImpl(this._promise.then(obj => obj.withUrl(url, options)), this._client);
    }
    withUrlForEndpoint(endpointName, callback) {
        return new PostgresDatabaseResourcePromiseImpl(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)), this._client);
    }
    excludeFromManifest() {
        return new PostgresDatabaseResourcePromiseImpl(this._promise.then(obj => obj.excludeFromManifest()), this._client);
    }
    withExplicitStart() {
        return new PostgresDatabaseResourcePromiseImpl(this._promise.then(obj => obj.withExplicitStart()), this._client);
    }
    withHealthCheck(key) {
        return new PostgresDatabaseResourcePromiseImpl(this._promise.then(obj => obj.withHealthCheck(key)), this._client);
    }
    withCommand(name, displayName, executeCommand, options) {
        return new PostgresDatabaseResourcePromiseImpl(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)), this._client);
    }
    withProcessCommand(commandName, displayName, options) {
        return new PostgresDatabaseResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommand(commandName, displayName, options)), this._client);
    }
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new PostgresDatabaseResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommandFactory(commandName, displayName, createProcessSpec, options)), this._client);
    }
    withRelationship(resourceBuilder, type) {
        return new PostgresDatabaseResourcePromiseImpl(this._promise.then(obj => obj.withRelationship(resourceBuilder, type)), this._client);
    }
    withParentRelationship(parent) {
        return new PostgresDatabaseResourcePromiseImpl(this._promise.then(obj => obj.withParentRelationship(parent)), this._client);
    }
    withChildRelationship(child) {
        return new PostgresDatabaseResourcePromiseImpl(this._promise.then(obj => obj.withChildRelationship(child)), this._client);
    }
    withIconName(iconName, options) {
        return new PostgresDatabaseResourcePromiseImpl(this._promise.then(obj => obj.withIconName(iconName, options)), this._client);
    }
    excludeFromMcp() {
        return new PostgresDatabaseResourcePromiseImpl(this._promise.then(obj => obj.excludeFromMcp()), this._client);
    }
    withHidden() {
        return new PostgresDatabaseResourcePromiseImpl(this._promise.then(obj => obj.withHidden()), this._client);
    }
    withHiddenOnCompletion(options) {
        return new PostgresDatabaseResourcePromiseImpl(this._promise.then(obj => obj.withHiddenOnCompletion(options)), this._client);
    }
    withPipelineStepFactory(stepName, callback, options) {
        return new PostgresDatabaseResourcePromiseImpl(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)), this._client);
    }
    withPipelineConfiguration(callback) {
        return new PostgresDatabaseResourcePromiseImpl(this._promise.then(obj => obj.withPipelineConfiguration(callback)), this._client);
    }
    getResourceName() {
        return this._promise.then(obj => obj.getResourceName());
    }
    onBeforeResourceStarted(callback) {
        return new PostgresDatabaseResourcePromiseImpl(this._promise.then(obj => obj.onBeforeResourceStarted(callback)), this._client);
    }
    onResourceStopped(callback) {
        return new PostgresDatabaseResourcePromiseImpl(this._promise.then(obj => obj.onResourceStopped(callback)), this._client);
    }
    onConnectionStringAvailable(callback) {
        return new PostgresDatabaseResourcePromiseImpl(this._promise.then(obj => obj.onConnectionStringAvailable(callback)), this._client);
    }
    onInitializeResource(callback) {
        return new PostgresDatabaseResourcePromiseImpl(this._promise.then(obj => obj.onInitializeResource(callback)), this._client);
    }
    onResourceReady(callback) {
        return new PostgresDatabaseResourcePromiseImpl(this._promise.then(obj => obj.onResourceReady(callback)), this._client);
    }
    createExecutionConfiguration() {
        return new ExecutionConfigurationBuilderPromiseImpl(this._promise.then(obj => obj.createExecutionConfiguration()), this._client);
    }
    withPostgresMcp(options) {
        return new PostgresDatabaseResourcePromiseImpl(this._promise.then(obj => obj.withPostgresMcp(options)), this._client);
    }
    withCreationScript(script) {
        return new PostgresDatabaseResourcePromiseImpl(this._promise.then(obj => obj.withCreationScript(script)), this._client);
    }
}
// ============================================================================
// PostgresMcpContainerResourceImpl
// ============================================================================
class PostgresMcpContainerResourceImpl extends ResourceBuilderBase {
    constructor(handle, client) {
        super(handle, client);
    }
    /** @internal */
    async _withContainerRegistryInternal(registry) {
        registry = isPromiseLike(registry) ? await registry : registry;
        const rpcArgs = { builder: this._handle, registry };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerRegistry', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Configures the resource to use the specified container registry for container image operations.
     *
     * This method adds a `ContainerRegistryReferenceAnnotation` to the resource,
     * indicating that the resource should use the specified container registry for container image operations.
     * @param registry The container registry resource builder.
     * @returns The resource builder for chaining.
     */
    withContainerRegistry(registry) {
        return new PostgresMcpContainerResourcePromiseImpl(this._withContainerRegistryInternal(registry), this._client);
    }
    /** @internal */
    async _withBindMountInternal(source, target, isReadOnly) {
        const rpcArgs = { builder: this._handle, source, target };
        if (isReadOnly !== undefined)
            rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability('Aspire.Hosting/withBindMount', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a bind mount to a container resource.
     * @param source The source path of the mount. This is the path to the file or directory on the host, relative to the app host project directory.
     * @param target The target path where the file or directory is mounted in the container.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withBindMount(source, target, options) {
        const isReadOnly = options?.isReadOnly;
        return new PostgresMcpContainerResourcePromiseImpl(this._withBindMountInternal(source, target, isReadOnly), this._client);
    }
    /** @internal */
    async _withEntrypointInternal(entrypoint) {
        const rpcArgs = { builder: this._handle, entrypoint };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEntrypoint', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Sets the Entrypoint for the container.
     * @param entrypoint The new entrypoint for the container.
     * @returns The resource builder.
     */
    withEntrypoint(entrypoint) {
        return new PostgresMcpContainerResourcePromiseImpl(this._withEntrypointInternal(entrypoint), this._client);
    }
    /** @internal */
    async _withImageTagInternal(tag) {
        const rpcArgs = { builder: this._handle, tag };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImageTag', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Allows overriding the image tag on a container.
     * @param tag Tag value.
     * @returns The resource builder.
     */
    withImageTag(tag) {
        return new PostgresMcpContainerResourcePromiseImpl(this._withImageTagInternal(tag), this._client);
    }
    /** @internal */
    async _withImageRegistryInternal(registry) {
        const rpcArgs = { builder: this._handle, registry };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImageRegistry', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Allows overriding the image registry on a container.
     * @param registry Registry value.
     * @returns The resource builder.
     */
    withImageRegistry(registry) {
        return new PostgresMcpContainerResourcePromiseImpl(this._withImageRegistryInternal(registry), this._client);
    }
    /** @internal */
    async _withImageInternal(image, tag) {
        const rpcArgs = { builder: this._handle, image };
        if (tag !== undefined)
            rpcArgs.tag = tag;
        const result = await this._client.invokeCapability('Aspire.Hosting/withImage', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Allows overriding the image on a container.
     * @param image Image value.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withImage(image, options) {
        const tag = options?.tag;
        return new PostgresMcpContainerResourcePromiseImpl(this._withImageInternal(image, tag), this._client);
    }
    /** @internal */
    async _withImageSHA256Internal(sha256) {
        const rpcArgs = { builder: this._handle, sha256 };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImageSHA256', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Allows setting the image to a specific sha256 on a container.
     * @param sha256 Registry value.
     * @returns The resource builder.
     */
    withImageSHA256(sha256) {
        return new PostgresMcpContainerResourcePromiseImpl(this._withImageSHA256Internal(sha256), this._client);
    }
    /** @internal */
    async _withContainerRuntimeArgsInternal(args) {
        const rpcArgs = { builder: this._handle, args };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerRuntimeArgs', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a callback to be executed with a list of arguments to add to the container runtime run command when a container resource is started.
     * @param args The arguments to be passed to the container runtime run command when the container resource is started.
     * @returns The resource builder.
     */
    withContainerRuntimeArgs(args) {
        return new PostgresMcpContainerResourcePromiseImpl(this._withContainerRuntimeArgsInternal(args), this._client);
    }
    /** @internal */
    async _withLifetimeInternal(lifetime) {
        const rpcArgs = { builder: this._handle, lifetime };
        const result = await this._client.invokeCapability('Aspire.Hosting/withLifetime', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Sets the lifetime behavior of the container resource.
     *
     * Prefer `WithPersistentLifetime``1` or
     * `WithSessionLifetime``1` for new code.
     * Marking a container resource to have a `Persistent` lifetime.
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddContainer("mycontainer", "myimage")
     * .WithPersistentLifetime();
     * builder.Build().Run();
     * ```
     * @param lifetime The lifetime behavior of the container resource. The default behavior is `Session`.
     * @returns The resource builder.
     */
    withLifetime(lifetime) {
        return new PostgresMcpContainerResourcePromiseImpl(this._withLifetimeInternal(lifetime), this._client);
    }
    /** @internal */
    async _withImagePullPolicyInternal(pullPolicy) {
        const rpcArgs = { builder: this._handle, pullPolicy };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImagePullPolicy', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Sets the pull policy for the container resource.
     * @param pullPolicy The pull policy behavior for the container resource.
     * @returns The resource builder.
     */
    withImagePullPolicy(pullPolicy) {
        return new PostgresMcpContainerResourcePromiseImpl(this._withImagePullPolicyInternal(pullPolicy), this._client);
    }
    /** @internal */
    async _publishAsContainerInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/publishAsContainer', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Changes the resource to be published as a container in the manifest.
     * @returns The resource builder.
     */
    publishAsContainer() {
        return new PostgresMcpContainerResourcePromiseImpl(this._publishAsContainerInternal(), this._client);
    }
    /** @internal */
    async _withDockerfileInternal(contextPath, dockerfilePath, stage) {
        const rpcArgs = { builder: this._handle, contextPath };
        if (dockerfilePath !== undefined)
            rpcArgs.dockerfilePath = dockerfilePath;
        if (stage !== undefined)
            rpcArgs.stage = stage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfile', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Causes Aspire to build the specified container image from a Dockerfile.
     * @param contextPath Path to be used as the context for the container image build.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfile(contextPath, options) {
        const dockerfilePath = options?.dockerfilePath;
        const stage = options?.stage;
        return new PostgresMcpContainerResourcePromiseImpl(this._withDockerfileInternal(contextPath, dockerfilePath, stage), this._client);
    }
    /** @internal */
    async _withDockerfileFactoryInternal(contextPath, dockerfileFactory, stage) {
        const dockerfileFactoryId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new DockerfileFactoryContextImpl(argHandle, this._client);
            return await dockerfileFactory(arg);
        });
        const rpcArgs = { builder: this._handle, contextPath, dockerfileFactory: dockerfileFactoryId };
        if (stage !== undefined)
            rpcArgs.stage = stage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileFactory', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Builds the specified container image from a Dockerfile generated by an asynchronous factory function.
     *
     * When this method is called, an annotation is added to the `ContainerResource` that specifies the context path
     * and a factory function that generates Dockerfile content. The factory is invoked at build time to produce the Dockerfile,
     * which is then written to a temporary file and used by the orchestrator to build the container image.
     * The `contextPath` is relative to the AppHost directory unless it is fully qualified.
     * The factory function is invoked once during the build process to generate the Dockerfile content.
     * The output is trusted and not validated.
     * Creates a container called `mycontainer` with a dynamically generated Dockerfile.
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddContainer("mycontainer", "myimage")
     * .WithDockerfileFactory("path/to/context", async context =>
     * {
     * var template = await File.ReadAllTextAsync("template.dockerfile", context.CancellationToken);
     * return template.Replace("{{VERSION}}", "1.0");
     * });
     * builder.Build().Run();
     * ```
     * @param contextPath Path to be used as the context for the container image build.
     * @param dockerfileFactory An asynchronous function that returns the Dockerfile content as a string.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileFactory(contextPath, dockerfileFactory, options) {
        const stage = options?.stage;
        return new PostgresMcpContainerResourcePromiseImpl(this._withDockerfileFactoryInternal(contextPath, dockerfileFactory, stage), this._client);
    }
    /** @internal */
    async _withContainerNameInternal(name) {
        const rpcArgs = { builder: this._handle, name };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerName', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Overrides the default container name for this resource. By default Aspire generates a unique container name based on the resource name and a random postfix (or a postfix based on a hash of the AppHost project path for persistent container resources). This method allows you to override that behavior with a custom name, but could lead to naming conflicts if the specified name is not unique.
     *
     * Combining this with `Persistent` will allow Aspire to re-use an existing container that was not
     * created by an Aspire AppHost.
     * @param name The desired container name. Must be a valid container name or your runtime will report an error.
     * @returns The resource builder.
     */
    withContainerName(name) {
        return new PostgresMcpContainerResourcePromiseImpl(this._withContainerNameInternal(name), this._client);
    }
    /** @internal */
    async _withBuildArgInternal(name, value) {
        value = isPromiseLike(value) ? await value : value;
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuildArg', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a build argument when the container is built from a Dockerfile.
     * @param name The name of the build argument.
     * @param value The build argument value, either a string or a parameter resource.
     * @returns The resource builder.
     */
    withBuildArg(name, value) {
        return new PostgresMcpContainerResourcePromiseImpl(this._withBuildArgInternal(name, value), this._client);
    }
    /** @internal */
    async _withBuildSecretInternal(name, value) {
        value = isPromiseLike(value) ? await value : value;
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withParameterBuildSecret', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a secret build argument when the container is built from a Dockerfile.
     * @param name The name of the secret build argument.
     * @param value The resource builder for a parameter resource.
     * @returns The resource builder.
     */
    withBuildSecret(name, value) {
        return new PostgresMcpContainerResourcePromiseImpl(this._withBuildSecretInternal(name, value), this._client);
    }
    /** @internal */
    async _withContainerCertificatePathsInternal(customCertificatesDestination, defaultCertificateBundlePaths, defaultCertificateDirectoryPaths) {
        const rpcArgs = { builder: this._handle };
        if (customCertificatesDestination !== undefined)
            rpcArgs.customCertificatesDestination = customCertificatesDestination;
        if (defaultCertificateBundlePaths !== undefined)
            rpcArgs.defaultCertificateBundlePaths = defaultCertificateBundlePaths;
        if (defaultCertificateDirectoryPaths !== undefined)
            rpcArgs.defaultCertificateDirectoryPaths = defaultCertificateDirectoryPaths;
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerCertificatePaths', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Adds container certificate path overrides used for certificate trust at run time.
     * @param options Additional options.
     * @returns The updated resource builder.
     */
    withContainerCertificatePaths(options) {
        const customCertificatesDestination = options?.customCertificatesDestination;
        const defaultCertificateBundlePaths = options?.defaultCertificateBundlePaths;
        const defaultCertificateDirectoryPaths = options?.defaultCertificateDirectoryPaths;
        return new PostgresMcpContainerResourcePromiseImpl(this._withContainerCertificatePathsInternal(customCertificatesDestination, defaultCertificateBundlePaths, defaultCertificateDirectoryPaths), this._client);
    }
    /** @internal */
    async _withDockerfileBuilderInternal(contextPath, callback, stage) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new DockerfileBuilderCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, contextPath, callback: callbackId };
        if (stage !== undefined)
            rpcArgs.stage = stage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileBuilder', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Configures the resource to use a programmatically generated Dockerfile
     *
     * This method provides a programmatic way to build Dockerfiles using the `DockerfileBuilder` API
     * instead of string manipulation. Callbacks can be composed by calling this method multiple times - each callback will be invoked
     * in order to build up the final Dockerfile.
     * The `contextPath` is relative to the AppHost directory unless it is fully qualified.
     * Creates a container with a programmatically built Dockerfile using fluent API:
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddContainer("mycontainer", "myimage")
     * .WithDockerfileBuilder("path/to/context", context =>
     * {
     * context.Builder.From("alpine:latest")
     * .WorkDir("/app")
     * .Run("apk add curl")
     * .Copy(".", ".")
     * .Cmd(["./myapp"]);
     * return Task.CompletedTask;
     * });
     * builder.Build().Run();
     * ```
     * @param contextPath Path to be used as the context for the container image build.
     * @param callback A callback that uses the `DockerfileBuilder` API to construct the Dockerfile.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileBuilder(contextPath, callback, options) {
        const stage = options?.stage;
        return new PostgresMcpContainerResourcePromiseImpl(this._withDockerfileBuilderInternal(contextPath, callback, stage), this._client);
    }
    /** @internal */
    async _withDockerfileBaseImageInternal(buildImage, runtimeImage) {
        const rpcArgs = { builder: this._handle };
        if (buildImage !== undefined)
            rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined)
            rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileBaseImage', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Configures custom base images for generated Dockerfiles.
     *
     * This extension method allows customization of the base images used in generated Dockerfiles.
     * For multi-stage Dockerfiles (e.g., Python with UV), you can specify separate build and runtime images.
     * Specify custom base images for a Python application:
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddPythonApp("myapp", "path/to/app", "main.py")
     * .WithDockerfileBaseImage(
     * buildImage: "ghcr.io/astral-sh/uv:python3.12-bookworm-slim",
     * runtimeImage: "python:3.12-slim-bookworm");
     * builder.Build().Run();
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileBaseImage(options) {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new PostgresMcpContainerResourcePromiseImpl(this._withDockerfileBaseImageInternal(buildImage, runtimeImage), this._client);
    }
    /** @internal */
    async _withContainerNetworkAliasInternal(alias) {
        const rpcArgs = { builder: this._handle, alias };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerNetworkAlias', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a network alias to container resource.
     *
     * Network aliases enable DNS resolution of the container on the network by custom names.
     * By default, containers are accessible on the network using their resource name as a DNS alias.
     * This method allows adding additional aliases for the same container.
     * Multiple aliases can be added by calling this method multiple times.
     * @param alias The network alias for the container.
     * @returns The resource builder.
     */
    withContainerNetworkAlias(alias) {
        return new PostgresMcpContainerResourcePromiseImpl(this._withContainerNetworkAliasInternal(alias), this._client);
    }
    /** @internal */
    async _withMcpServerInternal(path, endpointName) {
        const rpcArgs = { builder: this._handle };
        if (path !== undefined)
            rpcArgs.path = path;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withMcpServer', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Marks the resource as hosting a Model Context Protocol (MCP) server on the specified endpoint.
     *
     * This method adds an `McpServerEndpointAnnotation` to the resource, enabling the Aspire tooling
     * to discover and proxy the MCP server exposed by the resource.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withMcpServer(options) {
        const path = options?.path;
        const endpointName = options?.endpointName;
        return new PostgresMcpContainerResourcePromiseImpl(this._withMcpServerInternal(path, endpointName), this._client);
    }
    /** @internal */
    async _withOtlpExporterInternal(protocol) {
        const rpcArgs = { builder: this._handle };
        if (protocol !== undefined)
            rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability('Aspire.Hosting/withOtlpExporter', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Configures OTLP telemetry export
     * @param options Additional options.
     */
    withOtlpExporter(options) {
        const protocol = options?.protocol;
        return new PostgresMcpContainerResourcePromiseImpl(this._withOtlpExporterInternal(protocol), this._client);
    }
    /** @internal */
    async _publishAsConnectionStringInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/publishAsConnectionString', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Changes the resource to be published as a connection string reference in the manifest.
     * @returns The resource builder.
     */
    publishAsConnectionString() {
        return new PostgresMcpContainerResourcePromiseImpl(this._publishAsConnectionStringInternal(), this._client);
    }
    /** @internal */
    async _withRequiredCommandInternal(command, helpLink) {
        const rpcArgs = { builder: this._handle, command };
        if (helpLink !== undefined)
            rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability('Aspire.Hosting/withRequiredCommand', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Declares that a resource requires a specific command/executable to be available on the local machine PATH before it can start.
     *
     * The command is considered valid if either:
     * 1. It is an absolute or relative path (contains a directory separator) that points to an existing file, or
     * 2. It is discoverable on the current process PATH (respecting PATHEXT on Windows).
     * If the command is not found, a warning message will be logged but the resource will be allowed to attempt to start.
     * @param command The command string (file name or path) that should be validated.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withRequiredCommand(command, options) {
        const helpLink = options?.helpLink;
        return new PostgresMcpContainerResourcePromiseImpl(this._withRequiredCommandInternal(command, helpLink), this._client);
    }
    /** @internal */
    async _withSessionLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withSessionLifetime', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a session lifetime.
     * @returns The resource builder.
     */
    withSessionLifetime() {
        return new PostgresMcpContainerResourcePromiseImpl(this._withSessionLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withPersistentLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPersistentLifetime', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime.
     * @returns The resource builder.
     */
    withPersistentLifetime() {
        return new PostgresMcpContainerResourcePromiseImpl(this._withPersistentLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withLifetimeOfInternal(sourceBuilder) {
        sourceBuilder = isPromiseLike(sourceBuilder) ? await sourceBuilder : sourceBuilder;
        const rpcArgs = { builder: this._handle, sourceBuilder };
        const result = await this._client.invokeCapability('Aspire.Hosting/withLifetimeOf', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to match the lifetime of another resource.
     *
     * The resource lifetime is evaluated from `sourceBuilder` when the application model is prepared, so later lifetime
     * changes to the source resource are reflected by this resource.
     * @param sourceBuilder The resource builder whose lifetime should be used.
     * @returns The resource builder.
     */
    withLifetimeOf(sourceBuilder) {
        return new PostgresMcpContainerResourcePromiseImpl(this._withLifetimeOfInternal(sourceBuilder), this._client);
    }
    /** @internal */
    async _withParentProcessLifetimeInternal(parentProcessId) {
        const rpcArgs = { builder: this._handle, parentProcessId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withParentProcessLifetime', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime that ends when a parent process exits.
     * @param parentProcessId The ID of the parent process to monitor.
     * @returns The resource builder.
     */
    withParentProcessLifetime(parentProcessId) {
        return new PostgresMcpContainerResourcePromiseImpl(this._withParentProcessLifetimeInternal(parentProcessId), this._client);
    }
    /** @internal */
    async _withEnvironmentInternal(name, value) {
        value = isPromiseLike(value) ? await value : value;
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEnvironment', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /** Sets an environment variable */
    withEnvironment(name, value) {
        return new PostgresMcpContainerResourcePromiseImpl(this._withEnvironmentInternal(name, value), this._client);
    }
    /** @internal */
    async _withEnvironmentCallbackInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new EnvironmentCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEnvironmentCallback', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Allows for the population of environment variables on a resource.
     * @param callback A callback that allows for deferred execution for computing many environment variables. This runs after resources have been allocated by the orchestrator and allows access to other resources to resolve computed data, e.g. connection strings, ports.
     * @returns The resource builder.
     */
    withEnvironmentCallback(callback) {
        return new PostgresMcpContainerResourcePromiseImpl(this._withEnvironmentCallbackInternal(callback), this._client);
    }
    /** @internal */
    async _withArgsInternal(args) {
        const rpcArgs = { builder: this._handle, args };
        const result = await this._client.invokeCapability('Aspire.Hosting/withArgs', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Adds arguments to be passed to a resource that supports arguments when it is launched.
     * @param args The arguments to be passed to the resource when it is started.
     * @returns The resource builder.
     */
    withArgs(args) {
        return new PostgresMcpContainerResourcePromiseImpl(this._withArgsInternal(args), this._client);
    }
    /** @internal */
    async _withArgsCallbackInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new CommandLineArgsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withArgsCallback', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a callback to be executed with a list of command-line arguments when a resource is started.
     * @param callback A callback that allows for deferred execution for computing arguments. This runs after resources have been allocated by the orchestrator and allows access to other resources to resolve computed data, e.g. connection strings, ports.
     * @returns The resource builder.
     */
    withArgsCallback(callback) {
        return new PostgresMcpContainerResourcePromiseImpl(this._withArgsCallbackInternal(callback), this._client);
    }
    /** @internal */
    async _withReferenceEnvironmentInternal(options) {
        const rpcArgs = { builder: this._handle, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withReferenceEnvironment', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Configures how information is injected into environment variables when the resource references other resources.
     * @param options Options controlling which reference information is emitted.
     * @returns The resource builder.
     */
    withReferenceEnvironment(options) {
        return new PostgresMcpContainerResourcePromiseImpl(this._withReferenceEnvironmentInternal(options), this._client);
    }
    /** @internal */
    async _withReferenceInternal(source, connectionName, optional, name) {
        source = isPromiseLike(source) ? await source : source;
        const rpcArgs = { builder: this._handle, source };
        if (connectionName !== undefined)
            rpcArgs.connectionName = connectionName;
        if (optional !== undefined)
            rpcArgs.optional = optional;
        if (name !== undefined)
            rpcArgs.name = name;
        const result = await this._client.invokeCapability('Aspire.Hosting/withReference', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a reference to another resource
     * @param options Additional options.
     */
    withReference(source, options) {
        const connectionName = options?.connectionName;
        const optional = options?.optional;
        const name = options?.name;
        return new PostgresMcpContainerResourcePromiseImpl(this._withReferenceInternal(source, connectionName, optional, name), this._client);
    }
    /** @internal */
    async _withEndpointCallbackInternal(endpointName, callback, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpointCallback', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Updates a named endpoint via callback
     * @param options Additional options.
     */
    withEndpointCallback(endpointName, callback, options) {
        const createIfNotExists = options?.createIfNotExists;
        return new PostgresMcpContainerResourcePromiseImpl(this._withEndpointCallbackInternal(endpointName, callback, createIfNotExists), this._client);
    }
    /** @internal */
    async _withHttpEndpointCallbackInternal(callback, name, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        if (name !== undefined)
            rpcArgs.name = name;
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpEndpointCallback', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Updates an HTTP endpoint via callback
     * @param options Additional options.
     */
    withHttpEndpointCallback(callback, options) {
        const name = options?.name;
        const createIfNotExists = options?.createIfNotExists;
        return new PostgresMcpContainerResourcePromiseImpl(this._withHttpEndpointCallbackInternal(callback, name, createIfNotExists), this._client);
    }
    /** @internal */
    async _withHttpsEndpointCallbackInternal(callback, name, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        if (name !== undefined)
            rpcArgs.name = name;
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpsEndpointCallback', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Updates an HTTPS endpoint via callback
     * @param options Additional options.
     */
    withHttpsEndpointCallback(callback, options) {
        const name = options?.name;
        const createIfNotExists = options?.createIfNotExists;
        return new PostgresMcpContainerResourcePromiseImpl(this._withHttpsEndpointCallbackInternal(callback, name, createIfNotExists), this._client);
    }
    /** @internal */
    async _withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (scheme !== undefined)
            rpcArgs.scheme = scheme;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        if (isExternal !== undefined)
            rpcArgs.isExternal = isExternal;
        if (protocol !== undefined)
            rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpoint', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a network endpoint
     * @param options Additional options.
     * @returns The resource builder.
     */
    withEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const scheme = options?.scheme;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        const isExternal = options?.isExternal;
        const protocol = options?.protocol;
        return new PostgresMcpContainerResourcePromiseImpl(this._withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol), this._client);
    }
    /** @internal */
    async _withEndpointProxySupportInternal(proxyEnabled) {
        const rpcArgs = { builder: this._handle, proxyEnabled };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpointProxySupport', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Set whether a resource can use proxied endpoints or whether they should be disabled for all endpoints belonging to the resource. If set to `false`, endpoints belonging to the resource will ignore the configured proxy settings and run proxy-less.
     *
     * This method is intended to support scenarios with persistent lifetime resources where it is desirable for the resource to be accessible over the same
     * port whether the Aspire application is running or not. Proxied endpoints bind ports that are only accessible while the Aspire application is running.
     * The user needs to be careful to ensure that endpoints are using unique ports when disabling proxy support as by default for proxy-less
     * endpoints, Aspire will allocate the target port as the host port, which will increase the chance of port conflicts.
     * @param proxyEnabled Should endpoints for the resource support using a proxy?
     * @returns The resource builder.
     */
    withEndpointProxySupport(proxyEnabled) {
        return new PostgresMcpContainerResourcePromiseImpl(this._withEndpointProxySupportInternal(proxyEnabled), this._client);
    }
    /** @internal */
    async _withHttpEndpointInternal(port, targetPort, name, env, isProxied) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpEndpoint', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP endpoint
     *
     * If an endpoint with the same name already exists on the resource, the existing endpoint is updated
     * with any non-null parameter values. Parameters left as `null` will not modify the existing endpoint's values.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new PostgresMcpContainerResourcePromiseImpl(this._withHttpEndpointInternal(port, targetPort, name, env, isProxied), this._client);
    }
    /** @internal */
    async _withHttpsEndpointInternal(port, targetPort, name, env, isProxied) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpsEndpoint', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTPS endpoint
     *
     * If an endpoint with the same name already exists on the resource, the existing endpoint is updated
     * with any non-null parameter values. Parameters left as `null` will not modify the existing endpoint's values.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpsEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new PostgresMcpContainerResourcePromiseImpl(this._withHttpsEndpointInternal(port, targetPort, name, env, isProxied), this._client);
    }
    /** @internal */
    async _withExternalHttpEndpointsInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExternalHttpEndpoints', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Marks existing http or https endpoints on a resource as external.
     * @returns The resource builder.
     */
    withExternalHttpEndpoints() {
        return new PostgresMcpContainerResourcePromiseImpl(this._withExternalHttpEndpointsInternal(), this._client);
    }
    /**
     * Gets an endpoint reference
     * @param name The name of the endpoint.
     * @returns An `EndpointReference` that can be used to resolve the address of the endpoint after resource allocation has occurred.
     */
    getEndpoint(name) {
        const promise = (async () => {
            const rpcArgs = { builder: this._handle, name };
            const handle = await this._client.invokeCapability('Aspire.Hosting/getEndpoint', rpcArgs);
            return new EndpointReferenceImpl(handle, this._client);
        })();
        return new EndpointReferencePromiseImpl(promise, this._client);
    }
    /** @internal */
    async _asHttp2ServiceInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/asHttp2Service', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to mark all endpoints' transport as HTTP/2. This is useful for HTTP/2 services that need prior knowledge.
     * @returns The resource builder.
     */
    asHttp2Service() {
        return new PostgresMcpContainerResourcePromiseImpl(this._asHttp2ServiceInternal(), this._client);
    }
    /** @internal */
    async _withUrlsInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new ResourceUrlsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrls', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to customize the URLs displayed for the resource.
     * @param callback The callback that will customize URLs for the resource.
     * @returns The resource builder.
     */
    withUrls(callback) {
        return new PostgresMcpContainerResourcePromiseImpl(this._withUrlsInternal(callback), this._client);
    }
    /** @internal */
    async _withUrlInternal(url, displayText) {
        const rpcArgs = { builder: this._handle, url };
        if (displayText !== undefined)
            rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrl', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Adds or modifies displayed URLs
     * @param options Additional options.
     */
    withUrl(url, options) {
        const displayText = options?.displayText;
        return new PostgresMcpContainerResourcePromiseImpl(this._withUrlInternal(url, displayText), this._client);
    }
    /** @internal */
    async _withUrlForEndpointInternal(endpointName, callback) {
        const callbackId = registerCallback(async (objData) => {
            const obj = wrapIfHandle(objData);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrlForEndpoint', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to update the URL displayed for the endpoint with the specified name.
     * @param endpointName The name of the endpoint to customize the URL for.
     * @param callback The callback that will customize the URL.
     * @returns The resource builder.
     */
    withUrlForEndpoint(endpointName, callback) {
        return new PostgresMcpContainerResourcePromiseImpl(this._withUrlForEndpointInternal(endpointName, callback), this._client);
    }
    /** @internal */
    async _excludeFromManifestInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromManifest', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Excludes a resource from being published to the manifest.
     * @returns The resource builder.
     */
    excludeFromManifest() {
        return new PostgresMcpContainerResourcePromiseImpl(this._excludeFromManifestInternal(), this._client);
    }
    /** @internal */
    async _waitForInternal(dependency, waitBehavior) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (waitBehavior !== undefined)
            rpcArgs.waitBehavior = waitBehavior;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitFor', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Waits for another resource to be ready
     * @param options Additional options.
     */
    waitFor(dependency, options) {
        const waitBehavior = options?.waitBehavior;
        return new PostgresMcpContainerResourcePromiseImpl(this._waitForInternal(dependency, waitBehavior), this._client);
    }
    /** @internal */
    async _waitForStartInternal(dependency, waitBehavior) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (waitBehavior !== undefined)
            rpcArgs.waitBehavior = waitBehavior;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitForStart', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Waits for another resource to start
     * @param options Additional options.
     */
    waitForStart(dependency, options) {
        const waitBehavior = options?.waitBehavior;
        return new PostgresMcpContainerResourcePromiseImpl(this._waitForStartInternal(dependency, waitBehavior), this._client);
    }
    /** @internal */
    async _withExplicitStartInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExplicitStart', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Prevents resource from starting automatically
     * @returns The resource builder.
     */
    withExplicitStart() {
        return new PostgresMcpContainerResourcePromiseImpl(this._withExplicitStartInternal(), this._client);
    }
    /** @internal */
    async _waitForCompletionInternal(dependency, exitCode) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitForResourceCompletion', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Waits for the dependency resource to enter the Exited or Finished state before starting the resource.
     * @param dependency The resource builder for the dependency resource.
     * @param options Additional options.
     * @returns The resource builder.
     */
    waitForCompletion(dependency, options) {
        const exitCode = options?.exitCode;
        return new PostgresMcpContainerResourcePromiseImpl(this._waitForCompletionInternal(dependency, exitCode), this._client);
    }
    /** @internal */
    async _withHealthCheckInternal(key) {
        const rpcArgs = { builder: this._handle, key };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHealthCheck', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a health check by key
     * @param key The key for the health check.
     * @returns The resource builder.
     */
    withHealthCheck(key) {
        return new PostgresMcpContainerResourcePromiseImpl(this._withHealthCheckInternal(key), this._client);
    }
    /** @internal */
    async _withHttpHealthCheckInternal(path, statusCode, endpointName) {
        const rpcArgs = { builder: this._handle };
        if (path !== undefined)
            rpcArgs.path = path;
        if (statusCode !== undefined)
            rpcArgs.statusCode = statusCode;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpHealthCheck', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a health check to the resource which is mapped to a specific endpoint.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpHealthCheck(options) {
        const path = options?.path;
        const statusCode = options?.statusCode;
        const endpointName = options?.endpointName;
        return new PostgresMcpContainerResourcePromiseImpl(this._withHttpHealthCheckInternal(path, statusCode, endpointName), this._client);
    }
    /** @internal */
    async _withCommandInternal(name, displayName, executeCommand, commandOptions) {
        const executeCommandId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined)
            rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability('Aspire.Hosting/withCommand', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a resource command
     *
     * The `WithCommand` method is used to add commands to the resource. Commands are displayed in the dashboard
     * and can be executed by a user using the dashboard UI.
     * When a command is executed, the `executeCommand` callback is called and is run inside the Aspire host.
     * @param name The name of the command. The name uniquely identifies the command.
     * @param displayName The display name visible in UI.
     * @param executeCommand A callback that is executed when the command is executed. The callback is run inside the Aspire host. The callback result is used to indicate success or failure in the UI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withCommand(name, displayName, executeCommand, options) {
        const commandOptions = options?.commandOptions;
        return new PostgresMcpContainerResourcePromiseImpl(this._withCommandInternal(name, displayName, executeCommand, commandOptions), this._client);
    }
    /** @internal */
    async _withProcessCommandInternal(commandName, displayName, options) {
        const rpcArgs = { builder: this._handle, commandName, displayName, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommand', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /** Adds a command to the resource that starts a local process when invoked. */
    withProcessCommand(commandName, displayName, options) {
        return new PostgresMcpContainerResourcePromiseImpl(this._withProcessCommandInternal(commandName, displayName, options), this._client);
    }
    /** @internal */
    async _withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options) {
        const createProcessSpecId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await createProcessSpec(arg);
        });
        const rpcArgs = { builder: this._handle, commandName, displayName, createProcessSpec: createProcessSpecId };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommandFactory', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a command to the resource that starts a local process created by a callback when invoked.
     * @param options Additional options.
     */
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new PostgresMcpContainerResourcePromiseImpl(this._withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options), this._client);
    }
    /** @internal */
    async _withHttpCommandInternal(path, displayName, options) {
        const rpcArgs = { builder: this._handle, path, displayName };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpCommand', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP resource command
     * @param options Additional options.
     */
    withHttpCommand(path, displayName, options) {
        return new PostgresMcpContainerResourcePromiseImpl(this._withHttpCommandInternal(path, displayName, options), this._client);
    }
    /** @internal */
    async _withDeveloperCertificateTrustInternal(trust) {
        const rpcArgs = { builder: this._handle, trust };
        const result = await this._client.invokeCapability('Aspire.Hosting/withDeveloperCertificateTrust', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Indicates whether developer certificates should be treated as trusted certificate authorities for the resource at run time. Currently this indicates trust for the ASP.NET Core developer certificate. The developer certificate will only be trusted when running in local development scenarios; in publish mode resources will use their default certificate trust.
     *
     * Disable trust for app host managed developer certificate(s) for a container resource.
     * ```
     * var container = builder.AddContainer("my-service", "my-service:latest")
     * .WithDeveloperCertificateTrust(false);
     * ```
     * Disable automatic trust for app host managed developer certificate(s), but explicitly enable it for a specific resource.
     * ```
     * var builder = DistributedApplication.CreateBuilder(new DistributedApplicationOptions()
     * {
     * Args = args,
     * TrustDeveloperCertificate = false,
     * });
     * var project = builder.AddProject<MyService>("my-service")
     * .WithDeveloperCertificateTrust(true);
     * ```
     * @param trust Indicates whether the developer certificate should be treated as trusted.
     * @returns The resource builder.
     */
    withDeveloperCertificateTrust(trust) {
        return new PostgresMcpContainerResourcePromiseImpl(this._withDeveloperCertificateTrustInternal(trust), this._client);
    }
    /** @internal */
    async _withCertificateTrustScopeInternal(scope) {
        const rpcArgs = { builder: this._handle, scope };
        const result = await this._client.invokeCapability('Aspire.Hosting/withCertificateTrustScope', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Sets the certificate trust scope
     *
     * The default scope if not overridden is `Append` which means that custom certificate
     * authorities should be appended to the default trusted certificate authorities for the resource. Setting the scope to
     * `Override` indicates the set of certificates in referenced
     * `CertificateAuthorityCollection` (and optionally Aspire developer certificiates) should be used as the
     * exclusive source of trust for a resource.
     * In all cases, this is a best effort implementation as not all resources support full customization of certificate
     * trust.
     * Set the scope for custom certificate authorities to override the default trusted certificate authorities for a container resource.
     * ```
     * var caCollection = builder.AddCertificateAuthorityCollection("my-cas")
     * .WithCertificate(new X509Certificate2("my-ca.pem"));
     * var container = builder.AddContainer("my-service", "my-service:latest")
     * .WithCertificateAuthorityCollection(caCollection)
     * .WithCertificateTrustScope(CertificateTrustScope.Override);
     * ```
     * @param scope The scope to apply to custom certificate authorities associated with the resource.
     * @returns The resource builder.
     */
    withCertificateTrustScope(scope) {
        return new PostgresMcpContainerResourcePromiseImpl(this._withCertificateTrustScopeInternal(scope), this._client);
    }
    /** @internal */
    async _withHttpsDeveloperCertificateInternal(password) {
        password = isPromiseLike(password) ? await password : password;
        const rpcArgs = { builder: this._handle };
        if (password !== undefined)
            rpcArgs.password = password;
        const result = await this._client.invokeCapability('Aspire.Hosting/withParameterHttpsDeveloperCertificate', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Indicates that a resource should use the developer certificate key pair for HTTPS endpoints at run time. Currently this indicates use of the ASP.NET Core developer certificate. The developer certificate will only be used when running in local development scenarios; in publish mode resources will use their default certificate configuration.
     *
     * Use the developer certificate for HTTPS/TLS endpoints on a container resource:
     * ```
     * builder.AddContainer("my-service", "my-image")
     * .WithHttpsDeveloperCertificate()
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpsDeveloperCertificate(options) {
        let password = options?.password;
        return new PostgresMcpContainerResourcePromiseImpl(this._withHttpsDeveloperCertificateInternal(password), this._client);
    }
    /** @internal */
    async _withoutHttpsCertificateInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withoutHttpsCertificate', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Disable HTTPS/TLS server certificate configuration for the resource. No HTTPS/TLS termination configuration will be applied.
     *
     * Disable HTTPS certificate configuration for a Redis resource:
     * ```
     * var redis = builder.AddRedis("cache")
     * .WithoutHttpsCertificate();
     * ```
     * @returns The resource builder.
     */
    withoutHttpsCertificate() {
        return new PostgresMcpContainerResourcePromiseImpl(this._withoutHttpsCertificateInternal(), this._client);
    }
    /** @internal */
    async _withRelationshipInternal(resourceBuilder, type) {
        resourceBuilder = isPromiseLike(resourceBuilder) ? await resourceBuilder : resourceBuilder;
        const rpcArgs = { builder: this._handle, resourceBuilder, type };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderRelationship', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a relationship to another resource using its builder.
     * @param resourceBuilder The resource builder that the relationship is to.
     * @param type The relationship type.
     * @returns A resource builder.
     */
    withRelationship(resourceBuilder, type) {
        return new PostgresMcpContainerResourcePromiseImpl(this._withRelationshipInternal(resourceBuilder, type), this._client);
    }
    /** @internal */
    async _withParentRelationshipInternal(parent) {
        parent = isPromiseLike(parent) ? await parent : parent;
        const rpcArgs = { builder: this._handle, parent };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderParentRelationship', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Sets the parent relationship
     * @param parent The parent of `builder`.
     * @returns A resource builder.
     */
    withParentRelationship(parent) {
        return new PostgresMcpContainerResourcePromiseImpl(this._withParentRelationshipInternal(parent), this._client);
    }
    /** @internal */
    async _withChildRelationshipInternal(child) {
        child = isPromiseLike(child) ? await child : child;
        const rpcArgs = { builder: this._handle, child };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderChildRelationship', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Sets a child relationship
     * @param child The child of `builder`.
     * @returns A resource builder.
     */
    withChildRelationship(child) {
        return new PostgresMcpContainerResourcePromiseImpl(this._withChildRelationshipInternal(child), this._client);
    }
    /** @internal */
    async _withIconNameInternal(iconName, iconVariant) {
        const rpcArgs = { builder: this._handle, iconName };
        if (iconVariant !== undefined)
            rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability('Aspire.Hosting/withIconName', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Specifies the icon to use when displaying the resource in the dashboard.
     * @param iconName The name of the FluentUI icon to use. See https://aka.ms/fluentui-system-icons for available icons.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withIconName(iconName, options) {
        const iconVariant = options?.iconVariant;
        return new PostgresMcpContainerResourcePromiseImpl(this._withIconNameInternal(iconName, iconVariant), this._client);
    }
    /** @internal */
    async _withComputeEnvironmentInternal(computeEnvironmentResource) {
        computeEnvironmentResource = isPromiseLike(computeEnvironmentResource) ? await computeEnvironmentResource : computeEnvironmentResource;
        const rpcArgs = { builder: this._handle, computeEnvironmentResource };
        const result = await this._client.invokeCapability('Aspire.Hosting/withComputeEnvironment', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Configures the compute environment for the compute resource.
     *
     * This method allows associating a specific compute environment with the compute resource.
     * @param computeEnvironmentResource The compute environment resource to associate with the compute resource.
     * @returns The resource builder.
     */
    withComputeEnvironment(computeEnvironmentResource) {
        return new PostgresMcpContainerResourcePromiseImpl(this._withComputeEnvironmentInternal(computeEnvironmentResource), this._client);
    }
    /** @internal */
    async _withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName) {
        const rpcArgs = { builder: this._handle, probeType };
        if (path !== undefined)
            rpcArgs.path = path;
        if (initialDelaySeconds !== undefined)
            rpcArgs.initialDelaySeconds = initialDelaySeconds;
        if (periodSeconds !== undefined)
            rpcArgs.periodSeconds = periodSeconds;
        if (timeoutSeconds !== undefined)
            rpcArgs.timeoutSeconds = timeoutSeconds;
        if (failureThreshold !== undefined)
            rpcArgs.failureThreshold = failureThreshold;
        if (successThreshold !== undefined)
            rpcArgs.successThreshold = successThreshold;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpProbe', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP health probe to the resource
     * @param options Additional options.
     */
    withHttpProbe(probeType, options) {
        const path = options?.path;
        const initialDelaySeconds = options?.initialDelaySeconds;
        const periodSeconds = options?.periodSeconds;
        const timeoutSeconds = options?.timeoutSeconds;
        const failureThreshold = options?.failureThreshold;
        const successThreshold = options?.successThreshold;
        const endpointName = options?.endpointName;
        return new PostgresMcpContainerResourcePromiseImpl(this._withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName), this._client);
    }
    /** @internal */
    async _excludeFromMcpInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromMcp', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Exclude the resource from MCP operations using the Aspire MCP server. The resource is excluded from results that return resources, console logs and telemetry.
     * @returns The resource builder.
     */
    excludeFromMcp() {
        return new PostgresMcpContainerResourcePromiseImpl(this._excludeFromMcpInternal(), this._client);
    }
    /** @internal */
    async _withHiddenInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHidden', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists
     *
     * Use this method to hide resources that are implementation details and should never be displayed by default.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @returns The resource builder.
     */
    withHidden() {
        return new PostgresMcpContainerResourcePromiseImpl(this._withHiddenInternal(), this._client);
    }
    /** @internal */
    async _withHiddenOnCompletionInternal(exitCode, exitCodes) {
        const rpcArgs = { builder: this._handle };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        if (exitCodes !== undefined)
            rpcArgs.exitCodes = exitCodes;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHiddenOnCompletion', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists after successful completion
     *
     * This method is useful for one-off resources such as setup scripts, migrations, or build steps that should remain visible while running
     * and then be hidden after successful completion.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHiddenOnCompletion(options) {
        const exitCode = options?.exitCode;
        const exitCodes = options?.exitCodes;
        return new PostgresMcpContainerResourcePromiseImpl(this._withHiddenOnCompletionInternal(exitCode, exitCodes), this._client);
    }
    /** @internal */
    async _withImagePushOptionsInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ContainerImagePushOptionsCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImagePushOptions', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Adds an asynchronous callback to configure container image push options for the resource.
     *
     * This method allows customization of how container images are named and tagged when pushed to a registry using an asynchronous callback.
     * Use this overload when the callback needs to perform asynchronous operations such as retrieving configuration values from external sources.
     * The callback receives a `ContainerImagePushOptionsCallbackContext` that provides access to the resource
     * and the `ContainerImagePushOptions` that can be modified.
     * Multiple callbacks can be registered on the same resource, and they will be invoked in the order they were added.
     * @param callback The asynchronous callback to configure push options.
     * @returns The resource builder.
     */
    withImagePushOptions(callback) {
        return new PostgresMcpContainerResourcePromiseImpl(this._withImagePushOptionsInternal(callback), this._client);
    }
    /** @internal */
    async _withRemoteImageNameInternal(remoteImageName) {
        const rpcArgs = { builder: this._handle, remoteImageName };
        const result = await this._client.invokeCapability('Aspire.Hosting/withRemoteImageName', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Sets the remote image name (without registry endpoint or tag) for container push operations.
     *
     * Use this with `withRemoteImageTag` to fully customize the image reference used for container push operations.
     * @param remoteImageName The remote image name (e.g., "myapp" or "myorg/myapp").
     * @returns The resource builder.
     */
    withRemoteImageName(remoteImageName) {
        return new PostgresMcpContainerResourcePromiseImpl(this._withRemoteImageNameInternal(remoteImageName), this._client);
    }
    /** @internal */
    async _withRemoteImageTagInternal(remoteImageTag) {
        const rpcArgs = { builder: this._handle, remoteImageTag };
        const result = await this._client.invokeCapability('Aspire.Hosting/withRemoteImageTag', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Sets the remote image tag for container push operations.
     *
     * Use this with `withRemoteImageName` to fully customize the image reference used for container push operations.
     * @param remoteImageTag The remote image tag (e.g., "latest", "v1.0.0").
     * @returns The resource builder.
     */
    withRemoteImageTag(remoteImageTag) {
        return new PostgresMcpContainerResourcePromiseImpl(this._withRemoteImageTagInternal(remoteImageTag), this._client);
    }
    /** @internal */
    async _withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new PipelineStepContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined)
            rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined)
            rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined)
            rpcArgs.tags = tags;
        if (description !== undefined)
            rpcArgs.description = description;
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineStepFactory', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a pipeline step to the resource that will be executed during deployment.
     * @param stepName The unique name of the pipeline step.
     * @param callback The callback to execute when the step runs.
     * @param options Additional options.
     * @returns The resource builder for chaining.
     */
    withPipelineStepFactory(stepName, callback, options) {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new PostgresMcpContainerResourcePromiseImpl(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description), this._client);
    }
    /** @internal */
    async _withPipelineConfigurationInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new PipelineConfigurationContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineConfiguration', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to be executed during the pipeline configuration phase, allowing modification of step dependencies and relationships.
     * @param callback The callback function to execute during the configuration phase.
     * @returns The resource builder for chaining.
     */
    withPipelineConfiguration(callback) {
        return new PostgresMcpContainerResourcePromiseImpl(this._withPipelineConfigurationInternal(callback), this._client);
    }
    /** @internal */
    async _withVolumeInternal(target, name, isReadOnly) {
        const rpcArgs = { resource: this._handle, target };
        if (name !== undefined)
            rpcArgs.name = name;
        if (isReadOnly !== undefined)
            rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability('Aspire.Hosting/withVolume', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Adds a volume to a container resource.
     *
     * Volumes persist data across container restarts. Named volumes are managed
     * by Docker/Podman and stored in a system-managed location.
     * Why this wrapper exists: The original `ContainerResourceBuilderExtensions.WithVolume`
     * has parameter order `(name?, target, isReadOnly)` where the optional `name` comes first.
     * This wrapper reorders parameters to `(target, name?, isReadOnly)` so the required `target`
     * parameter comes first, providing a better API for polyglot consumers.
     * @param target The mount path inside the container.
     * @param options Additional options.
     * @returns The same resource builder handle for chaining.
     */
    withVolume(target, options) {
        const name = options?.name;
        const isReadOnly = options?.isReadOnly;
        return new PostgresMcpContainerResourcePromiseImpl(this._withVolumeInternal(target, name, isReadOnly), this._client);
    }
    /**
     * Gets the name of the resource from a builder.
     *
     * Why this wrapper exists: This capability accesses a nested property
     * (`resource.Resource.Name`) which requires a wrapper method. There is no single
     * .NET method that returns just the resource name that could be annotated directly.
     * @returns The resource name.
     */
    async getResourceName() {
        const rpcArgs = { resource: this._handle };
        return await this._client.invokeCapability('Aspire.Hosting/getResourceName', rpcArgs);
    }
    /** @internal */
    async _onBeforeResourceStartedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new BeforeResourceStartedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onBeforeResourceStarted', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the BeforeResourceStarted event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onBeforeResourceStarted(callback) {
        return new PostgresMcpContainerResourcePromiseImpl(this._onBeforeResourceStartedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceStoppedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceStoppedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceStopped', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceStopped event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceStopped(callback) {
        return new PostgresMcpContainerResourcePromiseImpl(this._onResourceStoppedInternal(callback), this._client);
    }
    /** @internal */
    async _onInitializeResourceInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new InitializeResourceEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onInitializeResource', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the InitializeResource event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onInitializeResource(callback) {
        return new PostgresMcpContainerResourcePromiseImpl(this._onInitializeResourceInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceEndpointsAllocatedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceEndpointsAllocatedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceEndpointsAllocated', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceEndpointsAllocated event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceEndpointsAllocated(callback) {
        return new PostgresMcpContainerResourcePromiseImpl(this._onResourceEndpointsAllocatedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceReadyInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceReadyEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceReady', rpcArgs);
        return new PostgresMcpContainerResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceReady event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceReady(callback) {
        return new PostgresMcpContainerResourcePromiseImpl(this._onResourceReadyInternal(callback), this._client);
    }
    /**
     * Creates an execution configuration builder for the specified resource.
     * @returns The execution configuration builder.
     */
    createExecutionConfiguration() {
        const promise = (async () => {
            const rpcArgs = { resource: this._handle };
            const handle = await this._client.invokeCapability('Aspire.Hosting/createExecutionConfiguration', rpcArgs);
            return new ExecutionConfigurationBuilderImpl(handle, this._client);
        })();
        return new ExecutionConfigurationBuilderPromiseImpl(promise, this._client);
    }
}
/**
 * Thenable wrapper for PostgresMcpContainerResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
class PostgresMcpContainerResourcePromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    withContainerRegistry(registry) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withContainerRegistry(registry)), this._client);
    }
    withBindMount(source, target, options) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withBindMount(source, target, options)), this._client);
    }
    withEntrypoint(entrypoint) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withEntrypoint(entrypoint)), this._client);
    }
    withImageTag(tag) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withImageTag(tag)), this._client);
    }
    withImageRegistry(registry) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withImageRegistry(registry)), this._client);
    }
    withImage(image, options) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withImage(image, options)), this._client);
    }
    withImageSHA256(sha256) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withImageSHA256(sha256)), this._client);
    }
    withContainerRuntimeArgs(args) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withContainerRuntimeArgs(args)), this._client);
    }
    withLifetime(lifetime) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withLifetime(lifetime)), this._client);
    }
    withImagePullPolicy(pullPolicy) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withImagePullPolicy(pullPolicy)), this._client);
    }
    publishAsContainer() {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.publishAsContainer()), this._client);
    }
    withDockerfile(contextPath, options) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withDockerfile(contextPath, options)), this._client);
    }
    withDockerfileFactory(contextPath, dockerfileFactory, options) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileFactory(contextPath, dockerfileFactory, options)), this._client);
    }
    withContainerName(name) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withContainerName(name)), this._client);
    }
    withBuildArg(name, value) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withBuildArg(name, value)), this._client);
    }
    withBuildSecret(name, value) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withBuildSecret(name, value)), this._client);
    }
    withContainerCertificatePaths(options) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withContainerCertificatePaths(options)), this._client);
    }
    withDockerfileBuilder(contextPath, callback, options) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileBuilder(contextPath, callback, options)), this._client);
    }
    withDockerfileBaseImage(options) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileBaseImage(options)), this._client);
    }
    withContainerNetworkAlias(alias) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withContainerNetworkAlias(alias)), this._client);
    }
    withMcpServer(options) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withMcpServer(options)), this._client);
    }
    withOtlpExporter(options) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withOtlpExporter(options)), this._client);
    }
    publishAsConnectionString() {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.publishAsConnectionString()), this._client);
    }
    withRequiredCommand(command, options) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withRequiredCommand(command, options)), this._client);
    }
    withSessionLifetime() {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withSessionLifetime()), this._client);
    }
    withPersistentLifetime() {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withPersistentLifetime()), this._client);
    }
    withLifetimeOf(sourceBuilder) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withLifetimeOf(sourceBuilder)), this._client);
    }
    withParentProcessLifetime(parentProcessId) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withParentProcessLifetime(parentProcessId)), this._client);
    }
    withEnvironment(name, value) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withEnvironment(name, value)), this._client);
    }
    withEnvironmentCallback(callback) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withEnvironmentCallback(callback)), this._client);
    }
    withArgs(args) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withArgs(args)), this._client);
    }
    withArgsCallback(callback) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withArgsCallback(callback)), this._client);
    }
    withReferenceEnvironment(options) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withReferenceEnvironment(options)), this._client);
    }
    withReference(source, options) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withReference(source, options)), this._client);
    }
    withEndpointCallback(endpointName, callback, options) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withEndpointCallback(endpointName, callback, options)), this._client);
    }
    withHttpEndpointCallback(callback, options) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withHttpEndpointCallback(callback, options)), this._client);
    }
    withHttpsEndpointCallback(callback, options) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withHttpsEndpointCallback(callback, options)), this._client);
    }
    withEndpoint(options) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withEndpoint(options)), this._client);
    }
    withEndpointProxySupport(proxyEnabled) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withEndpointProxySupport(proxyEnabled)), this._client);
    }
    withHttpEndpoint(options) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withHttpEndpoint(options)), this._client);
    }
    withHttpsEndpoint(options) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withHttpsEndpoint(options)), this._client);
    }
    withExternalHttpEndpoints() {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withExternalHttpEndpoints()), this._client);
    }
    getEndpoint(name) {
        return new EndpointReferencePromiseImpl(this._promise.then(obj => obj.getEndpoint(name)), this._client);
    }
    asHttp2Service() {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.asHttp2Service()), this._client);
    }
    withUrls(callback) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withUrls(callback)), this._client);
    }
    withUrl(url, options) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withUrl(url, options)), this._client);
    }
    withUrlForEndpoint(endpointName, callback) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)), this._client);
    }
    excludeFromManifest() {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.excludeFromManifest()), this._client);
    }
    waitFor(dependency, options) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.waitFor(dependency, options)), this._client);
    }
    waitForStart(dependency, options) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.waitForStart(dependency, options)), this._client);
    }
    withExplicitStart() {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withExplicitStart()), this._client);
    }
    waitForCompletion(dependency, options) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.waitForCompletion(dependency, options)), this._client);
    }
    withHealthCheck(key) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withHealthCheck(key)), this._client);
    }
    withHttpHealthCheck(options) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withHttpHealthCheck(options)), this._client);
    }
    withCommand(name, displayName, executeCommand, options) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)), this._client);
    }
    withProcessCommand(commandName, displayName, options) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommand(commandName, displayName, options)), this._client);
    }
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommandFactory(commandName, displayName, createProcessSpec, options)), this._client);
    }
    withHttpCommand(path, displayName, options) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withHttpCommand(path, displayName, options)), this._client);
    }
    withDeveloperCertificateTrust(trust) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withDeveloperCertificateTrust(trust)), this._client);
    }
    withCertificateTrustScope(scope) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withCertificateTrustScope(scope)), this._client);
    }
    withHttpsDeveloperCertificate(options) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withHttpsDeveloperCertificate(options)), this._client);
    }
    withoutHttpsCertificate() {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withoutHttpsCertificate()), this._client);
    }
    withRelationship(resourceBuilder, type) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withRelationship(resourceBuilder, type)), this._client);
    }
    withParentRelationship(parent) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withParentRelationship(parent)), this._client);
    }
    withChildRelationship(child) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withChildRelationship(child)), this._client);
    }
    withIconName(iconName, options) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withIconName(iconName, options)), this._client);
    }
    withComputeEnvironment(computeEnvironmentResource) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withComputeEnvironment(computeEnvironmentResource)), this._client);
    }
    withHttpProbe(probeType, options) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withHttpProbe(probeType, options)), this._client);
    }
    excludeFromMcp() {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.excludeFromMcp()), this._client);
    }
    withHidden() {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withHidden()), this._client);
    }
    withHiddenOnCompletion(options) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withHiddenOnCompletion(options)), this._client);
    }
    withImagePushOptions(callback) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withImagePushOptions(callback)), this._client);
    }
    withRemoteImageName(remoteImageName) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withRemoteImageName(remoteImageName)), this._client);
    }
    withRemoteImageTag(remoteImageTag) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withRemoteImageTag(remoteImageTag)), this._client);
    }
    withPipelineStepFactory(stepName, callback, options) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)), this._client);
    }
    withPipelineConfiguration(callback) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withPipelineConfiguration(callback)), this._client);
    }
    withVolume(target, options) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.withVolume(target, options)), this._client);
    }
    getResourceName() {
        return this._promise.then(obj => obj.getResourceName());
    }
    onBeforeResourceStarted(callback) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.onBeforeResourceStarted(callback)), this._client);
    }
    onResourceStopped(callback) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.onResourceStopped(callback)), this._client);
    }
    onInitializeResource(callback) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.onInitializeResource(callback)), this._client);
    }
    onResourceEndpointsAllocated(callback) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.onResourceEndpointsAllocated(callback)), this._client);
    }
    onResourceReady(callback) {
        return new PostgresMcpContainerResourcePromiseImpl(this._promise.then(obj => obj.onResourceReady(callback)), this._client);
    }
    createExecutionConfiguration() {
        return new ExecutionConfigurationBuilderPromiseImpl(this._promise.then(obj => obj.createExecutionConfiguration()), this._client);
    }
}
// ============================================================================
// PostgresServerResourceImpl
// ============================================================================
/** A resource that represents a PostgreSQL container. */
class PostgresServerResourceImpl extends ResourceBuilderBase {
    constructor(handle, client) {
        super(handle, client);
    }
    primaryEndpoint() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/PostgresServerResource.primaryEndpoint', { context: this._handle });
            return new EndpointReferenceImpl(handle, this._client);
        })();
        return new EndpointReferencePromiseImpl(promise, this._client, false);
    }
    userNameParameter = {
        get: () => {
            const promise = (async () => {
                const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/PostgresServerResource.userNameParameter', { context: this._handle });
                return new ParameterResourceImpl(handle, this._client);
            })();
            return new ParameterResourcePromiseImpl(promise, this._client, false);
        },
        set: async (value) => {
            value = isPromiseLike(value) ? await value : value;
            await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/PostgresServerResource.setUserNameParameter', { context: this._handle, value });
        }
    };
    async userNameReference() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/PostgresServerResource.userNameReference', { context: this._handle });
    }
    passwordParameter = {
        get: () => {
            const promise = (async () => {
                const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/PostgresServerResource.passwordParameter', { context: this._handle });
                return new ParameterResourceImpl(handle, this._client);
            })();
            return new ParameterResourcePromiseImpl(promise, this._client, false);
        },
        set: async (value) => {
            value = isPromiseLike(value) ? await value : value;
            await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/PostgresServerResource.setPasswordParameter', { context: this._handle, value });
        }
    };
    async connectionStringExpression() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/PostgresServerResource.connectionStringExpression', { context: this._handle });
    }
    _databases;
    async databases() {
        if (!this._databases) {
            this._databases = new AspireDict(this._handle, this._client, 'Aspire.Hosting.ApplicationModel/PostgresServerResource.databases', 'Aspire.Hosting.ApplicationModel/PostgresServerResource.databases');
        }
        return this._databases;
    }
    host() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/PostgresServerResource.host', { context: this._handle });
            return new EndpointReferenceExpressionImpl(handle, this._client);
        })();
        return new EndpointReferenceExpressionPromiseImpl(promise, this._client, false);
    }
    port() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/PostgresServerResource.port', { context: this._handle });
            return new EndpointReferenceExpressionImpl(handle, this._client);
        })();
        return new EndpointReferenceExpressionPromiseImpl(promise, this._client, false);
    }
    async uriExpression() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/PostgresServerResource.uriExpression', { context: this._handle });
    }
    async jdbcConnectionString() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/PostgresServerResource.jdbcConnectionString', { context: this._handle });
    }
    entrypoint = {
        get: async () => {
            return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/PostgresServerResource.entrypoint', { context: this._handle });
        },
        set: async (value) => {
            await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/PostgresServerResource.setEntrypoint', { context: this._handle, value });
        }
    };
    shellExecution = {
        get: async () => {
            return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/PostgresServerResource.shellExecution', { context: this._handle });
        },
        set: async (value) => {
            await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/PostgresServerResource.setShellExecution', { context: this._handle, value });
        }
    };
    async name() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/PostgresServerResource.name', { context: this._handle });
    }
    /** @internal */
    async _withContainerRegistryInternal(registry) {
        registry = isPromiseLike(registry) ? await registry : registry;
        const rpcArgs = { builder: this._handle, registry };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerRegistry', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Configures the resource to use the specified container registry for container image operations.
     *
     * This method adds a `ContainerRegistryReferenceAnnotation` to the resource,
     * indicating that the resource should use the specified container registry for container image operations.
     * @param registry The container registry resource builder.
     * @returns The resource builder for chaining.
     */
    withContainerRegistry(registry) {
        return new PostgresServerResourcePromiseImpl(this._withContainerRegistryInternal(registry), this._client);
    }
    /** @internal */
    async _withBindMountInternal(source, target, isReadOnly) {
        const rpcArgs = { builder: this._handle, source, target };
        if (isReadOnly !== undefined)
            rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability('Aspire.Hosting/withBindMount', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Adds a bind mount to a container resource.
     * @param source The source path of the mount. This is the path to the file or directory on the host, relative to the app host project directory.
     * @param target The target path where the file or directory is mounted in the container.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withBindMount(source, target, options) {
        const isReadOnly = options?.isReadOnly;
        return new PostgresServerResourcePromiseImpl(this._withBindMountInternal(source, target, isReadOnly), this._client);
    }
    /** @internal */
    async _withEntrypointInternal(entrypoint) {
        const rpcArgs = { builder: this._handle, entrypoint };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEntrypoint', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Sets the Entrypoint for the container.
     * @param entrypoint The new entrypoint for the container.
     * @returns The resource builder.
     */
    withEntrypoint(entrypoint) {
        return new PostgresServerResourcePromiseImpl(this._withEntrypointInternal(entrypoint), this._client);
    }
    /** @internal */
    async _withImageTagInternal(tag) {
        const rpcArgs = { builder: this._handle, tag };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImageTag', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Allows overriding the image tag on a container.
     * @param tag Tag value.
     * @returns The resource builder.
     */
    withImageTag(tag) {
        return new PostgresServerResourcePromiseImpl(this._withImageTagInternal(tag), this._client);
    }
    /** @internal */
    async _withImageRegistryInternal(registry) {
        const rpcArgs = { builder: this._handle, registry };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImageRegistry', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Allows overriding the image registry on a container.
     * @param registry Registry value.
     * @returns The resource builder.
     */
    withImageRegistry(registry) {
        return new PostgresServerResourcePromiseImpl(this._withImageRegistryInternal(registry), this._client);
    }
    /** @internal */
    async _withImageInternal(image, tag) {
        const rpcArgs = { builder: this._handle, image };
        if (tag !== undefined)
            rpcArgs.tag = tag;
        const result = await this._client.invokeCapability('Aspire.Hosting/withImage', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Allows overriding the image on a container.
     * @param image Image value.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withImage(image, options) {
        const tag = options?.tag;
        return new PostgresServerResourcePromiseImpl(this._withImageInternal(image, tag), this._client);
    }
    /** @internal */
    async _withImageSHA256Internal(sha256) {
        const rpcArgs = { builder: this._handle, sha256 };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImageSHA256', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Allows setting the image to a specific sha256 on a container.
     * @param sha256 Registry value.
     * @returns The resource builder.
     */
    withImageSHA256(sha256) {
        return new PostgresServerResourcePromiseImpl(this._withImageSHA256Internal(sha256), this._client);
    }
    /** @internal */
    async _withContainerRuntimeArgsInternal(args) {
        const rpcArgs = { builder: this._handle, args };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerRuntimeArgs', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Adds a callback to be executed with a list of arguments to add to the container runtime run command when a container resource is started.
     * @param args The arguments to be passed to the container runtime run command when the container resource is started.
     * @returns The resource builder.
     */
    withContainerRuntimeArgs(args) {
        return new PostgresServerResourcePromiseImpl(this._withContainerRuntimeArgsInternal(args), this._client);
    }
    /** @internal */
    async _withLifetimeInternal(lifetime) {
        const rpcArgs = { builder: this._handle, lifetime };
        const result = await this._client.invokeCapability('Aspire.Hosting/withLifetime', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Sets the lifetime behavior of the container resource.
     *
     * Prefer `WithPersistentLifetime``1` or
     * `WithSessionLifetime``1` for new code.
     * Marking a container resource to have a `Persistent` lifetime.
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddContainer("mycontainer", "myimage")
     * .WithPersistentLifetime();
     * builder.Build().Run();
     * ```
     * @param lifetime The lifetime behavior of the container resource. The default behavior is `Session`.
     * @returns The resource builder.
     */
    withLifetime(lifetime) {
        return new PostgresServerResourcePromiseImpl(this._withLifetimeInternal(lifetime), this._client);
    }
    /** @internal */
    async _withImagePullPolicyInternal(pullPolicy) {
        const rpcArgs = { builder: this._handle, pullPolicy };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImagePullPolicy', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Sets the pull policy for the container resource.
     * @param pullPolicy The pull policy behavior for the container resource.
     * @returns The resource builder.
     */
    withImagePullPolicy(pullPolicy) {
        return new PostgresServerResourcePromiseImpl(this._withImagePullPolicyInternal(pullPolicy), this._client);
    }
    /** @internal */
    async _publishAsContainerInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/publishAsContainer', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Changes the resource to be published as a container in the manifest.
     * @returns The resource builder.
     */
    publishAsContainer() {
        return new PostgresServerResourcePromiseImpl(this._publishAsContainerInternal(), this._client);
    }
    /** @internal */
    async _withDockerfileInternal(contextPath, dockerfilePath, stage) {
        const rpcArgs = { builder: this._handle, contextPath };
        if (dockerfilePath !== undefined)
            rpcArgs.dockerfilePath = dockerfilePath;
        if (stage !== undefined)
            rpcArgs.stage = stage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfile', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Causes Aspire to build the specified container image from a Dockerfile.
     * @param contextPath Path to be used as the context for the container image build.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfile(contextPath, options) {
        const dockerfilePath = options?.dockerfilePath;
        const stage = options?.stage;
        return new PostgresServerResourcePromiseImpl(this._withDockerfileInternal(contextPath, dockerfilePath, stage), this._client);
    }
    /** @internal */
    async _withDockerfileFactoryInternal(contextPath, dockerfileFactory, stage) {
        const dockerfileFactoryId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new DockerfileFactoryContextImpl(argHandle, this._client);
            return await dockerfileFactory(arg);
        });
        const rpcArgs = { builder: this._handle, contextPath, dockerfileFactory: dockerfileFactoryId };
        if (stage !== undefined)
            rpcArgs.stage = stage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileFactory', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Builds the specified container image from a Dockerfile generated by an asynchronous factory function.
     *
     * When this method is called, an annotation is added to the `ContainerResource` that specifies the context path
     * and a factory function that generates Dockerfile content. The factory is invoked at build time to produce the Dockerfile,
     * which is then written to a temporary file and used by the orchestrator to build the container image.
     * The `contextPath` is relative to the AppHost directory unless it is fully qualified.
     * The factory function is invoked once during the build process to generate the Dockerfile content.
     * The output is trusted and not validated.
     * Creates a container called `mycontainer` with a dynamically generated Dockerfile.
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddContainer("mycontainer", "myimage")
     * .WithDockerfileFactory("path/to/context", async context =>
     * {
     * var template = await File.ReadAllTextAsync("template.dockerfile", context.CancellationToken);
     * return template.Replace("{{VERSION}}", "1.0");
     * });
     * builder.Build().Run();
     * ```
     * @param contextPath Path to be used as the context for the container image build.
     * @param dockerfileFactory An asynchronous function that returns the Dockerfile content as a string.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileFactory(contextPath, dockerfileFactory, options) {
        const stage = options?.stage;
        return new PostgresServerResourcePromiseImpl(this._withDockerfileFactoryInternal(contextPath, dockerfileFactory, stage), this._client);
    }
    /** @internal */
    async _withContainerNameInternal(name) {
        const rpcArgs = { builder: this._handle, name };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerName', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Overrides the default container name for this resource. By default Aspire generates a unique container name based on the resource name and a random postfix (or a postfix based on a hash of the AppHost project path for persistent container resources). This method allows you to override that behavior with a custom name, but could lead to naming conflicts if the specified name is not unique.
     *
     * Combining this with `Persistent` will allow Aspire to re-use an existing container that was not
     * created by an Aspire AppHost.
     * @param name The desired container name. Must be a valid container name or your runtime will report an error.
     * @returns The resource builder.
     */
    withContainerName(name) {
        return new PostgresServerResourcePromiseImpl(this._withContainerNameInternal(name), this._client);
    }
    /** @internal */
    async _withBuildArgInternal(name, value) {
        value = isPromiseLike(value) ? await value : value;
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuildArg', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Adds a build argument when the container is built from a Dockerfile.
     * @param name The name of the build argument.
     * @param value The build argument value, either a string or a parameter resource.
     * @returns The resource builder.
     */
    withBuildArg(name, value) {
        return new PostgresServerResourcePromiseImpl(this._withBuildArgInternal(name, value), this._client);
    }
    /** @internal */
    async _withBuildSecretInternal(name, value) {
        value = isPromiseLike(value) ? await value : value;
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withParameterBuildSecret', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Adds a secret build argument when the container is built from a Dockerfile.
     * @param name The name of the secret build argument.
     * @param value The resource builder for a parameter resource.
     * @returns The resource builder.
     */
    withBuildSecret(name, value) {
        return new PostgresServerResourcePromiseImpl(this._withBuildSecretInternal(name, value), this._client);
    }
    /** @internal */
    async _withContainerCertificatePathsInternal(customCertificatesDestination, defaultCertificateBundlePaths, defaultCertificateDirectoryPaths) {
        const rpcArgs = { builder: this._handle };
        if (customCertificatesDestination !== undefined)
            rpcArgs.customCertificatesDestination = customCertificatesDestination;
        if (defaultCertificateBundlePaths !== undefined)
            rpcArgs.defaultCertificateBundlePaths = defaultCertificateBundlePaths;
        if (defaultCertificateDirectoryPaths !== undefined)
            rpcArgs.defaultCertificateDirectoryPaths = defaultCertificateDirectoryPaths;
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerCertificatePaths', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Adds container certificate path overrides used for certificate trust at run time.
     * @param options Additional options.
     * @returns The updated resource builder.
     */
    withContainerCertificatePaths(options) {
        const customCertificatesDestination = options?.customCertificatesDestination;
        const defaultCertificateBundlePaths = options?.defaultCertificateBundlePaths;
        const defaultCertificateDirectoryPaths = options?.defaultCertificateDirectoryPaths;
        return new PostgresServerResourcePromiseImpl(this._withContainerCertificatePathsInternal(customCertificatesDestination, defaultCertificateBundlePaths, defaultCertificateDirectoryPaths), this._client);
    }
    /** @internal */
    async _withDockerfileBuilderInternal(contextPath, callback, stage) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new DockerfileBuilderCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, contextPath, callback: callbackId };
        if (stage !== undefined)
            rpcArgs.stage = stage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileBuilder', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Configures the resource to use a programmatically generated Dockerfile
     *
     * This method provides a programmatic way to build Dockerfiles using the `DockerfileBuilder` API
     * instead of string manipulation. Callbacks can be composed by calling this method multiple times - each callback will be invoked
     * in order to build up the final Dockerfile.
     * The `contextPath` is relative to the AppHost directory unless it is fully qualified.
     * Creates a container with a programmatically built Dockerfile using fluent API:
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddContainer("mycontainer", "myimage")
     * .WithDockerfileBuilder("path/to/context", context =>
     * {
     * context.Builder.From("alpine:latest")
     * .WorkDir("/app")
     * .Run("apk add curl")
     * .Copy(".", ".")
     * .Cmd(["./myapp"]);
     * return Task.CompletedTask;
     * });
     * builder.Build().Run();
     * ```
     * @param contextPath Path to be used as the context for the container image build.
     * @param callback A callback that uses the `DockerfileBuilder` API to construct the Dockerfile.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileBuilder(contextPath, callback, options) {
        const stage = options?.stage;
        return new PostgresServerResourcePromiseImpl(this._withDockerfileBuilderInternal(contextPath, callback, stage), this._client);
    }
    /** @internal */
    async _withDockerfileBaseImageInternal(buildImage, runtimeImage) {
        const rpcArgs = { builder: this._handle };
        if (buildImage !== undefined)
            rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined)
            rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileBaseImage', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Configures custom base images for generated Dockerfiles.
     *
     * This extension method allows customization of the base images used in generated Dockerfiles.
     * For multi-stage Dockerfiles (e.g., Python with UV), you can specify separate build and runtime images.
     * Specify custom base images for a Python application:
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddPythonApp("myapp", "path/to/app", "main.py")
     * .WithDockerfileBaseImage(
     * buildImage: "ghcr.io/astral-sh/uv:python3.12-bookworm-slim",
     * runtimeImage: "python:3.12-slim-bookworm");
     * builder.Build().Run();
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileBaseImage(options) {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new PostgresServerResourcePromiseImpl(this._withDockerfileBaseImageInternal(buildImage, runtimeImage), this._client);
    }
    /** @internal */
    async _withContainerNetworkAliasInternal(alias) {
        const rpcArgs = { builder: this._handle, alias };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerNetworkAlias', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Adds a network alias to container resource.
     *
     * Network aliases enable DNS resolution of the container on the network by custom names.
     * By default, containers are accessible on the network using their resource name as a DNS alias.
     * This method allows adding additional aliases for the same container.
     * Multiple aliases can be added by calling this method multiple times.
     * @param alias The network alias for the container.
     * @returns The resource builder.
     */
    withContainerNetworkAlias(alias) {
        return new PostgresServerResourcePromiseImpl(this._withContainerNetworkAliasInternal(alias), this._client);
    }
    /** @internal */
    async _withMcpServerInternal(path, endpointName) {
        const rpcArgs = { builder: this._handle };
        if (path !== undefined)
            rpcArgs.path = path;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withMcpServer', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Marks the resource as hosting a Model Context Protocol (MCP) server on the specified endpoint.
     *
     * This method adds an `McpServerEndpointAnnotation` to the resource, enabling the Aspire tooling
     * to discover and proxy the MCP server exposed by the resource.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withMcpServer(options) {
        const path = options?.path;
        const endpointName = options?.endpointName;
        return new PostgresServerResourcePromiseImpl(this._withMcpServerInternal(path, endpointName), this._client);
    }
    /** @internal */
    async _withOtlpExporterInternal(protocol) {
        const rpcArgs = { builder: this._handle };
        if (protocol !== undefined)
            rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability('Aspire.Hosting/withOtlpExporter', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Configures OTLP telemetry export
     * @param options Additional options.
     */
    withOtlpExporter(options) {
        const protocol = options?.protocol;
        return new PostgresServerResourcePromiseImpl(this._withOtlpExporterInternal(protocol), this._client);
    }
    /** @internal */
    async _publishAsConnectionStringInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/publishAsConnectionString', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Changes the resource to be published as a connection string reference in the manifest.
     * @returns The resource builder.
     */
    publishAsConnectionString() {
        return new PostgresServerResourcePromiseImpl(this._publishAsConnectionStringInternal(), this._client);
    }
    /** @internal */
    async _withRequiredCommandInternal(command, helpLink) {
        const rpcArgs = { builder: this._handle, command };
        if (helpLink !== undefined)
            rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability('Aspire.Hosting/withRequiredCommand', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Declares that a resource requires a specific command/executable to be available on the local machine PATH before it can start.
     *
     * The command is considered valid if either:
     * 1. It is an absolute or relative path (contains a directory separator) that points to an existing file, or
     * 2. It is discoverable on the current process PATH (respecting PATHEXT on Windows).
     * If the command is not found, a warning message will be logged but the resource will be allowed to attempt to start.
     * @param command The command string (file name or path) that should be validated.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withRequiredCommand(command, options) {
        const helpLink = options?.helpLink;
        return new PostgresServerResourcePromiseImpl(this._withRequiredCommandInternal(command, helpLink), this._client);
    }
    /** @internal */
    async _withSessionLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withSessionLifetime', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a session lifetime.
     * @returns The resource builder.
     */
    withSessionLifetime() {
        return new PostgresServerResourcePromiseImpl(this._withSessionLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withPersistentLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPersistentLifetime', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime.
     * @returns The resource builder.
     */
    withPersistentLifetime() {
        return new PostgresServerResourcePromiseImpl(this._withPersistentLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withLifetimeOfInternal(sourceBuilder) {
        sourceBuilder = isPromiseLike(sourceBuilder) ? await sourceBuilder : sourceBuilder;
        const rpcArgs = { builder: this._handle, sourceBuilder };
        const result = await this._client.invokeCapability('Aspire.Hosting/withLifetimeOf', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to match the lifetime of another resource.
     *
     * The resource lifetime is evaluated from `sourceBuilder` when the application model is prepared, so later lifetime
     * changes to the source resource are reflected by this resource.
     * @param sourceBuilder The resource builder whose lifetime should be used.
     * @returns The resource builder.
     */
    withLifetimeOf(sourceBuilder) {
        return new PostgresServerResourcePromiseImpl(this._withLifetimeOfInternal(sourceBuilder), this._client);
    }
    /** @internal */
    async _withParentProcessLifetimeInternal(parentProcessId) {
        const rpcArgs = { builder: this._handle, parentProcessId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withParentProcessLifetime', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime that ends when a parent process exits.
     * @param parentProcessId The ID of the parent process to monitor.
     * @returns The resource builder.
     */
    withParentProcessLifetime(parentProcessId) {
        return new PostgresServerResourcePromiseImpl(this._withParentProcessLifetimeInternal(parentProcessId), this._client);
    }
    /** @internal */
    async _withEnvironmentInternal(name, value) {
        value = isPromiseLike(value) ? await value : value;
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEnvironment', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /** Sets an environment variable */
    withEnvironment(name, value) {
        return new PostgresServerResourcePromiseImpl(this._withEnvironmentInternal(name, value), this._client);
    }
    /** @internal */
    async _withEnvironmentCallbackInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new EnvironmentCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEnvironmentCallback', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Allows for the population of environment variables on a resource.
     * @param callback A callback that allows for deferred execution for computing many environment variables. This runs after resources have been allocated by the orchestrator and allows access to other resources to resolve computed data, e.g. connection strings, ports.
     * @returns The resource builder.
     */
    withEnvironmentCallback(callback) {
        return new PostgresServerResourcePromiseImpl(this._withEnvironmentCallbackInternal(callback), this._client);
    }
    /** @internal */
    async _withConnectionPropertyInternal(name, value) {
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withConnectionProperty', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Adds a connection property annotation to the resource being built.
     * @param name The name of the connection property to add.
     * @param value The value to assign to the connection property, specified as a string or reference expression.
     * @returns The same resource builder instance with the specified connection property annotation applied.
     */
    withConnectionProperty(name, value) {
        return new PostgresServerResourcePromiseImpl(this._withConnectionPropertyInternal(name, value), this._client);
    }
    /** @internal */
    async _withArgsInternal(args) {
        const rpcArgs = { builder: this._handle, args };
        const result = await this._client.invokeCapability('Aspire.Hosting/withArgs', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Adds arguments to be passed to a resource that supports arguments when it is launched.
     * @param args The arguments to be passed to the resource when it is started.
     * @returns The resource builder.
     */
    withArgs(args) {
        return new PostgresServerResourcePromiseImpl(this._withArgsInternal(args), this._client);
    }
    /** @internal */
    async _withArgsCallbackInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new CommandLineArgsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withArgsCallback', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Adds a callback to be executed with a list of command-line arguments when a resource is started.
     * @param callback A callback that allows for deferred execution for computing arguments. This runs after resources have been allocated by the orchestrator and allows access to other resources to resolve computed data, e.g. connection strings, ports.
     * @returns The resource builder.
     */
    withArgsCallback(callback) {
        return new PostgresServerResourcePromiseImpl(this._withArgsCallbackInternal(callback), this._client);
    }
    /** @internal */
    async _withReferenceEnvironmentInternal(options) {
        const rpcArgs = { builder: this._handle, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withReferenceEnvironment', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Configures how information is injected into environment variables when the resource references other resources.
     * @param options Options controlling which reference information is emitted.
     * @returns The resource builder.
     */
    withReferenceEnvironment(options) {
        return new PostgresServerResourcePromiseImpl(this._withReferenceEnvironmentInternal(options), this._client);
    }
    /** @internal */
    async _withReferenceInternal(source, connectionName, optional, name) {
        source = isPromiseLike(source) ? await source : source;
        const rpcArgs = { builder: this._handle, source };
        if (connectionName !== undefined)
            rpcArgs.connectionName = connectionName;
        if (optional !== undefined)
            rpcArgs.optional = optional;
        if (name !== undefined)
            rpcArgs.name = name;
        const result = await this._client.invokeCapability('Aspire.Hosting/withReference', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Adds a reference to another resource
     * @param options Additional options.
     */
    withReference(source, options) {
        const connectionName = options?.connectionName;
        const optional = options?.optional;
        const name = options?.name;
        return new PostgresServerResourcePromiseImpl(this._withReferenceInternal(source, connectionName, optional, name), this._client);
    }
    /**
     * Retrieves the value of a specified connection property from the resource's connection properties.
     *
     * Throws a KeyNotFoundException if the specified key does not exist in the resource's
     * connection properties.
     * @param key The key of the connection property to retrieve. Cannot be null.
     * @returns The value associated with the specified connection property key.
     */
    async getConnectionProperty(key) {
        const rpcArgs = { resource: this._handle, key };
        return await this._client.invokeCapability('Aspire.Hosting/getConnectionProperty', rpcArgs);
    }
    /** @internal */
    async _withEndpointCallbackInternal(endpointName, callback, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpointCallback', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Updates a named endpoint via callback
     * @param options Additional options.
     */
    withEndpointCallback(endpointName, callback, options) {
        const createIfNotExists = options?.createIfNotExists;
        return new PostgresServerResourcePromiseImpl(this._withEndpointCallbackInternal(endpointName, callback, createIfNotExists), this._client);
    }
    /** @internal */
    async _withHttpEndpointCallbackInternal(callback, name, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        if (name !== undefined)
            rpcArgs.name = name;
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpEndpointCallback', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Updates an HTTP endpoint via callback
     * @param options Additional options.
     */
    withHttpEndpointCallback(callback, options) {
        const name = options?.name;
        const createIfNotExists = options?.createIfNotExists;
        return new PostgresServerResourcePromiseImpl(this._withHttpEndpointCallbackInternal(callback, name, createIfNotExists), this._client);
    }
    /** @internal */
    async _withHttpsEndpointCallbackInternal(callback, name, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        if (name !== undefined)
            rpcArgs.name = name;
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpsEndpointCallback', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Updates an HTTPS endpoint via callback
     * @param options Additional options.
     */
    withHttpsEndpointCallback(callback, options) {
        const name = options?.name;
        const createIfNotExists = options?.createIfNotExists;
        return new PostgresServerResourcePromiseImpl(this._withHttpsEndpointCallbackInternal(callback, name, createIfNotExists), this._client);
    }
    /** @internal */
    async _withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (scheme !== undefined)
            rpcArgs.scheme = scheme;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        if (isExternal !== undefined)
            rpcArgs.isExternal = isExternal;
        if (protocol !== undefined)
            rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpoint', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Adds a network endpoint
     * @param options Additional options.
     * @returns The resource builder.
     */
    withEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const scheme = options?.scheme;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        const isExternal = options?.isExternal;
        const protocol = options?.protocol;
        return new PostgresServerResourcePromiseImpl(this._withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol), this._client);
    }
    /** @internal */
    async _withEndpointProxySupportInternal(proxyEnabled) {
        const rpcArgs = { builder: this._handle, proxyEnabled };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpointProxySupport', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Set whether a resource can use proxied endpoints or whether they should be disabled for all endpoints belonging to the resource. If set to `false`, endpoints belonging to the resource will ignore the configured proxy settings and run proxy-less.
     *
     * This method is intended to support scenarios with persistent lifetime resources where it is desirable for the resource to be accessible over the same
     * port whether the Aspire application is running or not. Proxied endpoints bind ports that are only accessible while the Aspire application is running.
     * The user needs to be careful to ensure that endpoints are using unique ports when disabling proxy support as by default for proxy-less
     * endpoints, Aspire will allocate the target port as the host port, which will increase the chance of port conflicts.
     * @param proxyEnabled Should endpoints for the resource support using a proxy?
     * @returns The resource builder.
     */
    withEndpointProxySupport(proxyEnabled) {
        return new PostgresServerResourcePromiseImpl(this._withEndpointProxySupportInternal(proxyEnabled), this._client);
    }
    /** @internal */
    async _withHttpEndpointInternal(port, targetPort, name, env, isProxied) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpEndpoint', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP endpoint
     *
     * If an endpoint with the same name already exists on the resource, the existing endpoint is updated
     * with any non-null parameter values. Parameters left as `null` will not modify the existing endpoint's values.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new PostgresServerResourcePromiseImpl(this._withHttpEndpointInternal(port, targetPort, name, env, isProxied), this._client);
    }
    /** @internal */
    async _withHttpsEndpointInternal(port, targetPort, name, env, isProxied) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpsEndpoint', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTPS endpoint
     *
     * If an endpoint with the same name already exists on the resource, the existing endpoint is updated
     * with any non-null parameter values. Parameters left as `null` will not modify the existing endpoint's values.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpsEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new PostgresServerResourcePromiseImpl(this._withHttpsEndpointInternal(port, targetPort, name, env, isProxied), this._client);
    }
    /** @internal */
    async _withExternalHttpEndpointsInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExternalHttpEndpoints', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Marks existing http or https endpoints on a resource as external.
     * @returns The resource builder.
     */
    withExternalHttpEndpoints() {
        return new PostgresServerResourcePromiseImpl(this._withExternalHttpEndpointsInternal(), this._client);
    }
    /**
     * Gets an endpoint reference
     * @param name The name of the endpoint.
     * @returns An `EndpointReference` that can be used to resolve the address of the endpoint after resource allocation has occurred.
     */
    getEndpoint(name) {
        const promise = (async () => {
            const rpcArgs = { builder: this._handle, name };
            const handle = await this._client.invokeCapability('Aspire.Hosting/getEndpoint', rpcArgs);
            return new EndpointReferenceImpl(handle, this._client);
        })();
        return new EndpointReferencePromiseImpl(promise, this._client);
    }
    /** @internal */
    async _asHttp2ServiceInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/asHttp2Service', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to mark all endpoints' transport as HTTP/2. This is useful for HTTP/2 services that need prior knowledge.
     * @returns The resource builder.
     */
    asHttp2Service() {
        return new PostgresServerResourcePromiseImpl(this._asHttp2ServiceInternal(), this._client);
    }
    /** @internal */
    async _withUrlsInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new ResourceUrlsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrls', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to customize the URLs displayed for the resource.
     * @param callback The callback that will customize URLs for the resource.
     * @returns The resource builder.
     */
    withUrls(callback) {
        return new PostgresServerResourcePromiseImpl(this._withUrlsInternal(callback), this._client);
    }
    /** @internal */
    async _withUrlInternal(url, displayText) {
        const rpcArgs = { builder: this._handle, url };
        if (displayText !== undefined)
            rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrl', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Adds or modifies displayed URLs
     * @param options Additional options.
     */
    withUrl(url, options) {
        const displayText = options?.displayText;
        return new PostgresServerResourcePromiseImpl(this._withUrlInternal(url, displayText), this._client);
    }
    /** @internal */
    async _withUrlForEndpointInternal(endpointName, callback) {
        const callbackId = registerCallback(async (objData) => {
            const obj = wrapIfHandle(objData);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrlForEndpoint', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to update the URL displayed for the endpoint with the specified name.
     * @param endpointName The name of the endpoint to customize the URL for.
     * @param callback The callback that will customize the URL.
     * @returns The resource builder.
     */
    withUrlForEndpoint(endpointName, callback) {
        return new PostgresServerResourcePromiseImpl(this._withUrlForEndpointInternal(endpointName, callback), this._client);
    }
    /** @internal */
    async _excludeFromManifestInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromManifest', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Excludes a resource from being published to the manifest.
     * @returns The resource builder.
     */
    excludeFromManifest() {
        return new PostgresServerResourcePromiseImpl(this._excludeFromManifestInternal(), this._client);
    }
    /** @internal */
    async _waitForInternal(dependency, waitBehavior) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (waitBehavior !== undefined)
            rpcArgs.waitBehavior = waitBehavior;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitFor', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Waits for another resource to be ready
     * @param options Additional options.
     */
    waitFor(dependency, options) {
        const waitBehavior = options?.waitBehavior;
        return new PostgresServerResourcePromiseImpl(this._waitForInternal(dependency, waitBehavior), this._client);
    }
    /** @internal */
    async _waitForStartInternal(dependency, waitBehavior) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (waitBehavior !== undefined)
            rpcArgs.waitBehavior = waitBehavior;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitForStart', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Waits for another resource to start
     * @param options Additional options.
     */
    waitForStart(dependency, options) {
        const waitBehavior = options?.waitBehavior;
        return new PostgresServerResourcePromiseImpl(this._waitForStartInternal(dependency, waitBehavior), this._client);
    }
    /** @internal */
    async _withExplicitStartInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExplicitStart', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Prevents resource from starting automatically
     * @returns The resource builder.
     */
    withExplicitStart() {
        return new PostgresServerResourcePromiseImpl(this._withExplicitStartInternal(), this._client);
    }
    /** @internal */
    async _waitForCompletionInternal(dependency, exitCode) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitForResourceCompletion', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Waits for the dependency resource to enter the Exited or Finished state before starting the resource.
     * @param dependency The resource builder for the dependency resource.
     * @param options Additional options.
     * @returns The resource builder.
     */
    waitForCompletion(dependency, options) {
        const exitCode = options?.exitCode;
        return new PostgresServerResourcePromiseImpl(this._waitForCompletionInternal(dependency, exitCode), this._client);
    }
    /** @internal */
    async _withHealthCheckInternal(key) {
        const rpcArgs = { builder: this._handle, key };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHealthCheck', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Adds a health check by key
     * @param key The key for the health check.
     * @returns The resource builder.
     */
    withHealthCheck(key) {
        return new PostgresServerResourcePromiseImpl(this._withHealthCheckInternal(key), this._client);
    }
    /** @internal */
    async _withHttpHealthCheckInternal(path, statusCode, endpointName) {
        const rpcArgs = { builder: this._handle };
        if (path !== undefined)
            rpcArgs.path = path;
        if (statusCode !== undefined)
            rpcArgs.statusCode = statusCode;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpHealthCheck', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Adds a health check to the resource which is mapped to a specific endpoint.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpHealthCheck(options) {
        const path = options?.path;
        const statusCode = options?.statusCode;
        const endpointName = options?.endpointName;
        return new PostgresServerResourcePromiseImpl(this._withHttpHealthCheckInternal(path, statusCode, endpointName), this._client);
    }
    /** @internal */
    async _withCommandInternal(name, displayName, executeCommand, commandOptions) {
        const executeCommandId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined)
            rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability('Aspire.Hosting/withCommand', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Adds a resource command
     *
     * The `WithCommand` method is used to add commands to the resource. Commands are displayed in the dashboard
     * and can be executed by a user using the dashboard UI.
     * When a command is executed, the `executeCommand` callback is called and is run inside the Aspire host.
     * @param name The name of the command. The name uniquely identifies the command.
     * @param displayName The display name visible in UI.
     * @param executeCommand A callback that is executed when the command is executed. The callback is run inside the Aspire host. The callback result is used to indicate success or failure in the UI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withCommand(name, displayName, executeCommand, options) {
        const commandOptions = options?.commandOptions;
        return new PostgresServerResourcePromiseImpl(this._withCommandInternal(name, displayName, executeCommand, commandOptions), this._client);
    }
    /** @internal */
    async _withProcessCommandInternal(commandName, displayName, options) {
        const rpcArgs = { builder: this._handle, commandName, displayName, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommand', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /** Adds a command to the resource that starts a local process when invoked. */
    withProcessCommand(commandName, displayName, options) {
        return new PostgresServerResourcePromiseImpl(this._withProcessCommandInternal(commandName, displayName, options), this._client);
    }
    /** @internal */
    async _withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options) {
        const createProcessSpecId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await createProcessSpec(arg);
        });
        const rpcArgs = { builder: this._handle, commandName, displayName, createProcessSpec: createProcessSpecId };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommandFactory', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Adds a command to the resource that starts a local process created by a callback when invoked.
     * @param options Additional options.
     */
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new PostgresServerResourcePromiseImpl(this._withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options), this._client);
    }
    /** @internal */
    async _withHttpCommandInternal(path, displayName, options) {
        const rpcArgs = { builder: this._handle, path, displayName };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpCommand', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP resource command
     * @param options Additional options.
     */
    withHttpCommand(path, displayName, options) {
        return new PostgresServerResourcePromiseImpl(this._withHttpCommandInternal(path, displayName, options), this._client);
    }
    /** @internal */
    async _withDeveloperCertificateTrustInternal(trust) {
        const rpcArgs = { builder: this._handle, trust };
        const result = await this._client.invokeCapability('Aspire.Hosting/withDeveloperCertificateTrust', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Indicates whether developer certificates should be treated as trusted certificate authorities for the resource at run time. Currently this indicates trust for the ASP.NET Core developer certificate. The developer certificate will only be trusted when running in local development scenarios; in publish mode resources will use their default certificate trust.
     *
     * Disable trust for app host managed developer certificate(s) for a container resource.
     * ```
     * var container = builder.AddContainer("my-service", "my-service:latest")
     * .WithDeveloperCertificateTrust(false);
     * ```
     * Disable automatic trust for app host managed developer certificate(s), but explicitly enable it for a specific resource.
     * ```
     * var builder = DistributedApplication.CreateBuilder(new DistributedApplicationOptions()
     * {
     * Args = args,
     * TrustDeveloperCertificate = false,
     * });
     * var project = builder.AddProject<MyService>("my-service")
     * .WithDeveloperCertificateTrust(true);
     * ```
     * @param trust Indicates whether the developer certificate should be treated as trusted.
     * @returns The resource builder.
     */
    withDeveloperCertificateTrust(trust) {
        return new PostgresServerResourcePromiseImpl(this._withDeveloperCertificateTrustInternal(trust), this._client);
    }
    /** @internal */
    async _withCertificateTrustScopeInternal(scope) {
        const rpcArgs = { builder: this._handle, scope };
        const result = await this._client.invokeCapability('Aspire.Hosting/withCertificateTrustScope', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Sets the certificate trust scope
     *
     * The default scope if not overridden is `Append` which means that custom certificate
     * authorities should be appended to the default trusted certificate authorities for the resource. Setting the scope to
     * `Override` indicates the set of certificates in referenced
     * `CertificateAuthorityCollection` (and optionally Aspire developer certificiates) should be used as the
     * exclusive source of trust for a resource.
     * In all cases, this is a best effort implementation as not all resources support full customization of certificate
     * trust.
     * Set the scope for custom certificate authorities to override the default trusted certificate authorities for a container resource.
     * ```
     * var caCollection = builder.AddCertificateAuthorityCollection("my-cas")
     * .WithCertificate(new X509Certificate2("my-ca.pem"));
     * var container = builder.AddContainer("my-service", "my-service:latest")
     * .WithCertificateAuthorityCollection(caCollection)
     * .WithCertificateTrustScope(CertificateTrustScope.Override);
     * ```
     * @param scope The scope to apply to custom certificate authorities associated with the resource.
     * @returns The resource builder.
     */
    withCertificateTrustScope(scope) {
        return new PostgresServerResourcePromiseImpl(this._withCertificateTrustScopeInternal(scope), this._client);
    }
    /** @internal */
    async _withHttpsDeveloperCertificateInternal(password) {
        password = isPromiseLike(password) ? await password : password;
        const rpcArgs = { builder: this._handle };
        if (password !== undefined)
            rpcArgs.password = password;
        const result = await this._client.invokeCapability('Aspire.Hosting/withParameterHttpsDeveloperCertificate', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Indicates that a resource should use the developer certificate key pair for HTTPS endpoints at run time. Currently this indicates use of the ASP.NET Core developer certificate. The developer certificate will only be used when running in local development scenarios; in publish mode resources will use their default certificate configuration.
     *
     * Use the developer certificate for HTTPS/TLS endpoints on a container resource:
     * ```
     * builder.AddContainer("my-service", "my-image")
     * .WithHttpsDeveloperCertificate()
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpsDeveloperCertificate(options) {
        let password = options?.password;
        return new PostgresServerResourcePromiseImpl(this._withHttpsDeveloperCertificateInternal(password), this._client);
    }
    /** @internal */
    async _withoutHttpsCertificateInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withoutHttpsCertificate', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Disable HTTPS/TLS server certificate configuration for the resource. No HTTPS/TLS termination configuration will be applied.
     *
     * Disable HTTPS certificate configuration for a Redis resource:
     * ```
     * var redis = builder.AddRedis("cache")
     * .WithoutHttpsCertificate();
     * ```
     * @returns The resource builder.
     */
    withoutHttpsCertificate() {
        return new PostgresServerResourcePromiseImpl(this._withoutHttpsCertificateInternal(), this._client);
    }
    /** @internal */
    async _withRelationshipInternal(resourceBuilder, type) {
        resourceBuilder = isPromiseLike(resourceBuilder) ? await resourceBuilder : resourceBuilder;
        const rpcArgs = { builder: this._handle, resourceBuilder, type };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderRelationship', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Adds a relationship to another resource using its builder.
     * @param resourceBuilder The resource builder that the relationship is to.
     * @param type The relationship type.
     * @returns A resource builder.
     */
    withRelationship(resourceBuilder, type) {
        return new PostgresServerResourcePromiseImpl(this._withRelationshipInternal(resourceBuilder, type), this._client);
    }
    /** @internal */
    async _withParentRelationshipInternal(parent) {
        parent = isPromiseLike(parent) ? await parent : parent;
        const rpcArgs = { builder: this._handle, parent };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderParentRelationship', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Sets the parent relationship
     * @param parent The parent of `builder`.
     * @returns A resource builder.
     */
    withParentRelationship(parent) {
        return new PostgresServerResourcePromiseImpl(this._withParentRelationshipInternal(parent), this._client);
    }
    /** @internal */
    async _withChildRelationshipInternal(child) {
        child = isPromiseLike(child) ? await child : child;
        const rpcArgs = { builder: this._handle, child };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderChildRelationship', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Sets a child relationship
     * @param child The child of `builder`.
     * @returns A resource builder.
     */
    withChildRelationship(child) {
        return new PostgresServerResourcePromiseImpl(this._withChildRelationshipInternal(child), this._client);
    }
    /** @internal */
    async _withIconNameInternal(iconName, iconVariant) {
        const rpcArgs = { builder: this._handle, iconName };
        if (iconVariant !== undefined)
            rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability('Aspire.Hosting/withIconName', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Specifies the icon to use when displaying the resource in the dashboard.
     * @param iconName The name of the FluentUI icon to use. See https://aka.ms/fluentui-system-icons for available icons.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withIconName(iconName, options) {
        const iconVariant = options?.iconVariant;
        return new PostgresServerResourcePromiseImpl(this._withIconNameInternal(iconName, iconVariant), this._client);
    }
    /** @internal */
    async _withComputeEnvironmentInternal(computeEnvironmentResource) {
        computeEnvironmentResource = isPromiseLike(computeEnvironmentResource) ? await computeEnvironmentResource : computeEnvironmentResource;
        const rpcArgs = { builder: this._handle, computeEnvironmentResource };
        const result = await this._client.invokeCapability('Aspire.Hosting/withComputeEnvironment', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Configures the compute environment for the compute resource.
     *
     * This method allows associating a specific compute environment with the compute resource.
     * @param computeEnvironmentResource The compute environment resource to associate with the compute resource.
     * @returns The resource builder.
     */
    withComputeEnvironment(computeEnvironmentResource) {
        return new PostgresServerResourcePromiseImpl(this._withComputeEnvironmentInternal(computeEnvironmentResource), this._client);
    }
    /** @internal */
    async _withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName) {
        const rpcArgs = { builder: this._handle, probeType };
        if (path !== undefined)
            rpcArgs.path = path;
        if (initialDelaySeconds !== undefined)
            rpcArgs.initialDelaySeconds = initialDelaySeconds;
        if (periodSeconds !== undefined)
            rpcArgs.periodSeconds = periodSeconds;
        if (timeoutSeconds !== undefined)
            rpcArgs.timeoutSeconds = timeoutSeconds;
        if (failureThreshold !== undefined)
            rpcArgs.failureThreshold = failureThreshold;
        if (successThreshold !== undefined)
            rpcArgs.successThreshold = successThreshold;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpProbe', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP health probe to the resource
     * @param options Additional options.
     */
    withHttpProbe(probeType, options) {
        const path = options?.path;
        const initialDelaySeconds = options?.initialDelaySeconds;
        const periodSeconds = options?.periodSeconds;
        const timeoutSeconds = options?.timeoutSeconds;
        const failureThreshold = options?.failureThreshold;
        const successThreshold = options?.successThreshold;
        const endpointName = options?.endpointName;
        return new PostgresServerResourcePromiseImpl(this._withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName), this._client);
    }
    /** @internal */
    async _excludeFromMcpInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromMcp', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Exclude the resource from MCP operations using the Aspire MCP server. The resource is excluded from results that return resources, console logs and telemetry.
     * @returns The resource builder.
     */
    excludeFromMcp() {
        return new PostgresServerResourcePromiseImpl(this._excludeFromMcpInternal(), this._client);
    }
    /** @internal */
    async _withHiddenInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHidden', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists
     *
     * Use this method to hide resources that are implementation details and should never be displayed by default.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @returns The resource builder.
     */
    withHidden() {
        return new PostgresServerResourcePromiseImpl(this._withHiddenInternal(), this._client);
    }
    /** @internal */
    async _withHiddenOnCompletionInternal(exitCode, exitCodes) {
        const rpcArgs = { builder: this._handle };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        if (exitCodes !== undefined)
            rpcArgs.exitCodes = exitCodes;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHiddenOnCompletion', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists after successful completion
     *
     * This method is useful for one-off resources such as setup scripts, migrations, or build steps that should remain visible while running
     * and then be hidden after successful completion.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHiddenOnCompletion(options) {
        const exitCode = options?.exitCode;
        const exitCodes = options?.exitCodes;
        return new PostgresServerResourcePromiseImpl(this._withHiddenOnCompletionInternal(exitCode, exitCodes), this._client);
    }
    /** @internal */
    async _withImagePushOptionsInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ContainerImagePushOptionsCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImagePushOptions', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Adds an asynchronous callback to configure container image push options for the resource.
     *
     * This method allows customization of how container images are named and tagged when pushed to a registry using an asynchronous callback.
     * Use this overload when the callback needs to perform asynchronous operations such as retrieving configuration values from external sources.
     * The callback receives a `ContainerImagePushOptionsCallbackContext` that provides access to the resource
     * and the `ContainerImagePushOptions` that can be modified.
     * Multiple callbacks can be registered on the same resource, and they will be invoked in the order they were added.
     * @param callback The asynchronous callback to configure push options.
     * @returns The resource builder.
     */
    withImagePushOptions(callback) {
        return new PostgresServerResourcePromiseImpl(this._withImagePushOptionsInternal(callback), this._client);
    }
    /** @internal */
    async _withRemoteImageNameInternal(remoteImageName) {
        const rpcArgs = { builder: this._handle, remoteImageName };
        const result = await this._client.invokeCapability('Aspire.Hosting/withRemoteImageName', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Sets the remote image name (without registry endpoint or tag) for container push operations.
     *
     * Use this with `withRemoteImageTag` to fully customize the image reference used for container push operations.
     * @param remoteImageName The remote image name (e.g., "myapp" or "myorg/myapp").
     * @returns The resource builder.
     */
    withRemoteImageName(remoteImageName) {
        return new PostgresServerResourcePromiseImpl(this._withRemoteImageNameInternal(remoteImageName), this._client);
    }
    /** @internal */
    async _withRemoteImageTagInternal(remoteImageTag) {
        const rpcArgs = { builder: this._handle, remoteImageTag };
        const result = await this._client.invokeCapability('Aspire.Hosting/withRemoteImageTag', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Sets the remote image tag for container push operations.
     *
     * Use this with `withRemoteImageName` to fully customize the image reference used for container push operations.
     * @param remoteImageTag The remote image tag (e.g., "latest", "v1.0.0").
     * @returns The resource builder.
     */
    withRemoteImageTag(remoteImageTag) {
        return new PostgresServerResourcePromiseImpl(this._withRemoteImageTagInternal(remoteImageTag), this._client);
    }
    /** @internal */
    async _withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new PipelineStepContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined)
            rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined)
            rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined)
            rpcArgs.tags = tags;
        if (description !== undefined)
            rpcArgs.description = description;
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineStepFactory', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Adds a pipeline step to the resource that will be executed during deployment.
     * @param stepName The unique name of the pipeline step.
     * @param callback The callback to execute when the step runs.
     * @param options Additional options.
     * @returns The resource builder for chaining.
     */
    withPipelineStepFactory(stepName, callback, options) {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new PostgresServerResourcePromiseImpl(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description), this._client);
    }
    /** @internal */
    async _withPipelineConfigurationInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new PipelineConfigurationContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineConfiguration', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to be executed during the pipeline configuration phase, allowing modification of step dependencies and relationships.
     * @param callback The callback function to execute during the configuration phase.
     * @returns The resource builder for chaining.
     */
    withPipelineConfiguration(callback) {
        return new PostgresServerResourcePromiseImpl(this._withPipelineConfigurationInternal(callback), this._client);
    }
    /** @internal */
    async _withVolumeInternal(target, name, isReadOnly) {
        const rpcArgs = { resource: this._handle, target };
        if (name !== undefined)
            rpcArgs.name = name;
        if (isReadOnly !== undefined)
            rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability('Aspire.Hosting/withVolume', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Adds a volume to a container resource.
     *
     * Volumes persist data across container restarts. Named volumes are managed
     * by Docker/Podman and stored in a system-managed location.
     * Why this wrapper exists: The original `ContainerResourceBuilderExtensions.WithVolume`
     * has parameter order `(name?, target, isReadOnly)` where the optional `name` comes first.
     * This wrapper reorders parameters to `(target, name?, isReadOnly)` so the required `target`
     * parameter comes first, providing a better API for polyglot consumers.
     * @param target The mount path inside the container.
     * @param options Additional options.
     * @returns The same resource builder handle for chaining.
     */
    withVolume(target, options) {
        const name = options?.name;
        const isReadOnly = options?.isReadOnly;
        return new PostgresServerResourcePromiseImpl(this._withVolumeInternal(target, name, isReadOnly), this._client);
    }
    /**
     * Gets the name of the resource from a builder.
     *
     * Why this wrapper exists: This capability accesses a nested property
     * (`resource.Resource.Name`) which requires a wrapper method. There is no single
     * .NET method that returns just the resource name that could be annotated directly.
     * @returns The resource name.
     */
    async getResourceName() {
        const rpcArgs = { resource: this._handle };
        return await this._client.invokeCapability('Aspire.Hosting/getResourceName', rpcArgs);
    }
    /** @internal */
    async _onBeforeResourceStartedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new BeforeResourceStartedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onBeforeResourceStarted', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the BeforeResourceStarted event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onBeforeResourceStarted(callback) {
        return new PostgresServerResourcePromiseImpl(this._onBeforeResourceStartedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceStoppedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceStoppedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceStopped', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceStopped event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceStopped(callback) {
        return new PostgresServerResourcePromiseImpl(this._onResourceStoppedInternal(callback), this._client);
    }
    /** @internal */
    async _onConnectionStringAvailableInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ConnectionStringAvailableEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onConnectionStringAvailable', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ConnectionStringAvailable event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onConnectionStringAvailable(callback) {
        return new PostgresServerResourcePromiseImpl(this._onConnectionStringAvailableInternal(callback), this._client);
    }
    /** @internal */
    async _onInitializeResourceInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new InitializeResourceEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onInitializeResource', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the InitializeResource event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onInitializeResource(callback) {
        return new PostgresServerResourcePromiseImpl(this._onInitializeResourceInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceEndpointsAllocatedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceEndpointsAllocatedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceEndpointsAllocated', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceEndpointsAllocated event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceEndpointsAllocated(callback) {
        return new PostgresServerResourcePromiseImpl(this._onResourceEndpointsAllocatedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceReadyInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceReadyEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceReady', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceReady event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceReady(callback) {
        return new PostgresServerResourcePromiseImpl(this._onResourceReadyInternal(callback), this._client);
    }
    /**
     * Creates an execution configuration builder for the specified resource.
     * @returns The execution configuration builder.
     */
    createExecutionConfiguration() {
        const promise = (async () => {
            const rpcArgs = { resource: this._handle };
            const handle = await this._client.invokeCapability('Aspire.Hosting/createExecutionConfiguration', rpcArgs);
            return new ExecutionConfigurationBuilderImpl(handle, this._client);
        })();
        return new ExecutionConfigurationBuilderPromiseImpl(promise, this._client);
    }
    /** @internal */
    async _addDatabaseInternal(name, databaseName) {
        const rpcArgs = { builder: this._handle, name };
        if (databaseName !== undefined)
            rpcArgs.databaseName = databaseName;
        const result = await this._client.invokeCapability('Aspire.Hosting.PostgreSQL/addDatabase', rpcArgs);
        return new PostgresDatabaseResourceImpl(result, this._client);
    }
    /**
     * Adds a PostgreSQL database to the application model.
     * @param name The name of the resource. This name will be used as the connection string name when referenced in a dependency.
     * @param options Additional options.
     * @returns The resource builder.
     */
    addDatabase(name, options) {
        const databaseName = options?.databaseName;
        return new PostgresDatabaseResourcePromiseImpl(this._addDatabaseInternal(name, databaseName), this._client);
    }
    /** @internal */
    async _withPgAdminInternal(configureContainer, containerName) {
        const configureContainerId = configureContainer ? registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new PgAdminContainerResourceImpl(objHandle, this._client);
            await configureContainer(obj);
        }) : undefined;
        const rpcArgs = { builder: this._handle };
        if (configureContainer !== undefined)
            rpcArgs.configureContainer = configureContainerId;
        if (containerName !== undefined)
            rpcArgs.containerName = containerName;
        const result = await this._client.invokeCapability('Aspire.Hosting.PostgreSQL/withPgAdmin', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Adds a pgAdmin 4 administration and development platform for PostgreSQL to the application model.
     *
     * This version of the package defaults to the tag of the container image.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withPgAdmin(options) {
        const configureContainer = options?.configureContainer;
        const containerName = options?.containerName;
        return new PostgresServerResourcePromiseImpl(this._withPgAdminInternal(configureContainer, containerName), this._client);
    }
    /** @internal */
    async _withPgWebInternal(configureContainer, containerName) {
        const configureContainerId = configureContainer ? registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new PgWebContainerResourceImpl(objHandle, this._client);
            await configureContainer(obj);
        }) : undefined;
        const rpcArgs = { builder: this._handle };
        if (configureContainer !== undefined)
            rpcArgs.configureContainer = configureContainerId;
        if (containerName !== undefined)
            rpcArgs.containerName = containerName;
        const result = await this._client.invokeCapability('Aspire.Hosting.PostgreSQL/withPgWeb', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Adds an administration and development platform for PostgreSQL to the application model using pgweb.
     *
     * This version of the package defaults to the tag of the container image.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withPgWeb(options) {
        const configureContainer = options?.configureContainer;
        const containerName = options?.containerName;
        return new PostgresServerResourcePromiseImpl(this._withPgWebInternal(configureContainer, containerName), this._client);
    }
    /** @internal */
    async _withDataVolumeInternal(name, isReadOnly) {
        const rpcArgs = { builder: this._handle };
        if (name !== undefined)
            rpcArgs.name = name;
        if (isReadOnly !== undefined)
            rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability('Aspire.Hosting.PostgreSQL/withDataVolume', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Adds a named volume for the data folder to a PostgreSQL container resource.
     *
     * The data directory location varies by PostgreSQL version:
     * - PostgreSQL 17 and earlier: `/var/lib/postgresql/data`
     * - PostgreSQL 18 and later: `/var/lib/postgresql`
     * This method automatically selects the correct path based on the configured container image tag.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDataVolume(options) {
        const name = options?.name;
        const isReadOnly = options?.isReadOnly;
        return new PostgresServerResourcePromiseImpl(this._withDataVolumeInternal(name, isReadOnly), this._client);
    }
    /** @internal */
    async _withDataBindMountInternal(source, isReadOnly) {
        const rpcArgs = { builder: this._handle, source };
        if (isReadOnly !== undefined)
            rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability('Aspire.Hosting.PostgreSQL/withDataBindMount', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Adds a bind mount for the data folder to a PostgreSQL container resource.
     *
     * The data directory location varies by PostgreSQL version:
     * - PostgreSQL 17 and earlier: `/var/lib/postgresql/data`
     * - PostgreSQL 18 and later: `/var/lib/postgresql`
     * This method automatically selects the correct path based on the configured container image tag.
     * @param source The source directory on the host to mount into the container.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDataBindMount(source, options) {
        const isReadOnly = options?.isReadOnly;
        return new PostgresServerResourcePromiseImpl(this._withDataBindMountInternal(source, isReadOnly), this._client);
    }
    /** @internal */
    async _withInitFilesInternal(source) {
        const rpcArgs = { builder: this._handle, source };
        const result = await this._client.invokeCapability('Aspire.Hosting.PostgreSQL/withInitFiles', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Copies init files to a PostgreSQL container resource.
     * @param source The source directory or files on the host to copy into the container.
     * @returns The resource builder.
     */
    withInitFiles(source) {
        return new PostgresServerResourcePromiseImpl(this._withInitFilesInternal(source), this._client);
    }
    /** @internal */
    async _withPasswordInternal(password) {
        password = isPromiseLike(password) ? await password : password;
        const rpcArgs = { builder: this._handle, password };
        const result = await this._client.invokeCapability('Aspire.Hosting.PostgreSQL/withPassword', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Configures the password that the PostgreSQL resource is used.
     * @param password The parameter used to provide the password for the PostgreSQL resource.
     * @returns The resource builder.
     */
    withPassword(password) {
        return new PostgresServerResourcePromiseImpl(this._withPasswordInternal(password), this._client);
    }
    /** @internal */
    async _withUserNameInternal(userName) {
        userName = isPromiseLike(userName) ? await userName : userName;
        const rpcArgs = { builder: this._handle, userName };
        const result = await this._client.invokeCapability('Aspire.Hosting.PostgreSQL/withUserName', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Configures the user name that the PostgreSQL resource is used.
     * @param userName The parameter used to provide the user name for the PostgreSQL resource.
     * @returns The resource builder.
     */
    withUserName(userName) {
        return new PostgresServerResourcePromiseImpl(this._withUserNameInternal(userName), this._client);
    }
    /** @internal */
    async _withHostPortInternal(port) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        const result = await this._client.invokeCapability('Aspire.Hosting.PostgreSQL/withPostgresHostPort', rpcArgs);
        return new PostgresServerResourceImpl(result, this._client);
    }
    /**
     * Configures the host port that the PostgreSQL resource is exposed on instead of using randomly assigned port.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHostPort(options) {
        const port = options?.port;
        return new PostgresServerResourcePromiseImpl(this._withHostPortInternal(port), this._client);
    }
}
/**
 * Thenable wrapper for PostgresServerResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
class PostgresServerResourcePromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    primaryEndpoint() {
        return new EndpointReferencePromiseImpl(this._promise.then(obj => obj.primaryEndpoint()), this._client, false);
    }
    userNameReference() {
        return this._promise.then(obj => obj.userNameReference());
    }
    connectionStringExpression() {
        return this._promise.then(obj => obj.connectionStringExpression());
    }
    databases() {
        return this._promise.then(obj => obj.databases());
    }
    host() {
        return new EndpointReferenceExpressionPromiseImpl(this._promise.then(obj => obj.host()), this._client, false);
    }
    port() {
        return new EndpointReferenceExpressionPromiseImpl(this._promise.then(obj => obj.port()), this._client, false);
    }
    uriExpression() {
        return this._promise.then(obj => obj.uriExpression());
    }
    jdbcConnectionString() {
        return this._promise.then(obj => obj.jdbcConnectionString());
    }
    name() {
        return this._promise.then(obj => obj.name());
    }
    withContainerRegistry(registry) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withContainerRegistry(registry)), this._client);
    }
    withBindMount(source, target, options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withBindMount(source, target, options)), this._client);
    }
    withEntrypoint(entrypoint) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withEntrypoint(entrypoint)), this._client);
    }
    withImageTag(tag) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withImageTag(tag)), this._client);
    }
    withImageRegistry(registry) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withImageRegistry(registry)), this._client);
    }
    withImage(image, options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withImage(image, options)), this._client);
    }
    withImageSHA256(sha256) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withImageSHA256(sha256)), this._client);
    }
    withContainerRuntimeArgs(args) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withContainerRuntimeArgs(args)), this._client);
    }
    withLifetime(lifetime) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withLifetime(lifetime)), this._client);
    }
    withImagePullPolicy(pullPolicy) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withImagePullPolicy(pullPolicy)), this._client);
    }
    publishAsContainer() {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.publishAsContainer()), this._client);
    }
    withDockerfile(contextPath, options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withDockerfile(contextPath, options)), this._client);
    }
    withDockerfileFactory(contextPath, dockerfileFactory, options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileFactory(contextPath, dockerfileFactory, options)), this._client);
    }
    withContainerName(name) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withContainerName(name)), this._client);
    }
    withBuildArg(name, value) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withBuildArg(name, value)), this._client);
    }
    withBuildSecret(name, value) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withBuildSecret(name, value)), this._client);
    }
    withContainerCertificatePaths(options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withContainerCertificatePaths(options)), this._client);
    }
    withDockerfileBuilder(contextPath, callback, options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileBuilder(contextPath, callback, options)), this._client);
    }
    withDockerfileBaseImage(options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileBaseImage(options)), this._client);
    }
    withContainerNetworkAlias(alias) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withContainerNetworkAlias(alias)), this._client);
    }
    withMcpServer(options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withMcpServer(options)), this._client);
    }
    withOtlpExporter(options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withOtlpExporter(options)), this._client);
    }
    publishAsConnectionString() {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.publishAsConnectionString()), this._client);
    }
    withRequiredCommand(command, options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withRequiredCommand(command, options)), this._client);
    }
    withSessionLifetime() {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withSessionLifetime()), this._client);
    }
    withPersistentLifetime() {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withPersistentLifetime()), this._client);
    }
    withLifetimeOf(sourceBuilder) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withLifetimeOf(sourceBuilder)), this._client);
    }
    withParentProcessLifetime(parentProcessId) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withParentProcessLifetime(parentProcessId)), this._client);
    }
    withEnvironment(name, value) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withEnvironment(name, value)), this._client);
    }
    withEnvironmentCallback(callback) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withEnvironmentCallback(callback)), this._client);
    }
    withConnectionProperty(name, value) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withConnectionProperty(name, value)), this._client);
    }
    withArgs(args) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withArgs(args)), this._client);
    }
    withArgsCallback(callback) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withArgsCallback(callback)), this._client);
    }
    withReferenceEnvironment(options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withReferenceEnvironment(options)), this._client);
    }
    withReference(source, options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withReference(source, options)), this._client);
    }
    getConnectionProperty(key) {
        return this._promise.then(obj => obj.getConnectionProperty(key));
    }
    withEndpointCallback(endpointName, callback, options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withEndpointCallback(endpointName, callback, options)), this._client);
    }
    withHttpEndpointCallback(callback, options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withHttpEndpointCallback(callback, options)), this._client);
    }
    withHttpsEndpointCallback(callback, options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withHttpsEndpointCallback(callback, options)), this._client);
    }
    withEndpoint(options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withEndpoint(options)), this._client);
    }
    withEndpointProxySupport(proxyEnabled) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withEndpointProxySupport(proxyEnabled)), this._client);
    }
    withHttpEndpoint(options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withHttpEndpoint(options)), this._client);
    }
    withHttpsEndpoint(options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withHttpsEndpoint(options)), this._client);
    }
    withExternalHttpEndpoints() {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withExternalHttpEndpoints()), this._client);
    }
    getEndpoint(name) {
        return new EndpointReferencePromiseImpl(this._promise.then(obj => obj.getEndpoint(name)), this._client);
    }
    asHttp2Service() {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.asHttp2Service()), this._client);
    }
    withUrls(callback) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withUrls(callback)), this._client);
    }
    withUrl(url, options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withUrl(url, options)), this._client);
    }
    withUrlForEndpoint(endpointName, callback) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)), this._client);
    }
    excludeFromManifest() {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.excludeFromManifest()), this._client);
    }
    waitFor(dependency, options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.waitFor(dependency, options)), this._client);
    }
    waitForStart(dependency, options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.waitForStart(dependency, options)), this._client);
    }
    withExplicitStart() {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withExplicitStart()), this._client);
    }
    waitForCompletion(dependency, options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.waitForCompletion(dependency, options)), this._client);
    }
    withHealthCheck(key) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withHealthCheck(key)), this._client);
    }
    withHttpHealthCheck(options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withHttpHealthCheck(options)), this._client);
    }
    withCommand(name, displayName, executeCommand, options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)), this._client);
    }
    withProcessCommand(commandName, displayName, options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommand(commandName, displayName, options)), this._client);
    }
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommandFactory(commandName, displayName, createProcessSpec, options)), this._client);
    }
    withHttpCommand(path, displayName, options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withHttpCommand(path, displayName, options)), this._client);
    }
    withDeveloperCertificateTrust(trust) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withDeveloperCertificateTrust(trust)), this._client);
    }
    withCertificateTrustScope(scope) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withCertificateTrustScope(scope)), this._client);
    }
    withHttpsDeveloperCertificate(options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withHttpsDeveloperCertificate(options)), this._client);
    }
    withoutHttpsCertificate() {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withoutHttpsCertificate()), this._client);
    }
    withRelationship(resourceBuilder, type) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withRelationship(resourceBuilder, type)), this._client);
    }
    withParentRelationship(parent) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withParentRelationship(parent)), this._client);
    }
    withChildRelationship(child) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withChildRelationship(child)), this._client);
    }
    withIconName(iconName, options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withIconName(iconName, options)), this._client);
    }
    withComputeEnvironment(computeEnvironmentResource) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withComputeEnvironment(computeEnvironmentResource)), this._client);
    }
    withHttpProbe(probeType, options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withHttpProbe(probeType, options)), this._client);
    }
    excludeFromMcp() {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.excludeFromMcp()), this._client);
    }
    withHidden() {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withHidden()), this._client);
    }
    withHiddenOnCompletion(options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withHiddenOnCompletion(options)), this._client);
    }
    withImagePushOptions(callback) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withImagePushOptions(callback)), this._client);
    }
    withRemoteImageName(remoteImageName) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withRemoteImageName(remoteImageName)), this._client);
    }
    withRemoteImageTag(remoteImageTag) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withRemoteImageTag(remoteImageTag)), this._client);
    }
    withPipelineStepFactory(stepName, callback, options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)), this._client);
    }
    withPipelineConfiguration(callback) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withPipelineConfiguration(callback)), this._client);
    }
    withVolume(target, options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withVolume(target, options)), this._client);
    }
    getResourceName() {
        return this._promise.then(obj => obj.getResourceName());
    }
    onBeforeResourceStarted(callback) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.onBeforeResourceStarted(callback)), this._client);
    }
    onResourceStopped(callback) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.onResourceStopped(callback)), this._client);
    }
    onConnectionStringAvailable(callback) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.onConnectionStringAvailable(callback)), this._client);
    }
    onInitializeResource(callback) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.onInitializeResource(callback)), this._client);
    }
    onResourceEndpointsAllocated(callback) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.onResourceEndpointsAllocated(callback)), this._client);
    }
    onResourceReady(callback) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.onResourceReady(callback)), this._client);
    }
    createExecutionConfiguration() {
        return new ExecutionConfigurationBuilderPromiseImpl(this._promise.then(obj => obj.createExecutionConfiguration()), this._client);
    }
    addDatabase(name, options) {
        return new PostgresDatabaseResourcePromiseImpl(this._promise.then(obj => obj.addDatabase(name, options)), this._client);
    }
    withPgAdmin(options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withPgAdmin(options)), this._client);
    }
    withPgWeb(options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withPgWeb(options)), this._client);
    }
    withDataVolume(options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withDataVolume(options)), this._client);
    }
    withDataBindMount(source, options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withDataBindMount(source, options)), this._client);
    }
    withInitFiles(source) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withInitFiles(source)), this._client);
    }
    withPassword(password) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withPassword(password)), this._client);
    }
    withUserName(userName) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withUserName(userName)), this._client);
    }
    withHostPort(options) {
        return new PostgresServerResourcePromiseImpl(this._promise.then(obj => obj.withHostPort(options)), this._client);
    }
}
// ============================================================================
// ProjectResourceImpl
// ============================================================================
/** A resource that represents a specified .NET project. */
class ProjectResourceImpl extends ResourceBuilderBase {
    constructor(handle, client) {
        super(handle, client);
    }
    /** @internal */
    async _withContainerRegistryInternal(registry) {
        registry = isPromiseLike(registry) ? await registry : registry;
        const rpcArgs = { builder: this._handle, registry };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerRegistry', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Configures the resource to use the specified container registry for container image operations.
     *
     * This method adds a `ContainerRegistryReferenceAnnotation` to the resource,
     * indicating that the resource should use the specified container registry for container image operations.
     * @param registry The container registry resource builder.
     * @returns The resource builder for chaining.
     */
    withContainerRegistry(registry) {
        return new ProjectResourcePromiseImpl(this._withContainerRegistryInternal(registry), this._client);
    }
    /** @internal */
    async _withDockerfileBaseImageInternal(buildImage, runtimeImage) {
        const rpcArgs = { builder: this._handle };
        if (buildImage !== undefined)
            rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined)
            rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileBaseImage', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Configures custom base images for generated Dockerfiles.
     *
     * This extension method allows customization of the base images used in generated Dockerfiles.
     * For multi-stage Dockerfiles (e.g., Python with UV), you can specify separate build and runtime images.
     * Specify custom base images for a Python application:
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddPythonApp("myapp", "path/to/app", "main.py")
     * .WithDockerfileBaseImage(
     * buildImage: "ghcr.io/astral-sh/uv:python3.12-bookworm-slim",
     * runtimeImage: "python:3.12-slim-bookworm");
     * builder.Build().Run();
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileBaseImage(options) {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new ProjectResourcePromiseImpl(this._withDockerfileBaseImageInternal(buildImage, runtimeImage), this._client);
    }
    /** @internal */
    async _withMcpServerInternal(path, endpointName) {
        const rpcArgs = { builder: this._handle };
        if (path !== undefined)
            rpcArgs.path = path;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withMcpServer', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Marks the resource as hosting a Model Context Protocol (MCP) server on the specified endpoint.
     *
     * This method adds an `McpServerEndpointAnnotation` to the resource, enabling the Aspire tooling
     * to discover and proxy the MCP server exposed by the resource.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withMcpServer(options) {
        const path = options?.path;
        const endpointName = options?.endpointName;
        return new ProjectResourcePromiseImpl(this._withMcpServerInternal(path, endpointName), this._client);
    }
    /** @internal */
    async _withOtlpExporterInternal(protocol) {
        const rpcArgs = { builder: this._handle };
        if (protocol !== undefined)
            rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability('Aspire.Hosting/withOtlpExporter', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Configures OTLP telemetry export
     * @param options Additional options.
     */
    withOtlpExporter(options) {
        const protocol = options?.protocol;
        return new ProjectResourcePromiseImpl(this._withOtlpExporterInternal(protocol), this._client);
    }
    /** @internal */
    async _withReplicasInternal(replicas) {
        const rpcArgs = { builder: this._handle, replicas };
        const result = await this._client.invokeCapability('Aspire.Hosting/withReplicas', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Configures how many replicas of the project should be created for the project.
     * @param replicas The number of replicas.
     * @returns The resource builder.
     */
    withReplicas(replicas) {
        return new ProjectResourcePromiseImpl(this._withReplicasInternal(replicas), this._client);
    }
    /** @internal */
    async _disableForwardedHeadersInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/disableForwardedHeaders', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Configures the project to disable forwarded headers when being published.
     * @returns The resource builder.
     */
    disableForwardedHeaders() {
        return new ProjectResourcePromiseImpl(this._disableForwardedHeadersInternal(), this._client);
    }
    /** @internal */
    async _publishAsDockerFileInternal(configure) {
        const configureId = configure ? registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new ContainerResourceImpl(objHandle, this._client);
            await configure(obj);
        }) : undefined;
        const rpcArgs = { builder: this._handle };
        if (configure !== undefined)
            rpcArgs.configure = configureId;
        const result = await this._client.invokeCapability('Aspire.Hosting/publishProjectAsDockerFileWithConfigure', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Publishes a project as a Docker file with optional container configuration
     *
     * When the executable resource is converted to a container resource, the arguments to the executable
     * are not used. This is because arguments to the project often contain physical paths that are not valid
     * in the container. The container can be set up with the correct arguments using the `configure` action.
     * @param options Additional options.
     * @returns The resource builder.
     */
    publishAsDockerFile(options) {
        const configure = options?.configure;
        return new ProjectResourcePromiseImpl(this._publishAsDockerFileInternal(configure), this._client);
    }
    /** @internal */
    async _withRequiredCommandInternal(command, helpLink) {
        const rpcArgs = { builder: this._handle, command };
        if (helpLink !== undefined)
            rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability('Aspire.Hosting/withRequiredCommand', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Declares that a resource requires a specific command/executable to be available on the local machine PATH before it can start.
     *
     * The command is considered valid if either:
     * 1. It is an absolute or relative path (contains a directory separator) that points to an existing file, or
     * 2. It is discoverable on the current process PATH (respecting PATHEXT on Windows).
     * If the command is not found, a warning message will be logged but the resource will be allowed to attempt to start.
     * @param command The command string (file name or path) that should be validated.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withRequiredCommand(command, options) {
        const helpLink = options?.helpLink;
        return new ProjectResourcePromiseImpl(this._withRequiredCommandInternal(command, helpLink), this._client);
    }
    /** @internal */
    async _withSessionLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withSessionLifetime', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a session lifetime.
     * @returns The resource builder.
     */
    withSessionLifetime() {
        return new ProjectResourcePromiseImpl(this._withSessionLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withPersistentLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPersistentLifetime', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime.
     * @returns The resource builder.
     */
    withPersistentLifetime() {
        return new ProjectResourcePromiseImpl(this._withPersistentLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withLifetimeOfInternal(sourceBuilder) {
        sourceBuilder = isPromiseLike(sourceBuilder) ? await sourceBuilder : sourceBuilder;
        const rpcArgs = { builder: this._handle, sourceBuilder };
        const result = await this._client.invokeCapability('Aspire.Hosting/withLifetimeOf', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to match the lifetime of another resource.
     *
     * The resource lifetime is evaluated from `sourceBuilder` when the application model is prepared, so later lifetime
     * changes to the source resource are reflected by this resource.
     * @param sourceBuilder The resource builder whose lifetime should be used.
     * @returns The resource builder.
     */
    withLifetimeOf(sourceBuilder) {
        return new ProjectResourcePromiseImpl(this._withLifetimeOfInternal(sourceBuilder), this._client);
    }
    /** @internal */
    async _withParentProcessLifetimeInternal(parentProcessId) {
        const rpcArgs = { builder: this._handle, parentProcessId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withParentProcessLifetime', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime that ends when a parent process exits.
     * @param parentProcessId The ID of the parent process to monitor.
     * @returns The resource builder.
     */
    withParentProcessLifetime(parentProcessId) {
        return new ProjectResourcePromiseImpl(this._withParentProcessLifetimeInternal(parentProcessId), this._client);
    }
    /** @internal */
    async _withEnvironmentInternal(name, value) {
        value = isPromiseLike(value) ? await value : value;
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEnvironment', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /** Sets an environment variable */
    withEnvironment(name, value) {
        return new ProjectResourcePromiseImpl(this._withEnvironmentInternal(name, value), this._client);
    }
    /** @internal */
    async _withEnvironmentCallbackInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new EnvironmentCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEnvironmentCallback', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Allows for the population of environment variables on a resource.
     * @param callback A callback that allows for deferred execution for computing many environment variables. This runs after resources have been allocated by the orchestrator and allows access to other resources to resolve computed data, e.g. connection strings, ports.
     * @returns The resource builder.
     */
    withEnvironmentCallback(callback) {
        return new ProjectResourcePromiseImpl(this._withEnvironmentCallbackInternal(callback), this._client);
    }
    /** @internal */
    async _withArgsInternal(args) {
        const rpcArgs = { builder: this._handle, args };
        const result = await this._client.invokeCapability('Aspire.Hosting/withArgs', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Adds arguments to be passed to a resource that supports arguments when it is launched.
     * @param args The arguments to be passed to the resource when it is started.
     * @returns The resource builder.
     */
    withArgs(args) {
        return new ProjectResourcePromiseImpl(this._withArgsInternal(args), this._client);
    }
    /** @internal */
    async _withArgsCallbackInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new CommandLineArgsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withArgsCallback', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Adds a callback to be executed with a list of command-line arguments when a resource is started.
     * @param callback A callback that allows for deferred execution for computing arguments. This runs after resources have been allocated by the orchestrator and allows access to other resources to resolve computed data, e.g. connection strings, ports.
     * @returns The resource builder.
     */
    withArgsCallback(callback) {
        return new ProjectResourcePromiseImpl(this._withArgsCallbackInternal(callback), this._client);
    }
    /** @internal */
    async _withReferenceEnvironmentInternal(options) {
        const rpcArgs = { builder: this._handle, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withReferenceEnvironment', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Configures how information is injected into environment variables when the resource references other resources.
     * @param options Options controlling which reference information is emitted.
     * @returns The resource builder.
     */
    withReferenceEnvironment(options) {
        return new ProjectResourcePromiseImpl(this._withReferenceEnvironmentInternal(options), this._client);
    }
    /** @internal */
    async _withReferenceInternal(source, connectionName, optional, name) {
        source = isPromiseLike(source) ? await source : source;
        const rpcArgs = { builder: this._handle, source };
        if (connectionName !== undefined)
            rpcArgs.connectionName = connectionName;
        if (optional !== undefined)
            rpcArgs.optional = optional;
        if (name !== undefined)
            rpcArgs.name = name;
        const result = await this._client.invokeCapability('Aspire.Hosting/withReference', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Adds a reference to another resource
     * @param options Additional options.
     */
    withReference(source, options) {
        const connectionName = options?.connectionName;
        const optional = options?.optional;
        const name = options?.name;
        return new ProjectResourcePromiseImpl(this._withReferenceInternal(source, connectionName, optional, name), this._client);
    }
    /** @internal */
    async _withEndpointCallbackInternal(endpointName, callback, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpointCallback', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Updates a named endpoint via callback
     * @param options Additional options.
     */
    withEndpointCallback(endpointName, callback, options) {
        const createIfNotExists = options?.createIfNotExists;
        return new ProjectResourcePromiseImpl(this._withEndpointCallbackInternal(endpointName, callback, createIfNotExists), this._client);
    }
    /** @internal */
    async _withHttpEndpointCallbackInternal(callback, name, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        if (name !== undefined)
            rpcArgs.name = name;
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpEndpointCallback', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Updates an HTTP endpoint via callback
     * @param options Additional options.
     */
    withHttpEndpointCallback(callback, options) {
        const name = options?.name;
        const createIfNotExists = options?.createIfNotExists;
        return new ProjectResourcePromiseImpl(this._withHttpEndpointCallbackInternal(callback, name, createIfNotExists), this._client);
    }
    /** @internal */
    async _withHttpsEndpointCallbackInternal(callback, name, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        if (name !== undefined)
            rpcArgs.name = name;
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpsEndpointCallback', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Updates an HTTPS endpoint via callback
     * @param options Additional options.
     */
    withHttpsEndpointCallback(callback, options) {
        const name = options?.name;
        const createIfNotExists = options?.createIfNotExists;
        return new ProjectResourcePromiseImpl(this._withHttpsEndpointCallbackInternal(callback, name, createIfNotExists), this._client);
    }
    /** @internal */
    async _withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (scheme !== undefined)
            rpcArgs.scheme = scheme;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        if (isExternal !== undefined)
            rpcArgs.isExternal = isExternal;
        if (protocol !== undefined)
            rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpoint', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Adds a network endpoint
     * @param options Additional options.
     * @returns The resource builder.
     */
    withEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const scheme = options?.scheme;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        const isExternal = options?.isExternal;
        const protocol = options?.protocol;
        return new ProjectResourcePromiseImpl(this._withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol), this._client);
    }
    /** @internal */
    async _withEndpointProxySupportInternal(proxyEnabled) {
        const rpcArgs = { builder: this._handle, proxyEnabled };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpointProxySupport', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Set whether a resource can use proxied endpoints or whether they should be disabled for all endpoints belonging to the resource. If set to `false`, endpoints belonging to the resource will ignore the configured proxy settings and run proxy-less.
     *
     * This method is intended to support scenarios with persistent lifetime resources where it is desirable for the resource to be accessible over the same
     * port whether the Aspire application is running or not. Proxied endpoints bind ports that are only accessible while the Aspire application is running.
     * The user needs to be careful to ensure that endpoints are using unique ports when disabling proxy support as by default for proxy-less
     * endpoints, Aspire will allocate the target port as the host port, which will increase the chance of port conflicts.
     * @param proxyEnabled Should endpoints for the resource support using a proxy?
     * @returns The resource builder.
     */
    withEndpointProxySupport(proxyEnabled) {
        return new ProjectResourcePromiseImpl(this._withEndpointProxySupportInternal(proxyEnabled), this._client);
    }
    /** @internal */
    async _withHttpEndpointInternal(port, targetPort, name, env, isProxied) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpEndpoint', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP endpoint
     *
     * If an endpoint with the same name already exists on the resource, the existing endpoint is updated
     * with any non-null parameter values. Parameters left as `null` will not modify the existing endpoint's values.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new ProjectResourcePromiseImpl(this._withHttpEndpointInternal(port, targetPort, name, env, isProxied), this._client);
    }
    /** @internal */
    async _withHttpsEndpointInternal(port, targetPort, name, env, isProxied) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpsEndpoint', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTPS endpoint
     *
     * If an endpoint with the same name already exists on the resource, the existing endpoint is updated
     * with any non-null parameter values. Parameters left as `null` will not modify the existing endpoint's values.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpsEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new ProjectResourcePromiseImpl(this._withHttpsEndpointInternal(port, targetPort, name, env, isProxied), this._client);
    }
    /** @internal */
    async _withExternalHttpEndpointsInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExternalHttpEndpoints', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Marks existing http or https endpoints on a resource as external.
     * @returns The resource builder.
     */
    withExternalHttpEndpoints() {
        return new ProjectResourcePromiseImpl(this._withExternalHttpEndpointsInternal(), this._client);
    }
    /**
     * Gets an endpoint reference
     * @param name The name of the endpoint.
     * @returns An `EndpointReference` that can be used to resolve the address of the endpoint after resource allocation has occurred.
     */
    getEndpoint(name) {
        const promise = (async () => {
            const rpcArgs = { builder: this._handle, name };
            const handle = await this._client.invokeCapability('Aspire.Hosting/getEndpoint', rpcArgs);
            return new EndpointReferenceImpl(handle, this._client);
        })();
        return new EndpointReferencePromiseImpl(promise, this._client);
    }
    /** @internal */
    async _asHttp2ServiceInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/asHttp2Service', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to mark all endpoints' transport as HTTP/2. This is useful for HTTP/2 services that need prior knowledge.
     * @returns The resource builder.
     */
    asHttp2Service() {
        return new ProjectResourcePromiseImpl(this._asHttp2ServiceInternal(), this._client);
    }
    /** @internal */
    async _withUrlsInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new ResourceUrlsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrls', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to customize the URLs displayed for the resource.
     * @param callback The callback that will customize URLs for the resource.
     * @returns The resource builder.
     */
    withUrls(callback) {
        return new ProjectResourcePromiseImpl(this._withUrlsInternal(callback), this._client);
    }
    /** @internal */
    async _withUrlInternal(url, displayText) {
        const rpcArgs = { builder: this._handle, url };
        if (displayText !== undefined)
            rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrl', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Adds or modifies displayed URLs
     * @param options Additional options.
     */
    withUrl(url, options) {
        const displayText = options?.displayText;
        return new ProjectResourcePromiseImpl(this._withUrlInternal(url, displayText), this._client);
    }
    /** @internal */
    async _withUrlForEndpointInternal(endpointName, callback) {
        const callbackId = registerCallback(async (objData) => {
            const obj = wrapIfHandle(objData);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrlForEndpoint', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to update the URL displayed for the endpoint with the specified name.
     * @param endpointName The name of the endpoint to customize the URL for.
     * @param callback The callback that will customize the URL.
     * @returns The resource builder.
     */
    withUrlForEndpoint(endpointName, callback) {
        return new ProjectResourcePromiseImpl(this._withUrlForEndpointInternal(endpointName, callback), this._client);
    }
    /** @internal */
    async _publishWithContainerFilesInternal(source, destinationPath) {
        source = isPromiseLike(source) ? await source : source;
        const rpcArgs = { builder: this._handle, source, destinationPath };
        const result = await this._client.invokeCapability('Aspire.Hosting/publishWithContainerFilesFromResource', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Configures the resource to copy container files from the specified source resource during publishing.
     * @param source The resource which contains the container files to be copied.
     * @param destinationPath The destination path within the resource's container where the files will be copied.
     */
    publishWithContainerFiles(source, destinationPath) {
        return new ProjectResourcePromiseImpl(this._publishWithContainerFilesInternal(source, destinationPath), this._client);
    }
    /** @internal */
    async _excludeFromManifestInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromManifest', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Excludes a resource from being published to the manifest.
     * @returns The resource builder.
     */
    excludeFromManifest() {
        return new ProjectResourcePromiseImpl(this._excludeFromManifestInternal(), this._client);
    }
    /** @internal */
    async _waitForInternal(dependency, waitBehavior) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (waitBehavior !== undefined)
            rpcArgs.waitBehavior = waitBehavior;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitFor', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Waits for another resource to be ready
     * @param options Additional options.
     */
    waitFor(dependency, options) {
        const waitBehavior = options?.waitBehavior;
        return new ProjectResourcePromiseImpl(this._waitForInternal(dependency, waitBehavior), this._client);
    }
    /** @internal */
    async _waitForStartInternal(dependency, waitBehavior) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (waitBehavior !== undefined)
            rpcArgs.waitBehavior = waitBehavior;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitForStart', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Waits for another resource to start
     * @param options Additional options.
     */
    waitForStart(dependency, options) {
        const waitBehavior = options?.waitBehavior;
        return new ProjectResourcePromiseImpl(this._waitForStartInternal(dependency, waitBehavior), this._client);
    }
    /** @internal */
    async _withExplicitStartInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExplicitStart', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Prevents resource from starting automatically
     * @returns The resource builder.
     */
    withExplicitStart() {
        return new ProjectResourcePromiseImpl(this._withExplicitStartInternal(), this._client);
    }
    /** @internal */
    async _waitForCompletionInternal(dependency, exitCode) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitForResourceCompletion', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Waits for the dependency resource to enter the Exited or Finished state before starting the resource.
     * @param dependency The resource builder for the dependency resource.
     * @param options Additional options.
     * @returns The resource builder.
     */
    waitForCompletion(dependency, options) {
        const exitCode = options?.exitCode;
        return new ProjectResourcePromiseImpl(this._waitForCompletionInternal(dependency, exitCode), this._client);
    }
    /** @internal */
    async _withHealthCheckInternal(key) {
        const rpcArgs = { builder: this._handle, key };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHealthCheck', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Adds a health check by key
     * @param key The key for the health check.
     * @returns The resource builder.
     */
    withHealthCheck(key) {
        return new ProjectResourcePromiseImpl(this._withHealthCheckInternal(key), this._client);
    }
    /** @internal */
    async _withHttpHealthCheckInternal(path, statusCode, endpointName) {
        const rpcArgs = { builder: this._handle };
        if (path !== undefined)
            rpcArgs.path = path;
        if (statusCode !== undefined)
            rpcArgs.statusCode = statusCode;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpHealthCheck', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Adds a health check to the resource which is mapped to a specific endpoint.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpHealthCheck(options) {
        const path = options?.path;
        const statusCode = options?.statusCode;
        const endpointName = options?.endpointName;
        return new ProjectResourcePromiseImpl(this._withHttpHealthCheckInternal(path, statusCode, endpointName), this._client);
    }
    /** @internal */
    async _withCommandInternal(name, displayName, executeCommand, commandOptions) {
        const executeCommandId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined)
            rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability('Aspire.Hosting/withCommand', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Adds a resource command
     *
     * The `WithCommand` method is used to add commands to the resource. Commands are displayed in the dashboard
     * and can be executed by a user using the dashboard UI.
     * When a command is executed, the `executeCommand` callback is called and is run inside the Aspire host.
     * @param name The name of the command. The name uniquely identifies the command.
     * @param displayName The display name visible in UI.
     * @param executeCommand A callback that is executed when the command is executed. The callback is run inside the Aspire host. The callback result is used to indicate success or failure in the UI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withCommand(name, displayName, executeCommand, options) {
        const commandOptions = options?.commandOptions;
        return new ProjectResourcePromiseImpl(this._withCommandInternal(name, displayName, executeCommand, commandOptions), this._client);
    }
    /** @internal */
    async _withProcessCommandInternal(commandName, displayName, options) {
        const rpcArgs = { builder: this._handle, commandName, displayName, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommand', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /** Adds a command to the resource that starts a local process when invoked. */
    withProcessCommand(commandName, displayName, options) {
        return new ProjectResourcePromiseImpl(this._withProcessCommandInternal(commandName, displayName, options), this._client);
    }
    /** @internal */
    async _withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options) {
        const createProcessSpecId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await createProcessSpec(arg);
        });
        const rpcArgs = { builder: this._handle, commandName, displayName, createProcessSpec: createProcessSpecId };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommandFactory', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Adds a command to the resource that starts a local process created by a callback when invoked.
     * @param options Additional options.
     */
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new ProjectResourcePromiseImpl(this._withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options), this._client);
    }
    /** @internal */
    async _withHttpCommandInternal(path, displayName, options) {
        const rpcArgs = { builder: this._handle, path, displayName };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpCommand', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP resource command
     * @param options Additional options.
     */
    withHttpCommand(path, displayName, options) {
        return new ProjectResourcePromiseImpl(this._withHttpCommandInternal(path, displayName, options), this._client);
    }
    /** @internal */
    async _withDeveloperCertificateTrustInternal(trust) {
        const rpcArgs = { builder: this._handle, trust };
        const result = await this._client.invokeCapability('Aspire.Hosting/withDeveloperCertificateTrust', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Indicates whether developer certificates should be treated as trusted certificate authorities for the resource at run time. Currently this indicates trust for the ASP.NET Core developer certificate. The developer certificate will only be trusted when running in local development scenarios; in publish mode resources will use their default certificate trust.
     *
     * Disable trust for app host managed developer certificate(s) for a container resource.
     * ```
     * var container = builder.AddContainer("my-service", "my-service:latest")
     * .WithDeveloperCertificateTrust(false);
     * ```
     * Disable automatic trust for app host managed developer certificate(s), but explicitly enable it for a specific resource.
     * ```
     * var builder = DistributedApplication.CreateBuilder(new DistributedApplicationOptions()
     * {
     * Args = args,
     * TrustDeveloperCertificate = false,
     * });
     * var project = builder.AddProject<MyService>("my-service")
     * .WithDeveloperCertificateTrust(true);
     * ```
     * @param trust Indicates whether the developer certificate should be treated as trusted.
     * @returns The resource builder.
     */
    withDeveloperCertificateTrust(trust) {
        return new ProjectResourcePromiseImpl(this._withDeveloperCertificateTrustInternal(trust), this._client);
    }
    /** @internal */
    async _withCertificateTrustScopeInternal(scope) {
        const rpcArgs = { builder: this._handle, scope };
        const result = await this._client.invokeCapability('Aspire.Hosting/withCertificateTrustScope', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Sets the certificate trust scope
     *
     * The default scope if not overridden is `Append` which means that custom certificate
     * authorities should be appended to the default trusted certificate authorities for the resource. Setting the scope to
     * `Override` indicates the set of certificates in referenced
     * `CertificateAuthorityCollection` (and optionally Aspire developer certificiates) should be used as the
     * exclusive source of trust for a resource.
     * In all cases, this is a best effort implementation as not all resources support full customization of certificate
     * trust.
     * Set the scope for custom certificate authorities to override the default trusted certificate authorities for a container resource.
     * ```
     * var caCollection = builder.AddCertificateAuthorityCollection("my-cas")
     * .WithCertificate(new X509Certificate2("my-ca.pem"));
     * var container = builder.AddContainer("my-service", "my-service:latest")
     * .WithCertificateAuthorityCollection(caCollection)
     * .WithCertificateTrustScope(CertificateTrustScope.Override);
     * ```
     * @param scope The scope to apply to custom certificate authorities associated with the resource.
     * @returns The resource builder.
     */
    withCertificateTrustScope(scope) {
        return new ProjectResourcePromiseImpl(this._withCertificateTrustScopeInternal(scope), this._client);
    }
    /** @internal */
    async _withHttpsDeveloperCertificateInternal(password) {
        password = isPromiseLike(password) ? await password : password;
        const rpcArgs = { builder: this._handle };
        if (password !== undefined)
            rpcArgs.password = password;
        const result = await this._client.invokeCapability('Aspire.Hosting/withParameterHttpsDeveloperCertificate', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Indicates that a resource should use the developer certificate key pair for HTTPS endpoints at run time. Currently this indicates use of the ASP.NET Core developer certificate. The developer certificate will only be used when running in local development scenarios; in publish mode resources will use their default certificate configuration.
     *
     * Use the developer certificate for HTTPS/TLS endpoints on a container resource:
     * ```
     * builder.AddContainer("my-service", "my-image")
     * .WithHttpsDeveloperCertificate()
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpsDeveloperCertificate(options) {
        let password = options?.password;
        return new ProjectResourcePromiseImpl(this._withHttpsDeveloperCertificateInternal(password), this._client);
    }
    /** @internal */
    async _withoutHttpsCertificateInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withoutHttpsCertificate', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Disable HTTPS/TLS server certificate configuration for the resource. No HTTPS/TLS termination configuration will be applied.
     *
     * Disable HTTPS certificate configuration for a Redis resource:
     * ```
     * var redis = builder.AddRedis("cache")
     * .WithoutHttpsCertificate();
     * ```
     * @returns The resource builder.
     */
    withoutHttpsCertificate() {
        return new ProjectResourcePromiseImpl(this._withoutHttpsCertificateInternal(), this._client);
    }
    /** @internal */
    async _withRelationshipInternal(resourceBuilder, type) {
        resourceBuilder = isPromiseLike(resourceBuilder) ? await resourceBuilder : resourceBuilder;
        const rpcArgs = { builder: this._handle, resourceBuilder, type };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderRelationship', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Adds a relationship to another resource using its builder.
     * @param resourceBuilder The resource builder that the relationship is to.
     * @param type The relationship type.
     * @returns A resource builder.
     */
    withRelationship(resourceBuilder, type) {
        return new ProjectResourcePromiseImpl(this._withRelationshipInternal(resourceBuilder, type), this._client);
    }
    /** @internal */
    async _withParentRelationshipInternal(parent) {
        parent = isPromiseLike(parent) ? await parent : parent;
        const rpcArgs = { builder: this._handle, parent };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderParentRelationship', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Sets the parent relationship
     * @param parent The parent of `builder`.
     * @returns A resource builder.
     */
    withParentRelationship(parent) {
        return new ProjectResourcePromiseImpl(this._withParentRelationshipInternal(parent), this._client);
    }
    /** @internal */
    async _withChildRelationshipInternal(child) {
        child = isPromiseLike(child) ? await child : child;
        const rpcArgs = { builder: this._handle, child };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderChildRelationship', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Sets a child relationship
     * @param child The child of `builder`.
     * @returns A resource builder.
     */
    withChildRelationship(child) {
        return new ProjectResourcePromiseImpl(this._withChildRelationshipInternal(child), this._client);
    }
    /** @internal */
    async _withIconNameInternal(iconName, iconVariant) {
        const rpcArgs = { builder: this._handle, iconName };
        if (iconVariant !== undefined)
            rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability('Aspire.Hosting/withIconName', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Specifies the icon to use when displaying the resource in the dashboard.
     * @param iconName The name of the FluentUI icon to use. See https://aka.ms/fluentui-system-icons for available icons.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withIconName(iconName, options) {
        const iconVariant = options?.iconVariant;
        return new ProjectResourcePromiseImpl(this._withIconNameInternal(iconName, iconVariant), this._client);
    }
    /** @internal */
    async _withComputeEnvironmentInternal(computeEnvironmentResource) {
        computeEnvironmentResource = isPromiseLike(computeEnvironmentResource) ? await computeEnvironmentResource : computeEnvironmentResource;
        const rpcArgs = { builder: this._handle, computeEnvironmentResource };
        const result = await this._client.invokeCapability('Aspire.Hosting/withComputeEnvironment', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Configures the compute environment for the compute resource.
     *
     * This method allows associating a specific compute environment with the compute resource.
     * @param computeEnvironmentResource The compute environment resource to associate with the compute resource.
     * @returns The resource builder.
     */
    withComputeEnvironment(computeEnvironmentResource) {
        return new ProjectResourcePromiseImpl(this._withComputeEnvironmentInternal(computeEnvironmentResource), this._client);
    }
    /** @internal */
    async _withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName) {
        const rpcArgs = { builder: this._handle, probeType };
        if (path !== undefined)
            rpcArgs.path = path;
        if (initialDelaySeconds !== undefined)
            rpcArgs.initialDelaySeconds = initialDelaySeconds;
        if (periodSeconds !== undefined)
            rpcArgs.periodSeconds = periodSeconds;
        if (timeoutSeconds !== undefined)
            rpcArgs.timeoutSeconds = timeoutSeconds;
        if (failureThreshold !== undefined)
            rpcArgs.failureThreshold = failureThreshold;
        if (successThreshold !== undefined)
            rpcArgs.successThreshold = successThreshold;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpProbe', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP health probe to the resource
     * @param options Additional options.
     */
    withHttpProbe(probeType, options) {
        const path = options?.path;
        const initialDelaySeconds = options?.initialDelaySeconds;
        const periodSeconds = options?.periodSeconds;
        const timeoutSeconds = options?.timeoutSeconds;
        const failureThreshold = options?.failureThreshold;
        const successThreshold = options?.successThreshold;
        const endpointName = options?.endpointName;
        return new ProjectResourcePromiseImpl(this._withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName), this._client);
    }
    /** @internal */
    async _excludeFromMcpInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromMcp', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Exclude the resource from MCP operations using the Aspire MCP server. The resource is excluded from results that return resources, console logs and telemetry.
     * @returns The resource builder.
     */
    excludeFromMcp() {
        return new ProjectResourcePromiseImpl(this._excludeFromMcpInternal(), this._client);
    }
    /** @internal */
    async _withHiddenInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHidden', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists
     *
     * Use this method to hide resources that are implementation details and should never be displayed by default.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @returns The resource builder.
     */
    withHidden() {
        return new ProjectResourcePromiseImpl(this._withHiddenInternal(), this._client);
    }
    /** @internal */
    async _withHiddenOnCompletionInternal(exitCode, exitCodes) {
        const rpcArgs = { builder: this._handle };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        if (exitCodes !== undefined)
            rpcArgs.exitCodes = exitCodes;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHiddenOnCompletion', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists after successful completion
     *
     * This method is useful for one-off resources such as setup scripts, migrations, or build steps that should remain visible while running
     * and then be hidden after successful completion.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHiddenOnCompletion(options) {
        const exitCode = options?.exitCode;
        const exitCodes = options?.exitCodes;
        return new ProjectResourcePromiseImpl(this._withHiddenOnCompletionInternal(exitCode, exitCodes), this._client);
    }
    /** @internal */
    async _withImagePushOptionsInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ContainerImagePushOptionsCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImagePushOptions', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Adds an asynchronous callback to configure container image push options for the resource.
     *
     * This method allows customization of how container images are named and tagged when pushed to a registry using an asynchronous callback.
     * Use this overload when the callback needs to perform asynchronous operations such as retrieving configuration values from external sources.
     * The callback receives a `ContainerImagePushOptionsCallbackContext` that provides access to the resource
     * and the `ContainerImagePushOptions` that can be modified.
     * Multiple callbacks can be registered on the same resource, and they will be invoked in the order they were added.
     * @param callback The asynchronous callback to configure push options.
     * @returns The resource builder.
     */
    withImagePushOptions(callback) {
        return new ProjectResourcePromiseImpl(this._withImagePushOptionsInternal(callback), this._client);
    }
    /** @internal */
    async _withRemoteImageNameInternal(remoteImageName) {
        const rpcArgs = { builder: this._handle, remoteImageName };
        const result = await this._client.invokeCapability('Aspire.Hosting/withRemoteImageName', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Sets the remote image name (without registry endpoint or tag) for container push operations.
     *
     * Use this with `withRemoteImageTag` to fully customize the image reference used for container push operations.
     * @param remoteImageName The remote image name (e.g., "myapp" or "myorg/myapp").
     * @returns The resource builder.
     */
    withRemoteImageName(remoteImageName) {
        return new ProjectResourcePromiseImpl(this._withRemoteImageNameInternal(remoteImageName), this._client);
    }
    /** @internal */
    async _withRemoteImageTagInternal(remoteImageTag) {
        const rpcArgs = { builder: this._handle, remoteImageTag };
        const result = await this._client.invokeCapability('Aspire.Hosting/withRemoteImageTag', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Sets the remote image tag for container push operations.
     *
     * Use this with `withRemoteImageName` to fully customize the image reference used for container push operations.
     * @param remoteImageTag The remote image tag (e.g., "latest", "v1.0.0").
     * @returns The resource builder.
     */
    withRemoteImageTag(remoteImageTag) {
        return new ProjectResourcePromiseImpl(this._withRemoteImageTagInternal(remoteImageTag), this._client);
    }
    /** @internal */
    async _withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new PipelineStepContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined)
            rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined)
            rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined)
            rpcArgs.tags = tags;
        if (description !== undefined)
            rpcArgs.description = description;
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineStepFactory', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Adds a pipeline step to the resource that will be executed during deployment.
     * @param stepName The unique name of the pipeline step.
     * @param callback The callback to execute when the step runs.
     * @param options Additional options.
     * @returns The resource builder for chaining.
     */
    withPipelineStepFactory(stepName, callback, options) {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new ProjectResourcePromiseImpl(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description), this._client);
    }
    /** @internal */
    async _withPipelineConfigurationInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new PipelineConfigurationContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineConfiguration', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to be executed during the pipeline configuration phase, allowing modification of step dependencies and relationships.
     * @param callback The callback function to execute during the configuration phase.
     * @returns The resource builder for chaining.
     */
    withPipelineConfiguration(callback) {
        return new ProjectResourcePromiseImpl(this._withPipelineConfigurationInternal(callback), this._client);
    }
    /**
     * Gets the name of the resource from a builder.
     *
     * Why this wrapper exists: This capability accesses a nested property
     * (`resource.Resource.Name`) which requires a wrapper method. There is no single
     * .NET method that returns just the resource name that could be annotated directly.
     * @returns The resource name.
     */
    async getResourceName() {
        const rpcArgs = { resource: this._handle };
        return await this._client.invokeCapability('Aspire.Hosting/getResourceName', rpcArgs);
    }
    /** @internal */
    async _onBeforeResourceStartedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new BeforeResourceStartedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onBeforeResourceStarted', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the BeforeResourceStarted event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onBeforeResourceStarted(callback) {
        return new ProjectResourcePromiseImpl(this._onBeforeResourceStartedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceStoppedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceStoppedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceStopped', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceStopped event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceStopped(callback) {
        return new ProjectResourcePromiseImpl(this._onResourceStoppedInternal(callback), this._client);
    }
    /** @internal */
    async _onInitializeResourceInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new InitializeResourceEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onInitializeResource', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the InitializeResource event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onInitializeResource(callback) {
        return new ProjectResourcePromiseImpl(this._onInitializeResourceInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceEndpointsAllocatedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceEndpointsAllocatedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceEndpointsAllocated', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceEndpointsAllocated event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceEndpointsAllocated(callback) {
        return new ProjectResourcePromiseImpl(this._onResourceEndpointsAllocatedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceReadyInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceReadyEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceReady', rpcArgs);
        return new ProjectResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceReady event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceReady(callback) {
        return new ProjectResourcePromiseImpl(this._onResourceReadyInternal(callback), this._client);
    }
    /**
     * Creates an execution configuration builder for the specified resource.
     * @returns The execution configuration builder.
     */
    createExecutionConfiguration() {
        const promise = (async () => {
            const rpcArgs = { resource: this._handle };
            const handle = await this._client.invokeCapability('Aspire.Hosting/createExecutionConfiguration', rpcArgs);
            return new ExecutionConfigurationBuilderImpl(handle, this._client);
        })();
        return new ExecutionConfigurationBuilderPromiseImpl(promise, this._client);
    }
}
/**
 * Thenable wrapper for ProjectResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
class ProjectResourcePromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    withContainerRegistry(registry) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withContainerRegistry(registry)), this._client);
    }
    withDockerfileBaseImage(options) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileBaseImage(options)), this._client);
    }
    withMcpServer(options) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withMcpServer(options)), this._client);
    }
    withOtlpExporter(options) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withOtlpExporter(options)), this._client);
    }
    withReplicas(replicas) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withReplicas(replicas)), this._client);
    }
    disableForwardedHeaders() {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.disableForwardedHeaders()), this._client);
    }
    publishAsDockerFile(options) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.publishAsDockerFile(options)), this._client);
    }
    withRequiredCommand(command, options) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withRequiredCommand(command, options)), this._client);
    }
    withSessionLifetime() {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withSessionLifetime()), this._client);
    }
    withPersistentLifetime() {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withPersistentLifetime()), this._client);
    }
    withLifetimeOf(sourceBuilder) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withLifetimeOf(sourceBuilder)), this._client);
    }
    withParentProcessLifetime(parentProcessId) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withParentProcessLifetime(parentProcessId)), this._client);
    }
    withEnvironment(name, value) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withEnvironment(name, value)), this._client);
    }
    withEnvironmentCallback(callback) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withEnvironmentCallback(callback)), this._client);
    }
    withArgs(args) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withArgs(args)), this._client);
    }
    withArgsCallback(callback) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withArgsCallback(callback)), this._client);
    }
    withReferenceEnvironment(options) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withReferenceEnvironment(options)), this._client);
    }
    withReference(source, options) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withReference(source, options)), this._client);
    }
    withEndpointCallback(endpointName, callback, options) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withEndpointCallback(endpointName, callback, options)), this._client);
    }
    withHttpEndpointCallback(callback, options) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withHttpEndpointCallback(callback, options)), this._client);
    }
    withHttpsEndpointCallback(callback, options) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withHttpsEndpointCallback(callback, options)), this._client);
    }
    withEndpoint(options) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withEndpoint(options)), this._client);
    }
    withEndpointProxySupport(proxyEnabled) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withEndpointProxySupport(proxyEnabled)), this._client);
    }
    withHttpEndpoint(options) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withHttpEndpoint(options)), this._client);
    }
    withHttpsEndpoint(options) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withHttpsEndpoint(options)), this._client);
    }
    withExternalHttpEndpoints() {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withExternalHttpEndpoints()), this._client);
    }
    getEndpoint(name) {
        return new EndpointReferencePromiseImpl(this._promise.then(obj => obj.getEndpoint(name)), this._client);
    }
    asHttp2Service() {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.asHttp2Service()), this._client);
    }
    withUrls(callback) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withUrls(callback)), this._client);
    }
    withUrl(url, options) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withUrl(url, options)), this._client);
    }
    withUrlForEndpoint(endpointName, callback) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)), this._client);
    }
    publishWithContainerFiles(source, destinationPath) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.publishWithContainerFiles(source, destinationPath)), this._client);
    }
    excludeFromManifest() {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.excludeFromManifest()), this._client);
    }
    waitFor(dependency, options) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.waitFor(dependency, options)), this._client);
    }
    waitForStart(dependency, options) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.waitForStart(dependency, options)), this._client);
    }
    withExplicitStart() {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withExplicitStart()), this._client);
    }
    waitForCompletion(dependency, options) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.waitForCompletion(dependency, options)), this._client);
    }
    withHealthCheck(key) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withHealthCheck(key)), this._client);
    }
    withHttpHealthCheck(options) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withHttpHealthCheck(options)), this._client);
    }
    withCommand(name, displayName, executeCommand, options) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)), this._client);
    }
    withProcessCommand(commandName, displayName, options) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommand(commandName, displayName, options)), this._client);
    }
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommandFactory(commandName, displayName, createProcessSpec, options)), this._client);
    }
    withHttpCommand(path, displayName, options) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withHttpCommand(path, displayName, options)), this._client);
    }
    withDeveloperCertificateTrust(trust) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withDeveloperCertificateTrust(trust)), this._client);
    }
    withCertificateTrustScope(scope) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withCertificateTrustScope(scope)), this._client);
    }
    withHttpsDeveloperCertificate(options) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withHttpsDeveloperCertificate(options)), this._client);
    }
    withoutHttpsCertificate() {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withoutHttpsCertificate()), this._client);
    }
    withRelationship(resourceBuilder, type) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withRelationship(resourceBuilder, type)), this._client);
    }
    withParentRelationship(parent) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withParentRelationship(parent)), this._client);
    }
    withChildRelationship(child) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withChildRelationship(child)), this._client);
    }
    withIconName(iconName, options) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withIconName(iconName, options)), this._client);
    }
    withComputeEnvironment(computeEnvironmentResource) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withComputeEnvironment(computeEnvironmentResource)), this._client);
    }
    withHttpProbe(probeType, options) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withHttpProbe(probeType, options)), this._client);
    }
    excludeFromMcp() {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.excludeFromMcp()), this._client);
    }
    withHidden() {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withHidden()), this._client);
    }
    withHiddenOnCompletion(options) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withHiddenOnCompletion(options)), this._client);
    }
    withImagePushOptions(callback) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withImagePushOptions(callback)), this._client);
    }
    withRemoteImageName(remoteImageName) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withRemoteImageName(remoteImageName)), this._client);
    }
    withRemoteImageTag(remoteImageTag) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withRemoteImageTag(remoteImageTag)), this._client);
    }
    withPipelineStepFactory(stepName, callback, options) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)), this._client);
    }
    withPipelineConfiguration(callback) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.withPipelineConfiguration(callback)), this._client);
    }
    getResourceName() {
        return this._promise.then(obj => obj.getResourceName());
    }
    onBeforeResourceStarted(callback) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.onBeforeResourceStarted(callback)), this._client);
    }
    onResourceStopped(callback) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.onResourceStopped(callback)), this._client);
    }
    onInitializeResource(callback) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.onInitializeResource(callback)), this._client);
    }
    onResourceEndpointsAllocated(callback) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.onResourceEndpointsAllocated(callback)), this._client);
    }
    onResourceReady(callback) {
        return new ProjectResourcePromiseImpl(this._promise.then(obj => obj.onResourceReady(callback)), this._client);
    }
    createExecutionConfiguration() {
        return new ExecutionConfigurationBuilderPromiseImpl(this._promise.then(obj => obj.createExecutionConfiguration()), this._client);
    }
}
// ============================================================================
// RedisCommanderResourceImpl
// ============================================================================
class RedisCommanderResourceImpl extends ResourceBuilderBase {
    constructor(handle, client) {
        super(handle, client);
    }
    /** @internal */
    async _withContainerRegistryInternal(registry) {
        registry = isPromiseLike(registry) ? await registry : registry;
        const rpcArgs = { builder: this._handle, registry };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerRegistry', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Configures the resource to use the specified container registry for container image operations.
     *
     * This method adds a `ContainerRegistryReferenceAnnotation` to the resource,
     * indicating that the resource should use the specified container registry for container image operations.
     * @param registry The container registry resource builder.
     * @returns The resource builder for chaining.
     */
    withContainerRegistry(registry) {
        return new RedisCommanderResourcePromiseImpl(this._withContainerRegistryInternal(registry), this._client);
    }
    /** @internal */
    async _withBindMountInternal(source, target, isReadOnly) {
        const rpcArgs = { builder: this._handle, source, target };
        if (isReadOnly !== undefined)
            rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability('Aspire.Hosting/withBindMount', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Adds a bind mount to a container resource.
     * @param source The source path of the mount. This is the path to the file or directory on the host, relative to the app host project directory.
     * @param target The target path where the file or directory is mounted in the container.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withBindMount(source, target, options) {
        const isReadOnly = options?.isReadOnly;
        return new RedisCommanderResourcePromiseImpl(this._withBindMountInternal(source, target, isReadOnly), this._client);
    }
    /** @internal */
    async _withEntrypointInternal(entrypoint) {
        const rpcArgs = { builder: this._handle, entrypoint };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEntrypoint', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Sets the Entrypoint for the container.
     * @param entrypoint The new entrypoint for the container.
     * @returns The resource builder.
     */
    withEntrypoint(entrypoint) {
        return new RedisCommanderResourcePromiseImpl(this._withEntrypointInternal(entrypoint), this._client);
    }
    /** @internal */
    async _withImageTagInternal(tag) {
        const rpcArgs = { builder: this._handle, tag };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImageTag', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Allows overriding the image tag on a container.
     * @param tag Tag value.
     * @returns The resource builder.
     */
    withImageTag(tag) {
        return new RedisCommanderResourcePromiseImpl(this._withImageTagInternal(tag), this._client);
    }
    /** @internal */
    async _withImageRegistryInternal(registry) {
        const rpcArgs = { builder: this._handle, registry };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImageRegistry', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Allows overriding the image registry on a container.
     * @param registry Registry value.
     * @returns The resource builder.
     */
    withImageRegistry(registry) {
        return new RedisCommanderResourcePromiseImpl(this._withImageRegistryInternal(registry), this._client);
    }
    /** @internal */
    async _withImageInternal(image, tag) {
        const rpcArgs = { builder: this._handle, image };
        if (tag !== undefined)
            rpcArgs.tag = tag;
        const result = await this._client.invokeCapability('Aspire.Hosting/withImage', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Allows overriding the image on a container.
     * @param image Image value.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withImage(image, options) {
        const tag = options?.tag;
        return new RedisCommanderResourcePromiseImpl(this._withImageInternal(image, tag), this._client);
    }
    /** @internal */
    async _withImageSHA256Internal(sha256) {
        const rpcArgs = { builder: this._handle, sha256 };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImageSHA256', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Allows setting the image to a specific sha256 on a container.
     * @param sha256 Registry value.
     * @returns The resource builder.
     */
    withImageSHA256(sha256) {
        return new RedisCommanderResourcePromiseImpl(this._withImageSHA256Internal(sha256), this._client);
    }
    /** @internal */
    async _withContainerRuntimeArgsInternal(args) {
        const rpcArgs = { builder: this._handle, args };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerRuntimeArgs', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Adds a callback to be executed with a list of arguments to add to the container runtime run command when a container resource is started.
     * @param args The arguments to be passed to the container runtime run command when the container resource is started.
     * @returns The resource builder.
     */
    withContainerRuntimeArgs(args) {
        return new RedisCommanderResourcePromiseImpl(this._withContainerRuntimeArgsInternal(args), this._client);
    }
    /** @internal */
    async _withLifetimeInternal(lifetime) {
        const rpcArgs = { builder: this._handle, lifetime };
        const result = await this._client.invokeCapability('Aspire.Hosting/withLifetime', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Sets the lifetime behavior of the container resource.
     *
     * Prefer `WithPersistentLifetime``1` or
     * `WithSessionLifetime``1` for new code.
     * Marking a container resource to have a `Persistent` lifetime.
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddContainer("mycontainer", "myimage")
     * .WithPersistentLifetime();
     * builder.Build().Run();
     * ```
     * @param lifetime The lifetime behavior of the container resource. The default behavior is `Session`.
     * @returns The resource builder.
     */
    withLifetime(lifetime) {
        return new RedisCommanderResourcePromiseImpl(this._withLifetimeInternal(lifetime), this._client);
    }
    /** @internal */
    async _withImagePullPolicyInternal(pullPolicy) {
        const rpcArgs = { builder: this._handle, pullPolicy };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImagePullPolicy', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Sets the pull policy for the container resource.
     * @param pullPolicy The pull policy behavior for the container resource.
     * @returns The resource builder.
     */
    withImagePullPolicy(pullPolicy) {
        return new RedisCommanderResourcePromiseImpl(this._withImagePullPolicyInternal(pullPolicy), this._client);
    }
    /** @internal */
    async _publishAsContainerInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/publishAsContainer', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Changes the resource to be published as a container in the manifest.
     * @returns The resource builder.
     */
    publishAsContainer() {
        return new RedisCommanderResourcePromiseImpl(this._publishAsContainerInternal(), this._client);
    }
    /** @internal */
    async _withDockerfileInternal(contextPath, dockerfilePath, stage) {
        const rpcArgs = { builder: this._handle, contextPath };
        if (dockerfilePath !== undefined)
            rpcArgs.dockerfilePath = dockerfilePath;
        if (stage !== undefined)
            rpcArgs.stage = stage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfile', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Causes Aspire to build the specified container image from a Dockerfile.
     * @param contextPath Path to be used as the context for the container image build.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfile(contextPath, options) {
        const dockerfilePath = options?.dockerfilePath;
        const stage = options?.stage;
        return new RedisCommanderResourcePromiseImpl(this._withDockerfileInternal(contextPath, dockerfilePath, stage), this._client);
    }
    /** @internal */
    async _withDockerfileFactoryInternal(contextPath, dockerfileFactory, stage) {
        const dockerfileFactoryId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new DockerfileFactoryContextImpl(argHandle, this._client);
            return await dockerfileFactory(arg);
        });
        const rpcArgs = { builder: this._handle, contextPath, dockerfileFactory: dockerfileFactoryId };
        if (stage !== undefined)
            rpcArgs.stage = stage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileFactory', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Builds the specified container image from a Dockerfile generated by an asynchronous factory function.
     *
     * When this method is called, an annotation is added to the `ContainerResource` that specifies the context path
     * and a factory function that generates Dockerfile content. The factory is invoked at build time to produce the Dockerfile,
     * which is then written to a temporary file and used by the orchestrator to build the container image.
     * The `contextPath` is relative to the AppHost directory unless it is fully qualified.
     * The factory function is invoked once during the build process to generate the Dockerfile content.
     * The output is trusted and not validated.
     * Creates a container called `mycontainer` with a dynamically generated Dockerfile.
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddContainer("mycontainer", "myimage")
     * .WithDockerfileFactory("path/to/context", async context =>
     * {
     * var template = await File.ReadAllTextAsync("template.dockerfile", context.CancellationToken);
     * return template.Replace("{{VERSION}}", "1.0");
     * });
     * builder.Build().Run();
     * ```
     * @param contextPath Path to be used as the context for the container image build.
     * @param dockerfileFactory An asynchronous function that returns the Dockerfile content as a string.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileFactory(contextPath, dockerfileFactory, options) {
        const stage = options?.stage;
        return new RedisCommanderResourcePromiseImpl(this._withDockerfileFactoryInternal(contextPath, dockerfileFactory, stage), this._client);
    }
    /** @internal */
    async _withContainerNameInternal(name) {
        const rpcArgs = { builder: this._handle, name };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerName', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Overrides the default container name for this resource. By default Aspire generates a unique container name based on the resource name and a random postfix (or a postfix based on a hash of the AppHost project path for persistent container resources). This method allows you to override that behavior with a custom name, but could lead to naming conflicts if the specified name is not unique.
     *
     * Combining this with `Persistent` will allow Aspire to re-use an existing container that was not
     * created by an Aspire AppHost.
     * @param name The desired container name. Must be a valid container name or your runtime will report an error.
     * @returns The resource builder.
     */
    withContainerName(name) {
        return new RedisCommanderResourcePromiseImpl(this._withContainerNameInternal(name), this._client);
    }
    /** @internal */
    async _withBuildArgInternal(name, value) {
        value = isPromiseLike(value) ? await value : value;
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuildArg', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Adds a build argument when the container is built from a Dockerfile.
     * @param name The name of the build argument.
     * @param value The build argument value, either a string or a parameter resource.
     * @returns The resource builder.
     */
    withBuildArg(name, value) {
        return new RedisCommanderResourcePromiseImpl(this._withBuildArgInternal(name, value), this._client);
    }
    /** @internal */
    async _withBuildSecretInternal(name, value) {
        value = isPromiseLike(value) ? await value : value;
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withParameterBuildSecret', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Adds a secret build argument when the container is built from a Dockerfile.
     * @param name The name of the secret build argument.
     * @param value The resource builder for a parameter resource.
     * @returns The resource builder.
     */
    withBuildSecret(name, value) {
        return new RedisCommanderResourcePromiseImpl(this._withBuildSecretInternal(name, value), this._client);
    }
    /** @internal */
    async _withContainerCertificatePathsInternal(customCertificatesDestination, defaultCertificateBundlePaths, defaultCertificateDirectoryPaths) {
        const rpcArgs = { builder: this._handle };
        if (customCertificatesDestination !== undefined)
            rpcArgs.customCertificatesDestination = customCertificatesDestination;
        if (defaultCertificateBundlePaths !== undefined)
            rpcArgs.defaultCertificateBundlePaths = defaultCertificateBundlePaths;
        if (defaultCertificateDirectoryPaths !== undefined)
            rpcArgs.defaultCertificateDirectoryPaths = defaultCertificateDirectoryPaths;
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerCertificatePaths', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Adds container certificate path overrides used for certificate trust at run time.
     * @param options Additional options.
     * @returns The updated resource builder.
     */
    withContainerCertificatePaths(options) {
        const customCertificatesDestination = options?.customCertificatesDestination;
        const defaultCertificateBundlePaths = options?.defaultCertificateBundlePaths;
        const defaultCertificateDirectoryPaths = options?.defaultCertificateDirectoryPaths;
        return new RedisCommanderResourcePromiseImpl(this._withContainerCertificatePathsInternal(customCertificatesDestination, defaultCertificateBundlePaths, defaultCertificateDirectoryPaths), this._client);
    }
    /** @internal */
    async _withDockerfileBuilderInternal(contextPath, callback, stage) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new DockerfileBuilderCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, contextPath, callback: callbackId };
        if (stage !== undefined)
            rpcArgs.stage = stage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileBuilder', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Configures the resource to use a programmatically generated Dockerfile
     *
     * This method provides a programmatic way to build Dockerfiles using the `DockerfileBuilder` API
     * instead of string manipulation. Callbacks can be composed by calling this method multiple times - each callback will be invoked
     * in order to build up the final Dockerfile.
     * The `contextPath` is relative to the AppHost directory unless it is fully qualified.
     * Creates a container with a programmatically built Dockerfile using fluent API:
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddContainer("mycontainer", "myimage")
     * .WithDockerfileBuilder("path/to/context", context =>
     * {
     * context.Builder.From("alpine:latest")
     * .WorkDir("/app")
     * .Run("apk add curl")
     * .Copy(".", ".")
     * .Cmd(["./myapp"]);
     * return Task.CompletedTask;
     * });
     * builder.Build().Run();
     * ```
     * @param contextPath Path to be used as the context for the container image build.
     * @param callback A callback that uses the `DockerfileBuilder` API to construct the Dockerfile.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileBuilder(contextPath, callback, options) {
        const stage = options?.stage;
        return new RedisCommanderResourcePromiseImpl(this._withDockerfileBuilderInternal(contextPath, callback, stage), this._client);
    }
    /** @internal */
    async _withDockerfileBaseImageInternal(buildImage, runtimeImage) {
        const rpcArgs = { builder: this._handle };
        if (buildImage !== undefined)
            rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined)
            rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileBaseImage', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Configures custom base images for generated Dockerfiles.
     *
     * This extension method allows customization of the base images used in generated Dockerfiles.
     * For multi-stage Dockerfiles (e.g., Python with UV), you can specify separate build and runtime images.
     * Specify custom base images for a Python application:
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddPythonApp("myapp", "path/to/app", "main.py")
     * .WithDockerfileBaseImage(
     * buildImage: "ghcr.io/astral-sh/uv:python3.12-bookworm-slim",
     * runtimeImage: "python:3.12-slim-bookworm");
     * builder.Build().Run();
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileBaseImage(options) {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new RedisCommanderResourcePromiseImpl(this._withDockerfileBaseImageInternal(buildImage, runtimeImage), this._client);
    }
    /** @internal */
    async _withContainerNetworkAliasInternal(alias) {
        const rpcArgs = { builder: this._handle, alias };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerNetworkAlias', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Adds a network alias to container resource.
     *
     * Network aliases enable DNS resolution of the container on the network by custom names.
     * By default, containers are accessible on the network using their resource name as a DNS alias.
     * This method allows adding additional aliases for the same container.
     * Multiple aliases can be added by calling this method multiple times.
     * @param alias The network alias for the container.
     * @returns The resource builder.
     */
    withContainerNetworkAlias(alias) {
        return new RedisCommanderResourcePromiseImpl(this._withContainerNetworkAliasInternal(alias), this._client);
    }
    /** @internal */
    async _withMcpServerInternal(path, endpointName) {
        const rpcArgs = { builder: this._handle };
        if (path !== undefined)
            rpcArgs.path = path;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withMcpServer', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Marks the resource as hosting a Model Context Protocol (MCP) server on the specified endpoint.
     *
     * This method adds an `McpServerEndpointAnnotation` to the resource, enabling the Aspire tooling
     * to discover and proxy the MCP server exposed by the resource.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withMcpServer(options) {
        const path = options?.path;
        const endpointName = options?.endpointName;
        return new RedisCommanderResourcePromiseImpl(this._withMcpServerInternal(path, endpointName), this._client);
    }
    /** @internal */
    async _withOtlpExporterInternal(protocol) {
        const rpcArgs = { builder: this._handle };
        if (protocol !== undefined)
            rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability('Aspire.Hosting/withOtlpExporter', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Configures OTLP telemetry export
     * @param options Additional options.
     */
    withOtlpExporter(options) {
        const protocol = options?.protocol;
        return new RedisCommanderResourcePromiseImpl(this._withOtlpExporterInternal(protocol), this._client);
    }
    /** @internal */
    async _publishAsConnectionStringInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/publishAsConnectionString', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Changes the resource to be published as a connection string reference in the manifest.
     * @returns The resource builder.
     */
    publishAsConnectionString() {
        return new RedisCommanderResourcePromiseImpl(this._publishAsConnectionStringInternal(), this._client);
    }
    /** @internal */
    async _withRequiredCommandInternal(command, helpLink) {
        const rpcArgs = { builder: this._handle, command };
        if (helpLink !== undefined)
            rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability('Aspire.Hosting/withRequiredCommand', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Declares that a resource requires a specific command/executable to be available on the local machine PATH before it can start.
     *
     * The command is considered valid if either:
     * 1. It is an absolute or relative path (contains a directory separator) that points to an existing file, or
     * 2. It is discoverable on the current process PATH (respecting PATHEXT on Windows).
     * If the command is not found, a warning message will be logged but the resource will be allowed to attempt to start.
     * @param command The command string (file name or path) that should be validated.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withRequiredCommand(command, options) {
        const helpLink = options?.helpLink;
        return new RedisCommanderResourcePromiseImpl(this._withRequiredCommandInternal(command, helpLink), this._client);
    }
    /** @internal */
    async _withSessionLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withSessionLifetime', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a session lifetime.
     * @returns The resource builder.
     */
    withSessionLifetime() {
        return new RedisCommanderResourcePromiseImpl(this._withSessionLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withPersistentLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPersistentLifetime', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime.
     * @returns The resource builder.
     */
    withPersistentLifetime() {
        return new RedisCommanderResourcePromiseImpl(this._withPersistentLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withLifetimeOfInternal(sourceBuilder) {
        sourceBuilder = isPromiseLike(sourceBuilder) ? await sourceBuilder : sourceBuilder;
        const rpcArgs = { builder: this._handle, sourceBuilder };
        const result = await this._client.invokeCapability('Aspire.Hosting/withLifetimeOf', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to match the lifetime of another resource.
     *
     * The resource lifetime is evaluated from `sourceBuilder` when the application model is prepared, so later lifetime
     * changes to the source resource are reflected by this resource.
     * @param sourceBuilder The resource builder whose lifetime should be used.
     * @returns The resource builder.
     */
    withLifetimeOf(sourceBuilder) {
        return new RedisCommanderResourcePromiseImpl(this._withLifetimeOfInternal(sourceBuilder), this._client);
    }
    /** @internal */
    async _withParentProcessLifetimeInternal(parentProcessId) {
        const rpcArgs = { builder: this._handle, parentProcessId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withParentProcessLifetime', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime that ends when a parent process exits.
     * @param parentProcessId The ID of the parent process to monitor.
     * @returns The resource builder.
     */
    withParentProcessLifetime(parentProcessId) {
        return new RedisCommanderResourcePromiseImpl(this._withParentProcessLifetimeInternal(parentProcessId), this._client);
    }
    /** @internal */
    async _withEnvironmentInternal(name, value) {
        value = isPromiseLike(value) ? await value : value;
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEnvironment', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /** Sets an environment variable */
    withEnvironment(name, value) {
        return new RedisCommanderResourcePromiseImpl(this._withEnvironmentInternal(name, value), this._client);
    }
    /** @internal */
    async _withEnvironmentCallbackInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new EnvironmentCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEnvironmentCallback', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Allows for the population of environment variables on a resource.
     * @param callback A callback that allows for deferred execution for computing many environment variables. This runs after resources have been allocated by the orchestrator and allows access to other resources to resolve computed data, e.g. connection strings, ports.
     * @returns The resource builder.
     */
    withEnvironmentCallback(callback) {
        return new RedisCommanderResourcePromiseImpl(this._withEnvironmentCallbackInternal(callback), this._client);
    }
    /** @internal */
    async _withArgsInternal(args) {
        const rpcArgs = { builder: this._handle, args };
        const result = await this._client.invokeCapability('Aspire.Hosting/withArgs', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Adds arguments to be passed to a resource that supports arguments when it is launched.
     * @param args The arguments to be passed to the resource when it is started.
     * @returns The resource builder.
     */
    withArgs(args) {
        return new RedisCommanderResourcePromiseImpl(this._withArgsInternal(args), this._client);
    }
    /** @internal */
    async _withArgsCallbackInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new CommandLineArgsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withArgsCallback', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Adds a callback to be executed with a list of command-line arguments when a resource is started.
     * @param callback A callback that allows for deferred execution for computing arguments. This runs after resources have been allocated by the orchestrator and allows access to other resources to resolve computed data, e.g. connection strings, ports.
     * @returns The resource builder.
     */
    withArgsCallback(callback) {
        return new RedisCommanderResourcePromiseImpl(this._withArgsCallbackInternal(callback), this._client);
    }
    /** @internal */
    async _withReferenceEnvironmentInternal(options) {
        const rpcArgs = { builder: this._handle, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withReferenceEnvironment', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Configures how information is injected into environment variables when the resource references other resources.
     * @param options Options controlling which reference information is emitted.
     * @returns The resource builder.
     */
    withReferenceEnvironment(options) {
        return new RedisCommanderResourcePromiseImpl(this._withReferenceEnvironmentInternal(options), this._client);
    }
    /** @internal */
    async _withReferenceInternal(source, connectionName, optional, name) {
        source = isPromiseLike(source) ? await source : source;
        const rpcArgs = { builder: this._handle, source };
        if (connectionName !== undefined)
            rpcArgs.connectionName = connectionName;
        if (optional !== undefined)
            rpcArgs.optional = optional;
        if (name !== undefined)
            rpcArgs.name = name;
        const result = await this._client.invokeCapability('Aspire.Hosting/withReference', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Adds a reference to another resource
     * @param options Additional options.
     */
    withReference(source, options) {
        const connectionName = options?.connectionName;
        const optional = options?.optional;
        const name = options?.name;
        return new RedisCommanderResourcePromiseImpl(this._withReferenceInternal(source, connectionName, optional, name), this._client);
    }
    /** @internal */
    async _withEndpointCallbackInternal(endpointName, callback, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpointCallback', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Updates a named endpoint via callback
     * @param options Additional options.
     */
    withEndpointCallback(endpointName, callback, options) {
        const createIfNotExists = options?.createIfNotExists;
        return new RedisCommanderResourcePromiseImpl(this._withEndpointCallbackInternal(endpointName, callback, createIfNotExists), this._client);
    }
    /** @internal */
    async _withHttpEndpointCallbackInternal(callback, name, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        if (name !== undefined)
            rpcArgs.name = name;
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpEndpointCallback', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Updates an HTTP endpoint via callback
     * @param options Additional options.
     */
    withHttpEndpointCallback(callback, options) {
        const name = options?.name;
        const createIfNotExists = options?.createIfNotExists;
        return new RedisCommanderResourcePromiseImpl(this._withHttpEndpointCallbackInternal(callback, name, createIfNotExists), this._client);
    }
    /** @internal */
    async _withHttpsEndpointCallbackInternal(callback, name, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        if (name !== undefined)
            rpcArgs.name = name;
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpsEndpointCallback', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Updates an HTTPS endpoint via callback
     * @param options Additional options.
     */
    withHttpsEndpointCallback(callback, options) {
        const name = options?.name;
        const createIfNotExists = options?.createIfNotExists;
        return new RedisCommanderResourcePromiseImpl(this._withHttpsEndpointCallbackInternal(callback, name, createIfNotExists), this._client);
    }
    /** @internal */
    async _withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (scheme !== undefined)
            rpcArgs.scheme = scheme;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        if (isExternal !== undefined)
            rpcArgs.isExternal = isExternal;
        if (protocol !== undefined)
            rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpoint', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Adds a network endpoint
     * @param options Additional options.
     * @returns The resource builder.
     */
    withEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const scheme = options?.scheme;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        const isExternal = options?.isExternal;
        const protocol = options?.protocol;
        return new RedisCommanderResourcePromiseImpl(this._withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol), this._client);
    }
    /** @internal */
    async _withEndpointProxySupportInternal(proxyEnabled) {
        const rpcArgs = { builder: this._handle, proxyEnabled };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpointProxySupport', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Set whether a resource can use proxied endpoints or whether they should be disabled for all endpoints belonging to the resource. If set to `false`, endpoints belonging to the resource will ignore the configured proxy settings and run proxy-less.
     *
     * This method is intended to support scenarios with persistent lifetime resources where it is desirable for the resource to be accessible over the same
     * port whether the Aspire application is running or not. Proxied endpoints bind ports that are only accessible while the Aspire application is running.
     * The user needs to be careful to ensure that endpoints are using unique ports when disabling proxy support as by default for proxy-less
     * endpoints, Aspire will allocate the target port as the host port, which will increase the chance of port conflicts.
     * @param proxyEnabled Should endpoints for the resource support using a proxy?
     * @returns The resource builder.
     */
    withEndpointProxySupport(proxyEnabled) {
        return new RedisCommanderResourcePromiseImpl(this._withEndpointProxySupportInternal(proxyEnabled), this._client);
    }
    /** @internal */
    async _withHttpEndpointInternal(port, targetPort, name, env, isProxied) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpEndpoint', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP endpoint
     *
     * If an endpoint with the same name already exists on the resource, the existing endpoint is updated
     * with any non-null parameter values. Parameters left as `null` will not modify the existing endpoint's values.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new RedisCommanderResourcePromiseImpl(this._withHttpEndpointInternal(port, targetPort, name, env, isProxied), this._client);
    }
    /** @internal */
    async _withHttpsEndpointInternal(port, targetPort, name, env, isProxied) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpsEndpoint', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTPS endpoint
     *
     * If an endpoint with the same name already exists on the resource, the existing endpoint is updated
     * with any non-null parameter values. Parameters left as `null` will not modify the existing endpoint's values.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpsEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new RedisCommanderResourcePromiseImpl(this._withHttpsEndpointInternal(port, targetPort, name, env, isProxied), this._client);
    }
    /** @internal */
    async _withExternalHttpEndpointsInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExternalHttpEndpoints', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Marks existing http or https endpoints on a resource as external.
     * @returns The resource builder.
     */
    withExternalHttpEndpoints() {
        return new RedisCommanderResourcePromiseImpl(this._withExternalHttpEndpointsInternal(), this._client);
    }
    /**
     * Gets an endpoint reference
     * @param name The name of the endpoint.
     * @returns An `EndpointReference` that can be used to resolve the address of the endpoint after resource allocation has occurred.
     */
    getEndpoint(name) {
        const promise = (async () => {
            const rpcArgs = { builder: this._handle, name };
            const handle = await this._client.invokeCapability('Aspire.Hosting/getEndpoint', rpcArgs);
            return new EndpointReferenceImpl(handle, this._client);
        })();
        return new EndpointReferencePromiseImpl(promise, this._client);
    }
    /** @internal */
    async _asHttp2ServiceInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/asHttp2Service', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to mark all endpoints' transport as HTTP/2. This is useful for HTTP/2 services that need prior knowledge.
     * @returns The resource builder.
     */
    asHttp2Service() {
        return new RedisCommanderResourcePromiseImpl(this._asHttp2ServiceInternal(), this._client);
    }
    /** @internal */
    async _withUrlsInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new ResourceUrlsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrls', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to customize the URLs displayed for the resource.
     * @param callback The callback that will customize URLs for the resource.
     * @returns The resource builder.
     */
    withUrls(callback) {
        return new RedisCommanderResourcePromiseImpl(this._withUrlsInternal(callback), this._client);
    }
    /** @internal */
    async _withUrlInternal(url, displayText) {
        const rpcArgs = { builder: this._handle, url };
        if (displayText !== undefined)
            rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrl', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Adds or modifies displayed URLs
     * @param options Additional options.
     */
    withUrl(url, options) {
        const displayText = options?.displayText;
        return new RedisCommanderResourcePromiseImpl(this._withUrlInternal(url, displayText), this._client);
    }
    /** @internal */
    async _withUrlForEndpointInternal(endpointName, callback) {
        const callbackId = registerCallback(async (objData) => {
            const obj = wrapIfHandle(objData);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrlForEndpoint', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to update the URL displayed for the endpoint with the specified name.
     * @param endpointName The name of the endpoint to customize the URL for.
     * @param callback The callback that will customize the URL.
     * @returns The resource builder.
     */
    withUrlForEndpoint(endpointName, callback) {
        return new RedisCommanderResourcePromiseImpl(this._withUrlForEndpointInternal(endpointName, callback), this._client);
    }
    /** @internal */
    async _excludeFromManifestInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromManifest', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Excludes a resource from being published to the manifest.
     * @returns The resource builder.
     */
    excludeFromManifest() {
        return new RedisCommanderResourcePromiseImpl(this._excludeFromManifestInternal(), this._client);
    }
    /** @internal */
    async _waitForInternal(dependency, waitBehavior) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (waitBehavior !== undefined)
            rpcArgs.waitBehavior = waitBehavior;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitFor', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Waits for another resource to be ready
     * @param options Additional options.
     */
    waitFor(dependency, options) {
        const waitBehavior = options?.waitBehavior;
        return new RedisCommanderResourcePromiseImpl(this._waitForInternal(dependency, waitBehavior), this._client);
    }
    /** @internal */
    async _waitForStartInternal(dependency, waitBehavior) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (waitBehavior !== undefined)
            rpcArgs.waitBehavior = waitBehavior;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitForStart', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Waits for another resource to start
     * @param options Additional options.
     */
    waitForStart(dependency, options) {
        const waitBehavior = options?.waitBehavior;
        return new RedisCommanderResourcePromiseImpl(this._waitForStartInternal(dependency, waitBehavior), this._client);
    }
    /** @internal */
    async _withExplicitStartInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExplicitStart', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Prevents resource from starting automatically
     * @returns The resource builder.
     */
    withExplicitStart() {
        return new RedisCommanderResourcePromiseImpl(this._withExplicitStartInternal(), this._client);
    }
    /** @internal */
    async _waitForCompletionInternal(dependency, exitCode) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitForResourceCompletion', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Waits for the dependency resource to enter the Exited or Finished state before starting the resource.
     * @param dependency The resource builder for the dependency resource.
     * @param options Additional options.
     * @returns The resource builder.
     */
    waitForCompletion(dependency, options) {
        const exitCode = options?.exitCode;
        return new RedisCommanderResourcePromiseImpl(this._waitForCompletionInternal(dependency, exitCode), this._client);
    }
    /** @internal */
    async _withHealthCheckInternal(key) {
        const rpcArgs = { builder: this._handle, key };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHealthCheck', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Adds a health check by key
     * @param key The key for the health check.
     * @returns The resource builder.
     */
    withHealthCheck(key) {
        return new RedisCommanderResourcePromiseImpl(this._withHealthCheckInternal(key), this._client);
    }
    /** @internal */
    async _withHttpHealthCheckInternal(path, statusCode, endpointName) {
        const rpcArgs = { builder: this._handle };
        if (path !== undefined)
            rpcArgs.path = path;
        if (statusCode !== undefined)
            rpcArgs.statusCode = statusCode;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpHealthCheck', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Adds a health check to the resource which is mapped to a specific endpoint.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpHealthCheck(options) {
        const path = options?.path;
        const statusCode = options?.statusCode;
        const endpointName = options?.endpointName;
        return new RedisCommanderResourcePromiseImpl(this._withHttpHealthCheckInternal(path, statusCode, endpointName), this._client);
    }
    /** @internal */
    async _withCommandInternal(name, displayName, executeCommand, commandOptions) {
        const executeCommandId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined)
            rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability('Aspire.Hosting/withCommand', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Adds a resource command
     *
     * The `WithCommand` method is used to add commands to the resource. Commands are displayed in the dashboard
     * and can be executed by a user using the dashboard UI.
     * When a command is executed, the `executeCommand` callback is called and is run inside the Aspire host.
     * @param name The name of the command. The name uniquely identifies the command.
     * @param displayName The display name visible in UI.
     * @param executeCommand A callback that is executed when the command is executed. The callback is run inside the Aspire host. The callback result is used to indicate success or failure in the UI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withCommand(name, displayName, executeCommand, options) {
        const commandOptions = options?.commandOptions;
        return new RedisCommanderResourcePromiseImpl(this._withCommandInternal(name, displayName, executeCommand, commandOptions), this._client);
    }
    /** @internal */
    async _withProcessCommandInternal(commandName, displayName, options) {
        const rpcArgs = { builder: this._handle, commandName, displayName, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommand', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /** Adds a command to the resource that starts a local process when invoked. */
    withProcessCommand(commandName, displayName, options) {
        return new RedisCommanderResourcePromiseImpl(this._withProcessCommandInternal(commandName, displayName, options), this._client);
    }
    /** @internal */
    async _withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options) {
        const createProcessSpecId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await createProcessSpec(arg);
        });
        const rpcArgs = { builder: this._handle, commandName, displayName, createProcessSpec: createProcessSpecId };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommandFactory', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Adds a command to the resource that starts a local process created by a callback when invoked.
     * @param options Additional options.
     */
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new RedisCommanderResourcePromiseImpl(this._withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options), this._client);
    }
    /** @internal */
    async _withHttpCommandInternal(path, displayName, options) {
        const rpcArgs = { builder: this._handle, path, displayName };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpCommand', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP resource command
     * @param options Additional options.
     */
    withHttpCommand(path, displayName, options) {
        return new RedisCommanderResourcePromiseImpl(this._withHttpCommandInternal(path, displayName, options), this._client);
    }
    /** @internal */
    async _withDeveloperCertificateTrustInternal(trust) {
        const rpcArgs = { builder: this._handle, trust };
        const result = await this._client.invokeCapability('Aspire.Hosting/withDeveloperCertificateTrust', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Indicates whether developer certificates should be treated as trusted certificate authorities for the resource at run time. Currently this indicates trust for the ASP.NET Core developer certificate. The developer certificate will only be trusted when running in local development scenarios; in publish mode resources will use their default certificate trust.
     *
     * Disable trust for app host managed developer certificate(s) for a container resource.
     * ```
     * var container = builder.AddContainer("my-service", "my-service:latest")
     * .WithDeveloperCertificateTrust(false);
     * ```
     * Disable automatic trust for app host managed developer certificate(s), but explicitly enable it for a specific resource.
     * ```
     * var builder = DistributedApplication.CreateBuilder(new DistributedApplicationOptions()
     * {
     * Args = args,
     * TrustDeveloperCertificate = false,
     * });
     * var project = builder.AddProject<MyService>("my-service")
     * .WithDeveloperCertificateTrust(true);
     * ```
     * @param trust Indicates whether the developer certificate should be treated as trusted.
     * @returns The resource builder.
     */
    withDeveloperCertificateTrust(trust) {
        return new RedisCommanderResourcePromiseImpl(this._withDeveloperCertificateTrustInternal(trust), this._client);
    }
    /** @internal */
    async _withCertificateTrustScopeInternal(scope) {
        const rpcArgs = { builder: this._handle, scope };
        const result = await this._client.invokeCapability('Aspire.Hosting/withCertificateTrustScope', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Sets the certificate trust scope
     *
     * The default scope if not overridden is `Append` which means that custom certificate
     * authorities should be appended to the default trusted certificate authorities for the resource. Setting the scope to
     * `Override` indicates the set of certificates in referenced
     * `CertificateAuthorityCollection` (and optionally Aspire developer certificiates) should be used as the
     * exclusive source of trust for a resource.
     * In all cases, this is a best effort implementation as not all resources support full customization of certificate
     * trust.
     * Set the scope for custom certificate authorities to override the default trusted certificate authorities for a container resource.
     * ```
     * var caCollection = builder.AddCertificateAuthorityCollection("my-cas")
     * .WithCertificate(new X509Certificate2("my-ca.pem"));
     * var container = builder.AddContainer("my-service", "my-service:latest")
     * .WithCertificateAuthorityCollection(caCollection)
     * .WithCertificateTrustScope(CertificateTrustScope.Override);
     * ```
     * @param scope The scope to apply to custom certificate authorities associated with the resource.
     * @returns The resource builder.
     */
    withCertificateTrustScope(scope) {
        return new RedisCommanderResourcePromiseImpl(this._withCertificateTrustScopeInternal(scope), this._client);
    }
    /** @internal */
    async _withHttpsDeveloperCertificateInternal(password) {
        password = isPromiseLike(password) ? await password : password;
        const rpcArgs = { builder: this._handle };
        if (password !== undefined)
            rpcArgs.password = password;
        const result = await this._client.invokeCapability('Aspire.Hosting/withParameterHttpsDeveloperCertificate', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Indicates that a resource should use the developer certificate key pair for HTTPS endpoints at run time. Currently this indicates use of the ASP.NET Core developer certificate. The developer certificate will only be used when running in local development scenarios; in publish mode resources will use their default certificate configuration.
     *
     * Use the developer certificate for HTTPS/TLS endpoints on a container resource:
     * ```
     * builder.AddContainer("my-service", "my-image")
     * .WithHttpsDeveloperCertificate()
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpsDeveloperCertificate(options) {
        let password = options?.password;
        return new RedisCommanderResourcePromiseImpl(this._withHttpsDeveloperCertificateInternal(password), this._client);
    }
    /** @internal */
    async _withoutHttpsCertificateInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withoutHttpsCertificate', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Disable HTTPS/TLS server certificate configuration for the resource. No HTTPS/TLS termination configuration will be applied.
     *
     * Disable HTTPS certificate configuration for a Redis resource:
     * ```
     * var redis = builder.AddRedis("cache")
     * .WithoutHttpsCertificate();
     * ```
     * @returns The resource builder.
     */
    withoutHttpsCertificate() {
        return new RedisCommanderResourcePromiseImpl(this._withoutHttpsCertificateInternal(), this._client);
    }
    /** @internal */
    async _withRelationshipInternal(resourceBuilder, type) {
        resourceBuilder = isPromiseLike(resourceBuilder) ? await resourceBuilder : resourceBuilder;
        const rpcArgs = { builder: this._handle, resourceBuilder, type };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderRelationship', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Adds a relationship to another resource using its builder.
     * @param resourceBuilder The resource builder that the relationship is to.
     * @param type The relationship type.
     * @returns A resource builder.
     */
    withRelationship(resourceBuilder, type) {
        return new RedisCommanderResourcePromiseImpl(this._withRelationshipInternal(resourceBuilder, type), this._client);
    }
    /** @internal */
    async _withParentRelationshipInternal(parent) {
        parent = isPromiseLike(parent) ? await parent : parent;
        const rpcArgs = { builder: this._handle, parent };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderParentRelationship', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Sets the parent relationship
     * @param parent The parent of `builder`.
     * @returns A resource builder.
     */
    withParentRelationship(parent) {
        return new RedisCommanderResourcePromiseImpl(this._withParentRelationshipInternal(parent), this._client);
    }
    /** @internal */
    async _withChildRelationshipInternal(child) {
        child = isPromiseLike(child) ? await child : child;
        const rpcArgs = { builder: this._handle, child };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderChildRelationship', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Sets a child relationship
     * @param child The child of `builder`.
     * @returns A resource builder.
     */
    withChildRelationship(child) {
        return new RedisCommanderResourcePromiseImpl(this._withChildRelationshipInternal(child), this._client);
    }
    /** @internal */
    async _withIconNameInternal(iconName, iconVariant) {
        const rpcArgs = { builder: this._handle, iconName };
        if (iconVariant !== undefined)
            rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability('Aspire.Hosting/withIconName', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Specifies the icon to use when displaying the resource in the dashboard.
     * @param iconName The name of the FluentUI icon to use. See https://aka.ms/fluentui-system-icons for available icons.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withIconName(iconName, options) {
        const iconVariant = options?.iconVariant;
        return new RedisCommanderResourcePromiseImpl(this._withIconNameInternal(iconName, iconVariant), this._client);
    }
    /** @internal */
    async _withComputeEnvironmentInternal(computeEnvironmentResource) {
        computeEnvironmentResource = isPromiseLike(computeEnvironmentResource) ? await computeEnvironmentResource : computeEnvironmentResource;
        const rpcArgs = { builder: this._handle, computeEnvironmentResource };
        const result = await this._client.invokeCapability('Aspire.Hosting/withComputeEnvironment', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Configures the compute environment for the compute resource.
     *
     * This method allows associating a specific compute environment with the compute resource.
     * @param computeEnvironmentResource The compute environment resource to associate with the compute resource.
     * @returns The resource builder.
     */
    withComputeEnvironment(computeEnvironmentResource) {
        return new RedisCommanderResourcePromiseImpl(this._withComputeEnvironmentInternal(computeEnvironmentResource), this._client);
    }
    /** @internal */
    async _withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName) {
        const rpcArgs = { builder: this._handle, probeType };
        if (path !== undefined)
            rpcArgs.path = path;
        if (initialDelaySeconds !== undefined)
            rpcArgs.initialDelaySeconds = initialDelaySeconds;
        if (periodSeconds !== undefined)
            rpcArgs.periodSeconds = periodSeconds;
        if (timeoutSeconds !== undefined)
            rpcArgs.timeoutSeconds = timeoutSeconds;
        if (failureThreshold !== undefined)
            rpcArgs.failureThreshold = failureThreshold;
        if (successThreshold !== undefined)
            rpcArgs.successThreshold = successThreshold;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpProbe', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP health probe to the resource
     * @param options Additional options.
     */
    withHttpProbe(probeType, options) {
        const path = options?.path;
        const initialDelaySeconds = options?.initialDelaySeconds;
        const periodSeconds = options?.periodSeconds;
        const timeoutSeconds = options?.timeoutSeconds;
        const failureThreshold = options?.failureThreshold;
        const successThreshold = options?.successThreshold;
        const endpointName = options?.endpointName;
        return new RedisCommanderResourcePromiseImpl(this._withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName), this._client);
    }
    /** @internal */
    async _excludeFromMcpInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromMcp', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Exclude the resource from MCP operations using the Aspire MCP server. The resource is excluded from results that return resources, console logs and telemetry.
     * @returns The resource builder.
     */
    excludeFromMcp() {
        return new RedisCommanderResourcePromiseImpl(this._excludeFromMcpInternal(), this._client);
    }
    /** @internal */
    async _withHiddenInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHidden', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists
     *
     * Use this method to hide resources that are implementation details and should never be displayed by default.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @returns The resource builder.
     */
    withHidden() {
        return new RedisCommanderResourcePromiseImpl(this._withHiddenInternal(), this._client);
    }
    /** @internal */
    async _withHiddenOnCompletionInternal(exitCode, exitCodes) {
        const rpcArgs = { builder: this._handle };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        if (exitCodes !== undefined)
            rpcArgs.exitCodes = exitCodes;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHiddenOnCompletion', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists after successful completion
     *
     * This method is useful for one-off resources such as setup scripts, migrations, or build steps that should remain visible while running
     * and then be hidden after successful completion.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHiddenOnCompletion(options) {
        const exitCode = options?.exitCode;
        const exitCodes = options?.exitCodes;
        return new RedisCommanderResourcePromiseImpl(this._withHiddenOnCompletionInternal(exitCode, exitCodes), this._client);
    }
    /** @internal */
    async _withImagePushOptionsInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ContainerImagePushOptionsCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImagePushOptions', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Adds an asynchronous callback to configure container image push options for the resource.
     *
     * This method allows customization of how container images are named and tagged when pushed to a registry using an asynchronous callback.
     * Use this overload when the callback needs to perform asynchronous operations such as retrieving configuration values from external sources.
     * The callback receives a `ContainerImagePushOptionsCallbackContext` that provides access to the resource
     * and the `ContainerImagePushOptions` that can be modified.
     * Multiple callbacks can be registered on the same resource, and they will be invoked in the order they were added.
     * @param callback The asynchronous callback to configure push options.
     * @returns The resource builder.
     */
    withImagePushOptions(callback) {
        return new RedisCommanderResourcePromiseImpl(this._withImagePushOptionsInternal(callback), this._client);
    }
    /** @internal */
    async _withRemoteImageNameInternal(remoteImageName) {
        const rpcArgs = { builder: this._handle, remoteImageName };
        const result = await this._client.invokeCapability('Aspire.Hosting/withRemoteImageName', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Sets the remote image name (without registry endpoint or tag) for container push operations.
     *
     * Use this with `withRemoteImageTag` to fully customize the image reference used for container push operations.
     * @param remoteImageName The remote image name (e.g., "myapp" or "myorg/myapp").
     * @returns The resource builder.
     */
    withRemoteImageName(remoteImageName) {
        return new RedisCommanderResourcePromiseImpl(this._withRemoteImageNameInternal(remoteImageName), this._client);
    }
    /** @internal */
    async _withRemoteImageTagInternal(remoteImageTag) {
        const rpcArgs = { builder: this._handle, remoteImageTag };
        const result = await this._client.invokeCapability('Aspire.Hosting/withRemoteImageTag', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Sets the remote image tag for container push operations.
     *
     * Use this with `withRemoteImageName` to fully customize the image reference used for container push operations.
     * @param remoteImageTag The remote image tag (e.g., "latest", "v1.0.0").
     * @returns The resource builder.
     */
    withRemoteImageTag(remoteImageTag) {
        return new RedisCommanderResourcePromiseImpl(this._withRemoteImageTagInternal(remoteImageTag), this._client);
    }
    /** @internal */
    async _withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new PipelineStepContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined)
            rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined)
            rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined)
            rpcArgs.tags = tags;
        if (description !== undefined)
            rpcArgs.description = description;
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineStepFactory', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Adds a pipeline step to the resource that will be executed during deployment.
     * @param stepName The unique name of the pipeline step.
     * @param callback The callback to execute when the step runs.
     * @param options Additional options.
     * @returns The resource builder for chaining.
     */
    withPipelineStepFactory(stepName, callback, options) {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new RedisCommanderResourcePromiseImpl(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description), this._client);
    }
    /** @internal */
    async _withPipelineConfigurationInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new PipelineConfigurationContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineConfiguration', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to be executed during the pipeline configuration phase, allowing modification of step dependencies and relationships.
     * @param callback The callback function to execute during the configuration phase.
     * @returns The resource builder for chaining.
     */
    withPipelineConfiguration(callback) {
        return new RedisCommanderResourcePromiseImpl(this._withPipelineConfigurationInternal(callback), this._client);
    }
    /** @internal */
    async _withVolumeInternal(target, name, isReadOnly) {
        const rpcArgs = { resource: this._handle, target };
        if (name !== undefined)
            rpcArgs.name = name;
        if (isReadOnly !== undefined)
            rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability('Aspire.Hosting/withVolume', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Adds a volume to a container resource.
     *
     * Volumes persist data across container restarts. Named volumes are managed
     * by Docker/Podman and stored in a system-managed location.
     * Why this wrapper exists: The original `ContainerResourceBuilderExtensions.WithVolume`
     * has parameter order `(name?, target, isReadOnly)` where the optional `name` comes first.
     * This wrapper reorders parameters to `(target, name?, isReadOnly)` so the required `target`
     * parameter comes first, providing a better API for polyglot consumers.
     * @param target The mount path inside the container.
     * @param options Additional options.
     * @returns The same resource builder handle for chaining.
     */
    withVolume(target, options) {
        const name = options?.name;
        const isReadOnly = options?.isReadOnly;
        return new RedisCommanderResourcePromiseImpl(this._withVolumeInternal(target, name, isReadOnly), this._client);
    }
    /**
     * Gets the name of the resource from a builder.
     *
     * Why this wrapper exists: This capability accesses a nested property
     * (`resource.Resource.Name`) which requires a wrapper method. There is no single
     * .NET method that returns just the resource name that could be annotated directly.
     * @returns The resource name.
     */
    async getResourceName() {
        const rpcArgs = { resource: this._handle };
        return await this._client.invokeCapability('Aspire.Hosting/getResourceName', rpcArgs);
    }
    /** @internal */
    async _onBeforeResourceStartedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new BeforeResourceStartedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onBeforeResourceStarted', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the BeforeResourceStarted event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onBeforeResourceStarted(callback) {
        return new RedisCommanderResourcePromiseImpl(this._onBeforeResourceStartedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceStoppedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceStoppedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceStopped', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceStopped event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceStopped(callback) {
        return new RedisCommanderResourcePromiseImpl(this._onResourceStoppedInternal(callback), this._client);
    }
    /** @internal */
    async _onInitializeResourceInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new InitializeResourceEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onInitializeResource', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the InitializeResource event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onInitializeResource(callback) {
        return new RedisCommanderResourcePromiseImpl(this._onInitializeResourceInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceEndpointsAllocatedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceEndpointsAllocatedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceEndpointsAllocated', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceEndpointsAllocated event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceEndpointsAllocated(callback) {
        return new RedisCommanderResourcePromiseImpl(this._onResourceEndpointsAllocatedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceReadyInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceReadyEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceReady', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceReady event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceReady(callback) {
        return new RedisCommanderResourcePromiseImpl(this._onResourceReadyInternal(callback), this._client);
    }
    /**
     * Creates an execution configuration builder for the specified resource.
     * @returns The execution configuration builder.
     */
    createExecutionConfiguration() {
        const promise = (async () => {
            const rpcArgs = { resource: this._handle };
            const handle = await this._client.invokeCapability('Aspire.Hosting/createExecutionConfiguration', rpcArgs);
            return new ExecutionConfigurationBuilderImpl(handle, this._client);
        })();
        return new ExecutionConfigurationBuilderPromiseImpl(promise, this._client);
    }
    /** @internal */
    async _withHostPortInternal(port) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        const result = await this._client.invokeCapability('Aspire.Hosting.Redis/withRedisCommanderHostPort', rpcArgs);
        return new RedisCommanderResourceImpl(result, this._client);
    }
    /**
     * Configures the host port that the Redis Commander resource is exposed on instead of using randomly assigned port.
     * @param options Additional options.
     * @returns The resource builder for RedisCommander.
     */
    withHostPort(options) {
        const port = options?.port;
        return new RedisCommanderResourcePromiseImpl(this._withHostPortInternal(port), this._client);
    }
}
/**
 * Thenable wrapper for RedisCommanderResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
class RedisCommanderResourcePromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    withContainerRegistry(registry) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withContainerRegistry(registry)), this._client);
    }
    withBindMount(source, target, options) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withBindMount(source, target, options)), this._client);
    }
    withEntrypoint(entrypoint) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withEntrypoint(entrypoint)), this._client);
    }
    withImageTag(tag) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withImageTag(tag)), this._client);
    }
    withImageRegistry(registry) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withImageRegistry(registry)), this._client);
    }
    withImage(image, options) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withImage(image, options)), this._client);
    }
    withImageSHA256(sha256) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withImageSHA256(sha256)), this._client);
    }
    withContainerRuntimeArgs(args) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withContainerRuntimeArgs(args)), this._client);
    }
    withLifetime(lifetime) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withLifetime(lifetime)), this._client);
    }
    withImagePullPolicy(pullPolicy) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withImagePullPolicy(pullPolicy)), this._client);
    }
    publishAsContainer() {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.publishAsContainer()), this._client);
    }
    withDockerfile(contextPath, options) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withDockerfile(contextPath, options)), this._client);
    }
    withDockerfileFactory(contextPath, dockerfileFactory, options) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileFactory(contextPath, dockerfileFactory, options)), this._client);
    }
    withContainerName(name) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withContainerName(name)), this._client);
    }
    withBuildArg(name, value) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withBuildArg(name, value)), this._client);
    }
    withBuildSecret(name, value) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withBuildSecret(name, value)), this._client);
    }
    withContainerCertificatePaths(options) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withContainerCertificatePaths(options)), this._client);
    }
    withDockerfileBuilder(contextPath, callback, options) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileBuilder(contextPath, callback, options)), this._client);
    }
    withDockerfileBaseImage(options) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileBaseImage(options)), this._client);
    }
    withContainerNetworkAlias(alias) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withContainerNetworkAlias(alias)), this._client);
    }
    withMcpServer(options) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withMcpServer(options)), this._client);
    }
    withOtlpExporter(options) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withOtlpExporter(options)), this._client);
    }
    publishAsConnectionString() {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.publishAsConnectionString()), this._client);
    }
    withRequiredCommand(command, options) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withRequiredCommand(command, options)), this._client);
    }
    withSessionLifetime() {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withSessionLifetime()), this._client);
    }
    withPersistentLifetime() {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withPersistentLifetime()), this._client);
    }
    withLifetimeOf(sourceBuilder) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withLifetimeOf(sourceBuilder)), this._client);
    }
    withParentProcessLifetime(parentProcessId) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withParentProcessLifetime(parentProcessId)), this._client);
    }
    withEnvironment(name, value) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withEnvironment(name, value)), this._client);
    }
    withEnvironmentCallback(callback) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withEnvironmentCallback(callback)), this._client);
    }
    withArgs(args) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withArgs(args)), this._client);
    }
    withArgsCallback(callback) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withArgsCallback(callback)), this._client);
    }
    withReferenceEnvironment(options) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withReferenceEnvironment(options)), this._client);
    }
    withReference(source, options) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withReference(source, options)), this._client);
    }
    withEndpointCallback(endpointName, callback, options) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withEndpointCallback(endpointName, callback, options)), this._client);
    }
    withHttpEndpointCallback(callback, options) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withHttpEndpointCallback(callback, options)), this._client);
    }
    withHttpsEndpointCallback(callback, options) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withHttpsEndpointCallback(callback, options)), this._client);
    }
    withEndpoint(options) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withEndpoint(options)), this._client);
    }
    withEndpointProxySupport(proxyEnabled) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withEndpointProxySupport(proxyEnabled)), this._client);
    }
    withHttpEndpoint(options) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withHttpEndpoint(options)), this._client);
    }
    withHttpsEndpoint(options) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withHttpsEndpoint(options)), this._client);
    }
    withExternalHttpEndpoints() {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withExternalHttpEndpoints()), this._client);
    }
    getEndpoint(name) {
        return new EndpointReferencePromiseImpl(this._promise.then(obj => obj.getEndpoint(name)), this._client);
    }
    asHttp2Service() {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.asHttp2Service()), this._client);
    }
    withUrls(callback) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withUrls(callback)), this._client);
    }
    withUrl(url, options) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withUrl(url, options)), this._client);
    }
    withUrlForEndpoint(endpointName, callback) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)), this._client);
    }
    excludeFromManifest() {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.excludeFromManifest()), this._client);
    }
    waitFor(dependency, options) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.waitFor(dependency, options)), this._client);
    }
    waitForStart(dependency, options) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.waitForStart(dependency, options)), this._client);
    }
    withExplicitStart() {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withExplicitStart()), this._client);
    }
    waitForCompletion(dependency, options) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.waitForCompletion(dependency, options)), this._client);
    }
    withHealthCheck(key) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withHealthCheck(key)), this._client);
    }
    withHttpHealthCheck(options) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withHttpHealthCheck(options)), this._client);
    }
    withCommand(name, displayName, executeCommand, options) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)), this._client);
    }
    withProcessCommand(commandName, displayName, options) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommand(commandName, displayName, options)), this._client);
    }
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommandFactory(commandName, displayName, createProcessSpec, options)), this._client);
    }
    withHttpCommand(path, displayName, options) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withHttpCommand(path, displayName, options)), this._client);
    }
    withDeveloperCertificateTrust(trust) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withDeveloperCertificateTrust(trust)), this._client);
    }
    withCertificateTrustScope(scope) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withCertificateTrustScope(scope)), this._client);
    }
    withHttpsDeveloperCertificate(options) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withHttpsDeveloperCertificate(options)), this._client);
    }
    withoutHttpsCertificate() {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withoutHttpsCertificate()), this._client);
    }
    withRelationship(resourceBuilder, type) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withRelationship(resourceBuilder, type)), this._client);
    }
    withParentRelationship(parent) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withParentRelationship(parent)), this._client);
    }
    withChildRelationship(child) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withChildRelationship(child)), this._client);
    }
    withIconName(iconName, options) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withIconName(iconName, options)), this._client);
    }
    withComputeEnvironment(computeEnvironmentResource) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withComputeEnvironment(computeEnvironmentResource)), this._client);
    }
    withHttpProbe(probeType, options) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withHttpProbe(probeType, options)), this._client);
    }
    excludeFromMcp() {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.excludeFromMcp()), this._client);
    }
    withHidden() {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withHidden()), this._client);
    }
    withHiddenOnCompletion(options) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withHiddenOnCompletion(options)), this._client);
    }
    withImagePushOptions(callback) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withImagePushOptions(callback)), this._client);
    }
    withRemoteImageName(remoteImageName) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withRemoteImageName(remoteImageName)), this._client);
    }
    withRemoteImageTag(remoteImageTag) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withRemoteImageTag(remoteImageTag)), this._client);
    }
    withPipelineStepFactory(stepName, callback, options) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)), this._client);
    }
    withPipelineConfiguration(callback) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withPipelineConfiguration(callback)), this._client);
    }
    withVolume(target, options) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withVolume(target, options)), this._client);
    }
    getResourceName() {
        return this._promise.then(obj => obj.getResourceName());
    }
    onBeforeResourceStarted(callback) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.onBeforeResourceStarted(callback)), this._client);
    }
    onResourceStopped(callback) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.onResourceStopped(callback)), this._client);
    }
    onInitializeResource(callback) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.onInitializeResource(callback)), this._client);
    }
    onResourceEndpointsAllocated(callback) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.onResourceEndpointsAllocated(callback)), this._client);
    }
    onResourceReady(callback) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.onResourceReady(callback)), this._client);
    }
    createExecutionConfiguration() {
        return new ExecutionConfigurationBuilderPromiseImpl(this._promise.then(obj => obj.createExecutionConfiguration()), this._client);
    }
    withHostPort(options) {
        return new RedisCommanderResourcePromiseImpl(this._promise.then(obj => obj.withHostPort(options)), this._client);
    }
}
// ============================================================================
// RedisInsightResourceImpl
// ============================================================================
class RedisInsightResourceImpl extends ResourceBuilderBase {
    constructor(handle, client) {
        super(handle, client);
    }
    /** @internal */
    async _withContainerRegistryInternal(registry) {
        registry = isPromiseLike(registry) ? await registry : registry;
        const rpcArgs = { builder: this._handle, registry };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerRegistry', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Configures the resource to use the specified container registry for container image operations.
     *
     * This method adds a `ContainerRegistryReferenceAnnotation` to the resource,
     * indicating that the resource should use the specified container registry for container image operations.
     * @param registry The container registry resource builder.
     * @returns The resource builder for chaining.
     */
    withContainerRegistry(registry) {
        return new RedisInsightResourcePromiseImpl(this._withContainerRegistryInternal(registry), this._client);
    }
    /** @internal */
    async _withBindMountInternal(source, target, isReadOnly) {
        const rpcArgs = { builder: this._handle, source, target };
        if (isReadOnly !== undefined)
            rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability('Aspire.Hosting/withBindMount', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Adds a bind mount to a container resource.
     * @param source The source path of the mount. This is the path to the file or directory on the host, relative to the app host project directory.
     * @param target The target path where the file or directory is mounted in the container.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withBindMount(source, target, options) {
        const isReadOnly = options?.isReadOnly;
        return new RedisInsightResourcePromiseImpl(this._withBindMountInternal(source, target, isReadOnly), this._client);
    }
    /** @internal */
    async _withEntrypointInternal(entrypoint) {
        const rpcArgs = { builder: this._handle, entrypoint };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEntrypoint', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Sets the Entrypoint for the container.
     * @param entrypoint The new entrypoint for the container.
     * @returns The resource builder.
     */
    withEntrypoint(entrypoint) {
        return new RedisInsightResourcePromiseImpl(this._withEntrypointInternal(entrypoint), this._client);
    }
    /** @internal */
    async _withImageTagInternal(tag) {
        const rpcArgs = { builder: this._handle, tag };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImageTag', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Allows overriding the image tag on a container.
     * @param tag Tag value.
     * @returns The resource builder.
     */
    withImageTag(tag) {
        return new RedisInsightResourcePromiseImpl(this._withImageTagInternal(tag), this._client);
    }
    /** @internal */
    async _withImageRegistryInternal(registry) {
        const rpcArgs = { builder: this._handle, registry };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImageRegistry', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Allows overriding the image registry on a container.
     * @param registry Registry value.
     * @returns The resource builder.
     */
    withImageRegistry(registry) {
        return new RedisInsightResourcePromiseImpl(this._withImageRegistryInternal(registry), this._client);
    }
    /** @internal */
    async _withImageInternal(image, tag) {
        const rpcArgs = { builder: this._handle, image };
        if (tag !== undefined)
            rpcArgs.tag = tag;
        const result = await this._client.invokeCapability('Aspire.Hosting/withImage', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Allows overriding the image on a container.
     * @param image Image value.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withImage(image, options) {
        const tag = options?.tag;
        return new RedisInsightResourcePromiseImpl(this._withImageInternal(image, tag), this._client);
    }
    /** @internal */
    async _withImageSHA256Internal(sha256) {
        const rpcArgs = { builder: this._handle, sha256 };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImageSHA256', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Allows setting the image to a specific sha256 on a container.
     * @param sha256 Registry value.
     * @returns The resource builder.
     */
    withImageSHA256(sha256) {
        return new RedisInsightResourcePromiseImpl(this._withImageSHA256Internal(sha256), this._client);
    }
    /** @internal */
    async _withContainerRuntimeArgsInternal(args) {
        const rpcArgs = { builder: this._handle, args };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerRuntimeArgs', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Adds a callback to be executed with a list of arguments to add to the container runtime run command when a container resource is started.
     * @param args The arguments to be passed to the container runtime run command when the container resource is started.
     * @returns The resource builder.
     */
    withContainerRuntimeArgs(args) {
        return new RedisInsightResourcePromiseImpl(this._withContainerRuntimeArgsInternal(args), this._client);
    }
    /** @internal */
    async _withLifetimeInternal(lifetime) {
        const rpcArgs = { builder: this._handle, lifetime };
        const result = await this._client.invokeCapability('Aspire.Hosting/withLifetime', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Sets the lifetime behavior of the container resource.
     *
     * Prefer `WithPersistentLifetime``1` or
     * `WithSessionLifetime``1` for new code.
     * Marking a container resource to have a `Persistent` lifetime.
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddContainer("mycontainer", "myimage")
     * .WithPersistentLifetime();
     * builder.Build().Run();
     * ```
     * @param lifetime The lifetime behavior of the container resource. The default behavior is `Session`.
     * @returns The resource builder.
     */
    withLifetime(lifetime) {
        return new RedisInsightResourcePromiseImpl(this._withLifetimeInternal(lifetime), this._client);
    }
    /** @internal */
    async _withImagePullPolicyInternal(pullPolicy) {
        const rpcArgs = { builder: this._handle, pullPolicy };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImagePullPolicy', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Sets the pull policy for the container resource.
     * @param pullPolicy The pull policy behavior for the container resource.
     * @returns The resource builder.
     */
    withImagePullPolicy(pullPolicy) {
        return new RedisInsightResourcePromiseImpl(this._withImagePullPolicyInternal(pullPolicy), this._client);
    }
    /** @internal */
    async _publishAsContainerInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/publishAsContainer', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Changes the resource to be published as a container in the manifest.
     * @returns The resource builder.
     */
    publishAsContainer() {
        return new RedisInsightResourcePromiseImpl(this._publishAsContainerInternal(), this._client);
    }
    /** @internal */
    async _withDockerfileInternal(contextPath, dockerfilePath, stage) {
        const rpcArgs = { builder: this._handle, contextPath };
        if (dockerfilePath !== undefined)
            rpcArgs.dockerfilePath = dockerfilePath;
        if (stage !== undefined)
            rpcArgs.stage = stage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfile', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Causes Aspire to build the specified container image from a Dockerfile.
     * @param contextPath Path to be used as the context for the container image build.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfile(contextPath, options) {
        const dockerfilePath = options?.dockerfilePath;
        const stage = options?.stage;
        return new RedisInsightResourcePromiseImpl(this._withDockerfileInternal(contextPath, dockerfilePath, stage), this._client);
    }
    /** @internal */
    async _withDockerfileFactoryInternal(contextPath, dockerfileFactory, stage) {
        const dockerfileFactoryId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new DockerfileFactoryContextImpl(argHandle, this._client);
            return await dockerfileFactory(arg);
        });
        const rpcArgs = { builder: this._handle, contextPath, dockerfileFactory: dockerfileFactoryId };
        if (stage !== undefined)
            rpcArgs.stage = stage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileFactory', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Builds the specified container image from a Dockerfile generated by an asynchronous factory function.
     *
     * When this method is called, an annotation is added to the `ContainerResource` that specifies the context path
     * and a factory function that generates Dockerfile content. The factory is invoked at build time to produce the Dockerfile,
     * which is then written to a temporary file and used by the orchestrator to build the container image.
     * The `contextPath` is relative to the AppHost directory unless it is fully qualified.
     * The factory function is invoked once during the build process to generate the Dockerfile content.
     * The output is trusted and not validated.
     * Creates a container called `mycontainer` with a dynamically generated Dockerfile.
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddContainer("mycontainer", "myimage")
     * .WithDockerfileFactory("path/to/context", async context =>
     * {
     * var template = await File.ReadAllTextAsync("template.dockerfile", context.CancellationToken);
     * return template.Replace("{{VERSION}}", "1.0");
     * });
     * builder.Build().Run();
     * ```
     * @param contextPath Path to be used as the context for the container image build.
     * @param dockerfileFactory An asynchronous function that returns the Dockerfile content as a string.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileFactory(contextPath, dockerfileFactory, options) {
        const stage = options?.stage;
        return new RedisInsightResourcePromiseImpl(this._withDockerfileFactoryInternal(contextPath, dockerfileFactory, stage), this._client);
    }
    /** @internal */
    async _withContainerNameInternal(name) {
        const rpcArgs = { builder: this._handle, name };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerName', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Overrides the default container name for this resource. By default Aspire generates a unique container name based on the resource name and a random postfix (or a postfix based on a hash of the AppHost project path for persistent container resources). This method allows you to override that behavior with a custom name, but could lead to naming conflicts if the specified name is not unique.
     *
     * Combining this with `Persistent` will allow Aspire to re-use an existing container that was not
     * created by an Aspire AppHost.
     * @param name The desired container name. Must be a valid container name or your runtime will report an error.
     * @returns The resource builder.
     */
    withContainerName(name) {
        return new RedisInsightResourcePromiseImpl(this._withContainerNameInternal(name), this._client);
    }
    /** @internal */
    async _withBuildArgInternal(name, value) {
        value = isPromiseLike(value) ? await value : value;
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuildArg', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Adds a build argument when the container is built from a Dockerfile.
     * @param name The name of the build argument.
     * @param value The build argument value, either a string or a parameter resource.
     * @returns The resource builder.
     */
    withBuildArg(name, value) {
        return new RedisInsightResourcePromiseImpl(this._withBuildArgInternal(name, value), this._client);
    }
    /** @internal */
    async _withBuildSecretInternal(name, value) {
        value = isPromiseLike(value) ? await value : value;
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withParameterBuildSecret', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Adds a secret build argument when the container is built from a Dockerfile.
     * @param name The name of the secret build argument.
     * @param value The resource builder for a parameter resource.
     * @returns The resource builder.
     */
    withBuildSecret(name, value) {
        return new RedisInsightResourcePromiseImpl(this._withBuildSecretInternal(name, value), this._client);
    }
    /** @internal */
    async _withContainerCertificatePathsInternal(customCertificatesDestination, defaultCertificateBundlePaths, defaultCertificateDirectoryPaths) {
        const rpcArgs = { builder: this._handle };
        if (customCertificatesDestination !== undefined)
            rpcArgs.customCertificatesDestination = customCertificatesDestination;
        if (defaultCertificateBundlePaths !== undefined)
            rpcArgs.defaultCertificateBundlePaths = defaultCertificateBundlePaths;
        if (defaultCertificateDirectoryPaths !== undefined)
            rpcArgs.defaultCertificateDirectoryPaths = defaultCertificateDirectoryPaths;
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerCertificatePaths', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Adds container certificate path overrides used for certificate trust at run time.
     * @param options Additional options.
     * @returns The updated resource builder.
     */
    withContainerCertificatePaths(options) {
        const customCertificatesDestination = options?.customCertificatesDestination;
        const defaultCertificateBundlePaths = options?.defaultCertificateBundlePaths;
        const defaultCertificateDirectoryPaths = options?.defaultCertificateDirectoryPaths;
        return new RedisInsightResourcePromiseImpl(this._withContainerCertificatePathsInternal(customCertificatesDestination, defaultCertificateBundlePaths, defaultCertificateDirectoryPaths), this._client);
    }
    /** @internal */
    async _withDockerfileBuilderInternal(contextPath, callback, stage) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new DockerfileBuilderCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, contextPath, callback: callbackId };
        if (stage !== undefined)
            rpcArgs.stage = stage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileBuilder', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Configures the resource to use a programmatically generated Dockerfile
     *
     * This method provides a programmatic way to build Dockerfiles using the `DockerfileBuilder` API
     * instead of string manipulation. Callbacks can be composed by calling this method multiple times - each callback will be invoked
     * in order to build up the final Dockerfile.
     * The `contextPath` is relative to the AppHost directory unless it is fully qualified.
     * Creates a container with a programmatically built Dockerfile using fluent API:
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddContainer("mycontainer", "myimage")
     * .WithDockerfileBuilder("path/to/context", context =>
     * {
     * context.Builder.From("alpine:latest")
     * .WorkDir("/app")
     * .Run("apk add curl")
     * .Copy(".", ".")
     * .Cmd(["./myapp"]);
     * return Task.CompletedTask;
     * });
     * builder.Build().Run();
     * ```
     * @param contextPath Path to be used as the context for the container image build.
     * @param callback A callback that uses the `DockerfileBuilder` API to construct the Dockerfile.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileBuilder(contextPath, callback, options) {
        const stage = options?.stage;
        return new RedisInsightResourcePromiseImpl(this._withDockerfileBuilderInternal(contextPath, callback, stage), this._client);
    }
    /** @internal */
    async _withDockerfileBaseImageInternal(buildImage, runtimeImage) {
        const rpcArgs = { builder: this._handle };
        if (buildImage !== undefined)
            rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined)
            rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileBaseImage', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Configures custom base images for generated Dockerfiles.
     *
     * This extension method allows customization of the base images used in generated Dockerfiles.
     * For multi-stage Dockerfiles (e.g., Python with UV), you can specify separate build and runtime images.
     * Specify custom base images for a Python application:
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddPythonApp("myapp", "path/to/app", "main.py")
     * .WithDockerfileBaseImage(
     * buildImage: "ghcr.io/astral-sh/uv:python3.12-bookworm-slim",
     * runtimeImage: "python:3.12-slim-bookworm");
     * builder.Build().Run();
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileBaseImage(options) {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new RedisInsightResourcePromiseImpl(this._withDockerfileBaseImageInternal(buildImage, runtimeImage), this._client);
    }
    /** @internal */
    async _withContainerNetworkAliasInternal(alias) {
        const rpcArgs = { builder: this._handle, alias };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerNetworkAlias', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Adds a network alias to container resource.
     *
     * Network aliases enable DNS resolution of the container on the network by custom names.
     * By default, containers are accessible on the network using their resource name as a DNS alias.
     * This method allows adding additional aliases for the same container.
     * Multiple aliases can be added by calling this method multiple times.
     * @param alias The network alias for the container.
     * @returns The resource builder.
     */
    withContainerNetworkAlias(alias) {
        return new RedisInsightResourcePromiseImpl(this._withContainerNetworkAliasInternal(alias), this._client);
    }
    /** @internal */
    async _withMcpServerInternal(path, endpointName) {
        const rpcArgs = { builder: this._handle };
        if (path !== undefined)
            rpcArgs.path = path;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withMcpServer', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Marks the resource as hosting a Model Context Protocol (MCP) server on the specified endpoint.
     *
     * This method adds an `McpServerEndpointAnnotation` to the resource, enabling the Aspire tooling
     * to discover and proxy the MCP server exposed by the resource.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withMcpServer(options) {
        const path = options?.path;
        const endpointName = options?.endpointName;
        return new RedisInsightResourcePromiseImpl(this._withMcpServerInternal(path, endpointName), this._client);
    }
    /** @internal */
    async _withOtlpExporterInternal(protocol) {
        const rpcArgs = { builder: this._handle };
        if (protocol !== undefined)
            rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability('Aspire.Hosting/withOtlpExporter', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Configures OTLP telemetry export
     * @param options Additional options.
     */
    withOtlpExporter(options) {
        const protocol = options?.protocol;
        return new RedisInsightResourcePromiseImpl(this._withOtlpExporterInternal(protocol), this._client);
    }
    /** @internal */
    async _publishAsConnectionStringInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/publishAsConnectionString', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Changes the resource to be published as a connection string reference in the manifest.
     * @returns The resource builder.
     */
    publishAsConnectionString() {
        return new RedisInsightResourcePromiseImpl(this._publishAsConnectionStringInternal(), this._client);
    }
    /** @internal */
    async _withRequiredCommandInternal(command, helpLink) {
        const rpcArgs = { builder: this._handle, command };
        if (helpLink !== undefined)
            rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability('Aspire.Hosting/withRequiredCommand', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Declares that a resource requires a specific command/executable to be available on the local machine PATH before it can start.
     *
     * The command is considered valid if either:
     * 1. It is an absolute or relative path (contains a directory separator) that points to an existing file, or
     * 2. It is discoverable on the current process PATH (respecting PATHEXT on Windows).
     * If the command is not found, a warning message will be logged but the resource will be allowed to attempt to start.
     * @param command The command string (file name or path) that should be validated.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withRequiredCommand(command, options) {
        const helpLink = options?.helpLink;
        return new RedisInsightResourcePromiseImpl(this._withRequiredCommandInternal(command, helpLink), this._client);
    }
    /** @internal */
    async _withSessionLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withSessionLifetime', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a session lifetime.
     * @returns The resource builder.
     */
    withSessionLifetime() {
        return new RedisInsightResourcePromiseImpl(this._withSessionLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withPersistentLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPersistentLifetime', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime.
     * @returns The resource builder.
     */
    withPersistentLifetime() {
        return new RedisInsightResourcePromiseImpl(this._withPersistentLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withLifetimeOfInternal(sourceBuilder) {
        sourceBuilder = isPromiseLike(sourceBuilder) ? await sourceBuilder : sourceBuilder;
        const rpcArgs = { builder: this._handle, sourceBuilder };
        const result = await this._client.invokeCapability('Aspire.Hosting/withLifetimeOf', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to match the lifetime of another resource.
     *
     * The resource lifetime is evaluated from `sourceBuilder` when the application model is prepared, so later lifetime
     * changes to the source resource are reflected by this resource.
     * @param sourceBuilder The resource builder whose lifetime should be used.
     * @returns The resource builder.
     */
    withLifetimeOf(sourceBuilder) {
        return new RedisInsightResourcePromiseImpl(this._withLifetimeOfInternal(sourceBuilder), this._client);
    }
    /** @internal */
    async _withParentProcessLifetimeInternal(parentProcessId) {
        const rpcArgs = { builder: this._handle, parentProcessId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withParentProcessLifetime', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime that ends when a parent process exits.
     * @param parentProcessId The ID of the parent process to monitor.
     * @returns The resource builder.
     */
    withParentProcessLifetime(parentProcessId) {
        return new RedisInsightResourcePromiseImpl(this._withParentProcessLifetimeInternal(parentProcessId), this._client);
    }
    /** @internal */
    async _withEnvironmentInternal(name, value) {
        value = isPromiseLike(value) ? await value : value;
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEnvironment', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /** Sets an environment variable */
    withEnvironment(name, value) {
        return new RedisInsightResourcePromiseImpl(this._withEnvironmentInternal(name, value), this._client);
    }
    /** @internal */
    async _withEnvironmentCallbackInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new EnvironmentCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEnvironmentCallback', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Allows for the population of environment variables on a resource.
     * @param callback A callback that allows for deferred execution for computing many environment variables. This runs after resources have been allocated by the orchestrator and allows access to other resources to resolve computed data, e.g. connection strings, ports.
     * @returns The resource builder.
     */
    withEnvironmentCallback(callback) {
        return new RedisInsightResourcePromiseImpl(this._withEnvironmentCallbackInternal(callback), this._client);
    }
    /** @internal */
    async _withArgsInternal(args) {
        const rpcArgs = { builder: this._handle, args };
        const result = await this._client.invokeCapability('Aspire.Hosting/withArgs', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Adds arguments to be passed to a resource that supports arguments when it is launched.
     * @param args The arguments to be passed to the resource when it is started.
     * @returns The resource builder.
     */
    withArgs(args) {
        return new RedisInsightResourcePromiseImpl(this._withArgsInternal(args), this._client);
    }
    /** @internal */
    async _withArgsCallbackInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new CommandLineArgsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withArgsCallback', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Adds a callback to be executed with a list of command-line arguments when a resource is started.
     * @param callback A callback that allows for deferred execution for computing arguments. This runs after resources have been allocated by the orchestrator and allows access to other resources to resolve computed data, e.g. connection strings, ports.
     * @returns The resource builder.
     */
    withArgsCallback(callback) {
        return new RedisInsightResourcePromiseImpl(this._withArgsCallbackInternal(callback), this._client);
    }
    /** @internal */
    async _withReferenceEnvironmentInternal(options) {
        const rpcArgs = { builder: this._handle, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withReferenceEnvironment', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Configures how information is injected into environment variables when the resource references other resources.
     * @param options Options controlling which reference information is emitted.
     * @returns The resource builder.
     */
    withReferenceEnvironment(options) {
        return new RedisInsightResourcePromiseImpl(this._withReferenceEnvironmentInternal(options), this._client);
    }
    /** @internal */
    async _withReferenceInternal(source, connectionName, optional, name) {
        source = isPromiseLike(source) ? await source : source;
        const rpcArgs = { builder: this._handle, source };
        if (connectionName !== undefined)
            rpcArgs.connectionName = connectionName;
        if (optional !== undefined)
            rpcArgs.optional = optional;
        if (name !== undefined)
            rpcArgs.name = name;
        const result = await this._client.invokeCapability('Aspire.Hosting/withReference', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Adds a reference to another resource
     * @param options Additional options.
     */
    withReference(source, options) {
        const connectionName = options?.connectionName;
        const optional = options?.optional;
        const name = options?.name;
        return new RedisInsightResourcePromiseImpl(this._withReferenceInternal(source, connectionName, optional, name), this._client);
    }
    /** @internal */
    async _withEndpointCallbackInternal(endpointName, callback, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpointCallback', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Updates a named endpoint via callback
     * @param options Additional options.
     */
    withEndpointCallback(endpointName, callback, options) {
        const createIfNotExists = options?.createIfNotExists;
        return new RedisInsightResourcePromiseImpl(this._withEndpointCallbackInternal(endpointName, callback, createIfNotExists), this._client);
    }
    /** @internal */
    async _withHttpEndpointCallbackInternal(callback, name, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        if (name !== undefined)
            rpcArgs.name = name;
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpEndpointCallback', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Updates an HTTP endpoint via callback
     * @param options Additional options.
     */
    withHttpEndpointCallback(callback, options) {
        const name = options?.name;
        const createIfNotExists = options?.createIfNotExists;
        return new RedisInsightResourcePromiseImpl(this._withHttpEndpointCallbackInternal(callback, name, createIfNotExists), this._client);
    }
    /** @internal */
    async _withHttpsEndpointCallbackInternal(callback, name, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        if (name !== undefined)
            rpcArgs.name = name;
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpsEndpointCallback', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Updates an HTTPS endpoint via callback
     * @param options Additional options.
     */
    withHttpsEndpointCallback(callback, options) {
        const name = options?.name;
        const createIfNotExists = options?.createIfNotExists;
        return new RedisInsightResourcePromiseImpl(this._withHttpsEndpointCallbackInternal(callback, name, createIfNotExists), this._client);
    }
    /** @internal */
    async _withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (scheme !== undefined)
            rpcArgs.scheme = scheme;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        if (isExternal !== undefined)
            rpcArgs.isExternal = isExternal;
        if (protocol !== undefined)
            rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpoint', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Adds a network endpoint
     * @param options Additional options.
     * @returns The resource builder.
     */
    withEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const scheme = options?.scheme;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        const isExternal = options?.isExternal;
        const protocol = options?.protocol;
        return new RedisInsightResourcePromiseImpl(this._withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol), this._client);
    }
    /** @internal */
    async _withEndpointProxySupportInternal(proxyEnabled) {
        const rpcArgs = { builder: this._handle, proxyEnabled };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpointProxySupport', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Set whether a resource can use proxied endpoints or whether they should be disabled for all endpoints belonging to the resource. If set to `false`, endpoints belonging to the resource will ignore the configured proxy settings and run proxy-less.
     *
     * This method is intended to support scenarios with persistent lifetime resources where it is desirable for the resource to be accessible over the same
     * port whether the Aspire application is running or not. Proxied endpoints bind ports that are only accessible while the Aspire application is running.
     * The user needs to be careful to ensure that endpoints are using unique ports when disabling proxy support as by default for proxy-less
     * endpoints, Aspire will allocate the target port as the host port, which will increase the chance of port conflicts.
     * @param proxyEnabled Should endpoints for the resource support using a proxy?
     * @returns The resource builder.
     */
    withEndpointProxySupport(proxyEnabled) {
        return new RedisInsightResourcePromiseImpl(this._withEndpointProxySupportInternal(proxyEnabled), this._client);
    }
    /** @internal */
    async _withHttpEndpointInternal(port, targetPort, name, env, isProxied) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpEndpoint', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP endpoint
     *
     * If an endpoint with the same name already exists on the resource, the existing endpoint is updated
     * with any non-null parameter values. Parameters left as `null` will not modify the existing endpoint's values.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new RedisInsightResourcePromiseImpl(this._withHttpEndpointInternal(port, targetPort, name, env, isProxied), this._client);
    }
    /** @internal */
    async _withHttpsEndpointInternal(port, targetPort, name, env, isProxied) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpsEndpoint', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTPS endpoint
     *
     * If an endpoint with the same name already exists on the resource, the existing endpoint is updated
     * with any non-null parameter values. Parameters left as `null` will not modify the existing endpoint's values.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpsEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new RedisInsightResourcePromiseImpl(this._withHttpsEndpointInternal(port, targetPort, name, env, isProxied), this._client);
    }
    /** @internal */
    async _withExternalHttpEndpointsInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExternalHttpEndpoints', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Marks existing http or https endpoints on a resource as external.
     * @returns The resource builder.
     */
    withExternalHttpEndpoints() {
        return new RedisInsightResourcePromiseImpl(this._withExternalHttpEndpointsInternal(), this._client);
    }
    /**
     * Gets an endpoint reference
     * @param name The name of the endpoint.
     * @returns An `EndpointReference` that can be used to resolve the address of the endpoint after resource allocation has occurred.
     */
    getEndpoint(name) {
        const promise = (async () => {
            const rpcArgs = { builder: this._handle, name };
            const handle = await this._client.invokeCapability('Aspire.Hosting/getEndpoint', rpcArgs);
            return new EndpointReferenceImpl(handle, this._client);
        })();
        return new EndpointReferencePromiseImpl(promise, this._client);
    }
    /** @internal */
    async _asHttp2ServiceInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/asHttp2Service', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to mark all endpoints' transport as HTTP/2. This is useful for HTTP/2 services that need prior knowledge.
     * @returns The resource builder.
     */
    asHttp2Service() {
        return new RedisInsightResourcePromiseImpl(this._asHttp2ServiceInternal(), this._client);
    }
    /** @internal */
    async _withUrlsInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new ResourceUrlsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrls', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to customize the URLs displayed for the resource.
     * @param callback The callback that will customize URLs for the resource.
     * @returns The resource builder.
     */
    withUrls(callback) {
        return new RedisInsightResourcePromiseImpl(this._withUrlsInternal(callback), this._client);
    }
    /** @internal */
    async _withUrlInternal(url, displayText) {
        const rpcArgs = { builder: this._handle, url };
        if (displayText !== undefined)
            rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrl', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Adds or modifies displayed URLs
     * @param options Additional options.
     */
    withUrl(url, options) {
        const displayText = options?.displayText;
        return new RedisInsightResourcePromiseImpl(this._withUrlInternal(url, displayText), this._client);
    }
    /** @internal */
    async _withUrlForEndpointInternal(endpointName, callback) {
        const callbackId = registerCallback(async (objData) => {
            const obj = wrapIfHandle(objData);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrlForEndpoint', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to update the URL displayed for the endpoint with the specified name.
     * @param endpointName The name of the endpoint to customize the URL for.
     * @param callback The callback that will customize the URL.
     * @returns The resource builder.
     */
    withUrlForEndpoint(endpointName, callback) {
        return new RedisInsightResourcePromiseImpl(this._withUrlForEndpointInternal(endpointName, callback), this._client);
    }
    /** @internal */
    async _excludeFromManifestInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromManifest', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Excludes a resource from being published to the manifest.
     * @returns The resource builder.
     */
    excludeFromManifest() {
        return new RedisInsightResourcePromiseImpl(this._excludeFromManifestInternal(), this._client);
    }
    /** @internal */
    async _waitForInternal(dependency, waitBehavior) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (waitBehavior !== undefined)
            rpcArgs.waitBehavior = waitBehavior;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitFor', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Waits for another resource to be ready
     * @param options Additional options.
     */
    waitFor(dependency, options) {
        const waitBehavior = options?.waitBehavior;
        return new RedisInsightResourcePromiseImpl(this._waitForInternal(dependency, waitBehavior), this._client);
    }
    /** @internal */
    async _waitForStartInternal(dependency, waitBehavior) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (waitBehavior !== undefined)
            rpcArgs.waitBehavior = waitBehavior;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitForStart', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Waits for another resource to start
     * @param options Additional options.
     */
    waitForStart(dependency, options) {
        const waitBehavior = options?.waitBehavior;
        return new RedisInsightResourcePromiseImpl(this._waitForStartInternal(dependency, waitBehavior), this._client);
    }
    /** @internal */
    async _withExplicitStartInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExplicitStart', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Prevents resource from starting automatically
     * @returns The resource builder.
     */
    withExplicitStart() {
        return new RedisInsightResourcePromiseImpl(this._withExplicitStartInternal(), this._client);
    }
    /** @internal */
    async _waitForCompletionInternal(dependency, exitCode) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitForResourceCompletion', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Waits for the dependency resource to enter the Exited or Finished state before starting the resource.
     * @param dependency The resource builder for the dependency resource.
     * @param options Additional options.
     * @returns The resource builder.
     */
    waitForCompletion(dependency, options) {
        const exitCode = options?.exitCode;
        return new RedisInsightResourcePromiseImpl(this._waitForCompletionInternal(dependency, exitCode), this._client);
    }
    /** @internal */
    async _withHealthCheckInternal(key) {
        const rpcArgs = { builder: this._handle, key };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHealthCheck', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Adds a health check by key
     * @param key The key for the health check.
     * @returns The resource builder.
     */
    withHealthCheck(key) {
        return new RedisInsightResourcePromiseImpl(this._withHealthCheckInternal(key), this._client);
    }
    /** @internal */
    async _withHttpHealthCheckInternal(path, statusCode, endpointName) {
        const rpcArgs = { builder: this._handle };
        if (path !== undefined)
            rpcArgs.path = path;
        if (statusCode !== undefined)
            rpcArgs.statusCode = statusCode;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpHealthCheck', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Adds a health check to the resource which is mapped to a specific endpoint.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpHealthCheck(options) {
        const path = options?.path;
        const statusCode = options?.statusCode;
        const endpointName = options?.endpointName;
        return new RedisInsightResourcePromiseImpl(this._withHttpHealthCheckInternal(path, statusCode, endpointName), this._client);
    }
    /** @internal */
    async _withCommandInternal(name, displayName, executeCommand, commandOptions) {
        const executeCommandId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined)
            rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability('Aspire.Hosting/withCommand', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Adds a resource command
     *
     * The `WithCommand` method is used to add commands to the resource. Commands are displayed in the dashboard
     * and can be executed by a user using the dashboard UI.
     * When a command is executed, the `executeCommand` callback is called and is run inside the Aspire host.
     * @param name The name of the command. The name uniquely identifies the command.
     * @param displayName The display name visible in UI.
     * @param executeCommand A callback that is executed when the command is executed. The callback is run inside the Aspire host. The callback result is used to indicate success or failure in the UI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withCommand(name, displayName, executeCommand, options) {
        const commandOptions = options?.commandOptions;
        return new RedisInsightResourcePromiseImpl(this._withCommandInternal(name, displayName, executeCommand, commandOptions), this._client);
    }
    /** @internal */
    async _withProcessCommandInternal(commandName, displayName, options) {
        const rpcArgs = { builder: this._handle, commandName, displayName, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommand', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /** Adds a command to the resource that starts a local process when invoked. */
    withProcessCommand(commandName, displayName, options) {
        return new RedisInsightResourcePromiseImpl(this._withProcessCommandInternal(commandName, displayName, options), this._client);
    }
    /** @internal */
    async _withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options) {
        const createProcessSpecId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await createProcessSpec(arg);
        });
        const rpcArgs = { builder: this._handle, commandName, displayName, createProcessSpec: createProcessSpecId };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommandFactory', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Adds a command to the resource that starts a local process created by a callback when invoked.
     * @param options Additional options.
     */
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new RedisInsightResourcePromiseImpl(this._withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options), this._client);
    }
    /** @internal */
    async _withHttpCommandInternal(path, displayName, options) {
        const rpcArgs = { builder: this._handle, path, displayName };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpCommand', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP resource command
     * @param options Additional options.
     */
    withHttpCommand(path, displayName, options) {
        return new RedisInsightResourcePromiseImpl(this._withHttpCommandInternal(path, displayName, options), this._client);
    }
    /** @internal */
    async _withDeveloperCertificateTrustInternal(trust) {
        const rpcArgs = { builder: this._handle, trust };
        const result = await this._client.invokeCapability('Aspire.Hosting/withDeveloperCertificateTrust', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Indicates whether developer certificates should be treated as trusted certificate authorities for the resource at run time. Currently this indicates trust for the ASP.NET Core developer certificate. The developer certificate will only be trusted when running in local development scenarios; in publish mode resources will use their default certificate trust.
     *
     * Disable trust for app host managed developer certificate(s) for a container resource.
     * ```
     * var container = builder.AddContainer("my-service", "my-service:latest")
     * .WithDeveloperCertificateTrust(false);
     * ```
     * Disable automatic trust for app host managed developer certificate(s), but explicitly enable it for a specific resource.
     * ```
     * var builder = DistributedApplication.CreateBuilder(new DistributedApplicationOptions()
     * {
     * Args = args,
     * TrustDeveloperCertificate = false,
     * });
     * var project = builder.AddProject<MyService>("my-service")
     * .WithDeveloperCertificateTrust(true);
     * ```
     * @param trust Indicates whether the developer certificate should be treated as trusted.
     * @returns The resource builder.
     */
    withDeveloperCertificateTrust(trust) {
        return new RedisInsightResourcePromiseImpl(this._withDeveloperCertificateTrustInternal(trust), this._client);
    }
    /** @internal */
    async _withCertificateTrustScopeInternal(scope) {
        const rpcArgs = { builder: this._handle, scope };
        const result = await this._client.invokeCapability('Aspire.Hosting/withCertificateTrustScope', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Sets the certificate trust scope
     *
     * The default scope if not overridden is `Append` which means that custom certificate
     * authorities should be appended to the default trusted certificate authorities for the resource. Setting the scope to
     * `Override` indicates the set of certificates in referenced
     * `CertificateAuthorityCollection` (and optionally Aspire developer certificiates) should be used as the
     * exclusive source of trust for a resource.
     * In all cases, this is a best effort implementation as not all resources support full customization of certificate
     * trust.
     * Set the scope for custom certificate authorities to override the default trusted certificate authorities for a container resource.
     * ```
     * var caCollection = builder.AddCertificateAuthorityCollection("my-cas")
     * .WithCertificate(new X509Certificate2("my-ca.pem"));
     * var container = builder.AddContainer("my-service", "my-service:latest")
     * .WithCertificateAuthorityCollection(caCollection)
     * .WithCertificateTrustScope(CertificateTrustScope.Override);
     * ```
     * @param scope The scope to apply to custom certificate authorities associated with the resource.
     * @returns The resource builder.
     */
    withCertificateTrustScope(scope) {
        return new RedisInsightResourcePromiseImpl(this._withCertificateTrustScopeInternal(scope), this._client);
    }
    /** @internal */
    async _withHttpsDeveloperCertificateInternal(password) {
        password = isPromiseLike(password) ? await password : password;
        const rpcArgs = { builder: this._handle };
        if (password !== undefined)
            rpcArgs.password = password;
        const result = await this._client.invokeCapability('Aspire.Hosting/withParameterHttpsDeveloperCertificate', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Indicates that a resource should use the developer certificate key pair for HTTPS endpoints at run time. Currently this indicates use of the ASP.NET Core developer certificate. The developer certificate will only be used when running in local development scenarios; in publish mode resources will use their default certificate configuration.
     *
     * Use the developer certificate for HTTPS/TLS endpoints on a container resource:
     * ```
     * builder.AddContainer("my-service", "my-image")
     * .WithHttpsDeveloperCertificate()
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpsDeveloperCertificate(options) {
        let password = options?.password;
        return new RedisInsightResourcePromiseImpl(this._withHttpsDeveloperCertificateInternal(password), this._client);
    }
    /** @internal */
    async _withoutHttpsCertificateInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withoutHttpsCertificate', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Disable HTTPS/TLS server certificate configuration for the resource. No HTTPS/TLS termination configuration will be applied.
     *
     * Disable HTTPS certificate configuration for a Redis resource:
     * ```
     * var redis = builder.AddRedis("cache")
     * .WithoutHttpsCertificate();
     * ```
     * @returns The resource builder.
     */
    withoutHttpsCertificate() {
        return new RedisInsightResourcePromiseImpl(this._withoutHttpsCertificateInternal(), this._client);
    }
    /** @internal */
    async _withRelationshipInternal(resourceBuilder, type) {
        resourceBuilder = isPromiseLike(resourceBuilder) ? await resourceBuilder : resourceBuilder;
        const rpcArgs = { builder: this._handle, resourceBuilder, type };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderRelationship', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Adds a relationship to another resource using its builder.
     * @param resourceBuilder The resource builder that the relationship is to.
     * @param type The relationship type.
     * @returns A resource builder.
     */
    withRelationship(resourceBuilder, type) {
        return new RedisInsightResourcePromiseImpl(this._withRelationshipInternal(resourceBuilder, type), this._client);
    }
    /** @internal */
    async _withParentRelationshipInternal(parent) {
        parent = isPromiseLike(parent) ? await parent : parent;
        const rpcArgs = { builder: this._handle, parent };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderParentRelationship', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Sets the parent relationship
     * @param parent The parent of `builder`.
     * @returns A resource builder.
     */
    withParentRelationship(parent) {
        return new RedisInsightResourcePromiseImpl(this._withParentRelationshipInternal(parent), this._client);
    }
    /** @internal */
    async _withChildRelationshipInternal(child) {
        child = isPromiseLike(child) ? await child : child;
        const rpcArgs = { builder: this._handle, child };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderChildRelationship', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Sets a child relationship
     * @param child The child of `builder`.
     * @returns A resource builder.
     */
    withChildRelationship(child) {
        return new RedisInsightResourcePromiseImpl(this._withChildRelationshipInternal(child), this._client);
    }
    /** @internal */
    async _withIconNameInternal(iconName, iconVariant) {
        const rpcArgs = { builder: this._handle, iconName };
        if (iconVariant !== undefined)
            rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability('Aspire.Hosting/withIconName', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Specifies the icon to use when displaying the resource in the dashboard.
     * @param iconName The name of the FluentUI icon to use. See https://aka.ms/fluentui-system-icons for available icons.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withIconName(iconName, options) {
        const iconVariant = options?.iconVariant;
        return new RedisInsightResourcePromiseImpl(this._withIconNameInternal(iconName, iconVariant), this._client);
    }
    /** @internal */
    async _withComputeEnvironmentInternal(computeEnvironmentResource) {
        computeEnvironmentResource = isPromiseLike(computeEnvironmentResource) ? await computeEnvironmentResource : computeEnvironmentResource;
        const rpcArgs = { builder: this._handle, computeEnvironmentResource };
        const result = await this._client.invokeCapability('Aspire.Hosting/withComputeEnvironment', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Configures the compute environment for the compute resource.
     *
     * This method allows associating a specific compute environment with the compute resource.
     * @param computeEnvironmentResource The compute environment resource to associate with the compute resource.
     * @returns The resource builder.
     */
    withComputeEnvironment(computeEnvironmentResource) {
        return new RedisInsightResourcePromiseImpl(this._withComputeEnvironmentInternal(computeEnvironmentResource), this._client);
    }
    /** @internal */
    async _withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName) {
        const rpcArgs = { builder: this._handle, probeType };
        if (path !== undefined)
            rpcArgs.path = path;
        if (initialDelaySeconds !== undefined)
            rpcArgs.initialDelaySeconds = initialDelaySeconds;
        if (periodSeconds !== undefined)
            rpcArgs.periodSeconds = periodSeconds;
        if (timeoutSeconds !== undefined)
            rpcArgs.timeoutSeconds = timeoutSeconds;
        if (failureThreshold !== undefined)
            rpcArgs.failureThreshold = failureThreshold;
        if (successThreshold !== undefined)
            rpcArgs.successThreshold = successThreshold;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpProbe', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP health probe to the resource
     * @param options Additional options.
     */
    withHttpProbe(probeType, options) {
        const path = options?.path;
        const initialDelaySeconds = options?.initialDelaySeconds;
        const periodSeconds = options?.periodSeconds;
        const timeoutSeconds = options?.timeoutSeconds;
        const failureThreshold = options?.failureThreshold;
        const successThreshold = options?.successThreshold;
        const endpointName = options?.endpointName;
        return new RedisInsightResourcePromiseImpl(this._withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName), this._client);
    }
    /** @internal */
    async _excludeFromMcpInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromMcp', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Exclude the resource from MCP operations using the Aspire MCP server. The resource is excluded from results that return resources, console logs and telemetry.
     * @returns The resource builder.
     */
    excludeFromMcp() {
        return new RedisInsightResourcePromiseImpl(this._excludeFromMcpInternal(), this._client);
    }
    /** @internal */
    async _withHiddenInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHidden', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists
     *
     * Use this method to hide resources that are implementation details and should never be displayed by default.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @returns The resource builder.
     */
    withHidden() {
        return new RedisInsightResourcePromiseImpl(this._withHiddenInternal(), this._client);
    }
    /** @internal */
    async _withHiddenOnCompletionInternal(exitCode, exitCodes) {
        const rpcArgs = { builder: this._handle };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        if (exitCodes !== undefined)
            rpcArgs.exitCodes = exitCodes;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHiddenOnCompletion', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists after successful completion
     *
     * This method is useful for one-off resources such as setup scripts, migrations, or build steps that should remain visible while running
     * and then be hidden after successful completion.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHiddenOnCompletion(options) {
        const exitCode = options?.exitCode;
        const exitCodes = options?.exitCodes;
        return new RedisInsightResourcePromiseImpl(this._withHiddenOnCompletionInternal(exitCode, exitCodes), this._client);
    }
    /** @internal */
    async _withImagePushOptionsInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ContainerImagePushOptionsCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImagePushOptions', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Adds an asynchronous callback to configure container image push options for the resource.
     *
     * This method allows customization of how container images are named and tagged when pushed to a registry using an asynchronous callback.
     * Use this overload when the callback needs to perform asynchronous operations such as retrieving configuration values from external sources.
     * The callback receives a `ContainerImagePushOptionsCallbackContext` that provides access to the resource
     * and the `ContainerImagePushOptions` that can be modified.
     * Multiple callbacks can be registered on the same resource, and they will be invoked in the order they were added.
     * @param callback The asynchronous callback to configure push options.
     * @returns The resource builder.
     */
    withImagePushOptions(callback) {
        return new RedisInsightResourcePromiseImpl(this._withImagePushOptionsInternal(callback), this._client);
    }
    /** @internal */
    async _withRemoteImageNameInternal(remoteImageName) {
        const rpcArgs = { builder: this._handle, remoteImageName };
        const result = await this._client.invokeCapability('Aspire.Hosting/withRemoteImageName', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Sets the remote image name (without registry endpoint or tag) for container push operations.
     *
     * Use this with `withRemoteImageTag` to fully customize the image reference used for container push operations.
     * @param remoteImageName The remote image name (e.g., "myapp" or "myorg/myapp").
     * @returns The resource builder.
     */
    withRemoteImageName(remoteImageName) {
        return new RedisInsightResourcePromiseImpl(this._withRemoteImageNameInternal(remoteImageName), this._client);
    }
    /** @internal */
    async _withRemoteImageTagInternal(remoteImageTag) {
        const rpcArgs = { builder: this._handle, remoteImageTag };
        const result = await this._client.invokeCapability('Aspire.Hosting/withRemoteImageTag', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Sets the remote image tag for container push operations.
     *
     * Use this with `withRemoteImageName` to fully customize the image reference used for container push operations.
     * @param remoteImageTag The remote image tag (e.g., "latest", "v1.0.0").
     * @returns The resource builder.
     */
    withRemoteImageTag(remoteImageTag) {
        return new RedisInsightResourcePromiseImpl(this._withRemoteImageTagInternal(remoteImageTag), this._client);
    }
    /** @internal */
    async _withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new PipelineStepContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined)
            rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined)
            rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined)
            rpcArgs.tags = tags;
        if (description !== undefined)
            rpcArgs.description = description;
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineStepFactory', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Adds a pipeline step to the resource that will be executed during deployment.
     * @param stepName The unique name of the pipeline step.
     * @param callback The callback to execute when the step runs.
     * @param options Additional options.
     * @returns The resource builder for chaining.
     */
    withPipelineStepFactory(stepName, callback, options) {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new RedisInsightResourcePromiseImpl(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description), this._client);
    }
    /** @internal */
    async _withPipelineConfigurationInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new PipelineConfigurationContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineConfiguration', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to be executed during the pipeline configuration phase, allowing modification of step dependencies and relationships.
     * @param callback The callback function to execute during the configuration phase.
     * @returns The resource builder for chaining.
     */
    withPipelineConfiguration(callback) {
        return new RedisInsightResourcePromiseImpl(this._withPipelineConfigurationInternal(callback), this._client);
    }
    /** @internal */
    async _withVolumeInternal(target, name, isReadOnly) {
        const rpcArgs = { resource: this._handle, target };
        if (name !== undefined)
            rpcArgs.name = name;
        if (isReadOnly !== undefined)
            rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability('Aspire.Hosting/withVolume', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Adds a volume to a container resource.
     *
     * Volumes persist data across container restarts. Named volumes are managed
     * by Docker/Podman and stored in a system-managed location.
     * Why this wrapper exists: The original `ContainerResourceBuilderExtensions.WithVolume`
     * has parameter order `(name?, target, isReadOnly)` where the optional `name` comes first.
     * This wrapper reorders parameters to `(target, name?, isReadOnly)` so the required `target`
     * parameter comes first, providing a better API for polyglot consumers.
     * @param target The mount path inside the container.
     * @param options Additional options.
     * @returns The same resource builder handle for chaining.
     */
    withVolume(target, options) {
        const name = options?.name;
        const isReadOnly = options?.isReadOnly;
        return new RedisInsightResourcePromiseImpl(this._withVolumeInternal(target, name, isReadOnly), this._client);
    }
    /**
     * Gets the name of the resource from a builder.
     *
     * Why this wrapper exists: This capability accesses a nested property
     * (`resource.Resource.Name`) which requires a wrapper method. There is no single
     * .NET method that returns just the resource name that could be annotated directly.
     * @returns The resource name.
     */
    async getResourceName() {
        const rpcArgs = { resource: this._handle };
        return await this._client.invokeCapability('Aspire.Hosting/getResourceName', rpcArgs);
    }
    /** @internal */
    async _onBeforeResourceStartedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new BeforeResourceStartedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onBeforeResourceStarted', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the BeforeResourceStarted event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onBeforeResourceStarted(callback) {
        return new RedisInsightResourcePromiseImpl(this._onBeforeResourceStartedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceStoppedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceStoppedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceStopped', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceStopped event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceStopped(callback) {
        return new RedisInsightResourcePromiseImpl(this._onResourceStoppedInternal(callback), this._client);
    }
    /** @internal */
    async _onInitializeResourceInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new InitializeResourceEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onInitializeResource', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the InitializeResource event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onInitializeResource(callback) {
        return new RedisInsightResourcePromiseImpl(this._onInitializeResourceInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceEndpointsAllocatedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceEndpointsAllocatedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceEndpointsAllocated', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceEndpointsAllocated event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceEndpointsAllocated(callback) {
        return new RedisInsightResourcePromiseImpl(this._onResourceEndpointsAllocatedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceReadyInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceReadyEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceReady', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceReady event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceReady(callback) {
        return new RedisInsightResourcePromiseImpl(this._onResourceReadyInternal(callback), this._client);
    }
    /**
     * Creates an execution configuration builder for the specified resource.
     * @returns The execution configuration builder.
     */
    createExecutionConfiguration() {
        const promise = (async () => {
            const rpcArgs = { resource: this._handle };
            const handle = await this._client.invokeCapability('Aspire.Hosting/createExecutionConfiguration', rpcArgs);
            return new ExecutionConfigurationBuilderImpl(handle, this._client);
        })();
        return new ExecutionConfigurationBuilderPromiseImpl(promise, this._client);
    }
    /** @internal */
    async _withHostPortInternal(port) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        const result = await this._client.invokeCapability('Aspire.Hosting.Redis/withRedisInsightHostPort', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Configures the host port that the Redis Insight resource is exposed on instead of using randomly assigned port.
     * @param options Additional options.
     * @returns The resource builder for RedisInsight.
     */
    withHostPort(options) {
        const port = options?.port;
        return new RedisInsightResourcePromiseImpl(this._withHostPortInternal(port), this._client);
    }
    /** @internal */
    async _withDataVolumeInternal(name) {
        const rpcArgs = { builder: this._handle };
        if (name !== undefined)
            rpcArgs.name = name;
        const result = await this._client.invokeCapability('Aspire.Hosting.Redis/withRedisInsightDataVolume', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Adds a named volume for the data folder to a Redis Insight container resource.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDataVolume(options) {
        const name = options?.name;
        return new RedisInsightResourcePromiseImpl(this._withDataVolumeInternal(name), this._client);
    }
    /** @internal */
    async _withDataBindMountInternal(source) {
        const rpcArgs = { builder: this._handle, source };
        const result = await this._client.invokeCapability('Aspire.Hosting.Redis/withRedisInsightDataBindMount', rpcArgs);
        return new RedisInsightResourceImpl(result, this._client);
    }
    /**
     * Adds a bind mount for the data folder to a Redis Insight container resource.
     * @param source The source directory on the host to mount into the container.
     * @returns The resource builder.
     */
    withDataBindMount(source) {
        return new RedisInsightResourcePromiseImpl(this._withDataBindMountInternal(source), this._client);
    }
}
/**
 * Thenable wrapper for RedisInsightResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
class RedisInsightResourcePromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    withContainerRegistry(registry) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withContainerRegistry(registry)), this._client);
    }
    withBindMount(source, target, options) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withBindMount(source, target, options)), this._client);
    }
    withEntrypoint(entrypoint) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withEntrypoint(entrypoint)), this._client);
    }
    withImageTag(tag) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withImageTag(tag)), this._client);
    }
    withImageRegistry(registry) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withImageRegistry(registry)), this._client);
    }
    withImage(image, options) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withImage(image, options)), this._client);
    }
    withImageSHA256(sha256) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withImageSHA256(sha256)), this._client);
    }
    withContainerRuntimeArgs(args) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withContainerRuntimeArgs(args)), this._client);
    }
    withLifetime(lifetime) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withLifetime(lifetime)), this._client);
    }
    withImagePullPolicy(pullPolicy) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withImagePullPolicy(pullPolicy)), this._client);
    }
    publishAsContainer() {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.publishAsContainer()), this._client);
    }
    withDockerfile(contextPath, options) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withDockerfile(contextPath, options)), this._client);
    }
    withDockerfileFactory(contextPath, dockerfileFactory, options) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileFactory(contextPath, dockerfileFactory, options)), this._client);
    }
    withContainerName(name) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withContainerName(name)), this._client);
    }
    withBuildArg(name, value) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withBuildArg(name, value)), this._client);
    }
    withBuildSecret(name, value) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withBuildSecret(name, value)), this._client);
    }
    withContainerCertificatePaths(options) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withContainerCertificatePaths(options)), this._client);
    }
    withDockerfileBuilder(contextPath, callback, options) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileBuilder(contextPath, callback, options)), this._client);
    }
    withDockerfileBaseImage(options) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileBaseImage(options)), this._client);
    }
    withContainerNetworkAlias(alias) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withContainerNetworkAlias(alias)), this._client);
    }
    withMcpServer(options) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withMcpServer(options)), this._client);
    }
    withOtlpExporter(options) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withOtlpExporter(options)), this._client);
    }
    publishAsConnectionString() {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.publishAsConnectionString()), this._client);
    }
    withRequiredCommand(command, options) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withRequiredCommand(command, options)), this._client);
    }
    withSessionLifetime() {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withSessionLifetime()), this._client);
    }
    withPersistentLifetime() {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withPersistentLifetime()), this._client);
    }
    withLifetimeOf(sourceBuilder) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withLifetimeOf(sourceBuilder)), this._client);
    }
    withParentProcessLifetime(parentProcessId) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withParentProcessLifetime(parentProcessId)), this._client);
    }
    withEnvironment(name, value) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withEnvironment(name, value)), this._client);
    }
    withEnvironmentCallback(callback) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withEnvironmentCallback(callback)), this._client);
    }
    withArgs(args) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withArgs(args)), this._client);
    }
    withArgsCallback(callback) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withArgsCallback(callback)), this._client);
    }
    withReferenceEnvironment(options) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withReferenceEnvironment(options)), this._client);
    }
    withReference(source, options) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withReference(source, options)), this._client);
    }
    withEndpointCallback(endpointName, callback, options) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withEndpointCallback(endpointName, callback, options)), this._client);
    }
    withHttpEndpointCallback(callback, options) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withHttpEndpointCallback(callback, options)), this._client);
    }
    withHttpsEndpointCallback(callback, options) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withHttpsEndpointCallback(callback, options)), this._client);
    }
    withEndpoint(options) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withEndpoint(options)), this._client);
    }
    withEndpointProxySupport(proxyEnabled) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withEndpointProxySupport(proxyEnabled)), this._client);
    }
    withHttpEndpoint(options) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withHttpEndpoint(options)), this._client);
    }
    withHttpsEndpoint(options) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withHttpsEndpoint(options)), this._client);
    }
    withExternalHttpEndpoints() {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withExternalHttpEndpoints()), this._client);
    }
    getEndpoint(name) {
        return new EndpointReferencePromiseImpl(this._promise.then(obj => obj.getEndpoint(name)), this._client);
    }
    asHttp2Service() {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.asHttp2Service()), this._client);
    }
    withUrls(callback) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withUrls(callback)), this._client);
    }
    withUrl(url, options) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withUrl(url, options)), this._client);
    }
    withUrlForEndpoint(endpointName, callback) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)), this._client);
    }
    excludeFromManifest() {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.excludeFromManifest()), this._client);
    }
    waitFor(dependency, options) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.waitFor(dependency, options)), this._client);
    }
    waitForStart(dependency, options) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.waitForStart(dependency, options)), this._client);
    }
    withExplicitStart() {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withExplicitStart()), this._client);
    }
    waitForCompletion(dependency, options) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.waitForCompletion(dependency, options)), this._client);
    }
    withHealthCheck(key) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withHealthCheck(key)), this._client);
    }
    withHttpHealthCheck(options) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withHttpHealthCheck(options)), this._client);
    }
    withCommand(name, displayName, executeCommand, options) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)), this._client);
    }
    withProcessCommand(commandName, displayName, options) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommand(commandName, displayName, options)), this._client);
    }
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommandFactory(commandName, displayName, createProcessSpec, options)), this._client);
    }
    withHttpCommand(path, displayName, options) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withHttpCommand(path, displayName, options)), this._client);
    }
    withDeveloperCertificateTrust(trust) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withDeveloperCertificateTrust(trust)), this._client);
    }
    withCertificateTrustScope(scope) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withCertificateTrustScope(scope)), this._client);
    }
    withHttpsDeveloperCertificate(options) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withHttpsDeveloperCertificate(options)), this._client);
    }
    withoutHttpsCertificate() {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withoutHttpsCertificate()), this._client);
    }
    withRelationship(resourceBuilder, type) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withRelationship(resourceBuilder, type)), this._client);
    }
    withParentRelationship(parent) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withParentRelationship(parent)), this._client);
    }
    withChildRelationship(child) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withChildRelationship(child)), this._client);
    }
    withIconName(iconName, options) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withIconName(iconName, options)), this._client);
    }
    withComputeEnvironment(computeEnvironmentResource) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withComputeEnvironment(computeEnvironmentResource)), this._client);
    }
    withHttpProbe(probeType, options) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withHttpProbe(probeType, options)), this._client);
    }
    excludeFromMcp() {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.excludeFromMcp()), this._client);
    }
    withHidden() {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withHidden()), this._client);
    }
    withHiddenOnCompletion(options) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withHiddenOnCompletion(options)), this._client);
    }
    withImagePushOptions(callback) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withImagePushOptions(callback)), this._client);
    }
    withRemoteImageName(remoteImageName) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withRemoteImageName(remoteImageName)), this._client);
    }
    withRemoteImageTag(remoteImageTag) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withRemoteImageTag(remoteImageTag)), this._client);
    }
    withPipelineStepFactory(stepName, callback, options) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)), this._client);
    }
    withPipelineConfiguration(callback) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withPipelineConfiguration(callback)), this._client);
    }
    withVolume(target, options) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withVolume(target, options)), this._client);
    }
    getResourceName() {
        return this._promise.then(obj => obj.getResourceName());
    }
    onBeforeResourceStarted(callback) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.onBeforeResourceStarted(callback)), this._client);
    }
    onResourceStopped(callback) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.onResourceStopped(callback)), this._client);
    }
    onInitializeResource(callback) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.onInitializeResource(callback)), this._client);
    }
    onResourceEndpointsAllocated(callback) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.onResourceEndpointsAllocated(callback)), this._client);
    }
    onResourceReady(callback) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.onResourceReady(callback)), this._client);
    }
    createExecutionConfiguration() {
        return new ExecutionConfigurationBuilderPromiseImpl(this._promise.then(obj => obj.createExecutionConfiguration()), this._client);
    }
    withHostPort(options) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withHostPort(options)), this._client);
    }
    withDataVolume(options) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withDataVolume(options)), this._client);
    }
    withDataBindMount(source) {
        return new RedisInsightResourcePromiseImpl(this._promise.then(obj => obj.withDataBindMount(source)), this._client);
    }
}
// ============================================================================
// RedisResourceImpl
// ============================================================================
/**
 * A resource that represents a Redis resource independent of the hosting model.
 *
 * A resource that represents a Redis resource independent of the hosting model.
 */
class RedisResourceImpl extends ResourceBuilderBase {
    constructor(handle, client) {
        super(handle, client);
    }
    primaryEndpoint() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/RedisResource.primaryEndpoint', { context: this._handle });
            return new EndpointReferenceImpl(handle, this._client);
        })();
        return new EndpointReferencePromiseImpl(promise, this._client, false);
    }
    host() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/RedisResource.host', { context: this._handle });
            return new EndpointReferenceExpressionImpl(handle, this._client);
        })();
        return new EndpointReferenceExpressionPromiseImpl(promise, this._client, false);
    }
    port() {
        const promise = (async () => {
            const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/RedisResource.port', { context: this._handle });
            return new EndpointReferenceExpressionImpl(handle, this._client);
        })();
        return new EndpointReferenceExpressionPromiseImpl(promise, this._client, false);
    }
    passwordParameter = {
        get: () => {
            const promise = (async () => {
                const handle = await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/RedisResource.passwordParameter', { context: this._handle });
                return new ParameterResourceImpl(handle, this._client);
            })();
            return new ParameterResourcePromiseImpl(promise, this._client, false);
        },
        set: async (value) => {
            value = isPromiseLike(value) ? await value : value;
            await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/RedisResource.setPasswordParameter', { context: this._handle, value });
        }
    };
    async tlsEnabled() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/RedisResource.tlsEnabled', { context: this._handle });
    }
    async connectionStringExpression() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/RedisResource.connectionStringExpression', { context: this._handle });
    }
    async uriExpression() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/RedisResource.uriExpression', { context: this._handle });
    }
    entrypoint = {
        get: async () => {
            return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/RedisResource.entrypoint', { context: this._handle });
        },
        set: async (value) => {
            await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/RedisResource.setEntrypoint', { context: this._handle, value });
        }
    };
    shellExecution = {
        get: async () => {
            return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/RedisResource.shellExecution', { context: this._handle });
        },
        set: async (value) => {
            await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/RedisResource.setShellExecution', { context: this._handle, value });
        }
    };
    async name() {
        return await this._client.invokeCapability('Aspire.Hosting.ApplicationModel/RedisResource.name', { context: this._handle });
    }
    /** @internal */
    async _withContainerRegistryInternal(registry) {
        registry = isPromiseLike(registry) ? await registry : registry;
        const rpcArgs = { builder: this._handle, registry };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerRegistry', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Configures the resource to use the specified container registry for container image operations.
     *
     * This method adds a `ContainerRegistryReferenceAnnotation` to the resource,
     * indicating that the resource should use the specified container registry for container image operations.
     * @param registry The container registry resource builder.
     * @returns The resource builder for chaining.
     */
    withContainerRegistry(registry) {
        return new RedisResourcePromiseImpl(this._withContainerRegistryInternal(registry), this._client);
    }
    /** @internal */
    async _withBindMountInternal(source, target, isReadOnly) {
        const rpcArgs = { builder: this._handle, source, target };
        if (isReadOnly !== undefined)
            rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability('Aspire.Hosting/withBindMount', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Adds a bind mount to a container resource.
     * @param source The source path of the mount. This is the path to the file or directory on the host, relative to the app host project directory.
     * @param target The target path where the file or directory is mounted in the container.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withBindMount(source, target, options) {
        const isReadOnly = options?.isReadOnly;
        return new RedisResourcePromiseImpl(this._withBindMountInternal(source, target, isReadOnly), this._client);
    }
    /** @internal */
    async _withEntrypointInternal(entrypoint) {
        const rpcArgs = { builder: this._handle, entrypoint };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEntrypoint', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Sets the Entrypoint for the container.
     * @param entrypoint The new entrypoint for the container.
     * @returns The resource builder.
     */
    withEntrypoint(entrypoint) {
        return new RedisResourcePromiseImpl(this._withEntrypointInternal(entrypoint), this._client);
    }
    /** @internal */
    async _withImageTagInternal(tag) {
        const rpcArgs = { builder: this._handle, tag };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImageTag', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Allows overriding the image tag on a container.
     * @param tag Tag value.
     * @returns The resource builder.
     */
    withImageTag(tag) {
        return new RedisResourcePromiseImpl(this._withImageTagInternal(tag), this._client);
    }
    /** @internal */
    async _withImageRegistryInternal(registry) {
        const rpcArgs = { builder: this._handle, registry };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImageRegistry', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Allows overriding the image registry on a container.
     * @param registry Registry value.
     * @returns The resource builder.
     */
    withImageRegistry(registry) {
        return new RedisResourcePromiseImpl(this._withImageRegistryInternal(registry), this._client);
    }
    /** @internal */
    async _withImageInternal(image, tag) {
        const rpcArgs = { builder: this._handle, image };
        if (tag !== undefined)
            rpcArgs.tag = tag;
        const result = await this._client.invokeCapability('Aspire.Hosting/withImage', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Allows overriding the image on a container.
     * @param image Image value.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withImage(image, options) {
        const tag = options?.tag;
        return new RedisResourcePromiseImpl(this._withImageInternal(image, tag), this._client);
    }
    /** @internal */
    async _withImageSHA256Internal(sha256) {
        const rpcArgs = { builder: this._handle, sha256 };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImageSHA256', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Allows setting the image to a specific sha256 on a container.
     * @param sha256 Registry value.
     * @returns The resource builder.
     */
    withImageSHA256(sha256) {
        return new RedisResourcePromiseImpl(this._withImageSHA256Internal(sha256), this._client);
    }
    /** @internal */
    async _withContainerRuntimeArgsInternal(args) {
        const rpcArgs = { builder: this._handle, args };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerRuntimeArgs', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Adds a callback to be executed with a list of arguments to add to the container runtime run command when a container resource is started.
     * @param args The arguments to be passed to the container runtime run command when the container resource is started.
     * @returns The resource builder.
     */
    withContainerRuntimeArgs(args) {
        return new RedisResourcePromiseImpl(this._withContainerRuntimeArgsInternal(args), this._client);
    }
    /** @internal */
    async _withLifetimeInternal(lifetime) {
        const rpcArgs = { builder: this._handle, lifetime };
        const result = await this._client.invokeCapability('Aspire.Hosting/withLifetime', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Sets the lifetime behavior of the container resource.
     *
     * Prefer `WithPersistentLifetime``1` or
     * `WithSessionLifetime``1` for new code.
     * Marking a container resource to have a `Persistent` lifetime.
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddContainer("mycontainer", "myimage")
     * .WithPersistentLifetime();
     * builder.Build().Run();
     * ```
     * @param lifetime The lifetime behavior of the container resource. The default behavior is `Session`.
     * @returns The resource builder.
     */
    withLifetime(lifetime) {
        return new RedisResourcePromiseImpl(this._withLifetimeInternal(lifetime), this._client);
    }
    /** @internal */
    async _withImagePullPolicyInternal(pullPolicy) {
        const rpcArgs = { builder: this._handle, pullPolicy };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImagePullPolicy', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Sets the pull policy for the container resource.
     * @param pullPolicy The pull policy behavior for the container resource.
     * @returns The resource builder.
     */
    withImagePullPolicy(pullPolicy) {
        return new RedisResourcePromiseImpl(this._withImagePullPolicyInternal(pullPolicy), this._client);
    }
    /** @internal */
    async _publishAsContainerInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/publishAsContainer', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Changes the resource to be published as a container in the manifest.
     * @returns The resource builder.
     */
    publishAsContainer() {
        return new RedisResourcePromiseImpl(this._publishAsContainerInternal(), this._client);
    }
    /** @internal */
    async _withDockerfileInternal(contextPath, dockerfilePath, stage) {
        const rpcArgs = { builder: this._handle, contextPath };
        if (dockerfilePath !== undefined)
            rpcArgs.dockerfilePath = dockerfilePath;
        if (stage !== undefined)
            rpcArgs.stage = stage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfile', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Causes Aspire to build the specified container image from a Dockerfile.
     * @param contextPath Path to be used as the context for the container image build.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfile(contextPath, options) {
        const dockerfilePath = options?.dockerfilePath;
        const stage = options?.stage;
        return new RedisResourcePromiseImpl(this._withDockerfileInternal(contextPath, dockerfilePath, stage), this._client);
    }
    /** @internal */
    async _withDockerfileFactoryInternal(contextPath, dockerfileFactory, stage) {
        const dockerfileFactoryId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new DockerfileFactoryContextImpl(argHandle, this._client);
            return await dockerfileFactory(arg);
        });
        const rpcArgs = { builder: this._handle, contextPath, dockerfileFactory: dockerfileFactoryId };
        if (stage !== undefined)
            rpcArgs.stage = stage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileFactory', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Builds the specified container image from a Dockerfile generated by an asynchronous factory function.
     *
     * When this method is called, an annotation is added to the `ContainerResource` that specifies the context path
     * and a factory function that generates Dockerfile content. The factory is invoked at build time to produce the Dockerfile,
     * which is then written to a temporary file and used by the orchestrator to build the container image.
     * The `contextPath` is relative to the AppHost directory unless it is fully qualified.
     * The factory function is invoked once during the build process to generate the Dockerfile content.
     * The output is trusted and not validated.
     * Creates a container called `mycontainer` with a dynamically generated Dockerfile.
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddContainer("mycontainer", "myimage")
     * .WithDockerfileFactory("path/to/context", async context =>
     * {
     * var template = await File.ReadAllTextAsync("template.dockerfile", context.CancellationToken);
     * return template.Replace("{{VERSION}}", "1.0");
     * });
     * builder.Build().Run();
     * ```
     * @param contextPath Path to be used as the context for the container image build.
     * @param dockerfileFactory An asynchronous function that returns the Dockerfile content as a string.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileFactory(contextPath, dockerfileFactory, options) {
        const stage = options?.stage;
        return new RedisResourcePromiseImpl(this._withDockerfileFactoryInternal(contextPath, dockerfileFactory, stage), this._client);
    }
    /** @internal */
    async _withContainerNameInternal(name) {
        const rpcArgs = { builder: this._handle, name };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerName', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Overrides the default container name for this resource. By default Aspire generates a unique container name based on the resource name and a random postfix (or a postfix based on a hash of the AppHost project path for persistent container resources). This method allows you to override that behavior with a custom name, but could lead to naming conflicts if the specified name is not unique.
     *
     * Combining this with `Persistent` will allow Aspire to re-use an existing container that was not
     * created by an Aspire AppHost.
     * @param name The desired container name. Must be a valid container name or your runtime will report an error.
     * @returns The resource builder.
     */
    withContainerName(name) {
        return new RedisResourcePromiseImpl(this._withContainerNameInternal(name), this._client);
    }
    /** @internal */
    async _withBuildArgInternal(name, value) {
        value = isPromiseLike(value) ? await value : value;
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuildArg', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Adds a build argument when the container is built from a Dockerfile.
     * @param name The name of the build argument.
     * @param value The build argument value, either a string or a parameter resource.
     * @returns The resource builder.
     */
    withBuildArg(name, value) {
        return new RedisResourcePromiseImpl(this._withBuildArgInternal(name, value), this._client);
    }
    /** @internal */
    async _withBuildSecretInternal(name, value) {
        value = isPromiseLike(value) ? await value : value;
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withParameterBuildSecret', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Adds a secret build argument when the container is built from a Dockerfile.
     * @param name The name of the secret build argument.
     * @param value The resource builder for a parameter resource.
     * @returns The resource builder.
     */
    withBuildSecret(name, value) {
        return new RedisResourcePromiseImpl(this._withBuildSecretInternal(name, value), this._client);
    }
    /** @internal */
    async _withContainerCertificatePathsInternal(customCertificatesDestination, defaultCertificateBundlePaths, defaultCertificateDirectoryPaths) {
        const rpcArgs = { builder: this._handle };
        if (customCertificatesDestination !== undefined)
            rpcArgs.customCertificatesDestination = customCertificatesDestination;
        if (defaultCertificateBundlePaths !== undefined)
            rpcArgs.defaultCertificateBundlePaths = defaultCertificateBundlePaths;
        if (defaultCertificateDirectoryPaths !== undefined)
            rpcArgs.defaultCertificateDirectoryPaths = defaultCertificateDirectoryPaths;
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerCertificatePaths', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Adds container certificate path overrides used for certificate trust at run time.
     * @param options Additional options.
     * @returns The updated resource builder.
     */
    withContainerCertificatePaths(options) {
        const customCertificatesDestination = options?.customCertificatesDestination;
        const defaultCertificateBundlePaths = options?.defaultCertificateBundlePaths;
        const defaultCertificateDirectoryPaths = options?.defaultCertificateDirectoryPaths;
        return new RedisResourcePromiseImpl(this._withContainerCertificatePathsInternal(customCertificatesDestination, defaultCertificateBundlePaths, defaultCertificateDirectoryPaths), this._client);
    }
    /** @internal */
    async _withDockerfileBuilderInternal(contextPath, callback, stage) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new DockerfileBuilderCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, contextPath, callback: callbackId };
        if (stage !== undefined)
            rpcArgs.stage = stage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileBuilder', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Configures the resource to use a programmatically generated Dockerfile
     *
     * This method provides a programmatic way to build Dockerfiles using the `DockerfileBuilder` API
     * instead of string manipulation. Callbacks can be composed by calling this method multiple times - each callback will be invoked
     * in order to build up the final Dockerfile.
     * The `contextPath` is relative to the AppHost directory unless it is fully qualified.
     * Creates a container with a programmatically built Dockerfile using fluent API:
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddContainer("mycontainer", "myimage")
     * .WithDockerfileBuilder("path/to/context", context =>
     * {
     * context.Builder.From("alpine:latest")
     * .WorkDir("/app")
     * .Run("apk add curl")
     * .Copy(".", ".")
     * .Cmd(["./myapp"]);
     * return Task.CompletedTask;
     * });
     * builder.Build().Run();
     * ```
     * @param contextPath Path to be used as the context for the container image build.
     * @param callback A callback that uses the `DockerfileBuilder` API to construct the Dockerfile.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileBuilder(contextPath, callback, options) {
        const stage = options?.stage;
        return new RedisResourcePromiseImpl(this._withDockerfileBuilderInternal(contextPath, callback, stage), this._client);
    }
    /** @internal */
    async _withDockerfileBaseImageInternal(buildImage, runtimeImage) {
        const rpcArgs = { builder: this._handle };
        if (buildImage !== undefined)
            rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined)
            rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileBaseImage', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Configures custom base images for generated Dockerfiles.
     *
     * This extension method allows customization of the base images used in generated Dockerfiles.
     * For multi-stage Dockerfiles (e.g., Python with UV), you can specify separate build and runtime images.
     * Specify custom base images for a Python application:
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddPythonApp("myapp", "path/to/app", "main.py")
     * .WithDockerfileBaseImage(
     * buildImage: "ghcr.io/astral-sh/uv:python3.12-bookworm-slim",
     * runtimeImage: "python:3.12-slim-bookworm");
     * builder.Build().Run();
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileBaseImage(options) {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new RedisResourcePromiseImpl(this._withDockerfileBaseImageInternal(buildImage, runtimeImage), this._client);
    }
    /** @internal */
    async _withContainerNetworkAliasInternal(alias) {
        const rpcArgs = { builder: this._handle, alias };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerNetworkAlias', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Adds a network alias to container resource.
     *
     * Network aliases enable DNS resolution of the container on the network by custom names.
     * By default, containers are accessible on the network using their resource name as a DNS alias.
     * This method allows adding additional aliases for the same container.
     * Multiple aliases can be added by calling this method multiple times.
     * @param alias The network alias for the container.
     * @returns The resource builder.
     */
    withContainerNetworkAlias(alias) {
        return new RedisResourcePromiseImpl(this._withContainerNetworkAliasInternal(alias), this._client);
    }
    /** @internal */
    async _withMcpServerInternal(path, endpointName) {
        const rpcArgs = { builder: this._handle };
        if (path !== undefined)
            rpcArgs.path = path;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withMcpServer', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Marks the resource as hosting a Model Context Protocol (MCP) server on the specified endpoint.
     *
     * This method adds an `McpServerEndpointAnnotation` to the resource, enabling the Aspire tooling
     * to discover and proxy the MCP server exposed by the resource.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withMcpServer(options) {
        const path = options?.path;
        const endpointName = options?.endpointName;
        return new RedisResourcePromiseImpl(this._withMcpServerInternal(path, endpointName), this._client);
    }
    /** @internal */
    async _withOtlpExporterInternal(protocol) {
        const rpcArgs = { builder: this._handle };
        if (protocol !== undefined)
            rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability('Aspire.Hosting/withOtlpExporter', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Configures OTLP telemetry export
     * @param options Additional options.
     */
    withOtlpExporter(options) {
        const protocol = options?.protocol;
        return new RedisResourcePromiseImpl(this._withOtlpExporterInternal(protocol), this._client);
    }
    /** @internal */
    async _publishAsConnectionStringInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/publishAsConnectionString', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Changes the resource to be published as a connection string reference in the manifest.
     * @returns The resource builder.
     */
    publishAsConnectionString() {
        return new RedisResourcePromiseImpl(this._publishAsConnectionStringInternal(), this._client);
    }
    /** @internal */
    async _withRequiredCommandInternal(command, helpLink) {
        const rpcArgs = { builder: this._handle, command };
        if (helpLink !== undefined)
            rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability('Aspire.Hosting/withRequiredCommand', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Declares that a resource requires a specific command/executable to be available on the local machine PATH before it can start.
     *
     * The command is considered valid if either:
     * 1. It is an absolute or relative path (contains a directory separator) that points to an existing file, or
     * 2. It is discoverable on the current process PATH (respecting PATHEXT on Windows).
     * If the command is not found, a warning message will be logged but the resource will be allowed to attempt to start.
     * @param command The command string (file name or path) that should be validated.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withRequiredCommand(command, options) {
        const helpLink = options?.helpLink;
        return new RedisResourcePromiseImpl(this._withRequiredCommandInternal(command, helpLink), this._client);
    }
    /** @internal */
    async _withSessionLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withSessionLifetime', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a session lifetime.
     * @returns The resource builder.
     */
    withSessionLifetime() {
        return new RedisResourcePromiseImpl(this._withSessionLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withPersistentLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPersistentLifetime', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime.
     * @returns The resource builder.
     */
    withPersistentLifetime() {
        return new RedisResourcePromiseImpl(this._withPersistentLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withLifetimeOfInternal(sourceBuilder) {
        sourceBuilder = isPromiseLike(sourceBuilder) ? await sourceBuilder : sourceBuilder;
        const rpcArgs = { builder: this._handle, sourceBuilder };
        const result = await this._client.invokeCapability('Aspire.Hosting/withLifetimeOf', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to match the lifetime of another resource.
     *
     * The resource lifetime is evaluated from `sourceBuilder` when the application model is prepared, so later lifetime
     * changes to the source resource are reflected by this resource.
     * @param sourceBuilder The resource builder whose lifetime should be used.
     * @returns The resource builder.
     */
    withLifetimeOf(sourceBuilder) {
        return new RedisResourcePromiseImpl(this._withLifetimeOfInternal(sourceBuilder), this._client);
    }
    /** @internal */
    async _withParentProcessLifetimeInternal(parentProcessId) {
        const rpcArgs = { builder: this._handle, parentProcessId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withParentProcessLifetime', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime that ends when a parent process exits.
     * @param parentProcessId The ID of the parent process to monitor.
     * @returns The resource builder.
     */
    withParentProcessLifetime(parentProcessId) {
        return new RedisResourcePromiseImpl(this._withParentProcessLifetimeInternal(parentProcessId), this._client);
    }
    /** @internal */
    async _withEnvironmentInternal(name, value) {
        value = isPromiseLike(value) ? await value : value;
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEnvironment', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /** Sets an environment variable */
    withEnvironment(name, value) {
        return new RedisResourcePromiseImpl(this._withEnvironmentInternal(name, value), this._client);
    }
    /** @internal */
    async _withEnvironmentCallbackInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new EnvironmentCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEnvironmentCallback', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Allows for the population of environment variables on a resource.
     * @param callback A callback that allows for deferred execution for computing many environment variables. This runs after resources have been allocated by the orchestrator and allows access to other resources to resolve computed data, e.g. connection strings, ports.
     * @returns The resource builder.
     */
    withEnvironmentCallback(callback) {
        return new RedisResourcePromiseImpl(this._withEnvironmentCallbackInternal(callback), this._client);
    }
    /** @internal */
    async _withConnectionPropertyInternal(name, value) {
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withConnectionProperty', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Adds a connection property annotation to the resource being built.
     * @param name The name of the connection property to add.
     * @param value The value to assign to the connection property, specified as a string or reference expression.
     * @returns The same resource builder instance with the specified connection property annotation applied.
     */
    withConnectionProperty(name, value) {
        return new RedisResourcePromiseImpl(this._withConnectionPropertyInternal(name, value), this._client);
    }
    /** @internal */
    async _withArgsInternal(args) {
        const rpcArgs = { builder: this._handle, args };
        const result = await this._client.invokeCapability('Aspire.Hosting/withArgs', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Adds arguments to be passed to a resource that supports arguments when it is launched.
     * @param args The arguments to be passed to the resource when it is started.
     * @returns The resource builder.
     */
    withArgs(args) {
        return new RedisResourcePromiseImpl(this._withArgsInternal(args), this._client);
    }
    /** @internal */
    async _withArgsCallbackInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new CommandLineArgsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withArgsCallback', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Adds a callback to be executed with a list of command-line arguments when a resource is started.
     * @param callback A callback that allows for deferred execution for computing arguments. This runs after resources have been allocated by the orchestrator and allows access to other resources to resolve computed data, e.g. connection strings, ports.
     * @returns The resource builder.
     */
    withArgsCallback(callback) {
        return new RedisResourcePromiseImpl(this._withArgsCallbackInternal(callback), this._client);
    }
    /** @internal */
    async _withReferenceEnvironmentInternal(options) {
        const rpcArgs = { builder: this._handle, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withReferenceEnvironment', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Configures how information is injected into environment variables when the resource references other resources.
     * @param options Options controlling which reference information is emitted.
     * @returns The resource builder.
     */
    withReferenceEnvironment(options) {
        return new RedisResourcePromiseImpl(this._withReferenceEnvironmentInternal(options), this._client);
    }
    /** @internal */
    async _withReferenceInternal(source, connectionName, optional, name) {
        source = isPromiseLike(source) ? await source : source;
        const rpcArgs = { builder: this._handle, source };
        if (connectionName !== undefined)
            rpcArgs.connectionName = connectionName;
        if (optional !== undefined)
            rpcArgs.optional = optional;
        if (name !== undefined)
            rpcArgs.name = name;
        const result = await this._client.invokeCapability('Aspire.Hosting/withReference', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Adds a reference to another resource
     * @param options Additional options.
     */
    withReference(source, options) {
        const connectionName = options?.connectionName;
        const optional = options?.optional;
        const name = options?.name;
        return new RedisResourcePromiseImpl(this._withReferenceInternal(source, connectionName, optional, name), this._client);
    }
    /**
     * Retrieves the value of a specified connection property from the resource's connection properties.
     *
     * Throws a KeyNotFoundException if the specified key does not exist in the resource's
     * connection properties.
     * @param key The key of the connection property to retrieve. Cannot be null.
     * @returns The value associated with the specified connection property key.
     */
    async getConnectionProperty(key) {
        const rpcArgs = { resource: this._handle, key };
        return await this._client.invokeCapability('Aspire.Hosting/getConnectionProperty', rpcArgs);
    }
    /** @internal */
    async _withEndpointCallbackInternal(endpointName, callback, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpointCallback', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Updates a named endpoint via callback
     * @param options Additional options.
     */
    withEndpointCallback(endpointName, callback, options) {
        const createIfNotExists = options?.createIfNotExists;
        return new RedisResourcePromiseImpl(this._withEndpointCallbackInternal(endpointName, callback, createIfNotExists), this._client);
    }
    /** @internal */
    async _withHttpEndpointCallbackInternal(callback, name, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        if (name !== undefined)
            rpcArgs.name = name;
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpEndpointCallback', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Updates an HTTP endpoint via callback
     * @param options Additional options.
     */
    withHttpEndpointCallback(callback, options) {
        const name = options?.name;
        const createIfNotExists = options?.createIfNotExists;
        return new RedisResourcePromiseImpl(this._withHttpEndpointCallbackInternal(callback, name, createIfNotExists), this._client);
    }
    /** @internal */
    async _withHttpsEndpointCallbackInternal(callback, name, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        if (name !== undefined)
            rpcArgs.name = name;
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpsEndpointCallback', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Updates an HTTPS endpoint via callback
     * @param options Additional options.
     */
    withHttpsEndpointCallback(callback, options) {
        const name = options?.name;
        const createIfNotExists = options?.createIfNotExists;
        return new RedisResourcePromiseImpl(this._withHttpsEndpointCallbackInternal(callback, name, createIfNotExists), this._client);
    }
    /** @internal */
    async _withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (scheme !== undefined)
            rpcArgs.scheme = scheme;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        if (isExternal !== undefined)
            rpcArgs.isExternal = isExternal;
        if (protocol !== undefined)
            rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpoint', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Adds a network endpoint
     * @param options Additional options.
     * @returns The resource builder.
     */
    withEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const scheme = options?.scheme;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        const isExternal = options?.isExternal;
        const protocol = options?.protocol;
        return new RedisResourcePromiseImpl(this._withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol), this._client);
    }
    /** @internal */
    async _withEndpointProxySupportInternal(proxyEnabled) {
        const rpcArgs = { builder: this._handle, proxyEnabled };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpointProxySupport', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Set whether a resource can use proxied endpoints or whether they should be disabled for all endpoints belonging to the resource. If set to `false`, endpoints belonging to the resource will ignore the configured proxy settings and run proxy-less.
     *
     * This method is intended to support scenarios with persistent lifetime resources where it is desirable for the resource to be accessible over the same
     * port whether the Aspire application is running or not. Proxied endpoints bind ports that are only accessible while the Aspire application is running.
     * The user needs to be careful to ensure that endpoints are using unique ports when disabling proxy support as by default for proxy-less
     * endpoints, Aspire will allocate the target port as the host port, which will increase the chance of port conflicts.
     * @param proxyEnabled Should endpoints for the resource support using a proxy?
     * @returns The resource builder.
     */
    withEndpointProxySupport(proxyEnabled) {
        return new RedisResourcePromiseImpl(this._withEndpointProxySupportInternal(proxyEnabled), this._client);
    }
    /** @internal */
    async _withHttpEndpointInternal(port, targetPort, name, env, isProxied) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpEndpoint', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP endpoint
     *
     * If an endpoint with the same name already exists on the resource, the existing endpoint is updated
     * with any non-null parameter values. Parameters left as `null` will not modify the existing endpoint's values.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new RedisResourcePromiseImpl(this._withHttpEndpointInternal(port, targetPort, name, env, isProxied), this._client);
    }
    /** @internal */
    async _withHttpsEndpointInternal(port, targetPort, name, env, isProxied) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpsEndpoint', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTPS endpoint
     *
     * If an endpoint with the same name already exists on the resource, the existing endpoint is updated
     * with any non-null parameter values. Parameters left as `null` will not modify the existing endpoint's values.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpsEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new RedisResourcePromiseImpl(this._withHttpsEndpointInternal(port, targetPort, name, env, isProxied), this._client);
    }
    /** @internal */
    async _withExternalHttpEndpointsInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExternalHttpEndpoints', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Marks existing http or https endpoints on a resource as external.
     * @returns The resource builder.
     */
    withExternalHttpEndpoints() {
        return new RedisResourcePromiseImpl(this._withExternalHttpEndpointsInternal(), this._client);
    }
    /**
     * Gets an endpoint reference
     * @param name The name of the endpoint.
     * @returns An `EndpointReference` that can be used to resolve the address of the endpoint after resource allocation has occurred.
     */
    getEndpoint(name) {
        const promise = (async () => {
            const rpcArgs = { builder: this._handle, name };
            const handle = await this._client.invokeCapability('Aspire.Hosting/getEndpoint', rpcArgs);
            return new EndpointReferenceImpl(handle, this._client);
        })();
        return new EndpointReferencePromiseImpl(promise, this._client);
    }
    /** @internal */
    async _asHttp2ServiceInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/asHttp2Service', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to mark all endpoints' transport as HTTP/2. This is useful for HTTP/2 services that need prior knowledge.
     * @returns The resource builder.
     */
    asHttp2Service() {
        return new RedisResourcePromiseImpl(this._asHttp2ServiceInternal(), this._client);
    }
    /** @internal */
    async _withUrlsInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new ResourceUrlsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrls', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to customize the URLs displayed for the resource.
     * @param callback The callback that will customize URLs for the resource.
     * @returns The resource builder.
     */
    withUrls(callback) {
        return new RedisResourcePromiseImpl(this._withUrlsInternal(callback), this._client);
    }
    /** @internal */
    async _withUrlInternal(url, displayText) {
        const rpcArgs = { builder: this._handle, url };
        if (displayText !== undefined)
            rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrl', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Adds or modifies displayed URLs
     * @param options Additional options.
     */
    withUrl(url, options) {
        const displayText = options?.displayText;
        return new RedisResourcePromiseImpl(this._withUrlInternal(url, displayText), this._client);
    }
    /** @internal */
    async _withUrlForEndpointInternal(endpointName, callback) {
        const callbackId = registerCallback(async (objData) => {
            const obj = wrapIfHandle(objData);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrlForEndpoint', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to update the URL displayed for the endpoint with the specified name.
     * @param endpointName The name of the endpoint to customize the URL for.
     * @param callback The callback that will customize the URL.
     * @returns The resource builder.
     */
    withUrlForEndpoint(endpointName, callback) {
        return new RedisResourcePromiseImpl(this._withUrlForEndpointInternal(endpointName, callback), this._client);
    }
    /** @internal */
    async _excludeFromManifestInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromManifest', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Excludes a resource from being published to the manifest.
     * @returns The resource builder.
     */
    excludeFromManifest() {
        return new RedisResourcePromiseImpl(this._excludeFromManifestInternal(), this._client);
    }
    /** @internal */
    async _waitForInternal(dependency, waitBehavior) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (waitBehavior !== undefined)
            rpcArgs.waitBehavior = waitBehavior;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitFor', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Waits for another resource to be ready
     * @param options Additional options.
     */
    waitFor(dependency, options) {
        const waitBehavior = options?.waitBehavior;
        return new RedisResourcePromiseImpl(this._waitForInternal(dependency, waitBehavior), this._client);
    }
    /** @internal */
    async _waitForStartInternal(dependency, waitBehavior) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (waitBehavior !== undefined)
            rpcArgs.waitBehavior = waitBehavior;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitForStart', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Waits for another resource to start
     * @param options Additional options.
     */
    waitForStart(dependency, options) {
        const waitBehavior = options?.waitBehavior;
        return new RedisResourcePromiseImpl(this._waitForStartInternal(dependency, waitBehavior), this._client);
    }
    /** @internal */
    async _withExplicitStartInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExplicitStart', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Prevents resource from starting automatically
     * @returns The resource builder.
     */
    withExplicitStart() {
        return new RedisResourcePromiseImpl(this._withExplicitStartInternal(), this._client);
    }
    /** @internal */
    async _waitForCompletionInternal(dependency, exitCode) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitForResourceCompletion', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Waits for the dependency resource to enter the Exited or Finished state before starting the resource.
     * @param dependency The resource builder for the dependency resource.
     * @param options Additional options.
     * @returns The resource builder.
     */
    waitForCompletion(dependency, options) {
        const exitCode = options?.exitCode;
        return new RedisResourcePromiseImpl(this._waitForCompletionInternal(dependency, exitCode), this._client);
    }
    /** @internal */
    async _withHealthCheckInternal(key) {
        const rpcArgs = { builder: this._handle, key };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHealthCheck', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Adds a health check by key
     * @param key The key for the health check.
     * @returns The resource builder.
     */
    withHealthCheck(key) {
        return new RedisResourcePromiseImpl(this._withHealthCheckInternal(key), this._client);
    }
    /** @internal */
    async _withHttpHealthCheckInternal(path, statusCode, endpointName) {
        const rpcArgs = { builder: this._handle };
        if (path !== undefined)
            rpcArgs.path = path;
        if (statusCode !== undefined)
            rpcArgs.statusCode = statusCode;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpHealthCheck', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Adds a health check to the resource which is mapped to a specific endpoint.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpHealthCheck(options) {
        const path = options?.path;
        const statusCode = options?.statusCode;
        const endpointName = options?.endpointName;
        return new RedisResourcePromiseImpl(this._withHttpHealthCheckInternal(path, statusCode, endpointName), this._client);
    }
    /** @internal */
    async _withCommandInternal(name, displayName, executeCommand, commandOptions) {
        const executeCommandId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined)
            rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability('Aspire.Hosting/withCommand', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Adds a resource command
     *
     * The `WithCommand` method is used to add commands to the resource. Commands are displayed in the dashboard
     * and can be executed by a user using the dashboard UI.
     * When a command is executed, the `executeCommand` callback is called and is run inside the Aspire host.
     * @param name The name of the command. The name uniquely identifies the command.
     * @param displayName The display name visible in UI.
     * @param executeCommand A callback that is executed when the command is executed. The callback is run inside the Aspire host. The callback result is used to indicate success or failure in the UI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withCommand(name, displayName, executeCommand, options) {
        const commandOptions = options?.commandOptions;
        return new RedisResourcePromiseImpl(this._withCommandInternal(name, displayName, executeCommand, commandOptions), this._client);
    }
    /** @internal */
    async _withProcessCommandInternal(commandName, displayName, options) {
        const rpcArgs = { builder: this._handle, commandName, displayName, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommand', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /** Adds a command to the resource that starts a local process when invoked. */
    withProcessCommand(commandName, displayName, options) {
        return new RedisResourcePromiseImpl(this._withProcessCommandInternal(commandName, displayName, options), this._client);
    }
    /** @internal */
    async _withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options) {
        const createProcessSpecId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await createProcessSpec(arg);
        });
        const rpcArgs = { builder: this._handle, commandName, displayName, createProcessSpec: createProcessSpecId };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommandFactory', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Adds a command to the resource that starts a local process created by a callback when invoked.
     * @param options Additional options.
     */
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new RedisResourcePromiseImpl(this._withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options), this._client);
    }
    /** @internal */
    async _withHttpCommandInternal(path, displayName, options) {
        const rpcArgs = { builder: this._handle, path, displayName };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpCommand', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP resource command
     * @param options Additional options.
     */
    withHttpCommand(path, displayName, options) {
        return new RedisResourcePromiseImpl(this._withHttpCommandInternal(path, displayName, options), this._client);
    }
    /** @internal */
    async _withDeveloperCertificateTrustInternal(trust) {
        const rpcArgs = { builder: this._handle, trust };
        const result = await this._client.invokeCapability('Aspire.Hosting/withDeveloperCertificateTrust', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Indicates whether developer certificates should be treated as trusted certificate authorities for the resource at run time. Currently this indicates trust for the ASP.NET Core developer certificate. The developer certificate will only be trusted when running in local development scenarios; in publish mode resources will use their default certificate trust.
     *
     * Disable trust for app host managed developer certificate(s) for a container resource.
     * ```
     * var container = builder.AddContainer("my-service", "my-service:latest")
     * .WithDeveloperCertificateTrust(false);
     * ```
     * Disable automatic trust for app host managed developer certificate(s), but explicitly enable it for a specific resource.
     * ```
     * var builder = DistributedApplication.CreateBuilder(new DistributedApplicationOptions()
     * {
     * Args = args,
     * TrustDeveloperCertificate = false,
     * });
     * var project = builder.AddProject<MyService>("my-service")
     * .WithDeveloperCertificateTrust(true);
     * ```
     * @param trust Indicates whether the developer certificate should be treated as trusted.
     * @returns The resource builder.
     */
    withDeveloperCertificateTrust(trust) {
        return new RedisResourcePromiseImpl(this._withDeveloperCertificateTrustInternal(trust), this._client);
    }
    /** @internal */
    async _withCertificateTrustScopeInternal(scope) {
        const rpcArgs = { builder: this._handle, scope };
        const result = await this._client.invokeCapability('Aspire.Hosting/withCertificateTrustScope', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Sets the certificate trust scope
     *
     * The default scope if not overridden is `Append` which means that custom certificate
     * authorities should be appended to the default trusted certificate authorities for the resource. Setting the scope to
     * `Override` indicates the set of certificates in referenced
     * `CertificateAuthorityCollection` (and optionally Aspire developer certificiates) should be used as the
     * exclusive source of trust for a resource.
     * In all cases, this is a best effort implementation as not all resources support full customization of certificate
     * trust.
     * Set the scope for custom certificate authorities to override the default trusted certificate authorities for a container resource.
     * ```
     * var caCollection = builder.AddCertificateAuthorityCollection("my-cas")
     * .WithCertificate(new X509Certificate2("my-ca.pem"));
     * var container = builder.AddContainer("my-service", "my-service:latest")
     * .WithCertificateAuthorityCollection(caCollection)
     * .WithCertificateTrustScope(CertificateTrustScope.Override);
     * ```
     * @param scope The scope to apply to custom certificate authorities associated with the resource.
     * @returns The resource builder.
     */
    withCertificateTrustScope(scope) {
        return new RedisResourcePromiseImpl(this._withCertificateTrustScopeInternal(scope), this._client);
    }
    /** @internal */
    async _withHttpsDeveloperCertificateInternal(password) {
        password = isPromiseLike(password) ? await password : password;
        const rpcArgs = { builder: this._handle };
        if (password !== undefined)
            rpcArgs.password = password;
        const result = await this._client.invokeCapability('Aspire.Hosting/withParameterHttpsDeveloperCertificate', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Indicates that a resource should use the developer certificate key pair for HTTPS endpoints at run time. Currently this indicates use of the ASP.NET Core developer certificate. The developer certificate will only be used when running in local development scenarios; in publish mode resources will use their default certificate configuration.
     *
     * Use the developer certificate for HTTPS/TLS endpoints on a container resource:
     * ```
     * builder.AddContainer("my-service", "my-image")
     * .WithHttpsDeveloperCertificate()
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpsDeveloperCertificate(options) {
        let password = options?.password;
        return new RedisResourcePromiseImpl(this._withHttpsDeveloperCertificateInternal(password), this._client);
    }
    /** @internal */
    async _withoutHttpsCertificateInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withoutHttpsCertificate', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Disable HTTPS/TLS server certificate configuration for the resource. No HTTPS/TLS termination configuration will be applied.
     *
     * Disable HTTPS certificate configuration for a Redis resource:
     * ```
     * var redis = builder.AddRedis("cache")
     * .WithoutHttpsCertificate();
     * ```
     * @returns The resource builder.
     */
    withoutHttpsCertificate() {
        return new RedisResourcePromiseImpl(this._withoutHttpsCertificateInternal(), this._client);
    }
    /** @internal */
    async _withRelationshipInternal(resourceBuilder, type) {
        resourceBuilder = isPromiseLike(resourceBuilder) ? await resourceBuilder : resourceBuilder;
        const rpcArgs = { builder: this._handle, resourceBuilder, type };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderRelationship', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Adds a relationship to another resource using its builder.
     * @param resourceBuilder The resource builder that the relationship is to.
     * @param type The relationship type.
     * @returns A resource builder.
     */
    withRelationship(resourceBuilder, type) {
        return new RedisResourcePromiseImpl(this._withRelationshipInternal(resourceBuilder, type), this._client);
    }
    /** @internal */
    async _withParentRelationshipInternal(parent) {
        parent = isPromiseLike(parent) ? await parent : parent;
        const rpcArgs = { builder: this._handle, parent };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderParentRelationship', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Sets the parent relationship
     * @param parent The parent of `builder`.
     * @returns A resource builder.
     */
    withParentRelationship(parent) {
        return new RedisResourcePromiseImpl(this._withParentRelationshipInternal(parent), this._client);
    }
    /** @internal */
    async _withChildRelationshipInternal(child) {
        child = isPromiseLike(child) ? await child : child;
        const rpcArgs = { builder: this._handle, child };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderChildRelationship', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Sets a child relationship
     * @param child The child of `builder`.
     * @returns A resource builder.
     */
    withChildRelationship(child) {
        return new RedisResourcePromiseImpl(this._withChildRelationshipInternal(child), this._client);
    }
    /** @internal */
    async _withIconNameInternal(iconName, iconVariant) {
        const rpcArgs = { builder: this._handle, iconName };
        if (iconVariant !== undefined)
            rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability('Aspire.Hosting/withIconName', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Specifies the icon to use when displaying the resource in the dashboard.
     * @param iconName The name of the FluentUI icon to use. See https://aka.ms/fluentui-system-icons for available icons.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withIconName(iconName, options) {
        const iconVariant = options?.iconVariant;
        return new RedisResourcePromiseImpl(this._withIconNameInternal(iconName, iconVariant), this._client);
    }
    /** @internal */
    async _withComputeEnvironmentInternal(computeEnvironmentResource) {
        computeEnvironmentResource = isPromiseLike(computeEnvironmentResource) ? await computeEnvironmentResource : computeEnvironmentResource;
        const rpcArgs = { builder: this._handle, computeEnvironmentResource };
        const result = await this._client.invokeCapability('Aspire.Hosting/withComputeEnvironment', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Configures the compute environment for the compute resource.
     *
     * This method allows associating a specific compute environment with the compute resource.
     * @param computeEnvironmentResource The compute environment resource to associate with the compute resource.
     * @returns The resource builder.
     */
    withComputeEnvironment(computeEnvironmentResource) {
        return new RedisResourcePromiseImpl(this._withComputeEnvironmentInternal(computeEnvironmentResource), this._client);
    }
    /** @internal */
    async _withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName) {
        const rpcArgs = { builder: this._handle, probeType };
        if (path !== undefined)
            rpcArgs.path = path;
        if (initialDelaySeconds !== undefined)
            rpcArgs.initialDelaySeconds = initialDelaySeconds;
        if (periodSeconds !== undefined)
            rpcArgs.periodSeconds = periodSeconds;
        if (timeoutSeconds !== undefined)
            rpcArgs.timeoutSeconds = timeoutSeconds;
        if (failureThreshold !== undefined)
            rpcArgs.failureThreshold = failureThreshold;
        if (successThreshold !== undefined)
            rpcArgs.successThreshold = successThreshold;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpProbe', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP health probe to the resource
     * @param options Additional options.
     */
    withHttpProbe(probeType, options) {
        const path = options?.path;
        const initialDelaySeconds = options?.initialDelaySeconds;
        const periodSeconds = options?.periodSeconds;
        const timeoutSeconds = options?.timeoutSeconds;
        const failureThreshold = options?.failureThreshold;
        const successThreshold = options?.successThreshold;
        const endpointName = options?.endpointName;
        return new RedisResourcePromiseImpl(this._withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName), this._client);
    }
    /** @internal */
    async _excludeFromMcpInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromMcp', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Exclude the resource from MCP operations using the Aspire MCP server. The resource is excluded from results that return resources, console logs and telemetry.
     * @returns The resource builder.
     */
    excludeFromMcp() {
        return new RedisResourcePromiseImpl(this._excludeFromMcpInternal(), this._client);
    }
    /** @internal */
    async _withHiddenInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHidden', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists
     *
     * Use this method to hide resources that are implementation details and should never be displayed by default.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @returns The resource builder.
     */
    withHidden() {
        return new RedisResourcePromiseImpl(this._withHiddenInternal(), this._client);
    }
    /** @internal */
    async _withHiddenOnCompletionInternal(exitCode, exitCodes) {
        const rpcArgs = { builder: this._handle };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        if (exitCodes !== undefined)
            rpcArgs.exitCodes = exitCodes;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHiddenOnCompletion', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists after successful completion
     *
     * This method is useful for one-off resources such as setup scripts, migrations, or build steps that should remain visible while running
     * and then be hidden after successful completion.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHiddenOnCompletion(options) {
        const exitCode = options?.exitCode;
        const exitCodes = options?.exitCodes;
        return new RedisResourcePromiseImpl(this._withHiddenOnCompletionInternal(exitCode, exitCodes), this._client);
    }
    /** @internal */
    async _withImagePushOptionsInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ContainerImagePushOptionsCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImagePushOptions', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Adds an asynchronous callback to configure container image push options for the resource.
     *
     * This method allows customization of how container images are named and tagged when pushed to a registry using an asynchronous callback.
     * Use this overload when the callback needs to perform asynchronous operations such as retrieving configuration values from external sources.
     * The callback receives a `ContainerImagePushOptionsCallbackContext` that provides access to the resource
     * and the `ContainerImagePushOptions` that can be modified.
     * Multiple callbacks can be registered on the same resource, and they will be invoked in the order they were added.
     * @param callback The asynchronous callback to configure push options.
     * @returns The resource builder.
     */
    withImagePushOptions(callback) {
        return new RedisResourcePromiseImpl(this._withImagePushOptionsInternal(callback), this._client);
    }
    /** @internal */
    async _withRemoteImageNameInternal(remoteImageName) {
        const rpcArgs = { builder: this._handle, remoteImageName };
        const result = await this._client.invokeCapability('Aspire.Hosting/withRemoteImageName', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Sets the remote image name (without registry endpoint or tag) for container push operations.
     *
     * Use this with `withRemoteImageTag` to fully customize the image reference used for container push operations.
     * @param remoteImageName The remote image name (e.g., "myapp" or "myorg/myapp").
     * @returns The resource builder.
     */
    withRemoteImageName(remoteImageName) {
        return new RedisResourcePromiseImpl(this._withRemoteImageNameInternal(remoteImageName), this._client);
    }
    /** @internal */
    async _withRemoteImageTagInternal(remoteImageTag) {
        const rpcArgs = { builder: this._handle, remoteImageTag };
        const result = await this._client.invokeCapability('Aspire.Hosting/withRemoteImageTag', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Sets the remote image tag for container push operations.
     *
     * Use this with `withRemoteImageName` to fully customize the image reference used for container push operations.
     * @param remoteImageTag The remote image tag (e.g., "latest", "v1.0.0").
     * @returns The resource builder.
     */
    withRemoteImageTag(remoteImageTag) {
        return new RedisResourcePromiseImpl(this._withRemoteImageTagInternal(remoteImageTag), this._client);
    }
    /** @internal */
    async _withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new PipelineStepContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined)
            rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined)
            rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined)
            rpcArgs.tags = tags;
        if (description !== undefined)
            rpcArgs.description = description;
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineStepFactory', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Adds a pipeline step to the resource that will be executed during deployment.
     * @param stepName The unique name of the pipeline step.
     * @param callback The callback to execute when the step runs.
     * @param options Additional options.
     * @returns The resource builder for chaining.
     */
    withPipelineStepFactory(stepName, callback, options) {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new RedisResourcePromiseImpl(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description), this._client);
    }
    /** @internal */
    async _withPipelineConfigurationInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new PipelineConfigurationContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineConfiguration', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to be executed during the pipeline configuration phase, allowing modification of step dependencies and relationships.
     * @param callback The callback function to execute during the configuration phase.
     * @returns The resource builder for chaining.
     */
    withPipelineConfiguration(callback) {
        return new RedisResourcePromiseImpl(this._withPipelineConfigurationInternal(callback), this._client);
    }
    /** @internal */
    async _withVolumeInternal(target, name, isReadOnly) {
        const rpcArgs = { resource: this._handle, target };
        if (name !== undefined)
            rpcArgs.name = name;
        if (isReadOnly !== undefined)
            rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability('Aspire.Hosting/withVolume', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Adds a volume to a container resource.
     *
     * Volumes persist data across container restarts. Named volumes are managed
     * by Docker/Podman and stored in a system-managed location.
     * Why this wrapper exists: The original `ContainerResourceBuilderExtensions.WithVolume`
     * has parameter order `(name?, target, isReadOnly)` where the optional `name` comes first.
     * This wrapper reorders parameters to `(target, name?, isReadOnly)` so the required `target`
     * parameter comes first, providing a better API for polyglot consumers.
     * @param target The mount path inside the container.
     * @param options Additional options.
     * @returns The same resource builder handle for chaining.
     */
    withVolume(target, options) {
        const name = options?.name;
        const isReadOnly = options?.isReadOnly;
        return new RedisResourcePromiseImpl(this._withVolumeInternal(target, name, isReadOnly), this._client);
    }
    /**
     * Gets the name of the resource from a builder.
     *
     * Why this wrapper exists: This capability accesses a nested property
     * (`resource.Resource.Name`) which requires a wrapper method. There is no single
     * .NET method that returns just the resource name that could be annotated directly.
     * @returns The resource name.
     */
    async getResourceName() {
        const rpcArgs = { resource: this._handle };
        return await this._client.invokeCapability('Aspire.Hosting/getResourceName', rpcArgs);
    }
    /** @internal */
    async _onBeforeResourceStartedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new BeforeResourceStartedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onBeforeResourceStarted', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the BeforeResourceStarted event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onBeforeResourceStarted(callback) {
        return new RedisResourcePromiseImpl(this._onBeforeResourceStartedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceStoppedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceStoppedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceStopped', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceStopped event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceStopped(callback) {
        return new RedisResourcePromiseImpl(this._onResourceStoppedInternal(callback), this._client);
    }
    /** @internal */
    async _onConnectionStringAvailableInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ConnectionStringAvailableEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onConnectionStringAvailable', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ConnectionStringAvailable event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onConnectionStringAvailable(callback) {
        return new RedisResourcePromiseImpl(this._onConnectionStringAvailableInternal(callback), this._client);
    }
    /** @internal */
    async _onInitializeResourceInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new InitializeResourceEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onInitializeResource', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the InitializeResource event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onInitializeResource(callback) {
        return new RedisResourcePromiseImpl(this._onInitializeResourceInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceEndpointsAllocatedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceEndpointsAllocatedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceEndpointsAllocated', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceEndpointsAllocated event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceEndpointsAllocated(callback) {
        return new RedisResourcePromiseImpl(this._onResourceEndpointsAllocatedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceReadyInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceReadyEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceReady', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceReady event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceReady(callback) {
        return new RedisResourcePromiseImpl(this._onResourceReadyInternal(callback), this._client);
    }
    /**
     * Creates an execution configuration builder for the specified resource.
     * @returns The execution configuration builder.
     */
    createExecutionConfiguration() {
        const promise = (async () => {
            const rpcArgs = { resource: this._handle };
            const handle = await this._client.invokeCapability('Aspire.Hosting/createExecutionConfiguration', rpcArgs);
            return new ExecutionConfigurationBuilderImpl(handle, this._client);
        })();
        return new ExecutionConfigurationBuilderPromiseImpl(promise, this._client);
    }
    /** @internal */
    async _withRedisCommanderInternal(configureContainer, containerName) {
        const configureContainerId = configureContainer ? registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new RedisCommanderResourceImpl(objHandle, this._client);
            await configureContainer(obj);
        }) : undefined;
        const rpcArgs = { builder: this._handle };
        if (configureContainer !== undefined)
            rpcArgs.configureContainer = configureContainerId;
        if (containerName !== undefined)
            rpcArgs.containerName = containerName;
        const result = await this._client.invokeCapability('Aspire.Hosting.Redis/withRedisCommander', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Adds Redis Commander management UI
     *
     * This version of the package defaults to the tag of the container image.
     * @param options Additional options.
     */
    withRedisCommander(options) {
        const configureContainer = options?.configureContainer;
        const containerName = options?.containerName;
        return new RedisResourcePromiseImpl(this._withRedisCommanderInternal(configureContainer, containerName), this._client);
    }
    /** @internal */
    async _withRedisInsightInternal(configureContainer, containerName) {
        const configureContainerId = configureContainer ? registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new RedisInsightResourceImpl(objHandle, this._client);
            await configureContainer(obj);
        }) : undefined;
        const rpcArgs = { builder: this._handle };
        if (configureContainer !== undefined)
            rpcArgs.configureContainer = configureContainerId;
        if (containerName !== undefined)
            rpcArgs.containerName = containerName;
        const result = await this._client.invokeCapability('Aspire.Hosting.Redis/withRedisInsight', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Adds Redis Insight management UI
     *
     * This version of the package defaults to the tag of the container image.
     * @param options Additional options.
     */
    withRedisInsight(options) {
        const configureContainer = options?.configureContainer;
        const containerName = options?.containerName;
        return new RedisResourcePromiseImpl(this._withRedisInsightInternal(configureContainer, containerName), this._client);
    }
    /** @internal */
    async _withDataVolumeInternal(name, isReadOnly) {
        const rpcArgs = { builder: this._handle };
        if (name !== undefined)
            rpcArgs.name = name;
        if (isReadOnly !== undefined)
            rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability('Aspire.Hosting.Redis/withDataVolume', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Adds a named volume for the data folder to a Redis container resource and enables Redis persistence.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDataVolume(options) {
        const name = options?.name;
        const isReadOnly = options?.isReadOnly;
        return new RedisResourcePromiseImpl(this._withDataVolumeInternal(name, isReadOnly), this._client);
    }
    /** @internal */
    async _withDataBindMountInternal(source, isReadOnly) {
        const rpcArgs = { builder: this._handle, source };
        if (isReadOnly !== undefined)
            rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability('Aspire.Hosting.Redis/withDataBindMount', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Adds a bind mount for the data folder to a Redis container resource and enables Redis persistence.
     * @param source The source directory on the host to mount into the container.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDataBindMount(source, options) {
        const isReadOnly = options?.isReadOnly;
        return new RedisResourcePromiseImpl(this._withDataBindMountInternal(source, isReadOnly), this._client);
    }
    /** @internal */
    async _withPersistenceInternal(interval, keysChangedThreshold) {
        const rpcArgs = { builder: this._handle };
        if (interval !== undefined)
            rpcArgs.interval = interval;
        if (keysChangedThreshold !== undefined)
            rpcArgs.keysChangedThreshold = keysChangedThreshold;
        const result = await this._client.invokeCapability('Aspire.Hosting.Redis/withPersistence', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Configures a Redis container resource for persistence.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withPersistence(options) {
        const interval = options?.interval;
        const keysChangedThreshold = options?.keysChangedThreshold;
        return new RedisResourcePromiseImpl(this._withPersistenceInternal(interval, keysChangedThreshold), this._client);
    }
    /** @internal */
    async _withPasswordInternal(password) {
        password = isPromiseLike(password) ? await password : password;
        const rpcArgs = { builder: this._handle, password };
        const result = await this._client.invokeCapability('Aspire.Hosting.Redis/withPassword', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Configures the password that the Redis resource is used.
     * @param password The parameter used to provide the password for the Redis resource. If `null`, no password will be configured.
     * @returns The resource builder.
     */
    withPassword(password) {
        return new RedisResourcePromiseImpl(this._withPasswordInternal(password), this._client);
    }
    /** @internal */
    async _withHostPortInternal(port) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        const result = await this._client.invokeCapability('Aspire.Hosting.Redis/withHostPort', rpcArgs);
        return new RedisResourceImpl(result, this._client);
    }
    /**
     * Configures the host port that the Redis resource is exposed on instead of using randomly assigned port.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHostPort(options) {
        const port = options?.port;
        return new RedisResourcePromiseImpl(this._withHostPortInternal(port), this._client);
    }
}
/**
 * Thenable wrapper for RedisResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
class RedisResourcePromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    primaryEndpoint() {
        return new EndpointReferencePromiseImpl(this._promise.then(obj => obj.primaryEndpoint()), this._client, false);
    }
    host() {
        return new EndpointReferenceExpressionPromiseImpl(this._promise.then(obj => obj.host()), this._client, false);
    }
    port() {
        return new EndpointReferenceExpressionPromiseImpl(this._promise.then(obj => obj.port()), this._client, false);
    }
    tlsEnabled() {
        return this._promise.then(obj => obj.tlsEnabled());
    }
    connectionStringExpression() {
        return this._promise.then(obj => obj.connectionStringExpression());
    }
    uriExpression() {
        return this._promise.then(obj => obj.uriExpression());
    }
    name() {
        return this._promise.then(obj => obj.name());
    }
    withContainerRegistry(registry) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withContainerRegistry(registry)), this._client);
    }
    withBindMount(source, target, options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withBindMount(source, target, options)), this._client);
    }
    withEntrypoint(entrypoint) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withEntrypoint(entrypoint)), this._client);
    }
    withImageTag(tag) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withImageTag(tag)), this._client);
    }
    withImageRegistry(registry) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withImageRegistry(registry)), this._client);
    }
    withImage(image, options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withImage(image, options)), this._client);
    }
    withImageSHA256(sha256) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withImageSHA256(sha256)), this._client);
    }
    withContainerRuntimeArgs(args) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withContainerRuntimeArgs(args)), this._client);
    }
    withLifetime(lifetime) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withLifetime(lifetime)), this._client);
    }
    withImagePullPolicy(pullPolicy) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withImagePullPolicy(pullPolicy)), this._client);
    }
    publishAsContainer() {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.publishAsContainer()), this._client);
    }
    withDockerfile(contextPath, options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withDockerfile(contextPath, options)), this._client);
    }
    withDockerfileFactory(contextPath, dockerfileFactory, options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileFactory(contextPath, dockerfileFactory, options)), this._client);
    }
    withContainerName(name) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withContainerName(name)), this._client);
    }
    withBuildArg(name, value) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withBuildArg(name, value)), this._client);
    }
    withBuildSecret(name, value) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withBuildSecret(name, value)), this._client);
    }
    withContainerCertificatePaths(options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withContainerCertificatePaths(options)), this._client);
    }
    withDockerfileBuilder(contextPath, callback, options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileBuilder(contextPath, callback, options)), this._client);
    }
    withDockerfileBaseImage(options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileBaseImage(options)), this._client);
    }
    withContainerNetworkAlias(alias) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withContainerNetworkAlias(alias)), this._client);
    }
    withMcpServer(options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withMcpServer(options)), this._client);
    }
    withOtlpExporter(options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withOtlpExporter(options)), this._client);
    }
    publishAsConnectionString() {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.publishAsConnectionString()), this._client);
    }
    withRequiredCommand(command, options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withRequiredCommand(command, options)), this._client);
    }
    withSessionLifetime() {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withSessionLifetime()), this._client);
    }
    withPersistentLifetime() {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withPersistentLifetime()), this._client);
    }
    withLifetimeOf(sourceBuilder) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withLifetimeOf(sourceBuilder)), this._client);
    }
    withParentProcessLifetime(parentProcessId) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withParentProcessLifetime(parentProcessId)), this._client);
    }
    withEnvironment(name, value) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withEnvironment(name, value)), this._client);
    }
    withEnvironmentCallback(callback) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withEnvironmentCallback(callback)), this._client);
    }
    withConnectionProperty(name, value) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withConnectionProperty(name, value)), this._client);
    }
    withArgs(args) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withArgs(args)), this._client);
    }
    withArgsCallback(callback) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withArgsCallback(callback)), this._client);
    }
    withReferenceEnvironment(options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withReferenceEnvironment(options)), this._client);
    }
    withReference(source, options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withReference(source, options)), this._client);
    }
    getConnectionProperty(key) {
        return this._promise.then(obj => obj.getConnectionProperty(key));
    }
    withEndpointCallback(endpointName, callback, options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withEndpointCallback(endpointName, callback, options)), this._client);
    }
    withHttpEndpointCallback(callback, options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withHttpEndpointCallback(callback, options)), this._client);
    }
    withHttpsEndpointCallback(callback, options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withHttpsEndpointCallback(callback, options)), this._client);
    }
    withEndpoint(options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withEndpoint(options)), this._client);
    }
    withEndpointProxySupport(proxyEnabled) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withEndpointProxySupport(proxyEnabled)), this._client);
    }
    withHttpEndpoint(options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withHttpEndpoint(options)), this._client);
    }
    withHttpsEndpoint(options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withHttpsEndpoint(options)), this._client);
    }
    withExternalHttpEndpoints() {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withExternalHttpEndpoints()), this._client);
    }
    getEndpoint(name) {
        return new EndpointReferencePromiseImpl(this._promise.then(obj => obj.getEndpoint(name)), this._client);
    }
    asHttp2Service() {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.asHttp2Service()), this._client);
    }
    withUrls(callback) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withUrls(callback)), this._client);
    }
    withUrl(url, options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withUrl(url, options)), this._client);
    }
    withUrlForEndpoint(endpointName, callback) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)), this._client);
    }
    excludeFromManifest() {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.excludeFromManifest()), this._client);
    }
    waitFor(dependency, options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.waitFor(dependency, options)), this._client);
    }
    waitForStart(dependency, options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.waitForStart(dependency, options)), this._client);
    }
    withExplicitStart() {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withExplicitStart()), this._client);
    }
    waitForCompletion(dependency, options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.waitForCompletion(dependency, options)), this._client);
    }
    withHealthCheck(key) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withHealthCheck(key)), this._client);
    }
    withHttpHealthCheck(options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withHttpHealthCheck(options)), this._client);
    }
    withCommand(name, displayName, executeCommand, options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)), this._client);
    }
    withProcessCommand(commandName, displayName, options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommand(commandName, displayName, options)), this._client);
    }
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommandFactory(commandName, displayName, createProcessSpec, options)), this._client);
    }
    withHttpCommand(path, displayName, options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withHttpCommand(path, displayName, options)), this._client);
    }
    withDeveloperCertificateTrust(trust) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withDeveloperCertificateTrust(trust)), this._client);
    }
    withCertificateTrustScope(scope) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withCertificateTrustScope(scope)), this._client);
    }
    withHttpsDeveloperCertificate(options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withHttpsDeveloperCertificate(options)), this._client);
    }
    withoutHttpsCertificate() {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withoutHttpsCertificate()), this._client);
    }
    withRelationship(resourceBuilder, type) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withRelationship(resourceBuilder, type)), this._client);
    }
    withParentRelationship(parent) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withParentRelationship(parent)), this._client);
    }
    withChildRelationship(child) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withChildRelationship(child)), this._client);
    }
    withIconName(iconName, options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withIconName(iconName, options)), this._client);
    }
    withComputeEnvironment(computeEnvironmentResource) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withComputeEnvironment(computeEnvironmentResource)), this._client);
    }
    withHttpProbe(probeType, options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withHttpProbe(probeType, options)), this._client);
    }
    excludeFromMcp() {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.excludeFromMcp()), this._client);
    }
    withHidden() {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withHidden()), this._client);
    }
    withHiddenOnCompletion(options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withHiddenOnCompletion(options)), this._client);
    }
    withImagePushOptions(callback) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withImagePushOptions(callback)), this._client);
    }
    withRemoteImageName(remoteImageName) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withRemoteImageName(remoteImageName)), this._client);
    }
    withRemoteImageTag(remoteImageTag) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withRemoteImageTag(remoteImageTag)), this._client);
    }
    withPipelineStepFactory(stepName, callback, options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)), this._client);
    }
    withPipelineConfiguration(callback) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withPipelineConfiguration(callback)), this._client);
    }
    withVolume(target, options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withVolume(target, options)), this._client);
    }
    getResourceName() {
        return this._promise.then(obj => obj.getResourceName());
    }
    onBeforeResourceStarted(callback) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.onBeforeResourceStarted(callback)), this._client);
    }
    onResourceStopped(callback) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.onResourceStopped(callback)), this._client);
    }
    onConnectionStringAvailable(callback) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.onConnectionStringAvailable(callback)), this._client);
    }
    onInitializeResource(callback) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.onInitializeResource(callback)), this._client);
    }
    onResourceEndpointsAllocated(callback) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.onResourceEndpointsAllocated(callback)), this._client);
    }
    onResourceReady(callback) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.onResourceReady(callback)), this._client);
    }
    createExecutionConfiguration() {
        return new ExecutionConfigurationBuilderPromiseImpl(this._promise.then(obj => obj.createExecutionConfiguration()), this._client);
    }
    withRedisCommander(options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withRedisCommander(options)), this._client);
    }
    withRedisInsight(options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withRedisInsight(options)), this._client);
    }
    withDataVolume(options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withDataVolume(options)), this._client);
    }
    withDataBindMount(source, options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withDataBindMount(source, options)), this._client);
    }
    withPersistence(options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withPersistence(options)), this._client);
    }
    withPassword(password) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withPassword(password)), this._client);
    }
    withHostPort(options) {
        return new RedisResourcePromiseImpl(this._promise.then(obj => obj.withHostPort(options)), this._client);
    }
}
// ============================================================================
// ViteAppResourceImpl
// ============================================================================
/** Represents a Vite application resource that can be managed and executed within a Node.js environment. */
class ViteAppResourceImpl extends ResourceBuilderBase {
    constructor(handle, client) {
        super(handle, client);
    }
    async command() {
        return await this._client.invokeCapability('Aspire.Hosting.JavaScript/JavaScriptAppResource.command', { context: this._handle });
    }
    async workingDirectory() {
        return await this._client.invokeCapability('Aspire.Hosting.JavaScript/JavaScriptAppResource.workingDirectory', { context: this._handle });
    }
    async name() {
        return await this._client.invokeCapability('Aspire.Hosting.JavaScript/JavaScriptAppResource.name', { context: this._handle });
    }
    /** @internal */
    async _withContainerRegistryInternal(registry) {
        registry = isPromiseLike(registry) ? await registry : registry;
        const rpcArgs = { builder: this._handle, registry };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerRegistry', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Configures the resource to use the specified container registry for container image operations.
     *
     * This method adds a `ContainerRegistryReferenceAnnotation` to the resource,
     * indicating that the resource should use the specified container registry for container image operations.
     * @param registry The container registry resource builder.
     * @returns The resource builder for chaining.
     */
    withContainerRegistry(registry) {
        return new ViteAppResourcePromiseImpl(this._withContainerRegistryInternal(registry), this._client);
    }
    /** @internal */
    async _withDockerfileBaseImageInternal(buildImage, runtimeImage) {
        const rpcArgs = { builder: this._handle };
        if (buildImage !== undefined)
            rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined)
            rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileBaseImage', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Configures custom base images for generated Dockerfiles.
     *
     * This extension method allows customization of the base images used in generated Dockerfiles.
     * For multi-stage Dockerfiles (e.g., Python with UV), you can specify separate build and runtime images.
     * Specify custom base images for a Python application:
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddPythonApp("myapp", "path/to/app", "main.py")
     * .WithDockerfileBaseImage(
     * buildImage: "ghcr.io/astral-sh/uv:python3.12-bookworm-slim",
     * runtimeImage: "python:3.12-slim-bookworm");
     * builder.Build().Run();
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileBaseImage(options) {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new ViteAppResourcePromiseImpl(this._withDockerfileBaseImageInternal(buildImage, runtimeImage), this._client);
    }
    /** @internal */
    async _publishAsDockerFileInternal(configure) {
        const configureId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new ContainerResourceImpl(objHandle, this._client);
            await configure(obj);
        });
        const rpcArgs = { builder: this._handle, configure: configureId };
        const result = await this._client.invokeCapability('Aspire.Hosting/publishAsDockerFile', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Publishes an executable as a Docker file
     *
     * When the executable resource is converted to a container resource, the arguments to the executable
     * are not used. This is because arguments to the executable often contain physical paths that are not valid
     * in the container. The container can be set up with the correct arguments using the `configure` action.
     * @param configure Optional action to configure the container resource
     * @returns The resource builder.
     */
    publishAsDockerFile(configure) {
        return new ViteAppResourcePromiseImpl(this._publishAsDockerFileInternal(configure), this._client);
    }
    /** @internal */
    async _withExecutableCommandInternal(command) {
        const rpcArgs = { builder: this._handle, command };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExecutableCommand', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Sets the command for the executable resource.
     * @param command Command.
     * @returns The resource builder.
     */
    withExecutableCommand(command) {
        return new ViteAppResourcePromiseImpl(this._withExecutableCommandInternal(command), this._client);
    }
    /** @internal */
    async _withWorkingDirectoryInternal(workingDirectory) {
        const rpcArgs = { builder: this._handle, workingDirectory };
        const result = await this._client.invokeCapability('Aspire.Hosting/withWorkingDirectory', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Sets the working directory for the executable resource.
     * @param workingDirectory Working directory.
     * @returns The resource builder.
     */
    withWorkingDirectory(workingDirectory) {
        return new ViteAppResourcePromiseImpl(this._withWorkingDirectoryInternal(workingDirectory), this._client);
    }
    /** @internal */
    async _withMcpServerInternal(path, endpointName) {
        const rpcArgs = { builder: this._handle };
        if (path !== undefined)
            rpcArgs.path = path;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withMcpServer', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Marks the resource as hosting a Model Context Protocol (MCP) server on the specified endpoint.
     *
     * This method adds an `McpServerEndpointAnnotation` to the resource, enabling the Aspire tooling
     * to discover and proxy the MCP server exposed by the resource.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withMcpServer(options) {
        const path = options?.path;
        const endpointName = options?.endpointName;
        return new ViteAppResourcePromiseImpl(this._withMcpServerInternal(path, endpointName), this._client);
    }
    /** @internal */
    async _withOtlpExporterInternal(protocol) {
        const rpcArgs = { builder: this._handle };
        if (protocol !== undefined)
            rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability('Aspire.Hosting/withOtlpExporter', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Configures OTLP telemetry export
     * @param options Additional options.
     */
    withOtlpExporter(options) {
        const protocol = options?.protocol;
        return new ViteAppResourcePromiseImpl(this._withOtlpExporterInternal(protocol), this._client);
    }
    /** @internal */
    async _withRequiredCommandInternal(command, helpLink) {
        const rpcArgs = { builder: this._handle, command };
        if (helpLink !== undefined)
            rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability('Aspire.Hosting/withRequiredCommand', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Declares that a resource requires a specific command/executable to be available on the local machine PATH before it can start.
     *
     * The command is considered valid if either:
     * 1. It is an absolute or relative path (contains a directory separator) that points to an existing file, or
     * 2. It is discoverable on the current process PATH (respecting PATHEXT on Windows).
     * If the command is not found, a warning message will be logged but the resource will be allowed to attempt to start.
     * @param command The command string (file name or path) that should be validated.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withRequiredCommand(command, options) {
        const helpLink = options?.helpLink;
        return new ViteAppResourcePromiseImpl(this._withRequiredCommandInternal(command, helpLink), this._client);
    }
    /** @internal */
    async _withSessionLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withSessionLifetime', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a session lifetime.
     * @returns The resource builder.
     */
    withSessionLifetime() {
        return new ViteAppResourcePromiseImpl(this._withSessionLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withPersistentLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPersistentLifetime', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime.
     * @returns The resource builder.
     */
    withPersistentLifetime() {
        return new ViteAppResourcePromiseImpl(this._withPersistentLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withLifetimeOfInternal(sourceBuilder) {
        sourceBuilder = isPromiseLike(sourceBuilder) ? await sourceBuilder : sourceBuilder;
        const rpcArgs = { builder: this._handle, sourceBuilder };
        const result = await this._client.invokeCapability('Aspire.Hosting/withLifetimeOf', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to match the lifetime of another resource.
     *
     * The resource lifetime is evaluated from `sourceBuilder` when the application model is prepared, so later lifetime
     * changes to the source resource are reflected by this resource.
     * @param sourceBuilder The resource builder whose lifetime should be used.
     * @returns The resource builder.
     */
    withLifetimeOf(sourceBuilder) {
        return new ViteAppResourcePromiseImpl(this._withLifetimeOfInternal(sourceBuilder), this._client);
    }
    /** @internal */
    async _withParentProcessLifetimeInternal(parentProcessId) {
        const rpcArgs = { builder: this._handle, parentProcessId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withParentProcessLifetime', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime that ends when a parent process exits.
     * @param parentProcessId The ID of the parent process to monitor.
     * @returns The resource builder.
     */
    withParentProcessLifetime(parentProcessId) {
        return new ViteAppResourcePromiseImpl(this._withParentProcessLifetimeInternal(parentProcessId), this._client);
    }
    /** @internal */
    async _withEnvironmentInternal(name, value) {
        value = isPromiseLike(value) ? await value : value;
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEnvironment', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /** Sets an environment variable */
    withEnvironment(name, value) {
        return new ViteAppResourcePromiseImpl(this._withEnvironmentInternal(name, value), this._client);
    }
    /** @internal */
    async _withEnvironmentCallbackInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new EnvironmentCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEnvironmentCallback', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Allows for the population of environment variables on a resource.
     * @param callback A callback that allows for deferred execution for computing many environment variables. This runs after resources have been allocated by the orchestrator and allows access to other resources to resolve computed data, e.g. connection strings, ports.
     * @returns The resource builder.
     */
    withEnvironmentCallback(callback) {
        return new ViteAppResourcePromiseImpl(this._withEnvironmentCallbackInternal(callback), this._client);
    }
    /** @internal */
    async _withArgsInternal(args) {
        const rpcArgs = { builder: this._handle, args };
        const result = await this._client.invokeCapability('Aspire.Hosting/withArgs', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Adds arguments to be passed to a resource that supports arguments when it is launched.
     * @param args The arguments to be passed to the resource when it is started.
     * @returns The resource builder.
     */
    withArgs(args) {
        return new ViteAppResourcePromiseImpl(this._withArgsInternal(args), this._client);
    }
    /** @internal */
    async _withArgsCallbackInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new CommandLineArgsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withArgsCallback', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Adds a callback to be executed with a list of command-line arguments when a resource is started.
     * @param callback A callback that allows for deferred execution for computing arguments. This runs after resources have been allocated by the orchestrator and allows access to other resources to resolve computed data, e.g. connection strings, ports.
     * @returns The resource builder.
     */
    withArgsCallback(callback) {
        return new ViteAppResourcePromiseImpl(this._withArgsCallbackInternal(callback), this._client);
    }
    /** @internal */
    async _withReferenceEnvironmentInternal(options) {
        const rpcArgs = { builder: this._handle, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withReferenceEnvironment', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Configures how information is injected into environment variables when the resource references other resources.
     * @param options Options controlling which reference information is emitted.
     * @returns The resource builder.
     */
    withReferenceEnvironment(options) {
        return new ViteAppResourcePromiseImpl(this._withReferenceEnvironmentInternal(options), this._client);
    }
    /** @internal */
    async _withReferenceInternal(source, connectionName, optional, name) {
        source = isPromiseLike(source) ? await source : source;
        const rpcArgs = { builder: this._handle, source };
        if (connectionName !== undefined)
            rpcArgs.connectionName = connectionName;
        if (optional !== undefined)
            rpcArgs.optional = optional;
        if (name !== undefined)
            rpcArgs.name = name;
        const result = await this._client.invokeCapability('Aspire.Hosting/withReference', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Adds a reference to another resource
     * @param options Additional options.
     */
    withReference(source, options) {
        const connectionName = options?.connectionName;
        const optional = options?.optional;
        const name = options?.name;
        return new ViteAppResourcePromiseImpl(this._withReferenceInternal(source, connectionName, optional, name), this._client);
    }
    /** @internal */
    async _withEndpointCallbackInternal(endpointName, callback, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpointCallback', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Updates a named endpoint via callback
     * @param options Additional options.
     */
    withEndpointCallback(endpointName, callback, options) {
        const createIfNotExists = options?.createIfNotExists;
        return new ViteAppResourcePromiseImpl(this._withEndpointCallbackInternal(endpointName, callback, createIfNotExists), this._client);
    }
    /** @internal */
    async _withHttpEndpointCallbackInternal(callback, name, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        if (name !== undefined)
            rpcArgs.name = name;
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpEndpointCallback', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Updates an HTTP endpoint via callback
     * @param options Additional options.
     */
    withHttpEndpointCallback(callback, options) {
        const name = options?.name;
        const createIfNotExists = options?.createIfNotExists;
        return new ViteAppResourcePromiseImpl(this._withHttpEndpointCallbackInternal(callback, name, createIfNotExists), this._client);
    }
    /** @internal */
    async _withHttpsEndpointCallbackInternal(callback, name, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        if (name !== undefined)
            rpcArgs.name = name;
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpsEndpointCallback', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Updates an HTTPS endpoint via callback
     * @param options Additional options.
     */
    withHttpsEndpointCallback(callback, options) {
        const name = options?.name;
        const createIfNotExists = options?.createIfNotExists;
        return new ViteAppResourcePromiseImpl(this._withHttpsEndpointCallbackInternal(callback, name, createIfNotExists), this._client);
    }
    /** @internal */
    async _withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (scheme !== undefined)
            rpcArgs.scheme = scheme;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        if (isExternal !== undefined)
            rpcArgs.isExternal = isExternal;
        if (protocol !== undefined)
            rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpoint', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Adds a network endpoint
     * @param options Additional options.
     * @returns The resource builder.
     */
    withEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const scheme = options?.scheme;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        const isExternal = options?.isExternal;
        const protocol = options?.protocol;
        return new ViteAppResourcePromiseImpl(this._withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol), this._client);
    }
    /** @internal */
    async _withEndpointProxySupportInternal(proxyEnabled) {
        const rpcArgs = { builder: this._handle, proxyEnabled };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpointProxySupport', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Set whether a resource can use proxied endpoints or whether they should be disabled for all endpoints belonging to the resource. If set to `false`, endpoints belonging to the resource will ignore the configured proxy settings and run proxy-less.
     *
     * This method is intended to support scenarios with persistent lifetime resources where it is desirable for the resource to be accessible over the same
     * port whether the Aspire application is running or not. Proxied endpoints bind ports that are only accessible while the Aspire application is running.
     * The user needs to be careful to ensure that endpoints are using unique ports when disabling proxy support as by default for proxy-less
     * endpoints, Aspire will allocate the target port as the host port, which will increase the chance of port conflicts.
     * @param proxyEnabled Should endpoints for the resource support using a proxy?
     * @returns The resource builder.
     */
    withEndpointProxySupport(proxyEnabled) {
        return new ViteAppResourcePromiseImpl(this._withEndpointProxySupportInternal(proxyEnabled), this._client);
    }
    /** @internal */
    async _withHttpEndpointInternal(port, targetPort, name, env, isProxied) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpEndpoint', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP endpoint
     *
     * If an endpoint with the same name already exists on the resource, the existing endpoint is updated
     * with any non-null parameter values. Parameters left as `null` will not modify the existing endpoint's values.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new ViteAppResourcePromiseImpl(this._withHttpEndpointInternal(port, targetPort, name, env, isProxied), this._client);
    }
    /** @internal */
    async _withHttpsEndpointInternal(port, targetPort, name, env, isProxied) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpsEndpoint', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTPS endpoint
     *
     * If an endpoint with the same name already exists on the resource, the existing endpoint is updated
     * with any non-null parameter values. Parameters left as `null` will not modify the existing endpoint's values.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpsEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new ViteAppResourcePromiseImpl(this._withHttpsEndpointInternal(port, targetPort, name, env, isProxied), this._client);
    }
    /** @internal */
    async _withExternalHttpEndpointsInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExternalHttpEndpoints', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Marks existing http or https endpoints on a resource as external.
     * @returns The resource builder.
     */
    withExternalHttpEndpoints() {
        return new ViteAppResourcePromiseImpl(this._withExternalHttpEndpointsInternal(), this._client);
    }
    /**
     * Gets an endpoint reference
     * @param name The name of the endpoint.
     * @returns An `EndpointReference` that can be used to resolve the address of the endpoint after resource allocation has occurred.
     */
    getEndpoint(name) {
        const promise = (async () => {
            const rpcArgs = { builder: this._handle, name };
            const handle = await this._client.invokeCapability('Aspire.Hosting/getEndpoint', rpcArgs);
            return new EndpointReferenceImpl(handle, this._client);
        })();
        return new EndpointReferencePromiseImpl(promise, this._client);
    }
    /** @internal */
    async _asHttp2ServiceInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/asHttp2Service', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to mark all endpoints' transport as HTTP/2. This is useful for HTTP/2 services that need prior knowledge.
     * @returns The resource builder.
     */
    asHttp2Service() {
        return new ViteAppResourcePromiseImpl(this._asHttp2ServiceInternal(), this._client);
    }
    /** @internal */
    async _withUrlsInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new ResourceUrlsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrls', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to customize the URLs displayed for the resource.
     * @param callback The callback that will customize URLs for the resource.
     * @returns The resource builder.
     */
    withUrls(callback) {
        return new ViteAppResourcePromiseImpl(this._withUrlsInternal(callback), this._client);
    }
    /** @internal */
    async _withUrlInternal(url, displayText) {
        const rpcArgs = { builder: this._handle, url };
        if (displayText !== undefined)
            rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrl', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Adds or modifies displayed URLs
     * @param options Additional options.
     */
    withUrl(url, options) {
        const displayText = options?.displayText;
        return new ViteAppResourcePromiseImpl(this._withUrlInternal(url, displayText), this._client);
    }
    /** @internal */
    async _withUrlForEndpointInternal(endpointName, callback) {
        const callbackId = registerCallback(async (objData) => {
            const obj = wrapIfHandle(objData);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrlForEndpoint', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to update the URL displayed for the endpoint with the specified name.
     * @param endpointName The name of the endpoint to customize the URL for.
     * @param callback The callback that will customize the URL.
     * @returns The resource builder.
     */
    withUrlForEndpoint(endpointName, callback) {
        return new ViteAppResourcePromiseImpl(this._withUrlForEndpointInternal(endpointName, callback), this._client);
    }
    /** @internal */
    async _withContainerFilesSourceInternal(sourcePath) {
        const rpcArgs = { builder: this._handle, sourcePath };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerFilesSource', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Adds a container files source annotation to the resource being built, specifying the path to the container files source.
     * @param sourcePath The path to the container files source to associate with the resource. Cannot be null.
     * @returns The resource builder instance with the container files source annotation applied.
     */
    withContainerFilesSource(sourcePath) {
        return new ViteAppResourcePromiseImpl(this._withContainerFilesSourceInternal(sourcePath), this._client);
    }
    /** @internal */
    async _clearContainerFilesSourcesInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/clearContainerFilesSources', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Removes any container files source annotation from the resource being built.
     * @returns The resource builder instance with the container files source annotation applied.
     */
    clearContainerFilesSources() {
        return new ViteAppResourcePromiseImpl(this._clearContainerFilesSourcesInternal(), this._client);
    }
    /** @internal */
    async _excludeFromManifestInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromManifest', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Excludes a resource from being published to the manifest.
     * @returns The resource builder.
     */
    excludeFromManifest() {
        return new ViteAppResourcePromiseImpl(this._excludeFromManifestInternal(), this._client);
    }
    /** @internal */
    async _waitForInternal(dependency, waitBehavior) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (waitBehavior !== undefined)
            rpcArgs.waitBehavior = waitBehavior;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitFor', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Waits for another resource to be ready
     * @param options Additional options.
     */
    waitFor(dependency, options) {
        const waitBehavior = options?.waitBehavior;
        return new ViteAppResourcePromiseImpl(this._waitForInternal(dependency, waitBehavior), this._client);
    }
    /** @internal */
    async _waitForStartInternal(dependency, waitBehavior) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (waitBehavior !== undefined)
            rpcArgs.waitBehavior = waitBehavior;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitForStart', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Waits for another resource to start
     * @param options Additional options.
     */
    waitForStart(dependency, options) {
        const waitBehavior = options?.waitBehavior;
        return new ViteAppResourcePromiseImpl(this._waitForStartInternal(dependency, waitBehavior), this._client);
    }
    /** @internal */
    async _withExplicitStartInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExplicitStart', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Prevents resource from starting automatically
     * @returns The resource builder.
     */
    withExplicitStart() {
        return new ViteAppResourcePromiseImpl(this._withExplicitStartInternal(), this._client);
    }
    /** @internal */
    async _waitForCompletionInternal(dependency, exitCode) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitForResourceCompletion', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Waits for the dependency resource to enter the Exited or Finished state before starting the resource.
     * @param dependency The resource builder for the dependency resource.
     * @param options Additional options.
     * @returns The resource builder.
     */
    waitForCompletion(dependency, options) {
        const exitCode = options?.exitCode;
        return new ViteAppResourcePromiseImpl(this._waitForCompletionInternal(dependency, exitCode), this._client);
    }
    /** @internal */
    async _withHealthCheckInternal(key) {
        const rpcArgs = { builder: this._handle, key };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHealthCheck', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Adds a health check by key
     * @param key The key for the health check.
     * @returns The resource builder.
     */
    withHealthCheck(key) {
        return new ViteAppResourcePromiseImpl(this._withHealthCheckInternal(key), this._client);
    }
    /** @internal */
    async _withHttpHealthCheckInternal(path, statusCode, endpointName) {
        const rpcArgs = { builder: this._handle };
        if (path !== undefined)
            rpcArgs.path = path;
        if (statusCode !== undefined)
            rpcArgs.statusCode = statusCode;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpHealthCheck', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Adds a health check to the resource which is mapped to a specific endpoint.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpHealthCheck(options) {
        const path = options?.path;
        const statusCode = options?.statusCode;
        const endpointName = options?.endpointName;
        return new ViteAppResourcePromiseImpl(this._withHttpHealthCheckInternal(path, statusCode, endpointName), this._client);
    }
    /** @internal */
    async _withCommandInternal(name, displayName, executeCommand, commandOptions) {
        const executeCommandId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined)
            rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability('Aspire.Hosting/withCommand', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Adds a resource command
     *
     * The `WithCommand` method is used to add commands to the resource. Commands are displayed in the dashboard
     * and can be executed by a user using the dashboard UI.
     * When a command is executed, the `executeCommand` callback is called and is run inside the Aspire host.
     * @param name The name of the command. The name uniquely identifies the command.
     * @param displayName The display name visible in UI.
     * @param executeCommand A callback that is executed when the command is executed. The callback is run inside the Aspire host. The callback result is used to indicate success or failure in the UI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withCommand(name, displayName, executeCommand, options) {
        const commandOptions = options?.commandOptions;
        return new ViteAppResourcePromiseImpl(this._withCommandInternal(name, displayName, executeCommand, commandOptions), this._client);
    }
    /** @internal */
    async _withProcessCommandInternal(commandName, displayName, options) {
        const rpcArgs = { builder: this._handle, commandName, displayName, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommand', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /** Adds a command to the resource that starts a local process when invoked. */
    withProcessCommand(commandName, displayName, options) {
        return new ViteAppResourcePromiseImpl(this._withProcessCommandInternal(commandName, displayName, options), this._client);
    }
    /** @internal */
    async _withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options) {
        const createProcessSpecId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await createProcessSpec(arg);
        });
        const rpcArgs = { builder: this._handle, commandName, displayName, createProcessSpec: createProcessSpecId };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommandFactory', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Adds a command to the resource that starts a local process created by a callback when invoked.
     * @param options Additional options.
     */
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new ViteAppResourcePromiseImpl(this._withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options), this._client);
    }
    /** @internal */
    async _withHttpCommandInternal(path, displayName, options) {
        const rpcArgs = { builder: this._handle, path, displayName };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpCommand', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP resource command
     * @param options Additional options.
     */
    withHttpCommand(path, displayName, options) {
        return new ViteAppResourcePromiseImpl(this._withHttpCommandInternal(path, displayName, options), this._client);
    }
    /** @internal */
    async _withDeveloperCertificateTrustInternal(trust) {
        const rpcArgs = { builder: this._handle, trust };
        const result = await this._client.invokeCapability('Aspire.Hosting/withDeveloperCertificateTrust', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Indicates whether developer certificates should be treated as trusted certificate authorities for the resource at run time. Currently this indicates trust for the ASP.NET Core developer certificate. The developer certificate will only be trusted when running in local development scenarios; in publish mode resources will use their default certificate trust.
     *
     * Disable trust for app host managed developer certificate(s) for a container resource.
     * ```
     * var container = builder.AddContainer("my-service", "my-service:latest")
     * .WithDeveloperCertificateTrust(false);
     * ```
     * Disable automatic trust for app host managed developer certificate(s), but explicitly enable it for a specific resource.
     * ```
     * var builder = DistributedApplication.CreateBuilder(new DistributedApplicationOptions()
     * {
     * Args = args,
     * TrustDeveloperCertificate = false,
     * });
     * var project = builder.AddProject<MyService>("my-service")
     * .WithDeveloperCertificateTrust(true);
     * ```
     * @param trust Indicates whether the developer certificate should be treated as trusted.
     * @returns The resource builder.
     */
    withDeveloperCertificateTrust(trust) {
        return new ViteAppResourcePromiseImpl(this._withDeveloperCertificateTrustInternal(trust), this._client);
    }
    /** @internal */
    async _withCertificateTrustScopeInternal(scope) {
        const rpcArgs = { builder: this._handle, scope };
        const result = await this._client.invokeCapability('Aspire.Hosting/withCertificateTrustScope', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Sets the certificate trust scope
     *
     * The default scope if not overridden is `Append` which means that custom certificate
     * authorities should be appended to the default trusted certificate authorities for the resource. Setting the scope to
     * `Override` indicates the set of certificates in referenced
     * `CertificateAuthorityCollection` (and optionally Aspire developer certificiates) should be used as the
     * exclusive source of trust for a resource.
     * In all cases, this is a best effort implementation as not all resources support full customization of certificate
     * trust.
     * Set the scope for custom certificate authorities to override the default trusted certificate authorities for a container resource.
     * ```
     * var caCollection = builder.AddCertificateAuthorityCollection("my-cas")
     * .WithCertificate(new X509Certificate2("my-ca.pem"));
     * var container = builder.AddContainer("my-service", "my-service:latest")
     * .WithCertificateAuthorityCollection(caCollection)
     * .WithCertificateTrustScope(CertificateTrustScope.Override);
     * ```
     * @param scope The scope to apply to custom certificate authorities associated with the resource.
     * @returns The resource builder.
     */
    withCertificateTrustScope(scope) {
        return new ViteAppResourcePromiseImpl(this._withCertificateTrustScopeInternal(scope), this._client);
    }
    /** @internal */
    async _withHttpsDeveloperCertificateInternal(password) {
        password = isPromiseLike(password) ? await password : password;
        const rpcArgs = { builder: this._handle };
        if (password !== undefined)
            rpcArgs.password = password;
        const result = await this._client.invokeCapability('Aspire.Hosting/withParameterHttpsDeveloperCertificate', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Indicates that a resource should use the developer certificate key pair for HTTPS endpoints at run time. Currently this indicates use of the ASP.NET Core developer certificate. The developer certificate will only be used when running in local development scenarios; in publish mode resources will use their default certificate configuration.
     *
     * Use the developer certificate for HTTPS/TLS endpoints on a container resource:
     * ```
     * builder.AddContainer("my-service", "my-image")
     * .WithHttpsDeveloperCertificate()
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpsDeveloperCertificate(options) {
        let password = options?.password;
        return new ViteAppResourcePromiseImpl(this._withHttpsDeveloperCertificateInternal(password), this._client);
    }
    /** @internal */
    async _withoutHttpsCertificateInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withoutHttpsCertificate', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Disable HTTPS/TLS server certificate configuration for the resource. No HTTPS/TLS termination configuration will be applied.
     *
     * Disable HTTPS certificate configuration for a Redis resource:
     * ```
     * var redis = builder.AddRedis("cache")
     * .WithoutHttpsCertificate();
     * ```
     * @returns The resource builder.
     */
    withoutHttpsCertificate() {
        return new ViteAppResourcePromiseImpl(this._withoutHttpsCertificateInternal(), this._client);
    }
    /** @internal */
    async _withRelationshipInternal(resourceBuilder, type) {
        resourceBuilder = isPromiseLike(resourceBuilder) ? await resourceBuilder : resourceBuilder;
        const rpcArgs = { builder: this._handle, resourceBuilder, type };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderRelationship', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Adds a relationship to another resource using its builder.
     * @param resourceBuilder The resource builder that the relationship is to.
     * @param type The relationship type.
     * @returns A resource builder.
     */
    withRelationship(resourceBuilder, type) {
        return new ViteAppResourcePromiseImpl(this._withRelationshipInternal(resourceBuilder, type), this._client);
    }
    /** @internal */
    async _withParentRelationshipInternal(parent) {
        parent = isPromiseLike(parent) ? await parent : parent;
        const rpcArgs = { builder: this._handle, parent };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderParentRelationship', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Sets the parent relationship
     * @param parent The parent of `builder`.
     * @returns A resource builder.
     */
    withParentRelationship(parent) {
        return new ViteAppResourcePromiseImpl(this._withParentRelationshipInternal(parent), this._client);
    }
    /** @internal */
    async _withChildRelationshipInternal(child) {
        child = isPromiseLike(child) ? await child : child;
        const rpcArgs = { builder: this._handle, child };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderChildRelationship', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Sets a child relationship
     * @param child The child of `builder`.
     * @returns A resource builder.
     */
    withChildRelationship(child) {
        return new ViteAppResourcePromiseImpl(this._withChildRelationshipInternal(child), this._client);
    }
    /** @internal */
    async _withIconNameInternal(iconName, iconVariant) {
        const rpcArgs = { builder: this._handle, iconName };
        if (iconVariant !== undefined)
            rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability('Aspire.Hosting/withIconName', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Specifies the icon to use when displaying the resource in the dashboard.
     * @param iconName The name of the FluentUI icon to use. See https://aka.ms/fluentui-system-icons for available icons.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withIconName(iconName, options) {
        const iconVariant = options?.iconVariant;
        return new ViteAppResourcePromiseImpl(this._withIconNameInternal(iconName, iconVariant), this._client);
    }
    /** @internal */
    async _withComputeEnvironmentInternal(computeEnvironmentResource) {
        computeEnvironmentResource = isPromiseLike(computeEnvironmentResource) ? await computeEnvironmentResource : computeEnvironmentResource;
        const rpcArgs = { builder: this._handle, computeEnvironmentResource };
        const result = await this._client.invokeCapability('Aspire.Hosting/withComputeEnvironment', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Configures the compute environment for the compute resource.
     *
     * This method allows associating a specific compute environment with the compute resource.
     * @param computeEnvironmentResource The compute environment resource to associate with the compute resource.
     * @returns The resource builder.
     */
    withComputeEnvironment(computeEnvironmentResource) {
        return new ViteAppResourcePromiseImpl(this._withComputeEnvironmentInternal(computeEnvironmentResource), this._client);
    }
    /** @internal */
    async _withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName) {
        const rpcArgs = { builder: this._handle, probeType };
        if (path !== undefined)
            rpcArgs.path = path;
        if (initialDelaySeconds !== undefined)
            rpcArgs.initialDelaySeconds = initialDelaySeconds;
        if (periodSeconds !== undefined)
            rpcArgs.periodSeconds = periodSeconds;
        if (timeoutSeconds !== undefined)
            rpcArgs.timeoutSeconds = timeoutSeconds;
        if (failureThreshold !== undefined)
            rpcArgs.failureThreshold = failureThreshold;
        if (successThreshold !== undefined)
            rpcArgs.successThreshold = successThreshold;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpProbe', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Adds an HTTP health probe to the resource
     * @param options Additional options.
     */
    withHttpProbe(probeType, options) {
        const path = options?.path;
        const initialDelaySeconds = options?.initialDelaySeconds;
        const periodSeconds = options?.periodSeconds;
        const timeoutSeconds = options?.timeoutSeconds;
        const failureThreshold = options?.failureThreshold;
        const successThreshold = options?.successThreshold;
        const endpointName = options?.endpointName;
        return new ViteAppResourcePromiseImpl(this._withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName), this._client);
    }
    /** @internal */
    async _excludeFromMcpInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromMcp', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Exclude the resource from MCP operations using the Aspire MCP server. The resource is excluded from results that return resources, console logs and telemetry.
     * @returns The resource builder.
     */
    excludeFromMcp() {
        return new ViteAppResourcePromiseImpl(this._excludeFromMcpInternal(), this._client);
    }
    /** @internal */
    async _withHiddenInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHidden', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists
     *
     * Use this method to hide resources that are implementation details and should never be displayed by default.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @returns The resource builder.
     */
    withHidden() {
        return new ViteAppResourcePromiseImpl(this._withHiddenInternal(), this._client);
    }
    /** @internal */
    async _withHiddenOnCompletionInternal(exitCode, exitCodes) {
        const rpcArgs = { builder: this._handle };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        if (exitCodes !== undefined)
            rpcArgs.exitCodes = exitCodes;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHiddenOnCompletion', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists after successful completion
     *
     * This method is useful for one-off resources such as setup scripts, migrations, or build steps that should remain visible while running
     * and then be hidden after successful completion.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHiddenOnCompletion(options) {
        const exitCode = options?.exitCode;
        const exitCodes = options?.exitCodes;
        return new ViteAppResourcePromiseImpl(this._withHiddenOnCompletionInternal(exitCode, exitCodes), this._client);
    }
    /** @internal */
    async _withImagePushOptionsInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ContainerImagePushOptionsCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImagePushOptions', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Adds an asynchronous callback to configure container image push options for the resource.
     *
     * This method allows customization of how container images are named and tagged when pushed to a registry using an asynchronous callback.
     * Use this overload when the callback needs to perform asynchronous operations such as retrieving configuration values from external sources.
     * The callback receives a `ContainerImagePushOptionsCallbackContext` that provides access to the resource
     * and the `ContainerImagePushOptions` that can be modified.
     * Multiple callbacks can be registered on the same resource, and they will be invoked in the order they were added.
     * @param callback The asynchronous callback to configure push options.
     * @returns The resource builder.
     */
    withImagePushOptions(callback) {
        return new ViteAppResourcePromiseImpl(this._withImagePushOptionsInternal(callback), this._client);
    }
    /** @internal */
    async _withRemoteImageNameInternal(remoteImageName) {
        const rpcArgs = { builder: this._handle, remoteImageName };
        const result = await this._client.invokeCapability('Aspire.Hosting/withRemoteImageName', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Sets the remote image name (without registry endpoint or tag) for container push operations.
     *
     * Use this with `withRemoteImageTag` to fully customize the image reference used for container push operations.
     * @param remoteImageName The remote image name (e.g., "myapp" or "myorg/myapp").
     * @returns The resource builder.
     */
    withRemoteImageName(remoteImageName) {
        return new ViteAppResourcePromiseImpl(this._withRemoteImageNameInternal(remoteImageName), this._client);
    }
    /** @internal */
    async _withRemoteImageTagInternal(remoteImageTag) {
        const rpcArgs = { builder: this._handle, remoteImageTag };
        const result = await this._client.invokeCapability('Aspire.Hosting/withRemoteImageTag', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Sets the remote image tag for container push operations.
     *
     * Use this with `withRemoteImageName` to fully customize the image reference used for container push operations.
     * @param remoteImageTag The remote image tag (e.g., "latest", "v1.0.0").
     * @returns The resource builder.
     */
    withRemoteImageTag(remoteImageTag) {
        return new ViteAppResourcePromiseImpl(this._withRemoteImageTagInternal(remoteImageTag), this._client);
    }
    /** @internal */
    async _withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new PipelineStepContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined)
            rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined)
            rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined)
            rpcArgs.tags = tags;
        if (description !== undefined)
            rpcArgs.description = description;
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineStepFactory', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Adds a pipeline step to the resource that will be executed during deployment.
     * @param stepName The unique name of the pipeline step.
     * @param callback The callback to execute when the step runs.
     * @param options Additional options.
     * @returns The resource builder for chaining.
     */
    withPipelineStepFactory(stepName, callback, options) {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new ViteAppResourcePromiseImpl(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description), this._client);
    }
    /** @internal */
    async _withPipelineConfigurationInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new PipelineConfigurationContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineConfiguration', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to be executed during the pipeline configuration phase, allowing modification of step dependencies and relationships.
     * @param callback The callback function to execute during the configuration phase.
     * @returns The resource builder for chaining.
     */
    withPipelineConfiguration(callback) {
        return new ViteAppResourcePromiseImpl(this._withPipelineConfigurationInternal(callback), this._client);
    }
    /**
     * Gets the name of the resource from a builder.
     *
     * Why this wrapper exists: This capability accesses a nested property
     * (`resource.Resource.Name`) which requires a wrapper method. There is no single
     * .NET method that returns just the resource name that could be annotated directly.
     * @returns The resource name.
     */
    async getResourceName() {
        const rpcArgs = { resource: this._handle };
        return await this._client.invokeCapability('Aspire.Hosting/getResourceName', rpcArgs);
    }
    /** @internal */
    async _onBeforeResourceStartedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new BeforeResourceStartedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onBeforeResourceStarted', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the BeforeResourceStarted event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onBeforeResourceStarted(callback) {
        return new ViteAppResourcePromiseImpl(this._onBeforeResourceStartedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceStoppedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceStoppedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceStopped', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceStopped event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceStopped(callback) {
        return new ViteAppResourcePromiseImpl(this._onResourceStoppedInternal(callback), this._client);
    }
    /** @internal */
    async _onInitializeResourceInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new InitializeResourceEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onInitializeResource', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the InitializeResource event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onInitializeResource(callback) {
        return new ViteAppResourcePromiseImpl(this._onInitializeResourceInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceEndpointsAllocatedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceEndpointsAllocatedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceEndpointsAllocated', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceEndpointsAllocated event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceEndpointsAllocated(callback) {
        return new ViteAppResourcePromiseImpl(this._onResourceEndpointsAllocatedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceReadyInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceReadyEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceReady', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceReady event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceReady(callback) {
        return new ViteAppResourcePromiseImpl(this._onResourceReadyInternal(callback), this._client);
    }
    /**
     * Creates an execution configuration builder for the specified resource.
     * @returns The execution configuration builder.
     */
    createExecutionConfiguration() {
        const promise = (async () => {
            const rpcArgs = { resource: this._handle };
            const handle = await this._client.invokeCapability('Aspire.Hosting/createExecutionConfiguration', rpcArgs);
            return new ExecutionConfigurationBuilderImpl(handle, this._client);
        })();
        return new ExecutionConfigurationBuilderPromiseImpl(promise, this._client);
    }
    /** @internal */
    async _publishAsStaticWebsiteInternal(apiPath, apiTarget, outputPath, stripPrefix, targetEndpointName) {
        apiTarget = isPromiseLike(apiTarget) ? await apiTarget : apiTarget;
        const rpcArgs = { builder: this._handle };
        if (apiPath !== undefined)
            rpcArgs.apiPath = apiPath;
        if (apiTarget !== undefined)
            rpcArgs.apiTarget = apiTarget;
        if (outputPath !== undefined)
            rpcArgs.outputPath = outputPath;
        if (stripPrefix !== undefined)
            rpcArgs.stripPrefix = stripPrefix;
        if (targetEndpointName !== undefined)
            rpcArgs.targetEndpointName = targetEndpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/publishAsStaticWebsite', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Publishes the JavaScript application as a standalone static website using YARP.
     * @param options Additional options.
     */
    publishAsStaticWebsite(options) {
        const apiPath = options?.apiPath;
        let apiTarget = options?.apiTarget;
        const outputPath = options?.outputPath;
        const stripPrefix = options?.stripPrefix;
        const targetEndpointName = options?.targetEndpointName;
        return new ViteAppResourcePromiseImpl(this._publishAsStaticWebsiteInternal(apiPath, apiTarget, outputPath, stripPrefix, targetEndpointName), this._client);
    }
    /** @internal */
    async _publishAsNodeServerInternal(entryPoint, outputPath) {
        const rpcArgs = { builder: this._handle, entryPoint };
        if (outputPath !== undefined)
            rpcArgs.outputPath = outputPath;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/publishAsNodeServer', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Configures the JavaScript application to publish as a standalone Node.js server that runs a built artifact directly.
     *
     * Use this method for frameworks that produce a Node.js server artifact during the build and recommend
     * running that artifact directly in production rather than invoking a package manager script at runtime.
     * The application source is still built using the configured package manager and build script; this method
     * only changes the publish-time runtime container shape.
     * The container files source path is automatically set to `outputPath` so that only
     * the built output directory is copied into the runtime container, not the full application source.
     * @param entryPoint The relative path to the Node.js entry point to execute in the published container after the build completes, such as `.output/server/index.js` or `build/index.js`.
     * @param options Additional options.
     * @returns The updated resource builder.
     */
    publishAsNodeServer(entryPoint, options) {
        const outputPath = options?.outputPath;
        return new ViteAppResourcePromiseImpl(this._publishAsNodeServerInternal(entryPoint, outputPath), this._client);
    }
    /** @internal */
    async _publishAsPackageScriptInternal(scriptName, runScriptArguments) {
        const rpcArgs = { builder: this._handle };
        if (scriptName !== undefined)
            rpcArgs.scriptName = scriptName;
        if (runScriptArguments !== undefined)
            rpcArgs.runScriptArguments = runScriptArguments;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/publishAsPackageScript', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Configures the JavaScript application to publish as a Node.js server that uses a `package.json` script at runtime.
     *
     * Use this method for frameworks where the production server depends on packages in `node_modules` at runtime.
     * The resulting container includes the full application with production dependencies installed.
     * This method is appropriate for frameworks like Nuxt (where `useAsyncData`/`useFetch` requires the
     * full Nitro environment), Remix (where `react-router-serve` is an npm dependency), and Astro SSR
     * (where the built entry point imports unbundled `@astrojs/*` packages).
     * For frameworks that produce a self-contained server artifact that does not require `node_modules`,
     * use `PublishAsNodeServer``1` instead for a smaller runtime image.
     * @param options Additional options.
     * @returns The updated resource builder.
     */
    publishAsPackageScript(options) {
        const scriptName = options?.scriptName;
        const runScriptArguments = options?.runScriptArguments;
        return new ViteAppResourcePromiseImpl(this._publishAsPackageScriptInternal(scriptName, runScriptArguments), this._client);
    }
    /** @internal */
    async _withViteConfigInternal(configPath) {
        const rpcArgs = { builder: this._handle, configPath };
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/withViteConfig', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Configures the Vite app to use the specified Vite configuration file instead of the default resolution behavior.
     *
     * Use this method to specify a specific Vite configuration file if you need to override the default Vite configuration resolution behavior.
     * @param configPath The path to the Vite configuration file. Relative to the Vite service project root.
     * @returns The resource builder.
     */
    withViteConfig(configPath) {
        return new ViteAppResourcePromiseImpl(this._withViteConfigInternal(configPath), this._client);
    }
    /** @internal */
    async _withNpmInternal(install, installCommand, installArgs) {
        const rpcArgs = { resource: this._handle };
        if (install !== undefined)
            rpcArgs.install = install;
        if (installCommand !== undefined)
            rpcArgs.installCommand = installCommand;
        if (installArgs !== undefined)
            rpcArgs.installArgs = installArgs;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/withNpm', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Configures the Node.js resource to use npm as the package manager and optionally installs packages before the application starts.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withNpm(options) {
        const install = options?.install;
        const installCommand = options?.installCommand;
        const installArgs = options?.installArgs;
        return new ViteAppResourcePromiseImpl(this._withNpmInternal(install, installCommand, installArgs), this._client);
    }
    /** @internal */
    async _withBunInternal(install, installArgs) {
        const rpcArgs = { resource: this._handle };
        if (install !== undefined)
            rpcArgs.install = install;
        if (installArgs !== undefined)
            rpcArgs.installArgs = installArgs;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/withBun', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Configures the JavaScript resource to use Bun as the package manager and optionally installs packages before the application starts.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withBun(options) {
        const install = options?.install;
        const installArgs = options?.installArgs;
        return new ViteAppResourcePromiseImpl(this._withBunInternal(install, installArgs), this._client);
    }
    /** @internal */
    async _withYarnInternal(install, installArgs) {
        const rpcArgs = { resource: this._handle };
        if (install !== undefined)
            rpcArgs.install = install;
        if (installArgs !== undefined)
            rpcArgs.installArgs = installArgs;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/withYarn', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Configures the Node.js resource to use yarn as the package manager and optionally installs packages before the application starts.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withYarn(options) {
        const install = options?.install;
        const installArgs = options?.installArgs;
        return new ViteAppResourcePromiseImpl(this._withYarnInternal(install, installArgs), this._client);
    }
    /** @internal */
    async _withPnpmInternal(install, installArgs) {
        const rpcArgs = { resource: this._handle };
        if (install !== undefined)
            rpcArgs.install = install;
        if (installArgs !== undefined)
            rpcArgs.installArgs = installArgs;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/withPnpm', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Configures the Node.js resource to use pnpm as the package manager and optionally installs packages before the application starts.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withPnpm(options) {
        const install = options?.install;
        const installArgs = options?.installArgs;
        return new ViteAppResourcePromiseImpl(this._withPnpmInternal(install, installArgs), this._client);
    }
    /** @internal */
    async _withBuildScriptInternal(scriptName, args) {
        const rpcArgs = { resource: this._handle, scriptName };
        if (args !== undefined)
            rpcArgs.args = args;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/withBuildScript', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Adds a build script annotation to the resource builder using the specified command-line arguments.
     *
     * Use this method to specify custom build scripts for JavaScript application resources during
     * deployment.
     * @param scriptName The name of the script to be executed when the resource is built.
     * @param options Additional options.
     * @returns The same resource builder instance with the build script annotation applied.
     */
    withBuildScript(scriptName, options) {
        const args = options?.args;
        return new ViteAppResourcePromiseImpl(this._withBuildScriptInternal(scriptName, args), this._client);
    }
    /** @internal */
    async _withRunScriptInternal(scriptName, args) {
        const rpcArgs = { resource: this._handle, scriptName };
        if (args !== undefined)
            rpcArgs.args = args;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/withRunScript', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Adds a run script annotation to the specified JavaScript application resource builder, specifying the script to execute and its arguments during run mode.
     *
     * Use this method to specify a custom script and its arguments that should be executed when the resource is executed
     * in RunMode.
     * @param scriptName The name of the script to be executed when the resource is run.
     * @param options Additional options.
     * @returns The same resource builder instance with the run script annotation applied, enabling further configuration.
     */
    withRunScript(scriptName, options) {
        const args = options?.args;
        return new ViteAppResourcePromiseImpl(this._withRunScriptInternal(scriptName, args), this._client);
    }
    /** @internal */
    async _withBrowserDebuggerInternal(browser) {
        const rpcArgs = { builder: this._handle };
        if (browser !== undefined)
            rpcArgs.browser = browser;
        const result = await this._client.invokeCapability('Aspire.Hosting.JavaScript/withBrowserDebugger', rpcArgs);
        return new ViteAppResourceImpl(result, this._client);
    }
    /**
     * Configures a browser debugger for the JavaScript application resource, enabling browser-based debugging through a child resource that launches when the parent application is ready.
     *
     * This method creates a child `BrowserDebuggerResource` that waits for the parent JavaScript
     * application to start, then launches a browser debug session targeting the parent's HTTP or HTTPS endpoint.
     * The parent resource must have at least one HTTP or HTTPS endpoint configured.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withBrowserDebugger(options) {
        const browser = options?.browser;
        return new ViteAppResourcePromiseImpl(this._withBrowserDebuggerInternal(browser), this._client);
    }
}
/**
 * Thenable wrapper for ViteAppResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
class ViteAppResourcePromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    command() {
        return this._promise.then(obj => obj.command());
    }
    workingDirectory() {
        return this._promise.then(obj => obj.workingDirectory());
    }
    name() {
        return this._promise.then(obj => obj.name());
    }
    withContainerRegistry(registry) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withContainerRegistry(registry)), this._client);
    }
    withDockerfileBaseImage(options) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileBaseImage(options)), this._client);
    }
    publishAsDockerFile(configure) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.publishAsDockerFile(configure)), this._client);
    }
    withExecutableCommand(command) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withExecutableCommand(command)), this._client);
    }
    withWorkingDirectory(workingDirectory) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withWorkingDirectory(workingDirectory)), this._client);
    }
    withMcpServer(options) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withMcpServer(options)), this._client);
    }
    withOtlpExporter(options) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withOtlpExporter(options)), this._client);
    }
    withRequiredCommand(command, options) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withRequiredCommand(command, options)), this._client);
    }
    withSessionLifetime() {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withSessionLifetime()), this._client);
    }
    withPersistentLifetime() {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withPersistentLifetime()), this._client);
    }
    withLifetimeOf(sourceBuilder) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withLifetimeOf(sourceBuilder)), this._client);
    }
    withParentProcessLifetime(parentProcessId) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withParentProcessLifetime(parentProcessId)), this._client);
    }
    withEnvironment(name, value) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withEnvironment(name, value)), this._client);
    }
    withEnvironmentCallback(callback) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withEnvironmentCallback(callback)), this._client);
    }
    withArgs(args) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withArgs(args)), this._client);
    }
    withArgsCallback(callback) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withArgsCallback(callback)), this._client);
    }
    withReferenceEnvironment(options) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withReferenceEnvironment(options)), this._client);
    }
    withReference(source, options) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withReference(source, options)), this._client);
    }
    withEndpointCallback(endpointName, callback, options) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withEndpointCallback(endpointName, callback, options)), this._client);
    }
    withHttpEndpointCallback(callback, options) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpEndpointCallback(callback, options)), this._client);
    }
    withHttpsEndpointCallback(callback, options) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpsEndpointCallback(callback, options)), this._client);
    }
    withEndpoint(options) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withEndpoint(options)), this._client);
    }
    withEndpointProxySupport(proxyEnabled) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withEndpointProxySupport(proxyEnabled)), this._client);
    }
    withHttpEndpoint(options) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpEndpoint(options)), this._client);
    }
    withHttpsEndpoint(options) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpsEndpoint(options)), this._client);
    }
    withExternalHttpEndpoints() {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withExternalHttpEndpoints()), this._client);
    }
    getEndpoint(name) {
        return new EndpointReferencePromiseImpl(this._promise.then(obj => obj.getEndpoint(name)), this._client);
    }
    asHttp2Service() {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.asHttp2Service()), this._client);
    }
    withUrls(callback) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withUrls(callback)), this._client);
    }
    withUrl(url, options) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withUrl(url, options)), this._client);
    }
    withUrlForEndpoint(endpointName, callback) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)), this._client);
    }
    withContainerFilesSource(sourcePath) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withContainerFilesSource(sourcePath)), this._client);
    }
    clearContainerFilesSources() {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.clearContainerFilesSources()), this._client);
    }
    excludeFromManifest() {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.excludeFromManifest()), this._client);
    }
    waitFor(dependency, options) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.waitFor(dependency, options)), this._client);
    }
    waitForStart(dependency, options) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.waitForStart(dependency, options)), this._client);
    }
    withExplicitStart() {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withExplicitStart()), this._client);
    }
    waitForCompletion(dependency, options) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.waitForCompletion(dependency, options)), this._client);
    }
    withHealthCheck(key) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withHealthCheck(key)), this._client);
    }
    withHttpHealthCheck(options) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpHealthCheck(options)), this._client);
    }
    withCommand(name, displayName, executeCommand, options) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)), this._client);
    }
    withProcessCommand(commandName, displayName, options) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommand(commandName, displayName, options)), this._client);
    }
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommandFactory(commandName, displayName, createProcessSpec, options)), this._client);
    }
    withHttpCommand(path, displayName, options) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpCommand(path, displayName, options)), this._client);
    }
    withDeveloperCertificateTrust(trust) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withDeveloperCertificateTrust(trust)), this._client);
    }
    withCertificateTrustScope(scope) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withCertificateTrustScope(scope)), this._client);
    }
    withHttpsDeveloperCertificate(options) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpsDeveloperCertificate(options)), this._client);
    }
    withoutHttpsCertificate() {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withoutHttpsCertificate()), this._client);
    }
    withRelationship(resourceBuilder, type) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withRelationship(resourceBuilder, type)), this._client);
    }
    withParentRelationship(parent) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withParentRelationship(parent)), this._client);
    }
    withChildRelationship(child) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withChildRelationship(child)), this._client);
    }
    withIconName(iconName, options) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withIconName(iconName, options)), this._client);
    }
    withComputeEnvironment(computeEnvironmentResource) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withComputeEnvironment(computeEnvironmentResource)), this._client);
    }
    withHttpProbe(probeType, options) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withHttpProbe(probeType, options)), this._client);
    }
    excludeFromMcp() {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.excludeFromMcp()), this._client);
    }
    withHidden() {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withHidden()), this._client);
    }
    withHiddenOnCompletion(options) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withHiddenOnCompletion(options)), this._client);
    }
    withImagePushOptions(callback) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withImagePushOptions(callback)), this._client);
    }
    withRemoteImageName(remoteImageName) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withRemoteImageName(remoteImageName)), this._client);
    }
    withRemoteImageTag(remoteImageTag) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withRemoteImageTag(remoteImageTag)), this._client);
    }
    withPipelineStepFactory(stepName, callback, options) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)), this._client);
    }
    withPipelineConfiguration(callback) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withPipelineConfiguration(callback)), this._client);
    }
    getResourceName() {
        return this._promise.then(obj => obj.getResourceName());
    }
    onBeforeResourceStarted(callback) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.onBeforeResourceStarted(callback)), this._client);
    }
    onResourceStopped(callback) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.onResourceStopped(callback)), this._client);
    }
    onInitializeResource(callback) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.onInitializeResource(callback)), this._client);
    }
    onResourceEndpointsAllocated(callback) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.onResourceEndpointsAllocated(callback)), this._client);
    }
    onResourceReady(callback) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.onResourceReady(callback)), this._client);
    }
    createExecutionConfiguration() {
        return new ExecutionConfigurationBuilderPromiseImpl(this._promise.then(obj => obj.createExecutionConfiguration()), this._client);
    }
    publishAsStaticWebsite(options) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.publishAsStaticWebsite(options)), this._client);
    }
    publishAsNodeServer(entryPoint, options) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.publishAsNodeServer(entryPoint, options)), this._client);
    }
    publishAsPackageScript(options) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.publishAsPackageScript(options)), this._client);
    }
    withViteConfig(configPath) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withViteConfig(configPath)), this._client);
    }
    withNpm(options) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withNpm(options)), this._client);
    }
    withBun(options) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withBun(options)), this._client);
    }
    withYarn(options) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withYarn(options)), this._client);
    }
    withPnpm(options) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withPnpm(options)), this._client);
    }
    withBuildScript(scriptName, options) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withBuildScript(scriptName, options)), this._client);
    }
    withRunScript(scriptName, options) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withRunScript(scriptName, options)), this._client);
    }
    withBrowserDebugger(options) {
        return new ViteAppResourcePromiseImpl(this._promise.then(obj => obj.withBrowserDebugger(options)), this._client);
    }
}
// ============================================================================
// ComputeEnvironmentResourceImpl
// ============================================================================
class ComputeEnvironmentResourceImpl extends ResourceBuilderBase {
    constructor(handle, client) {
        super(handle, client);
    }
}
// ============================================================================
// ComputeResourceImpl
// ============================================================================
class ComputeResourceImpl extends ResourceBuilderBase {
    constructor(handle, client) {
        super(handle, client);
    }
    /** @internal */
    async _withComputeEnvironmentInternal(computeEnvironmentResource) {
        computeEnvironmentResource = isPromiseLike(computeEnvironmentResource) ? await computeEnvironmentResource : computeEnvironmentResource;
        const rpcArgs = { builder: this._handle, computeEnvironmentResource };
        const result = await this._client.invokeCapability('Aspire.Hosting/withComputeEnvironment', rpcArgs);
        return new ComputeResourceImpl(result, this._client);
    }
    /**
     * Configures the compute environment for the compute resource.
     *
     * This method allows associating a specific compute environment with the compute resource.
     * @param computeEnvironmentResource The compute environment resource to associate with the compute resource.
     * @returns The resource builder.
     */
    withComputeEnvironment(computeEnvironmentResource) {
        return new ComputeResourcePromiseImpl(this._withComputeEnvironmentInternal(computeEnvironmentResource), this._client);
    }
    /** @internal */
    async _withImagePushOptionsInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ContainerImagePushOptionsCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withImagePushOptions', rpcArgs);
        return new ComputeResourceImpl(result, this._client);
    }
    /**
     * Adds an asynchronous callback to configure container image push options for the resource.
     *
     * This method allows customization of how container images are named and tagged when pushed to a registry using an asynchronous callback.
     * Use this overload when the callback needs to perform asynchronous operations such as retrieving configuration values from external sources.
     * The callback receives a `ContainerImagePushOptionsCallbackContext` that provides access to the resource
     * and the `ContainerImagePushOptions` that can be modified.
     * Multiple callbacks can be registered on the same resource, and they will be invoked in the order they were added.
     * @param callback The asynchronous callback to configure push options.
     * @returns The resource builder.
     */
    withImagePushOptions(callback) {
        return new ComputeResourcePromiseImpl(this._withImagePushOptionsInternal(callback), this._client);
    }
    /** @internal */
    async _withRemoteImageNameInternal(remoteImageName) {
        const rpcArgs = { builder: this._handle, remoteImageName };
        const result = await this._client.invokeCapability('Aspire.Hosting/withRemoteImageName', rpcArgs);
        return new ComputeResourceImpl(result, this._client);
    }
    /**
     * Sets the remote image name (without registry endpoint or tag) for container push operations.
     *
     * Use this with `withRemoteImageTag` to fully customize the image reference used for container push operations.
     * @param remoteImageName The remote image name (e.g., "myapp" or "myorg/myapp").
     * @returns The resource builder.
     */
    withRemoteImageName(remoteImageName) {
        return new ComputeResourcePromiseImpl(this._withRemoteImageNameInternal(remoteImageName), this._client);
    }
    /** @internal */
    async _withRemoteImageTagInternal(remoteImageTag) {
        const rpcArgs = { builder: this._handle, remoteImageTag };
        const result = await this._client.invokeCapability('Aspire.Hosting/withRemoteImageTag', rpcArgs);
        return new ComputeResourceImpl(result, this._client);
    }
    /**
     * Sets the remote image tag for container push operations.
     *
     * Use this with `withRemoteImageName` to fully customize the image reference used for container push operations.
     * @param remoteImageTag The remote image tag (e.g., "latest", "v1.0.0").
     * @returns The resource builder.
     */
    withRemoteImageTag(remoteImageTag) {
        return new ComputeResourcePromiseImpl(this._withRemoteImageTagInternal(remoteImageTag), this._client);
    }
}
/**
 * Thenable wrapper for ComputeResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
class ComputeResourcePromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    withComputeEnvironment(computeEnvironmentResource) {
        return new ComputeResourcePromiseImpl(this._promise.then(obj => obj.withComputeEnvironment(computeEnvironmentResource)), this._client);
    }
    withImagePushOptions(callback) {
        return new ComputeResourcePromiseImpl(this._promise.then(obj => obj.withImagePushOptions(callback)), this._client);
    }
    withRemoteImageName(remoteImageName) {
        return new ComputeResourcePromiseImpl(this._promise.then(obj => obj.withRemoteImageName(remoteImageName)), this._client);
    }
    withRemoteImageTag(remoteImageTag) {
        return new ComputeResourcePromiseImpl(this._promise.then(obj => obj.withRemoteImageTag(remoteImageTag)), this._client);
    }
}
// ============================================================================
// ContainerFilesDestinationResourceImpl
// ============================================================================
class ContainerFilesDestinationResourceImpl extends ResourceBuilderBase {
    constructor(handle, client) {
        super(handle, client);
    }
    /** @internal */
    async _publishWithContainerFilesInternal(source, destinationPath) {
        source = isPromiseLike(source) ? await source : source;
        const rpcArgs = { builder: this._handle, source, destinationPath };
        const result = await this._client.invokeCapability('Aspire.Hosting/publishWithContainerFilesFromResource', rpcArgs);
        return new ContainerFilesDestinationResourceImpl(result, this._client);
    }
    /**
     * Configures the resource to copy container files from the specified source resource during publishing.
     * @param source The resource which contains the container files to be copied.
     * @param destinationPath The destination path within the resource's container where the files will be copied.
     */
    publishWithContainerFiles(source, destinationPath) {
        return new ContainerFilesDestinationResourcePromiseImpl(this._publishWithContainerFilesInternal(source, destinationPath), this._client);
    }
}
/**
 * Thenable wrapper for ContainerFilesDestinationResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
class ContainerFilesDestinationResourcePromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    publishWithContainerFiles(source, destinationPath) {
        return new ContainerFilesDestinationResourcePromiseImpl(this._promise.then(obj => obj.publishWithContainerFiles(source, destinationPath)), this._client);
    }
}
// ============================================================================
// ResourceImpl
// ============================================================================
/** Represents a resource that can be hosted by an application. */
class ResourceImpl extends ResourceBuilderBase {
    constructor(handle, client) {
        super(handle, client);
    }
    /** @internal */
    async _withContainerRegistryInternal(registry) {
        registry = isPromiseLike(registry) ? await registry : registry;
        const rpcArgs = { builder: this._handle, registry };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerRegistry', rpcArgs);
        return new ResourceImpl(result, this._client);
    }
    /**
     * Configures the resource to use the specified container registry for container image operations.
     *
     * This method adds a `ContainerRegistryReferenceAnnotation` to the resource,
     * indicating that the resource should use the specified container registry for container image operations.
     * @param registry The container registry resource builder.
     * @returns The resource builder for chaining.
     */
    withContainerRegistry(registry) {
        return new ResourcePromiseImpl(this._withContainerRegistryInternal(registry), this._client);
    }
    /** @internal */
    async _withDockerfileBaseImageInternal(buildImage, runtimeImage) {
        const rpcArgs = { builder: this._handle };
        if (buildImage !== undefined)
            rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined)
            rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability('Aspire.Hosting/withDockerfileBaseImage', rpcArgs);
        return new ResourceImpl(result, this._client);
    }
    /**
     * Configures custom base images for generated Dockerfiles.
     *
     * This extension method allows customization of the base images used in generated Dockerfiles.
     * For multi-stage Dockerfiles (e.g., Python with UV), you can specify separate build and runtime images.
     * Specify custom base images for a Python application:
     * ```
     * var builder = DistributedApplication.CreateBuilder(args);
     * builder.AddPythonApp("myapp", "path/to/app", "main.py")
     * .WithDockerfileBaseImage(
     * buildImage: "ghcr.io/astral-sh/uv:python3.12-bookworm-slim",
     * runtimeImage: "python:3.12-slim-bookworm");
     * builder.Build().Run();
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withDockerfileBaseImage(options) {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new ResourcePromiseImpl(this._withDockerfileBaseImageInternal(buildImage, runtimeImage), this._client);
    }
    /** @internal */
    async _withRequiredCommandInternal(command, helpLink) {
        const rpcArgs = { builder: this._handle, command };
        if (helpLink !== undefined)
            rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability('Aspire.Hosting/withRequiredCommand', rpcArgs);
        return new ResourceImpl(result, this._client);
    }
    /**
     * Declares that a resource requires a specific command/executable to be available on the local machine PATH before it can start.
     *
     * The command is considered valid if either:
     * 1. It is an absolute or relative path (contains a directory separator) that points to an existing file, or
     * 2. It is discoverable on the current process PATH (respecting PATHEXT on Windows).
     * If the command is not found, a warning message will be logged but the resource will be allowed to attempt to start.
     * @param command The command string (file name or path) that should be validated.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withRequiredCommand(command, options) {
        const helpLink = options?.helpLink;
        return new ResourcePromiseImpl(this._withRequiredCommandInternal(command, helpLink), this._client);
    }
    /** @internal */
    async _withSessionLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withSessionLifetime', rpcArgs);
        return new ResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a session lifetime.
     * @returns The resource builder.
     */
    withSessionLifetime() {
        return new ResourcePromiseImpl(this._withSessionLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withPersistentLifetimeInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPersistentLifetime', rpcArgs);
        return new ResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime.
     * @returns The resource builder.
     */
    withPersistentLifetime() {
        return new ResourcePromiseImpl(this._withPersistentLifetimeInternal(), this._client);
    }
    /** @internal */
    async _withLifetimeOfInternal(sourceBuilder) {
        sourceBuilder = isPromiseLike(sourceBuilder) ? await sourceBuilder : sourceBuilder;
        const rpcArgs = { builder: this._handle, sourceBuilder };
        const result = await this._client.invokeCapability('Aspire.Hosting/withLifetimeOf', rpcArgs);
        return new ResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to match the lifetime of another resource.
     *
     * The resource lifetime is evaluated from `sourceBuilder` when the application model is prepared, so later lifetime
     * changes to the source resource are reflected by this resource.
     * @param sourceBuilder The resource builder whose lifetime should be used.
     * @returns The resource builder.
     */
    withLifetimeOf(sourceBuilder) {
        return new ResourcePromiseImpl(this._withLifetimeOfInternal(sourceBuilder), this._client);
    }
    /** @internal */
    async _withParentProcessLifetimeInternal(parentProcessId) {
        const rpcArgs = { builder: this._handle, parentProcessId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withParentProcessLifetime', rpcArgs);
        return new ResourceImpl(result, this._client);
    }
    /**
     * Configures a resource to use a persistent lifetime that ends when a parent process exits.
     * @param parentProcessId The ID of the parent process to monitor.
     * @returns The resource builder.
     */
    withParentProcessLifetime(parentProcessId) {
        return new ResourcePromiseImpl(this._withParentProcessLifetimeInternal(parentProcessId), this._client);
    }
    /** @internal */
    async _withUrlsInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new ResourceUrlsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrls', rpcArgs);
        return new ResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to customize the URLs displayed for the resource.
     * @param callback The callback that will customize URLs for the resource.
     * @returns The resource builder.
     */
    withUrls(callback) {
        return new ResourcePromiseImpl(this._withUrlsInternal(callback), this._client);
    }
    /** @internal */
    async _withUrlInternal(url, displayText) {
        const rpcArgs = { builder: this._handle, url };
        if (displayText !== undefined)
            rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrl', rpcArgs);
        return new ResourceImpl(result, this._client);
    }
    /**
     * Adds or modifies displayed URLs
     * @param options Additional options.
     */
    withUrl(url, options) {
        const displayText = options?.displayText;
        return new ResourcePromiseImpl(this._withUrlInternal(url, displayText), this._client);
    }
    /** @internal */
    async _withUrlForEndpointInternal(endpointName, callback) {
        const callbackId = registerCallback(async (objData) => {
            const obj = wrapIfHandle(objData);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withUrlForEndpoint', rpcArgs);
        return new ResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to update the URL displayed for the endpoint with the specified name.
     * @param endpointName The name of the endpoint to customize the URL for.
     * @param callback The callback that will customize the URL.
     * @returns The resource builder.
     */
    withUrlForEndpoint(endpointName, callback) {
        return new ResourcePromiseImpl(this._withUrlForEndpointInternal(endpointName, callback), this._client);
    }
    /** @internal */
    async _excludeFromManifestInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromManifest', rpcArgs);
        return new ResourceImpl(result, this._client);
    }
    /**
     * Excludes a resource from being published to the manifest.
     * @returns The resource builder.
     */
    excludeFromManifest() {
        return new ResourcePromiseImpl(this._excludeFromManifestInternal(), this._client);
    }
    /** @internal */
    async _withExplicitStartInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExplicitStart', rpcArgs);
        return new ResourceImpl(result, this._client);
    }
    /**
     * Prevents resource from starting automatically
     * @returns The resource builder.
     */
    withExplicitStart() {
        return new ResourcePromiseImpl(this._withExplicitStartInternal(), this._client);
    }
    /** @internal */
    async _withHealthCheckInternal(key) {
        const rpcArgs = { builder: this._handle, key };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHealthCheck', rpcArgs);
        return new ResourceImpl(result, this._client);
    }
    /**
     * Adds a health check by key
     * @param key The key for the health check.
     * @returns The resource builder.
     */
    withHealthCheck(key) {
        return new ResourcePromiseImpl(this._withHealthCheckInternal(key), this._client);
    }
    /** @internal */
    async _withCommandInternal(name, displayName, executeCommand, commandOptions) {
        const executeCommandId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined)
            rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability('Aspire.Hosting/withCommand', rpcArgs);
        return new ResourceImpl(result, this._client);
    }
    /**
     * Adds a resource command
     *
     * The `WithCommand` method is used to add commands to the resource. Commands are displayed in the dashboard
     * and can be executed by a user using the dashboard UI.
     * When a command is executed, the `executeCommand` callback is called and is run inside the Aspire host.
     * @param name The name of the command. The name uniquely identifies the command.
     * @param displayName The display name visible in UI.
     * @param executeCommand A callback that is executed when the command is executed. The callback is run inside the Aspire host. The callback result is used to indicate success or failure in the UI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withCommand(name, displayName, executeCommand, options) {
        const commandOptions = options?.commandOptions;
        return new ResourcePromiseImpl(this._withCommandInternal(name, displayName, executeCommand, commandOptions), this._client);
    }
    /** @internal */
    async _withProcessCommandInternal(commandName, displayName, options) {
        const rpcArgs = { builder: this._handle, commandName, displayName, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommand', rpcArgs);
        return new ResourceImpl(result, this._client);
    }
    /** Adds a command to the resource that starts a local process when invoked. */
    withProcessCommand(commandName, displayName, options) {
        return new ResourcePromiseImpl(this._withProcessCommandInternal(commandName, displayName, options), this._client);
    }
    /** @internal */
    async _withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options) {
        const createProcessSpecId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ExecuteCommandContextImpl(argHandle, this._client);
            return await createProcessSpec(arg);
        });
        const rpcArgs = { builder: this._handle, commandName, displayName, createProcessSpec: createProcessSpecId };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withProcessCommandFactory', rpcArgs);
        return new ResourceImpl(result, this._client);
    }
    /**
     * Adds a command to the resource that starts a local process created by a callback when invoked.
     * @param options Additional options.
     */
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new ResourcePromiseImpl(this._withProcessCommandFactoryInternal(commandName, displayName, createProcessSpec, options), this._client);
    }
    /** @internal */
    async _withRelationshipInternal(resourceBuilder, type) {
        resourceBuilder = isPromiseLike(resourceBuilder) ? await resourceBuilder : resourceBuilder;
        const rpcArgs = { builder: this._handle, resourceBuilder, type };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderRelationship', rpcArgs);
        return new ResourceImpl(result, this._client);
    }
    /**
     * Adds a relationship to another resource using its builder.
     * @param resourceBuilder The resource builder that the relationship is to.
     * @param type The relationship type.
     * @returns A resource builder.
     */
    withRelationship(resourceBuilder, type) {
        return new ResourcePromiseImpl(this._withRelationshipInternal(resourceBuilder, type), this._client);
    }
    /** @internal */
    async _withParentRelationshipInternal(parent) {
        parent = isPromiseLike(parent) ? await parent : parent;
        const rpcArgs = { builder: this._handle, parent };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderParentRelationship', rpcArgs);
        return new ResourceImpl(result, this._client);
    }
    /**
     * Sets the parent relationship
     * @param parent The parent of `builder`.
     * @returns A resource builder.
     */
    withParentRelationship(parent) {
        return new ResourcePromiseImpl(this._withParentRelationshipInternal(parent), this._client);
    }
    /** @internal */
    async _withChildRelationshipInternal(child) {
        child = isPromiseLike(child) ? await child : child;
        const rpcArgs = { builder: this._handle, child };
        const result = await this._client.invokeCapability('Aspire.Hosting/withBuilderChildRelationship', rpcArgs);
        return new ResourceImpl(result, this._client);
    }
    /**
     * Sets a child relationship
     * @param child The child of `builder`.
     * @returns A resource builder.
     */
    withChildRelationship(child) {
        return new ResourcePromiseImpl(this._withChildRelationshipInternal(child), this._client);
    }
    /** @internal */
    async _withIconNameInternal(iconName, iconVariant) {
        const rpcArgs = { builder: this._handle, iconName };
        if (iconVariant !== undefined)
            rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability('Aspire.Hosting/withIconName', rpcArgs);
        return new ResourceImpl(result, this._client);
    }
    /**
     * Specifies the icon to use when displaying the resource in the dashboard.
     * @param iconName The name of the FluentUI icon to use. See https://aka.ms/fluentui-system-icons for available icons.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withIconName(iconName, options) {
        const iconVariant = options?.iconVariant;
        return new ResourcePromiseImpl(this._withIconNameInternal(iconName, iconVariant), this._client);
    }
    /** @internal */
    async _excludeFromMcpInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/excludeFromMcp', rpcArgs);
        return new ResourceImpl(result, this._client);
    }
    /**
     * Exclude the resource from MCP operations using the Aspire MCP server. The resource is excluded from results that return resources, console logs and telemetry.
     * @returns The resource builder.
     */
    excludeFromMcp() {
        return new ResourcePromiseImpl(this._excludeFromMcpInternal(), this._client);
    }
    /** @internal */
    async _withHiddenInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withHidden', rpcArgs);
        return new ResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists
     *
     * Use this method to hide resources that are implementation details and should never be displayed by default.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @returns The resource builder.
     */
    withHidden() {
        return new ResourcePromiseImpl(this._withHiddenInternal(), this._client);
    }
    /** @internal */
    async _withHiddenOnCompletionInternal(exitCode, exitCodes) {
        const rpcArgs = { builder: this._handle };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        if (exitCodes !== undefined)
            rpcArgs.exitCodes = exitCodes;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHiddenOnCompletion', rpcArgs);
        return new ResourceImpl(result, this._client);
    }
    /**
     * Hides the resource from default resource lists after successful completion
     *
     * This method is useful for one-off resources such as setup scripts, migrations, or build steps that should remain visible while running
     * and then be hidden after successful completion.
     * Hidden resources can still be accessed directly by their name, by using `Show hidden resources` toggle in the dashboard or by using `aspire describe --include-hidden` from the CLI.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHiddenOnCompletion(options) {
        const exitCode = options?.exitCode;
        const exitCodes = options?.exitCodes;
        return new ResourcePromiseImpl(this._withHiddenOnCompletionInternal(exitCode, exitCodes), this._client);
    }
    /** @internal */
    async _withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new PipelineStepContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined)
            rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined)
            rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined)
            rpcArgs.tags = tags;
        if (description !== undefined)
            rpcArgs.description = description;
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineStepFactory', rpcArgs);
        return new ResourceImpl(result, this._client);
    }
    /**
     * Adds a pipeline step to the resource that will be executed during deployment.
     * @param stepName The unique name of the pipeline step.
     * @param callback The callback to execute when the step runs.
     * @param options Additional options.
     * @returns The resource builder for chaining.
     */
    withPipelineStepFactory(stepName, callback, options) {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new ResourcePromiseImpl(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description), this._client);
    }
    /** @internal */
    async _withPipelineConfigurationInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new PipelineConfigurationContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withPipelineConfiguration', rpcArgs);
        return new ResourceImpl(result, this._client);
    }
    /**
     * Registers a callback to be executed during the pipeline configuration phase, allowing modification of step dependencies and relationships.
     * @param callback The callback function to execute during the configuration phase.
     * @returns The resource builder for chaining.
     */
    withPipelineConfiguration(callback) {
        return new ResourcePromiseImpl(this._withPipelineConfigurationInternal(callback), this._client);
    }
    /**
     * Gets the name of the resource from a builder.
     *
     * Why this wrapper exists: This capability accesses a nested property
     * (`resource.Resource.Name`) which requires a wrapper method. There is no single
     * .NET method that returns just the resource name that could be annotated directly.
     * @returns The resource name.
     */
    async getResourceName() {
        const rpcArgs = { resource: this._handle };
        return await this._client.invokeCapability('Aspire.Hosting/getResourceName', rpcArgs);
    }
    /** @internal */
    async _onBeforeResourceStartedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new BeforeResourceStartedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onBeforeResourceStarted', rpcArgs);
        return new ResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the BeforeResourceStarted event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onBeforeResourceStarted(callback) {
        return new ResourcePromiseImpl(this._onBeforeResourceStartedInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceStoppedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceStoppedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceStopped', rpcArgs);
        return new ResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceStopped event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceStopped(callback) {
        return new ResourcePromiseImpl(this._onResourceStoppedInternal(callback), this._client);
    }
    /** @internal */
    async _onInitializeResourceInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new InitializeResourceEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onInitializeResource', rpcArgs);
        return new ResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the InitializeResource event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onInitializeResource(callback) {
        return new ResourcePromiseImpl(this._onInitializeResourceInternal(callback), this._client);
    }
    /** @internal */
    async _onResourceReadyInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceReadyEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceReady', rpcArgs);
        return new ResourceImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceReady event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceReady(callback) {
        return new ResourcePromiseImpl(this._onResourceReadyInternal(callback), this._client);
    }
    /**
     * Creates an execution configuration builder for the specified resource.
     * @returns The execution configuration builder.
     */
    createExecutionConfiguration() {
        const promise = (async () => {
            const rpcArgs = { resource: this._handle };
            const handle = await this._client.invokeCapability('Aspire.Hosting/createExecutionConfiguration', rpcArgs);
            return new ExecutionConfigurationBuilderImpl(handle, this._client);
        })();
        return new ExecutionConfigurationBuilderPromiseImpl(promise, this._client);
    }
}
/**
 * Thenable wrapper for Resource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
class ResourcePromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    withContainerRegistry(registry) {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.withContainerRegistry(registry)), this._client);
    }
    withDockerfileBaseImage(options) {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.withDockerfileBaseImage(options)), this._client);
    }
    withRequiredCommand(command, options) {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.withRequiredCommand(command, options)), this._client);
    }
    withSessionLifetime() {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.withSessionLifetime()), this._client);
    }
    withPersistentLifetime() {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.withPersistentLifetime()), this._client);
    }
    withLifetimeOf(sourceBuilder) {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.withLifetimeOf(sourceBuilder)), this._client);
    }
    withParentProcessLifetime(parentProcessId) {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.withParentProcessLifetime(parentProcessId)), this._client);
    }
    withUrls(callback) {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.withUrls(callback)), this._client);
    }
    withUrl(url, options) {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.withUrl(url, options)), this._client);
    }
    withUrlForEndpoint(endpointName, callback) {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)), this._client);
    }
    excludeFromManifest() {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.excludeFromManifest()), this._client);
    }
    withExplicitStart() {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.withExplicitStart()), this._client);
    }
    withHealthCheck(key) {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.withHealthCheck(key)), this._client);
    }
    withCommand(name, displayName, executeCommand, options) {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)), this._client);
    }
    withProcessCommand(commandName, displayName, options) {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommand(commandName, displayName, options)), this._client);
    }
    withProcessCommandFactory(commandName, displayName, createProcessSpec, options) {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.withProcessCommandFactory(commandName, displayName, createProcessSpec, options)), this._client);
    }
    withRelationship(resourceBuilder, type) {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.withRelationship(resourceBuilder, type)), this._client);
    }
    withParentRelationship(parent) {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.withParentRelationship(parent)), this._client);
    }
    withChildRelationship(child) {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.withChildRelationship(child)), this._client);
    }
    withIconName(iconName, options) {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.withIconName(iconName, options)), this._client);
    }
    excludeFromMcp() {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.excludeFromMcp()), this._client);
    }
    withHidden() {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.withHidden()), this._client);
    }
    withHiddenOnCompletion(options) {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.withHiddenOnCompletion(options)), this._client);
    }
    withPipelineStepFactory(stepName, callback, options) {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)), this._client);
    }
    withPipelineConfiguration(callback) {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.withPipelineConfiguration(callback)), this._client);
    }
    getResourceName() {
        return this._promise.then(obj => obj.getResourceName());
    }
    onBeforeResourceStarted(callback) {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.onBeforeResourceStarted(callback)), this._client);
    }
    onResourceStopped(callback) {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.onResourceStopped(callback)), this._client);
    }
    onInitializeResource(callback) {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.onInitializeResource(callback)), this._client);
    }
    onResourceReady(callback) {
        return new ResourcePromiseImpl(this._promise.then(obj => obj.onResourceReady(callback)), this._client);
    }
    createExecutionConfiguration() {
        return new ExecutionConfigurationBuilderPromiseImpl(this._promise.then(obj => obj.createExecutionConfiguration()), this._client);
    }
}
// ============================================================================
// ResourceWithArgsImpl
// ============================================================================
/** Represents a resource that is associated with commandline arguments. */
class ResourceWithArgsImpl extends ResourceBuilderBase {
    constructor(handle, client) {
        super(handle, client);
    }
    /** @internal */
    async _withArgsInternal(args) {
        const rpcArgs = { builder: this._handle, args };
        const result = await this._client.invokeCapability('Aspire.Hosting/withArgs', rpcArgs);
        return new ResourceWithArgsImpl(result, this._client);
    }
    /**
     * Adds arguments to be passed to a resource that supports arguments when it is launched.
     * @param args The arguments to be passed to the resource when it is started.
     * @returns The resource builder.
     */
    withArgs(args) {
        return new ResourceWithArgsPromiseImpl(this._withArgsInternal(args), this._client);
    }
    /** @internal */
    async _withArgsCallbackInternal(callback) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new CommandLineArgsCallbackContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withArgsCallback', rpcArgs);
        return new ResourceWithArgsImpl(result, this._client);
    }
    /**
     * Adds a callback to be executed with a list of command-line arguments when a resource is started.
     * @param callback A callback that allows for deferred execution for computing arguments. This runs after resources have been allocated by the orchestrator and allows access to other resources to resolve computed data, e.g. connection strings, ports.
     * @returns The resource builder.
     */
    withArgsCallback(callback) {
        return new ResourceWithArgsPromiseImpl(this._withArgsCallbackInternal(callback), this._client);
    }
}
/**
 * Thenable wrapper for ResourceWithArgs that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
class ResourceWithArgsPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    withArgs(args) {
        return new ResourceWithArgsPromiseImpl(this._promise.then(obj => obj.withArgs(args)), this._client);
    }
    withArgsCallback(callback) {
        return new ResourceWithArgsPromiseImpl(this._promise.then(obj => obj.withArgsCallback(callback)), this._client);
    }
}
// ============================================================================
// ResourceWithConnectionStringImpl
// ============================================================================
/** Represents a resource that has a connection string associated with it. */
class ResourceWithConnectionStringImpl extends ResourceBuilderBase {
    constructor(handle, client) {
        super(handle, client);
    }
    /** @internal */
    async _withConnectionPropertyInternal(name, value) {
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withConnectionProperty', rpcArgs);
        return new ResourceWithConnectionStringImpl(result, this._client);
    }
    /**
     * Adds a connection property annotation to the resource being built.
     * @param name The name of the connection property to add.
     * @param value The value to assign to the connection property, specified as a string or reference expression.
     * @returns The same resource builder instance with the specified connection property annotation applied.
     */
    withConnectionProperty(name, value) {
        return new ResourceWithConnectionStringPromiseImpl(this._withConnectionPropertyInternal(name, value), this._client);
    }
    /**
     * Retrieves the value of a specified connection property from the resource's connection properties.
     *
     * Throws a KeyNotFoundException if the specified key does not exist in the resource's
     * connection properties.
     * @param key The key of the connection property to retrieve. Cannot be null.
     * @returns The value associated with the specified connection property key.
     */
    async getConnectionProperty(key) {
        const rpcArgs = { resource: this._handle, key };
        return await this._client.invokeCapability('Aspire.Hosting/getConnectionProperty', rpcArgs);
    }
    /** @internal */
    async _onConnectionStringAvailableInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ConnectionStringAvailableEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onConnectionStringAvailable', rpcArgs);
        return new ResourceWithConnectionStringImpl(result, this._client);
    }
    /**
     * Subscribes to the ConnectionStringAvailable event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onConnectionStringAvailable(callback) {
        return new ResourceWithConnectionStringPromiseImpl(this._onConnectionStringAvailableInternal(callback), this._client);
    }
}
/**
 * Thenable wrapper for ResourceWithConnectionString that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
class ResourceWithConnectionStringPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    withConnectionProperty(name, value) {
        return new ResourceWithConnectionStringPromiseImpl(this._promise.then(obj => obj.withConnectionProperty(name, value)), this._client);
    }
    getConnectionProperty(key) {
        return this._promise.then(obj => obj.getConnectionProperty(key));
    }
    onConnectionStringAvailable(callback) {
        return new ResourceWithConnectionStringPromiseImpl(this._promise.then(obj => obj.onConnectionStringAvailable(callback)), this._client);
    }
}
// ============================================================================
// ResourceWithContainerFilesImpl
// ============================================================================
class ResourceWithContainerFilesImpl extends ResourceBuilderBase {
    constructor(handle, client) {
        super(handle, client);
    }
    /** @internal */
    async _withContainerFilesSourceInternal(sourcePath) {
        const rpcArgs = { builder: this._handle, sourcePath };
        const result = await this._client.invokeCapability('Aspire.Hosting/withContainerFilesSource', rpcArgs);
        return new ResourceWithContainerFilesImpl(result, this._client);
    }
    /**
     * Adds a container files source annotation to the resource being built, specifying the path to the container files source.
     * @param sourcePath The path to the container files source to associate with the resource. Cannot be null.
     * @returns The resource builder instance with the container files source annotation applied.
     */
    withContainerFilesSource(sourcePath) {
        return new ResourceWithContainerFilesPromiseImpl(this._withContainerFilesSourceInternal(sourcePath), this._client);
    }
    /** @internal */
    async _clearContainerFilesSourcesInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/clearContainerFilesSources', rpcArgs);
        return new ResourceWithContainerFilesImpl(result, this._client);
    }
    /**
     * Removes any container files source annotation from the resource being built.
     * @returns The resource builder instance with the container files source annotation applied.
     */
    clearContainerFilesSources() {
        return new ResourceWithContainerFilesPromiseImpl(this._clearContainerFilesSourcesInternal(), this._client);
    }
}
/**
 * Thenable wrapper for ResourceWithContainerFiles that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
class ResourceWithContainerFilesPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    withContainerFilesSource(sourcePath) {
        return new ResourceWithContainerFilesPromiseImpl(this._promise.then(obj => obj.withContainerFilesSource(sourcePath)), this._client);
    }
    clearContainerFilesSources() {
        return new ResourceWithContainerFilesPromiseImpl(this._promise.then(obj => obj.clearContainerFilesSources()), this._client);
    }
}
// ============================================================================
// ResourceWithEndpointsImpl
// ============================================================================
/** Represents a resource that has endpoints associated with it. */
class ResourceWithEndpointsImpl extends ResourceBuilderBase {
    constructor(handle, client) {
        super(handle, client);
    }
    /** @internal */
    async _withMcpServerInternal(path, endpointName) {
        const rpcArgs = { builder: this._handle };
        if (path !== undefined)
            rpcArgs.path = path;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withMcpServer', rpcArgs);
        return new ResourceWithEndpointsImpl(result, this._client);
    }
    /**
     * Marks the resource as hosting a Model Context Protocol (MCP) server on the specified endpoint.
     *
     * This method adds an `McpServerEndpointAnnotation` to the resource, enabling the Aspire tooling
     * to discover and proxy the MCP server exposed by the resource.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withMcpServer(options) {
        const path = options?.path;
        const endpointName = options?.endpointName;
        return new ResourceWithEndpointsPromiseImpl(this._withMcpServerInternal(path, endpointName), this._client);
    }
    /** @internal */
    async _withEndpointCallbackInternal(endpointName, callback, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, endpointName, callback: callbackId };
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpointCallback', rpcArgs);
        return new ResourceWithEndpointsImpl(result, this._client);
    }
    /**
     * Updates a named endpoint via callback
     * @param options Additional options.
     */
    withEndpointCallback(endpointName, callback, options) {
        const createIfNotExists = options?.createIfNotExists;
        return new ResourceWithEndpointsPromiseImpl(this._withEndpointCallbackInternal(endpointName, callback, createIfNotExists), this._client);
    }
    /** @internal */
    async _withHttpEndpointCallbackInternal(callback, name, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        if (name !== undefined)
            rpcArgs.name = name;
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpEndpointCallback', rpcArgs);
        return new ResourceWithEndpointsImpl(result, this._client);
    }
    /**
     * Updates an HTTP endpoint via callback
     * @param options Additional options.
     */
    withHttpEndpointCallback(callback, options) {
        const name = options?.name;
        const createIfNotExists = options?.createIfNotExists;
        return new ResourceWithEndpointsPromiseImpl(this._withHttpEndpointCallbackInternal(callback, name, createIfNotExists), this._client);
    }
    /** @internal */
    async _withHttpsEndpointCallbackInternal(callback, name, createIfNotExists) {
        const callbackId = registerCallback(async (objData) => {
            const objHandle = wrapIfHandle(objData);
            const obj = new EndpointUpdateContextImpl(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        if (name !== undefined)
            rpcArgs.name = name;
        if (createIfNotExists !== undefined)
            rpcArgs.createIfNotExists = createIfNotExists;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpsEndpointCallback', rpcArgs);
        return new ResourceWithEndpointsImpl(result, this._client);
    }
    /**
     * Updates an HTTPS endpoint via callback
     * @param options Additional options.
     */
    withHttpsEndpointCallback(callback, options) {
        const name = options?.name;
        const createIfNotExists = options?.createIfNotExists;
        return new ResourceWithEndpointsPromiseImpl(this._withHttpsEndpointCallbackInternal(callback, name, createIfNotExists), this._client);
    }
    /** @internal */
    async _withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (scheme !== undefined)
            rpcArgs.scheme = scheme;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        if (isExternal !== undefined)
            rpcArgs.isExternal = isExternal;
        if (protocol !== undefined)
            rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpoint', rpcArgs);
        return new ResourceWithEndpointsImpl(result, this._client);
    }
    /**
     * Adds a network endpoint
     * @param options Additional options.
     * @returns The resource builder.
     */
    withEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const scheme = options?.scheme;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        const isExternal = options?.isExternal;
        const protocol = options?.protocol;
        return new ResourceWithEndpointsPromiseImpl(this._withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol), this._client);
    }
    /** @internal */
    async _withEndpointProxySupportInternal(proxyEnabled) {
        const rpcArgs = { builder: this._handle, proxyEnabled };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEndpointProxySupport', rpcArgs);
        return new ResourceWithEndpointsImpl(result, this._client);
    }
    /**
     * Set whether a resource can use proxied endpoints or whether they should be disabled for all endpoints belonging to the resource. If set to `false`, endpoints belonging to the resource will ignore the configured proxy settings and run proxy-less.
     *
     * This method is intended to support scenarios with persistent lifetime resources where it is desirable for the resource to be accessible over the same
     * port whether the Aspire application is running or not. Proxied endpoints bind ports that are only accessible while the Aspire application is running.
     * The user needs to be careful to ensure that endpoints are using unique ports when disabling proxy support as by default for proxy-less
     * endpoints, Aspire will allocate the target port as the host port, which will increase the chance of port conflicts.
     * @param proxyEnabled Should endpoints for the resource support using a proxy?
     * @returns The resource builder.
     */
    withEndpointProxySupport(proxyEnabled) {
        return new ResourceWithEndpointsPromiseImpl(this._withEndpointProxySupportInternal(proxyEnabled), this._client);
    }
    /** @internal */
    async _withHttpEndpointInternal(port, targetPort, name, env, isProxied) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpEndpoint', rpcArgs);
        return new ResourceWithEndpointsImpl(result, this._client);
    }
    /**
     * Adds an HTTP endpoint
     *
     * If an endpoint with the same name already exists on the resource, the existing endpoint is updated
     * with any non-null parameter values. Parameters left as `null` will not modify the existing endpoint's values.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new ResourceWithEndpointsPromiseImpl(this._withHttpEndpointInternal(port, targetPort, name, env, isProxied), this._client);
    }
    /** @internal */
    async _withHttpsEndpointInternal(port, targetPort, name, env, isProxied) {
        const rpcArgs = { builder: this._handle };
        if (port !== undefined)
            rpcArgs.port = port;
        if (targetPort !== undefined)
            rpcArgs.targetPort = targetPort;
        if (name !== undefined)
            rpcArgs.name = name;
        if (env !== undefined)
            rpcArgs.env = env;
        if (isProxied !== undefined)
            rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpsEndpoint', rpcArgs);
        return new ResourceWithEndpointsImpl(result, this._client);
    }
    /**
     * Adds an HTTPS endpoint
     *
     * If an endpoint with the same name already exists on the resource, the existing endpoint is updated
     * with any non-null parameter values. Parameters left as `null` will not modify the existing endpoint's values.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpsEndpoint(options) {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new ResourceWithEndpointsPromiseImpl(this._withHttpsEndpointInternal(port, targetPort, name, env, isProxied), this._client);
    }
    /** @internal */
    async _withExternalHttpEndpointsInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withExternalHttpEndpoints', rpcArgs);
        return new ResourceWithEndpointsImpl(result, this._client);
    }
    /**
     * Marks existing http or https endpoints on a resource as external.
     * @returns The resource builder.
     */
    withExternalHttpEndpoints() {
        return new ResourceWithEndpointsPromiseImpl(this._withExternalHttpEndpointsInternal(), this._client);
    }
    /**
     * Gets an endpoint reference
     * @param name The name of the endpoint.
     * @returns An `EndpointReference` that can be used to resolve the address of the endpoint after resource allocation has occurred.
     */
    getEndpoint(name) {
        const promise = (async () => {
            const rpcArgs = { builder: this._handle, name };
            const handle = await this._client.invokeCapability('Aspire.Hosting/getEndpoint', rpcArgs);
            return new EndpointReferenceImpl(handle, this._client);
        })();
        return new EndpointReferencePromiseImpl(promise, this._client);
    }
    /** @internal */
    async _asHttp2ServiceInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/asHttp2Service', rpcArgs);
        return new ResourceWithEndpointsImpl(result, this._client);
    }
    /**
     * Configures a resource to mark all endpoints' transport as HTTP/2. This is useful for HTTP/2 services that need prior knowledge.
     * @returns The resource builder.
     */
    asHttp2Service() {
        return new ResourceWithEndpointsPromiseImpl(this._asHttp2ServiceInternal(), this._client);
    }
    /** @internal */
    async _withHttpHealthCheckInternal(path, statusCode, endpointName) {
        const rpcArgs = { builder: this._handle };
        if (path !== undefined)
            rpcArgs.path = path;
        if (statusCode !== undefined)
            rpcArgs.statusCode = statusCode;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpHealthCheck', rpcArgs);
        return new ResourceWithEndpointsImpl(result, this._client);
    }
    /**
     * Adds a health check to the resource which is mapped to a specific endpoint.
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpHealthCheck(options) {
        const path = options?.path;
        const statusCode = options?.statusCode;
        const endpointName = options?.endpointName;
        return new ResourceWithEndpointsPromiseImpl(this._withHttpHealthCheckInternal(path, statusCode, endpointName), this._client);
    }
    /** @internal */
    async _withHttpCommandInternal(path, displayName, options) {
        const rpcArgs = { builder: this._handle, path, displayName };
        if (options !== undefined)
            rpcArgs.options = options;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpCommand', rpcArgs);
        return new ResourceWithEndpointsImpl(result, this._client);
    }
    /**
     * Adds an HTTP resource command
     * @param options Additional options.
     */
    withHttpCommand(path, displayName, options) {
        return new ResourceWithEndpointsPromiseImpl(this._withHttpCommandInternal(path, displayName, options), this._client);
    }
    /** @internal */
    async _withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName) {
        const rpcArgs = { builder: this._handle, probeType };
        if (path !== undefined)
            rpcArgs.path = path;
        if (initialDelaySeconds !== undefined)
            rpcArgs.initialDelaySeconds = initialDelaySeconds;
        if (periodSeconds !== undefined)
            rpcArgs.periodSeconds = periodSeconds;
        if (timeoutSeconds !== undefined)
            rpcArgs.timeoutSeconds = timeoutSeconds;
        if (failureThreshold !== undefined)
            rpcArgs.failureThreshold = failureThreshold;
        if (successThreshold !== undefined)
            rpcArgs.successThreshold = successThreshold;
        if (endpointName !== undefined)
            rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability('Aspire.Hosting/withHttpProbe', rpcArgs);
        return new ResourceWithEndpointsImpl(result, this._client);
    }
    /**
     * Adds an HTTP health probe to the resource
     * @param options Additional options.
     */
    withHttpProbe(probeType, options) {
        const path = options?.path;
        const initialDelaySeconds = options?.initialDelaySeconds;
        const periodSeconds = options?.periodSeconds;
        const timeoutSeconds = options?.timeoutSeconds;
        const failureThreshold = options?.failureThreshold;
        const successThreshold = options?.successThreshold;
        const endpointName = options?.endpointName;
        return new ResourceWithEndpointsPromiseImpl(this._withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName), this._client);
    }
    /** @internal */
    async _onResourceEndpointsAllocatedInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new ResourceEndpointsAllocatedEventImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/onResourceEndpointsAllocated', rpcArgs);
        return new ResourceWithEndpointsImpl(result, this._client);
    }
    /**
     * Subscribes to the ResourceEndpointsAllocated event.
     * @param callback The callback to invoke when the event fires.
     * @returns The resource builder.
     */
    onResourceEndpointsAllocated(callback) {
        return new ResourceWithEndpointsPromiseImpl(this._onResourceEndpointsAllocatedInternal(callback), this._client);
    }
}
/**
 * Thenable wrapper for ResourceWithEndpoints that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
class ResourceWithEndpointsPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    withMcpServer(options) {
        return new ResourceWithEndpointsPromiseImpl(this._promise.then(obj => obj.withMcpServer(options)), this._client);
    }
    withEndpointCallback(endpointName, callback, options) {
        return new ResourceWithEndpointsPromiseImpl(this._promise.then(obj => obj.withEndpointCallback(endpointName, callback, options)), this._client);
    }
    withHttpEndpointCallback(callback, options) {
        return new ResourceWithEndpointsPromiseImpl(this._promise.then(obj => obj.withHttpEndpointCallback(callback, options)), this._client);
    }
    withHttpsEndpointCallback(callback, options) {
        return new ResourceWithEndpointsPromiseImpl(this._promise.then(obj => obj.withHttpsEndpointCallback(callback, options)), this._client);
    }
    withEndpoint(options) {
        return new ResourceWithEndpointsPromiseImpl(this._promise.then(obj => obj.withEndpoint(options)), this._client);
    }
    withEndpointProxySupport(proxyEnabled) {
        return new ResourceWithEndpointsPromiseImpl(this._promise.then(obj => obj.withEndpointProxySupport(proxyEnabled)), this._client);
    }
    withHttpEndpoint(options) {
        return new ResourceWithEndpointsPromiseImpl(this._promise.then(obj => obj.withHttpEndpoint(options)), this._client);
    }
    withHttpsEndpoint(options) {
        return new ResourceWithEndpointsPromiseImpl(this._promise.then(obj => obj.withHttpsEndpoint(options)), this._client);
    }
    withExternalHttpEndpoints() {
        return new ResourceWithEndpointsPromiseImpl(this._promise.then(obj => obj.withExternalHttpEndpoints()), this._client);
    }
    getEndpoint(name) {
        return new EndpointReferencePromiseImpl(this._promise.then(obj => obj.getEndpoint(name)), this._client);
    }
    asHttp2Service() {
        return new ResourceWithEndpointsPromiseImpl(this._promise.then(obj => obj.asHttp2Service()), this._client);
    }
    withHttpHealthCheck(options) {
        return new ResourceWithEndpointsPromiseImpl(this._promise.then(obj => obj.withHttpHealthCheck(options)), this._client);
    }
    withHttpCommand(path, displayName, options) {
        return new ResourceWithEndpointsPromiseImpl(this._promise.then(obj => obj.withHttpCommand(path, displayName, options)), this._client);
    }
    withHttpProbe(probeType, options) {
        return new ResourceWithEndpointsPromiseImpl(this._promise.then(obj => obj.withHttpProbe(probeType, options)), this._client);
    }
    onResourceEndpointsAllocated(callback) {
        return new ResourceWithEndpointsPromiseImpl(this._promise.then(obj => obj.onResourceEndpointsAllocated(callback)), this._client);
    }
}
// ============================================================================
// ResourceWithEnvironmentImpl
// ============================================================================
/** Represents a resource that is associated with an environment. */
class ResourceWithEnvironmentImpl extends ResourceBuilderBase {
    constructor(handle, client) {
        super(handle, client);
    }
    /** @internal */
    async _withOtlpExporterInternal(protocol) {
        const rpcArgs = { builder: this._handle };
        if (protocol !== undefined)
            rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability('Aspire.Hosting/withOtlpExporter', rpcArgs);
        return new ResourceWithEnvironmentImpl(result, this._client);
    }
    /**
     * Configures OTLP telemetry export
     * @param options Additional options.
     */
    withOtlpExporter(options) {
        const protocol = options?.protocol;
        return new ResourceWithEnvironmentPromiseImpl(this._withOtlpExporterInternal(protocol), this._client);
    }
    /** @internal */
    async _withEnvironmentInternal(name, value) {
        value = isPromiseLike(value) ? await value : value;
        const rpcArgs = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEnvironment', rpcArgs);
        return new ResourceWithEnvironmentImpl(result, this._client);
    }
    /** Sets an environment variable */
    withEnvironment(name, value) {
        return new ResourceWithEnvironmentPromiseImpl(this._withEnvironmentInternal(name, value), this._client);
    }
    /** @internal */
    async _withEnvironmentCallbackInternal(callback) {
        const callbackId = registerCallback(async (argData) => {
            const argHandle = wrapIfHandle(argData);
            const arg = new EnvironmentCallbackContextImpl(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability('Aspire.Hosting/withEnvironmentCallback', rpcArgs);
        return new ResourceWithEnvironmentImpl(result, this._client);
    }
    /**
     * Allows for the population of environment variables on a resource.
     * @param callback A callback that allows for deferred execution for computing many environment variables. This runs after resources have been allocated by the orchestrator and allows access to other resources to resolve computed data, e.g. connection strings, ports.
     * @returns The resource builder.
     */
    withEnvironmentCallback(callback) {
        return new ResourceWithEnvironmentPromiseImpl(this._withEnvironmentCallbackInternal(callback), this._client);
    }
    /** @internal */
    async _withReferenceEnvironmentInternal(options) {
        const rpcArgs = { builder: this._handle, options };
        const result = await this._client.invokeCapability('Aspire.Hosting/withReferenceEnvironment', rpcArgs);
        return new ResourceWithEnvironmentImpl(result, this._client);
    }
    /**
     * Configures how information is injected into environment variables when the resource references other resources.
     * @param options Options controlling which reference information is emitted.
     * @returns The resource builder.
     */
    withReferenceEnvironment(options) {
        return new ResourceWithEnvironmentPromiseImpl(this._withReferenceEnvironmentInternal(options), this._client);
    }
    /** @internal */
    async _withReferenceInternal(source, connectionName, optional, name) {
        source = isPromiseLike(source) ? await source : source;
        const rpcArgs = { builder: this._handle, source };
        if (connectionName !== undefined)
            rpcArgs.connectionName = connectionName;
        if (optional !== undefined)
            rpcArgs.optional = optional;
        if (name !== undefined)
            rpcArgs.name = name;
        const result = await this._client.invokeCapability('Aspire.Hosting/withReference', rpcArgs);
        return new ResourceWithEnvironmentImpl(result, this._client);
    }
    /**
     * Adds a reference to another resource
     * @param options Additional options.
     */
    withReference(source, options) {
        const connectionName = options?.connectionName;
        const optional = options?.optional;
        const name = options?.name;
        return new ResourceWithEnvironmentPromiseImpl(this._withReferenceInternal(source, connectionName, optional, name), this._client);
    }
    /** @internal */
    async _withDeveloperCertificateTrustInternal(trust) {
        const rpcArgs = { builder: this._handle, trust };
        const result = await this._client.invokeCapability('Aspire.Hosting/withDeveloperCertificateTrust', rpcArgs);
        return new ResourceWithEnvironmentImpl(result, this._client);
    }
    /**
     * Indicates whether developer certificates should be treated as trusted certificate authorities for the resource at run time. Currently this indicates trust for the ASP.NET Core developer certificate. The developer certificate will only be trusted when running in local development scenarios; in publish mode resources will use their default certificate trust.
     *
     * Disable trust for app host managed developer certificate(s) for a container resource.
     * ```
     * var container = builder.AddContainer("my-service", "my-service:latest")
     * .WithDeveloperCertificateTrust(false);
     * ```
     * Disable automatic trust for app host managed developer certificate(s), but explicitly enable it for a specific resource.
     * ```
     * var builder = DistributedApplication.CreateBuilder(new DistributedApplicationOptions()
     * {
     * Args = args,
     * TrustDeveloperCertificate = false,
     * });
     * var project = builder.AddProject<MyService>("my-service")
     * .WithDeveloperCertificateTrust(true);
     * ```
     * @param trust Indicates whether the developer certificate should be treated as trusted.
     * @returns The resource builder.
     */
    withDeveloperCertificateTrust(trust) {
        return new ResourceWithEnvironmentPromiseImpl(this._withDeveloperCertificateTrustInternal(trust), this._client);
    }
    /** @internal */
    async _withCertificateTrustScopeInternal(scope) {
        const rpcArgs = { builder: this._handle, scope };
        const result = await this._client.invokeCapability('Aspire.Hosting/withCertificateTrustScope', rpcArgs);
        return new ResourceWithEnvironmentImpl(result, this._client);
    }
    /**
     * Sets the certificate trust scope
     *
     * The default scope if not overridden is `Append` which means that custom certificate
     * authorities should be appended to the default trusted certificate authorities for the resource. Setting the scope to
     * `Override` indicates the set of certificates in referenced
     * `CertificateAuthorityCollection` (and optionally Aspire developer certificiates) should be used as the
     * exclusive source of trust for a resource.
     * In all cases, this is a best effort implementation as not all resources support full customization of certificate
     * trust.
     * Set the scope for custom certificate authorities to override the default trusted certificate authorities for a container resource.
     * ```
     * var caCollection = builder.AddCertificateAuthorityCollection("my-cas")
     * .WithCertificate(new X509Certificate2("my-ca.pem"));
     * var container = builder.AddContainer("my-service", "my-service:latest")
     * .WithCertificateAuthorityCollection(caCollection)
     * .WithCertificateTrustScope(CertificateTrustScope.Override);
     * ```
     * @param scope The scope to apply to custom certificate authorities associated with the resource.
     * @returns The resource builder.
     */
    withCertificateTrustScope(scope) {
        return new ResourceWithEnvironmentPromiseImpl(this._withCertificateTrustScopeInternal(scope), this._client);
    }
    /** @internal */
    async _withHttpsDeveloperCertificateInternal(password) {
        password = isPromiseLike(password) ? await password : password;
        const rpcArgs = { builder: this._handle };
        if (password !== undefined)
            rpcArgs.password = password;
        const result = await this._client.invokeCapability('Aspire.Hosting/withParameterHttpsDeveloperCertificate', rpcArgs);
        return new ResourceWithEnvironmentImpl(result, this._client);
    }
    /**
     * Indicates that a resource should use the developer certificate key pair for HTTPS endpoints at run time. Currently this indicates use of the ASP.NET Core developer certificate. The developer certificate will only be used when running in local development scenarios; in publish mode resources will use their default certificate configuration.
     *
     * Use the developer certificate for HTTPS/TLS endpoints on a container resource:
     * ```
     * builder.AddContainer("my-service", "my-image")
     * .WithHttpsDeveloperCertificate()
     * ```
     * @param options Additional options.
     * @returns The resource builder.
     */
    withHttpsDeveloperCertificate(options) {
        let password = options?.password;
        return new ResourceWithEnvironmentPromiseImpl(this._withHttpsDeveloperCertificateInternal(password), this._client);
    }
    /** @internal */
    async _withoutHttpsCertificateInternal() {
        const rpcArgs = { builder: this._handle };
        const result = await this._client.invokeCapability('Aspire.Hosting/withoutHttpsCertificate', rpcArgs);
        return new ResourceWithEnvironmentImpl(result, this._client);
    }
    /**
     * Disable HTTPS/TLS server certificate configuration for the resource. No HTTPS/TLS termination configuration will be applied.
     *
     * Disable HTTPS certificate configuration for a Redis resource:
     * ```
     * var redis = builder.AddRedis("cache")
     * .WithoutHttpsCertificate();
     * ```
     * @returns The resource builder.
     */
    withoutHttpsCertificate() {
        return new ResourceWithEnvironmentPromiseImpl(this._withoutHttpsCertificateInternal(), this._client);
    }
}
/**
 * Thenable wrapper for ResourceWithEnvironment that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
class ResourceWithEnvironmentPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    withOtlpExporter(options) {
        return new ResourceWithEnvironmentPromiseImpl(this._promise.then(obj => obj.withOtlpExporter(options)), this._client);
    }
    withEnvironment(name, value) {
        return new ResourceWithEnvironmentPromiseImpl(this._promise.then(obj => obj.withEnvironment(name, value)), this._client);
    }
    withEnvironmentCallback(callback) {
        return new ResourceWithEnvironmentPromiseImpl(this._promise.then(obj => obj.withEnvironmentCallback(callback)), this._client);
    }
    withReferenceEnvironment(options) {
        return new ResourceWithEnvironmentPromiseImpl(this._promise.then(obj => obj.withReferenceEnvironment(options)), this._client);
    }
    withReference(source, options) {
        return new ResourceWithEnvironmentPromiseImpl(this._promise.then(obj => obj.withReference(source, options)), this._client);
    }
    withDeveloperCertificateTrust(trust) {
        return new ResourceWithEnvironmentPromiseImpl(this._promise.then(obj => obj.withDeveloperCertificateTrust(trust)), this._client);
    }
    withCertificateTrustScope(scope) {
        return new ResourceWithEnvironmentPromiseImpl(this._promise.then(obj => obj.withCertificateTrustScope(scope)), this._client);
    }
    withHttpsDeveloperCertificate(options) {
        return new ResourceWithEnvironmentPromiseImpl(this._promise.then(obj => obj.withHttpsDeveloperCertificate(options)), this._client);
    }
    withoutHttpsCertificate() {
        return new ResourceWithEnvironmentPromiseImpl(this._promise.then(obj => obj.withoutHttpsCertificate()), this._client);
    }
}
// ============================================================================
// ResourceWithServiceDiscoveryImpl
// ============================================================================
class ResourceWithServiceDiscoveryImpl extends ResourceBuilderBase {
    constructor(handle, client) {
        super(handle, client);
    }
}
// ============================================================================
// ResourceWithWaitSupportImpl
// ============================================================================
/** Represents a resource that can wait for other resources to be running, health, and/or completed. */
class ResourceWithWaitSupportImpl extends ResourceBuilderBase {
    constructor(handle, client) {
        super(handle, client);
    }
    /** @internal */
    async _waitForInternal(dependency, waitBehavior) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (waitBehavior !== undefined)
            rpcArgs.waitBehavior = waitBehavior;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitFor', rpcArgs);
        return new ResourceWithWaitSupportImpl(result, this._client);
    }
    /**
     * Waits for another resource to be ready
     * @param options Additional options.
     */
    waitFor(dependency, options) {
        const waitBehavior = options?.waitBehavior;
        return new ResourceWithWaitSupportPromiseImpl(this._waitForInternal(dependency, waitBehavior), this._client);
    }
    /** @internal */
    async _waitForStartInternal(dependency, waitBehavior) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (waitBehavior !== undefined)
            rpcArgs.waitBehavior = waitBehavior;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitForStart', rpcArgs);
        return new ResourceWithWaitSupportImpl(result, this._client);
    }
    /**
     * Waits for another resource to start
     * @param options Additional options.
     */
    waitForStart(dependency, options) {
        const waitBehavior = options?.waitBehavior;
        return new ResourceWithWaitSupportPromiseImpl(this._waitForStartInternal(dependency, waitBehavior), this._client);
    }
    /** @internal */
    async _waitForCompletionInternal(dependency, exitCode) {
        dependency = isPromiseLike(dependency) ? await dependency : dependency;
        const rpcArgs = { builder: this._handle, dependency };
        if (exitCode !== undefined)
            rpcArgs.exitCode = exitCode;
        const result = await this._client.invokeCapability('Aspire.Hosting/waitForResourceCompletion', rpcArgs);
        return new ResourceWithWaitSupportImpl(result, this._client);
    }
    /**
     * Waits for the dependency resource to enter the Exited or Finished state before starting the resource.
     * @param dependency The resource builder for the dependency resource.
     * @param options Additional options.
     * @returns The resource builder.
     */
    waitForCompletion(dependency, options) {
        const exitCode = options?.exitCode;
        return new ResourceWithWaitSupportPromiseImpl(this._waitForCompletionInternal(dependency, exitCode), this._client);
    }
}
/**
 * Thenable wrapper for ResourceWithWaitSupport that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
class ResourceWithWaitSupportPromiseImpl {
    _promise;
    _client;
    constructor(_promise, _client, track = true) {
        this._promise = _promise;
        this._client = _client;
        if (track) {
            _client.trackPromise(_promise);
        }
    }
    then(onfulfilled, onrejected) {
        return this._promise.then(onfulfilled, onrejected);
    }
    waitFor(dependency, options) {
        return new ResourceWithWaitSupportPromiseImpl(this._promise.then(obj => obj.waitFor(dependency, options)), this._client);
    }
    waitForStart(dependency, options) {
        return new ResourceWithWaitSupportPromiseImpl(this._promise.then(obj => obj.waitForStart(dependency, options)), this._client);
    }
    waitForCompletion(dependency, options) {
        return new ResourceWithWaitSupportPromiseImpl(this._promise.then(obj => obj.waitForCompletion(dependency, options)), this._client);
    }
}
// ============================================================================
// Connection Helper
// ============================================================================
/**
 * Creates and connects to the Aspire AppHost.
 * Reads connection info from environment variables set by `aspire run`.
 */
export async function connect() {
    const socketPath = process.env.REMOTE_APP_HOST_SOCKET_PATH;
    if (!socketPath) {
        throw new Error('REMOTE_APP_HOST_SOCKET_PATH environment variable not set. ' +
            'Run this application using `aspire run`.');
    }
    const client = new AspireClient(socketPath);
    await client.connect();
    // Exit the process if the server connection is lost
    client.onDisconnect(() => {
        console.error('Connection to AppHost lost. Exiting...');
        process.exit(1);
    });
    return client;
}
/**
 * Creates a new distributed application builder.
 * This is the entry point for building Aspire applications.
 *
 * @param options - Optional configuration options for the builder
 * @returns A DistributedApplicationBuilder instance
 *
 * @example
 * const builder = await createBuilder();
 * await builder.addRedis("cache");
 * await builder.addContainer("api", "mcr.microsoft.com/dotnet/samples:aspnetapp");
 * const app = await builder.build();
 * await app.run();
 */
export async function createBuilder(options) {
    const client = await connect();
    // Apply client-side options before any tracking begins
    if (options?.throwOnPendingRejections === false) {
        client.throwOnPendingRejections = false;
    }
    // Default args, projectDirectory, and appHostFilePath if not provided
    // ASPIRE_APPHOST_FILEPATH is set by the CLI for consistent socket hash computation
    const effectiveOptions = {
        ...options,
        args: options?.args ?? process.argv.slice(2),
        projectDirectory: options?.projectDirectory ?? process.env.ASPIRE_PROJECT_DIRECTORY ?? process.cwd(),
        appHostFilePath: options?.appHostFilePath ?? process.env.ASPIRE_APPHOST_FILEPATH
    };
    // Strip client-only options before sending to the host
    delete effectiveOptions.throwOnPendingRejections;
    const handle = await client.invokeCapability('Aspire.Hosting/createBuilder', { argsOrOptions: effectiveOptions });
    return new DistributedApplicationBuilderImpl(handle, client);
}
// Re-export commonly used types
export { Handle, AppHostUsageError, CancellationToken, CapabilityError, registerCallback } from './transport.js';
export { refExpr, ReferenceExpression } from './base.js';
// ============================================================================
// Global Error Handling
// ============================================================================
/**
 * Set up global error handlers to ensure the process exits properly on errors.
 * Node.js doesn't exit on unhandled rejections by default, so we need to handle them.
 */
process.on('unhandledRejection', (reason) => {
    const error = reason instanceof Error ? reason : new Error(String(reason));
    if (reason instanceof AppHostUsageError) {
        console.error(`\n❌ AppHost Error: ${error.message}`);
    }
    else if (reason instanceof CapabilityError) {
        console.error(`\n❌ Capability Error: ${error.message}`);
        console.error(`   Code: ${reason.code}`);
        if (reason.capability) {
            console.error(`   Capability: ${reason.capability}`);
        }
    }
    else {
        console.error(`\n❌ Unhandled Error: ${error.message}`);
        if (error.stack) {
            console.error(error.stack);
        }
    }
    process.exit(1);
});
process.on('uncaughtException', (error) => {
    if (error instanceof AppHostUsageError) {
        console.error(`\n❌ AppHost Error: ${error.message}`);
    }
    else if (error instanceof CapabilityError) {
        console.error(`\n❌ Capability Error: ${error.message}`);
        console.error(`   Code: ${error.code}`);
        if (error.capability) {
            console.error(`   Capability: ${error.capability}`);
        }
    }
    else {
        console.error(`\n❌ Uncaught Exception: ${error.message}`);
    }
    // Suppress stack traces for structured errors (AppHostUsageError, CapabilityError)
    // to keep polyglot output clean. Use --verbose for full diagnostics.
    if (!(error instanceof AppHostUsageError) && !(error instanceof CapabilityError) && error.stack) {
        console.error(error.stack);
    }
    process.exit(1);
});
// ============================================================================
// Handle Wrapper Registrations
// ============================================================================
// Register wrapper factories for typed handle wrapping in callbacks
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.Publishing.AfterPublishEvent', (handle, client) => new AfterPublishEventImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.AfterResourcesCreatedEvent', (handle, client) => new AfterResourcesCreatedEventImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.Publishing.BeforePublishEvent', (handle, client) => new BeforePublishEventImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.BeforeResourceStartedEvent', (handle, client) => new BeforeResourceStartedEventImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.BeforeStartEvent', (handle, client) => new BeforeStartEventImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.CommandLineArgsCallbackContext', (handle, client) => new CommandLineArgsCallbackContextImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.CommandLineArgsEditor', (handle, client) => new CommandLineArgsEditorImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.ConnectionStringAvailableEvent', (handle, client) => new ConnectionStringAvailableEventImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.ContainerImagePushOptions', (handle, client) => new ContainerImagePushOptionsImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.ContainerImagePushOptionsCallbackContext', (handle, client) => new ContainerImagePushOptionsCallbackContextImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.ContainerImageReference', (handle, client) => new ContainerImageReferenceImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.ContainerMountAnnotation', (handle, client) => new ContainerMountAnnotationImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.ContainerPortReference', (handle, client) => new ContainerPortReferenceImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.DistributedApplication', (handle, client) => new DistributedApplicationImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.DistributedApplicationExecutionContext', (handle, client) => new DistributedApplicationExecutionContextImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.DistributedApplicationModel', (handle, client) => new DistributedApplicationModelImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.Docker.DockerfileBuilder', (handle, client) => new DockerfileBuilderImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.DockerfileBuilderCallbackContext', (handle, client) => new DockerfileBuilderCallbackContextImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.DockerfileFactoryContext', (handle, client) => new DockerfileFactoryContextImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.Docker.DockerfileStage', (handle, client) => new DockerfileStageImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.EndpointReference', (handle, client) => new EndpointReferenceImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.EndpointReferenceExpression', (handle, client) => new EndpointReferenceExpressionImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.EndpointUpdateContext', (handle, client) => new EndpointUpdateContextImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.EnvironmentCallbackContext', (handle, client) => new EnvironmentCallbackContextImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.EnvironmentEditor', (handle, client) => new EnvironmentEditorImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.Ats.EventingSubscriberRegistrationContext', (handle, client) => new EventingSubscriberRegistrationContextImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.ExecuteCommandContext', (handle, client) => new ExecuteCommandContextImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.InitializeResourceEvent', (handle, client) => new InitializeResourceEventImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.InputsDialogValidationContext', (handle, client) => new InputsDialogValidationContextImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.LogFacade', (handle, client) => new LogFacadeImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.Pipelines.PipelineConfigurationContext', (handle, client) => new PipelineConfigurationContextImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.Pipelines.PipelineContext', (handle, client) => new PipelineContextImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.Pipelines.PipelineEditor', (handle, client) => new PipelineEditorImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.Pipelines.PipelineStep', (handle, client) => new PipelineStepImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.Pipelines.PipelineStepContext', (handle, client) => new PipelineStepContextImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.Pipelines.PipelineStepFactoryContext', (handle, client) => new PipelineStepFactoryContextImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.Pipelines.PipelineSummary', (handle, client) => new PipelineSummaryImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ProjectResourceOptions', (handle, client) => new ProjectResourceOptionsImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.ReferenceExpressionBuilder', (handle, client) => new ReferenceExpressionBuilderImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.ResourceCommandService', (handle, client) => new ResourceCommandServiceImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.ResourceEndpointsAllocatedEvent', (handle, client) => new ResourceEndpointsAllocatedEventImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.ResourceLoggerService', (handle, client) => new ResourceLoggerServiceImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.ResourceNotificationService', (handle, client) => new ResourceNotificationServiceImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.ResourceReadyEvent', (handle, client) => new ResourceReadyEventImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.ResourceStoppedEvent', (handle, client) => new ResourceStoppedEventImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.ResourceUrlsCallbackContext', (handle, client) => new ResourceUrlsCallbackContextImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.ResourceUrlsEditor', (handle, client) => new ResourceUrlsEditorImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.UpdateCommandStateContext', (handle, client) => new UpdateCommandStateContextImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.IAspireStore', (handle, client) => new AspireStoreImpl(handle, client));
registerHandleWrapper('Microsoft.Extensions.Configuration.Abstractions/Microsoft.Extensions.Configuration.IConfiguration', (handle, client) => new ConfigurationImpl(handle, client));
registerHandleWrapper('Microsoft.Extensions.Configuration.Abstractions/Microsoft.Extensions.Configuration.IConfigurationSection', (handle, client) => new ConfigurationSectionImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.IDistributedApplicationBuilder', (handle, client) => new DistributedApplicationBuilderImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.Eventing.IDistributedApplicationEventing', (handle, client) => new DistributedApplicationEventingImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.Pipelines.IDistributedApplicationPipeline', (handle, client) => new DistributedApplicationPipelineImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.IExecutionConfigurationBuilder', (handle, client) => new ExecutionConfigurationBuilderImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.IExecutionConfigurationResult', (handle, client) => new ExecutionConfigurationResultImpl(handle, client));
registerHandleWrapper('Microsoft.Extensions.Hosting.Abstractions/Microsoft.Extensions.Hosting.IHostEnvironment', (handle, client) => new HostEnvironmentImpl(handle, client));
registerHandleWrapper('Microsoft.Extensions.Logging.Abstractions/Microsoft.Extensions.Logging.ILogger', (handle, client) => new LoggerImpl(handle, client));
registerHandleWrapper('Microsoft.Extensions.Logging.Abstractions/Microsoft.Extensions.Logging.ILoggerFactory', (handle, client) => new LoggerFactoryImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.Pipelines.IReportingStep', (handle, client) => new ReportingStepImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.Pipelines.IReportingTask', (handle, client) => new ReportingTaskImpl(handle, client));
registerHandleWrapper('System.ComponentModel/System.IServiceProvider', (handle, client) => new ServiceProviderImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.IUserSecretsManager', (handle, client) => new UserSecretsManagerImpl(handle, client));
registerHandleWrapper('Aspire.Hosting.JavaScript/Aspire.Hosting.JavaScript.BunAppResource', (handle, client) => new BunAppResourceImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.ContainerRegistryResource', (handle, client) => new ContainerRegistryResourceImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.ContainerResource', (handle, client) => new ContainerResourceImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.CSharpAppResource', (handle, client) => new CSharpAppResourceImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.DotnetToolResource', (handle, client) => new DotnetToolResourceImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.ExecutableResource', (handle, client) => new ExecutableResourceImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ExternalServiceResource', (handle, client) => new ExternalServiceResourceImpl(handle, client));
registerHandleWrapper('Aspire.Hosting.JavaScript/Aspire.Hosting.JavaScript.JavaScriptAppResource', (handle, client) => new JavaScriptAppResourceImpl(handle, client));
registerHandleWrapper('Aspire.Hosting.JavaScript/Aspire.Hosting.JavaScript.NextJsAppResource', (handle, client) => new NextJsAppResourceImpl(handle, client));
registerHandleWrapper('Aspire.Hosting.JavaScript/Aspire.Hosting.JavaScript.NodeAppResource', (handle, client) => new NodeAppResourceImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.ParameterResource', (handle, client) => new ParameterResourceImpl(handle, client));
registerHandleWrapper('Aspire.Hosting.PostgreSQL/Aspire.Hosting.Postgres.PgAdminContainerResource', (handle, client) => new PgAdminContainerResourceImpl(handle, client));
registerHandleWrapper('Aspire.Hosting.PostgreSQL/Aspire.Hosting.Postgres.PgWebContainerResource', (handle, client) => new PgWebContainerResourceImpl(handle, client));
registerHandleWrapper('Aspire.Hosting.PostgreSQL/Aspire.Hosting.ApplicationModel.PostgresDatabaseResource', (handle, client) => new PostgresDatabaseResourceImpl(handle, client));
registerHandleWrapper('Aspire.Hosting.PostgreSQL/Aspire.Hosting.Postgres.PostgresMcpContainerResource', (handle, client) => new PostgresMcpContainerResourceImpl(handle, client));
registerHandleWrapper('Aspire.Hosting.PostgreSQL/Aspire.Hosting.ApplicationModel.PostgresServerResource', (handle, client) => new PostgresServerResourceImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.ProjectResource', (handle, client) => new ProjectResourceImpl(handle, client));
registerHandleWrapper('Aspire.Hosting.Redis/Aspire.Hosting.Redis.RedisCommanderResource', (handle, client) => new RedisCommanderResourceImpl(handle, client));
registerHandleWrapper('Aspire.Hosting.Redis/Aspire.Hosting.Redis.RedisInsightResource', (handle, client) => new RedisInsightResourceImpl(handle, client));
registerHandleWrapper('Aspire.Hosting.Redis/Aspire.Hosting.ApplicationModel.RedisResource', (handle, client) => new RedisResourceImpl(handle, client));
registerHandleWrapper('Aspire.Hosting.JavaScript/Aspire.Hosting.JavaScript.ViteAppResource', (handle, client) => new ViteAppResourceImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.IComputeEnvironmentResource', (handle, client) => new ComputeEnvironmentResourceImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.IComputeResource', (handle, client) => new ComputeResourceImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.IContainerFilesDestinationResource', (handle, client) => new ContainerFilesDestinationResourceImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.IResource', (handle, client) => new ResourceImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.IResourceWithArgs', (handle, client) => new ResourceWithArgsImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.IResourceWithConnectionString', (handle, client) => new ResourceWithConnectionStringImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.IResourceWithContainerFiles', (handle, client) => new ResourceWithContainerFilesImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.IResourceWithEndpoints', (handle, client) => new ResourceWithEndpointsImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.IResourceWithEnvironment', (handle, client) => new ResourceWithEnvironmentImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.IResourceWithServiceDiscovery', (handle, client) => new ResourceWithServiceDiscoveryImpl(handle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.IResourceWithWaitSupport', (handle, client) => new ResourceWithWaitSupportImpl(handle, client));
